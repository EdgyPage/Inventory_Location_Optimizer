{
  "nodes": {
    "Diagnostics/bucket_fill.py": {
      "decorators": [],
      "doc": "bucket_fill.py \u2014 read-only diagnostic for the queue-blowup / sub-target-fill bug.\n\nLocalises the per-(handling, category, size, unit_type) bucket mismatch behind a deep\n`_stock_queue` coexisting with global fill below target.  Two views:\n\n  SIZING  \u2014 per bucket: built capacity (bins), the `stock_plan` footprint the run's initial\n            stock places, and the DEFAULT-PACK footprint the same levels need with the plan\n            stripped.  Plus default-pack timing, since retiring stock_plan would put that\n            packing back on the hot path.\n\n            THOSE TWO COLUMNS ARE NOW THE SAME STATEMENT, and reading them is how you check\n            it.  The planner used to SPREAD a SKU's units across whichever tiers had the\n            most free bins, so the plan and the default packing genuinely differed and this\n            view measured the gap.  Since \"Field the requirement\" the plan IS the default\n            packing at the declared level \u2014 `field_requirement` records exactly what\n            `bucket_requirements` counted \u2014 so a bucket where the columns disagree is a\n            planner that fielded something other than what it sized from, which is the one\n            failure the whole contract exists to prevent.\n\n  RUNTIME \u2014 runs a short single-process FIFO sim (mirrors strategy_runner's loop) and\n            snapshots per-bucket {occupied, free, queued units} + the global fill / queue\n            trajectory, flagging buckets >95% full (overflow) and <40% full (waste).\n\nBOTH SIDES OF THE SIZING VIEW READ THE RUN'S PLANNED INVENTORY, never the catalogue.\nA stock level is a run's declaration and a generated catalogue carries none (ADR-0002):\n`load_inventory_from_db` on a profile DB returns orders whose level slots are UNSET, and\n`bucket_requirements` over those is a caller bug \u2014 `_equilibrium_qty` raises\n`UndeclaredStock` rather than answering the 1 it used to, which would have sized the\nDEFAULT column at one unit per SKU instead of at what default packing needs.  The\nwarehouse planner's `stock_plan` survives ADR-0002 untouched; only its provenance moved,\nfrom a column the generator authored to the packing the planner writes for the level a\nrun declared.\n\nNothing here mutates production data; it only reads DBs and runs an in-memory sim.\n\nUsage\n-----\n    python Diagnostics/bucket_fill.py [profile_label_or_inv_db] [--max-skus N]\n        [--batches N] [--max-bins N] [--min-bins N]",
      "kind": "module",
      "signature": null
    },
    "Diagnostics/bucket_fill.py::Bucket": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple"
    },
    "Diagnostics/bucket_fill.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Diagnostics/bucket_fill.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Diagnostics/bucket_fill.py::_bin_bucket": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(b) -> Bucket"
    },
    "Diagnostics/bucket_fill.py::_capacity_by_bucket": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(warehouse) -> dict[Bucket, int]"
    },
    "Diagnostics/bucket_fill.py::_diagnose_unplaced": {
      "decorators": [],
      "doc": "For each queued (unplaced) unit, check whether free bins exist in its bucket \u2014\nif so, the placement (not capacity) is the cause.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, warehouse, log) -> None"
    },
    "Diagnostics/bucket_fill.py::_fmt_bucket": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(b: Bucket) -> str"
    },
    "Diagnostics/bucket_fill.py::_occupied_by_bucket": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(warehouse) -> dict[Bucket, int]"
    },
    "Diagnostics/bucket_fill.py::_queued_by_bucket": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr) -> dict[Bucket, int]"
    },
    "Diagnostics/bucket_fill.py::_setup_strategy": {
      "decorators": [],
      "doc": "Mirror strategy_runner's uniform-stock arm: uniform initial stock, arm aisle\nstate, then build() swaps in the strategy's (possibly ranked-wave) placement.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, strat, planned_inv, affinity, wp) -> None"
    },
    "Diagnostics/bucket_fill.py::_unit_bucket": {
      "decorators": [],
      "doc": "Bucket for a StorageUnit **or** a queued PutawayItem.\n\n`_stock_queue` holds PutawayItem (`.unit`, `.source`), so the three call sites that\nwalk the queue used to hand this function a wrapper and get AttributeError.  Accepting\nboth keeps those call sites reading naturally, and a bin-side caller is unaffected.",
      "is_async": false,
      "kind": "function",
      "signature": "(u) -> Bucket"
    },
    "Diagnostics/bucket_fill.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Diagnostics/bucket_fill.py::runtime_view": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(planned_inv, warehouse, affinity, batch_cfg, n_batches: int, strategy: str | None, log) -> None"
    },
    "Diagnostics/bucket_fill.py::sizing_view": {
      "decorators": [],
      "doc": "Per-bucket capacity vs the two footprints the SAME declared levels produce.\n\nThe A/B is the PACKING and nothing else: both sides are the run's planned inventory,\nso every SKU brings the equilibrium quantity this run declared (ADR-0002 \u2014 the\ncatalogue has no level to bring).  The spread side keeps the planner's `stock_plan`\nand the default side has it stripped, which is what makes the two columns comparable\nand the two timings a like-for-like measure of what packing costs.",
      "is_async": false,
      "kind": "function",
      "signature": "(planned_inv_db: str, warehouse, planned_cartons: list, log) -> None"
    },
    "Diagnostics/equilibrium_report.py": {
      "decorators": [],
      "doc": "equilibrium_report.py \u2014 the calibrated era's equilibrium check, per leaf, off a finished run.\n\nReads a run tree through its own contract and prints `Optimization/simconfig/equilibrium.py`'s\nverdict for every arm of every channel run: the five clauses (labour, released_late,\nutilization, supply, rework) over a window of working days, each with its reading, exactly\nas the throughput audit logs them -- without re-running the analysis.  Built for \"Split the\nmissed-share clause into supply and labour\" (department-calibration 30), whose done-when is a\nre-read of two finished runs, and for the re-check that follows it (31).\n\n    python Diagnostics/equilibrium_report.py <run_root>                  # every closed day\n    python Diagnostics/equilibrium_report.py <run_root> --window 20-39   # the measured window\n    python Diagnostics/equilibrium_report.py <run_root> --window campaign  # ...as DECLARED\n    python Diagnostics/equilibrium_report.py <run_root> --arm fifo --json out.json\n\n`<run_root>` is a run directory under the `COMPARISON_OUTPUT_DIR` from `.env` (a path, or a\nbare name resolved against it) -- read the key, never paste a machine-local path.\n\nThe expectations come off the run's OWN staffing record (the run spec at the run root, the\nsame object `run_analysis` stamps onto `sim_result`), keyed by the leaf's pair and channel, and re-centred\nper arm on its stamped `expected_pick` -- the same three calls the audit makes\n(`expectations_for`, `arm_expectations`, `check`), so the two cannot disagree.  A leaf whose\nrecord carries no derived block (a flag-off run) is reported as such and skipped.  A window\noutside the ledger's closed days is refused per arm rather than judged over days that never\nhappened.\n\nExit status: 0 when every judged arm passed, 1 when any failed, 2 when nothing could be\njudged.  The `--json` file carries every verdict's `as_dict()` under `<cell>/<leaf>/<arm>`,\nwhich is what a ticket's answer quotes.",
      "kind": "module",
      "signature": null
    },
    "Diagnostics/equilibrium_report.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.dirname(os.path.abspath(__file__)))"
    },
    "Diagnostics/equilibrium_report.py::_db_path": {
      "decorators": [],
      "doc": "The arm's sim DB through the run's own contract (`leaf_path`), never the `db_path` the\nleaf's meta recorded: that field belongs to the machine that ran it and goes stale when a\nrun is moved (memory `results-drive-location`).",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cr, strategy: dict) -> str"
    },
    "Diagnostics/equilibrium_report.py::_leaf_label": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cell: str, cr) -> str"
    },
    "Diagnostics/equilibrium_report.py::_site_reading": {
      "decorators": [],
      "doc": "The site clause as one line: each department's banded site number, then the\nper-channel SHARES beside it -- never instead of it (memory\n`a-right-site-total-hides-two-wrong-shares`: a right site total hid two bands failing in\nopposite directions).",
      "is_async": false,
      "kind": "function",
      "signature": "(clause) -> str"
    },
    "Diagnostics/equilibrium_report.py::_site_verdicts": {
      "decorators": [],
      "doc": "One SITE verdict per `(cell, pair, arm-pair)`: put and recv banded ONCE.\n\nKeyed by `(cell, pair, RANK)`, never by an arm key, and that is the whole of it: the\ndiagonal is rank against rank (site-dock 06), so the two halves of one pair are named\ndifferently on any run whose channels sweep different rule subsets -- and keying on the\nkey would then put each leaf in its own bucket, leave every bucket one channel short,\nand turn the site clause off for the entire run while reporting it as skipped.  The rank\nis the leaf's position in its OWN ordered `sim_meta['strategies']`, which is the same\nordering `_prepare_site_run` zips the pair out of.\n\nA pair with one channel is not banded: a site clause over half a site is a smaller\ndenominator, not a smaller site, and it would pass or fail for the wrong reason.",
      "is_async": false,
      "kind": "function",
      "signature": "(site_rows: dict, out) -> dict"
    },
    "Diagnostics/equilibrium_report.py::_window_arg": {
      "decorators": [],
      "doc": "`LO-HI` as a pair of working days, or the literal `campaign` for the funnel's own\nmeasured window -- which is DECLARED, in `whatif_config.CAMPAIGN_WINDOW_DAYS`, and read\nhere rather than retyped (\"Re-size the funnel in site days\").  A campaign leaf judged\nagainst a hand-typed window is a utilization reported against a warm-up the constants\nnever saw, and nothing in the output would say so.  The days are SITE days: under the era\none batch is one 28,800 s site day, three times shorter than the calendar day the yard fee\naccrues in.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str | None) -> tuple[int, int] | None"
    },
    "Diagnostics/equilibrium_report.py::bucket_table": {
      "decorators": [],
      "doc": "The rework clause's per-bucket depth as text rows: one line per bucket of the leaf's\nsection -- setup free (the record), the window's first and last readings, its minimum\nand mean, the drawdown, and the batches that read dry.  A header line first; one line\nsaying so when the run's vintage never recorded the depth per bucket.",
      "is_async": false,
      "kind": "function",
      "signature": "(verdict) -> list[str]"
    },
    "Diagnostics/equilibrium_report.py::bucket_table._n": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(x, fmt=',d')"
    },
    "Diagnostics/equilibrium_report.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Diagnostics/equilibrium_report.py::report": {
      "decorators": [],
      "doc": "Every arm's verdict, `{leaf_label/arm_key: Verdict.as_dict()}`; prints one line each,\nfollowed (unless `buckets=False`) by the rework clause's per-bucket depth table.",
      "is_async": false,
      "kind": "function",
      "signature": "(root: str, *, window: tuple[int, int] | None=None, arm: str | None=None, buckets: bool=True, out=print) -> dict"
    },
    "Diagnostics/receiving_report.py": {
      "decorators": [],
      "doc": "receiving_report.py \u2014 the receiving stream, reconciled against the run that wrote it.\n\n# \u2500\u2500 why this exists at all \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`work_events` has no consumer. Outside `Tests/`, the only modules that mention it are the\nconfig, the metrics writer, the persistence layer and the runner \u2014 nothing in\n`Visualization/` or `Performance_Evaluations/` reads a single row. So a third work stream can\nbe wired end to end, write duplicated or malformed rows, and produce a run that looks\ncompletely healthy from every existing angle.\n\nThat makes the receiving crew unfalsifiable unless something reads it back. This is that\nsomething, and it is deliberately a RECONCILIATION rather than a report: it compares two\nsurfaces that were written by different code from different state, so agreement is evidence\nand disagreement names which half is wrong.\n\n  `work_events`   one row per unload, written by `metrics.work_events.recv_rows` from the\n                  dock's own drained records, stamped against the crew's absolute carry.\n  `batch_stats`   four scalars per batch, written by `persistence.Picking_Data` from\n                  `Inventory_Manager.receiving_snapshot()`, which reads the dock's counters.\n\nNothing forces those to agree. They share no code below the manager, and each is a plausible\nnumber on its own.\n\n# \u2500\u2500 the six checks, and the specific defect each one catches \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n1. **Seconds agree.** `SUM(duration) WHERE role='receive'` against `SUM(recv_seconds)`.\n   Catches a dropped `we.extend`, a `bs.recv_seconds` assignment that never ran (the skipped\n   branch), and a checkpoint buffer that was not cleared \u2014 `INSERT OR REPLACE` on a composite\n   key makes re-inserted rows look correct, but it doubles this sum. The tolerance SCALES\n   with the sum (`_tol_for`), because a flat one failed four archived arms on float\n   re-association; read that function before changing either constant.\n\n2. **Counts agree, EXACTLY.** `COUNT(*) WHERE role='receive'` against `SUM(recv_unloaded)`.\n   Both are carried integers rather than recomputed floats, so `==` is the stronger statement\n   and a tolerance here would be hiding something.\n\n3. **The uid blocks are disjoint.** `actor_uid` is the only thing that says\n   WHO did a unit of work, and nothing enforces it: `Worker` validates only non-negativity,\n   the DDL has no uniqueness constraint, `put_rows` bounds-checks `0 <= widx < len(workers)`\n   which a collision passes, and the merged view still sorts. Nothing else in the repo can\n   see a collision \u2014 it surfaces only as a per-actor rollup quietly merging two people, and\n   it is quietest when the receiving crew is small, which is the likely configuration.\n   Contiguity is deliberately NOT checked -- an allocated-but-idle crew leaves the same gap\n   as a misallocated one, so the check failed healthy runs and passed the defect it was for.\n\n4. **No duplicate merge keys.** `(t_abs, batch_id, role, mode, actor_uid, seq)` is the\n   declared total order. The existing guard is `assert rows == sorted(rows)`, which any\n   non-decreasing sequence satisfies \u2014 including one full of identical keys. A genuinely\n   ambiguous merge passes the test that exists to prove the order is total.\n\n5. **`role` and `event_type` agree.** `put_rows` takes `role` from the worker and used to\n   hard-code the event type, so a receive row could carry `role='receive', event_type='put'`.\n   Then `SUM(duration) WHERE role='put'` and the same query on `event_type` disagree, and\n   there is nothing to point at. Every put and receive row must carry an event_type equal\n   to its role, and no other row may claim one -- which also catches a put row typed 'pick'.\n   Two exemptions, and both are declared rather than defensive. Pick rows are exempt because\n   picking has its own vocabulary (task_start / pick / done / cut) and is the one stream\n   where the two legitimately differ. `repack` is exempt because it is the one `role =\n   'receive'` event type that is DECLARED to differ: `metrics.work_events.repack_rows` says\n   outright that a repack is receiving work by the receiving crew at the dock's own per-pack\n   price, so `role` is `'receive'` and only the event type moves -- which is what lets \"what\n   did receiving cost\" sum `role='receive'` and get unloads AND rework while \"how much rework\n   was there\" filters `event_type='repack'`. `repack` joins 'put' and 'receive' as a word no\n   foreign row may claim, so the exemption widens the vocabulary rather than weakening it.\n\n6. **The unload price is ONE constant.** Every `role='receive'` row satisfies\n\n       duration - qty * sku_scores.handle_var(sku)  ==  C\n\n   for a single C per run, because `unload_cost` IS `per_item + intercept + qty * v_s`\n   (`Inbound/unload.py`, `Warehouse/kernel/cost_model.per_pick`) and `v_s` is already in this\n   very DB: `UnloadCost.from_putaway` takes its weight/volume coefficients from\n   `PutawayCost.from_pick`, which carries the PICK coefficients through unchanged, and\n   `Order.compute_labor_cost` computes the stored `handle_var` from the same function on the\n   same inputs. So the whole chain picking -> put-away -> receiving is checkable from one\n   file: no catalogue, no config record, no fit.\n\n   What it catches is a coefficient entering receiving by the side door -- a dock built from\n   its class defaults instead of the run's pick config (the 55x drift `UnloadCost`'s docstring\n   was written against), a weight/volume transform that diverged, an intercept scale applied\n   twice. Every one of those moves the residual per ROW while leaving the two surfaces checks\n   1 and 2 compare in perfect agreement with each other.\n\n   Repack rows are INCLUDED, not filtered: `_charge_repack` prices a rescue with\n   `dock.unload_seconds` -- the same `unload_cost` -- so a repack satisfies the same C, and\n   including them makes this the only thing in the repo that verifies that docstring's central\n   claim, *\"no new coefficient enters the model\"*. It is also why check 5's exemption lands\n   first: a check that FAILed every repack row would mask this one.\n\n   C is fixed by SPREAD (`max(c) - min(c) <= _TOL`) rather than against a reference, because\n   there is no second place the intercept is written down; the recompute re-associates the\n   float, so it is not bit-exact (relative error ~1e-14 on a ~50 s duration). An EMPTY\n   `sku_scores` makes the check inactive -- the same idiom a run with no receiving crew gets,\n   and not a silent PASS on a vintage that cannot answer. A receive row whose SKU is absent\n   from a NON-empty `sku_scores` is a separate failure (`every_received_sku_is_scored`), kept\n   separate because it is the one clause here that can go red for a reason outside receiving.\n\n   ON THE ARCHIVE, this check partitions the runs by ERA, and the partition is sharp rather\n   than marginal. Every run from 2026-09-06 on passes at a spread of 1e-15 .. 1e-13 seconds;\n   every run before the per-item charge break (`fc7a46a5`, 2026-09-05) fails by ~4,600 s,\n   because that era's dock was built from `UnloadCost`'s CLASS DEFAULTS rather than from the\n   run's pick config -- measured on a 2026-09-01 arm: 1.03 s charged to unload a unit whose\n   `handle_var` alone is 1.79 s. That is not a false alarm and it is not a vintage the check\n   should be quiet about: it is the \"second set of magic numbers\" `UnloadCost`'s docstring\n   was written against, realized, and it means pre-break receiving labour is not denominated\n   in the same model as the rest of the run. A red verdict there is the tool working. What\n   would have made the check unsound is an inability to tell the two apart, which is the sin\n   the missing contiguity check below is a monument to -- and there is no ambiguity here:\n   1e-15 against 4.6e+03.\n\n   The cross-leaf half of this check needs two leaves and lives in `reconcile_pair` below.\n   `C_store == C_ful` -- which site-dock 15 called the site dock's sharpest falsifier and an\n   earlier draft of this docstring promised -- is RETIRED and must not be written: the unload\n   price is a statement about the MERCHANDISE, so the site dock holds a price LIST keyed by\n   the unloaded unit's own regime (site-dock 27) and the equality is false BY CONSTRUCTION.\n   What replaces it is the TWO-CONSTANT form, and it is strictly stronger; see\n   `reconcile_pair`.\n\n# \u2500\u2500 the pair-scope checks, and why they are a sibling rather than five more arguments \u2500\u2500\n\n`reconcile_pair` takes a COUPLED unit's two channel leaves and its site DB. Widening\n`reconcile` with an optional peer was rejected (site-dock 15 section 5): it puts a scope\ndiscriminator inside a function whose contract is \"one DB, six checks\", and every per-leaf\ncheck would grow an `if peer is not None` branch that no run in the archive ever takes.\n\nOn an UNCOUPLED run there is no pair, so there is no verdict \u2014 `main` reports\n`0 coupled pair(s)` rather than printing something green about a run that has no site. Every\npublished run is inbound-off and uncoupled, so that is the common case rather than the\ncorner. An ABSENT SITE DB on a coupled pair is a FAIL and not a MISSING: `MISSING` means\n\"this arm was not run\", and a pair that produced two leaf DBs and no site DB is a run that\nLOST its site scope.\n\n  A. **No uid carries a site role in one leaf and a per-channel role in the other.** A uid\n     is not an identity; `(role, uid)` is. Site-dock 04 preserves `actor_uid == picker_id`,\n     so store picker 3 and fulfillment picker 3 are DIFFERENT people, while the put pool's\n     block starts at `max(k_pickers)` over both channels precisely so a putter's uid means\n     the SAME person in both DBs. The defect this catches is a leaf that builds its own site\n     crew off its OWN pick cursor: fulfillment's putter lands at `k_ful`, inside store's\n     picker block, and every per-actor rollup silently merges a putter with a picker.\n     Nothing else in the repo can see it.\n\n  B. **Every site-role uid lies at or above both leaves' pick blocks.** A FLOOR over\n     `max(max pick uid) + 1` across the pair, and never set equality: an idle putter writes\n     no rows in one leaf, and the smaller leaf is left a deliberate uid GAP (site-dock 19),\n     so a check that assumed a dense range would be wrong by construction. That is the same\n     trap the retired contiguity check below is a monument to.\n\n  C. **The site total closes against the two leaves' rows, PER BATCH.** The sum over both\n     leaves of `work_events` receive seconds equals the site dock's own\n     `site_receiving.recv_seconds`. It is a real check rather than a second tautology because\n     the two accumulators run on DIFFERENT CODE: the dock's total accrues at the charge site\n     (`Dock.charge`), the leaves' rows carry per-row durations stamped from drained records.\n\n     WHAT IT UNIQUELY COVERS IS NARROWER THAN THAT MAKES IT SOUND, and the narrowing is the\n     honest claim. `SiteReceiving._partition` already ASSERTS that the leaves' own\n     `receiving_seconds` sum to the dock's, and RAISES \u2014 so a routing or decomposition defect\n     kills the run before a row is written and never reaches this check. What is left for\n     this one is everything that goes wrong AFTER the partition: a row that never reached the\n     DB, a flush that never fired, a checkpoint buffer that was not cleared. That is the one\n     class no per-leaf check can see either \u2014 a final flush that never fired takes BOTH of a\n     leaf's surfaces with it, so check 1 passes over exactly that defect while this one\n     fails.\n\n     Per batch, never a run total: a run total says the site lost 400 seconds and nothing\n     about where, and the drain IS a batch boundary. The counts close too, and they filter\n     `event_type='receive'`: a REPACK is receiving work priced at the dock and charged into\n     `recv_seconds`, but no `unloaded` counter counts it, so the seconds closure includes\n     repacks and the count closure cannot.\n\n  D. **The two constants are per regime** \u2014 check 6's cross-leaf half, in the two-constant\n     form site-dock 27 chose over the retired equality. Each leaf is regime-PURE (the\n     coordinator partitions the dock's records by SKU before the driver stamps anything, so\n     one DB never holds both regimes' rows), so `C_store` is measured over the store leaf's\n     rows and `C_ful` over the fulfillment leaf's, each against its own regime's entry in the\n     dock's price list. Three clauses, because the defect has three shapes and only the\n     per-leaf spread is visible from one DB:\n\n       * each leaf's own residuals collapse to ONE constant (check 6, restated at pair scope\n         so the pair verdict does not depend on a reader having run the per-leaf one);\n       * NO ROW of either leaf prices at the OTHER regime's constant \u2014 the exact second\n         equality, which NAMES a contaminated row rather than only reporting a spread;\n       * and if the two leaves price their PICKING differently, they must price their\n         UNLOADING differently too. `sku_scores.labor_cost - handle_var` is\n         `pick_intercept + pick_per_item`, one constant per run, recoverable from the same\n         file; `C` is a positive linear map of that same pair of coefficients through\n         site-wide scales. So two channels whose pick constants differ cannot charge the\n         same unload price unless the two terms exactly cancel. THAT is the clause that sees\n         a WHOLE leaf charged at the other channel's rate \u2014 the shape the per-leaf spread is\n         blind to, because a uniformly mispriced leaf has a spread of zero.\n\n     Both cross-leaf clauses go INACTIVE (reported, never a silent pass) when the pair's two\n     constants coincide: two channels MAY run the same pick config, and from two sim DBs\n     \"the same config\" and \"one regime resolved to the other\" are the same reading.\n\n# \u2500\u2500 usage \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    python Diagnostics/receiving_report.py <run_root_or_name>     # every arm, PASS/FAIL\n    python Diagnostics/receiving_report.py <run_root> --verbose   # per-arm numbers\n\nRun roots may be bare names (resolved under COMPARISON_OUTPUT_DIR) or absolute paths. Arms\nare enumerated through the run's own contract (`runschema.resolver_for`) and never by joining\npath strings: the store CONFIG and the store CHANNEL are both named `store`, so\n`<cell>/<pair>/store/store/` is a real path, and two levels are conditional. The pair pass\nruns on top of the same walk when the run layout carries the `coupled` marker.",
      "kind": "module",
      "signature": null
    },
    "Diagnostics/receiving_report.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': {'work_events.duration': 'sum', 'work_events.role': 'read', 'work_events.actor_uid': 'read', 'work_events.t_abs': 'read', 'work_events.batch_id': 'read', 'work_events.mode': 'read', 'wor..."
    },
    "Diagnostics/receiving_report.py::_BUCKET_DP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "round(-math.log10(_TOL))"
    },
    "Diagnostics/receiving_report.py::_CHANNEL_ROLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('pick',)"
    },
    "Diagnostics/receiving_report.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Diagnostics/receiving_report.py::_REL_TOL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1e-09"
    },
    "Diagnostics/receiving_report.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Diagnostics/receiving_report.py::_SITE_ROLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('put', 'receive')"
    },
    "Diagnostics/receiving_report.py::_TOL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1e-06"
    },
    "Diagnostics/receiving_report.py::_coupled_pairs": {
      "decorators": [],
      "doc": "`(pairs, dockless)` for a COUPLED run \u2014 `([], [])` on every uncoupled one.\n\n`pairs` is `[(label, leaf_dbs, site_db)]`, one entry per arm pair that HAS a site DB.\n`dockless` is `[(label, leaf_dbs)]` for everything that should have one and does not --\na whole pair directory with no site DB at all, and (the hard case below) a single arm a\nleaf ran that no site-DB stem names.  ONE list, because both take the same rule: a\ndefect when those leaves received, the ordinary inbound-off case when they did not.\n\nTHE PAIRING COMES FROM THE SITE DB'S OWN NAME, which is the only declared record of it:\n`<store_arm>__<fulfillment_arm>` is the artifact's `{strategy}` capture (site-dock 24),\ninverted through `rt.arm_pair_of` and split by `rt.arm_pair_halves`.  There is\ndeliberately no fallback that zips the two channels' arm lists -- site-dock 06's finding\nis that `sorted(set(arms))` destroys the rank the diagonal IS, so a pairing built that\nway is alphabetical, the right length, and wrong in a way nothing downstream detects.\n\nTHE ARM PAIR THAT WROTE NO SITE DB IS THE HARD CASE, and reading the pairing off the\nfilenames is what makes it hard: an arm pair whose `inbound_*.db` never reached disk is\nnot ENUMERATED, so without the sweep below it would be neither FAIL nor MISSING but\nabsent -- 33 of 34 pairs reported green while one lost its site scope, which is the\ndefect site-dock 15 section 5 was written for, unreachable.  So every arm a leaf ran and\nno site-DB stem names is swept up here and handed on with `site_db=None`; the pairing\nfor such an arm is unknown by construction, so the arm is reported under its own key and\nits leaves are the ones that ran it.\n\nA COUPLED INBOUND-OFF cell contributes no pairs at all, because it fields no dock and\nwrites no site DB anywhere under the pair.  That is `dockless`, and the caller decides\nwhat it means: nothing, if its leaves never received; a lost site scope, if they did.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt) -> tuple"
    },
    "Diagnostics/receiving_report.py::_dominant_constant": {
      "decorators": [],
      "doc": "The rate a leaf MOSTLY charged, out of its re-priced residuals.\n\nNOT `min`, which is what `reconcile` reports per leaf and is right there: on a clean leaf\nthe spread is ~1e-15 and every summary of the residuals is the same number. This one is\nused where the leaf may be PARTLY contaminated, and there `min` is actively wrong \u2014 a\nsingle store row charged at the fulfillment rate makes `min` return the FULFILLMENT\nconstant, the two leaves then measure the same C, and the cross-regime clause goes\ninactive on exactly the defect it exists to name. (Found by the test, not by reading.)\n\nThe mode over residuals rounded to `_BUCKET_DP` places \u2014 DERIVED from `_TOL`, so a change\nto the tolerance carries the bucket with it: every correctly-priced row lands in one\nbucket, so it is the rate the crew actually charged. The value RETURNED is a real\nresidual from that bucket rather than the rounded key, so a comparison against `_TOL`\nis not spending half its budget on the rounding.\n\nA leaf split exactly 50/50 has no dominant rate and this picks one arbitrarily \u2014 which\nis the right answer anyway: the other half is then named as cross-priced, and the\nleaf's own spread has already gone red.",
      "is_async": false,
      "kind": "function",
      "signature": "(residuals) -> float"
    },
    "Diagnostics/receiving_report.py::_finite": {
      "decorators": [],
      "doc": "Whether a seconds value is a real number a tolerance can be compared against.\n\nREACHABLE, and via INFINITY rather than NaN. SQLite stores a Python NaN as NULL, so a\nNaN never comes back out of these queries -- but an infinity round-trips, and `SUM` of a\ncolumn holding one is `inf`. That breaks the comparison two ways at once: `_tol_for(inf)`\nis itself `inf`, so `abs(x - inf) > inf` is False for any finite other side; and\n`abs(inf - inf)` is NaN, which compares False against everything. A check that only\ncompared would report a clean PASS about a value that is not a number.\n\nNamed rather than inlined because both surfaces that sum seconds need it.",
      "is_async": false,
      "kind": "function",
      "signature": "(v) -> bool"
    },
    "Diagnostics/receiving_report.py::_fmt_s": {
      "decorators": [],
      "doc": "A seconds constant for the report, or `-` when it could not be measured.\n\nASCII only, like every other printed string here: this CLI runs on a cp1252 console\nwhere a stray em dash kills the process mid-report (`windows-console-is-cp1252`).",
      "is_async": false,
      "kind": "function",
      "signature": "(v) -> str"
    },
    "Diagnostics/receiving_report.py::_has": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con, table: str) -> bool"
    },
    "Diagnostics/receiving_report.py::_leaf_state": {
      "decorators": [],
      "doc": "Everything the pair clauses need out of ONE channel leaf's sim DB, in one open.\n\nGathered per leaf rather than per clause so the file is opened once (these are archived\nWAL-less files and `_open_ro`'s immutable flag is what keeps a read from minting sidecars\nit cannot remove -- memory `wal-sidecars-come-from-readers`).\n\nTHE CHANNEL COMES OFF `simulation_runs.channel`, never off the directory the file sits\nin. A leaf's config directory and its channel directory can carry the same name (the\nstore config and the store channel are both `store`), and site-dock 24 established that\nthe regime a row belongs to is resolvable from the sim DB ALONE precisely because this\ncolumn names it -- which is what made check 6's two-constant form buildable with no new\ncolumn and no `--sync`.\n\n`pick_constant` is `labor_cost - handle_var` off `sku_scores`, which is\n`pick_intercept + pick_per_item` (`Order.compute_labor_cost` computes `labor_cost` as\n`per_pick(1.0, intercept, handle_var, 1, per_item)`). One constant per run, and its\nSPREAD is reported so a caller can see whether it really is one before leaning on it.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str) -> dict"
    },
    "Diagnostics/receiving_report.py::_open_ro": {
      "decorators": [],
      "doc": "Read-only AND immutable: a plain `mode=ro` open still mints -wal/-shm sidecars beside\nan archived DB, and cannot remove them afterwards.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str)"
    },
    "Diagnostics/receiving_report.py::_sim_dbs": {
      "decorators": [],
      "doc": "Every arm's sim DB, through the run's own contract.",
      "is_async": false,
      "kind": "function",
      "signature": "(root: str)"
    },
    "Diagnostics/receiving_report.py::_site_totals": {
      "decorators": [],
      "doc": "`(by_batch, note, answerable)` \u2014 the SITE dock's per-batch counters, or why not.\n\n`answerable` is False ONLY for a file that cannot hold the answer \u2014 a site DB written\nbefore `site_receiving` existed. That is the difference between \"the site total is\nmissing\" (a defect: the rows were parked and never reached an artifact) and \"this\nvintage never recorded one\" (a fact about the file), and conflating them would make the\ntool red on every site DB site-dock 24's build wrote.\n\nThe `site_db is None` and \"does not exist\" branches are NOT reachable from `main`, which\nonly ever passes a path `rt.site_inbound_dbs` just found (a pair with no site DB never\nreaches `reconcile_pair` at all \u2014 `main` reports it separately). They are kept because\n`reconcile_pair` is callable on its own and a caller that hands it None should get the\nno-site-total answer rather than an exception.\n\n`by_batch` is `{batch: (seconds, unloaded)}`, straight off `site_receiving`.\n\nREAD WITH AN INLINE QUERY, and that is a deviation from CLAUDE.md section 2 worth stating\nrather than leaving to be noticed. The rule is that SQL belongs in a named query beside\nthe family with a per-vintage `dataset.override` where a shape moved. What moved here is\nnot a shape: the whole TABLE is absent before this build, so there is no older column\nlayout for an override to describe -- the negotiation is \"does it exist\", which is the\n`_has` probe this file already uses for `sku_scores` and which every one of checks 1-5\nreads `work_events` and `batch_stats` through. A named query plus an override that says\n\"on every earlier vintage, nothing\" would be a second declaration of the same absence.\nCheck 6 is the one clause here that goes through a loader, and only because\n`receive_event_frame` already existed and had been waiting for a caller.",
      "is_async": false,
      "kind": "function",
      "signature": "(site_db: str | None) -> tuple"
    },
    "Diagnostics/receiving_report.py::_split_arm_pair": {
      "decorators": [],
      "doc": "The leaf DBs of one arm pair, through the contract's OWN inversion.\n\n`rt.arm_pair_halves` is the other half of `rt.arm_pair_of` and owns the whole rule --\nthe positional read against the declared channel order, the every-split-position scan\n(an arm name may contain the joiner, which is why the arm pair is ONE capture in the\ndeclared template), and the refusal when no channel order is declared. This only turns\nthe `{channel: arm_key}` it returns back into the paths the caller already holds.\n\nA SECOND IMPLEMENTATION LIVED HERE and a third in `run_analysis._site_jobs`, and both\nhad the same defect independently: resolving which channel owns a half by MEMBERSHIP,\nwhich matches neither leaf on a campaign where both channels draw restock rules from one\nvocabulary, so every pair is skipped and the pass reports `0 coupled pair(s)` over a run\nthat has many. That is the argument for the rule having one home.\n\n`[]` when nothing resolves, which the caller reports rather than guessing at: a pair\nwhose halves cannot be found is a pair whose leaves were not both run.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, arm_pair: str, by_channel: dict) -> list"
    },
    "Diagnostics/receiving_report.py::_tol_for": {
      "decorators": [],
      "doc": "The seconds tolerance for comparing two accumulations of size ~`magnitudes`.\n\n# \u2500\u2500 why an absolute tolerance alone was wrong, and why a relative one alone is too \u2500\u2500\n\n`_TOL` was 1e-6 SECONDS flat, compared against sums that GROW WITH THE ROW COUNT. On a\n505,177-row arm check 1's two surfaces accumulate 1.3e-6 s apart over 4,177,040.9 s \u2014\na relative error of 3e-13, which is float re-association and nothing else \u2014 and the tool\nreports it as a FAIL. It does so on four arms of the archive today, and it gets sharper\nunder coupling, where a SITE total is the sum of two leaves' and carries both leaves'\naccumulation error.\n\nThe objection that kept this open is real and is the right objection: a purely RELATIVE\ntolerance hides a small real discrepancy on a large arm, which is the failure check 1\nexists for. What settles it is that the discrepancy check 1 hunts has a PHYSICAL LOWER\nBOUND. Every defect it was written for \u2014 a dropped `we.extend`, an unrun `recv_seconds`\nassignment, an uncleared checkpoint buffer \u2014 moves the sum by at least ONE UNLOAD, and\nthe smallest unload the archive actually prices is 5.1 s (the fulfillment constant;\nstore's is 7.6 s). So the question is only whether the tolerance can be kept far below\none unload while sitting far above float noise, and at this scale it can:\n\n    arm                 sum (s)      1e-9 x sum    observed float noise   one unload\n    505,177 rows      4,177,040.9      4.2e-3 s          1.3e-6 s          5.1-7.6 s\n    a 12-batch pair       62,278.9      6.2e-5 s          ~1e-11 s          5.1-7.6 s\n\n4.2e-3 s is under a THOUSANDTH of the smallest thing that can really go missing, and\nthree orders above the observed noise. The absolute floor is kept so that a tiny run \u2014\nor a comparison against zero \u2014 is not handed a tolerance of zero.\n\nTHE UPPER MARGIN IS EMPIRICAL, NOT A BOUND, and that is worth knowing before this is\nreused at a larger scale. Worst-case float summation error is `N * eps * |sum|`, which\nat 505,177 rows is ~1.1e-10 relative \u2014 only ~9x under this floor rather than the ~3e4x\nthe OBSERVED 3e-13 suggests. An arm an order of magnitude larger (~5M rows) would put\nthe worst case above `_REL_TOL` and the false FAIL would return. At that point the\nrelative term wants scaling by the row count, not raising.\n\nIT ALSO CHANGES VERDICTS ON THE ARCHIVE, dated here so a later reader is not confused by\na record that no longer reproduces: the four arms recorded as failing check 1 on\n1.3e-6 s of re-association PASS from 2026-09-12 (site-dock 25). Nothing about those runs\nmoved; the tolerance did.\n\nApplied ONLY to sums. A spread (check 6) does not accumulate: it is a max minus a min\nover per-row residuals, so its error is bounded by one row's rounding however many rows\nthere are, and giving it a row-count-dependent tolerance would loosen it for no reason.",
      "is_async": false,
      "kind": "function",
      "signature": "(*magnitudes) -> float"
    },
    "Diagnostics/receiving_report.py::_unload_price": {
      "decorators": [],
      "doc": "Check 6's state: re-price every receive row from the run's OWN per-SKU handle term.\n\n`duration - qty * handle_var` must collapse to `per_item + intercept` -- one constant per\nrun, carrying no SKU and no quantity. See the module docstring for why that identity holds\nand what a spread would mean.\n\nPER RUN, never across runs: `per_item` and `intercept` come from the run's pick config, so\ntwo runs in one file may legitimately price differently. A per-arm DB holds one run anyway\n(and since the fresh-run branch refuses to `create_run` over a db that already holds one,\na second is legacy or additive), but the loop is what makes `run_id=None` honest.\n\nReads `sku_scores` through the caller's IMMUTABLE connection rather than through\n`load_sku_scores`, which opens a plain `mode=ro`: that mints `-wal`/`-shm` beside an\narchived DB and cannot remove them (see `_open_ro`), and archived runs are exactly this\ntool's subject. The receive rows come through `load_receive_events`, whose own open is\nimmutable -- and whose `receive_event_frame` query serves the columns this needs, already\nfiltered to `role='receive'` (so repacks arrive included, which is the point). It and\n`load_receive_events` had ZERO callers since they were written; this is the caller they\nwere written for. A second inline query here would be a second thing to keep in step with\nthe DDL.\n\n`keep_residuals` additionally returns EVERY row's `c` under `unload_residuals`, which the\nPAIR-scope clause needs and `reconcile` deliberately does not: the pair check asks which\nrows price at the OTHER channel's constant, and a min-and-spread summary cannot answer\nthat. Off by default, because a 505,177-row arm would otherwise carry half a million\nfloats out of a function whose caller wants two numbers.",
      "is_async": false,
      "kind": "function",
      "signature": "(con, db_path: str, where: str, args: dict, keep_residuals: bool=False) -> dict"
    },
    "Diagnostics/receiving_report.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Diagnostics/receiving_report.py::reconcile": {
      "decorators": [],
      "doc": "Cross-check one sim DB's receiving surface. Returns a dict; never raises on content.\n\n`run_id` None means every run in the file, which is what a per-arm DB holds anyway.\n\nA DB with no receiving activity is a PASS with `active=False` \u2014 every run before this\nfeature, and every run that did not ask for a crew. That is not the same as a failure,\nand conflating them would make the tool useless on exactly the runs it should be quiet\nabout.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str, run_id: int | None=None) -> dict"
    },
    "Diagnostics/receiving_report.py::reconcile_pair": {
      "decorators": [],
      "doc": "Cross-check ONE coupled arm pair: two channel leaves against the site's own dock.\n\n`leaf_dbs` are the leaves of one arm pair -- one DB per channel, each naming its channel\nin `simulation_runs.channel`. `site_db` is that pair's site inbound DB (the contract's\n`site_inbound_db`), or None when the pair produced none. Returns a dict; never raises on content, exactly as\n`reconcile` does.\n\nFour clauses, A-D in the module docstring, and every one of them is UNSTATEABLE from one\nDB. What is NOT here is as deliberate: checks 1 and 2 stay PER LEAF and unedited\n(site-dock 15 section 2), because moving them to site scope discards the per-channel\nattribution ADR-0005 goes to some length to keep -- and because both of their surfaces\nnow delegate to one coordinator-held dock, so a leaf handed the whole drain would agree\nwith itself and PASS while owning the other channel's entire receiving labour. Clause C\nis what sees that.\n\nON THE ABSENT SITE DB, and this SHARPENS site-dock 15 section 5 rather than restating it.\n15 said an absent site DB on a coupled pair is a FAIL, full stop. It cannot be: site-dock\n06 couples every cell INCLUDING its inbound-OFF pole, and site-dock 24's\n`_build_site_dock` returns None there -- no trailer pipeline, no dock, no site DB, and\nthat is a real configuration rather than a degenerate one. So the rule is conditioned on\nthe evidence: a pair whose leaves recorded RECEIVING and has no site DB is a run that\nlost its site scope and FAILs; a pair whose leaves recorded none is the inbound-off pole\nand is a PASS with `active=False`. That keeps 15's rule reachable (it fires on exactly\nthe defect it was written for -- site-dock 21's parked rows never reaching an artifact)\nwithout failing half the campaign.",
      "is_async": false,
      "kind": "function",
      "signature": "(leaf_dbs, site_db: str | None) -> dict"
    },
    "Diagnostics/replay_run.py": {
      "decorators": [],
      "doc": "replay_run.py \u2014 export a real simulation run's persisted DBs to a dashboard trace.\n\nReads the SQLite output of a finished `run_simulation.py` run and emits a\nDiagnostics/out/replay_<label>.json with per-batch warehouse fill (overall, by\naisle, by bucket) so the SAME dashboard can visualise the actual misbehaving run\n\u2014 including the warehouses that settle at ~70% instead of the target ~85%.\n\nPer Visualization/RECONSTRUCTION.md the relevant files are (this docstring is also `--help`, so\nit stays inside cp1252 \u2014 no box drawing here, unlike the section banners below):\n\n    <pair>/warehouse.db            aisle_layout    (geometry -> per-aisle capacity)\n    <pair>/<config>/sim_X.db       bin_placement   PLACE  |\n                                   bin_eviction    EVICT  |  the bin-mutation log:\n                                   picks           PICK   |  fold -> EXACT occupancy\n                                   aisle_metrics   (n_bins occupied / aisle / batch)\n                                   bin_inventory   (pick deltas - ARCHIVED runs only)\n                                   batch_stats     (duration / batch)\n                                   simulation_runs (run params)\n\nWhere occupancy comes from \u2014 and what it is worth\n-------------------------------------------------\nThree sources, tried best-first.  Only the first is correct, so every export now RECORDS which one\nit used (`meta.occupancy_source` / `occupancy_exact` / `occupancy_phase` / `occupancy_note`) and the\nCLI prints a warning when the curve is an approximation.  A wrong fill curve presented as fact is\nthe failure this tool shipped with for its whole life.\n\n1. `bin_placement` + `bin_eviction` + `picks`  ->  **EXACT at every batch.**\n   Bin state changes at exactly five sites in the codebase (one of them dead code), and picks are\n   already fully recorded, so PLACE + EVICT + PICK is complete *by construction*.  Folding them in\n   application order \u2014 EVICT, then PLACE, then PICK, mirroring the runner \u2014 reproduces the\n   simulation's own bin state.  Reference fold: `Tests/bench/bin_log_harness.py::fold`; proof\n   against a live simulation: `Tests/integration/test_bin_log_replay.py`.  Checked here against a\n   production arm's `sim_X.keyframes.db` (an independent record written by the runner): 9,690/9,690\n   bins identical at batch 0 and 9,450/9,450 at batch 3, sku and qty included, zero ghosts.\n\n2. `aisle_metrics.n_bins`  ->  **approximate.**  It is the manager's own occupied-bin counter\n   (`sum(_aisle_sku_counts[aid].values())`) sampled after the restock pass and BEFORE the batch's\n   picks, so it is a different frame from the other two sources, and it lags a bin emptied by a\n   pick.  Only strategies that maintain aisle state write it at all \u2014 for a uniform-placement arm\n   the table is empty, which is why the third source is what usually ran.\n\n3. `bin_inventory` deltas  ->  **approximate and biased downward**, and no longer written at all:\n   only runs from the archive carry this table, and on a current DB source 1 always answers first.\n   The table records picks and\n   NEVER restocks: `check_reorders()` runs before the pre-batch snapshot, so a restocked bin is\n   already in it at its post-restock quantity and the `post_qty == pre_qty` skip drops it.\n   Measured on a production arm: **0 rows** with `post_qty > pre_qty` against 20,662-42,832\n   `reorder_placements` per batch.  Replaying those deltas can therefore only ever DECAY occupancy\n   \u2014 68,271 occupied bins against a true 165,519 five batches past a keyframe.  It draws a\n   warehouse that slowly drains, plausibly enough that nothing flagged it for months.\n\nOld runs keep working: the ~500 GB archive predates `bin_placement`, so those DBs fall through to\n2/3 and are labelled approximate instead of silently believed.\n\nUsage\n-----\n    python Diagnostics/replay_run.py <run_base_dir>\n    python Diagnostics/replay_run.py <run_base_dir>/<pair>/<config>/sim_uni_fifo_norsl.db\n    python Diagnostics/replay_run.py <run_base_dir> --max 200     # cap exported batches\n\nThen view with the dashboard:\n    cd Diagnostics && python -m http.server 8009  -> http://localhost:8009/static/",
      "kind": "module",
      "signature": null
    },
    "Diagnostics/replay_run.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': {'bin_eviction.batch_id': 'read', 'bin_eviction.aisle_id': 'read', 'bin_eviction.bayX': 'read', 'bin_eviction.bayY': 'read', 'bin_placement.batch_id': 'read', 'bin_placement.aisle_id': '..."
    },
    "Diagnostics/replay_run.py::_BatchStream": {
      "decorators": [],
      "doc": "One table's rows, grouped by `batch_id`, consumed strictly forward.\n\nA cursor with one row of lookahead rather than a list: a single production arm holds millions\nof picks, and the fold only ever needs the batch it is currently applying.  `take()` is a\ngenerator and MUST be drained before the stream is asked for its next batch.",
      "kind": "class",
      "signature": ""
    },
    "Diagnostics/replay_run.py::_BatchStream.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, conn: sqlite3.Connection, sql: str, args: tuple)"
    },
    "Diagnostics/replay_run.py::_BatchStream.batch": {
      "decorators": [
        "property"
      ],
      "doc": "The batch the next unread row belongs to, or None when exhausted.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int | None"
    },
    "Diagnostics/replay_run.py::_BatchStream.take": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int)"
    },
    "Diagnostics/replay_run.py::_FOLD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{_picking_data.CAP_BIN_LOG: lambda conn, run_id, max_batches: occupied_from_bin_log(conn, run_id, max_batches), _picking_data.CAP_AISLE_METRICS: lambda conn, run_id, max_batches: occupied_from_aisl..."
    },
    "Diagnostics/replay_run.py::_GRID_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "6"
    },
    "Diagnostics/replay_run.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Diagnostics/replay_run.py::_OUT_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_HERE, 'out')"
    },
    "Diagnostics/replay_run.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Diagnostics/replay_run.py::_RERUN_ADVICE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'Re-run the arm to get bin_placement and an exact curve.'"
    },
    "Diagnostics/replay_run.py::_contract_resolver": {
      "decorators": [],
      "doc": "The RunTree for a contract run root, or None (a single DB / pre-descriptor archive).",
      "is_async": false,
      "kind": "function",
      "signature": "(target: str)"
    },
    "Diagnostics/replay_run.py::_nearest_warehouse_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sim_db: str, rt=None) -> str | None"
    },
    "Diagnostics/replay_run.py::_note": {
      "decorators": [],
      "doc": "The `occupancy_note` an export carries for `cap`: the registry's caveat, then ours.\n\nJoined with a single space, skipping either half when it is empty.  The exported key stays\n`occupancy_note` and not `occupancy_caveat` \u2014 the dashboard and every replay JSON already on\ndisk read `meta.occupancy_note`, and renaming it to match the shared type's field name would\nbreak the output contract for no gain.",
      "is_async": false,
      "kind": "function",
      "signature": "(cap) -> str"
    },
    "Diagnostics/replay_run.py::_occupancy": {
      "decorators": [],
      "doc": "Fold the best occupancy source this DB carries; return (occ_by_batch, capability).\n\nProbing rather than assuming is what keeps the ~500 GB archive readable: those DBs predate the\nbin-mutation log entirely, so `bin_placement` is not merely empty \u2014 the table does not exist.\n`capability.has_rows` is False for both cases, and the run then falls through to a LABELLED\napproximation.\n\nThe probe stays `run_id`-FILTERED. An arm that was resumed or interrupted can leave a table\ncreated-but-empty for the run being replayed, and an unfiltered probe would then pick a source\nthat folds to nothing \u2014 not hypothetical: in the 2026-07-29 archive `aisle_metrics` exists on\nevery arm and is empty for the uniform ones, which is how `bin_inventory` gets reached at all.\n\nRaises `capability.NoSourceAvailable` when the run carries none of the three; the caller turns\nthat into a `SystemExit` naming the file.",
      "is_async": false,
      "kind": "function",
      "signature": "(conn, run_id, max_batches: int)"
    },
    "Diagnostics/replay_run.py::build_batches": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(occ_by_batch, capacity, aisles, durations, max_batches) -> list[dict]"
    },
    "Diagnostics/replay_run.py::discover_sim_dbs": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(target: str, rt=None) -> list[str]"
    },
    "Diagnostics/replay_run.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Diagnostics/replay_run.py::occupied_from_aisle_metrics": {
      "decorators": [],
      "doc": "APPROXIMATE fallback \u2014 see `Picking_Data.SIM_CAPABILITIES['aisle_metrics']`.\n\n`n_bins` is the manager's own occupied-bin counter sampled after the restock pass and BEFORE\nthe batch's picks, so this is the start-of-batch frame, not the end-of-batch one the log and\n`bin_inventory` produce.",
      "is_async": false,
      "kind": "function",
      "signature": "(conn, run_id) -> dict[int, dict[int, int]]"
    },
    "Diagnostics/replay_run.py::occupied_from_bin_inventory": {
      "decorators": [],
      "doc": "APPROXIMATE fallback of last resort \u2014 see `Picking_Data.SIM_CAPABILITIES['bin_inventory']`.\n\nARCHIVE-ONLY: `bin_inventory` is no longer written, so on a current DB this is unreachable \u2014\n`_occupancy` probes `bin_placement` first and the table is not even in the schema.  Reached\nonly for the ~500 GB of pre-log runs, which is exactly why it is kept.\n\nRolls bin_inventory deltas (full snapshot at batch 0 + changed bins after) into an occupied-bin\ncount per aisle at the end of each recorded batch.  The stream contains picks and no restocks,\nso the curve it produces can only ever fall.",
      "is_async": false,
      "kind": "function",
      "signature": "(conn, run_id) -> dict[int, dict[int, int]]"
    },
    "Diagnostics/replay_run.py::occupied_from_bin_log": {
      "decorators": [],
      "doc": "Fold PLACE + EVICT + PICK into an occupied-bin count per aisle at the end of each batch.\n\nThis is the correct reconstruction, and the only one: the three event streams are every\nmutation of `Aisle.Bin.storage` the simulation can make, so the fold reproduces its own state\nrather than approximating it.  Mirrors `Tests/bench/bin_log_harness.py::fold`, including the\nwithin-batch order **EVICT -> PLACE -> PICK** \u2014 the runner reloads, then calls\n`check_reorders()`, then simulates picks.\n\nOnly the aisle counts are kept, not `{loc: (sku, qty)}` as the harness does: the dashboard\ndraws fill per aisle, and a whole warehouse of per-bin tuples per batch is not worth carrying.\nCounts are maintained incrementally as bins enter and leave `qty`, so a batch costs its own\nevents and not a rescan of ~165k bins.\n\nQuantities are integers throughout (`qty`, `picks.quantity`), so the depletion test below is\nexact integer arithmetic \u2014 no float tolerance is involved anywhere in this fold.\n\n`max_batches` stops the fold early (0 = all).  Occupancy at batch k depends on every event\nbefore it, so this is a cap on output, never a shortcut through the history.",
      "is_async": false,
      "kind": "function",
      "signature": "(conn, run_id, max_batches: int=0) -> dict[int, dict[int, int]]"
    },
    "Diagnostics/replay_run.py::read_layout": {
      "decorators": [],
      "doc": "Aisle geometry + per-aisle bin capacity from a run's `warehouse.db`.\n\nHARD FAIL on an unvetted shape (`identity.check` raises `UnsupportedSchema`), unlike the\nviewer, which warns.  Capacity is the DENOMINATOR of every fill percentage this tool\nexports, and the dashboard renders those percentages as a measurement.  A geometry table\nthat is not the one we think it is produces a fill curve that is wrong in a way no reader\ncan see \u2014 the same class of defect as the `bin_inventory` decay carried in that capability's\ncaveat, which is why that entry exists at all.  Better to name the differing column and stop.\nBoth archived shapes are vetted (`Warehouse_Data.PRE_STAMP_WAREHOUSE_SCHEMA_ID` and\n`PRE_FINGERPRINT_WAREHOUSE_SCHEMA_ID`), so this refuses nothing that exists today.",
      "is_async": false,
      "kind": "function",
      "signature": "(warehouse_db: str) -> tuple[list[dict], dict[int, int]]"
    },
    "Diagnostics/replay_run.py::replay_sim_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sim_db: str, warehouse_db: str, max_batches: int) -> dict"
    },
    "Diagnostics/trace_lifecycle.py": {
      "decorators": [],
      "doc": "trace_lifecycle.py \u2014 instrumented simulation lifecycle tracer.\n\nRuns a small simulation for one or more strategies and records, per batch, the\nlifecycle of inventory units:\n\n    intake -> units placed -> stuck-in-queue -> picked -> emptied -> reclaimed\n\nplus per-aisle / per-bucket fill, queue depth, and per-function call timings.\nThe manager's lifecycle methods are wrapped on the instance (no edits to\nWarehouse/ or Optimization/ source \u2014 same approach as Tests/bench/profile_lifecycle.py).\n\nThe default compares a uniform-stocked strategy (fills to the target ~85%) against\na policy-stocked one (which can leave units stuck in the queue -> lower fill),\nreproducing the \"some warehouses start at ~70% vs ~85%\" discrepancy.\n\nOutput: Diagnostics/out/trace_<strategy>.json (one per strategy) plus manifest.json.\nOpen the dashboard with:\n\n    cd Diagnostics && python -m http.server 8009\n    # then browse http://localhost:8009/static/\n\nUsage\n-----\n    python Diagnostics/trace_lifecycle.py\n    python Diagnostics/trace_lifecycle.py --strategies uni_fifo_norsl,opt_map_norsl\n    python Diagnostics/trace_lifecycle.py --skus 3000 --batches 40 --pickers 10\n    python Diagnostics/trace_lifecycle.py --list      # print available strategy keys",
      "kind": "module",
      "signature": null
    },
    "Diagnostics/trace_lifecycle.py::Tracer": {
      "decorators": [],
      "doc": "Per-batch accumulators populated by the wrapped manager methods.",
      "kind": "class",
      "signature": ""
    },
    "Diagnostics/trace_lifecycle.py::Tracer.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Diagnostics/trace_lifecycle.py::Tracer.reset": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Diagnostics/trace_lifecycle.py::_GRID_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "6"
    },
    "Diagnostics/trace_lifecycle.py::_HANDLINGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['conveyable', 'non-conveyable']"
    },
    "Diagnostics/trace_lifecycle.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Diagnostics/trace_lifecycle.py::_OUT_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_HERE, 'out')"
    },
    "Diagnostics/trace_lifecycle.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Diagnostics/trace_lifecycle.py::_aisle_bucket": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(aisle) -> str"
    },
    "Diagnostics/trace_lifecycle.py::_declare_stock": {
      "decorators": [],
      "doc": "Declare Q = 2 x expected batch demand (capped at 3) with rp = 1 on every order.\n\nBoth numbers survive `declare_stock`'s clamps unchanged \u2014 `eq_qty` is already \u2265 1,\n`min(Q-1, 1)` is 1 for every Q \u2265 2, and the Q = 1 orders this cap does produce (a SKU\nwhose expected batch demand rounds below one unit) take rp = 1 from the Q = 1 branch \u2014\nso this is the same level the probe has always fielded; only the writer moved.\n\n`expected_batch_demand`, `lead_time_mean` and `supply_cv` stay direct assignments:\nthey are demand and supply facts of the SKU, not a level, and `Order.__init__` (the\nconstruction path `_build_inventory` uses) leaves all three unset.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, lead_time: float=2.0, supply_cv: float=0.1) -> None"
    },
    "Diagnostics/trace_lifecycle.py::_fill_snapshot": {
      "decorators": [],
      "doc": "Occupied / capacity \u2192 fill_overall, fill_by_aisle, fill_by_bucket.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, warehouse, capacity: dict[int, int]) -> dict"
    },
    "Diagnostics/trace_lifecycle.py::_fn_trace": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(tr: Tracer) -> list[dict]"
    },
    "Diagnostics/trace_lifecycle.py::_install_hooks": {
      "decorators": [],
      "doc": "Wrap manager lifecycle methods on the instance to feed the tracer.\n\nOnly manager-level methods are wrapped (never reassigned by strategy build),\nso a strategy swapping mgr.placement does not detach the hooks.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr: Inventory_Manager, tr: Tracer) -> None"
    },
    "Diagnostics/trace_lifecycle.py::_install_hooks._apply_picks_batch": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(picks, empties)"
    },
    "Diagnostics/trace_lifecycle.py::_install_hooks._execute_placement": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit, bin_, **kw)"
    },
    "Diagnostics/trace_lifecycle.py::_install_hooks._execute_topup": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(order, bin_, n, **kw)"
    },
    "Diagnostics/trace_lifecycle.py::_install_hooks._reclaim_empty_bins": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Diagnostics/trace_lifecycle.py::_install_hooks.check_reorders": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(*a, **kw)"
    },
    "Diagnostics/trace_lifecycle.py::_warehouse_layout": {
      "decorators": [],
      "doc": "Serialise aisles (one heatmap cell each) + return per-aisle capacity.",
      "is_async": false,
      "kind": "function",
      "signature": "(warehouse) -> tuple[list[dict], dict[int, int]]"
    },
    "Diagnostics/trace_lifecycle.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Diagnostics/trace_lifecycle.py::trace_strategy": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(strategy_key: str, *, n_skus: int, bins_per_aisle: int, n_batches: int, n_pickers: int, seed: int, fill: float) -> dict"
    },
    "Diagnostics/trace_lifecycle.py::trace_strategy._arm": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Inbound/__init__.py": {
      "decorators": [],
      "doc": "Inbound \u2014 the receiving side of the site: trailers, the dock, unloading and packing.\n\nA top-level package beside `Warehouse/`, split out so the two domains meet only at the\nInventory Manager's broker seam (see root `CONTEXT.md` for the vocabulary and\n`.scratch/inbound-groundwork/` while the effort is in flight).\n\n# \u2500\u2500 the import law \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nTwo rules, both enforced by `context/architecture.yml` boundaries:\n\n  * This package imports Warehouse VALUE layers only \u2014 `wh_kernel`, `wh_layout`,\n    `wh_catalog`, and `wh_operations` (for the `PutawayCost` anti-drift coupling in\n    `unload.py`).  Never `wh_inventory`, `wh_picking`, `wh_placement`, or the harness.\n  * NO Warehouse module imports this package \u2014 not even the Inventory Manager.  The driver\n    (`Optimization/`) constructs Inbound's objects from CONFIG specs and binds them on the\n    manager (`enable_receiving`, `mgr.packer`), the way observers and placement functions\n    are already injected.  Hub-ness is injection, not imports.\n\n# \u2500\u2500 what lives here \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    dock.py        the Dock: spec, crew clocks, and the merchandise standing on the floor\n    unload.py      what it costs to take one storage unit off a trailer\n    pack.py        packing one delivery into storage units, and the LoadPlan record\n    trailer.py     TrailerType (53/28), stateful Trailers, and the load pallets they carry\n    priorities.py  the policy registries (global/local + yard/dock), frozen DockContext,\n                   ordering bound\n    transit.py     TrailerTransit: order port -> trailers -> yard -> dock doors;\n                   YardTransit: the standing yard, where doors become real\n    space.py       the space timeline: per-drain frozen SpaceView (empties + untimed\n                   predicted clears) the standing dock's decisions read via ctx.space\n    gain.py        the unload-plan evaluator (expected future work, faithful-to-arm)\n                   and the gain-family ordering entries it registers",
      "kind": "module",
      "signature": null
    },
    "Inbound/__init__.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['Dock', 'DockSpec', 'GAIN_POLICIES', 'GainBundle', 'LoadPlan', 'LoadPallet', 'OneOwnerBundle', 'packer', 'receive', 'receive_all', 'shipment_penalty', 'SiteGainBundle', 'SpaceTimeline', 'SpaceView..."
    },
    "Inbound/dock.py": {
      "decorators": [],
      "doc": "dock.py \u2014 the receiving crew and what is standing on their floor.\n\n# \u2500\u2500 why this is not a fourth PutQueue \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`put_queue.py` says, in writing, that a fourth stream should be a row rather than a class.\nFor put-away streams that is right and this file does not change it. A receiving CREW is a\ndifferent thing, and three properties of `PutQueueSet` make membership actively wrong \u2014 each\nverified in the code, not argued from taste:\n\n  1. `Inventory_Manager._stock`'s drain loop iterates EVERY queue in `self.put_queues` and\n     hands its contents to the placement pool. A dock in that set would have its trailers\n     placed into bins on batch 1, which is the entire feature undone.\n  2. `_cost_putaway` recomputes `self._put_clock = max(x.finish for x in self._put_queues)`,\n     and that feeds `_put_base = max(bs.batch_start_time, put_clock)` \u2014 the offset applied to\n     every put row in every later batch. A dock in the set would silently shift the whole\n     put-away stream along the absolute axis with its durations unchanged.\n  3. A fourth member flips `len(self.put_queues) == 1` false, which swaps `_stock_queue` from\n     a bare deque to `_MultiQueueView` \u2014 whose setter raises, and which re-sorts the whole\n     backlog by age on every read. Three consumers outside this package read it.\n\nSo the dock stands alone and shares the part that genuinely IS the same: the crew's clock,\nvia `Warehouse.kernel.crew_clock`. There is exactly one copy of the whistle rule.\n\n# \u2500\u2500 what the dock does and does not model \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nIt holds `PutawayItem`s \u2014 ALREADY PACKED and ALREADY STAMPED \u2014 and hands them to the put\nqueue one at a time as its crew works through the day. Three consequences worth stating:\n\n**Packing happens at ARRIVAL, not at unload.** The load plan is the shipper's and is fixed\nwhen the trailer is loaded. If the day boundary re-packed a half-unloaded trailer, the tier\nmix \u2014 and therefore `unit_category`, the queue an item routes to, and which bins are legal \u2014\nwould become a function of crew size and day length, and a receiving-staffing sweep would\nsilently be a packing sweep as well. `Inbound/pack.py` measured the effect:\nhalving deliveries re-packed 5 of the first 12 SKUs, in BOTH directions.\n\n**The age stamp is the arrival's, not the unload's.** Items reach the dock through `_admit`,\nwhich stamps on arrival, so a pallet that waited three batches on the dock is three batches\nold when it finally gets floor space. Stamping at unload would make the longest-waiting\nmerchandise the youngest thing in the warehouse \u2014 the priority inversion `_admit`'s docstring\nexists to forbid.\n\n**The dock exerts backpressure on nothing, and nothing exerts backpressure on the dock.**\n`arrive()` never refuses: there is no floor limit, because a refusal needs somewhere for the\nmerchandise to go and the only candidate is `_held`, which `_admit_held` retries INSIDE\n`_stock` \u2014 so held trailer merchandise would be put away for free, bypassing the crew\nentirely. The only bound on receiving is the crew's hours. The dock's DEPTH is the report.\n(One real coupling is kept: unloading hands the item to the put queue, so if that queue is at\nits staging limit the item goes to `_held` \u2014 unloading and put-away admission are different\nacts, and the unit is legitimately off the trailer either way.)\n\n# \u2500\u2500 two limitations that will produce plausible wrong numbers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n**Arrivals are quantized to BATCHES.** `Inventory_Manager.LEAD_TIME_UNIT` is `'batches'`, so\nevery trailer in a batch arrives at the same instant \u2014 the batch epoch \u2014 and the crew has no\nsub-batch arrival times to schedule against. The direction is knowable: a real day's arrivals\nspread out, so the modelled crew faces its whole day's work at once and its makespan reads\nLONG. Sizing a receiving crew off this model would therefore over-staff it. Converting lead\ntime to seconds moves every restock result on every arm and is a change of its own.\n\n**A put row can carry a smaller `t_abs` than the receive row of the same unit.** Both clocks\nrestart at the batch epoch and the streams are simulated independently and merged, which is\nthe model `work_events` already declares. The overlap is bounded by one batch's receive\nmakespan. Widening `_put_base` to include the dock's finish would over-correct \u2014 it would\nmake the first pallet off the truck wait for the last \u2014 and the honest fix needs a dock\ncoordinate and a per-unit handoff instant, neither of which exists.\n\n# \u2500\u2500 one dock, one price, or one dock and a price LIST \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nA per-leaf dock holds ONE `UnloadCost`, derived from that channel's own pick config, and\nthat is every run on disk.  A SITE dock serves two channels whose pick configs differ, and\n\"Decide the site dock's unload price\" (site-dock 27, user, 2026-09-11) settled whose price\nit charges: **the unload price is a statement about the MERCHANDISE, not about the crew**,\nso the site dock holds a price LIST keyed by the unloaded unit's own regime.  A fulfillment\ntote genuinely is quicker to move than a store pallet, and one crew can work at two rates\nwithout incoherence \u2014 which is exactly what `Inbound/putaway_pool.py` already established\none crew over, where `s_put` is keyed by channel while the putters are one pool.\n\nThe list is not a mode the single-channel path runs with a one-entry map: `costs` is None\non every uncoupled dock and `cost` is None on every site dock, so a dock holds exactly one\nof the two and the other is structurally absent.  That is what keeps every archived run\nbyte-identical \u2014 an uncoupled leaf builds its own dock from its own pick config exactly as\nit always did, and `unload_seconds` never so much as resolves a regime.",
      "kind": "module",
      "signature": null
    },
    "Inbound/dock.py::Dock": {
      "decorators": [],
      "doc": "One spec, one crew clock, and the merchandise standing on the floor.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/dock.py::Dock.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, spec: DockSpec, cost=None, costs: dict | None=None)"
    },
    "Inbound/dock.py::Dock.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/dock.py::Dock.arrive": {
      "decorators": [],
      "doc": "Put one already-stamped item on the floor.\n\nNEVER refuses -- see the module docstring. Appended, so the deque is in arrival order\nand the head is the oldest, which is what makes the unload FIFO and keeps\n`PutawayItem.age` monotonic along it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, item) -> None"
    },
    "Inbound/dock.py::Dock.can_start": {
      "decorators": [],
      "doc": "Is anyone free to BEGIN an unload before `deadline`? A START gate -- see\n`crew_clock.can_start` for what `deadline` is measured against.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, deadline: float | None) -> bool"
    },
    "Inbound/dock.py::Dock.can_start_team": {
      "decorators": [],
      "doc": "Is anyone on this team free to BEGIN before `deadline`? Same START gate.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, idxs, deadline: float | None) -> bool"
    },
    "Inbound/dock.py::Dock.charge": {
      "decorators": [],
      "doc": "Book `dur` to whoever is free earliest; return (start, worker index).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dur: float)"
    },
    "Inbound/dock.py::Dock.charge_team": {
      "decorators": [],
      "doc": "Book `dur` to the team member free earliest; return (start, worker index).\n\nDoes NOT accrue `seconds`, unlike `charge` \u2014 deliberately.  The standing drain\ncharges teams in clock order but accrues `seconds` in the CANONICAL handoff\norder, so the batch's float sum associates identically whatever the allocation\nmode \u2014 a sum accrued in charge order differs from the merged run's by one ulp,\nwhich is exactly the kind of \"identical except labor\" leak the containment\nproperty forbids.  The caller that charges a team also hands off, and accrues\nthere, once per record.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, idxs, dur: float)"
    },
    "Inbound/dock.py::Dock.cost_for": {
      "decorators": [],
      "doc": "This dock's price for merchandise of `regime` \u2014 a SITE dock only.\n\nRefuses on an uncoupled dock rather than answering with its one price: \"which\nregime's price\" is a question that dock has no opinion about, and an answer would\nmake the caller's regime resolution look consulted when it was ignored.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, regime: str) -> UnloadCost"
    },
    "Inbound/dock.py::Dock.crew_size": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/dock.py::Dock.depth": {
      "decorators": [
        "property"
      ],
      "doc": "Storage units on the floor. A LEVEL -- `snapshot()` reports it without resetting.\n\nThe same unit as `Inventory_Manager.queue_depth`, and DISJOINT from it: an item is on\nthe dock or in a put queue, never both. A reader wanting the whole unbinned backlog\nsums them.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/dock.py::Dock.drain_records": {
      "decorators": [],
      "doc": "This batch's unload records, and start the crew's clock over.\n\nA DRAIN IS A BATCH BOUNDARY, for the same reason `drain_putaway_records` is: the\nrecords carry `t0` on the crew's own clock measured from the start of the batch,\nbecause the batch epoch is not known here -- receiving runs at the TOP of the batch,\nbefore the pickers have been simulated. The runner adds the epoch when it writes the\nrows. Resetting is load-bearing, not tidiness: without it the clock accumulates across\nthe whole arm while the runner still adds the epoch, and every row after batch 0 is\nstamped too late by the total receiving seconds of every preceding batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/dock.py::Dock.drain_repacks": {
      "decorators": [],
      "doc": "This batch's repack records (ADR-0003), and start the list over.\n\nDOES NOT RESET THE CLOCKS, and that is the whole difference from `drain_records`.\nBoth streams are charged against the same crew on the same batch-local clock, so\nexactly one of the two drains may restart it; a second reset would rebase the\nsurviving stream's `t0` against a clock that had already gone back to zero. The\nreset stays with `drain_records`, which is the one every dockful run makes.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/dock.py::Dock.finish": {
      "decorators": [
        "property"
      ],
      "doc": "When the last receiver becomes free, on the batch-local clock.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Inbound/dock.py::Dock.name": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Inbound/dock.py::Dock.note_arrivals": {
      "decorators": [],
      "doc": "Record that `plans` deliveries reached the dock this batch.\n\nA COUNT, not the plans themselves: a run is hundreds of batches and a `LoadPlan` pins\na tuple of live `StorageUnit`s plus a cloned `Order`, which is precisely the leak\n`drain_inbound` turned out to be.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, plans) -> None"
    },
    "Inbound/dock.py::Dock.reset_clocks": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Inbound/dock.py::Dock.snapshot": {
      "decorators": [],
      "doc": "`(depth, unloaded, cut, seconds)` \u2014 and reset the three per-batch counters.\n\nTwo of those three are flows; `cut` is not, though it resets like one.  See\n`PutQueue.cut`, which has the same shape and the same warning: it re-counts the\nwhole standing dock every batch, so it must never be summed across them.\n\nDepth is a LEVEL and survives, exactly as `PutQueue.drain_counters` treats its own.\nMust run once per batch: a second call in the same batch reports zeros, and that is\nnot hypothetical \u2014 the put side shipped that defect and persisted an idle-looking\nqueue for every skipped batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Inbound/dock.py::Dock.take_records": {
      "decorators": [],
      "doc": "This batch's unload records, WITHOUT restarting the crew's clock.\n\n`drain_records` is this plus the reset, and the reset is the half that has exactly\nONE owner.  Uncoupled that owner is `drain_records` itself and nothing calls this;\non a coupled run the site's receiving coordinator partitions these rows between two\nleaves and the reset belongs to `SiteReceiving.note_records`, which fires once both\nleaves have stamped \u2014 so the coordinator needs the rows without the reset rather\nthan a second reset it would have to undo.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/dock.py::Dock.takes": {
      "decorators": [],
      "doc": "Does this dock intercept an admission from `source`?",
      "is_async": false,
      "kind": "function",
      "signature": "(self, source: str | None) -> bool"
    },
    "Inbound/dock.py::Dock.team_next_free": {
      "decorators": [],
      "doc": "When the team's earliest worker frees up; +inf for a team of nobody.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, idxs) -> float"
    },
    "Inbound/dock.py::Dock.unload_seconds": {
      "decorators": [],
      "doc": "Seconds to take ONE storage unit off a trailer, priced by this dock's own cost\nmodel \u2014 here so the manager needs no import of `unload`; the dock owns its price\nlist.\n\n`unit` is the `StorageUnit` being taken off, and it is read ONLY by a site dock:\nthe price list is keyed by the unit's own regime (site-dock 27), so the resolution\nhappens here, at the charge site, where every caller already holds the unit.  An\nuncoupled dock has `costs is None` and returns before ever looking at it \u2014 which is\nwhat keeps the archived path byte-identical AND keeps `regime_of`'s six `getattr`s\nout of a hot loop that has no use for them.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, weight: float, volume: float, quantity: int, unit=None) -> float"
    },
    "Inbound/dock.py::DockSpec": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "The configuration of the receiving dock. Frozen, like `PutQueueSpec`: a crew's\nidentity must not drift mid-run, and a sweep varying these has to hash them.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/dock.py::DockSpec.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain.py": {
      "decorators": [],
      "doc": "gain \u2014 the unload-plan evaluator: EXPECTED FUTURE WORK, faithful-to-arm.\n\nThe gain arms (\"Name the policy arms\", 05) order standing trailers by the put + pick\nhours their placements will generate (\"Define the inbound objective\", 10): serve first\nthe load whose DEFERRAL to the next drain's pool costs the most.  One `@ordering` entry\nper family, called once per drain on the frozen ctx; the entries land in BOTH standing\nregistries, so an arm sets `INBOUND_YARD_POLICY` and `INBOUND_DOCK_POLICY` to one name.\n\n    gain(t) = E[put + pick work if t's load places from the deferral pool]\n            \u2212 E[same if it places from the pool NOW]\n    take argmax gain (ties -> the handed order, which is arrival order); virtually\n    consume what the chosen load would take; repeat.\n\n# \u2500\u2500 the deferral pool (the one recorded deviation from the prototype's letter) \u2500\u2500\u2500\u2500\n\nDecision 4 of the objective names the deferral pool \"the NEXT drain's pool (leftovers +\npredicted)\".  The prototype (ticket 04) approximated leftovers as the CURRENT virtual\npool \u2014 which is exact only when the standing loads do not contend, and makes the myopic\narm's gain identically zero (no predicted tier, same pool on both sides: a fake arm,\n`fifo` in disguise).  The build therefore prices leftovers as what the OTHER remaining\ncandidates leave behind: each greedy step first places every remaining load against the\ncurrent pool (the now side), then each candidate's deferral pool excludes every bin some\nOTHER candidate's now-placement took.  Zero extra placements, symmetric across\ncandidates (two identical loads still tie -> FIFO), degenerates to the prototype's shape\nexactly when takes do not overlap \u2014 and gives the myopic arm its real signal, which is\nCONTENTION for today's space.  Recorded on the build ticket (14), the 11 precedent.\n\n# \u2500\u2500 faithful-to-arm: the fidelity seam \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`gain` estimates the bins THIS arm's pool would grant, never an idealized best-bin cost\n(10 decision 5).  The seam is `GainBundle`, built and injected by the DRIVER (the\n`drain_sku` precedent \u2014 the import edges `Inbound -> wh_inventory / wh_placement` are\nforbidden, so the broker holds what it is handed):\n\n  * extremal-D arms (tmin / tmax) get the K-CHEAPEST MERGE structure: per BinKey class\n    the bins sorted ONCE per plan by the arm's own D, consumed by slicing \u2014 proven\n    order-equal to the full pool up to co-occurrence near-ties and ~5x cheaper (04\n    finding 1/2).  The merge orders units by freq x labor_cost (the pool's priority\n    minus the co term \u2014 exactly the proven residue).\n  * selector arms get `pool_factory`: the arm's own pool class, rebuilt per evaluation\n    over COPIES of the aisle bookkeeping (purity: live state is never mutated).\n    rank_random's virtual pool must consume no RNG (the seam purity pin), so it prices\n    by EXPECTATION over the pool's current aisle heads and consumes via a deterministic\n    stand-in selector \u2014 the deviation 04 decided and this docstring records.\n  * `fifo` gets the UNIFORM adapter.  It is the mandatory phase-2 rider (08) and it has\n    no pool at ALL \u2014 `_build_uniform` sets only `place_one`, a uniform draw over the\n    whole tier `_candidates_raw` returns \u2014 so neither adapter above fits.  It needs\n    neither: the draw's expectation is EXACT in closed form, because `_pair_cost` is\n    affine in a bin's (x, y, height multiplier) and sequential draws without\n    replacement leave every unit's bin marginally uniform over the tier as frozen.\n    Price = the tier's mean; consumption = a SEAT COUNT, since which bin a uniform\n    draw got is worth nothing to the next unit.  See `_place_uniform` (ticket 21).\n\nThe bundle reaches the evaluator through an OWNER INDIRECTION \u2014 `for_key(BinKey)`, never\nthe bundle itself.  A single-channel run hands over a `OneOwnerBundle`, whose `for_key`\nignores the key and returns the one bundle it holds, so there is exactly one resolution\nrule and no `if coupled:` in the pricing path; the site dock's `SiteGainBundle` (two arms\nover one mixed trailer, resolved by the key's own regime) is the same protocol answering\ntwo ways.  See `_Evaluator.b`.\n\nA mixed trailer's score is one SUM in hours, with no per-channel coefficient anywhere:\nthe objective is put + pick hours from the shared cost model, so a fulfillment hour and a\nstore hour are worth the same to the site.  That is a CLAIM, not a convention, and\n`Tests/unit/test_gain_plan.py` recovers the exchange rate from priced loads and asserts\nit is 1 \u2014 with a planted per-channel weight to prove the recovery can fail.  The two\nregimes' genuinely different pick costs (`wp.by_regime`) are not a counter-example: both\nare seconds of the same model, scalarized with one divisor rather than two.\n\nExhaustion resolves tiers over the `SpaceView` keys the way `_candidates_raw` does:\nsmallest non-empty fitting tier first, spilling UP (the injected `tier_ranks_for`\ncarries the tier tables).  Only past total exhaustion does a unit price at the worst\nbin of its whole spill chain x 1.5 \u2014 a deterministic, knob-free penalty that makes\n\"seats next drain but not now\" defer correctly and cancels when nothing seats anywhere.\n\n# \u2500\u2500 purity, and where the pieces come from \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nAn entry reads ONLY the frozen ctx: `ctx.space` (the drain's SpaceView) and `ctx.gain`\n(the driver-injected bundle, put there by `YardTransit.freeze_ctx`).  It mutates no\nmanager state, consumes no RNG, and returns a permutation (`bounded_order` raises\notherwise).  Cost-model pricing imports from `wh_kernel` \u2014 a legal edge: put travel is\npaid once at the put crew's speeds; pick pays E[visits] x (travel + per_pick at height),\nE[visits] = quantity / per-visit draw.  No new knobs anywhere in the score; the two\ndays-denominated knobs below belong to the GATE, which never blends them into hours.\n\n# \u2500\u2500 the urgency gate (`gain_gated`) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`gain_forecast` behind a FIFO gate: trailers within `INBOUND_URGENCY_HORIZON_DAYS` of\ncrossing `INBOUND_FEE_THRESHOLD_DAYS` of yard time form the URGENT SET, served FIFO\nahead of everyone; the rest follow the plan, which prices its space AFTER the urgent\nloads consume.  Hours and days never combine into one scalar \u2014 the gate IS the only\nlegal composition (05's two-separate-scores rule).  The horizon spans the poles: 0 ~\npure gain (only already-overdue trailers jump), >= threshold = pure FIFO.\n\n# \u2500\u2500 the futuresight window (`futuresight`) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`gain_forecast` reading `ctx.space.window` \u2014 the declared-unlawful CLAIRVOYANCE\nREFERENCE (\"Define the inbound objective\" decision 6; the term sharpened by \"Decide the\nfuturesight family's place\", 32): a real WMS cannot see undispatched orders, so this arm\nnever enters the recommendable set.\n\nNOT an upper bound, and the difference is publishable rather than pedantic.  The window\nreplaces the demand RATE inside an UNCHANGED greedy, so w=inf prices each unit's future\npicks exactly while the ORDERING stays a heuristic \u2014 it bounds PRICING ACCURACY, never\nachievable gain.  A reference arm may therefore finish BEHIND a lawful one, which is a\nfinding (the binding constraint is the ordering, not the estimate) and not a pathology;\n\"the lawful arm is at the ceiling\" is not a reading this arm supports in either\ndirection.  The window is\nthe next `INBOUND_FUTURESIGHT_BATCHES` script batches' realized demand ('all' = the\noracle w=inf), driver-fed on its own view slot; the batch script is i.i.d. draws from\nthe static rates, so the edge is exactly SAMPLING-NOISE knowledge \u2014 which SKUs land,\nand their counts.  The build reads it where the static rates otherwise stand, the\nPRICING: a SKU absent from the window is not picked in the visible future (put\ntravel only); a present one prices at its realized per-event draw (window total /\nwindow events) with visits capped at the event count, which is what makes w=inf\nhonestly the oracle.  The placement MACHINERY stays the arm's own, static rates and\nall (faithful-to-arm, 10 decision 5: the real pool cannot see the future, so a\nclairvoyant virtual pool would price placements the arm will never make).  The\ndeferral pool is `gain_forecast`'s \u2014 predicted stays one batch deep by charter; the\nwindow never projects bins.",
      "kind": "module",
      "signature": null
    },
    "Inbound/gain.py::_Evaluator": {
      "decorators": [],
      "doc": "One plan's virtual placement state \u2014 built per entry call, dies with it.\n\nOwns the sort-once structures (per-key bins in arm-D order, computed lazily the\nfirst time a key is touched and NEVER rebuilt \u2014 the structure the Tier-1 sabotage\ntest perturbs), the consumed-bin set the greedy advances, and the pricing.  Reads\nthe frozen `SpaceView` and the bundle provider; its only writes are its own\nbookkeeping.\n\n# \u2500\u2500 the owner cursor, and why ONE evaluator can serve two leaves \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`b` is not a field: it resolves `provider.for_key(self._key)`, and `_key` is set in\n`_params` \u2014 which `place_load` calls exactly once per BinKey group, before it\nbranches on the adapter, so every read that follows a group's `_params` sees that\ngroup's arm.  Under `OneOwnerBundle` that is one instance for every key and the\nindirection is inert; under `SiteGainBundle` it is the WHOLE dispatch, at the\ngranularity the loop already has.\n\nThe property this rests on: **a spill chain never crosses regimes.**  `_chain`\nvaries only `size`, holding `handling` / `category` / `unit_category` fixed, and\nregime is read off exactly those three (`inventory_common`: regime is itself a\nBinKey component).  Every other cache here \u2014 `_sorted_now`, `_sorted_pred`, `_wp`,\n`_chain_cache`, `_worst`, `_mom`, `taken` \u2014 is BinKey- or bin-id-keyed.  So two\nowners share this object with zero cross-talk, and nothing here needs to know which\nof them it is serving.  Take that property away and the caches, not the cursor, are\nwhat goes wrong.\n\nOne read precedes any cursor: `place_load` calls `self.b.binkey_of(u)` to key the\ngroups in the first place, with `_key` still None.  That is lawful because the\nthree site-wide fields (`binkey_of`, `tier_ranks_for`, `put_speed`) are the same\nobject for every owner \u2014 a provider that made THEM vary per key would be answering\na different question than this evaluator asks.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/gain.py::_Evaluator.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bundle, space, window_rates=None)"
    },
    "Inbound/gain.py::_Evaluator._avail": {
      "decorators": [],
      "doc": "One placement's view of a tier: the pre-sorted arrays filtered by the\nexcluded set, plus a cursor \u2014 shared across the placement's groups so spill\ninto an already-touched tier continues where consumption left off.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key, excluded, xk, yk, predicted, cache)"
    },
    "Inbound/gain.py::_Evaluator._chain": {
      "decorators": [],
      "doc": "The unit's tier spill chain, smallest fitting tier first \u2014 exactly\n`_candidates_raw`'s walk, over the injected tier tables.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit, own_key)"
    },
    "Inbound/gain.py::_Evaluator._cost_at": {
      "decorators": [],
      "doc": "put travel (paid once) + E[visits] x (pick travel + per_pick at height),\nread at a LOCATION (x, y) with height multiplier `hm`.\nA zero demand rate means the unit is never picked: its pick term is ZERO\n(not quantity visits, the maximum possible reading); put is still paid.\n\nUnder window rates (the futuresight entry) the SKU's REALIZED window demand\nstands where the static rate stands: absent from the window = not picked in\nthe visible future, put only; present = per-event draw total/events, visits\ncapped at the event count (a unit cannot be visited more often than demand\nevents exist \u2014 the cap is what makes w=inf honestly the oracle).\n\n`hm` None reads the bracket step at `y` \u2014 a real bin, the only caller that\nexisted before the uniform adapter.  A VALUE is a tier's MEAN multiplier, and\npassing one is exactly why the expectation below is exact: this expression is\nAFFINE in x, y and hm, so its mean over a bin set equals its value at the\nset's means.  The step function is evaluated per bin when the moments are\ntaken, never on an averaged y (which would be a different, wrong number).\nThe sentinel keeps the bracket walk off the put-only path, where it would be\ncomputed and thrown away for every never-picked unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit, x, y, hm, wp, xk, yk) -> float"
    },
    "Inbound/gain.py::_Evaluator._make_pool": {
      "decorators": [],
      "doc": "The arm's own pool over COPY-ON-WRITE VIEWS of the aisle bookkeeping \u2014 the purity\nrule holds unchanged: a virtual placement may never advance the live dicts.\n\nOne comprehension, per evaluation: the bundle's `aisle_state` says WHICH live\ndicts this arm's pool commits to and `AISLE_VIEWS` says how each is viewed, so\na family that touches more of them costs a driver branch and nothing here.\n\nTHIS IS THE PACKAGE'S HOTTEST LINE, and the reason is the TIER loop below rather than\nanything about yard depth.  Measured on the real driver: 12.59 pool opens per\n`place_load` against a mean of 2.8 candidate trailers, so the opens are driven by the\nspill chain, not by the greedy.  The eager copy walked all 46 live aisles to serve a\npool that touches 1.22 of them -- 9.3 million set-element copies in twenty batches on\none leaf.  A view pays for what it reads.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cands, wp)"
    },
    "Inbound/gain.py::_Evaluator._mean_cost": {
      "decorators": [],
      "doc": "`_cost_at` read at a tier's means \u2014 the exact expectation of `_pair_cost`\nover that tier, by the affineness `_cost_at` documents.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit, mom, wp, xk, yk) -> float"
    },
    "Inbound/gain.py::_Evaluator._moments": {
      "decorators": [],
      "doc": "(mean x, mean y, mean height multiplier) over a tier as FROZEN \u2014 the three\nnumbers the uniform expectation reads, computed once per (tier, source,\nbrackets) and never rebuilt.\n\nOver the FULL frozen tier, never the excluded-filtered one.  The bins another\nload consumed are a uniformly random subset under this arm, so what is left\nhas the same mean in expectation; filtering them out would bias the price by\nexactly the thing the arm does not choose on.  Only the COUNT is filtered\n(`_useat`).  Keyed by the height brackets because the multiplier is the one\nmoment that is regime-specific.  Callers only ask for a tier they know is\nnon-empty, so there is no empty-set division here.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key, predicted: bool, brackets)"
    },
    "Inbound/gain.py::_Evaluator._pair_cost": {
      "decorators": [],
      "doc": "`_cost_at` read at one real bin \u2014 the merge and pool adapters' pricing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit, bin_, wp, xk, yk) -> float"
    },
    "Inbound/gain.py::_Evaluator._params": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit, own_key)"
    },
    "Inbound/gain.py::_Evaluator._place_merge": {
      "decorators": [],
      "doc": "The k-cheapest merge: units in freq x labor_cost priority order take the\nclass's extremal-D available bins in order (the pool's priority minus the\nco-occurrence term \u2014 the proven residue).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, gunits, chain, wp, xk, yk, excluded, predicted, cache)"
    },
    "Inbound/gain.py::_Evaluator._place_pool": {
      "decorators": [],
      "doc": "The pool adapter: per tier, the arm's pool (over copies) serves the units\nin its own precedence; a unit the pool cannot seat spills up the chain.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, gunits, chain, wp, xk, yk, excluded, predicted, cache)"
    },
    "Inbound/gain.py::_Evaluator._place_uniform": {
      "decorators": [],
      "doc": "The uniform adapter: `fifo`'s draw, priced by its EXACT expectation and\nconsumed as a seat count.\n\n`_uniform_assignment` picks uniformly from the whole tier `_candidates_raw`\nhands it, so (a) every unit's bin is marginally uniform over that tier as\nfrozen \u2014 sequential draws WITHOUT replacement leave the marginal untouched,\nwhich is what makes this an expectation rather than an approximation \u2014 and\n(b) which bin a unit got is worth nothing to the next one.  Units therefore\nkeep LOAD order here: a uniform draw has no precedence to sort by (contrast\n`_place_merge`, which sorts by the pool's priority).\n\nThe reported takes come from `alloc`, the sweep's shared block allocator, so\ntwo candidates claim the same bin only once a tier is oversubscribed.  That is\nnot bookkeeping taste: the cost above is identity-blind, so identities exist\nONLY to feed `plan_order`'s leftover model, which unions the OTHER candidates'\ntakes.  Independent uniform draws essentially never collide, so hand every\ncandidate the same front-of-list bins and that union collapses to one load's\nworth \u2014 pricing a whole yard's contention as a single trailer's.  Past the end\nthe allocator WRAPS rather than truncating: an oversubscribed tier then marks\nits bins as shared (`n > 1`) for everyone, instead of leaving the last\ncandidates empty-handed and making a later arrival look starved purely because\nan earlier one drew its block first.\n\nRESIDUE, named: the union is exact while a tier's demand fits in it, and exact\nagain once every bin is claimed twice; between those it under-excludes,\nbecause the leftover model hands a candidate back the bins no OTHER\ncandidate's block happened to name.  Uniform contention wants a COUNT and the\nseam speaks in identities; this is as close as that seam gets.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, gunits, chain, wp, xk, yk, excluded, predicted, cache, alloc)"
    },
    "Inbound/gain.py::_Evaluator._tier_sorted": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key, xk, yk, predicted: bool) -> list"
    },
    "Inbound/gain.py::_Evaluator._unseated_cost": {
      "decorators": [],
      "doc": "Past total exhaustion: worst bin over the WHOLE spill chain (both tiers,\navailability-blind so both gain terms price it identically) x 1.5; zero when\nthe chain offers no bin anywhere, which then cancels out of the gain.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit, chain, wp, xk, yk) -> float"
    },
    "Inbound/gain.py::_Evaluator._useat": {
      "decorators": [],
      "doc": "One placement's view of a tier for the uniform adapter:\n`[seats, predicted weight, the now-list]`.\n\nBoth numbers are fixed at first touch and never recomputed as the tier is\nconsumed.  The units draw from ONE pool \u2014 the empties merged with whatever\npredicted clears the arm may see \u2014 and drawing uniformly from a mixed pool\nleaves its mix proportional in expectation, so the weight does not drift.\nWhat consumption changes is the SEAT COUNT, and that is the whole mechanism:\nit is the only way an inbound ordering can move a uniform arm's cost.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key, excluded, predicted, cache)"
    },
    "Inbound/gain.py::_Evaluator.b": {
      "decorators": [
        "property"
      ],
      "doc": "The arm machinery owning the group being priced (class docstring).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> GainBundle"
    },
    "Inbound/gain.py::_Evaluator.place_load": {
      "decorators": [],
      "doc": "(cost, takes) of placing `units` against the availability that `excluded`\nleaves standing; `predicted` merges the deferral tier in.  Groups by the\nunit's OWN BinKey in load order (the canonical pack order is deterministic),\nspilling up the chain per group.\n\n`alloc` is the sweep's shared block allocator, read by the uniform adapter\nONLY (see `_place_uniform`) and inert for the other two.  None gives this\nplacement its own, which is what the forced prefix and the deferral side\nwant: each of those stands alone, against an `excluded` that already carries\nwhatever went before it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, units, excluded, predicted: bool, *, alloc=None)"
    },
    "Inbound/gain.py::_load_units": {
      "decorators": [],
      "doc": "The candidate's remaining planned load \u2014 pending[taken:], the plans-at-arrival\ncontract.  A candidate with no plan is a wiring error, never a silent zero: in\nproduction every yard/staged trailer was planned (or discarded) before any\nranking runs.",
      "is_async": false,
      "kind": "function",
      "signature": "(trailer) -> list"
    },
    "Inbound/gain.py::_require": {
      "decorators": [],
      "doc": "A gain entry without its machinery is a wiring error, loudly \u2014 a silently\ninert arm would sweep as `fifo` and publish under its own name.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, name: str)"
    },
    "Inbound/gain.py::_window_rates": {
      "decorators": [],
      "doc": "Aggregate the view's window into `{sku: (total qty, events)}` \u2014 the realized\ndemand mass and the number of window batches that carry the SKU.  Once per entry\ncall, so the pricing loop reads a dict, not w of them.",
      "is_async": false,
      "kind": "function",
      "signature": "(window) -> dict"
    },
    "Inbound/gain.py::futuresight": {
      "decorators": [
        "ordering"
      ],
      "doc": "`gain_forecast` reading the window slot (module note) \u2014 the declared-unlawful\nCLAIRVOYANCE reference, never recommendable; it bounds pricing accuracy and NOT\nachievable gain, so it may legitimately finish behind a lawful arm (32).  An EMPTY\nwindow (a run at the end\nof its script) is legal and prices every pick term zero; a MISSING one (None)\nmeans no feed ran, and ranking anyway would silently be `gain_forecast` under\nthis arm's name \u2014 the same fake-arm hazard `_require` exists for.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, ctx) -> list"
    },
    "Inbound/gain.py::gain_forecast": {
      "decorators": [
        "ordering"
      ],
      "doc": "The unload plan with the deferral pool including `predicted` \u2014 the standing\ndemand's projected clears, the next drain's pool gain.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, ctx) -> list"
    },
    "Inbound/gain.py::gain_gated": {
      "decorators": [
        "ordering"
      ],
      "doc": "`gain_forecast` behind the FIFO urgency gate (module note): the URGENT SET is\nserved FIFO ahead of the plan; hours and days meet ONLY here.  A stampless\ntrailer (no clock reached the drain) accrues no yard days and is never urgent.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, ctx) -> list"
    },
    "Inbound/gain.py::gain_myopic": {
      "decorators": [
        "ordering"
      ],
      "doc": "The unload plan over `ctx.space.empties` only \u2014 the predicted tier is\ninvisible, so the arm's whole signal is contention for today's space.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, ctx) -> list"
    },
    "Inbound/gain.py::plan_order": {
      "decorators": [],
      "doc": "10's greedy over the frozen view.  `bundle` is the owner PROVIDER the evaluator\nresolves through (`OneOwnerBundle` on a single-channel run), never a bare\n`GainBundle`.  `forced_prefix` is the urgency gate's FIFO head \u2014 consumed first,\nunscored.  `window_rates` is the futuresight entry's\naggregated window (see `_window_rates`), None for every lawful arm.  `_ev` exists\nONLY for the Tier-1 sabotage test (a pre-warmed evaluator whose sorted structure\nthe test perturbs); production callers never pass it.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, bundle, space, *, predicted: bool, forced_prefix=(), window_rates=None, _ev: _Evaluator | None=None) -> list"
    },
    "Inbound/gain.py::plan_order._load": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(t)"
    },
    "Inbound/gain_bundle.py": {
      "decorators": [],
      "doc": "gain_bundle \u2014 which arm owns a unit, and what the evaluator may ask of it.\n\nOne mixed trailer is priced per unit by the OWNING channel's arm, so something has to answer\n\"whose arm is this?\" before anything can be priced. These three do, through one protocol:\n\n- `GainBundle`      \u2014 everything arm-specific the evaluator needs, driver-built and\n                      driver-injected, because `Inbound -> wh_inventory / wh_placement` are\n                      forbidden edges and the broker holds what it is handed.\n- `OneOwnerBundle`  \u2014 a single-channel run: `for_key` ignores the key and returns the one\n                      bundle it holds.\n- `SiteGainBundle`  \u2014 two arms over one mixed trailer, resolved by the key's own regime.\n\n## Why this is its own module\n\nThe deletion test again, and `gain.py:57-60` states it: deleting the trio brings `if coupled:`\nback into the PRICING PATH, which is exactly what it exists to prevent. There is one\nresolution rule and no coupling branch anywhere below it.",
      "kind": "module",
      "signature": null
    },
    "Inbound/gain_bundle.py::GainBundle": {
      "decorators": [],
      "doc": "Everything arm-specific the evaluator needs, injected by the driver.\n\nThree adapters, and the bundle picks exactly one.  `uniform` selects the uniform\nadapter (`fifo`: no pool, no direction \u2014 the tier's mean and a seat count).  Else\n`pool_factory` None selects the merge adapter (extremal-D family, direction\n`minimize`); otherwise the pool adapter calls `pool_factory(candidates, state, wp)`,\nwhere `state` is a FRESH copy per evaluation of every live aisle dict the arm's pool\ncommits to: `aisle_state` names them (manager attribute -> the live dict) and\n`AISLE_COPIERS` above says how each is copied.  Those names are the seam's whole\nvocabulary, which is what keeps it fixed -- a family that commits to one more dict is\na driver branch plus one more name, never a wider signature here.  `expect_heads` prices\neach unit at the mean over `heads_of(pool)` before consuming (rank_random's no-RNG\nexpectation).  `wp_of` / `binkey_of` / `tier_ranks_for` are handed over because\ntheir home (`wh_inventory`) is a forbidden import \u2014 the broker rule.\n\n`minimize` is INERT under `uniform`: a uniform draw has no extremal direction to\nsort a tier by, so nothing reads it.  It is left at its default rather than\nrefused, because the merge adapter's default is the same value.\n\nThe two days-denominated knobs ride here because the gate entry has no other\nchannel to CONFIG (`inbound_spec()` -> driver -> bundle, the five-seam path).",
      "kind": "class",
      "signature": ""
    },
    "Inbound/gain_bundle.py::GainBundle.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, put_speed, wp_of, binkey_of, tier_ranks_for, minimize: bool=True, pool_factory=None, expect_heads: bool=False, heads_of=None, uniform: bool=False, aisle_state=None, fee_threshold_days: float=DEFAULT_FEE_THRESHOLD_DAYS, urgency_horizon_days: float=DEFAULT_URGENCY_HORIZON_DAYS)"
    },
    "Inbound/gain_bundle.py::OneOwnerBundle": {
      "decorators": [],
      "doc": "The single-channel bundle provider: `for_key` ignores the key and hands back\nthe ONE `GainBundle` it holds \u2014 that same instance, never a copy.\n\nThe evaluator always resolves its arm machinery through `for_key`, so a run with\none channel needs something to resolve THROUGH.  This is it, and it is why there is\nno second path: a bare bundle is refused at `_Evaluator.__init__` rather than\nsniffed for, so the pricing hot path has one shape whether or not the site dock's\ncomposite is what answers.  Byte-identity is structural \u2014 the object the evaluator\nreads is the object the driver built.\n\n`key` is unused BY CONSTRUCTION, not by accident: one owner has one answer.  The\ncursor hands it `None` before the first group is keyed (`_Evaluator.b`), which this\nadapter answers like any other key.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/gain_bundle.py::OneOwnerBundle.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bundle: GainBundle)"
    },
    "Inbound/gain_bundle.py::OneOwnerBundle.fee_threshold_days": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Inbound/gain_bundle.py::OneOwnerBundle.for_key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Inbound/gain_bundle.py::OneOwnerBundle.urgency_horizon_days": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Inbound/gain_bundle.py::SiteGainBundle": {
      "decorators": [],
      "doc": "The site dock's bundle provider: ONE `GainBundle` per channel, dispatched by the\nBinKey's OWN regime \u2014 the composite of 05 decision 1.\n\nA mixed trailer's store units must be priced by the store arm's pool and its\nfulfillment units by the fulfillment arm's.  `place_load` already groups a load by\nBinKey and `regime_of_key` reads the regime straight off that key (regime is itself a\nBinKey component), so the charter's \"per-unit, keyed by owning channel\" needs no\nper-unit loop: the seam is one level coarser and free.  This is the SAME owner\nresolution the site receiving coordinator makes at its step-4 handoff\n(`regime_of(item.unit)`) \u2014 one fact, read through the key the loop already holds.\n\n# \u2500\u2500 two whole bundles, not one bundle with keyed fields \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nEach owner is the WHOLE `GainBundle` `_gain_bundle_for` built for that leaf, from that\nleaf's own `mgr` / `strat` / `sctx`.  `_gain_bundle_for` is called twice and is\notherwise untouched, which is what keeps each half FAITHFUL TO ITS ARM structurally\nrather than by argument: neither is rebuilt, reinterpreted or averaged, so the\ncomposite adds nothing neither arm would do.  Owner-keyed FIELDS on one bundle were\nthe rejected alternative \u2014 `GainBundle.__init__`'s cross-field refusals (an\n`expect_heads` with no pool; a `uniform` carrying one) would silently stop applying,\nbecause the five arm fields would no longer sit on one object to be checked against\neach other.\n\n# \u2500\u2500 what is NOT keyed, and the refusal that makes that safe \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nHalf the bundle is already site-wide: `binkey_of` and `tier_ranks_for` are pure,\n`put_speed` is the one put crew's (`put_crew_spec()` \u2014 one site CONFIG), and the\ngate's `fee_threshold_days` / `urgency_horizon_days` are site CONFIG too.  `wp_of`\nneeds no keying because on the payload this code actually receives the two owners'\ncopies are IDENTICAL FUNCTIONS OF DIFFERENT DATA and the refusal below covers them:\n`workunits` sets `ch_wp.by_regime = None` on every mixed-catalogue channel leaf, so\n`_wp_for(wp, unit)` returns that leaf's own `wp` unchanged and dispatches nothing.\n(An earlier draft of this paragraph said `_wp_for` \"ALREADY dispatches per regime\".\nIt does not, here \u2014 the per-regime table is nulled one layer up, and it is the leaf's\nown regime-pure `wp` that makes the answer right.  A false justification is how the\nnext change goes wrong.)\n\nSo those five are REFUSED at `bind` when two owners disagree, rather than resolved.\nThey cannot differ on a lawful run \u2014 both come from one `inbound_spec()` and one\n`put_crew` record \u2014 which is exactly why a silent first-wins would never be noticed.\nTwo of them are also what makes `for_key(None)` honest: the evaluator resolves with a\nNone cursor to key its groups in the first place, and answering that with the\nfirst-bound owner is only lawful because every owner answers it the same.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/gain_bundle.py::SiteGainBundle.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_bundle.py::SiteGainBundle.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_bundle.py::SiteGainBundle.bind": {
      "decorators": [],
      "doc": "Serve `regime`'s units with `bundle`.  Called once per leaf, at leaf build, by\nthe driver that owns both leaves \u2014 the same shape `SiteReceiving.bind` and\n`PutawayPool.bind` have, and for the same reason: a leaf cannot see the site.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, regime: str, bundle: GainBundle) -> None"
    },
    "Inbound/gain_bundle.py::SiteGainBundle.fee_threshold_days": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Inbound/gain_bundle.py::SiteGainBundle.for_key": {
      "decorators": [],
      "doc": "The arm machinery owning `key`'s group \u2014 `regime_of_key`, never `regime_of`.\n\nA BinKey is a plain tuple, so `regime_of` would fall through every getattr and\nanswer 'store' for a fulfillment key, silently and always in the same direction\n(`Inbound/site_space.py` hit the same trap on the bin side).\n\n`None` is the evaluator's pre-cursor read \u2014 `place_load` calls `binkey_of` to form\nthe groups before any of them is keyed \u2014 and it is answered with the FIRST-BOUND\nowner.  That is exact rather than arbitrary: `bind` refuses owners whose site-wide\nfields differ, so every owner answers those reads identically.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key) -> GainBundle"
    },
    "Inbound/gain_bundle.py::SiteGainBundle.owners": {
      "decorators": [
        "property"
      ],
      "doc": "The bound regimes, in bind order \u2014 observability for the driver and the tests.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Inbound/gain_bundle.py::SiteGainBundle.urgency_horizon_days": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Inbound/gain_bundle.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['GainBundle', 'OneOwnerBundle', 'SiteGainBundle']"
    },
    "Inbound/gain_bundle.py::_site_wide_disagreement": {
      "decorators": [],
      "doc": "`(field, ref value, other value)` for the first SITE-WIDE field two owners answer\ndifferently, or None when they agree on all of them.\n\n`put_speed` is checked on the two paces the evaluator actually READS (`x_pace` /\n`y_pace`), not on the profile object: the driver builds a `SpeedProfile` per leaf from\none payload record, so two equal-valued instances are the lawful case and an identity\ntest here would refuse every coupled run.",
      "is_async": false,
      "kind": "function",
      "signature": "(ref: GainBundle, other: GainBundle)"
    },
    "Inbound/gain_cow.py": {
      "decorators": [],
      "doc": "gain_cow \u2014 how a VIRTUAL placement sees the aisle books without advancing them.\n\nThe inbound gain evaluator prices a placement by actually making it, in a pool opened over\nthe arm's own builder. A virtual placement must therefore never touch the LIVE bookkeeping,\nand what \"a copy\" IS belongs to the DICT rather than to the arm reading it \u2014 two families\nsharing a dict share its copier. So the shapes are declared here, once.\n\n## Why this is its own module\n\nIt was ~200 lines inside a 1,410-line `gain.py` that is really four things: this, the owner\nresolution, the evaluator, and the entries. The deletion test passes loudly \u2014 removing it\nbrings back a MEASURED 37.8x copy blow-up at every `_make_pool` \u2014 it simply is not the same\nmodule as the evaluator.\n\n## READ THE TABLES THROUGH THIS MODULE, never through a bare imported name\n\n`Tests/unit/test_gain_cow_equivalence.py` REBINDS `AISLE_VIEWS` to sabotage the views, and\nits own docstring calls that the saving throw: without it the file passes if the views are\nquietly pointed back at the eager copiers. A reader that did `from Inbound.gain_cow import\nAISLE_VIEWS` would bind the name at import and never see the rebinding \u2014 the sabotage would\nstop sabotaging, silently, and the file would go on passing.\n\nThat is not hypothetical: `test_gain_bundle_labor_families.py` records it happening once\nalready, when `_make_pool` moved from `AISLE_COPIERS` to `AISLE_VIEWS`. So `_Evaluator` reads\n`_cow.AISLE_VIEWS[n]`, and `gain.py` does not re-export either table.",
      "kind": "module",
      "signature": null
    },
    "Inbound/gain_cow.py::AISLE_COPIERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'aisle_sku_sets': _copy_of_sets, 'aisle_idx_sets': _copy_of_sets, 'aisle_demand_sum': _copy_of_floats, 'aisle_pick_load_sum': _copy_of_floats, 'aisle_vol_sum': _copy_of_floats, 'aisle_member_pos':..."
    },
    "Inbound/gain_cow.py::AISLE_VIEWS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'aisle_sku_sets': _CowSets, 'aisle_idx_sets': _CowSets, 'aisle_demand_sum': _CowFloats, 'aisle_pick_load_sum': _CowFloats, 'aisle_vol_sum': _CowFloats, 'aisle_member_pos': _CowListsByKey}"
    },
    "Inbound/gain_cow.py::_CowFloats": {
      "decorators": [],
      "doc": "`{aisle: float}` -- and floats need NO copy at all.\n\nA float is immutable, so a read falls straight through to the live dict; only a write\nneeds an overlay.  `_ads[aid] += fq` is `__getitem__` then `__setitem__`, and the write\nlands in the overlay rather than on the manager's dict.\n\nA miss CREATES the entry at 0.0, matching `defaultdict(float)` -- the shape this\nreplaces -- so `len()` and iteration order are unchanged.",
      "kind": "class",
      "signature": "(_CowView)"
    },
    "Inbound/gain_cow.py::_CowFloats.__getitem__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, k)"
    },
    "Inbound/gain_cow.py::_CowListsByKey": {
      "decorators": [],
      "doc": "`{aisle: {sku_idx: [x_phys, ...]}}` -- the minlabor shape.\n\nTwo levels down, and the inner lists are appended to (`_amp[aid][idx].append(...)`), so\na shallow copy would hand the pool the live inner list.  An access materializes that one\naisle's whole inner mapping, which is still one of forty-six.",
      "kind": "class",
      "signature": "(_CowView)"
    },
    "Inbound/gain_cow.py::_CowListsByKey.__getitem__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, k)"
    },
    "Inbound/gain_cow.py::_CowSets": {
      "decorators": [],
      "doc": "`{aisle: set}`.\n\nUnlike floats the value is MUTABLE and the caller does `d[aid].add(sku)` -- and\n`__getitem__` cannot tell that from the `sku in d[aid]` two lines above it.  So an\naccess materializes that ONE aisle's set.  Bounded by the aisles a pool touches\n(measured 1.22), not by the warehouse (46).",
      "kind": "class",
      "signature": "(_CowView)"
    },
    "Inbound/gain_cow.py::_CowSets.__getitem__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, k)"
    },
    "Inbound/gain_cow.py::_CowView": {
      "decorators": [],
      "doc": "Copy-on-write over one of the manager's `{aisle: ...}` dicts.\n\nA virtual placement must never advance the live bookkeeping, and the original way to\nguarantee that was to hand the pool an EAGER copy of the whole dict.  The measurement in\nfront of this block is why that stopped: a pool's candidates are the free bins of ONE\nBinKey, living in one or two aisles, while the copy walked all forty-six -- 9.3M set\nentries copied per run, at 12.59 pool opens per placement.\n\nA view pays for the aisles the pool actually touches instead.  Reads fall through to the\nlive dict; the first access to an aisle whose value is MUTABLE materializes that one\naisle into an overlay, and every write lands in the overlay.  The purity rule is\nunchanged -- it is the price that moved.\n\n# -- what a subclass owns, and what it does not --------------------------------------\n\n`__getitem__` ONLY.  How a miss materializes IS the difference between the shapes: a\nfloat needs no copy at all (it is immutable, so a read can fall straight through), a set\nneeds its own set, and the minlabor shape needs a whole inner mapping.  The remaining\neight methods never differed, and were three byte-identical copies until this base\nexisted; `Tests/unit/test_gain_cow_protocol.py` asserts they stay one implementation and\nthat `__getitem__` stays three.\n\n# -- where the laziness stops, deliberately -------------------------------------------\n\n`values()` and `items()` materialize EVERY aisle, because a lazy one would hand out the\nLIVE container and the caller could mutate it -- the precise failure the eager copy\nexisted to prevent.  `_RankedAssignPool.__init__` builds\n`set().union(*aisle_idx_sets.values())`, so `rank_random` (its only phase-2 user) gets\ncorrectness and no win.  Every other ranked family reads by key and gets both.\n\nSlotted, and the subclasses re-declare `__slots__ = ()` to stay that way: a view is\nopened per pool, and a stray attribute would be per-pool heap nobody frees.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/gain_cow.py::_CowView.__contains__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, k)"
    },
    "Inbound/gain_cow.py::_CowView.__getitem__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, k)"
    },
    "Inbound/gain_cow.py::_CowView.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, live)"
    },
    "Inbound/gain_cow.py::_CowView.__iter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_cow.py::_CowView.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_cow.py::_CowView.__setitem__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, k, v)"
    },
    "Inbound/gain_cow.py::_CowView.get": {
      "decorators": [],
      "doc": "Read WITHOUT materializing -- which is the whole reason a pool ever calls it.\n\nThe fall-through hands back the LIVE value for a key the overlay has not taken.\nThat is read-only by contract: a caller that mutates what `get` returned writes\ninto the manager's own dict.  Callers that intend to write use `__getitem__`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, k, default=None)"
    },
    "Inbound/gain_cow.py::_CowView.items": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_cow.py::_CowView.keys": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_cow.py::_CowView.values": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/gain_cow.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['AISLE_COPIERS', 'AISLE_VIEWS']"
    },
    "Inbound/gain_cow.py::_copy_of_floats": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(d)"
    },
    "Inbound/gain_cow.py::_copy_of_lists_by_key": {
      "decorators": [],
      "doc": "Two levels down: {aisle: {sku_idx: [x_phys, ...]}}, and the lists are appended to.",
      "is_async": false,
      "kind": "function",
      "signature": "(d)"
    },
    "Inbound/gain_cow.py::_copy_of_sets": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(d)"
    },
    "Inbound/pack.py": {
      "decorators": [],
      "doc": "pack \u2014 what arrived, how it packed, and what a single delivery would have produced.\n\n(`Warehouse/operations/inbound.py` until the Inbound package split; `LoadPlan` becomes\n`PackPlan` in the convention pass, not before.)\n\n# \u2500\u2500 why packing belongs to the ARRIVAL, not to the order \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`viable_storage_units(order, quantity)` has always taken a quantity, so the packing has\nalways been a function of how much shows up at once. Nothing named that, so nothing could\nask the interesting question: *an order that would be palletized if it came in all at once,\nbut arrives split across two trailers and lands as two singleton packs instead.*\n\nThat is not hypothetical. Measured over the first twelve orders of a planned 200-SKU\ncatalogue, splitting each delivery in half changed the packing for **five of them**:\n\n    sku  52   eq   8    whole  3 pallets    split in two  4 pallets\n    sku 178   eq  14    whole  5 pallets    split in two  6 pallets\n    sku  21   eq 137    whole 34 pallets + 26 singletons  split  40 pallets + 0 singletons\n    sku  51   eq   3    whole  2 pallets    split in two  3 pallets\n    sku  77   eq  44    whole 27 pallets    split in two 24 pallets\n\n**Both directions occur, and they have different causes.** Default packing \u2014 fill pallets,\nroute the remainder to a singleton \u2014 is monotone: splitting it can only produce the same\nunits or more. The negative cases come from `stock_plan`, which overrides the packer with a\nrun-length list of `(is_singleton, per, count)` slots filled from the front. A whole arrival\nthat runs off the end of the pallet slots spills into the plan's singleton tail; each half of\nthe same arrival stops before that tail and stays on pallets. So on a **planned** SKU \u2014 which\nis most of a generated catalogue \u2014 \"splitting is worse\" is not a safe assumption, and the\nnumber has to be looked at rather than signed in advance.\n\n# \u2500\u2500 what this module adds, and what it deliberately does not \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nIt adds a NAME and a RECORD. `receive()` calls `viable_storage_units` unchanged and wraps the\nresult in a `LoadPlan` that knows what it received, what it packed into, and \u2014 on demand \u2014\nwhat the same order would have packed into arriving whole. Nothing about the packing\nbehaviour moves.\n\nIt does NOT model trailers, docks, or how a shipment gets split. That is the caller's\nbusiness: `receive_all()` takes the split as a list of quantities and says nothing about\nwhere the list came from. The point of the seam is that a trailer model can be written\nagainst it without touching the packer.\n\n# \u2500\u2500 the counterfactual is lazy \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`unsplit()` re-packs at the whole quantity, which costs as much as the packing itself. It is\na method rather than a field so the ordinary receive path pays nothing for a comparison\nnobody asked for.",
      "kind": "module",
      "signature": null
    },
    "Inbound/pack.py::FULFILLMENT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fulfillment'"
    },
    "Inbound/pack.py::LoadPlan": {
      "decorators": [],
      "doc": "One inbound delivery: what arrived, and the storage units it became.\n\nImmutable in the sense that matters -- the units are packed once at receipt and the plan\nis the record of that. `whole_qty` is the quantity this delivery WOULD have been had the\nshipment not been split, and it is what makes `unsplit()` answerable; it equals\n`received` for a delivery that was not split.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/pack.py::LoadPlan.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, order, received: int, units, whole_qty: int | None=None, index: int=0, of: int=1)"
    },
    "Inbound/pack.py::LoadPlan.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/pack.py::LoadPlan.packed_qty": {
      "decorators": [
        "property"
      ],
      "doc": "Units of MERCHANDISE packed. Equals `received` unless the packer could not place\nsome of it, which is worth being able to see rather than assume away.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/pack.py::LoadPlan.sku": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/pack.py::LoadPlan.split_penalty": {
      "decorators": [],
      "doc": "Extra storage units this delivery pattern cost, against arriving whole.\n\nNEGATIVE when splitting helped -- which happens on any SKU carrying a `stock_plan`,\nand is the reason this returns a signed number rather than a count of regret. sku 21\nabove packs into 60 units whole and 40 split, because each half stops short of the\nplan's singleton tail.\n\nMeaningful only summed over a whole shipment: one delivery of a split shipment is\nbeing compared against the whole thing, so its individual value is not interesting.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/pack.py::LoadPlan.tier_mix": {
      "decorators": [
        "property"
      ],
      "doc": "`{unit_category: count}` for this delivery.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Inbound/pack.py::LoadPlan.unit_count": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/pack.py::LoadPlan.unsplit": {
      "decorators": [],
      "doc": "The plan the WHOLE shipment would have produced, arriving at once.\n\nRe-packs at `whole_qty`, which costs as much as the original packing -- hence a\nmethod and not a field. For a delivery that was not split this returns an equivalent\nplan, so a caller can compare unconditionally.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> 'LoadPlan'"
    },
    "Inbound/pack.py::LoadPlan.was_split": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Inbound/pack.py::PALLET": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'pallet'"
    },
    "Inbound/pack.py::SINGLETON": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'singleton'"
    },
    "Inbound/pack.py::packer": {
      "decorators": [],
      "doc": "The injectable packer the Inventory Manager's broker seam consumes.\n\nOne callable covering both arrival shapes \u2014 `deliveries` is the split (or None for one\nwhole delivery), exactly the contract `_release_to_stock` has always had. The driver\nbinds this as `mgr.packer`; the manager's own default packs the identical unit stream\nwithout producing `LoadPlan`s, so binding this changes RECORDS, never units.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, quantity: int, deliveries=None) -> list"
    },
    "Inbound/pack.py::receive": {
      "decorators": [],
      "doc": "Pack one inbound delivery. The packer is unchanged; this names the result.\n\n`whole_qty` is the shipment's total when this is one delivery of several, and is what\nthe counterfactual is measured against.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, quantity: int, *, whole_qty: int | None=None, index: int=0, of: int=1) -> LoadPlan"
    },
    "Inbound/pack.py::receive_all": {
      "decorators": [],
      "doc": "Pack a shipment that arrives as several deliveries.\n\n`deliveries` is a list of quantities -- how the shipment was split, and nothing about\nWHY. Trailers, docks and load planning are the caller's business; this seam exists so\nthey can be written without touching the packer.\n\nEvery returned plan carries the shipment total, so `split_penalty()` is answerable on\neach and meaningful when summed.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, deliveries) -> list"
    },
    "Inbound/pack.py::shipment_penalty": {
      "decorators": [],
      "doc": "Extra storage units a whole shipment's split cost, against arriving at once.\n\nThe number `split_penalty()` is a component of. Positive means the split produced more,\nsmaller packs -- more put-away trips for the same merchandise. Negative means it\nproduced fewer, which is real and happens when the halves fall on cleaner boundaries.",
      "is_async": false,
      "kind": "function",
      "signature": "(plans) -> int"
    },
    "Inbound/priorities.py": {
      "decorators": [],
      "doc": "priorities \u2014 the dock's policy registries, on `put_policy`'s proven contract.\n\n# \u2500\u2500 the registries, and the split the standing yard made real \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nGLOBAL ranks TRAILERS for the v1 drain-everything path, where one ranking serves both dock\nmoments.  The STANDING YARD (`YardTransit`) splits that decision in two, each with its own\nregistry and knob: YARD (`INBOUND_YARD_POLICY`) answers \"a freed door goes to which\nstanding trailer\"; DOCK (`INBOUND_DOCK_POLICY`) answers \"the crew works which staged\ntrailer\" \u2014 under door-team allocation that is a worker-ALLOCATION preference, decisive when\nworkers < staged trailers and graded otherwise.  Both additive, both seeded `'fifo'`; the\nGLOBAL registry and its knob stay untouched and are simply unread in standing mode.  LOCAL\nranks a trailer's LOAD PALLETS \u2014 what a crew actually pulls; on a single-SKU pallet that\ndegenerates to the SKU lot \u2014 and is shared by both paths.\n\nThe contract is `put_policy`'s, generalized ONE step (\"Define the inbound objective\",\ndecision 4): an entry is EITHER a pure key function \u2014 HIGHER served first, over\n(candidate, ctx), the degenerate case every seeded `'fifo'` entry rides \u2014 OR an ORDERING\nfunction `(candidates, ctx) -> ordered list`, tagged `@ordering`, for policies whose\nwhole proposal is the value (a gain plan that virtually consumes space as it picks has no\nper-candidate key).  `bounded_order` resolves either kind, so the registries, the\naccessors, `transit.py` and the manager are all kind-blind.  Both kinds are PURE over a\n`ctx` FROZEN for the drain \u2014 computed once, reused across every decision in it; an entry\nmutates no manager state (an ordering entry is handed a COPY of its candidates) and\nconsumes no RNG.  `ctx` is also where the warehouse-space signal arrives \u2014 the\nreserved named view is now REAL: `ctx.space` carries the drain's frozen\n`Inbound.space.SpaceView` whenever the standing yard runs (always on with the flag), None\notherwise; no signature change, and every seeded 'fifo' key ignores it.  A LOCAL policy may\nreorder WORK but never pack composition \u2014 the pack plan is fixed per trailer.\n\nThere is deliberately NO `inherit` entry: at the dock there is no placement pool whose\nprecedence could stand in, so the default is plain `'fifo'`.\n\n# \u2500\u2500 the ordering bound \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`bounded_order` is the dock's `k_cap` analog, shipped now by decision: the policy PROPOSES\nan order, the bound limits how far it may depart from arrival order \u2014 \"of the `bound`\nlongest-waiting trailers, take the best\".  Denominated in TRAILERS; None = unbounded.  With\n`'fifo'` (every seeded policy) any bound is inert, which is what keeps v1 byte-identical\nwhile the interface is real.  The ONE bound covers the global ranking and both standing\nrankings alike \u2014 a per-registry bound waits for an arm that needs them separate.\n\nWith an ORDERING entry the bound composes BOUND-FIRST \u2014 bound the candidate set, then\norder: the entry is called ONCE per drain, on the `bound` longest-waiting candidates\nonly, and the remainder follows its proposal in arrival order.  Never re-run per pick \u2014 a\nplan is one call, however deep the drain consumes it.  Inert exactly when the proposal is\narrival order, the same degenerate case that keeps `'fifo'` keys inert.",
      "kind": "module",
      "signature": null
    },
    "Inbound/priorities.py::DockContext": {
      "decorators": [],
      "doc": "Everything a priority key may see, frozen once per drain.\n\nKeys must be pure functions of (candidate, ctx); anything that changes as work happens\nwould be read stale by design \u2014 the same rule, for the same one-key-per-drain cost\nreason, as `put_policy`.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/priorities.py::DockContext.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, doors: int, free_doors: int, yard_depth: int)"
    },
    "Inbound/priorities.py::_fifo_pallet": {
      "decorators": [],
      "doc": "Load order: the pallet loaded first is pulled first.  The candidate is\n(position_index, LoadPallet) so a key needs no back-pointer to its trailer.",
      "is_async": false,
      "kind": "function",
      "signature": "(indexed_pallet, ctx) -> float"
    },
    "Inbound/priorities.py::_fifo_standing": {
      "decorators": [],
      "doc": "Longest-STANDING first: earliest yard arrival wins.\n\nThe charter's order \u2014 arrival stamp, `seq` as tiebreak \u2014 needs only the stamp here:\nboth standing collections (the yard, the staged set) are maintained in (stamp, seq)\norder, and `bounded_order`'s sort is stable, so equal stamps fall back to `seq` for\nfree.  A trailer with no stamp (no clock reached the drain \u2014 bare test managers)\nranks oldest rather than crashing or silently sorting last: it has been standing\nsince before anything the clock can see.",
      "is_async": false,
      "kind": "function",
      "signature": "(trailer, ctx) -> float"
    },
    "Inbound/priorities.py::_fifo_trailer": {
      "decorators": [],
      "doc": "Strict arrival order: the longest-waiting trailer first.",
      "is_async": false,
      "kind": "function",
      "signature": "(trailer, ctx) -> float"
    },
    "Inbound/priorities.py::_lifo_standing": {
      "decorators": [],
      "doc": "Newest-STANDING first \u2014 the adversarial control (\"Name the policy arms\", 05):\nanchors the fee axis's bad end and null-checks whether trailer ordering moves\nlabor at all.  The exact sign-flip of `_fifo_standing` on stamped trailers; a\nstampless trailer (no clock reached the drain) is the OLDEST standing, so here it\nranks last \u2014 symmetric with fifo ranking it first.  Equal stamps keep arrival\norder (the stable sort), which only ever matters to zero-lead test rigs.",
      "is_async": false,
      "kind": "function",
      "signature": "(trailer, ctx) -> float"
    },
    "Inbound/priorities.py::bounded_order": {
      "decorators": [],
      "doc": "The entry's order, bounded.  A KEY entry: repeatedly take the best-keyed of the\n`bound` longest-waiting remaining candidates (arrival order = input order) \u2014 stable\non key ties, like the put drain.  An `@ordering` entry: bound FIRST, then order \u2014\none call, on a copy of the `bound` longest-waiting candidates, the remainder\nfollowing in arrival order.  None = the entry's order stands unbounded, either\nkind.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates: list, entry, ctx, bound: int | None) -> list"
    },
    "Inbound/priorities.py::dock_key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(policy: str)"
    },
    "Inbound/priorities.py::local_key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(policy: str)"
    },
    "Inbound/priorities.py::ordering": {
      "decorators": [],
      "doc": "Tag `fn` as an ORDERING entry: `(candidates, ctx) -> ordered list`.\n\nThe registries hold both kinds; this attribute \u2014 probed via getattr, the `STANDING`\nidiom \u2014 is how `bounded_order` tells a whole-order proposal from a per-candidate\nkey.  The return must be a PERMUTATION of the candidates handed in (the same\nobjects): `bounded_order` raises on anything else, because a silently dropped\ntrailer would stand in the yard forever and a duplicated one would stage twice \u2014\nneither with an error.",
      "is_async": false,
      "kind": "function",
      "signature": "(fn)"
    },
    "Inbound/priorities.py::view_needs": {
      "decorators": [],
      "doc": "The `SpaceView` fields `policy` reads \u2014 KeyError if it never declared any.\n\nLoud like `yard_key`, and for the same reason: an undeclared policy answering\n\"reads nothing\" would pass every composability gate by default.",
      "is_async": false,
      "kind": "function",
      "signature": "(policy: str) -> frozenset"
    },
    "Inbound/priorities.py::yard_key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(policy: str)"
    },
    "Inbound/putaway_pool.py": {
      "decorators": [],
      "doc": "putaway_pool \u2014 one site pool of putters over two channels' segregated volume.\n\n`PutawayPool` is the site's put-away crew: ONE `list[float]` of worker clocks, bound to\nboth channels' put queues, so `crew_clock.charge` greedy-books every put to whichever\nputter is free earliest ACROSS BOTH CHANNELS.  A putter genuinely takes work from either\nqueue, with no new scheduling vocabulary \u2014 the sharing is identity, not arithmetic.\n\nSegregation survives untouched.  The two managers keep their own queues, carts, geometries\nand `_put_seconds`; a cart carries one channel's packs only.  **Only the people are\nshared**, which is exactly the charter's \"one site pool over segregated volume\".\n\n# \u2500\u2500 why it lives here, and cannot live anywhere else \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`wh_operations -> wh_inventory` is forbidden (`context/architecture.yml:88`), so the\npackage where crews live cannot drive a manager's drain; `warehouse_core -> inbound` and\n`wh_inventory -> inbound` are forbidden in BOTH directions (`:102-103`).  `Inbound/` is\ntherefore the only package that may sit above two managers \u2014 the same argument that put\n`SiteReceiving` here.\n\nSo this module never imports a manager.  It DUCK-TYPES its leaves and reaches each one\nthrough exactly three public ports:\n\n    leaf.drain_putaway(deadline, charge_cut=False) -> None\n        Phase 5 of a completed batch, promoted from `_drain_putaway` for this caller.\n    leaf.count_put_cut(deadline) -> None\n        What the whistle stopped, charged ONCE per site day against the FULL day.\n    leaf.drain_putaway_records(reset_clocks=False)\n        Not called here \u2014 the driver calls it and reports the result to `note_records`,\n        because only the driver knows the batch epoch the rows are stamped from.\n\n# \u2500\u2500 the three things this owns that nobody else can \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n**The day's division.**  With shared clocks and a fixed leaf order, the first leaf drains,\npushes the clocks, and the second starts from wherever the first left them \u2014 so the second\nwould absorb every cut, every day, as a pure artefact of loop order.  That bias would look\nexactly like a finding.  Each leaf therefore drains against a CUMULATIVE sub-deadline at\nits own share of the day, and then every leaf re-drains against the whole day: the residue\npass IS the charter's \"a channel that finishes early releases labour to the other\", and it\nruns in both directions.  Earlier-to-later is free (the cumulative deadline hands the later\nleaf whatever the earlier one did not spend); later-to-earlier is what the residue pass is\nfor.\n\n**The reset.**  `drain_putaway_records()` resets every queue's clocks, and on a shared list\none leaf's drain resetting the pool before the other has recorded is a silent, plausible-\nlooking zeroing \u2014 every subsequent row wrong and nothing raising.  So on a coupled run the\nmanager drain stops resetting (`reset_clocks=False`) and the pool resets the one list once\nper site day, after BOTH leaves have handed their records over.  Giving that trap a single\nowner is the main argument for this module existing at all.\n\n**The site `put_clock`.**  A shared clock list cannot carry two epochs: `put_clock` is the\nabsolute carry `crew_start` is measured from, so two carries over one list would stamp the\nsame worker's same second at two different absolute instants.  Under one put crew there is\none put carry, based at the SITE DAY START \u2014 `max(day.start_of(i), put_clock)` \u2014 never at\neither leaf's `batch_start_time`, which would idle the site's putters whenever a pick crew\noverran its day and misattribute a picking overrun to put-away's cut.\n\n# \u2500\u2500 what is NOT here \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nWithin-day interleaving.  The two leaves each run their day as today and draw from one pool\nof crew-seconds; a putter is never dispatched between the channels mid-batch.  An\nevent-driven cadence is out of scope, inherited.\n\nThe band.  What the coupled put clause READS \u2014 one site number against the summed\nexpectation, retiring `expected_utilization`'s \"single-channel leaves undercut rho\" caveat\nfor put \u2014 needs the site analysis stage and belongs to \"Re-scope the analysis surfaces\".",
      "kind": "module",
      "signature": null
    },
    "Inbound/putaway_pool.py::PutawayPool": {
      "decorators": [],
      "doc": "The site's putters: one clock list, one uid block, one carry, N leaves.\n\nConstructed by the driver at UNIT scope, where the crew is sized from the staffing\nrecord, and bound onto each leaf it serves (`mgr.putaway_pool`) \u2014 the same injection\nprecedent as `enable_receiving`, `mgr.packer` and `mgr.receiving`: the broker holds\nwhat it is handed, and nothing under `Warehouse/` imports this package.\n\n`clocks` and `workers` are parallel and index-aligned: clock `w` is worker `w`, which\nis what lets `work_events.put_rows` name the same person in both channels' DBs.  The\ndriver mints both, exactly as it mints every other crew \u2014 this holds them.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/putaway_pool.py::PutawayPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, clocks: list, workers: tuple, expected_utilization: dict, day, *, channels: tuple, releases_per_day, cut_at_day_end: bool)"
    },
    "Inbound/putaway_pool.py::PutawayPool.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/putaway_pool.py::PutawayPool._channel_of": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> str"
    },
    "Inbound/putaway_pool.py::PutawayPool.bind": {
      "decorators": [],
      "doc": "Serve `channel`'s leaf.  Call once per leaf, before the batch loop.\n\nThe leaf's queues must already be bound to `self.clocks`\n(`enable_putaway_timing(..., clocks=pool.clocks)`) \u2014 that binding is what makes\nthe pool real, and this only records who the driver will be stepping.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf, channel: str) -> None"
    },
    "Inbound/putaway_pool.py::PutawayPool.drain": {
      "decorators": [],
      "doc": "Phase 5 for `leaf`, against its share of the day \u2014 and, on the last leaf of the\nday, the residue pass and the cut for every leaf.\n\nReached from `Inventory_Manager.check_reorders`, which routes phase 5 here when a\npool is bound exactly as it routes phase 4 to `mgr.receiving`.  `deadline` is the\nwhole day's remainder, the value `open_batch` handed the driver; it is CHECKED\nagainst the pool's own rather than trusted, because a leaf drained against a\ndifferent day than the one the clocks are running on would place work nobody has\nthe hours for, and every row of it would look ordinary.\n\nNEITHER PASS CHARGES THE CUT.  `cut` is a LEVEL \u2014 it re-counts the standing queue\nevery time it is charged \u2014 so two charges inside one batch inflate it where no\ndownstream \"count the non-zero batches\" rule can undo it.  Both drains pass\n`charge_cut=False` and the cut is counted once, below, against the FULL day.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf, deadline) -> None"
    },
    "Inbound/putaway_pool.py::PutawayPool.note_records": {
      "decorators": [],
      "doc": "`leaf` has handed its records over and stamped them; `finish` is the absolute\ninstant its last put ended, or None when it recorded nothing.\n\nCalled once per leaf per site day, on BOTH the picked and the skipped path \u2014 a\nbatch that produced no tasks still ran `check_reorders` and may have put hundreds\nof units away.  When the last leaf reports, the shared clocks reset and the site\ncarry is committed: both halves of \"the pool resets once per site day, after both\nleaves have drained\".\n\nThe carry moves only when somebody actually worked.  No records anywhere means the\ncrew is where it was, exactly as an unpooled leaf leaves `put_clock` alone.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf, finish) -> None"
    },
    "Inbound/putaway_pool.py::PutawayPool.open_batch": {
      "decorators": [],
      "doc": "`(base, deadline)` for site day `index`: the put crew's absolute epoch and the\nwhistle as a REMAINDER on the batch-local clocks.\n\nIDEMPOTENT PER INDEX, and that is the contract: every leaf asks, and every leaf\nmust get the same answer, because one clock list cannot carry two epochs.  The\nfirst call computes; the rest read.\n\n`base` is `max(day.start_of(index), put_clock)` \u2014 the putters start at shift start\nand work what is on the dock, or they carry on from where yesterday's overrun left\nthem.  `deadline` is the rest of the day CONTAINING that base, so base + deadline\nis the day's end however far the carry has run.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int) -> tuple"
    },
    "Inbound/putaway_pool.py::_cumulative_shares": {
      "decorators": [],
      "doc": "Each channel's share of the day, stacked in `channels` order.\n\n`expected_utilization[n]` is `load_n / (crew x S)` by construction, so crew and day\ncancel and the share is just `exp_n / sum(exp)` \u2014 the split needs no new record field.\nA single-channel catalogue gets 1.0 and the residue pass is a no-op, so the degenerate\ncase is correct for free.\n\nStacked (0.4, 1.0) rather than flat (0.4, 0.6) because the clocks are SHARED: the\nsecond leaf starts from wherever the first left them, so its whistle is a point on the\nsame day, not a length of its own.  The last channel's is 1.0 by construction, which\nis what hands it everything the earlier ones did not spend.\n\nA site expecting NO put work at all gives everyone the whole day: nobody should be\nstarved by a script that said nothing was coming, and with no expectation to divide\nthere is no fairer division than none.",
      "is_async": false,
      "kind": "function",
      "signature": "(expected_utilization: dict, channels: tuple) -> dict"
    },
    "Inbound/receiving.py": {
      "decorators": [],
      "doc": "receiving \u2014 the site's receiving coordinator: one dock, one yard, N leaves.\n\n`SiteReceiving` owns every DECISION of a standing drain and holds no merchandise.  It\nsits ABOVE the inventory managers rather than inside one, which is what lets one dock\nserve two channels without either manager knowing the other exists.\n\n# \u2500\u2500 why it lives here, and cannot live anywhere else \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`wh_operations -> wh_inventory` is forbidden (`context/architecture.yml:88`), so the\npackage where crews live cannot drive a manager's drain; `warehouse_core -> inbound` and\n`wh_inventory -> inbound` are forbidden in BOTH directions (`:102-103`).  `Inbound/` is\ntherefore the only package that may sit above two managers, which is why the put pool\nlands here too.\n\nThe consequence is that this module can never import a manager.  It DUCK-TYPES its\nleaves and reaches each one through exactly three public ports:\n\n    leaf.plan_lot(sku, qty, source) -> (plans, items)\n        Step 1's per-lot body: resolve `_originals`, apply `inbound_split`, pack, stamp,\n        and debit the pack shortfall against the remainder ledger.\n    leaf.accept(item, dur) -> None\n        Step 4's per-unit body: the deferred->queued flip, `_recv_seconds`, `_queue`.\n    leaf.owned_skus() -> iterable[int]\n        The catalogue partition this leaf is responsible for.  Read ONCE, at bind time,\n        to build the `{sku: leaf}` owner dict \u2014 never during a drain.\n\nEverything else a drain touches is the coordinator's own: the `Dock`, the `YardTransit`,\nthe ctx freeze, the door fill, the unload and the handoff ORDER.\n\n`receive` is the composition of those, and each step is a method rather than a comment\nbanner inside one \u2014 the order is the design, so it is worth being able to read the order\non its own:\n\n    _check_drain        the three refusals: no leaf, a partial site drain, the wrong day\n    _drain_epoch        ONE DRAIN IS ONE INSTANT -- the site epoch every decision reads\n    _plan_arrivals      STEP 1  plans-at-arrival, per contiguous lot, by the lot's owner\n    _fill_doors         STEP 2  the door fill (NOT budget-gated) + the frozen dock ranking\n    _unload_merged      STEP 3  the pooled gang -- v1's physics, the lockstep pin\n    _unload_split       STEP 3  the dealt door teams -- the standing model's own physics\n    _hand_off           STEP 4  the canonical handoff + the per-unit ledger flip\n    _count_remainders   what the whistle cost, in storage units, counted once\n\nHOW A MIXED TRAILER FINDS ITS WAY HOME \u2014 two routes, because they answer two different\nquestions:\n\n  * STEP 1 holds a bare `(sku, qty)` lot; no unit exists yet, so it resolves the owner\n    from the `{sku: leaf}` dict built at bind time.  An overlapping sku is REFUSED there:\n    it would mean the channel filter let one order into both leaves, and the symptom\n    would be merchandise silently delivered to the wrong warehouse.\n  * STEP 4 holds a `PutawayItem`, so it asks `regime_of(item.unit)` (wh_kernel,\n    dependency-free, single-valued per entity).  No owner field is carried, stamped or\n    mapped at the unit level \u2014 `regime_of` already answers it.\n\nThe two are CROSS-CHECKED at the handoff.  A unit whose regime names one leaf while the\nowner dict names another is the only way the catalogue partition and the regime tagging\ncan disagree, and a run that routes by one while the other is true is a run whose ledgers\nbalance in the wrong warehouse.\n\n# \u2500\u2500 what is NOT here \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nThe DRIVER wiring of a two-leaf standing run.  Three things a live coupled run needs are\nnamed out of scope by this build's ticket, and each is somebody else's decision: the\nsite-scoped yard rows (`yard_trailers`, `yard_drains`) have no declared artifact until\n`<pair>/_site/` lands with its contract bump (\"Design the site scope in the run tree\",\nADR-0005); the shared transit carries ONE `gain_bundle` slot for two owners (\"Design the\ncomposite gain bundle\"); and one dock has one `UnloadCost` while the two channels price at\ntwo (\"One unload price for the site dock\", the site-dock map's open question).  So the\ncoordinator is complete and the run that fields it is the next ticket's \u2014 the same\nseam-before-consumer order this map has used throughout.  `site_rows` below is where a\ncoupled drain's yard rows wait for that artifact, and `drain_site_rows` is its accessor.\n\nWithin-day interleaving.  One batch is one site day and the drain stays drain-quantized;\nan event-driven cadence is out of scope, inherited.",
      "kind": "module",
      "signature": null
    },
    "Inbound/receiving.py::SiteReceiving": {
      "decorators": [],
      "doc": "The site's dock, yard and drain record, driving N leaves through three ports.\n\nConstructed by the driver where the `Dock` and the `YardTransit` are built, and bound\nonto each leaf it serves (`mgr.receiving`) \u2014 the same injection precedent as\n`enable_receiving` and `mgr.packer`: the broker holds what it is handed, and nothing\nunder `Warehouse/` imports this package.\n\nA COUPLED RUN REQUIRES THE STANDING YARD, and this refuses a transit without it at\nCONSTRUCTION.  Of the three transits only `YardTransit` carries a yard, doors and a\ndock ranking; the v1 `TrailerTransit` and the flag-off `BatchTransit` drain through the\nmanager's own dock-deque branch, which is a separate ~20-line path with its own owner\nproblem.  Building owner routing twice, in two unrelated drains, for a mode no\nexperiment runs is work with no reader \u2014 and the alternative to refusing is a SILENT\nfallback to per-leaf receiving, which is a run that looks healthy and answers a\ndifferent question (memory `pool-run-swallows-dead-arms`).\n\n`day` is the SITE working day, and it is what the site receiving clock is based on.\nNone is every run without a whistle grid, and then `open_batch` refuses rather than\ninventing one \u2014 the same precondition `Inbound/putaway_pool.py` states for the put\ncrew, for the same reason: coupling is an era feature and the era completes the grid.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/receiving.py::SiteReceiving.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, dock, transit, *, day=None)"
    },
    "Inbound/receiving.py::SiteReceiving.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/receiving.py::SiteReceiving._channel_of": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> str"
    },
    "Inbound/receiving.py::SiteReceiving._check_drain": {
      "decorators": [],
      "doc": "THE THREE REFUSALS: no leaf, a partial site drain, and the wrong day.\n\nTogether they are one rule -- a drain of the site dock names every leaf the\ncoordinator serves, at the day its clocks are running on -- stated from the three\nsides it can be broken from.  Hoisted out of `receive` so the four steps below\nread as the four steps; nothing here is reordered or relaxed.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaves, deadline: float | None) -> None"
    },
    "Inbound/receiving.py::SiteReceiving._count_remainders": {
      "decorators": [],
      "doc": "What the whistle cost: the remainders standing on STAGED trailers, counted once.\n\nReturns `left` in storage units.  Computed above the whistle test, not inside it: a\ndrain that ran out of WORK leaves the same remainder as one that ran out of DAY,\nand only one of those is a cut -- the level says what was standing either way.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, transit, dock, deadline: float | None, solo) -> int"
    },
    "Inbound/receiving.py::SiteReceiving._drain_epoch": {
      "decorators": [],
      "doc": "ONE DRAIN IS ONE INSTANT: the site epoch every decision in this drain reads.\n\nReturns the leaves' shared `_now_s`, or 0.0 when none of them carries one.\n\nThe yard's calendar is the SITE's, so a drain stamped with two epochs would be two\narrival calendars over one set of doors.  CHECKED rather than picked from the first\nleaf: the two leaves' pick crews genuinely release at different instants inside one\nsite day, and it is the DRIVER's job to hand the site epoch down -- silently taking\nleaf[0]'s would make that a detail nobody could see was wrong.\n\nThe two refusals below are the same rule from both sides.  A leaf carrying no epoch\nbeside one that does is not \"the same instant as the others\", it is a leaf the driver\nforgot; and two leaves whose epochs differ by more than `_EPOCH_TOL` would rank the\nsame yard against two different \"now\"s.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaves) -> float"
    },
    "Inbound/receiving.py::SiteReceiving._fill_doors": {
      "decorators": [],
      "doc": "STEP 2: THE DOOR FILL -- NOT budget-gated -- and the drain-frozen dock ranking.\n\nReturns `(work_order, yard_next)`: the ranking the allocation and the handoff both\nread, and the queue a freed door pulls its replacement from.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, transit, ctx, epoch: float) -> tuple"
    },
    "Inbound/receiving.py::SiteReceiving._freeze_views": {
      "decorators": [],
      "doc": "`[(regime, view)]` \u2014 one frozen space view per leaf that runs a timeline.\n\nONE LEAF IS CONTRIBUTED UNTAGGED and more than one is TAGGED, and the asymmetry is\nthe design rather than a shortcut.  A composition of one partitions nothing, so a\ntag there would decide nothing \u2014 and `compose_site_view` returns a lone\ncontribution BY IDENTITY, which is what keeps every standing-yard run already on\ndisk byte-identical (filtering one leaf's `empties` would remove a phantom that\nbelongs to the UNCOUPLED model).  With two, the tag is what partitions `empties` so\na mixed trailer's store units rank against store bins, and the composer REFUSES an\nuntagged contribution the moment there are two (\"Design the site space view\").\n\nThe tag is the channel the leaf BOUND under, which is the same knowledge the\n`{sku: leaf}` owner dict is built from \u2014 not `regime_of` on anything, because the\ncoordinator is the one object that holds both managers and knows which is which.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaves, epoch: float) -> list"
    },
    "Inbound/receiving.py::SiteReceiving._hand_off": {
      "decorators": [],
      "doc": "STEP 4: THE CANONICAL HANDOFF, with the per-unit ledger flip.\n\nWhatever the crew allocation, unloaded units reach `_queue` in MERGED order --\ntrailers by dock rank, units by local rank -- never in labor-completion order.\nThat is the containment property: crew allocation moves labor stamps and makespans\nONLY, never placement physics.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dock, done, solo) -> None"
    },
    "Inbound/receiving.py::SiteReceiving._leaf_for": {
      "decorators": [],
      "doc": "Step 4's route: which leaf takes a unit of `regime`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, regime: str)"
    },
    "Inbound/receiving.py::SiteReceiving._owner_of": {
      "decorators": [],
      "doc": "Step 1's route: which leaf packs a bare `(sku, qty)` lot.\n\nUnbound coordinators (every one-leaf run built before a `bind` existed) keep the\nhistorical behaviour -- the single leaf the drain was handed owns everything -- and\nthat fallback lives at the ONE call site rather than here, so this method always\nmeans \"the dict says\".",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int)"
    },
    "Inbound/receiving.py::SiteReceiving._partition": {
      "decorators": [],
      "doc": "Split THIS site day's dock into per-channel shares.  Runs once per day.\n\nOne dock is one drain, so the rows, the flows and the labour all arrive as site\ntotals \u2014 and ADR-0005 says the pack-denominated half of them decomposes and belongs\nback on the owning channel's own record.  This is where that happens, once, so the\ntwo leaves cannot each take the whole (`a-right-site-total-hides-two-wrong-shares`\nis that failure, and `receiving_report` checks 1 and 2 would PASS through it because\nboth surfaces delegate to the same dock).\n\nEvery row is routed by its SKU through the same `{sku: leaf}` dict step 1 packs by,\nso an unload row, a repack row and the lot that produced them cannot land in\ndifferent channels.\n\nTHE CLOSURE IS ASSERTED, not assumed: the shares are accrued independently of the\ndock's own counters (the leaves' `receiving_seconds` especially, which is the only\nsurface that sees a repack), so summing them back to the dock's totals is a real\ncheck that the decomposition is complete rather than a restatement.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Inbound/receiving.py::SiteReceiving._plan_arrivals": {
      "decorators": [],
      "doc": "STEP 1: PLANS-AT-ARRIVAL -- every trailer that joined the yard is packed NOW.\n\nStamps the units immediately, so a pallet that stands three batches in the yard is\nthree batches old when it finally reaches floor space.  The merchandise stays in\n`_deferred_qty`: nothing is queued until a crew actually pulls it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dock, transit, ctx, epoch: float, source: str, solo) -> None"
    },
    "Inbound/receiving.py::SiteReceiving._take": {
      "decorators": [],
      "doc": "`leaf`'s share of `what` for this site day, served exactly once.\n\nThe same \"compute once, serve each caller once\" contract `open_batch` keeps, and\nfor a sharper reason: these are DRAINS.  A leaf that asked twice would take the\nother channel's rows the second time only if the first call had left them \u2014\npopping is what makes the second call raise instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf, what: str)"
    },
    "Inbound/receiving.py::SiteReceiving._unload_merged": {
      "decorators": [],
      "doc": "The 'merged' pooled gang: v1's physics kept as the verification bridge.\n\nOne crew works trailers strictly in dock-rank order, completing the top-ranked\nfirst; a freed door pulls the frozen-ranking-next trailer, which joins the END of\nthe work list.  In the degenerate configuration (FIFO, doors >= every trailer, no\ncap) the charge sequence is exactly the v1 drain's \u2014 the lockstep pin.\nUnder a door-team `cap` the gang IS one team of `cap` on one trailer \u2014 the cap is\na property of the trailer, not of the dealing rule \u2014 so the rest of the crew\nidles; None keeps the whole crew, byte-identically.\nReturns [(trailer, [(item, t0, dur, worker), ...])] in canonical drain order.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dock, transit, deadline, epoch, work_order, yard_next, cap: int | None=None)"
    },
    "Inbound/receiving.py::SiteReceiving._unload_split": {
      "decorators": [],
      "doc": "The 'split' door teams: the standing model's own physics.\n\nAt ctx-freeze the workers are DEALT across staged trailers in dock-priority\norder, cycling, so the top ranks take the extras when the division is uneven\n(`allocation.partition`, round-robin).  Each team charges earliest-free WITHIN\nthe team.  Doors therefore free STAGGERED \u2014 the realistic dynamic the fee and\nspace signals need \u2014 and when one does, the yard-pull stages the frozen-next\ntrailer and the freed team reassigns: (1) to the top-ranked staged trailer with\nNO workers \u2014 the one-worker-many-doors inversion's guard \u2014 (2) else to its own\ndoor's replacement, (3) else to the top-ranked trailer with the fewest workers.\nA worker idles only when nothing staged has units.  Dock priority is thereby a\nworker-ALLOCATION preference: decisive when workers < staged trailers, graded\notherwise.\n\nTHE DOOR-TEAM CAP (`cap`; None = uncapped, the dealing above verbatim).  At most\n`cap` receivers support one trailer's unload and pack at once, every worker\nadditive, the steps inside an unload not modelled.  The deal stays EVEN and each\nteam is then cut to `cap`; the cut-off workers simply are not dealt, and idle for\nthe drain.  Even, not greedy: 22 receivers over three staged trailers deal 8/7/7,\nand the cap binds only when the even division would put more than `cap` on a door\n(22 over two doors is 10/10, with two standing idle until a door frees).  A greedy\ndeal \u2014 10/10/2 \u2014 is the rule 25 rejected: it makes the cap bind at every door\ncount and turns dock priority into a capacity grant.\n\nThe reassignment respects the cap by SPREADING a freed team over the (1)-(2)-(3)\ntargets in order, each taking up to its own room, instead of handing the whole team\nto one.  That matters at step (3), where the target already has a team: the\npre-cap code extended it unconditionally, which under a cap would put `2 x cap`\nreceivers on one trailer.  Uncapped the room is unbounded, so the head of the list\ntakes the whole team and the None path is byte-identical rather than merely\nequivalent.  Nothing is dealt to a cut worker later: whenever a target has room it\nis a FRESH staging with room `cap`, and a freed team that was cut is itself exactly\n`cap`, so it fills the door alone.\n\nThe loop advances whichever team can start soonest, so charges interleave in\ntrue clock order and a reassignment always sees every earlier emptying's effect.\nReturns the same shape as `_unload_merged`, in the same canonical order.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dock, transit, deadline, epoch, work_order, yard_next, cap: int | None=None)"
    },
    "Inbound/receiving.py::SiteReceiving.bind": {
      "decorators": [],
      "doc": "Serve `channel`'s leaf.  Call once per leaf, before the batch loop.\n\nBuilds this leaf's half of the `{sku: leaf}` owner dict from its own catalogue\npartition, and REFUSES an overlap.  An sku owned by two leaves means the channel\nfilter (`_channel_runs_for`) let one order into both, and the only symptom\ndownstream would be merchandise delivered to the wrong warehouse and a ledger that\nbalances there \u2014 so it is checked at the one moment both partitions are in hand.\n\nThe leaf's transit must BE this coordinator's.  That is what makes `SITE_PHASES`'\nsite-scoped `_advance_lead_queue` honest: the phase is driven on one leaf and\nreaches `transit.advance()`, so \"which leaf\" is not a choice as long as the\nidentity holds, and this is where it is established rather than assumed.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf, channel: str) -> None"
    },
    "Inbound/receiving.py::SiteReceiving.dock_depth_for": {
      "decorators": [],
      "doc": "Storage units of THIS leaf's merchandise standing on the site dock floor.\n\nThe site-scoped replacement for `Inventory_Manager.dock_depth`.  A LEVEL, so it\nneither drains nor resets and may be read at any instant -- unlike `snapshot_for`,\nwhich is a per-site-day drain and can be taken once.\n\nZero on every standing-yard run by construction: merchandise waits on a trailer\nuntil a receiver pulls it, and `accept` hands it straight to the put queue.  Written\nout anyway, because \"the floor is empty\" is a fact about the standing model and not\na licence for the caller to assume it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> int"
    },
    "Inbound/receiving.py::SiteReceiving.drain": {
      "decorators": [],
      "doc": "Drive N leaves' phases with ONE shared receive, and return the SKUs each\ntriggered, keyed by `id(leaf)`.\n\nKeyed by identity rather than by the leaf itself because a manager is unhashable\nby value here and the caller already holds the leaves it passed in; a caller doing\n`triggered[leaf]` gets a `KeyError`, so the key is spelled out.\n\n`SITE_PHASES` IS THIS BODY, and the module constant is where the two facts it\nencodes are argued: which phases are the SITE's, and why the loop is phase-major.\nThe composition is the same seven phases `check_reorders` composes for one channel,\nin the same order, because THE ORDER IS THE BEHAVIOUR \u2014 firing before the lead tick\nwould decrement an order in the batch it was placed, and releasing before firing\nwould delay every lead-0 arrival by a batch.\n\nPhase 3 (`_release_arrivals`) runs per leaf even though it is a structural no-op\nin standing mode \u2014 it is the phase that lands trailers in the yard, so it must\nrun for every leaf BEFORE the shared receive.  Dropping it because its return is\nempty would strand every arrival.\n\nPhase 5 is routed exactly as `check_reorders` routes it: to the SITE PUT POOL when\none is bound, which divides the day between the channels and re-drains both against\nthe whole of it, and to the leaf otherwise.  Reached from HERE rather than from each\nleaf's own composition, it lands after the shared receive for every leaf \u2014 so a unit\nunloaded this morning gets a bin today, in both channels, which is the whole reason\nthe receive sits where it does.\n\nWith a single leaf this is `check_reorders` phase for phase, and\n`Tests/unit/test_site_receiving.py` pins that equality.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaves, put_deadline: float | None=None, recv_deadline: float | None=None, now_s: float | None=None) -> dict"
    },
    "Inbound/receiving.py::SiteReceiving.drain_records_for": {
      "decorators": [],
      "doc": "THIS leaf's unload rows for the site day \u2014 the replacement for\n`drain_receiving_records`.  Does NOT restart the crew's clocks: that reset has one\nowner and it is `note_records`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> list"
    },
    "Inbound/receiving.py::SiteReceiving.drain_repacks_for": {
      "decorators": [],
      "doc": "THIS leaf's repack rows for the site day \u2014 the replacement for\n`drain_repack_records`.  Routed by sku like every other row, so a rescue is\nrecorded in the warehouse whose merchandise was rescued.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> list"
    },
    "Inbound/receiving.py::SiteReceiving.drain_site_rows": {
      "decorators": [],
      "doc": "This drain window's SITE-scoped yard rows, and start the list over.\n\nA coupled drain's `(yard_start, free_doors_start, yard_end, remainder)` belongs to\nneither leaf -- it is a statement about the site's trailers and the site's doors --\nso `receive` parks it here instead of handing an arbitrary leaf a number that reads\nas its channel's.  Nothing consumes this yet; `<pair>/_site/inbound_<pair>.db` is\nthe declared home and its contract bump is the next ticket's.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/receiving.py::SiteReceiving.drain_site_totals": {
      "decorators": [],
      "doc": "This drain window's SITE dock totals, `[(day, depth, unloaded, cut, seconds)]`,\nand start the list over.\n\nThe twin of `drain_site_rows`, and drained on the same cadence for the same reason:\na list that only grows is the only evidence the site day was partitioned at all, and\ncompounding it would hand the writer several days' counters stamped as one.\n\nNORMALLY ONE ROW per call -- `_partition` runs once per site day -- but the list is\nreturned whole rather than popped singly, because a caller that took \"the\" row would\nsilently drop the second one if a grid ever put two site days in one batch.  The\ndriver asserts the day index instead, which fails loudly on exactly that.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/receiving.py::SiteReceiving.drain_trailer_stamps": {
      "decorators": [],
      "doc": "The site's FINISHED trailer stamps for this batch, and start the list over.\n\nTrailer-denominated, so the site's and not a channel's (ADR-0005).  Both leaves\nhold this one transit, so a leaf draining it would take every trailer on the site\ninto its own table and leave the other channel's yard looking empty \u2014 which is why\n`Inventory_Manager.drain_yard_trailers` refuses once the scope is the site's.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/receiving.py::SiteReceiving.leaves": {
      "decorators": [
        "property"
      ],
      "doc": "The bound leaves in BIND ORDER, which is the declared channel order.\n\nThe order is the model: `SITE_PHASES` is phase-major over exactly this sequence,\nand `_fire_reorders` loads the shared open trailer in it, so store-first means a\nmixed trailer's store lots load first.  A tuple, not the dict, because callers\nsequence leaves and never index one by name.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Inbound/receiving.py::SiteReceiving.note_records": {
      "decorators": [],
      "doc": "`leaf` has stamped its unload rows; `finish` is the absolute instant its last\none ended, or None when it recorded nothing.\n\nCalled once per leaf per site day, on BOTH the picked and the skipped path \u2014 a\nbatch that picked nothing still drained the dock.  When the last leaf reports, the\nsite carry is committed.  THE RESET HAS ONE OWNER for exactly the reason the put\npool says so: one leaf restarting a shared crew's clocks before the other has\nrecorded is a silent, plausible-looking zeroing of a half-spent day.\n\nThe carry moves only when somebody actually worked: no records anywhere means the\ncrew is where it was, exactly as an unpooled leaf leaves `recv_clock` alone.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf, finish) -> None"
    },
    "Inbound/receiving.py::SiteReceiving.open_batch": {
      "decorators": [],
      "doc": "`(base, deadline)` for site day `index`: the receiving crew's absolute epoch and\nthe whistle as a REMAINDER on the dock's batch-local clocks.\n\nIDEMPOTENT PER INDEX, and that is the contract \u2014 one base, asked for twice.  Both\nleaves of one batch stamp their unload rows from the same epoch because there is\none crew on one dock, so the first call computes and the rest read.  The identical\nrule, for the identical reason, as `PutawayPool.open_batch`.\n\n`base` is `max(day.start_of(index), recv_clock)`: the receivers start at shift start\nand work what is standing, or they carry on from where yesterday overrun left them.\nNever either leaf's `arm_clock` -- that is a PICK crew's release instant, and basing\nthe dock on it would idle the site receivers whenever a pick crew overran its day\nand would misattribute a picking overrun to the dock's cut.  `deadline` is the rest\nof the day CONTAINING that base, so `base + deadline` is the day's end however far\nthe carry has run.\n\nTHIS IS NOT WHAT AN UNCOUPLED RUN DOES, and the divergence is deliberate rather\nthan a restatement: the shipped runner bases its receive rows at\n`max(arm_clock, recv_clock)` \u2014 this leaf's pick-crew release instant. Under one\ncrew on one dock that instant is a property of one channel, so the site base moves\nto the shift start. Any run that adopts this is therefore not row-comparable with\none that did not, which is why the coordinator does not quietly apply it to the\none-leaf path (`open_batch` has no caller until a coupled run has one).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int) -> tuple"
    },
    "Inbound/receiving.py::SiteReceiving.receive": {
      "decorators": [],
      "doc": "One drain of the standing dock: plan arrivals, fill doors, unload, hand off.\n\n`leaves` is a SEQUENCE, and there is one drain for the whole site however many it\nholds.  A coordinator serving AT MOST ONE leaf routes and returns exactly as the\nsingle-channel drain always did: the yard row comes back and the caller decides\nwhere it belongs (`_receive` appends it to that leaf's `_yard_drains`).  From the\nSECOND leaf the row is site-scoped and belongs in neither leaf's table, so it is\nparked on `site_rows` and None comes back \u2014 a leaf cannot record a number that is\nnot its channel's if it is never handed one (\"Design the site scope in the run\ntree\").  That threshold is the one `bind` stamps `site_scoped` on and\n`_freeze_views` tags on, deliberately: three spellings of \"is the site real yet\"\nis three things to keep in step.\n\nTWO REFUSALS, and they are the same rule from both sides.  A coordinator serving\nseveral leaves refuses a drain that does not name all of them \u2014 the only way to\nreach that is a coupled leaf's own `check_reorders`, and what it would do is drain\nthe site's dock for one channel while the other's arrivals are still on the yard:\nhalf a site day's receiving, attributed whole.  An UNBOUND coordinator refuses more\nthan one leaf outright, because the owner dict is built by `bind` and without it\nthere is no way to say whose merchandise a lot is.\n\nFour steps, and their order is the design:\n\n1. PLANS-AT-ARRIVAL.  Every trailer that joined the yard gets its pack plan NOW,\n   per contiguous lot \u2014 the same portions the v1 drain packs \u2014 and its units are\n   stamped immediately, so a pallet that stands three batches in the yard is three\n   batches old when it finally reaches floor space.  The merchandise stays in\n   `_deferred_qty`: nothing is queued until a crew actually pulls it.\n2. THE DOOR FILL, NOT budget-gated.  Staging is yard-jockey work, not receiving\n   labour, so even a zero-budget drain fills every free door from the drain-frozen\n   yard ranking.\n3. THE UNLOAD, budget-gated, per the crew-allocation mode ('split' door teams or\n   the 'merged' pooled gang).  The whistle is a START gate, one unload of overtime\n   per worker, exactly as the v1 path's.  Same-drain refills consume the frozen\n   rankings; no mid-drain re-scoring.\n4. THE CANONICAL HANDOFF.  Whatever the allocation, unloaded units reach `_queue`\n   in merged order \u2014 trailers by dock rank, units by local rank, filtered to what\n   actually unloaded \u2014 never in labor-completion order.  That is the containment\n   property: crew allocation moves labor stamps and makespans ONLY, never\n   placement physics.  The deferred->queued flip rides the handoff, per unit, so\n   `position = on_hand + queued + deferred` never wobbles.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaves, deadline: float | None) -> tuple | None"
    },
    "Inbound/receiving.py::SiteReceiving.snapshot_for": {
      "decorators": [],
      "doc": "`(depth, unloaded, cut, seconds)` \u2014 THIS leaf's share of the site dock.\n\nThe site-scoped replacement for `Inventory_Manager.receiving_snapshot`, which\nrefuses on a coupled leaf because all four counters are the site's and three of them\nRESET: the first caller would take the site's whole batch and leave the other\nchannel reporting an idle dock.  Here the reset happens once, in `_partition`, and\neach leaf is served its own share.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> tuple"
    },
    "Inbound/receiving.py::SiteReceiving.standing_trailer_stamps": {
      "decorators": [],
      "doc": "Stamps for trailers STILL ON SITE at run end \u2014 read, never drained.\n\nWorse than the drain above if a leaf were left to do it: this one does NOT empty\nanything, so BOTH leaves would report the same censored trailers and every detention\nday at run end would be counted twice.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/receiving.py::SiteReceiving.transit_census_for": {
      "decorators": [],
      "doc": "`(rows, qty, depth)` \u2014 THIS leaf's share of the site's in-flight merchandise.\n\nThe site-scoped replacement for the three transit reads a leaf refuses:\n`transit_snapshot` (the replay rows), `in_transit_qty` (the units level) and the\n`lead_queue_depth` a leaf would take from `transit.depth`.  All three come off ONE\ncensus pass so they cannot disagree with each other, which is the failure two\nseparate walks of a live yard invite.\n\nTHE SPLIT IS BY SKU, which is the pack rule ADR-0005 settles: a lot has exactly one\nowning channel however mixed the trailer carrying it.  `rows` and `qty` therefore\nSUM across the leaves to the site's own census exactly.\n\n`depth` is the one that needs saying out loud.  Uncoupled it counts in-flight\nENTRIES, which under a trailer pipeline are TRAILERS \u2014 and a trailer is mixed by\nconstruction, so it decomposes to nothing.  Here it counts this leaf's in-flight\nLOTS instead, which is what `BatchTransit.depth` counted before the trailer pipeline\nexisted and is the only reading that both decomposes and sums.  A mixed trailer\ntherefore contributes one lot to each channel rather than one trailer to both: the\n\"two copies of one yard sum to twice the trailers\" outcome ADR-0005 rejected is what\ncounting trailers here would produce.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf) -> tuple"
    },
    "Inbound/site_space.py": {
      "decorators": [],
      "doc": "site_space \u2014 one space view for the site, composed from one frozen view per leaf.\n\n`SpaceTimeline` stays ONE PER LEAF and `freeze(mgr, epoch)` keeps its single-manager\nsignature and its purity pin.  All the site-ness lives here, in a pure function over\nalready-frozen data: `compose_site_view` takes what each leaf froze and returns the one\nview a drain's decisions read.\n\n# \u2500\u2500 why this is not in `space.py` \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`Inbound/space.py` states its own defining property in its docstring \u2014 *\"This module\nimports nothing from Warehouse at all\"* \u2014 and the composer needs `regime_of` to check\nwhich channel a bin belongs to.  So the rule gets its own module rather than costing\n`space.py` the property that makes it testable in isolation.  `regime_of` is `wh_kernel`\nand imports nothing itself, so `inbound -> wh_kernel` is the only edge this adds.\n\n# \u2500\u2500 the deletion test \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nDelete this and every gain `@ordering` entry has to learn to read TWO views, at five\n`.get(key, ())` sites each.  Its interface is one function over frozen data \u2014 the\nsmallest surface that carries the whole rule.\n\n# \u2500\u2500 the one that would have been silent \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n**A leaf's `empties` carries the OTHER channel's bins.**  It is snapshotted from\n`mgr._index`, which is the WHOLE geometry's free index, and a leaf simulates only its own\nsection \u2014 so every leaf's view lists the other channel's free bins as permanently,\nfalsely available (memory `free-bins-counts-the-whole-geometry`; `CONTEXT.md` states it\nunder **Free index**).  A naive union of two leaves' views therefore imports BOTH leaves'\nphantoms, and a mixed trailer's store units would rank against fulfillment bins that no\nstore putter can ever reach.  So `empties` is FILTERED per contributing leaf and the other\ntwo tiers are not: `predicted` is projected from that leaf's own demand over its own SKUs,\nand `emptied_at` is harvested from that leaf's own reclaims, so both are already leaf-own.\nFiltering all three uniformly would be three times the surface for one real defect.\n\nThe invariant that establishes, and it is the point of the whole module: **the composed\nkey set is PARTITIONED BY REGIME**, so a mixed trailer's store units rank against store\nbins and its fulfillment units against fulfillment bins.  There is no site free-space\n*scalar* anywhere in this design, and there never was one that meant anything.\n\n# \u2500\u2500 what a composition of ONE is \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nThe view itself, by identity.  A composition of one partitions nothing, so there is\nnothing to filter \u2014 and filtering it would silently change every standing-yard run on the\nbooks, removing a phantom that belongs to the uncoupled model rather than to a defect this\nfunction is allowed to fix on its way past.",
      "kind": "module",
      "signature": null
    },
    "Inbound/site_space.py::compose_site_view": {
      "decorators": [],
      "doc": "One site view from `[(regime, view), ...]`, one entry per leaf that froze.\n\n`regime` is the CONTRIBUTING LEAF's channel \u2014 the coordinator tags each contribution\nbecause it is the thing that holds both managers and called `freeze` on each, which is\nthe same knowledge its `{sku: leaf}` owner dict is built from.  It is allowed to be\nNone on a single-entry composition and only there: nothing is partitioned, so there is\nnothing for a tag to decide.\n\nReturns None when nothing froze (no leaf runs a timeline), which is every run without\na standing yard, and the single view UNCHANGED when one leaf froze.",
      "is_async": false,
      "kind": "function",
      "signature": "(contributions) -> SpaceView | None"
    },
    "Inbound/site_space.py::uncomposable_policies": {
      "decorators": [],
      "doc": "`{policy: the fields it reads that a coupled composition does not carry}`, for the\npolicies that have any \u2014 empty when every one of them can run coupled.\n\nTHE JOIN, and the only place the two halves meet.  `POLICY_VIEW_NEEDS` says what an\nentry opens; `COMPOSED_VIEW_FIELDS` says what a two-leaf view can hand it.  A\ncampaign axis names policies, so this is what a spec-time gate can ask without\nbuilding a warehouse: `validate_spec` calls it over the cells a coupled spec declares\nand refuses before the run directory exists, where the failure it replaces was a\nburned freeze and 24 units dying at their first drain.\n\nUnknown policy names raise (`view_needs`): a gate that skipped what it could not\nresolve would pass the one spelling mistake the registries also miss.\n\nAGAINST WHAT IS COMPOSED, never against what is refused, and the difference is the\nwhole generality of this predicate.  The two sets partition the view, so for today's\nfields the answers are identical \u2014 but `UNCOMPOSED_VIEW_FIELDS` empties when 34 lands\nthe window zip, and an intersection with an empty set can never catch anything again.\nSubtracting the composed set keeps asking the question the class is actually about:\n*does this policy need a structure a composed view does not carry* \u2014 which still has\nan answer for a field nobody has classified yet, and for one that does not exist on\nthe view at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(policies) -> dict"
    },
    "Inbound/space.py": {
      "decorators": [],
      "doc": "space \u2014 the event-stamped space timeline the standing dock's decisions read.\n\nTwo tiers against the absolute clock, and nothing inferred between them: bins that are\nempty NOW (with the actual second each ran dry, where a pick produced one), and bins\nPREDICTED to clear from the standing demand \u2014 the released-but-unpicked batch, one batch\ndeep, which is information a real WMS has.  Predictions are deliberately UNTIMED: no\nmakespan estimates, no pick rates against the clock.  The only times carried are facts \u2014\nactual clear stamps, and the release instant of the demand.  (\"How far ahead can\navailability reliably be planned\" is a future view-arm family, parked in the\ninbound-optimization map's fog.)\n\n# \u2500\u2500 how it attaches, and why flag-off is byte-identical by construction \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`SpaceTimeline` is constructed by the DRIVER when `INBOUND_STANDING_YARD` is on \u2014 always\non with the flag, no policy gate, no extra knob \u2014 and attached to the manager instance\n(`attach(mgr)`, the `BinRecorder.attach` rebind precedent: one attribute, no listener\nregistry).  Flag-off it is never constructed, and every manager-side hook is an\n`is None` no-op, so the store-only path cannot be perturbed.\n\nThe drain rule is INJECTED at construction (`Warehouse.picking.Workload_Builder.\ndrain_sku`, handed over by the driver) because the import edge `Inbound -> wh_picking`\nis forbidden \u2014 the broker holds what it is handed.  This module imports nothing from\nWarehouse at all.\n\n**ONE TIMELINE PER LEAF, AND THE SITE-NESS IS SOMEWHERE ELSE.**  Under a site dock two\nleaves each freeze their own view and `Inbound/site_space.compose_site_view` turns them\ninto the one a drain reads \u2014 a pure function over frozen data.  `freeze` keeps its\nsingle-manager signature and its purity pin, and the composer lives in its own module\nprecisely so this one keeps the property stated above: it needs `regime_of` to tell which\nchannel a bin belongs to, and that is the filter the composition cannot do without (a\nleaf's `empties` is the WHOLE geometry's free index, so it lists the other channel's bins\nas permanently, falsely free).\n\n# \u2500\u2500 the five touchpoints (decisions are drain-quantized; data is event-stamped) \u2500\u2500\u2500\n\n    inject_demand   driver, BEFORE check_reorders: the batch about to be released plus\n                    the rollover carry \u2014 everything released-but-unpicked at the\n                    decision instant\n    harvest         `_reclaim_empty_bins`: the actual `_emptied_at` stamps, captured at\n                    the one moment the stamps and the bins meet before both are wiped\n    evict           `requeue_bin`: the reloader's eviction returned a bin to the free\n                    index \u2014 the OTHER door into `_index`, and the one that runs with\n                    the standing yard off\n    fill            `_execute_placement`: a bin was occupied \u2014 version bump, stamp expiry\n    freeze          ctx-freeze inside `SiteReceiving.receive`: ONE projection per drain\n                    serves every decision in it (no per-decision rescans), delivered as\n                    `ctx.space`, the named view the priority seams reserved\n\n# \u2500\u2500 the version contract (what the cache ticket keys on) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nThree per-event-class counters, so tables can be processed in parallel without an I/O\nrace: `demand_v` +1 per injection (once per batch), `reclaim_v` +1 PER BIN RETURNED TO\nTHE FREE INDEX (harvest or eviction \u2014 see below), `fill_v` +1 per placement.  The\ncounters partition by what CHANGED, not by which function ran: `_index` grows through\nexactly two doors and both bump the same counter, which is what makes the vector a\ncomplete description of the free index rather than a log of call sites.  EQUALITY is\nthe only legal operation \u2014 magnitudes and\ncross-class comparisons are meaningless by contract.  The predicted set is a pure\nfunction of the other three states, so it carries no fourth counter, and the cache\nlayer composes keys from this vector and may not add counters of its own.  The\nfuturesight window rides the same rule from the other side: a pure function of\n(batch script, batch index), replaced by the injection that bumps `demand_v`, so it\ntoo adds no counter (see `SpaceView.window`).\n\nThat completeness was not free.  `requeue_bin` evictions (reloader arms) originally\nreturned a bin to the free index through none of the three classes, so two version-equal\nfreezes could straddle an eviction-only change and a cache keyed on the vector alone\nwent stale until the evicted unit re-placed.  \"Draw the cache-sharing boundary\"\n(inbound-optimization 06) closed it by GENERALIZING `reclaim_v` rather than adding a\nfourth counter: an eviction is a reclaim by every property the vector exists to express.\nNote the eviction hook is the one touchpoint reachable with the standing yard OFF (the\nreloader gates on `reslot_frac` alone), so its `is None` guard is load-bearing, not\nceremonial.",
      "kind": "module",
      "signature": null
    },
    "Inbound/space.py::SpaceTimeline": {
      "decorators": [],
      "doc": "The incrementally-maintained timeline; one per arm, riding the standing yard.\n\nOwns the standing-demand injection, the harvested clear stamps and the three version\ncounters; builds one frozen `SpaceView` per drain.  `freeze` is PURE with respect to\nthe manager: it mutates no manager state and consumes no RNG (pinned by\n`Tests/unit/test_space_timeline.py`); its only writes are this object's own\nbookkeeping (`views_built`).",
      "kind": "class",
      "signature": ""
    },
    "Inbound/space.py::SpaceTimeline.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, drain_rule)"
    },
    "Inbound/space.py::SpaceTimeline.attach": {
      "decorators": [],
      "doc": "Bind onto the manager instance \u2014 rebind, no listener registry (the\n`BinRecorder.attach` precedent).  The manager's hooks are `is None` tests on\nthis one attribute, so detaching is unbinding and flag-off is never-bound.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr) -> 'SpaceTimeline'"
    },
    "Inbound/space.py::SpaceTimeline.evict": {
      "decorators": [],
      "doc": "A reloader eviction returned `bin_` to the free index (`requeue_bin`, the\nsecond and last door into `_index`): bump `reclaim_v`, whose meaning is \"+1 per\nbin returned to the free index\".  No fourth counter \u2014 an eviction changes the\nfree index in exactly the way a harvest does, and 03's equality-only contract\ncannot tell the two apart, nor should it.\n\nNO stamp is written, and none is expired.  An evicted bin was OCCUPIED an\ninstant ago, so it never ran dry \u2014 `emptied_at` carries actual clear stamps\nonly, exactly as it treats the harvest's stampless notifications.  Nor can a\nstale stamp survive here: occupancy has a single site (`_execute_placement`),\nand the `fill` that put this unit in the bin already popped it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_) -> None"
    },
    "Inbound/space.py::SpaceTimeline.fill": {
      "decorators": [],
      "doc": "A placement occupied `bin_` (`_execute_placement`, the chokepoint every\nput-away funnels through): bump `fill_v` and expire the bin's clear stamp \u2014\n`emptied_at` describes bins that are empty, and this one no longer is.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_) -> None"
    },
    "Inbound/space.py::SpaceTimeline.freeze": {
      "decorators": [],
      "doc": "Build the drain's frozen view: snapshot the empties, project the predicted\nclears, stamp the version vector.  ONE projection per drain serves every\ndecision in it.\n\nThe projection runs the injected drain rule over the manager's live per-SKU bin\nindexes \u2014 the exact walk `Task.from_batch` will do when this demand is picked \u2014\nand a bin is predicted-clear when its projected take equals its quantity.\nPer-SKU projections are independent (a bin holds one SKU), so demand order\ncannot change the set.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, frozen_at: float) -> SpaceView"
    },
    "Inbound/space.py::SpaceTimeline.harvest": {
      "decorators": [],
      "doc": "The reclaim-harvest: called by `_reclaim_empty_bins` with `_pending_reclaim`\nand `_emptied_at` at the one moment they meet before both are wiped.  Every\nreclaimed bin bumps `reclaim_v` (the index changed for each); only bins with an\nactual stamp enter `emptied_at` (a stampless notification \u2014 a caller with no\nclock \u2014 frees the bin without asserting when).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bins, stamps) -> None"
    },
    "Inbound/space.py::SpaceTimeline.inject_demand": {
      "decorators": [],
      "doc": "The driver's per-batch injection, BEFORE check_reorders: the batch about to\nbe released plus the rollover carry.  REPLACES the standing demand \u2014 it is one\nbatch deep by charter, never an accumulation.  `released_at` is the batch's\nrelease instant on the absolute clock (a fact the view carries verbatim).\n\n`window` is the futuresight feed (see `SpaceView.window`): already-copied\nper-batch `{sku: qty}` dicts, replaced wholesale like the demand and riding\nthe SAME `demand_v` bump \u2014 one event, no fourth counter.  None (the default,\nand every lawful run) keeps the slot empty; the projection below never reads\nit, so `predicted` keeps meaning one batch deep regardless.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, items, released_at: float | None, window: tuple | None=None) -> None"
    },
    "Inbound/space.py::SpaceView": {
      "decorators": [],
      "doc": "One drain's frozen view of warehouse space \u2014 pure data, copied at freeze.\n\nPolicy keys read it as `ctx.space` (None when no standing yard runs).  The staging\ndecision anchors to `empties` \u2014 immediately available bins, the operationally stable\nsignal; `predicted` rides as data for the planning formulation still being designed\n(\"Define the inbound objective\").  NO aggregates live here: what gets derived from\nthe tiers is the evaluator's business.\n\nThe container sets are FROZEN copies (the live `_index` lists swap-remove mid-drain,\nso a held reference would violate the frozen-ctx contract); the `Bin` objects inside\nare the live ones \u2014 a key that reads `bin.storage` mid-drain is reading stale-by-\ndesign data, exactly as the `put_policy` purity contract states.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/space.py::SpaceView.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, empties, emptied_at, predicted, released_at, versions, frozen_at, window=None)"
    },
    "Inbound/trailer.py": {
      "decorators": [],
      "doc": "trailer \u2014 the vehicle, its load pallets, and the FIFO loading that fills them.\n\n# \u2500\u2500 the two-level object model \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`TrailerType` is stateless class-as-config on the `StorageCart` pattern\n(`Warehouse/layout/Storage_Primitive.py`): capacity read off the TYPE, one subclass per\nreal vehicle.  Positions come from footprint arithmetic at the 48-inch square pallet\n(`Warehouse.physical.PALLET_FOOTPRINT`), single-stacked: a 53-foot trailer is 13 rows x 2\nacross = 26 positions; a 28-foot pup is 6 x 2 = 12.  Both are overridable class attributes,\nand a mixed fleet is a future policy the type seam permits \u2014 one run uses one type today.\n\n`Trailer` is the stateful half and persists day over day: contents, remaining volume, an\noptional lead, the arrival stamp, door state.  Its pack plan is computed once at arrival\n(fixed by its full per-SKU contents \u2014 crew interruption pauses packing WORK, never changes\nthe packs) and dropped when the trailer is fully worked, so nothing pins packs across\nbatches \u2014 the constraint the removed LoadPlan-retention leak taught.\n\nUnder the STANDING YARD (`YardTransit`) a trailer also carries its unload state across\ndrains: `pending` is the planned put-away work in canonical order and `taken` is the\nconsumed index into it \u2014 a remainder is the tail, never a re-pack.  Decisions are\ndrain-quantized but the DATA is event-stamped: `arrived_s` (yard entry), `staged_s` (when\nit took its door) and `emptied_s` (when its last unit came off) are absolute-clock\nseconds, so a later event-driven cadence is a cadence change, not a data redesign.\n\n# \u2500\u2500 the load pallet, and why it is not a pack \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nA LOAD PALLET is the transport grouping loose items ride on \u2014 it may MIX SKUs that fit its\n48-cubed volume, and it is what the receiving crew pulls.  It is NOT a pack: packing has\nnot happened yet (root `CONTEXT.md` carries both entries with cross-avoid notes).  Fit is\nvolumetric with the perfect-packing assumption verbatim from `StorageCart.add_from_bin`;\nnext-fit closes a pallet, pallets fill positions, and an item that fits no remaining\nposition starts a NEW trailer \u2014 which is what keeps loading strictly FIFO (\"placed\nperfectly in FIFO order into a trailer if it fits\").\n\nFIFO loading makes SKU lots CONTIGUOUS by construction: all of one reorder loads before the\nnext begins, so \"three pallets of x\" sit together and the local-priority key's candidate is\nthe load pallet, degenerating to the SKU lot on single-SKU pallets.",
      "kind": "module",
      "signature": null
    },
    "Inbound/trailer.py::LoadPallet": {
      "decorators": [],
      "doc": "One transport grouping of loose items: (sku, qty, unit_volume) lots that fit 48^3.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/trailer.py::LoadPallet.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/trailer.py::LoadPallet.add": {
      "decorators": [],
      "doc": "Take up to `qty` items volumetrically; returns how many fit (perfect packing,\nthe `StorageCart.add_from_bin` assumption verbatim).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, unit_volume: int) -> int"
    },
    "Inbound/trailer.py::LoadPallet.remaining_volume": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/trailer.py::Trailer": {
      "decorators": [],
      "doc": "One vehicle in flight or on the ground.  State matters and persists day over day.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/trailer.py::Trailer.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer_type: type, seq: int, lead_s: float=0.0, dispatched_s: float | None=None)"
    },
    "Inbound/trailer.py::Trailer.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Inbound/trailer.py::Trailer.arrived": {
      "decorators": [],
      "doc": "Has this trailer reached the yard?\n\nLead zero (the decided default) arrives instantly.  A positive lead needs the\nabsolute clock; when no clock reaches the calendar (`now_s` None \u2014 bare test\nmanagers), a positive lead simply has not arrived, which fails safe: merchandise\nwaits rather than teleports.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now_s: float | None) -> bool"
    },
    "Inbound/trailer.py::Trailer.load": {
      "decorators": [],
      "doc": "Next-fit at the pallet level: fill the open pallet, then a new one while\npositions remain.  Returns how many items fit; the remainder starts a new trailer\n(the caller's next-fit, which is what preserves FIFO).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, unit_volume: int) -> int"
    },
    "Inbound/trailer.py::Trailer.lots": {
      "decorators": [],
      "doc": "Every (sku, qty, unit_volume) lot in pallet order \u2014 contiguous by construction.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/trailer.py::Trailer.remaining_qty": {
      "decorators": [],
      "doc": "Pieces still aboard: the un-taken planned tail once a plan exists, the whole\nload before one does.  The standing yard's contribution to the deferred census \u2014\nplanned quantities, so a packing shortfall (already debited from the ledger at\narrival) is not counted twice.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/trailer.py::Trailer.remaining_totals": {
      "decorators": [],
      "doc": "{sku: qty} still aboard \u2014 `remaining_qty` with the per-SKU split the\nreplay/reorder_queue rows want.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Inbound/trailer.py::Trailer.sku_totals": {
      "decorators": [],
      "doc": "{sku: qty} of the whole load \u2014 what fixes the pack plan at arrival.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Inbound/trailer.py::Trailer28": {
      "decorators": [],
      "doc": "28-foot pup: 6 rows x 2 across, single-stacked.",
      "kind": "class",
      "signature": "(TrailerType)"
    },
    "Inbound/trailer.py::Trailer53": {
      "decorators": [],
      "doc": "53-foot trailer: 13 rows x 2 across of 48-inch pallets, single-stacked.",
      "kind": "class",
      "signature": "(TrailerType)"
    },
    "Inbound/trailer.py::TrailerType": {
      "decorators": [],
      "doc": "Stateless config: how many pallet positions a vehicle holds. Subclass per vehicle.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/trailer.py::TrailerType.capacity": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Total loose-item volume, positions x one max pallet each.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls) -> int"
    },
    "Inbound/transit.py": {
      "decorators": [],
      "doc": "transit \u2014 the trailer model on the manager's order-port seam.\n\n`TrailerTransit` binds where `BatchTransit` (the flag-off default in\n`Warehouse/inventory/inventory_reorder.py`) sits: same seam, same phase wrappers, injected\nby the driver and never imported by anything under `Warehouse/` \u2014 the broker rule.  What\nchanges flag-on is the SHAPE of transit: fired reorders load item-by-item into trailers\n(FIFO next-fit \u2014 a lot that outgrows the open trailer continues in a fresh one, which is\n`inbound_split` realized structurally: each trailer's portion packs as its own delivery),\ntrailers dispatch when loading passes them by and arrive per their lead (seconds on the\nabsolute clock, minutes-authored, default ZERO = instantly), stand in the YARD,\nstage to DOCK DOORS, and are worked in priority order \u2014 global over trailers at both dock\nmoments, local over load pallets, both `'fifo'` in v1.\n\n# \u2500\u2500 the lead, and why it is drawn here \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nA lead is PER TRAILER, drawn at creation from one lognormal \u2014 median `lead_s` \u00d7 spread\n`lead_sigma` \u2014 and homogeneous leads are that distribution's \u03c3 = 0 case, taken literally:\nno generator is constructed and `lead_s` is returned as-is, so \"the default is\nbyte-identical\" is a property of the code path rather than a claim about a distribution.\nThe draw is stateless and SEQ-KEYED (`SeedSequence([seed, tag, seq])`, one draw,\ndiscarded), which is what makes it survive this repo's constraints for free: nothing to\npickle across the spawn boundary, nothing to restore on a resume, and trailer #N drawing\nthe same lead in every arm of a run \u2014 common random numbers across arms.  Heterogeneity\nis the standing yard's alone (`inbound_spec` refuses a spread without it): v1 ranks by\ndispatch seq, so a spread there would shift arrival batches without scrambling any order.\n\n# \u2500\u2500 the supplier lead, and where it waits \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nThe SKU's `lead_time_mean` (batches, the catalogue's attribute) is its SUPPLIER lead \u2014\norder to ready-to-ship at the ordering site \u2014 and it is served BEFORE the trailer, never\ninstead of it (\"Declare the coverage against the inbound lead\", decision 2).  A reorder\nfired with a nonzero supplier lead waits that many batches in `_at_site`, the same\nbatch-denominated countdown the flag-off `BatchTransit` runs (advance one per\n`check_reorders`, release at zero), and loads onto the open trailer only when it\nreleases; lead 0 loads the instant it fires, as it always did.  The two stages are\nadditive, which is exactly what the coverage record prices (`attr_s + transit_days`).\nReleased orders load in FIRE order, ahead of anything fired in the same batch: the\nqueue is flushed before every load, so an order two batches old never queues behind\nthis batch's lead-0 newcomer \u2014 the tiebreak `BatchTransit.release` has by construction\n(queue order).  On a lead-free catalogue the queue is never entered, and every trailer,\nstamp and drain is byte-identical to the pipeline before the queue existed.\n\n# \u2500\u2500 what the doors do in v1, and deliberately do not \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nDoors are BOOKKEEPING in v1: every arrival lands at the batch epoch and every policy is\nFIFO, so a per-batch door throttle would invent staffing physics the shift-end decision\nalready declined \u2014 the crew's hours are the bound on receiving, exactly as the dock's\nno-backpressure note says.  The door count, free-door census and staging order are all\nreal and reported through `DockContext`, so the policies that make doors bite (arrival\nspread, non-FIFO staging) arrive as registry entries, not as rewiring.\n\n`YardTransit` below is where doors BECOME real: at most `doors` trailers staged, a trailer\nholds its door across drains until fully unloaded, and the yard-pull fires when a door\nfrees.  Gated by `INBOUND_STANDING_YARD`; flag-off binds this class untouched.\n\n# \u2500\u2500 the seam contract \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nMirrors `BatchTransit` exactly \u2014 `SOURCE`, `dispatch`, `advance`, `release`, `depth`,\n`merchandise()`, `snapshot()` \u2014 so the manager's phase bodies cannot tell which transit is\nbound.  `release()` returns `[sku, qty, remaining]` DELIVERIES (one per contiguous trailer\nlot, in unload order); the manager's ledger debit, packer and admission flow are untouched,\nand admissions carry `SOURCE = 'trailer'`, the fourth `PutawayItem` provenance.",
      "kind": "module",
      "signature": null
    },
    "Inbound/transit.py::DISCARDED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'discarded'"
    },
    "Inbound/transit.py::DONE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'done'"
    },
    "Inbound/transit.py::STANDING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'standing'"
    },
    "Inbound/transit.py::TrailerTransit": {
      "decorators": [],
      "doc": "Trailers from the order port to the dock doors, in one object.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/transit.py::TrailerTransit.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer_type: type=None, *, lead_s: float=0.0, lead_sigma: float=0.0, lead_seed: int=0, doors: int=DEFAULT_DOCK_DOORS, local_policy: str='fifo', bound: int | None=None)"
    },
    "Inbound/transit.py::TrailerTransit._depart": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer: Trailer) -> None"
    },
    "Inbound/transit.py::TrailerTransit._load": {
      "decorators": [],
      "doc": "Load one order, item-by-item, FIFO next-fit across trailers.\n\nThe trailer's lead is drawn HERE, at creation, and never again: it is a property\nof the trailer from the instant it exists, so `dispatched_s + lead_s` is a fixed\narrival the yard can be sorted by however many drains later it is observed.\n`dispatched_s` is the epoch of the drain the order LOADED in \u2014 after its supplier\nlead, not the drain it fired in.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, unit_volume: int | None, now_s: float | None) -> None"
    },
    "Inbound/transit.py::TrailerTransit._release_site": {
      "decorators": [],
      "doc": "Load every order whose supplier lead has elapsed (remaining <= 0), in queue \u2014\ni.e. fire \u2014 order.  Called at the top of every `dispatch` and every `release`,\nso a due order rides the same drain whether or not anything fired that batch.\nA negative remainder is never clamped, exactly as `BatchTransit` leaves it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now_s: float | None) -> None"
    },
    "Inbound/transit.py::TrailerTransit._site_qty": {
      "decorators": [],
      "doc": "Pieces waiting at the ordering site \u2014 one definition for both censuses.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::TrailerTransit._site_rows": {
      "decorators": [],
      "doc": "(sku, qty, remaining_batches) per site entry, in fire order \u2014 the snapshot's\nleading rows in both classes.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::TrailerTransit.advance": {
      "decorators": [],
      "doc": "One batch elapsed: orders waiting their supplier lead move one tick closer.\n\nThe trailer stage is untouched \u2014 its leads are absolute-clock seconds, and a batch\ntick moves nothing there.  The tick is one BATCH, the manager's `LEAD_TIME_UNIT`,\ncounted exactly as `BatchTransit.advance` counts it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Inbound/transit.py::TrailerTransit.depth": {
      "decorators": [
        "property"
      ],
      "doc": "In-flight ENTRIES: orders waiting at the ordering site, trailers loading, in\ntransit, or standing in the yard.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::TrailerTransit.dispatch": {
      "decorators": [],
      "doc": "Accept one fired reorder: queue it for its supplier lead, or load it now.\n\n`lead` (batches) is the SKU's SUPPLIER lead \u2014 the catalogue's `lead_time_mean` as\n`_fire_reorders` rounds it \u2014 and it is served HERE, in front of the trailer: a\npositive lead joins `_at_site` and loads `lead` drains later (`advance` ticks it,\n`_release_site` loads it at zero); lead 0 loads this instant.  The trailer's own\nlead is a different stage \u2014 seconds on the absolute clock, drawn per trailer by\n`lead_for` \u2014 and the two ADD, which is what the coverage record prices.\n\nAnything already due at the ordering site loads FIRST, so a released order never\nqueues behind this batch's lead-0 newcomers: loading order is fire order, the\ntiebreak `BatchTransit.release` has by construction.  `unit_volume` None falls\nback to one item per position-volume share of nothing \u2014 callers on this seam\npass `order.volume()`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, lead: int, unit_volume: int | None=None, now_s: float | None=None) -> None"
    },
    "Inbound/transit.py::TrailerTransit.lead_for": {
      "decorators": [],
      "doc": "Trailer `seq`'s transit lead, in absolute-clock seconds.\n\nSpread 0 \u2014 the default, and every run before this existed \u2014 returns the median\nUNTOUCHED, constructing no generator and consuming no entropy.  The scalar path is\nnot re-derived through a degenerate distribution; it is the same expression it\nalways was, which is why the byte-identity claim here is a property of the code\nrather than a tolerance on a comparison.\n\nOtherwise `median \u00b7 exp(\u03c3 \u00b7 Z_seq)`, lognormal: strictly positive, so nothing needs\nclamping, and with the right tail that does the work \u2014 stragglers overtaken by\nlater dispatches (the ordering lever's gradient) and trailers at risk of crossing\nthe fee threshold while they stand.\n\nA PURE FUNCTION of `(seed, tag, seq)`, deliberately: no generator object exists to\npickle across the spawn boundary or restore on a resume, drawing lead #7 never\ndepends on whether #0..#6 were drawn first, and trailer #N gets the same lead in\nevery arm of a run \u2014 common random numbers across arms, at no cost.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, seq: int) -> float"
    },
    "Inbound/transit.py::TrailerTransit.merchandise": {
      "decorators": [],
      "doc": "Pieces not yet released to the dock \u2014 the in_transit_qty level.  An order\nwaiting at the ordering site counts: the ledger credited it when it fired.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::TrailerTransit.release": {
      "decorators": [],
      "doc": "Everything the dock can start on this batch, as [sku, qty, 0] deliveries.\n\nOrder of operations IS the model: (0) orders whose supplier lead has elapsed load,\nin fire order; (1) anything still loading departs \u2014 a trailer waits for nothing\nin v1; (2) arrivals (lead elapsed against `now_s`) join the yard; (3) the yard\nstages to doors and staged trailers are worked in GLOBAL-priority order, each\ntrailer's pallets in LOCAL-priority order, emitting one delivery per contiguous\nSKU lot \u2014 so a split shipment packs as the pieces it arrived in.  Worked trailers\nfree their doors within the drain (v1: doors are bookkeeping; see the module note).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now_s: float | None=None) -> list"
    },
    "Inbound/transit.py::TrailerTransit.snapshot": {
      "decorators": [],
      "doc": "(sku, qty, remaining_lead) tuples for the replay rows: the batches left for an\norder waiting at the ordering site, then 1 for anything riding a trailer's lead\nand 0 for standing \u2014 batches were never the trailer stage's unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::YardTransit": {
      "decorators": [],
      "doc": "The standing yard: doors become real, and a trailer keeps its door until empty.\n\nWhat changes against the parent, and nothing else: `release()` is PURE CALENDAR \u2014\ndeparts loaders, lands arrivals in the yard, returns NO deliveries \u2014 and every door\nand crew decision moves to `_receive`, which drives the standing surfaces below over\nDRAIN-FROZEN rankings.  The parent's `release()` body is not edited; flag-off binds\nthe parent untouched, which is the first of the four byte-identity layers.\n\n# \u2500\u2500 the standing surfaces (what `_receive` drives) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nThe manager owns `_originals`, the packer and the crew, so it cannot live here \u2014 and\nthis class may not reach any of those, so the split is: THIS object holds trailer,\nyard and door STATE (`unplanned`/`planned_lots` for plans-at-arrival, `freeze_ctx`/\n`yard_order`/`dock_order` for the frozen rankings, `stage`/`door_freed`/`discard` for\nthe door lifecycle, `staged`/`free_doors` for the census) and the MANAGER makes every\ndecision over them.  The other transits satisfy these surfaces trivially \u2014 no\n`STANDING` attribute, no standing work \u2014 which is why neither is edited.\n\n# \u2500\u2500 invariants worth naming \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n* The yard is kept in (arrival stamp, `seq`) order \u2014 the charter's entry order \u2014 so\n  the stable ranking sort breaks stamp ties by seq for free.\n* At most `doors` trailers are staged; `stage` raises past that rather than quietly\n  widening the dock.\n* Decisions are drain-quantized, data is event-stamped: `stage`/`door_freed` take the\n  ABSOLUTE instant the caller derived (drain epoch, or epoch + crew-clock offset for\n  a mid-drain refill), and `stamps` keeps one\n  `(seq, arrived_s, staged_s, emptied_s, status)` tuple per finished trailer \u2014 the raw\n  material the `yard_trailers` table reports from, kept here because the trailer object\n  itself is dropped when it empties.  The STATUS is stamped by whichever door the\n  trailer left through rather than re-derived downstream from the null pattern: the\n  two producers know which they are, and a reader inferring `discarded` from \"never\n  staged\" would silently reclassify the day someone stages a trailer they then drop.\n* The parent's trailer order is UNREAD here \u2014 the yard/dock registries are the\n  standing model's split of that decision.  There was a `GLOBAL_POLICIES` registry and\n  an `INBOUND_GLOBAL_POLICY` knob behind it until 2026-09-16; this class is the reason\n  they could never gain a second entry, so both were deleted.",
      "kind": "class",
      "signature": "(TrailerTransit)"
    },
    "Inbound/transit.py::YardTransit.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer_type: type=None, *, lead_s: float=0.0, lead_sigma: float=0.0, lead_seed: int=0, doors: int=DEFAULT_DOCK_DOORS, yard_policy: str='fifo', dock_policy: str='fifo', local_policy: str='fifo', bound: int | None=None, allocation: str='split', door_team: int | None=None)"
    },
    "Inbound/transit.py::YardTransit.depth": {
      "decorators": [
        "property"
      ],
      "doc": "In-flight ENTRIES: loading, in transit, standing in the yard, or staged with\na remainder \u2014 a trailer leaves the census only when its last unit comes off.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::YardTransit.discard": {
      "decorators": [],
      "doc": "Drop a yard trailer whose plan packed NOTHING (shortfall to zero) \u2014 there is\nno work to stage a door for.  The ledger debit is the manager's, done where the\nshortfall was measured; this only keeps the yard free of undrainable entries.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer: Trailer, at_s: float | None) -> None"
    },
    "Inbound/transit.py::YardTransit.dock_order": {
      "decorators": [],
      "doc": "The drain-frozen DOCK ranking over staged trailers: the crew-allocation\npreference, and the canonical handoff order's first key.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, ctx: DockContext) -> list"
    },
    "Inbound/transit.py::YardTransit.door_freed": {
      "decorators": [],
      "doc": "A trailer came up empty: stamp it, free its door, and drop its plan.\n\nThe pack plan and pending list die here (the LoadPlan-retention lesson \u2014 nothing\nmay pin a run's worth of live StorageUnits); the stamps survive in `stamps`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer: Trailer, at_s: float | None) -> None"
    },
    "Inbound/transit.py::YardTransit.drain_stamps": {
      "decorators": [],
      "doc": "Hand over the finished-trailer stamps and start the list over.\n\nDrained rather than read for `drain_putaway_records`' reason: the caller takes\nownership once per batch, so this object never holds a run's worth of rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::YardTransit.free_doors": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::YardTransit.freeze_ctx": {
      "decorators": [],
      "doc": "The drain's frozen context: computed once, before any staging or unload.\n`ctx.gain` rides here the way `ctx.space` rides the manager's freeze \u2014 the\ninjected bundle if a gain policy runs, None otherwise.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> DockContext"
    },
    "Inbound/transit.py::YardTransit.merchandise": {
      "decorators": [],
      "doc": "Pieces not yet handed to the put queue \u2014 mirrors the deferred ledger: full\nloads before arrival, PLANNED quantities once a plan exists (a packing shortfall\nwas already debited at arrival), remainders only on staged trailers \u2014 and, in\nfront of all of it, what still waits at the ordering site.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::YardTransit.planned_lots": {
      "decorators": [],
      "doc": "(sku, qty) per contiguous lot, in canonical LOCAL-priority order \u2014 the exact\nportions the parent's `release()` would have emitted as deliveries, so the pack\nplan is v1's, fixed at loading, whatever later interrupts the unload.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer: Trailer, ctx: DockContext) -> list"
    },
    "Inbound/transit.py::YardTransit.release": {
      "decorators": [],
      "doc": "Pure calendar: loads what the supplier lead released, departs loaders, lands\narrivals in the yard, delivers NOTHING.\n\nThe unload pull lives in `_receive` because it is LABOUR \u2014 a lead elapses whether\nor not anyone is at work, but nothing comes off a trailer without a crew.  An\nempty return keeps `_release_arrivals` a structural no-op: no ledger debit, no\npacking, no admission \u2014 the deferred-until-unload contract.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now_s: float | None=None) -> list"
    },
    "Inbound/transit.py::YardTransit.snapshot": {
      "decorators": [],
      "doc": "(sku, qty, remaining_lead) rows: the batches left at the ordering site, 1\nriding a lead, 0 standing \u2014 yard trailers whole, staged trailers by their\nremainders.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::YardTransit.stage": {
      "decorators": [],
      "doc": "Move one yard trailer to a door.  `at_s` is the event instant: the drain epoch\nfor the whistle-independent fill, epoch + crew-clock offset for a refill.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, trailer: Trailer, at_s: float | None) -> None"
    },
    "Inbound/transit.py::YardTransit.staged": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::YardTransit.standing_stamps": {
      "decorators": [],
      "doc": "Rows for every trailer still on site \u2014 the CENSORED detention the run ends in.\n\nNon-destructive, and deliberately: nothing has finished, so nothing may be\nforgotten.  `emptied_s` is NULL here and that null is the whole point \u2014 the\ndetention span of these trailers is right-censored at the run end, not zero.\nUnder an adversarial ordering (`lifo`) this is exactly where the concentrated\noverage sits, so a table that dropped these rows would report `lifo`'s fee as\nCLIPPED rather than concentrated, which is the opposite of its signal.\n\nYard and staged alike: a trailer at a door with units still on it has been held\njust as long as one that never reached one.  `staged_s` tells them apart.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::YardTransit.unplanned": {
      "decorators": [],
      "doc": "Yard trailers with no pack plan yet \u2014 arrived this drain, in yard order.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Inbound/transit.py::YardTransit.yard_depth": {
      "decorators": [
        "property"
      ],
      "doc": "Trailers standing in the yard RIGHT NOW \u2014 the live twin of the frozen\n`DockContext.yard_depth`.  Read at drain end, where the frozen one is stale by\nexactly the staging this drain did, which is the whole measurement.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Inbound/transit.py::YardTransit.yard_order": {
      "decorators": [],
      "doc": "The drain-frozen YARD ranking: which standing trailer takes the next freed\ndoor.  Consumed front-first by every same-drain refill \u2014 no mid-drain re-score.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, ctx: DockContext) -> list"
    },
    "Inbound/unload.py": {
      "decorators": [],
      "doc": "unload \u2014 what it costs to take merchandise off a trailer.\n\n# \u2500\u2500 the put-away cost, minus the two terms a dock does not have \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`putaway.put_cost` is `travel + per_pick(height_multiplier(...), intercept, handle_var(...),\nquantity)`.  An unload keeps the handling term verbatim and drops both of the others:\n\n**No travel.**  `put_cost`'s travel is `x_phys * speed.x_pace + y_phys * speed.y_pace`\nagainst a BIN.  A receive has no bin, and there is no dock coordinate anywhere in the model\n-- grepping `dock|trailer|unload` across `Warehouse/` finds this file and prose.  The\ntempting shortcut is to call `put_cost` with the dock at `(0, 0)`: travel comes out exactly\n0.0 and `height_multiplier(brackets, 0.0)` comes out exactly 1.0, so it returns a plausible\nnumber while silently asserting that unloading involves no travel at all.  Same for a\n`dock_travel = 0.0` field: it is a slot for a number no layout can validate, and setting it\nto 200 would assert 200 inches of x-travel corresponding to nothing.  So the term is ABSENT,\nand this paragraph is the record that its absence is a decision.\n\n**No height multiplier.**  A trailer floor is one height.  `UnloadCost` therefore has no\n`height_brackets` field at all, rather than one defaulted to a flat bracket -- the field's\nabsence is what makes the claim checkable.\n\n**No speed, and therefore no mode.**  Without a travel term there is nothing for a speed to\nscale, so `unload_cost` takes no `SpeedProfile`.  The consequence is worth stating plainly\nbecause someone will want a knob for it: *this model cannot say whether a forklift crew\nunloads faster than a hand crew.*  Only crew SIZE changes a receiving makespan, because size\nis the number of clocks.  A mode knob would label rows without moving a second, and a sweep\nover it would publish \"mode makes no difference to receiving\".  When an unload gains a\ntravel term, mode arrives with it.\n\n# \u2500\u2500 the granularity, which moves the answer by an order of magnitude \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`per_pick(mult, intercept, var, qty, per_item)` is `mult * (intercept + qty * per_item +\nqty * var)`, so the intercept is charged ONCE PER CALL.  A 60-unit arrival therefore costs\none intercept if it is costed as one call and sixty if it is costed per pack -- and at the\nput-away default that is a 59 x 0.5 s difference on a single arrival, scaling with the run.\n\nThe charge is **per `StorageUnit`**: one call per pack, because a pack is what a person\nlifts, and the fixed grab-orient-set-down charge belongs to the lift.  **So is the per-item\ncharge**, by decision (`.scratch/department-calibration`, \"Define the calibrated era\"): a\nreceiver handles and labels the PACK, not the items inside it, so the charge lands once per\npack rather than once per item -- an order split into five pallets and a bag of singletons\nis six intercepts and six per-item charges, never six intercepts and sixty charges.  It is\ntherefore added OUTSIDE `per_pick`'s quantity term below rather than passed as `per_item`.\nThere is deliberately no per-DELIVERY setup term (backing the trailer in, opening the\ndoors): its default would have to be invented, and a coefficient defaulted to 0.0 is dead\ncode that rots.",
      "kind": "module",
      "signature": null
    },
    "Inbound/unload.py::UnloadCost": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "The unload time model's coefficients \u2014 the receiving twin of `PutawayCost`.\n\nEvery default is the PUT-AWAY default **by reference**, not by copied literal.  That is\nthe whole point: `PutawayCost`'s own docstring warns against \"a second set of magic\nnumbers to reconcile later (this project has already paid for two default sets that\ndrifted 55x apart)\", and a reference makes the drift structurally impossible rather than\nmerely a diff someone might notice.  Change a put-away coefficient and this moves with\nit, which is right until someone measures a dock and has a reason to separate them.\n\nThe one declared divergence is `RECV_INTERCEPT_SCALE` (default 1.0): the receive\nintercept is put-away's \u00d7 that scale, and the per-item charge is put-away's, charged\nonce per PACK (module docstring).  `from_putaway` is how a run builds one from the put\ncrew's own cost and the run's scale, so the chain picking -> put-away -> receiving is\nthe same chain at runtime as it is here at the class defaults.\n\nNo `height_brackets`, and no speed \u2014 see the module docstring for why each is absent\nrather than defaulted.",
      "kind": "class",
      "signature": ""
    },
    "Inbound/unload.py::UnloadCost.from_putaway": {
      "decorators": [
        "classmethod"
      ],
      "doc": "The receiving cost derived from the put crew's \u2014 the runtime form of the\nby-reference defaults above, so a run whose pickers charge 15 s a line receives\nat 15 \u00d7 PUT_INTERCEPT_SCALE \u00d7 RECV_INTERCEPT_SCALE rather than the kernel's 0.5.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, put: PutawayCost, *, intercept_scale: float=DEFAULT_RECV_INTERCEPT_SCALE) -> 'UnloadCost'"
    },
    "Inbound/unload.py::unload_cost": {
      "decorators": [],
      "doc": "Seconds to take ONE storage unit off a trailer:\n\n    intercept + per_item + quantity \u00b7 var\n\n`weight` and `volume` are PER ITEM \u2014 `order.weight` and `order.volume()`, exactly as\n`put_cost` takes them.  Passing a pack's or a trailer's volume instead inflates the log\nterm by `ln(quantity)` per unit, and `handle_var` clamps its log input to >= 1 so it\nnever raises: a 34-pallet arrival would simply cost as if every item were 34x bigger.\n\n`quantity` is the merchandise in THIS pack (`unit.quantity`), so the intercept AND the\nper-item charge are charged once for the pack and only the variable term once per item\nin it.  The per-item charge is added outside `per_pick`'s quantity term on purpose \u2014\npassing it as `per_item` would charge it `quantity` times, which is the pick model, not\nthe receiving one (module docstring, \"the granularity\").\n\nReturns SECONDS, the unit the whole simulator counts in\n(`Warehouse.kernel.timeline.TIME_UNIT`).",
      "is_async": false,
      "kind": "function",
      "signature": "(weight: float, volume: float, quantity: int, cost: UnloadCost) -> float"
    },
    "Optimization/Performance_Evaluations/__init__.py": {
      "decorators": [],
      "doc": "Performance_Evaluations \u2014 registry-driven analysis/graph system.\n\nEach graph/analysis is a small self-registering module under one of the category\nsubpackages (per_strategy/, comparison/, breakdown/, stats/, aggregate/).  A module\ndeclares an Evaluation via the @evaluation decorator (core/registry.py); importing the\npackage walks every submodule so the decorators fire and populate the registry.\n\nThe driver (driver.py) builds an EvalContext once per config (and an AggregateContext\nper cross-profile group), then runs the evaluations named by a preset (presets.py).\nAdding a graph = drop a file here + add its key to a preset; no central script to edit.\n\nmatplotlib's Agg backend is set HERE, before any submodule imports pyplot, so every\nworker process (Windows spawn re-imports this package) is headless-safe.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/aggregate/__init__.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/aggregate/sig.py": {
      "decorators": [],
      "doc": "agg.sig \u2014 the cross-profile significance FIGURES (the aggregate mirror of the\nper-config significance suite, over the steady-state scalar metrics).\n\nReuses the shared significance painters and the shared aggregate computation from the\naggregate tables module, so the panels show exactly the numbers the tables carry.  The\n`by_initial` fork (default True) renders the headline assignments \u00d7 metrics improvement\nheatmap plus one forest plot per assignment function; the all-strategies fork renders\none merged distribution+effect panel per steady-state metric.  Distribution axes are in\npresentation units \u2014 items-per-hour for rates, and for a DURATION the time unit resolved\nat render time from that metric's own pooled values, because a fixed hours axis reads\n0.0008 whenever the metric's magnitude is seconds; effects and % improvements are\nunitless.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/aggregate/sig.py::_render_all": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, out)"
    },
    "Optimization/Performance_Evaluations/aggregate/sig.py::_render_by_initial": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, out)"
    },
    "Optimization/Performance_Evaluations/aggregate/sig.py::render": {
      "decorators": [
        "evaluation(key='agg.sig', label='Cross-profile significance figures', scope='aggregate', needs=('series',), family='significance', shape='effect', defaults={'by_initial': True})"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py": {
      "decorators": [],
      "doc": "agg.tables \u2014 the cross-profile significance DATA products (strategies paired by\nprofile over the steady-state scalars).\n\nThe aggregate mirror of the per-config tables evaluations: the numbers land flat in the\ndeclared tables dir under the aggregate root, the figures live in the aggregate\nsignificance eval, and BOTH are built from the shared computation in this module\n(`compute_aggregate_stats` / `compute_aggregate_by_initial`) so they cannot disagree.\nThe retired per-assignment-function directory tree is gone \u2014 the by-initial fork writes\none combined CSV (exact retired columns) plus ONE consolidated test document keyed by\nassignment function.\n\nValues are the RAW per-profile steady-state scalars from each profile's series\ndocument \u2014 not the \u00d7-baseline-normalized curves the trajectory painter draws \u2014 so the\ndescriptives keep honest units, exactly as the retired suite computed them.  The\n`by_initial` param picks the fork (default True, matching the by-initial preset).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py::MIN_PAIRED_PROFILES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3"
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py::_finite": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(x)"
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py::compute_aggregate_by_initial": {
      "decorators": [],
      "doc": "The uni-vs-opt contrast per assignment function, paired by profile.\n\nReturns (combined, per_fn) shaped like the per-config counterpart:\n  combined  rows with EXACTLY the retired summary columns (assignment, metric,\n            n_profiles, uni_median, opt_median, pct_change_opt_vs_uni, p_wilcoxon,\n            better) \u2014 medians/p computed with the retired semantics (each family's\n            values collected and finite-filtered independently; p only when the two\n            samples pair up 1:1);\n  per_fn    {fn: {'keys', 'metrics': {name: detail}}} where the detail's u/o arrays\n            ARE profile-paired (profiles holding both arms, both finite) so the\n            test document and the significance panels rest on honest pairs.",
      "is_async": false,
      "kind": "function",
      "signature": "(profile_series_list)"
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py::compute_aggregate_stats": {
      "decorators": [],
      "doc": "The all-strategies suite over the steady-state scalars, paired by profile.\n\nReturns (keys, colors, summary_rows, per_metric):\n  keys / colors  the strategies common to EVERY profile, first-profile order;\n  summary_rows   {'strategy', 'metric', **descriptives} rows \u2014 the CSV body;\n  per_metric     {metric_name: {'box_values', 'tests', 'lower'}} for the panels.",
      "is_async": false,
      "kind": "function",
      "signature": "(profile_series_list)"
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py::paired_profile_diagnosis": {
      "decorators": [],
      "doc": "Why a by-initial contrast produced nothing \u2014 in the reader's terms, not a shrug.\n\nReturns '' when it should have produced something.",
      "is_async": false,
      "kind": "function",
      "signature": "(profile_series_list) -> str"
    },
    "Optimization/Performance_Evaluations/aggregate/tables.py::render": {
      "decorators": [
        "evaluation(key='agg.tables', label='Cross-profile significance tables', scope='aggregate', needs=('series',), out_subdir='tables', defaults={'by_initial': True})"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/aggregate/traj.py": {
      "decorators": [],
      "doc": "agg.traj \u2014 cross-profile over-time trajectories, percent view only.\n\nThe aggregate series (AggregateContext.agg_series) are already \u00d7-baseline normalized\nper profile and averaged across profiles (common/series.py), so the baseline strategy's\nown curve sits at \u22481.0 \u2014 exactly the reference the shared over-time painter's percent\nview divides against, which is why this stage renders the SAME figures as the\nper-config trajectories family with zero aggregate-specific drawing code.  An absolute\nview would just restate the normalization (every axis \"\u00d7 baseline\"), so only the\npercent view is declared.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/aggregate/traj.py::SERIES_QUANTITIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "SERIES_ORDER"
    },
    "Optimization/Performance_Evaluations/aggregate/traj.py::render": {
      "decorators": [
        "evaluation(key='agg.traj', label='Cross-profile trajectories (% vs baseline)', scope='aggregate', needs=('series',), family='trajectories', shape='serial', quantities=SERIES_QUANTITIES, views_suppressed=(('absolute', 'the aggregate series are already x-baseline normalized per profile and averaged across profiles, so an absolute view would restate the normalization: every axis would read \"x baseline\" and every baseline curve would sit at 1.0'), ('delta', \"a difference of two already-normalized ratios is not in the quantity's units and is not in any other units either; the percent view is the honest one at this scope\")))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/catalog/__init__.py": {
      "decorators": [],
      "doc": "catalog.* evaluations \u2014 what the run WAS, emitted from the run.\n\nNot results: identity.  Which rule is which Python symbol and what it optimises; what the\ninventory model actually produced as opposed to what its formula says; which factors were\nvaried and which were held fixed.  Every one of these was hand-written prose on the\npublished site, and every one of them was found drifting from the code or the data it\ndescribed.\n\nRun scope, because the answers are properties of the whole sweep rather than of a leaf,\nand because emitting them beside the results is what makes them versioned WITH the results\ninstead of maintained alongside them.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py": {
      "decorators": [],
      "doc": "catalog.fixed \u2014 what this sweep VARIED, and what it held still.\n\n\"Not tested\" is a finding, and it was the one part of the published methodology that had no\nartifact behind it.  A reader asking \"is this sensitive to safety stock?\" got silence, which\nreads as an oversight rather than as a scope boundary \u2014 and a reader asking \"what else did\nyou hold fixed?\" had to reconstruct the answer from four config files.\n\nThis derives both halves from the run itself.  A factor is VARIED when the run's own tree\ncarries more than one distinct value of it, and FIXED when it carries exactly one; the\nfixed list is therefore complete by construction rather than by an author remembering.  A\nfactor whose value never appears anywhere \u2014 inter-aisle travel, which no scheduler models \u2014\nis reported as ABSENT, which is a third and different thing: not held at a value, but not a\nknob this model has.\n\nThe three-way split is only worth having if the third bucket is policed, and it was not.\nSAFETY STOCK sat in `_ABSENT` claiming the plan builder \"takes no safety-stock parameter\",\nwhich was true of the generator's closed form and stopped being true when ADR-0002 retired\nit: a run now declares `safety_days` on `STAFFING_KEYS`, with a `--safety-days` flag, and\nderives its reorder point from it.  So it is a knob HELD FIXED, and the correction is\nstructural rather than a re-worded line \u2014 the two families of factor come from two different\nrecords.  `_CONFIG_FACTORS` are per-leaf and read out of each leaf's `config_json`;\n`_SPEC_FACTORS` are declared once for the whole run and read out of the run spec's staffing\nrecord.  A factor listed in the wrong family does not merely mis-report: it never appears in\na leaf, so it drops out of the register entirely, which is the shape of the original mistake.\n\nBoth records are named by their CONTRACT alias throughout \u2014 `config_json`, `run_spec` \u2014 and\nnever by filename: the path is `rt.path`'s business, and a literal spelled even in a comment\nis the hand-joined-path debt the run-tree ratchet exists to stop.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_ABSENT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('breaks_and_shifts', 'Breaks, lunches and shift changes WITHIN a working day are not modeled: a crew works continuously from the moment its day opens until its whistle. The day itself is modeled ..."
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_CONFIG_FACTORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('n_batches', 'waves per run'), ('num_pickers', 'crew size'), ('total_aisles', 'aisles'), ('total_bins', 'bins'), ('n_skus', 'stocked SKUs'), ('sampler', 'demand sampler'), ('pick_intercept', 'per..."
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_PATH_KEYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('profiles_dir', 'base_dir', 'output_dir', 'inv_db', 'aff_db')"
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_SPEC_FACTORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('coverage_days', 'stock coverage, in days of demand'), ('safety_days', 'safety stock, in days of demand'), ('floor_lines', \"the stock floor, in lines of the SKU's own mean line\"))"
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_levels": {
      "decorators": [],
      "doc": "{factor: sorted distinct values} across the whole run \u2014 the tree, the leaves, the spec.\n\nThree sources, one rule.  The tree supplies the sweep's own axes (cell, inventory, pick\nconfig, channel); each leaf's `config_json` supplies `_CONFIG_FACTORS`; the run spec's\nstaffing record supplies `_SPEC_FACTORS`, which no leaf carries.  Whatever the source, a\nfactor with one distinct value is FIXED and one with several is VARIED, so a run-level\ndeclaration is reported exactly as honestly as a per-leaf one.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_levels.add": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name, value)"
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::_publishable_spec": {
      "decorators": [],
      "doc": "The run's shaping scalars, with anything path-shaped removed.\n\nScalars only (a nested list is the sweep's own matrix, reported by `varied` instead),\nand no value that looks like a filesystem location \u2014 a key name is not enough to go\non, so the value is checked too.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec: dict) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::register": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/fixed.py::render": {
      "decorators": [
        "evaluation(key='catalog.fixed', label='What the sweep varied, and what it held fixed', scope='run', out_subdir='')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py": {
      "decorators": [],
      "doc": "catalog.inventory \u2014 the stock levels this run DECLARED, published as a distribution.\n\nWHY THIS EXISTS.  A page has to say how much stock the simulation held, and the honest answer\nhas never been a formula.  It used to fail to be a formula for a particular reason: the\nCATALOGUE authored each SKU's order-up-to quantity and reorder point, the published pages\nrestated the generator's closed form, and measured against the run's own file that form\nreproduced neither quantity \u2014 the planner then rescaled both to fit the warehouse it had\nalready built (`sample_to_capacity`, since retired).  So this tool scored the candidate\nclosed forms against the stored values and published the implied rescale beside them, on\nthe principle that \"neither formula holds\" should be a measurement rather than an\nassertion.\n\nBoth of those measurements are now about a mechanism that no longer exists.  ADR-0002 retired\nthe authored level: a stock level is not a fact about a SKU, and a generated catalogue carries\nnone.  Every run DERIVES its levels at setup from a coverage in DAYS\n(`Optimization/simconfig/coverage.py`, driven to a pair-level fixed point by\n`Optimization/simdriver/era_coverage.py`) and records them in its own planned inventory.  No\nclosed form authors a level any more, so a reproduction rate would score the agreement of two\nthings the run never used, and the planner growth the rescale ratio measured has no generator\nfigure left to grow away from.  Both blocks are gone.  What remains is the reason the tool\nexisted in the first place: PUBLISH THE DISTRIBUTION the run actually stocked.\n\nWHAT THIS EMITS.  Per inventory pair: the realised distribution of the three declared levels\n(order-up-to quantity, reorder point, lead pipeline) and of the lead time the reorder point is\ndenominated in, how many SKUs the run declared on, how many carry a hand-written packing plan,\nand THE DECLARATION ITSELF \u2014 `coverage_days` / `safety_days` / `floor_lines`, read off the\nrun's own staffing record (`staffing.calibration[<pair>].coverage`), which is where a run\nstates what produced these numbers.  A page renders the distribution and names the\ndeclaration; change the declaration and both change with it.\n\nWHICH FILE, AND HOW IT IS READ.  The `planned_inventory` contract alias \u2014 a RUN'S OWN planned\ninventory, never a generated catalogue, because only a run declares.  Read through\n`Schema.dataset`'s named queries instead of hand-written SQL against `cartons`: the\n`stock_levels` query serves the declaration out of the table that holds it on a current file\nand, through its per-vintage override, out of `cartons` on every archived one.  So an old run\nstill reports the levels it fielded, under the same logical names, with no version branch\nhere.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'inventory_db': {'stock_levels.equilibrium_qty': 'read', 'stock_levels.reorder_point': 'read', 'stock_levels.pipeline_qty': 'read', 'stock_levels.stock_plan': 'read', 'cartons.lead_time_mean': 're..."
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_CARTON_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('lead_time_mean',)"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_CSV_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('pair', 'quantity', 'n', 'mean', 'median', 'p5', 'p25', 'p75', 'p95', 'min', 'max')"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_LEVEL_FIELDS + _CARTON_FIELDS"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_LEVEL_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('equilibrium_qty', 'reorder_point', 'pipeline_qty')"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_declaration": {
      "decorators": [],
      "doc": "The coverage this run DECLARED for `pair` \u2014 the three knobs, off the staffing record.\n\nThe run states its own inputs; this never re-derives them.  Read defensively because an\nabsent declaration is DATA: a run whose assets were rebuilt from a frozen inventory, or\none that predates the coverage loop, records no `coverage` block, and the right report for\nit is \"the file's levels, declaration unrecorded\" rather than a raise or an invented\ndefault.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, pair: str) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_dist": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(vals: list) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::_dist.q": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(p)"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::floor_text": {
      "decorators": [],
      "doc": "The declared floor as words: `1 line(s)` for one number, `1.27 (store) / 1.29\n(fulfillment) line(s), solved` when each section solved its own (ADR-0004).",
      "is_async": false,
      "kind": "function",
      "signature": "(d: dict) -> str"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::pair_model": {
      "decorators": [],
      "doc": "Realised distributions for one pair's planned inventory, read by LOGICAL name.\n\n`dataset.bind` resolves the file to its OWN vintage and refuses one the family does not\nvet \u2014 deliberately left to raise.  This path publishes: a level read out of a shape\nnobody has vetted is exactly the plausible-wrong-number the schema layer exists to stop,\nand a run's pairs are all written by one build, so catching per pair would rescue\nnothing anyway.\n\nA pair that declares NOTHING is not an error and does not raise: `stock_levels` comes\nback empty (a generated catalogue frozen without a derivation, or a rebuild from an\ninventory that carried none), the level distributions are `{}`, and `n_declared` says so.\n\nNo `requires=` is passed, deliberately.  A `Requires` names PHYSICAL tables and would\nrefuse every pre-split vintage \u2014 whose `stock_levels` table does not exist, because its\noverride reads the declaration out of `cartons` \u2014 which is exactly the file this tool\nmost needs to read.  The named-query registry is the guarantee here: `UnsupportedQuery`\nnames the (vintage, query) pair when no SQL can serve it, by name and before any row.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/inventory.py::render": {
      "decorators": [
        "evaluation(key='catalog.inventory', label='The stock levels this run declared', scope='run', needs=('catalogue',), out_subdir=('', 'tables'))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/catalog/rules.py": {
      "decorators": [],
      "doc": "catalog.rules \u2014 the rule catalogue, as the RUN executed it.\n\n`Optimization/config/objectives.py` is the registry; this emits it into the run, joined\nagainst what the run actually swept.  The join is the point: a catalogue printed straight\nfrom the registry describes the code at read time, and a page rendering it beside results\nwould silently list rules the sweep never ran, or omit that a rule ran on one channel only.\nEvery entry therefore carries `ran`, the arms that carried it, and the channels it appeared\non \u2014 read from the run's own per-arm rows.\n\nMap-family entries additionally carry the measured solver split.  The published site says\nthe Map target comes from \"the full linear assignment problem\"; the exact solver has a size\ngate, and at production catalogue scale the large BinKey classes are far past it.  Whatever\nthe split was, it is now a field rather than an adjective.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/catalog/rules.py::_observed": {
      "decorators": [],
      "doc": "{rule: {'arms': [...], 'channels': [...], 'map_lap_pct': float|None}} from the run.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/rules.py::catalog": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rows) -> dict"
    },
    "Optimization/Performance_Evaluations/catalog/rules.py::render": {
      "decorators": [
        "evaluation(key='catalog.rules', label='What each placement rule is, and whether it ran', scope='run', needs=('runtime',), out_subdir='')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/common/__init__.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/chartkit.py": {
      "decorators": [],
      "doc": "chartkit \u2014 the one rendering contract every figure in the suite draws through.\n\nThe retired ad-hoc era had 18 distinct figsize formulas, three legend idioms, four sign\nconventions, a provenance footer that collided with tick labels in a third of the chart\ntypes, and axes anchored at zero that compressed 2\u20133% treatment effects into the top tenth\nof the canvas.  This module replaces all of that with ONE set of policies:\n\n  SIZING    computed from content (panel count, category count, legend label extents),\n            never a per-module literal.\n  LEGEND    lives in a RESERVED right gutter (or below, for wide categorical charts) that\n            is part of the figure geometry \u2014 it cannot intersect the data region, and the\n            save path no longer needs bbox_inches='tight' rescue tricks.\n  FOOTER    the provenance line gets its own reserved band at the bottom of the figure;\n            nothing else may draw there.\n  TITLE     short (one clause, <= 60 chars); run keys and context belong to the footer.\n  AXES      y-limits hug the data (`data_ylim`) with an explicit truncation marker when\n            zero is excluded; panels either share a scale (`shared_ylim`) or say out loud\n            that they don't (`annotate_unshared`).\n  SIGN      `improvement_pct` \u2014 positive = better than baseline, everywhere.  No chart may\n            invent its own convention.\n  UNITS     durations render in a NAMED time unit \u2014 `to_time` picks hours/minutes/seconds\n            from the data's own magnitude (a picker task is seconds, a batch of labor is\n            hours) and returns the label with it; the phrase \"sim units\" is banned.\n  COLOR     one palette contract: color = assignment function (stable across every chart\n            of a run), dash = initial family, and the baseline is always drawn in\n            BASELINE_STYLE so FIFO is recognisable at a glance.\n  EFFECT    `effect_label` prints the effect size first and the significance stars second \u2014\n            with n=75 paired batches everything is `***`, so the stars alone discriminate\n            nothing.\n\nA render builds a `Chart` via `make(...)`, draws on `chart.axes`, then `chart.save(path,\nview=...)`.  `view` names which member of the family grammar (core/families.py) the figure\nis \u2014 the save asserts the filename carries the matching `<view>_` prefix so the folder\nlisting itself documents what each file shows.  Non-registry callers (the what-if writers,\nnotebooks) pass view=None and skip the grammar check.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::BASELINE_STYLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "dict(color='#222222', lw=2.2, ls='-', zorder=10)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::BOUND_STYLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "dict(color='#1a7a4d', lw=1.6, ls='--', zorder=3)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart": {
      "decorators": [],
      "doc": "A figure plus the reserved-geometry bookkeeping `make` computed for it.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, fig, axes, *, gutter_frac, legend_mode, legend_title, footer, gutter_cols=1, gs=None, margins=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart._panel_center_x": {
      "decorators": [],
      "doc": "Figure-fraction centre of the PANEL area, not of the whole canvas.\n\nA header centred on the canvas drifts right into the legend gutter, and a long\nsubtitle runs underneath it \u2014 which is how five trajectory figures ended up with\ntheir explanatory line clipped by the legend box.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.abandon": {
      "decorators": [],
      "doc": "Close the figure and return None \u2014 the \"nothing could be drawn\" exit.\n\nEight renders reached for `import matplotlib.pyplot as plt; plt.close(ch.fig)`\nmid-function to do this, and a render that forgot the close leaked a figure per\narm per leaf across a 21-minute pass.  The idiom is\n`if not drawn: return ch.abandon()`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.ax": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.fit": {
      "decorators": [],
      "doc": "Grow the canvas until nothing is clipped, keeping the footer band clear.\n\nCalled automatically by `save`.  The reserved bands solve the collisions chartkit\ncan predict, but not the one it cannot: how wide a caller's own tick labels turn\nout to be.  Seventeen rotated 20-character arm names need more left and bottom\nmargin than any fixed constant should promise, and the old answer \u2014 crop with a\ntight bbox \u2014 is what cut the provenance line off in the first place.  So measure\nafter drawing, and EXPAND rather than crop: the panels keep the size they were\ndesigned at, and the overflow gets canvas of its own.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.legend": {
      "decorators": [],
      "doc": "Place THE legend in the reserved gutter (or bottom strip).  Deduplicates\nidentical (label) rows \u2014 six rows reading the same number is a table, not a\nlegend.  A second call stacks below the first in the gutter.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, handles=None, labels=None, *, title=None, ncol=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.save": {
      "decorators": [],
      "doc": "Save through io._save_close.  `view` asserts the family-grammar filename\nprefix (`<view>_name.png`) for registry renders; what-if writers and notebooks\npass view=None and make no grammar claim.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, path, *, view=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::Chart.title": {
      "decorators": [],
      "doc": "SHORT title policy: one clause.  Run keys/context belong to the footer \u2014\nanything longer than 60 chars is a caption trying to be a title.\n\nBoth lines are placed a fixed DISTANCE below the top edge, not at a fixed\nfraction of the height: the title band is a constant number of inches, so on a\ntall figure (a 34-row ladder runs past 16 in) a fractional offset walks the\nsubtitle straight down into the axes.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, text, subtitle=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::HOURS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'hours'"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::REFERENCE_STYLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "dict(color=BASELINE_STYLE['color'], lw=1.2, ls='-', zorder=3)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_CHAR_W_IN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.062"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_DASHES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['-', '--', '-.', ':']"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_FOOTER_BAND": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.32"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_GUTTER_MAX_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_LEGEND_HEAD_H": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.34"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_LEGEND_ROW_H": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.165"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_PANEL_TITLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.3"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_ROW_GAP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.42 * _PANEL_H"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_TIME_UNITS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_units.TIME_UNITS"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_TITLE_BAND": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.55"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_assert_view_allowed": {
      "decorators": [],
      "doc": "Inside a registry render, the view must be one the evaluation's family declares.\nOutside a render (io._CURRENT_EVAL is None) there is no declaration to check.",
      "is_async": false,
      "kind": "function",
      "signature": "(view)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_gutter_layout": {
      "decorators": [],
      "doc": "(ncol, width, height) for a gutter legend that must FIT beside the panels.\n\nA reserved gutter only guarantees the legend cannot overlap the data if the legend\nalso fits vertically.  With one entry per arm a 34-arm overlay needs ~5.9 in of\ncolumn \u2014 taller than the panels \u2014 and a single column simply runs off the canvas and\nthrough the footer band.  So: add columns until it fits (up to a limit), then let the\ncaller grow the figure for whatever is still too tall.",
      "is_async": false,
      "kind": "function",
      "signature": "(labels, title, content_h)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_gutter_width": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(labels, title, ncol=1)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::_hspace": {
      "decorators": [],
      "doc": "Row gap as a CONSTANT physical distance, expressed in matplotlib's fraction units.\n\n`hspace` is a fraction of the mean axes height, so a tall panel gets a proportionally\ntaller gap: a 17-category ladder at ~6 in per panel opened a 2.5-inch band of white\nbetween rows, which reads as two unrelated figures stacked in one file.  The gap has\nto clear one x-label plus the next row's title, and that is a fixed number of inches\nno matter how tall the panels are.\n\nCapped at the historical 0.42 so nothing SHORTER than the default panel moves \u2014 this\nonly ever shrinks an oversized gap.",
      "is_async": false,
      "kind": "function",
      "signature": "(panel_h: float) -> float"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::annotate_bars": {
      "decorators": [],
      "doc": "Print each bar's value just past its end, on the outside, in the bar's direction.\n\nFour modules did this with four offset conventions, two of which put the text INSIDE\na short bar where it collided with the axis.  The offset is a fraction of the axis\nrange, so it does not shrink when the data does.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, positions, values, *, orient='h', fmt='{:+.1f}', fontsize=6.5, gap=0.01)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::annotate_points": {
      "decorators": [],
      "doc": "Label scattered points without letting the labels overlap each other.\n\n`points` is [(x, y, text), ...].  Labels are placed to the right of their point and\npushed apart vertically to at least `min_gap_frac` of the axes height, with a leader\nline back to the point whenever one had to move.  Series that end up close together\nare exactly the ones a reader most needs told apart \u2014 on this suite the arms that\nfinish within a few tenths of a percent of each other are the podium \u2014 and a pile of\noverlapping labels there is worse than none, because it looks like information.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, points, *, fontsize=6.5, dx=6.0, min_gap_frac=0.045, colors=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::annotate_unshared": {
      "decorators": [],
      "doc": "When panels legitimately cannot share a scale, they must SAY so.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, axis='x')"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::baseline_handle": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(label='FIFO baseline')"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::category_axis": {
      "decorators": [],
      "doc": "Tick, label and bound one axis of categories, so N categories always look alike.\n\n`orient='h'` lays the categories down the y axis (horizontal bars/dots, first at the\nTOP); 'v' lays them along x.  Returns the positions, which is what the caller plots\nagainst.\n\nEight modules did this by hand with four different tick fontsizes and four different\nlimit paddings, so the same 34 arms occupied visibly different amounts of chart from\none family to the next.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, labels, *, orient='h', fontsize=7, pad=0.8)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::data_ylim": {
      "decorators": [],
      "doc": "Clip y to the data range (+pad fraction) instead of anchoring at zero.  When the\nresulting window excludes zero the axis is visibly TRUNCATED: a marker glyph on the\ny-axis spine plus a small corner note, so nobody mistakes a zoomed axis for the\nfull range.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, values, *, pad=0.08, include=())"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::draw_ci": {
      "decorators": [],
      "doc": "Draw the confidence interval the stats layer computes (and, until now, nothing\never rendered).  band=True fills between series; band=False draws error whiskers.\n\n`orient='h'` puts the interval along X with `x` as the row positions \u2014 the ranked\ncategory case, where three modules hand-drew a line plus two end caps in a loop\nbecause this function only knew how to draw vertically.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, x, lo, hi, *, color='#555555', alpha=0.18, band=True, orient='v', **kw)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::effect_label": {
      "decorators": [],
      "doc": "Effect first, stars second: '\u22124.1% (g=0.62) ***'.  The paired n=75 design makes\nnearly every p three-star, so magnitude is what the label must lead with.",
      "is_async": false,
      "kind": "function",
      "signature": "(p, effect, *, kind='g', pct=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::height_for_categories": {
      "decorators": [],
      "doc": "Panel height for n horizontal category rows \u2014 readable at any n.",
      "is_async": false,
      "kind": "function",
      "signature": "(n, *, per=0.26, base=1.6, cap=10.0)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::improvement_pct": {
      "decorators": [],
      "doc": "Percent improvement vs baseline, POSITIVE = BETTER \u2014 the only sign convention\nany chart in this package may use.\n\nReturns NaN when the comparison is UNDEFINED (a zero, missing or non-finite\nbaseline), never 0.0.  A zero would read as \"no change\", and a batch where the\nbaseline did no work at all is not a batch where the two arms tied: fabricating\nties there drags a mean toward zero and tightens the interval around it, exactly\non the observations where the contrast is largest.  Callers that aggregate must\nuse `improvement_pct_series`, which drops the undefined pairs; callers that draw\none value get a point matplotlib skips.",
      "is_async": false,
      "kind": "function",
      "signature": "(value, baseline, *, lower_is_better)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::improvement_pct_series": {
      "decorators": [],
      "doc": "Paired per-element improvement percentages with the UNDEFINED pairs dropped.\n\nTHE way to build a sample of paired improvements: every statistic downstream (a\nmean, a median, a confidence interval, a test) is only honest over the pairs where\nthe comparison exists.  Returns a float array that may be shorter than its inputs \u2014\nand empty when no pair is comparable.",
      "is_async": false,
      "kind": "function",
      "signature": "(values, baselines, *, lower_is_better)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::make": {
      "decorators": [],
      "doc": "Build a Chart whose geometry is computed from its content.\n\npanels     total data panels; laid out on `ncols` columns (default: all in one row\n           up to 3, then near-square).\npanel_w/h  per-panel inches; category-heavy charts pass the `for_categories` helpers.\nlegend     'gutter' reserves a right-hand strip sized to `legend_labels`;\n           'bottom' reserves a strip below the panels (wide categorical charts);\n           'none' reserves nothing.\npanel_titles  the caller will `ax.set_title` each panel \u2014 matplotlib draws those\n           ABOVE the axes, i.e. into the suptitle band, so the band grows to hold\n           them.  Without this a faceted chart's first-row titles overprint the\n           subtitle, which is legible enough in a thumbnail to ship and illegible\n           at full size.\nThe suptitle band and the provenance footer band are always part of the geometry, so\nneither can ever collide with data, ticks, or the legend.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, panels=1, ncols=None, panel_w=_PANEL_W, panel_h=_PANEL_H, legend='gutter', legend_labels=(), legend_title=None, sharex=False, sharey=False, footer=True, panel_titles=False)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::mark_baseline": {
      "decorators": [],
      "doc": "Draw the baseline series so it reads as the reference in every chart.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, x, y, label='FIFO baseline')"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::mark_baseline_row": {
      "decorators": [],
      "doc": "Shade the baseline's own row/column so it reads as the datum, not a competitor.\n\nFour modules marked it four incompatible ways \u2014 a black bar, a grey bar, an annotation\nwith no mark, and nothing at all \u2014 so on one chart the baseline was the darkest thing\nand on the next it was invisible.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, index, *, orient='h', label=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::pct_axis": {
      "decorators": [],
      "doc": "Format an improvement axis: % ticks + the direction cue in the label position.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, *, better='up', axis='y')"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::rank": {
      "decorators": [],
      "doc": "Sort indices best-first, with ONE convention for a value that is not a number.\n\nSix sites sorted a ranked chart and used three different infinities to push the\nundefined entries somewhere; two of them pushed them to the TOP, so a metric that\nfailed to compute for an arm presented that arm as the winner.  Undefined always\nsorts LAST here, whichever direction is better.",
      "is_async": false,
      "kind": "function",
      "signature": "(values, *, lower_is_better)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::reference_line": {
      "decorators": [],
      "doc": "Draw the datum a comparison is read against, in the one house style.\n\n`orient='y'` means the QUANTITY is on y, so the datum is a horizontal line; 'x' means\nthe quantity is on x (a ranked horizontal bar chart) and the datum is vertical.  That\nis deliberately named after the quantity rather than after the line's direction: at 16\ncall sites the axis the quantity lives on is the thing the author knows, and half the\n`axhline`/`axvline` confusion came from translating it by hand.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, value=0.0, *, orient='y', label=None, legend_label=None, style=None, **kw)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::rolling_mean": {
      "decorators": [],
      "doc": "Moving average that does not fabricate a collapse at the ends.\n\n`np.convolve(..., mode='same')` pads with zeros, so a smoothed line dives toward zero\nover the first and last few points \u2014 at exactly the two places a reader looks for a\ntrend to start or finish.  Here the ends average only the points that exist.\n\nNOTE the scope.  This is the CHART-time smoother, promoted from `labor/per_batch`,\nwhich was the only edge-correct implementation of the three.  The trailing pandas\nsmoothing in `common/frames.py` and `common/series.py` is a different convention\napplied at a different layer (data preparation, not drawing) and is deliberately left\nalone: switching those to centred would move every published trajectory curve, which\nis a decision about the numbers rather than a cleanup.",
      "is_async": false,
      "kind": "function",
      "signature": "(y, win, *, centred=True)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::shared_ylim": {
      "decorators": [],
      "doc": "Force one honest scale across panels \u2014 the small-multiples rule.",
      "is_async": false,
      "kind": "function",
      "signature": "(axes, values_per_ax, *, pad=0.08, include=())"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::stars": {
      "decorators": [],
      "doc": "Significance stars \u2014 thresholds 1e-3 / 1e-2 / 5e-2 (moved here from painters).",
      "is_async": false,
      "kind": "function",
      "signature": "(p)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::strategy_color": {
      "decorators": [],
      "doc": "Stable color for a strategy dict: its upstream-assigned hex when present (the\nper-run summary publishes the same value, so charts and tables agree), else a\ndeterministic tab20 slot keyed by sorted assignment name within `strategies`.",
      "is_async": false,
      "kind": "function",
      "signature": "(s, strategies=None)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::strategy_dash": {
      "decorators": [],
      "doc": "Linestyle by initial family \u2014 uni solid, opt dashed, anything else dash-dot.\nThis is the ONLY meaning a dash pattern carries in the suite.",
      "is_async": false,
      "kind": "function",
      "signature": "(s)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::time_units": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_units.time_units"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::to_hours": {
      "decorators": [],
      "doc": "Duration sim values are SECONDS; render an HOURS axis.\n\nUse this only where hours is the right unit on its face \u2014 a run's elapsed time, a\nbatch's labor total.  For anything whose magnitude depends on the data, use\n`to_time`: durations in this model span five orders of magnitude (one picker task is\nseconds, a batch of labor is hours), and a fixed unit turns half the suite into\ncolumns of `0.0008`.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_seconds)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::to_time": {
      "decorators": [],
      "doc": "(converted_values, unit_label) for a set of sim-SECOND durations.\n\nPool everything that shares an axis into ONE call so every series on it lands in the\nsame unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_seconds)"
    },
    "Optimization/Performance_Evaluations/common/chartkit.py::width_for_categories": {
      "decorators": [],
      "doc": "Panel width for n vertical category columns.",
      "is_async": false,
      "kind": "function",
      "signature": "(n, *, per=0.5, base=3.0, cap=16.0)"
    },
    "Optimization/Performance_Evaluations/common/frames.py": {
      "decorators": [],
      "doc": "DataFrame builders + steady-state series alignment (verbatim from the monoliths).\n\n`_bdf`/`_tdf` turn the namedtuple stat lists from Picking_Data into the wide per-batch /\nper-task frames every graph consumes; `_roll` is the rolling-mean helper; `_metric_series`\nand `_aligned` build the paired matrices the stats suite tests.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/frames.py::MISSED_REASONS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('unpicked_unstocked', 'unpicked_unavailable')"
    },
    "Optimization/Performance_Evaluations/common/frames.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': {'batch_stats.duration': 'ratio', 'batch_stats.task_makespan': 'ratio', 'batch_stats.total_items': 'ratio', 'batch_stats.avg_concurrent_pickers': 'read', 'batch_stats.picking_pct': 'read..."
    },
    "Optimization/Performance_Evaluations/common/frames.py::_CARRY_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['batch_id', 'reason', 'sku', 'qty']"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_FREE_INDEX_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['batch_id', 'handling', 'category', 'size', 'unit', 'free']"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_SHIFT_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['day', 'cap_end', 'end_s', 'drained', 'capped', 'standing', 'standing_put', 'standing_dock', 'standing_carry', 'standing_carry_labour', 'standing_carry_supply', 'last_finish', 'overtime_s', 'n_bat..."
    },
    "Optimization/Performance_Evaluations/common/frames.py::_SOURCE_FRAME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'batch': ('batch', None), 'task_mean': ('task', 'mean'), 'task_sum': ('task', 'sum'), 'work': ('work', None)}"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_WORK_ROLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'put': 'put_seconds', 'receive': 'unload_seconds'}"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_aligned": {
      "decorators": [],
      "doc": "Intersect batch indices common to all strategies \u2192 paired matrix (n \u00d7 k).",
      "is_async": false,
      "kind": "function",
      "signature": "(series_by_key, keys)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_bdf": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(stats)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_cdf": {
      "decorators": [],
      "doc": "Per-BATCH demand service, folded from carryover rows over the two supply reasons.\n\nThe fold is reason-SELECTIVE, and that is the whole design: three reason families share\n`carryover.qty` under one PK \u2014 the put-side ones are LEVELS re-emitted every batch and\nthe pick-side ones are FLOWS \u2014 so a total over the table is not defined.  Filtering to\ntwo FLOWS first makes the sum legal; `sim_semantics`' ByDiscriminator on `reason` is\nthe declaration that says so.\n\nThe denominator is the STATED whole `items_demanded`, not what was picked: a\ndenominator that shrinks as service degrades would flatter exactly the arms this\nquantity exists to expose.\n\nTHIS IS A PER-EFFECTIVE-BATCH SHARE, NOT THE EQUILIBRIUM CHECK'S.  Under the era\n`items_demanded` is the sampled demand PLUS the previous batch's carry, and a unit the\nshelf fails twice appears in two batches' rows -- so the share here counts a re-offered\nunit each time it is re-offered, on each batch that was asked for it.  The check's\n`supply` clause reads the same two reasons over FRESH demand with each unit counted once\n(`equilibrium.demand_flows`); the two agree only with rollover off, where nothing is\nre-offered.  This frame keeps its definition because it serves every vintage, rollover\non or off, and the run alone cannot say which it was.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, df_b)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_crdf": {
      "decorators": [],
      "doc": "The RAW carryover rows, one per (batch, reason, sku) -- the equilibrium check's flows.\n\nDeliberately unfolded: `equilibrium.demand_flows` needs the per-SKU rows to count a\nre-attempted supply failure once and to recover fresh demand from the previous batch's\nwhole carry, and it selects reasons itself (`sim_semantics`' ByDiscriminator on `reason`\nis why no total is taken here).  Empty with its columns on a vintage without the table,\nwhich the check reports as unrecorded rather than as everything served.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_ddf": {
      "decorators": [],
      "doc": "Per-DRAIN frame: the contention pair, the binding-cut pair, and the indicator.\n\nEvery column here is a LEVEL.  `binding_cut` is the one derived value and it is a\nBOOLEAN per drain, never a magnitude: the additive statistic over the pair is the count\nof drains where inbound work was left, and summing the levels themselves would re-count\nthe same standing trailer once per batch it waits (the `recv_cut` scar, 101x on a\npublished headline).",
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_elapsed": {
      "decorators": [],
      "doc": "Add `elapsed` and `thr_elapsed`: throughput against the DAY, not the makespan.\n\n`duration` is the batch MAKESPAN -- first picker starting to last finishing.  Under the\ncontinuous default that is also the whole elapsed time, because the next batch is\nreleased the instant this one ends, so \"how fast did the crew work\" and \"how much did\nthe day produce\" have one answer and nothing had to tell them apart.\n\nA PACED schedule separates them.  `ReleaseSchedule` releases batch i at its slot, and a\ncrew that finishes early waits.  That gap is real elapsed time in which nothing was\npicked, and `duration` does not contain it -- so a paced run's `thr_batch` is the rate\nthe crew worked AT while being read as the rate the day DELIVERED.  `thr_elapsed` is the\nsecond number, named so the two cannot be confused.\n\nBoth are legitimate and a scheduling change moves them in OPPOSITE directions: fewer,\nfuller waves raise what the day produces while leaving the working rate alone.  So this\nis an addition, never a replacement.\n\nByte-identical under the default: the runner sets `arm_clock = batch_start_time +\nduration` and then `release_at`, which returns the clock unchanged with no cadence\nconfigured -- so consecutive epochs differ by exactly `duration` and `thr_elapsed ==\nthr_batch` to the last bit.\n\nThe LAST batch has no successor to measure against and falls back to its own makespan:\nthere is no gap after the final wave, because nothing was waiting for it.",
      "is_async": false,
      "kind": "function",
      "signature": "(df)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_fidf": {
      "decorators": [],
      "doc": "The RAW `free_index` rows, one LEVEL per (batch, BinKey) -- the rework clause's\nper-bucket depth (department-calibration 32, decision 3).\n\nTHE AUDIT'S ONLY PATH TO THE TABLE, and an explicit column dict like `_bdf`'s for the\nsame reason: `throughput/audit.py` builds `free_rows` from this frame, so a column\nmissing here would make `equilibrium._rework_clause` read every bucket as unrecorded\nand report a clean depth on exactly the run it exists to flag (the vacuity ticket 24\nfell into with the flows).  Deliberately unfolded -- the clause takes per-bucket minima,\nmeans and drawdowns over a window, none of which survive a fold here.  Empty with its\ncolumns on a vintage without the table, which the clause reports as \"depth unrecorded\nper bucket\", never as a warehouse with no free bins.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_level_or_nan": {
      "decorators": [],
      "doc": "A close-out LEVEL the vintage did not record (the pre-split carry halves) is NaN --\nunknown -- never 0, which would read as \"nothing stood\".",
      "is_async": false,
      "kind": "function",
      "signature": "(v)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_metric_series": {
      "decorators": [],
      "doc": "Per-batch steady-state Series (indexed by batch_id) for one strategy/metric.\n\n`frames` is {'batch': df_b, 'task': df_t, 'work': df_w} for ONE strategy.  A mapping\nrather than positional frames because the set grows: each new per-batch source used to\nmean a new positional argument threaded through six call sites, and the one that was\nmissed would have fallen through to the task frame rather than failing.",
      "is_async": false,
      "kind": "function",
      "signature": "(frames, source, col, ss_lo)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_roll": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df, col, win=50)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_sdf": {
      "decorators": [],
      "doc": "Per-DAY frame of the drain-or-cap ledger, joined to the batches and the labour.\n\nTHE THROUGHPUT AUDIT'S FRAME.  One row per working day the ledger closed: the verdict\n(`drained` / `capped` -- as `load_shift_days` serves it, with overtime folded in, so an\n`overtime_s` above 0 is always a capped row and `days_capped` follows), the close-out\nlevels, the START-gate overtime\n(`last_finish - cap_end`, floored at 0), and -- joined through `batch_stats.work_day`\n-- the day's batches, released-late seconds, demand and picks, and the three crews'\nworked seconds.  With `expectations` (`equilibrium.expectations_for`) each department's\nUTILIZATION is worked \u00f7 (crew \u00d7 S) for that day; without them the three columns are\nNaN, never zero -- an unknown crew is not a crew of nobody.\n\nAn EMPTY frame when `rows` is empty, by the same rule `_ydf` / `_wdf` follow: a run\nwithout the drain-or-cap shift never closed a day, and a frame of zeros would say every\nday drained.  Utilization here is per DAY for the figures; the equilibrium check\ncomputes it as a ratio of sums over the window itself and never from these rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, df_b, df_w, expectations=None)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_tdf": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(stats, aisle_unittype_map, aisle_handling_map)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_wdf": {
      "decorators": [],
      "doc": "Per-BATCH production labour: put + unload from `work_events`, pick from the tasks.\n\nTHE OBJECTIVE'S FRAME.  `total_production_seconds` is the quantity the whole inbound\neffort selects on \u2014 put-away hours and picking hours move in OPPOSITE directions under\na placement rule, so a comparison on either leg alone systematically favours arms that\nbuy one with the other.\n\nAn EMPTY frame when `rows` is empty, never a frame of zeros.  This is the same rule\n`_ydf` and `_ddf` follow and it is the load-bearing one here: a run predating\n`work_events` and a run whose crews did no work are indistinguishable by row count, and\nfilling the missing legs with 0.0 would publish `total = pick` as if the put leg had\nbeen measured and found to be nothing.  Empty makes the metric ABSENT instead, which is\nwhat the capability gate and every downstream `_aligned` are written to handle.\n\nZeros WITHIN a populated frame are honest and are filled deliberately: once the table\nhas rows, a batch with no put row genuinely put nothing away, and dropping it would\nmake the batch index of this frame disagree with the batch frame's for no reason a\nreader could recover.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, df_b, df_t)"
    },
    "Optimization/Performance_Evaluations/common/frames.py::_ydf": {
      "decorators": [],
      "doc": "Per-TRAILER frame: raw stamps in, spans and the fee proxy out.\n\n`run_end_s` closes the CENSORED rows.  A trailer still on site when the run stops has\n`emptied_s` NULL, and its detention is right-censored rather than absent: it has been\nheld at least until the run ended, so the run end is the honest lower bound and the\n`censored` column is what keeps that visible.  Dropping those rows instead would report\nan adversarial ordering's fee as CLIPPED rather than concentrated, which inverts the\nsignal that arm exists to produce.\n\n`threshold_days` is the run's own `INBOUND_FEE_THRESHOLD_DAYS`.  It enters HERE and\nnowhere else, which is what makes a finished run re-reportable under a different\nthreshold without re-simulating.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, run_end_s, threshold_days)"
    },
    "Optimization/Performance_Evaluations/common/io.py": {
      "decorators": [],
      "doc": "Figure-save + fresh-directory helpers (deduped from the two retired monoliths).\n\n`_save_close` stamps a process-global footer (set per context by the driver) onto every\nfigure, so all graphs carry the origin simulation folder + inventory provenance without\neach graph having to opt in.  It also VALIDATES every save against the current evaluation's\ndeclared `out_subdir` (set per render by the driver, same pattern as the footer): a figure\nlanding outside its owner's directory is logged as a warning \u2014 once per (eval, dir) \u2014 and\nthe e2e tests assert zero such warnings, so the declarations cannot rot into fiction.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/io.py::_CURRENT_EVAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/Performance_Evaluations/common/io.py::_FOOTER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/Performance_Evaluations/common/io.py::_fresh_dir": {
      "decorators": [],
      "doc": "Remove a stale output directory and recreate it empty, so a re-run can never\nleave mismatched plots (e.g. an old top5_* beside a new top3_by_initial_*) behind.",
      "is_async": false,
      "kind": "function",
      "signature": "(path)"
    },
    "Optimization/Performance_Evaluations/common/io.py::_save_close": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fig, path)"
    },
    "Optimization/Performance_Evaluations/common/io.py::out_dir": {
      "decorators": [],
      "doc": "The CURRENT evaluation's declared output dir under this context's root, created.\n\nTHE way a render obtains its output directory: the subdir comes from the registration's\n`out_subdir` declaration (via artifact_map.figure_subdir), never a retyped literal \u2014 so a\nfuture directory change is a one-line declaration edit and no render body moves.\n\nroot = ctx.run_dir (config scope) or ctx.out_dir (aggregate scope).  `pick` selects one\nmember when the declaration is a tuple (agg.cross_profile) and must name a declared\nmember; it is an error for a single-dir declaration.  '' (root-declaring evals, e.g.\nconfig.series) returns the root itself, uncreated-beyond-existing.  fresh=True routes\nthrough _fresh_dir \u2014 the wipe-own-leaf contract of the single-owner stats suites;\notherwise os.makedirs(exist_ok=True), which also ends the old split where some renders\nmade their dirs and others silently relied on the parent pre-pass.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, pick=None, fresh=False)"
    },
    "Optimization/Performance_Evaluations/common/io.py::set_current_eval": {
      "decorators": [],
      "doc": "Name the evaluation whose render is executing (None clears it).",
      "is_async": false,
      "kind": "function",
      "signature": "(key)"
    },
    "Optimization/Performance_Evaluations/common/io.py::set_footer": {
      "decorators": [],
      "doc": "Set the provenance footer stamped on subsequently saved figures (None clears it).",
      "is_async": false,
      "kind": "function",
      "signature": "(text)"
    },
    "Optimization/Performance_Evaluations/common/marks.py": {
      "decorators": [],
      "doc": "marks \u2014 draw a quantity in a view, from RAW values, so a stance cannot be mislabelled.\n\nTwo shapes cover 25 of the suite's 31 figures: the RANKED CATEGORY (one bar or dot per\narm, sorted) and the SERIAL (one line per arm over batches).  Before this module they had\nten and seven independent implementations respectively, and the implementations disagreed\nabout the things a reader actually reads \u2014 which way \"best\" points, whether the baseline\nis a competitor or a datum, what an undefined value sorts as, how wide the zero line is.\n\nBut the reason this module exists is narrower and sharper than de-duplication.\n\n## A caller cannot hand in a percentage\n\nEvery entry point takes values **in the quantity's own raw units** and a `view`.  There is\nno parameter through which a pre-computed percentage, difference or ratio can be passed.\nThe renderer derives:\n\n    absolute   raw, scaled into presentation units by the quantity's own `Unit`\n    percent    `chartkit.improvement_pct_series(raw, baseline_raw, ...)` \u2014 the one sign\n               convention, oriented by the quantity's own direction\n    delta      a signed difference against the baseline, in presentation units\n\nso the `<view>_` prefix on the filename, the axis label, and the arithmetic that produced\nthe numbers all come from one decision.  `absolute_compute_cost.png` shipped for a day\nplotting a delta and passed all five of the save-time checks, because every one of them\ncompared a declaration against another declaration and none of them ever saw a number.\n`delta_travel_vs_baseline.png` shipped for longer plotting a percent, for the same reason\nand with an additional push: its family's allowed-view list had no `percent` in it, so the\nauthor had nowhere honest to put the figure.\n\n## An interval is the interval OF THE COMPARISON\n\nA comparison view takes the PAIRING \u2014 each arm's per-instance values beside the baseline's,\nin raw units \u2014 and derives both the dot and its whiskers from it.  Two things that costs\nnothing to get wrong otherwise:\n\n  * the dot and the whiskers estimate the SAME quantity.  Four paired-comparison sites in\n    the retired suite reported a median with a bootstrap of that median and a fifth\n    reported a mean with a t-interval, a split `stats_core` and `tables/vs_baseline` both\n    write comments forbidding;\n  * the interval is not the arm's own SPREAD wearing a percentage.  That is a much wider\n    statistic, and it makes the sentence published pages carry beside these charts false \u2014\n    \"where an interval crosses zero the arm has not been shown to move travel at all\" is\n    only true of the bootstrap of the paired improvements.\n\nTwo of the retired implementations also converted the point and left the whiskers in sim\nunits, drawing an interval of the wrong width around the right dot.  Deriving both from\none input is what makes that unrepresentable.\n\n## What is NOT here\n\nCumulative curves, scatter, facet grids, heatmaps and rendered tables stay bespoke \u2014 their\nmarks are not interchangeable and forcing them through one signature would produce a\nfunction with a mode switch per caller.  They are declared in `BESPOKE` with a reason, so\n\"bespoke\" stays a decision with a ceiling rather than a habit.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/marks.py::BESPOKE_CEILING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "18"
    },
    "Optimization/Performance_Evaluations/common/marks.py::_hug_x": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ax, vals, *, include_zero)"
    },
    "Optimization/Performance_Evaluations/common/marks.py::_paired_series": {
      "decorators": [],
      "doc": "The per-instance stance values for one arm: improvements, or raw differences.\n\n`got` is (arm_values, baseline_values) in the quantity's own units, already aligned.\n`improvement_pct_series` drops the undefined pairs, which is the only honest sample to\ntake a median or a bootstrap over.",
      "is_async": false,
      "kind": "function",
      "signature": "(quantity, got, view)"
    },
    "Optimization/Performance_Evaluations/common/marks.py::_presented": {
      "decorators": [],
      "doc": "Raw values -> presentation values, with the quantity's own conversion.",
      "is_async": false,
      "kind": "function",
      "signature": "(quantity, raw, samples)"
    },
    "Optimization/Performance_Evaluations/common/marks.py::_view_values": {
      "decorators": [],
      "doc": "(values, axis_label) for one view \u2014 the ONLY place a stance is computed.\n\n`raw` and `baseline_raw` are in the quantity's own units.  There is deliberately no\npath by which a caller supplies the answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(quantity, raw, baseline_raw, view, samples)"
    },
    "Optimization/Performance_Evaluations/common/marks.py::ranked": {
      "decorators": [],
      "doc": "One bar (absolute) or dot+interval (percent/delta) per arm, best first.\n\n`entries`  [(strategy_dict, raw_value)] in the quantity's own units \u2014 the arm's scalar.\n`paired`   optional {strategy_key: (arm_values, baseline_values)}, both in the SAME raw\n           units, aligned instance for instance (batch i against batch i).\n`baseline` the baseline strategy dict; its raw value must be in `entries`.\n\n## Why the interval takes a PAIRED input rather than an interval\n\nA comparison view's interval has to be the interval OF THE COMPARISON. Bootstrapping\neach arm's own spread and then expressing that as a percentage of the baseline is a\ndifferent \u2014 and much wider \u2014 statistic, and it makes the sentence published pages carry\nbeside these charts false: \"where an interval crosses zero the arm has not been shown\nto move travel at all\" is only true of the bootstrap of the paired improvements.\n\nSo the caller hands over the pairing and the mark derives both the point and the\ninterval from it, in whatever stance `view` names. That keeps the rule this module\nexists for \u2014 there is no parameter through which a pre-computed percentage can arrive \u2014\nwhile producing the statistic the caption claims. Without `paired`, a comparison view\ndraws points with no interval, which is honest about what it does not know.",
      "is_async": false,
      "kind": "function",
      "signature": "(ch, entries, *, quantity, view, baseline, strategies, paired=None, annotate=None, ax=None)"
    },
    "Optimization/Performance_Evaluations/common/marks.py::serial": {
      "decorators": [],
      "doc": "One line per arm over batches, in one of the three views.\n\n`series`  {strategy_key: {x_key: [...], y_key: [...], ...}} in raw units.\n`band`    optional (lo_key, hi_key) drawn only in the absolute view, where a band in\n          raw units means something; against a baseline it would need its own paired\n          interval, which is a different computation and not this one.\n\nReturns the number of arms drawn.",
      "is_async": false,
      "kind": "function",
      "signature": "(ch, series, *, quantity, view, baseline, strategies, x_key, y_key, band=None, smooth=0, ax=None)"
    },
    "Optimization/Performance_Evaluations/common/marks.py::views_for": {
      "decorators": [],
      "doc": "The views one evaluation must emit \u2014 the union over the quantities it draws.\n\nThin, but it is the seam: an evaluation asks what it must draw rather than declaring\nit, which is what stops `layout.churn` from shipping absolute-only beside\n`layout.travel`'s two views because two different authors made two different calls.",
      "is_async": false,
      "kind": "function",
      "signature": "(eval_key: str, quantity_keys, shape) -> frozenset"
    },
    "Optimization/Performance_Evaluations/common/painters.py": {
      "decorators": [],
      "doc": "Shared figure painters \u2014 the builders more than one evaluation draws with.\n\nPost-redesign this module holds exactly three things:\n\n  * `overtime_metrics` / `top_tag` \u2014 the shared filename-stem vocabulary.  Every\n    over-time figure basename and every `top{n}[_by_{dim}]` suffix in the published\n    site composes from these two functions, so THIS module is where those names are\n    minted (and where the figure-registry test looks for the stems).  What each name\n    MEANS \u2014 its unit, axis label and direction \u2014 comes from `core/quantities.py`;\n    `overtime_metrics` is a projection of that table, not a second copy of it.\n  * `paint_overtime` \u2014 the one over-time painter, drawn through chartkit for both its\n    views: 'absolute' (honest units \u2014 a data-chosen time unit, items/hour, f\u00b7D) and\n    'percent' (improvement vs the baseline strategy, positive = better).  The config-stage\n    trajectories family and the aggregate stage both render with it, which is what\n    keeps their charts visually identical.\n  * unit helpers binding the metric vocabulary to chartkit's unit policy.\n\nEverything else the old painters carried (facet grids, top-N line picks, breakdown and\ndelta bars) either died in the redesign or moved into the single family module that owns\nit now.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/painters.py::_LEVEL_NOTE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'band = the middle half of all arms; only the extremes are drawn individually \u2014 the percent view separates the rest'"
    },
    "Optimization/Performance_Evaluations/common/painters.py::_SPAGHETTI": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "8"
    },
    "Optimization/Performance_Evaluations/common/painters.py::_conv": {
      "decorators": [],
      "doc": "Raw series values -> presentation units.  `div` is the shared time divisor a\ntime metric resolved for its axis; a fixed-unit metric uses its own `conv`.",
      "is_async": false,
      "kind": "function",
      "signature": "(m, vals, div=None)"
    },
    "Optimization/Performance_Evaluations/common/painters.py::_label": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(s)"
    },
    "Optimization/Performance_Evaluations/common/painters.py::_paint_levels": {
      "decorators": [],
      "doc": "The absolute view's body.  Returns how many series were drawn.\n\nUp to `_SPAGHETTI` arms, every arm is a line.  Beyond that they are not: 34 arms of\nthe same warehouse trace the same demand curve within a few percent, so the overlay\nbecomes one opaque ribbon in which no arm can be followed \u2014 an SME reading this very\nchart at 34 arms attributed a 6-12% gap to two arms whose sim databases are in fact\nbyte-identical.  So the large case draws the SHAPE honestly: the cross-arm median,\nthe interquartile band, and only the extreme arms named.  Which arm is which at this\nscale is the percent view's question, and it answers it against a baseline.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, strategies, S, m, baseline, div)"
    },
    "Optimization/Performance_Evaluations/common/painters.py::_time_axis": {
      "decorators": [],
      "doc": "(divisor, unit_label) for a time metric \u2014 ONE unit for its whole y-axis.\n\nEvery value that will share the axis is pooled into a single `chartkit.time_units`\ncall: each arm's series, the baseline's, and the IQR band edges.  Fixing the unit at\nhours instead would print a column of 0.0008 for a metric whose median is seconds.",
      "is_async": false,
      "kind": "function",
      "signature": "(m, S, strategies, baseline)"
    },
    "Optimization/Performance_Evaluations/common/painters.py::overtime_metrics": {
      "decorators": [],
      "doc": "The over-time metric specs, DERIVED from the quantity table.\n\nEvery over-time figure basename in the published site composes from `f` here, so this\nstays the place those names are minted (and where the figure-registry test looks for\nthe stems) \u2014 but the names themselves, and the units, labels and directions beside\nthem, now come from `core/quantities.py`.  They were a literal here, a literal in each\nof the two significance `_PRESENT` tables, and a literal in the headline module's\n`_METRIC_GROUPS`, and the four had drifted: the same quantity was `\u03a3 task time per\nbatch` on one axis and `total task time per batch` on another.\n\nEach spec carries its `Quantity` under `q`.  Prefer reading that over the flattened\nkeys, which exist so `paint_overtime` and its two callers did not have to change in\nthe same commit:\n\n  x/y/blo/bhi  series columns (`Source.series`)\n  f            figure-basename stem (`Quantity.stem`)\n  t            panel title (`Quantity.series_title`)\n  time         True for a duration, whose unit is resolved from the data\n  conv         raw -> presentation multiplier, or None for a metric needing none\n  yl           the axis label WITHOUT the resolved time unit \u2014 `paint_overtime`\n               appends it for a time metric, because the divisor is chosen from every\n               arm's values pooled, which only the painter has",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/Performance_Evaluations/common/painters.py::paint_overtime": {
      "decorators": [],
      "doc": "Render one over-time metric in one view and return the saved path (or None\nwhen nothing could be drawn).\n\nview='absolute': every strategy in presentation units, baseline in BASELINE_STYLE.\nview='percent' : per-batch % improvement vs the baseline strategy, aligned on the\n                 metric's own x \u2014 the baseline IS the zero line, so it is drawn as\n                 a reference line, not a series.\nagg=True labels the aggregate stage (values are already \u00d7-baseline normalized\nupstream; the absolute view is skipped there by the caller).",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, S, m, baseline, out_dir_path, *, view, agg=False)"
    },
    "Optimization/Performance_Evaluations/common/painters.py::top_tag": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(top_n, top_by)"
    },
    "Optimization/Performance_Evaluations/common/present.py": {
      "decorators": [],
      "doc": "present \u2014 raw sim values to presentation values, for every consumer, once.\n\n`core/quantities.py` declares what a number IS and `common/units.py` declares how a unit\nis named and scaled; both are stdlib-only on purpose.  This module is the thin numpy-aware\nlayer between them and the painters: it hands back the `(conv, axis_label)` pair the\ndistribution and over-time painters have always taken, so those call sites are unchanged\nwhile their four private lookup tables are gone.\n\n`conv` is `None` when a quantity needs no scaling.  That is not an optimisation \u2014 it is\nthe contract the painters already had, and returning an identity function instead would\nquietly change how `merged_effect_panel` decides whether values were converted.\n\nThe converter is a CLASS, not a closure or a lambda.  Two reasons: it is picklable, so a\nspec can cross into a spawned worker; and it prints as something a reader can identify in\na traceback rather than `<lambda>`.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/present.py::Scale": {
      "decorators": [],
      "doc": "Multiply raw values into presentation units.  Picklable, and named in a repr.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/common/present.py::Scale.__call__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, values)"
    },
    "Optimization/Performance_Evaluations/common/present.py::Scale.__eq__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, other)"
    },
    "Optimization/Performance_Evaluations/common/present.py::Scale.__hash__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/present.py::Scale.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mult: float, unit_noun: str='')"
    },
    "Optimization/Performance_Evaluations/common/present.py::Scale.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/present.py::for_metric": {
      "decorators": [],
      "doc": "(conv, axis_label) for one metric name against the samples that share its axis.\n\nPool EVERY series that will land on the axis into `samples`: a duration's unit is\nresolved from its own magnitude, and resolving it per-series is how two boxes on one\npanel end up in different units with one label between them.\n\n`name` may be either the per-batch key or the historical aggregate spelling \u2014\n`production_time` and `productivity_hours` are the same quantity and resolve to the\nsame declaration.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, samples=None) -> tuple"
    },
    "Optimization/Performance_Evaluations/common/present.py::pooled": {
      "decorators": [],
      "doc": "Flatten an iterable of per-key sample arrays into one list for `for_metric`.\n\nExists so the callers stop each writing their own `[np.ravel(v) for v in ... if len]`\nguard; an empty pool is legal and resolves a duration to seconds.",
      "is_async": false,
      "kind": "function",
      "signature": "(values_by_key) -> list"
    },
    "Optimization/Performance_Evaluations/common/series.py": {
      "decorators": [],
      "doc": "Per-strategy time-series construction, the series.json dump, top-N selection, and the\ncross-profile aggregation (verbatim from the retired Comparison_Plots).\n\n`_build_series` is wrapped by EvalContext.series(); `_aggregate_series` by\nAggregateContext.agg_series().  `_dump_series` and `_select_top` are used by the\nconfig.series graph and the top_metric graph respectively.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/series.py::_aggregate_series": {
      "decorators": [],
      "doc": "Average per-strategy curves across profiles, normalized per-profile to that\nprofile's baseline-strategy steady-state mean (scale-free).  Returns\n(agg_strategies, agg_S) ready for the same builders.",
      "is_async": false,
      "kind": "function",
      "signature": "(profile_series_list)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_aggregate_series._avg_norm": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(items, field, norm_field)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_build_series": {
      "decorators": [],
      "doc": "Per-strategy time series + steady-state scalars for the comparison suite.\n\nReturns {key: dict | None}.  Task metrics carry their own x ('task_batch')\nbecause some batches may have no surviving (non-outlier) tasks.\n\n`df_w` (production labour) and `df_y` (the yard's trailers) were the third and fourth\nframes to reach this builder, and they arrived together for a structural reason: a slot\nin `quantities.HEADLINE_ORDER` is validated at import to name a steady-state scalar,\nand this function is the only thing that writes one.  Until it could see a frame other\nthan batch and task, the total-production-hours objective and the yard fee were both\nunheadlineable no matter how well either was measured.\n\nBoth default to None so a caller with only the two original frames still gets the\noriginal document \u2014 the scalars they feed come out absent rather than zero, which is\nthe same distinction `_wdf`'s empty frame draws and for the same reason.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, df_b, df_t, df_w=None, df_y=None)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_dump_series": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, S, path, extra=None)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_prodtime_delta": {
      "decorators": [],
      "doc": "Per-batch and cumulative % improvement of production time (\u03a3 task time / batch =\n'prod_hours') for `key` vs `base_key`, aligned by task_batch id.\n\nReturns (batches, per_batch_pct, cum_pct) as np arrays; positive = faster than baseline:\n  per_batch_pct = (base \u2212 strat)/base \u00b7 100   per shared batch\n  cum_pct       = \u03a3(base \u2212 strat) / \u03a3 base \u00b7 100  (running task-time saved \u00f7 running\n                  baseline total \u2014 the honest cumulative measure, not a mean of percents)\nEmpty arrays if either series is missing.  Uses the same smoothed prod_hours as the other\nover-time graphs; a raw per-batch variant could read the unsmoothed tsum instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(S, key, base_key)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_select_top": {
      "decorators": [],
      "doc": "Choose which strategies the top/ plot shows.\n\n'global'  \u2192 the top_n strategies overall (by steady-state throughput).\na dim     \u2192 the top_n WITHIN each value of that dim (initial/assignment/reslot),\n            so the comparison isn't dominated by one family (e.g. optimal-start).\nReturns (selected_strategies, group_of_key) where group_of_key maps key\u2192group\nlabel ('' for global).",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, S, top_n, top_by)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_select_top.rank": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(s)"
    },
    "Optimization/Performance_Evaluations/common/series.py::_ss_mean": {
      "decorators": [],
      "doc": "Mean of a per-batch column over the steady-state batches, or NaN.\n\n`ssb` is the batch frame already cut to the window, so the cut is stated once and the\nproduction scalars cannot drift onto a different window from `ss_dur` beside them.\nNaN whenever the frame is absent or empty: an unmeasured leg is not a leg measured at\nzero, and every consumer here treats NaN as \"this run cannot answer that\".",
      "is_async": false,
      "kind": "function",
      "signature": "(df, ssb, col)"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py": {
      "decorators": [],
      "doc": "Statistical machinery for the significance suite (verbatim from the retired\nStats_Analysis): metric specs, multiple-comparison correction, paired effect sizes,\ndescriptives, the omnibus+pairwise test runner, and the by-initial grouping helpers.\n\nThe four plot primitives that consume these results live in stats/plots.py.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_AGG_METRICS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_quantities.aggregate_specs()"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_BOOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "2000"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_METRICS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_quantities.metric_specs()"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_assignment_fn": {
      "decorators": [],
      "doc": "Strip the leading initial-family token ('uni_'/'opt_') \u2192 the assignment+reslot key\nthat identifies the function held constant (e.g. uni_rank_labor_norsl \u2192 rank_labor_norsl).",
      "is_async": false,
      "kind": "function",
      "signature": "(key: str) -> str"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_bh": {
      "decorators": [],
      "doc": "Benjamini-Hochberg FDR on a 1D array of p-values (NaNs passed through).",
      "is_async": false,
      "kind": "function",
      "signature": "(pvals: np.ndarray) -> np.ndarray"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_block_len": {
      "decorators": [],
      "doc": "Moving-block length for a series of n observations \u2014 the standard n^(1/3) rule.",
      "is_async": false,
      "kind": "function",
      "signature": "(n: int) -> int"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_boot_ci": {
      "decorators": [],
      "doc": "(lo, hi) MOVING-BLOCK bootstrap interval for `stat` over a per-batch series.\n\nBlocks, not independent draws.  These samples are per-batch differences from one\ncontinuous run: each batch inherits the previous batch's layout, so the differences\ncarry serial correlation \u2014 measured on this design, lag-1 through lag-3 sit outside\nthe +/-2/sqrt(n) white-noise band for some arms.  Resampling batches independently\nwould treat 75 correlated observations as 75 independent ones and report an interval\nnarrower than the data earns.  Resampling contiguous blocks keeps the local\ndependence intact; with no correlation present the block length collapses toward the\ni.i.d. case and costs nothing.\n\nDeterministic by construction: a seeded Generator, never the global RNG.  NaN pair\nwhen fewer than 3 finite observations survive \u2014 the same floor the paired tests use.",
      "is_async": false,
      "kind": "function",
      "signature": "(sample, stat=np.median, *, n_boot: int=_BOOT, seed: int=0, alpha: float=0.05, block: int | None=None) -> tuple"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_census_one": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(group: dict, values, sign: float, alpha: float) -> dict"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_clean": {
      "decorators": [],
      "doc": "Recursively replace NaN/Inf with None so the JSON is valid + portable.",
      "is_async": false,
      "kind": "function",
      "signature": "(obj)"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_descriptives": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(a: np.ndarray) -> dict"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_group_by_assignment": {
      "decorators": [],
      "doc": "{assignment_fn: {'uni': key, 'opt': key}} for keys that carry an initial token,\npreserving first-seen function order.",
      "is_async": false,
      "kind": "function",
      "signature": "(keys: list) -> dict"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_hedges_g_paired": {
      "decorators": [],
      "doc": "Hedges' g on the paired differences (small-sample corrected Cohen's d_z).",
      "is_async": false,
      "kind": "function",
      "signature": "(a: np.ndarray, b: np.ndarray) -> float"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_holm": {
      "decorators": [],
      "doc": "Holm-Bonferroni step-down on a 1D array of p-values (NaNs passed through).",
      "is_async": false,
      "kind": "function",
      "signature": "(pvals: np.ndarray) -> np.ndarray"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_initial_of": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(key: str)"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_lag1_autocorr": {
      "decorators": [],
      "doc": "Lag-1 autocorrelation of a series; NaN when it cannot be computed.\n\nReported beside an interval so a reader can see WHY the interval is block-based:\ncompare it against the white-noise band 2/sqrt(n).",
      "is_async": false,
      "kind": "function",
      "signature": "(sample) -> float"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_mean_ranks": {
      "decorators": [],
      "doc": "Average within-block rank per column; rank 1 = best (direction-aware).",
      "is_async": false,
      "kind": "function",
      "signature": "(M: np.ndarray, lower_better: bool) -> np.ndarray"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_mirror": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(template: np.ndarray, iu, vals: np.ndarray) -> np.ndarray"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_moving_block_index": {
      "decorators": [],
      "doc": "(n_boot, n) resampling index built from overlapping blocks of `block` batches.",
      "is_async": false,
      "kind": "function",
      "signature": "(rng, n: int, n_boot: int, block: int) -> np.ndarray"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_opt_better": {
      "decorators": [],
      "doc": "'opt' if the optimal-initial median is the better one for this metric, else 'uni'.",
      "is_async": false,
      "kind": "function",
      "signature": "(uni_med: float, opt_med: float, lower: bool) -> str"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_pluck": {
      "decorators": [],
      "doc": "Read `spec` off `row` \u2014 a mapping key, an attribute, or a callable applied to it.",
      "is_async": false,
      "kind": "function",
      "signature": "(row, spec)"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_rank_biserial": {
      "decorators": [],
      "doc": "Matched-pairs rank-biserial correlation in [-1, 1].\n>0 \u21d2 a tends to exceed b.  Derived from the signed-rank sums of (a-b).",
      "is_async": false,
      "kind": "function",
      "signature": "(a: np.ndarray, b: np.ndarray) -> float"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_rank_biserial_ci": {
      "decorators": [],
      "doc": "(lo, hi) for the matched-pairs rank-biserial, resampling the PAIRS.\n\nThe obvious closed form \u2014 the signed-rank null variance \u2014 depends only on n, so it\nhands every row of a panel an identical whisker and is widest exactly where the true\nuncertainty is smallest (an arm that wins every batch has r = 1 with almost no\nsampling error).  Resampling the pairs lets the data set the width.",
      "is_async": false,
      "kind": "function",
      "signature": "(a, b, *, n_boot: int=_BOOT, seed: int=0, alpha: float=0.05) -> tuple"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_run_tests": {
      "decorators": [],
      "doc": "Omnibus + all-pairwise tests on a paired matrix M (rows = blocks, cols = keys).",
      "is_async": false,
      "kind": "function",
      "signature": "(M: np.ndarray, keys: list, lower_better: bool) -> dict"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::_sign_test": {
      "decorators": [],
      "doc": "(p, p_floor, ci_lo, ci_hi) for \"more comparisons land in the claimed direction\".\n\nExact binomial against p=0.5, one-sided (the claim is directional), with a\nClopper-Pearson interval on the win rate.  Ties are excluded from the denominator\nhere \u2014 the standard sign-test convention \u2014 and reported separately by the caller so\nthe exclusion is visible rather than assumed.\n\n`p_floor` is 0.5**n: the SMALLEST p this many comparisons can produce, reached only by\na clean sweep.  It is returned because without it a small group is silently\nunfalsifiable \u2014 at n=4 the floor is 0.0625, so a rule that went the claimed way in\nevery single comparison still prints \"not significant\" against a conventional\nthreshold, and a reader scanning the column would draw the opposite conclusion from\nthe one the data supports.  Publish the floor beside the p and the reader can see when\nthe test, rather than the effect, ran out of room.\n\n(nan, \u2026) when nothing is left to test: an all-ties group has no direction to have been\nright about, and reporting p=1 there would read as evidence of no effect.",
      "is_async": false,
      "kind": "function",
      "signature": "(n_pos: int, n_neg: int, alpha: float=0.05) -> tuple"
    },
    "Optimization/Performance_Evaluations/common/stats_core.py::census": {
      "decorators": [],
      "doc": "Count/span evidence for a directional claim, over any flat comparison row set.\n\nOne dict per group plus a trailing overall row (``group={}``), each carrying:\n\n    n n_pos n_neg n_zero n_nan     the population and how it splits\n    win_rate                       n_pos / (n_pos + n_neg), ties excluded\n    p_sign ci_lo ci_hi             exact one-sided sign test + Clopper-Pearson\n    p_floor                        the smallest p this n could ever reach (0.5**n) \u2014\n                                   read `p_sign` against it, not against 0.05\n    median q1 q3 iqr min max       the span, over the FINITE values\n\n`value` may be a mapping key, an attribute name, or a callable taking the row.  Each\nmember of `group_by` is a NAME (used as the ``group_<name>`` column) or a\n``(name, callable)`` pair when the group is derived rather than stored \u2014 a bare\ncallable is refused, because a group column with no name cannot be written to a CSV\nor looked up from a page.  `better` fixes the sign convention \u2014 with ``'lower'`` a\nnegative value counts as positive evidence \u2014 so `n_pos` always means \"in the claimed\ndirection\" no matter which way the underlying metric points.  That is the whole\nreason this takes a direction rather than leaving it to the caller: a census whose\nsign convention lives at the call site is exactly the hand-rolled count it replaces.\n\nNaNs are dropped from every statistic and counted in `n_nan`; zeros are counted in\n`n_zero`, kept in the span, and excluded from the sign test.  `n` is the finite\ncount, so ``n == n_pos + n_neg + n_zero``.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, *, value, group_by=(), better='higher', alpha: float=0.05) -> list"
    },
    "Optimization/Performance_Evaluations/common/style.py": {
      "decorators": [],
      "doc": "Styling + layout helpers shared across graphs (verbatim from the retired monoliths).\n\nHolds the rolling-window constants, color/linestyle maps, compact-label helpers, the\nKDE overlay, subplot-grid builders, and the focus filter applied once at context build.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/style.py::_LINESTYLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['-', '--', '-.', ':']"
    },
    "Optimization/Performance_Evaluations/common/style.py::_SMOOTH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "5"
    },
    "Optimization/Performance_Evaluations/common/style.py::_TOP_DIMS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('initial', 'assignment', 'reslot')"
    },
    "Optimization/Performance_Evaluations/common/style.py::_TRAVEL_COL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'#a9a9a9'"
    },
    "Optimization/Performance_Evaluations/common/style.py::_WIN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "50"
    },
    "Optimization/Performance_Evaluations/common/style.py::_assign_color_map": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(strategies)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_focus_filter": {
      "decorators": [],
      "doc": "Restrict strategies to one initial family by key prefix ('uni'/'opt'); fall back\nto all if the prefix matches nothing or focus='all'.  opt_* win mainly from their\ninitial-stock advantage, so 'uni' isolates the reorder-policy comparison.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, focus)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_grid": {
      "decorators": [],
      "doc": "A roughly-square subplot grid for n strategies; returns (fig, n flat axes).",
      "is_async": false,
      "kind": "function",
      "signature": "(n, panel_w=3.0, panel_h=2.3, max_cols=8)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_ir_style_map": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(strategies)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_kde_plot": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ax, data, color, bins)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_pct_delta": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(val, base)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_row": {
      "decorators": [],
      "doc": "A row of n subplots; always returns a flat list of axes.",
      "is_async": false,
      "kind": "function",
      "signature": "(n, figw=4.8, h=4.5)"
    },
    "Optimization/Performance_Evaluations/common/style.py::_short": {
      "decorators": [],
      "doc": "Compact axis label: drop the trailing reslot token (_norsl).",
      "is_async": false,
      "kind": "function",
      "signature": "(key: str) -> str"
    },
    "Optimization/Performance_Evaluations/common/style.py::_stitle": {
      "decorators": [],
      "doc": "Compact strategy label: initial|assignment|reslot (falls back to label/key).",
      "is_async": false,
      "kind": "function",
      "signature": "(s)"
    },
    "Optimization/Performance_Evaluations/common/style.py::legend_right": {
      "decorators": [],
      "doc": "Place a legend just OUTSIDE the axes on the right, never overlapping the data.\n\n_save_close saves with bbox_inches='tight', so the outside legend is captured in full.\n`anchor` is the axes-fraction bbox_to_anchor (use a lower y to stack a second legend).",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, handles=None, *, anchor=(1.02, 1.0), **kw)"
    },
    "Optimization/Performance_Evaluations/common/units.py": {
      "decorators": [],
      "doc": "units \u2014 the presentation-unit policy, in one place and with no third-party imports.\n\nEvery number this suite draws is in a SIM unit: durations are SECONDS, rates are items\nper second, and the objective is unitless.  Turning those into something a\nreader can hold is a two-part job \u2014 pick the scale, then name it \u2014 and the suite used to\ndo it in four places with four vocabularies (`chartkit.time_units`, two `_PRESENT` tables\nand `painters.overtime_metrics`).  The names disagreed: the same quantity was\n`\u03a3 task time per batch` on one axis and `total task time per batch` on another, and both\nthroughput metrics printed the identical `items / hour` while measuring different things.\n\nTwo rules, and they are the whole module:\n\n  1. A DURATION has no fixed unit.  Its readable unit depends on its own magnitude \u2014 one\n     picker task is seconds, one batch of labor is hours \u2014 so it is resolved at render\n     time from the pooled samples that will share the axis.  `resolve` is the only place\n     that choice is made.\n  2. An axis label is `stem` plus the resolved unit in parentheses, or just `stem` when\n     the quantity carries no unit noun.  One composition rule, so two charts of the same\n     quantity cannot disagree about what to call it.\n\n**Deliberately stdlib-only.**  `core/quantities.py` imports this, and the point of that\nmodule is that a test, `ingest.py` or a schema tool can read the quantity table without\ndragging matplotlib and the whole analysis package into the process.  `chartkit`\nre-exports `time_units` so the ~20 existing call sites keep working unchanged.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/common/units.py::DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('dimensionless', 'days')"
    },
    "Optimization/Performance_Evaluations/common/units.py::DURATION": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('duration_s')"
    },
    "Optimization/Performance_Evaluations/common/units.py::KINDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('duration_s', 'rate_per_s', 'count', 'share', 'score', 'dimensionless')"
    },
    "Optimization/Performance_Evaluations/common/units.py::NONE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('dimensionless')"
    },
    "Optimization/Performance_Evaluations/common/units.py::PER_HOUR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "SECONDS_PER_HOUR"
    },
    "Optimization/Performance_Evaluations/common/units.py::RATE_PER_HOUR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('rate_per_s', 'items / hour', PER_HOUR)"
    },
    "Optimization/Performance_Evaluations/common/units.py::TIME_UNITS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('hours', SECONDS_PER_HOUR), ('minutes', 60.0), ('seconds', 1.0))"
    },
    "Optimization/Performance_Evaluations/common/units.py::Unit": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "How one quantity's raw values become presentation values, and what to call them.\n\n`suffix` is the parenthesised unit noun ('units', 'items / hour', 'W') or '' when the\nstem already reads as a complete axis label.  For `duration_s` it is ignored \u2014 the\nunit comes from the data.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/common/units.py::Unit.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/common/units.py::_flat": {
      "decorators": [],
      "doc": "Every finite float in an arbitrarily nested iterable (numpy arrays included).\n\nWritten against the iterator protocol rather than `np.ravel` so this module keeps its\nno-third-party promise; numpy arrays and pandas Series both iterate.",
      "is_async": false,
      "kind": "function",
      "signature": "(values)"
    },
    "Optimization/Performance_Evaluations/common/units.py::axis_label": {
      "decorators": [],
      "doc": "The one composition rule: `stem` + the unit in parentheses, when there is one.",
      "is_async": false,
      "kind": "function",
      "signature": "(stem: str, unit: Unit, samples=None) -> str"
    },
    "Optimization/Performance_Evaluations/common/units.py::resolve": {
      "decorators": [],
      "doc": "(multiplier, unit_noun) for one unit against the samples that share its axis.\n\nMultiply raw values by the multiplier to get presentation values.  Pool EVERY series\nthat will share the axis into one call: resolving per-series is how two boxes on one\npanel end up in different units with one label between them.",
      "is_async": false,
      "kind": "function",
      "signature": "(unit: Unit, samples=None) -> tuple[float, str]"
    },
    "Optimization/Performance_Evaluations/common/units.py::time_units": {
      "decorators": [],
      "doc": "(divisor, unit_label) \u2014 the largest time unit keeping typical magnitudes >= 1.\n\nChosen from the pooled MEDIAN absolute magnitude so one outlier cannot drag the whole\naxis into a smaller unit.  Deterministic, so the same quantity picks the same unit on\nevery re-render; the unit always appears in the axis label, so no chart can be read in\nthe wrong one.  Falls back to seconds on empty/degenerate input.",
      "is_async": false,
      "kind": "function",
      "signature": "(values) -> tuple[float, str]"
    },
    "Optimization/Performance_Evaluations/config/__init__.py": {
      "decorators": [],
      "doc": "config.* evaluations \u2014 the per-config DATA PRODUCTS (the series document, summary CSVs).\n\nSplit from the graph packages: these two renders emit the documents downstream stages read\n(the aggregate suite, run_channel_rollup, the docs ingest), not figures.  The package name is\nthe key namespace, per Tests/architecture/test_naming_alignment.py.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/config/series.py": {
      "decorators": [],
      "doc": "config.series \u2014 write series.json (per-strategy trajectories + steady-state scalars +\noptimal floors).  The cross-profile aggregate stage consumes these files.\n\nOrdered FIRST in the preset so it lands before downstream readers, but every config graph\nthat needs the curves calls ctx.series() directly (so there is no hard ordering).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/config/series.py::render": {
      "decorators": [
        "evaluation(key='config.series', label='series.json (trajectories + ss scalars)', scope='config', needs=('series',), out_subdir='')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/core/__init__.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py": {
      "decorators": [],
      "doc": "The output map \u2014 which directories each evaluation's outputs land in, derived, not retyped.\n\nTwo sources, cross-checked against each other and against the run-tree contract:\n\n  * every `@evaluation`'s `out_subdir=` declaration (core/registry.py) \u2014 the dormant field,\n    now VALIDATED: `io._save_close` warns when a figure lands outside its evaluation's\n    declared subdir, and `findings()` fails the consistency test when a declaration\n    contradicts the contract;\n  * the HEAD run-tree contract's `evaluation:` attributions (Optimization/runschema/schema.py)\n    \u2014 the CSV/JSON artifacts each evaluation owns, with their full path templates.\n\nDerived, memoised lazily (the registry is populated by Performance_Evaluations.import_all(),\nso nothing here may run at import time).  `config_dirs()` replaces the hand-typed literals in\n`driver.prepare_config_dirs` with a rule instead of a list:\n\n    a top-level subdir is PRE-WIPED by the parent iff two or more evaluations write under it\n    (no single owner may wipe a shared dir \u2014 the worker-race rule the driver docstring states);\n    a subdir with exactly one owner is that evaluation's own to wipe (`_fresh_dir` in its\n    render), which is what stats/ and stats_by_initial/ have always done.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::_rel_dir": {
      "decorators": [],
      "doc": "A template's directory relative to its stage root \u2014 the segments after the tree levels\n(`{cell}/{pair}/{config}/{channel?}` per-leaf, `{cell}/_aggregate/{config}/{channel?}`\naggregate, `_dossier/...` run).  Level placeholders are exactly the `{...}`-braced\nsegments; `{initial_group}` is NOT a level (it is a subdir the eval creates), so braced\nsegments only strip from the FRONT.\n\nA `RESERVED_PREFIX`-prefixed leading segment is a RESERVED STAGE ROOT, not an output\nsubdir: the contract uses that prefix for exactly these (`_aggregate`, `_dossier`,\n`_frozen`, `_site`) and the run tree DECLARES it, so matching the declared prefix is the\nrule rather than listing the names.  This read `parts[0] == '_aggregate'` when\n`_aggregate` was the only one, then a retyped `'_'` literal.",
      "is_async": false,
      "kind": "function",
      "signature": "(template: str) -> str"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::config_dirs": {
      "decorators": [],
      "doc": "(wipe_tops, nested_creates) for the parent pre-pass over one config leaf.\n\nwipe_tops: top-level out_subdir segments shared by >= 2 config-stage evaluations \u2014\npre-wiped once by the parent so no worker races.  nested_creates: every declared\nmulti-level out_subdir under a wiped top, created empty after the wipe (workers only ever\nwrite files into them).  Single-owner tops (stats/, stats_by_initial/) are absent: their\nowner wipes its own leaf, as the driver docstring has always promised.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::figure_subdir": {
      "decorators": [],
      "doc": "The subdir(s) (relative to the context root) evaluation `eval_key`'s FIGURES belong in\n\u2014 its `out_subdir` verbatim: a str, '' meaning the root itself, a TUPLE for a declared\nmulti-dir owner (agg.cross_profile), None for an unregistered key.",
      "is_async": false,
      "kind": "function",
      "signature": "(eval_key: str)"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::findings": {
      "decorators": [],
      "doc": "Inconsistencies between `out_subdir=` declarations and the contract's attributions.\nEmpty when honest; the consistency test asserts exactly that.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::owned_artifacts": {
      "decorators": [],
      "doc": "{eval_key: ((artifact_name, path_template), ...)} from the HEAD run-tree contract's\nunhashed `evaluation:` attributions.  Artifacts with no attribution belong to no\nevaluation (sim writers, run-root docs) and do not appear.\n\nAn attribution is a STRING for an artifact one evaluation writes and a LIST for a glob\nseveral write into \u2014 a family's figure folder is filled by every evaluation in that\nfamily, and naming one of them made the entry read as if that one owned the folder.\nBoth shapes land here as one entry per (evaluation, artifact) pair.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::run_dirs": {
      "decorators": [],
      "doc": "Every declared out_subdir of a RUN-scope evaluation, deepest-safe creation order.\n\nThe config stage needs the shared-top/single-owner distinction because its evaluations\nrun in a worker pool and two owners must not race to wipe one directory.  Run scope has\nno such hazard \u2014 `analyze_run` executes these serially in the parent, after every cell\nis finished \u2014 so the whole `_dossier/` root is wiped once and every declared subdir is\nrecreated, with no ownership rule to get wrong.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::save_in_bounds": {
      "decorators": [],
      "doc": "Is a figure save into `save_dir` consistent with `eval_key`'s declared out_subdir?\n\nTrue when ANY declared subdir's segments appear as a contiguous run in the save path \u2014\nchecked WITHOUT knowing the context root, because `_save_close` only sees the path.  An\neval declaring the root ('') makes no checkable claim; unknown evals are not this\nfunction's problem (the driver only sets registered keys).",
      "is_async": false,
      "kind": "function",
      "signature": "(eval_key: str, save_dir: str) -> bool"
    },
    "Optimization/Performance_Evaluations/core/artifact_map.py::site_dirs": {
      "decorators": [],
      "doc": "Every out_subdir a SITE-scope evaluation writes into, under `<pair>/_site/`.\n\nFlat, like `run_dirs()` and for the same reason rather than by copying it: the site\nstage's jobs are one per PAIR, so no two workers share an output root and there is no\nwipe race for the shared-top ownership rule to arbitrate.  `prepare_site_dir` wipes the\nroot once and creates these under it.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple"
    },
    "Optimization/Performance_Evaluations/core/baseline.py": {
      "decorators": [],
      "doc": "baseline \u2014 WHICH arm every comparison is quoted against, declared instead of counted.\n\n`EvalContext.base` was `self.strategies[0]`.  That is correct only because the strategy\nlist happens to be built baseline-first, which is a property of how the run harness\nassembles `sim_result['strategies']` \u2014 a fact no chart module can see and none of them\nstates.  Two things in this package already knew better and said so separately:\n`cost/rollup.BASE_RULE = 'fifo'`, and about ninety prose strings naming FIFO in titles and\naxis labels.\n\nSo the selection is declared here, once, and resolved by matching rather than by index.\nThe positional answer stays as an explicit FALLBACK with a log line, because two cases\nmake it the only available answer:\n\n  * `_focus_filter` runs BEFORE the baseline is taken.  Under `focus='opt'` the arm list\n    is restricted to `opt_*` and the FIFO arm may not be in it at all.\n  * an ad-hoc or synthetic strategy list (tests, notebooks) carries no `assignment` key.\n\nBoth are legitimate, and both must be visible when they happen \u2014 a comparison quietly\nre-baselined against a different arm changes every number on the page without changing\na filename.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/baseline.py::BASELINE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Baseline(field='assignment', value='fifo', label='FIFO baseline')"
    },
    "Optimization/Performance_Evaluations/core/baseline.py::Baseline": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "The do-nothing arm: how to find it, and what to call it on a chart.\n\n`field`/`value` select it out of the strategy list.  `label` is the legend and\nreference-line text `chartkit.mark_baseline` and `baseline_handle` default to.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/baseline.py::Baseline.select": {
      "decorators": [],
      "doc": "The declared arm, or None when this strategy list does not contain it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, strategies) -> dict | None"
    },
    "Optimization/Performance_Evaluations/core/baseline.py::resolve": {
      "decorators": [],
      "doc": "The baseline arm for one strategy list, falling back positionally out loud.\n\nReturns None only for an empty list.  Every other path returns an arm and, when that\narm is not the declared one, says so at WARNING with the reason a reader would need\nto judge whether it matters.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, log=None, *, where: str='') -> dict | None"
    },
    "Optimization/Performance_Evaluations/core/context.py": {
      "decorators": [],
      "doc": "EvalContext / AggregateContext \u2014 the per-config (and per-aggregate-group) data hub.\n\nBuilt once by the driver and passed to every graph's render(ctx, params).  Lazily loads\nand caches exactly what each graph touches (batch/task frames per strategy, the series\ndict, the travel/handling breakdown, steady-state cutoffs), so the duplicated per-graph\nreloading of the old monolith is gone.  A graph never reloads a DB the context already\nholds; it just calls ctx.batch_df(key) / ctx.series() / ctx.breakdown().",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/context.py::AggregateContext": {
      "decorators": [],
      "doc": "Cross-profile context for one pick-config group (consumes the series.json list).",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/context.py::AggregateContext.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, profile_series_list: list, out_dir: str, pickcfg: str, focus: str, log: logging.Logger) -> None"
    },
    "Optimization/Performance_Evaluations/core/context.py::AggregateContext.agg_series": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/core/context.py::AggregateContext.footer": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::AggregateContext.from_job": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, job: dict, focus: str) -> 'AggregateContext'"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext": {
      "decorators": [],
      "doc": "Per-config context.  Build via EvalContext.from_job (driver) or the __init__.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, sim_result: dict, slim: dict, focus: str, log: logging.Logger) -> None"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.batch_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.batch_frames": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.breakdown": {
      "decorators": [],
      "doc": "{key: (travel, handling)} from picker_events over a steady-state sample.\nMemoized; returns {} (logged) on failure so dependent graphs degrade gracefully.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.capabilities": {
      "decorators": [],
      "doc": "The optional sources every arm here can answer from \u2014 memoised, probed once.\n\nOne probe per DISTINCT arm, each a `SELECT 1 ... LIMIT 1` per capability, paid once\nper context because `run_analysis` memoises contexts per (config, focus). Lazy on\npurpose: a preset with no capability-gated evaluation never opens a connection for\nthis at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> frozenset"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.carry_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.dock_ceiling": {
      "decorators": [],
      "doc": "`cap x doors / crew` \u2014 the share of the derived receiving crew the dock could\nhave employed AT ONCE, or None when the run declared no door-team cap.\n\nThe read-out the door-team cap owes (\"Decide the contention regime under the derived\ncrew\", 4): receiver utilization stays a REPORTED number and the crew stays as the\nstaffing record derives it, so when a capped dock cannot seat the whole crew, the\nreceiving row's realized share has a ceiling that is PHYSICS rather than a staffing\nerror. Below 1.0 the dock is the narrower resource; at or above it the cap never\nbinds and the number is inert.\n\nNone means uncapped, and that is honest for every run before the cap existed as well\nas for one that chose it \u2014 unlike `fee_threshold_days`, whose default is a real\nnumber and so has to say when it is falling back. Reads the values stamped onto\n`sim_result` (CONFIG is not a channel to a spawned analysis worker) and the crew off\nthe staffing record, so it answers None on a flag-off run, which derives no crew.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float | None"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.drain_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.fee_threshold_days": {
      "decorators": [],
      "doc": "The free yard days this run's overage accrues past \u2014 RECORDED, else HEAD's.\n\nThe whole reason the DB holds raw stamps: the fee is derived here, so a finished\nrun can be re-reported under a different threshold without re-simulating. Which\nalso means the threshold has to be the RUN's, not this checkout's, or the number\nis quietly about the wrong configuration.\n\nThe run spec now records it and both restore sites replay it, so `sim_result` carries\nthe run's own value: `_apply_run_shape` puts it back into CONFIG in the parent, and\n`_sim_result_from_meta` stamps it onto the pickled job \u2014 CONFIG cannot be the channel,\nbecause an analysis worker is SPAWNED and re-imports sim_config with pristine defaults.\n\nThe fallback to `Optimization.config.settings` remains, for runs that predate the\nrecording. It is correct for those and WRONG the moment someone sweeps the threshold,\nso it says so in the log rather than resolving silently; the campaign must never\nexercise it, which is why the run-shape layer was a prerequisite of phase 1.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.footer": {
      "decorators": [],
      "doc": "Provenance line stamped on every figure: origin sim folder + inventory.\n\nrun_dir is `<run>/<cell>/<pair>/<config>` on a store-only run and\n`<run>/<cell>/<pair>/<config>/<channel>` on a mixed one, so a FIXED number of dirname hops\nmislabels one of the two \u2014 the old three-hop version rendered the config name in the \"run\"\nslot for every channelized run.  Walk up to the run root (the dir holding run_layout.json)\ninstead, so the label is right at either depth.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.free_index_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.from_job": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, job: dict, focus: str) -> 'EvalContext'"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.full_title": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, s) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.maxb": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.metric_frames": {
      "decorators": [],
      "doc": "{key: {kind: frame}} for every arm \u2014 what the `_METRICS` consumers iterate.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.metric_frames_for": {
      "decorators": [],
      "doc": "{kind: frame} for one arm, as `frames._metric_series` expects it.\n\nThe one place a metric-source frame set is assembled: every consumer that iterates\n`stats_core._METRICS` goes through here, so none of them can serve three kinds and\nstarve the fourth.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.missed_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.series": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.shift_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.ss_lo": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.staffing_expectations": {
      "decorators": [],
      "doc": "This leaf's expected utilization per department, off the staffing record stamped\nonto `sim_result` -- or None, logged once, when the record carries no derived block\n(a flag-off run: nothing was derived, so nothing is expected).\n\n`equilibrium.expectations_for` is the ONE reader of the record for both the\nreference-run driver and the audit, so the two cannot disagree about which number\nis \"expected\".  Keyed by PAIR (`self.inv`) and this leaf's channel, both of which\n`run_analysis._sim_result_from_meta` stamps beside the record.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.task_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.task_frames": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.title": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.work_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.work_frames": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::EvalContext.yard_df": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='Performance_Evaluations analysis context', tables={'batch_stats': ('run_id', 'batch_id', 'duration', 'num_tasks', 'total_items', 'avg_concurrent_pickers', '..."
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext": {
      "decorators": [],
      "doc": "Whole-run context: the run ROOT and everything under it, across cells.\n\nThe third scope, and the one the other two structurally cannot be.  `EvalContext` sees\none channel-run leaf; `AggregateContext` sees the profiles of one cell x config x\nchannel.  Neither can see two cells, so every cross-cell question \u2014 the scheduler\ncomparison, the compute cost of a rule across the whole sweep, what the run held fixed\n\u2014 had to live outside the registry, in hand-written writers whose figures the family\ngrammar never checked and whose PNGs the site guard needs a hardcoded exemption for.\nThis is the scope those belong in.\n\nReads are memoised and lazy, in the same spirit as `EvalContext`: nothing is opened\nuntil an evaluation's `needs=` resolves it through the broker.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, run_root: str, out_dir: str, log: logging.Logger) -> None"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.catalogue_dbs": {
      "decorators": [],
      "doc": "[(pair, planned_inventory_db_path)] for the catalogues this run actually stocked.\n\n`planned_inventory` is a contract ALIAS: a multi-cell run freezes one catalogue per\npair under the run root, a single-cell run writes one per cell.  Both `cell` and\n`pair` are supplied so the alias can resolve either shape \u2014 which is the whole\nreason to go through the resolver instead of joining the frozen pair directory.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.footer": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.from_run_root": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, run_root: str, out_dir: str) -> 'RunContext'"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.leaf_rows": {
      "decorators": [],
      "doc": "Every channel-run leaf's copy of a per-leaf CSV, each row tagged with its leaf.\n\nThe tag is what makes a run-scope reduction possible at all: the per-leaf tables\ncarry no cell/pair/config/channel columns, because at leaf scope those are the\ndirectory.  Positional resolution through the resolver, never a name match \u2014 the\nstore CONFIG and the store CHANNEL are both called `store`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str) -> list"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.reader": {
      "decorators": [],
      "doc": "A resolver for an analysis-produced artifact \u2014 HEAD's contract, then the run's.\n\nRun-scope evaluations read what the SAME analysis pass just wrote, so they must\nlook where today's suite puts things.  `runschema.reader_for` carries the whole\nargument, including why an absence-only fallback silently loses a moved file.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str)"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.rt": {
      "decorators": [
        "property"
      ],
      "doc": "The run-tree resolver for this run \u2014 the ONLY way to reach a path under it.\n\nBound to the contract the RUN recorded, which is right for everything the\nsimulation wrote.  Artifacts the ANALYSIS writes go through `reader()` instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.run_spec": {
      "decorators": [],
      "doc": "The run's own recorded invocation spec, or {} when it has none.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.run_workers": {
      "decorators": [],
      "doc": "How many worker processes the sweep ran, or None if unrecorded.\n\nLoad-bearing provenance for anything quoting a wall time: these seconds were\nmeasured under that much contention, so they are an upper bound rather than a\nclean benchmark, and a ratio against an uncontended measurement is not a ratio.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.runtime_rows": {
      "decorators": [],
      "doc": "Per-arm compute-cost rows, or [] when the run predates the runtime DB.\n\nThese are REAL wall seconds, contended against whatever worker pool the sweep\nran \u2014 never the modeled warehouse seconds every other context serves.  Anything\nrendering them says which kind of second it means.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.whatif_doc": {
      "decorators": [],
      "doc": "A cross-cell what-if JSON summary, resolved through the contract ({} if absent).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str) -> dict"
    },
    "Optimization/Performance_Evaluations/core/context.py::RunContext.whatif_rows": {
      "decorators": [],
      "doc": "A cross-cell what-if CSV as a list of dicts, resolved through the contract.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str) -> list"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext": {
      "decorators": [],
      "doc": "One PAIR's site, under a coupled run: both channel leaves and the yard they share.\n\nTHE FOURTH EVALUATION SCOPE that reads simulation output, and it is a subclass rather\nthan a second implementation because that IS the design (\"Re-scope the analysis\nsurfaces to the site\", section 1): every frame broker keys on `s['db_path']` and\n`s['run_id']` and nothing else, so site-ness lives in what the keys POINT AT.  A site\n\"strategy\" is an ARM PAIR whose `db_path` is the contract's `site_inbound_db`, and\nwhich additionally carries its two leaves so the denominators can be the site's.\n\nInheriting is what keeps `staffing_expectations`, `fee_threshold_days`, `dock_ceiling`\nand `footer` single implementations.  All four read `sim_result`, and the values they\nread are genuinely site-wide already: the door count, the door-team cap, the fee\nthreshold and the derived receiving crew are one site's, stamped per leaf only because\na leaf is where `_sim_result_from_meta` runs.\n\nONE JOB PER `(cell, pair)`, NOT PER ARM PAIR, and that deviates from 07 section 1 with\na reason: the yard family's marks are `ranked` and `serial`, which compare arms, so\nevery arm pair has to be in ONE context or there is nothing to rank against; and the\ndeclared output tree is `figures_site_yard_pngs`, one directory per pair, which two\nconcurrent per-arm-pair jobs would race to wipe.\n\nTHE ERA GATE PROBES THE SITE DB, and this class has to say so rather than assume it is\nexempt.  An earlier draft of this docstring claimed there was no `capabilities()` here\nand that the gate was therefore a no-op \u2014 which is FALSE for a SUBCLASS: `era_shortfall`\nhasattr-checks the method, finds the inherited one, and calls it.  Three of the four\nyard evaluations declare quantities and every one of them raised `AttributeError` on the\nfirst real coupled run, rendering nothing while the `[access]` summary reported a grant\n(`a-grant-is-not-an-output`, exactly).  So the cache is initialised like every other and\nthe probe reads the SITE DB, which is the right file to ask: a site capability is what\nthe site's own record carries, not what one channel's does.",
      "kind": "class",
      "signature": "(EvalContext)"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, pairs: list, out_dir: str, sim_result: dict, log: logging.Logger) -> None"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.batch_df": {
      "decorators": [],
      "doc": "The SITE's batch record: every leaf's frame, concatenated.\n\nThis is the one override that matters.  `_arm_end_s` and both of\n`yard.scorecard`'s denominators read through it, so the censoring bound becomes the\nunion of the two leaves' clocks and the receiver's busy share is both leaves'\nseconds over the days they share.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.channels": {
      "decorators": [
        "property"
      ],
      "doc": "The channels this site serves, in declared order \u2014 the order the per-channel\nshares are printed in, taken off the first arm pair because every pair of one site\nhas the same two leaves.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.footer": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.from_job": {
      "decorators": [
        "classmethod"
      ],
      "doc": "`focus` is accepted and IGNORED, and that is stated rather than silent.\n\nA focus filter selects `uni_*` or `opt_*` ARMS; a site's members are arm PAIRS, and\nthe pairing is the diagonal by rank (site-dock 06) \u2014 dropping half a diagonal would\nleave pairs whose two halves came from different ranks, which is a different\nexperiment rather than a subset of this one.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, job: dict, focus: str) -> 'SiteContext'"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.leaf_batch_df": {
      "decorators": [],
      "doc": "ONE leaf's batch frame inside a site \u2014 the per-channel SHARE, never the site's.\n\nPrinted BESIDE the site number and never instead of it (memory\n`a-right-site-total-hides-two-wrong-shares`: put-away's site load was 0.9% exact\nwhile both per-channel bands failed in opposite directions).  Not memoised: it is\nread once per arm pair per render, and a second cache keyed by `(key, channel)`\nbeside the shared frame cache is a second thing to invalidate.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key, channel)"
    },
    "Optimization/Performance_Evaluations/core/context.py::SiteContext.title": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/context.py::_probe_capabilities": {
      "decorators": [],
      "doc": "Which optional sources EVERY arm of this context can actually answer from.\n\nThe runtime half of the era gate, and it opened the day a quantity first named a\ncapability. `core/era.py`'s static rule proves a declared read is either inside the\nguaranteed surface or named as a capability; that is a statement about the QUANTITY\nTABLE and says nothing about the file in front of you. A vetted vintage can carry a\nconditional table and no rows in it \u2014 measured across two archived cells,\n`aisle_metrics` has rows in 52 of 60 arms and `reorder_queue` in 68 of 166 \u2014 and\nwithout this probe the only symptom would be a figure that silently does not appear.\n\nINTERSECTED across arms, not unioned. A comparison figure draws every arm on one axis,\nso a capability one arm cannot answer is a capability the figure cannot use: the union\nwould license a chart that silently omits an arm, which reads as that arm scoring zero.\n\nAbsent files are skipped rather than counted as answering nothing \u2014 `requests` already\ndenies on those, with a better message, and folding the two would report a missing file\nas a data-era problem.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, log: logging.Logger) -> frozenset"
    },
    "Optimization/Performance_Evaluations/core/context.py::_provenance_parts": {
      "decorators": [],
      "doc": "Directory names from the RUN ROOT down to `leaf_dir`, for a figure's provenance footer.\n\nThe run root is the dir holding run_layout.json (the run-tree descriptor).  Anchoring on that\nmarker keeps the label correct whether the leaf is `<cell>/<pair>/<config>` (store-only) or\n`<cell>/<pair>/<config>/<channel>` (mixed) \u2014 a fixed dirname-hop count cannot be right for both.\nFalls back to the last three components when no descriptor is found (e.g. an ad-hoc dir under\ntest), so this never raises inside a plotting call.",
      "is_async": false,
      "kind": "function",
      "signature": "(leaf_dir: str, max_up: int=6) -> list[str]"
    },
    "Optimization/Performance_Evaluations/core/context.py::_verify_sim_dbs": {
      "decorators": [],
      "doc": "Refuse to build a context over an unvetted sim DB.  Raises `UnsupportedSchema`.\n\nOne check per DISTINCT file, ~10 PRAGMA round trips each.  Measured at 28 ms per sim DB on\nthe external results drive, so ~1 s for a 34-arm config \u2014 small beside the `batch_stats`\nload it guards, and paid once per context because `run_analysis` memoizes contexts per\n(config, focus) in `_CFG_CTX`.  Not worth a cache of its own.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, log: logging.Logger) -> None"
    },
    "Optimization/Performance_Evaluations/core/discovery.py": {
      "decorators": [],
      "doc": "Auto-discovery: import every submodule so @evaluation decorators fire.\n\nUsed instead of a hand-maintained import list so dropping a new graph file into a\ncategory subpackage makes it available with zero edits elsewhere.  Walk order is\nirrelevant \u2014 execution order comes from the preset's `keys` list, not import order.\n\nCritical for multiprocessing: the package __init__ calls import_all() at import time,\nso each spawned worker that does `import Performance_Evaluations` repopulates the\nregistry in its own process before any job runs.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/discovery.py::import_all": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/Performance_Evaluations/core/era.py": {
      "decorators": [],
      "doc": "era \u2014 can this run's data answer the quantities the suite declares?\n\nThe publish loop has one route it cannot automate: a stakeholder asks for a number the\nsimulation never wrote down.  That cannot be backfilled \u2014 schema identity is derived from\nshape and archived files are never rewritten \u2014 so the only honest answers are a new sweep\non a vintage that records it, or a recorded caveat and the degraded form.\n\nWhat made that dangerous is that nothing said which route you were on.  A quantity reading\na column only SOME vetted vintages carry produces a figure on the runs that have it, no\nfigure at all on the ones that do not, and an INFO line either way.\n\n## The rule\n\nStraight out of `Schema/compat.py`'s own docstring, applied to the quantity table rather\nthan reinvented beside it.  Every quantity must satisfy EITHER\n\n    compat.validate(requires) == []   inside `guaranteed_surface` -> version-free, done\n    q.capability names a real key     outside it -> a CAPABILITY, and it is SAID\n\nNeither is a failure.  Being NEITHER is: it means a figure will be silently absent, or\nsilently wrong, on every archived run made before the column existed.\n\n## Why this is a separate module\n\n`core/quantities.py` is stdlib-only on purpose \u2014 that is what lets a test, `ingest.py` or a\nschema tool read the quantity table without importing the analysis package.  The gate needs\n`Schema.compat` and the sim-DB family registration, so it lives here and imports there.\nThe layering also reads correctly: the quantity table describes MEASUREMENTS; whether a\ngiven archive can serve one is a schema question.\n\n## The runtime half, and why it is not here\n\n`EvalContext._verify_sim_dbs` already hard-fails on a sim DB whose schema is not vetted, so\nan archived run is either vetted \u2014 and every read below is inside the guaranteed surface,\nso it can answer \u2014 or the analysis refuses to start.  The gap a runtime probe would cover\n(a VETTED vintage missing a non-guaranteed column) opens the moment a quantity names a\ncapability, and not before.  `Tests/architecture/test_data_era_gate.py` fails on that day\nwith the design attached; a probe with no consumer is validated infrastructure nobody\ncalls, and this repo already has one of those.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/era.py::GATED_READS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "compat.Requires(family='sim_db', label='Performance_Evaluations quantity table (capability-gated)', tables=_table_columns(gated=True))"
    },
    "Optimization/Performance_Evaluations/core/era.py::QUANTITY_READS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "compat.Requires(family='sim_db', label='Performance_Evaluations quantity table', tables=_table_columns(gated=False))"
    },
    "Optimization/Performance_Evaluations/core/era.py::_table_columns": {
      "decorators": [],
      "doc": "{table: (columns,)} over the quantities that do (or do not) name a capability.",
      "is_async": false,
      "kind": "function",
      "signature": "(gated: bool) -> dict"
    },
    "Optimization/Performance_Evaluations/core/era.py::findings": {
      "decorators": [],
      "doc": "Quantities that read something not every vetted sim vintage carries, unnamed.\n\nTHE STATIC RULE \u2014 no database, runnable in CI.  Empty when honest.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Optimization/Performance_Evaluations/core/era.py::requires_for": {
      "decorators": [],
      "doc": "The `Requires` one quantity's sim-DB read amounts to, or None for no read.\n\nDERIVED, not declared: the table comes from the frame builder the source names and the\ncolumns from the source itself, so a quantity cannot describe a read it does not make.\nA quantity with no per-batch source reads no database \u2014 its numbers come from a series\ndocument the analysis wrote, or from the run's own cost rows.\n\n`all_db_reads`, not `db_reads`: a quantity whose denominator lives in another table\n(`missed_share`) makes two reads, and validating only the first would let the second\nslip past the gate \u2014 which is the whole failure this module is the guard against.",
      "is_async": false,
      "kind": "function",
      "signature": "(q)"
    },
    "Optimization/Performance_Evaluations/core/families.py": {
      "decorators": [],
      "doc": "The chart-family grammar \u2014 which VIEWS a figure carries, and where the answer comes from.\n\nA family is one folder under a leaf's figures/ tree (figures/<family>/) holding every\nfigure that answers one class of question.  A VIEW is the measurement stance of a single\nfigure, named by its filename prefix:\n\n    absolute_  raw quantities in honest units (hours, items, % of time)\n    percent_   improvement vs the baseline, positive = better (chartkit.improvement_pct)\n    delta_     the same comparison in the quantity's own units, not as a ratio\n    effect_    effect sizes + significance (the discriminating stance at n=75, where\n               p-stars alone are all ***)\n    table_     a tabular summary rendered as a figure\n\n## Views are DERIVED, not declared\n\nEach family used to carry an editorial `views=` allow-list and each evaluation an\neditorial `views=` claim, and the result was exactly the drift you would predict:\n`throughput` structurally could not have a delta while `labor` had to; `layout.churn`\nshipped absolute-only beside `layout.travel`'s two views; `task_time.breakdown`\nabsolute-only beside `task_time.duration`'s; `headline.all_arms` percent-only beside\n`headline.rollup`'s two.  `agg.traj` emitted `percent` while its family REQUIRED\nabsolute+percent, and nothing noticed \u2014 the `required` field claimed a golden test\nasserted it and no such test existed.  `RUN_SCOPE_FAMILIES` was declared and read by\nnothing at all.\n\nWorse than any single gap: `layout`'s allow-list had no `percent` in it, so the author of\n`layout.travel`'s comparison figure had nowhere honest to put a percent view and saved one\nunder `delta_travel_vs_baseline.png`.  An editorial allow-list does not merely permit\ndrift; it manufactures mislabelling.\n\nSo a family now declares only what a family IS \u2014 the question it answers and the scope its\nfigures live at.  What views a figure carries comes from\n`core.quantities.derive_views(quantity, shape)`: a property of the thing being measured and\nof the mark drawing it, neither of which any single module gets a vote on.\n\n## The transition\n\nEvaluations still carrying a hand-written `views=` are listed in `LEGACY_VIEWS` with the\nreason they have not moved, and the count is capped.  An entry there is a debt with a name,\nnot a permission.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/families.py::LEAF_FAMILIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple((f for f, d in FAMILIES.items() if d['scope'] == 'leaf'))"
    },
    "Optimization/Performance_Evaluations/core/families.py::LEGACY_VIEWS_CEILING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "12"
    },
    "Optimization/Performance_Evaluations/core/families.py::RUN_SCOPE_FAMILIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple((f for f, d in FAMILIES.items() if d['scope'] == 'run'))"
    },
    "Optimization/Performance_Evaluations/core/families.py::SITE_SCOPE_FAMILIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('yard',)"
    },
    "Optimization/Performance_Evaluations/core/families.py::VIEWS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('absolute', 'percent', 'delta', 'effect', 'table')"
    },
    "Optimization/Performance_Evaluations/core/families.py::check_view": {
      "decorators": [],
      "doc": "Raise unless `view` is one the evaluation actually carries.\n\nCalled from chartkit at save time, inside a render only.  There is no family\nallow-list any more: the evaluation's own view set is derived from its quantities and\nits mark, so a view outside it is not a permissions problem \u2014 it is a figure whose\nstance nothing asked for.",
      "is_async": false,
      "kind": "function",
      "signature": "(eval_key: str, view: str) -> None"
    },
    "Optimization/Performance_Evaluations/core/families.py::figures_subdir": {
      "decorators": [],
      "doc": "The out_subdir a family's figures land in.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str) -> str"
    },
    "Optimization/Performance_Evaluations/core/quantities.py": {
      "decorators": [],
      "doc": "quantities \u2014 WHAT this suite measures, declared once.\n\n`stats_core._METRICS` already proved the pattern works: it declares\n`(name, source, column, direction)` and, because three consumers read it, adding a metric\nadds a CSV block, a `vs_baseline` row group and an `effect_*.png` with no other edit.\n\nIt stops at presentation.  The three fields it lacks \u2014 **unit, label, stance** \u2014 were then\nre-typed in four competing tables:\n\n    significance/suite.py  `_PRESENT`            11 entries\n    aggregate/sig.py       `_PRESENT`             5 entries\n    common/painters.py     `overtime_metrics()`   5 entries\n    headline/top_vs_baseline.py `_METRIC_GROUPS`  5 entries  (a hand-join of\n                                                  `_METRICS` and `_AGG_METRICS`)\n\n`throughput`, `makespan` and `task_mean_duration` appear in ALL FOUR with independently\nmaintained direction flags, and the tables had already drifted: the same quantity was\nlabelled `\u03a3 task time per batch` in one and `total task time per batch` in another, while\n`throughput` and `throughput_task` \u2014 different measurements \u2014 shared the single axis label\n`items / hour`.  Nothing could have caught either; there was no place the two statements\nmet.\n\nThis module is that place.  A `Quantity` says what a number IS (unit, direction, stance),\nwhere to read it at each scope (`Source`), what to call it, and what it may NOT be shown\nas (`views_suppressed`, with a mandatory reason).  Everything downstream is derived.\n\n## Why stdlib-only\n\nNo numpy, no matplotlib, no lambdas.  Two reasons, both load-bearing:\n\n  * a test, `ingest.py`, or a schema tool can read this table without importing the\n    analysis package \u2014 which is what makes \"the emitted figure set equals the derived one\"\n    a checkable property rather than a convention;\n  * a converter that cannot be a lambda has to be a declared `Unit`, so a conversion\n    cannot be spelled inline in one consumer and forgotten in the next.  That is exactly\n    how the hours divisor came to be written out five times -- and, because each copy\n    restated it rather than importing it, how all five stayed wrong by 1000x.\n\n## The view derivation\n\n`derive_views(q, shape)` replaces the per-evaluation `views=` declaration.  Coverage stops\nbeing editorial: `layout.churn` cannot ship absolute-only beside `layout.travel`'s\ndelta+absolute because neither module gets a vote.  What CAN differ is the shape \u2014 a\ncomparison needs a baseline to compare against, and a paired delta needs per-instance\npairing \u2014 and those are properties of the mark, not opinions of the module.\n\nSuppression is the safety valve, and its reason is mandatory (validated in\n`__post_init__`, the idiom `Schema.capability.Capability` already uses for caveats).\nWithout it the derivation manufactures charts that are structurally valid and\nmeaningless \u2014 a percent-improvement of a quantity whose baseline is zero, a delta of a\nshare in points labelled as percent \u2014 and a meaningless chart under a caption asserting\nmeaning is worse than the drift it replaces.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/quantities.py::COMPOSITE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('composite', fixed=frozenset({'absolute'}))"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::CURVE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('curve', comparable=True)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::DIRECTIONS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('lower', 'higher')"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::EFFECT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('effect', fixed=frozenset({'effect'}))"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::FACET": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('facet', comparable=True, paired=True)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::FRAME_KINDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'batch': FrameKind('batch_stats', paired=True), 'task_mean': FrameKind('task_stats', paired=True), 'task_sum': FrameKind('task_stats', paired=True), 'work': FrameKind('work_events', paired=True), ..."
    },
    "Optimization/Performance_Evaluations/core/quantities.py::FRAME_TABLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{kind: spec.table for kind, spec in FRAME_KINDS.items()}"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::FrameKind": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One frame-source kind: where its rows come from, and whether it pairs by batch.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/quantities.py::INSPECTION": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('inspection', fixed=frozenset({'absolute'}))"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::PAIRED_KINDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple((kind for kind, spec in FRAME_KINDS.items() if spec.paired))"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Quantity": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One measurable thing, declared once for every consumer that shows it.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Quantity.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Quantity.axis_label": {
      "decorators": [],
      "doc": "The axis text: stem, unit, and \u2014 for a `score` \u2014 which way is good.\n\nA `score` is a model quantity with no physical unit (\u03a3 f\u00b7D, analytical labor W).\nWhen it also carries no unit noun, nothing on the axis tells a reader whether up\nor down is the win, so the direction is stated.  Every other kind names a unit\nthat already carries the answer ('items / hour', 'units', '% of time picking'),\nand appending a hint there is noise.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, samples=None) -> str"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Quantity.lower_is_better": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::RANKED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('ranked', comparable=True)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::SCATTER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('scatter', comparable=True)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::SERIAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('serial', comparable=True, paired=True)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::SHAPES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(RANKED, SERIAL, FACET, CURVE, SCATTER, COMPOSITE, INSPECTION, EFFECT, TABLE)"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::STANCES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('level', 'contrast')"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Shape": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "A mark's structural capabilities \u2014 what it CAN show, never what it should.\n\n`comparable`  the mark can place an arm beside a baseline arm at all.\n`paired`      the mark's instances (batches, bins, tasks) line up 1:1 between two\n              arms, so a per-instance difference is defined.\n`fixed`       for a mark whose view is a property of the mark itself rather than of\n              anything it draws \u2014 an effect panel IS the contrast, a rendered table\n              IS its cells.  A fixed shape needs no quantity to know its views.\n\nThese live on the shape rather than the family because that is what lets `throughput`\ncarry a delta (it has a per-batch series) without forcing one onto a ranked churn\nchart whose categories are arms, not paired instances.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Shape.self_describing": {
      "decorators": [
        "property"
      ],
      "doc": "True when the mark's views do not depend on what it draws.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Source": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Source.agg_name": {
      "decorators": [
        "property"
      ],
      "doc": "The name this quantity carries in cross-profile products, or None if it has\nno steady-state scalar at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str | None"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Source.all_db_reads": {
      "decorators": [
        "property"
      ],
      "doc": "Every (table, columns) pair this quantity amounts to \u2014 primary plus `db_also`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Source.db_reads": {
      "decorators": [
        "property"
      ],
      "doc": "(table, columns) this quantity's PRIMARY frame reads, or ().\n\nDeliberately still one pair: every consumer of this property unpacks it, and a\nsecond table is the exception rather than the shape.  `all_db_reads` is the\ncomplete answer and is what the era gate validates against.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Source.frame_kind": {
      "decorators": [
        "property"
      ],
      "doc": "The frame this quantity is read out of ('batch', 'trailer', ...), or ''.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::Source.readable": {
      "decorators": [
        "property"
      ],
      "doc": "True when SOMETHING can actually read this quantity.\n\nA declaration with no source at any scope is a declaration nothing can draw \u2014 and\nit would still satisfy the view derivation, which is why the check lives at\nconstruction rather than in a linter.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::TABLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Shape('table', fixed=frozenset({'table'}))"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_COUNT_UNITS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('count', 'units')"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_LABOR_W": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('score', 'W')"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_SCORE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('score')"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_SHARE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Unit('share')"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_agg": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_q.source.agg_name"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_check_order": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, keys: tuple, attr: str) -> None"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::_missing": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[q.key for q in QUANTITIES if q.source.series is not None and q.key not in SERIES_ORDER and (q.key not in SERIES_ELSEWHERE)]"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::aggregate_specs": {
      "decorators": [],
      "doc": "`stats_core._AGG_METRICS` \u2014 (name, steady_state_field, lower_is_better).\n\nNames are the aggregate spellings, which is why `production_time` goes out as\n`productivity_hours`: those strings are the `metric` column of a published CSV.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::derive_views": {
      "decorators": [],
      "doc": "The views a quantity drawn with this shape MUST carry.\n\nNot \"may\".  This is the whole point: view coverage stops being a per-evaluation\neditorial decision and becomes a function of what the quantity is and what the mark\ncan structurally show.  Two figures of the same quantity through the same shape get\nthe same view set, in every family, or the derivation is wrong and gets fixed once.",
      "is_async": false,
      "kind": "function",
      "signature": "(q: Quantity, shape: Shape) -> frozenset"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::metric_specs": {
      "decorators": [],
      "doc": "`stats_core._METRICS` \u2014 (name, source_kind, column, lower_is_better).\n\nFiltered to `PAIRED_KINDS`: the significance suite pairs batch i of one arm against\nbatch i of another, and a per-trailer or per-drain frame has no such pairing to offer.\nA kind that is in neither list raises rather than being dropped, because \"silently\nabsent from every published CSV\" is the exact failure mode this table was built to end.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::quantity_for": {
      "decorators": [],
      "doc": "The declaration for a metric name, under either its per-batch or aggregate name.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> Quantity"
    },
    "Optimization/Performance_Evaluations/core/quantities.py::suppression_reason": {
      "decorators": [],
      "doc": "Why `view` is withheld for `q`, or '' if it is not withheld.",
      "is_async": false,
      "kind": "function",
      "signature": "(q: Quantity, view: str) -> str"
    },
    "Optimization/Performance_Evaluations/core/registry.py": {
      "decorators": [],
      "doc": "The Evaluation descriptor + @evaluation decorator + module-level registry.\n\nMirrors the registry style of Optimization/config/strategies.py (a dataclass per item, a flat\nlist, and a by-key dict), but populated by a decorator so a graph module self-registers\non import.  A graph's `render(ctx, params)` does the actual plotting; the descriptor\ncarries only the metadata the driver needs to schedule it.\n\n**`views` is derived.**  A figure evaluation declares the QUANTITIES it draws and the MARK\nit draws them with, and its view set follows from `core.quantities.derive_views`.  It was\na free per-evaluation declaration, and the drift it produced is catalogued in\n`core/families.py`; the short version is that two figures of the same quantity through the\nsame mark carried different view sets because two authors made two different calls, and in\none case the family's allow-list left a percent view with nowhere honest to go, so it\nshipped under a `delta_` filename.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/registry.py::Evaluation": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/registry.py::SCOPES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('per_strategy', 'config', 'aggregate', 'run', 'site')"
    },
    "Optimization/Performance_Evaluations/core/registry.py::_derive_views": {
      "decorators": [],
      "doc": "The view set for one figure evaluation \u2014 quantities x mark, minus its suppressions.\n\nAn INSPECTION or EFFECT mark answers for itself: a scorecard has no baseline in it and\na distribution+effect panel IS the contrast, so neither needs a quantity declared to\nknow what it can show.",
      "is_async": false,
      "kind": "function",
      "signature": "(key, family, quantities, shape, suppressed, pending)"
    },
    "Optimization/Performance_Evaluations/core/registry.py::_reasons_are_real": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(key, entries, field)"
    },
    "Optimization/Performance_Evaluations/core/registry.py::evaluation": {
      "decorators": [],
      "doc": "Decorator: register the wrapped render fn as an Evaluation.\n\nA figure eval declares `family=` and its out_subdir is DERIVED (figures/<family>) \u2014\npassing both is an error, so a family's folder can never be retyped inconsistently.\nNon-figure evals declare out_subdir explicitly ('tables', or '' for the leaf root).\n\nA figure eval also declares `shape=` (the mark) and `quantities=` (what it draws), and\nits `views` are derived from those.  `views=` may still be passed, but ONLY by an\nevaluation listed in `families.LEGACY_VIEWS` with the reason it has not moved \u2014 a debt\nwith a name, not a permission.\n\n## `quantities_optional=`\n\nA quantity the figure draws WHEN THE RUN CAN ANSWER IT and omits when it cannot.  It\nfeeds the view derivation exactly like `quantities=` \u2014 it is drawn, so it must be\ndrawable \u2014 but `requests.era_shortfall` ignores it, so a capability the run lacks costs\nthe figure that quantity rather than the whole render.\n\nThis is a narrow permission, not a softer `quantities=`.  It is only honest when the\nrender genuinely degrades per quantity and SAYS SO \u2014 `headline.top_vs_baseline`, whose\ntwo funnel-appended groups (total production hours, yard overage) are dropped from the\npanel with a log line when no arm has a finite value, so the reader sees five groups\nrather than seven blank-panelled ones.  An evaluation whose figure would be a lie\nwithout the quantity declares it in `quantities=` and takes the refusal: that is what\nthe whole `yard` family does, and correctly.\n\nReturns the plain function unchanged so it stays directly unit-testable.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, key, label, scope, needs=(), defaults=None, out_subdir=None, family=None, quantities=(), quantities_optional=(), shape=None, views_suppressed=(), views_pending=(), views=(), by_initial=False)"
    },
    "Optimization/Performance_Evaluations/core/registry.py::evaluation._wrap": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fn)"
    },
    "Optimization/Performance_Evaluations/core/requests.py": {
      "decorators": [],
      "doc": "The request broker \u2014 consumers ask for a resource BY NAME; this module gets it or says why not.\n\nThe `needs=` vocabulary every `@evaluation` already declares ('batch', 'task', 'series',\n'breakdown') is the request vocabulary.  (An 'events' request once existed with no declarer \u2014\npicker events are streamed per-batch INSIDE the 'breakdown' compose, so a separate grant had\nnothing to grant.  Re-adding it is six lines if a consumer ever streams events directly.)  A graph's render never navigates the\nversioned inputs itself: the broker is the hard-coded intermediary that knows where each\nresource lives (sim DBs via the `Picking_Data` loaders, which serve every vetted vintage\nthrough `Schema.dataset`'s named queries; series/breakdown composed from those frames) and\nreturns the composed resource \u2014 or a `Denied(reason)` when the resource does not exist.\n\nThree rules, in tension and resolved deliberately:\n\n  * A MISSING resource never raises.  `resolve_needs` returns `Denied` sentinels; the driver\n    logs `[access] <eval> requested ... -> DENIED (<reason>); render skipped` and moves on.\n    Exceptions remain for real corruption \u2014 an unvetted schema, an unreadable file \u2014 which\n    must stop a publish, not be skipped politely (see core/context.py's identity gate).\n  * When everything is present, behavior is byte-identical to the pre-broker code: the compose\n    functions ARE the old `EvalContext` method bodies, memoised into the same per-context\n    caches, so a granted render costs nothing extra and produces the same frames.\n  * An EMPTY resource is granted, not denied: `breakdown()` returning `{}` (no steady-state\n    batches, a failed reconstruction) is a degraded product its consumers are written to\n    tolerate \u2014 `travel_handling` renders nothing, the stats suites skip those rows.  Denial is\n    reserved for absence of the SOURCE (a sim DB not on disk, no series.json for a group).\n\nThe per-process tally (`tally_snapshot`) is what makes \"did every consumer get what it asked\nfor\" answerable from the log alone: run_analysis collects each worker's counts and prints a\nrun-end summary.  Process-global is safe for the same reason `io.set_footer` is \u2014 workers are\nseparate spawned processes, and within one, jobs run sequentially.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/core/requests.py::Denied": {
      "decorators": [],
      "doc": "Falsy sentinel carrying the reason the broker actually hit.  Never an exception.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/requests.py::Denied.__bool__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/Performance_Evaluations/core/requests.py::Denied.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, reason: str) -> None"
    },
    "Optimization/Performance_Evaluations/core/requests.py::Denied.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/requests.py::EraUnmet": {
      "decorators": [],
      "doc": "A refusal about the DATA ERA, not about a missing resource.\n\nKept distinct from a plain `Denied` because the two call for different actions and a\nsingle bucket taught the reader the wrong one. A denial says a file was not there \u2014\nre-run the stage, fix the path. This says the file IS there and cannot answer the\nquestion, because the run predates the column or never wrote the rows. There is no\nfixing that from the analysis side: the only honest routes are a new sweep on a vintage\nthat records it, or the degraded form with the caveat said out loud.\n\nIt reports under its own `[era]` summary line for the same reason. `[access]` counts\nINPUTS, and an era shortfall arriving in that column would read as a pipeline fault on\na run that is simply older than the measurement.",
      "kind": "class",
      "signature": "(Denied)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::EraUnmet.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, reason: str, missing=()) -> None"
    },
    "Optimization/Performance_Evaluations/core/requests.py::EraUnmet.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/Performance_Evaluations/core/requests.py::REQUEST_SCOPE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'per_strategy': 'config', 'config': 'config', 'aggregate': 'aggregate', 'run': 'run', 'site': 'site'}"
    },
    "Optimization/Performance_Evaluations/core/requests.py::Request": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One named request: bespoke, hard-coded navigation from versioned sources to a resource.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/requests.py::_FrameSpec": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "How one frame kind is loaded and built.  `extra(ctx, key, rows) -> tuple`.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/Performance_Evaluations/core/requests.py::_absent_dbs": {
      "decorators": [],
      "doc": "Arm keys whose sim DB is not on disk \u2014 THE denial condition for every config request.\n(An unreadable or unvetted file that IS on disk still raises; that is corruption, not\nabsence, and must stop the publish.)",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx) -> list"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_agg_series": {
      "decorators": [
        "request('series', 'aggregate')"
      ],
      "doc": "The per-profile series.json list the aggregate stage was built from.\n\nThe grant validates `ctx.profile_series_list` \u2014 THAT is the resource.  Consumers may read\nit raw (the aggregate stats suite feeds it to the pairing machinery per profile) or\ncomposed via `ctx.agg_series()` (cross_profile's merged view); both are the same granted\nresource, and the composed form is a memoised transform of the raw one, not a second\nrequest.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_arm_end_s": {
      "decorators": [],
      "doc": "When this arm stopped, on its own absolute axis \u2014 the CENSORING bound.\n\n`max(batch_start_time + duration)` rather than the last row's, because DB order is not\nguaranteed sorted (the same reason `_elapsed` argsorts).  0.0 on an empty frame, which\nmakes every censored detention 0 \u2014 honest for a run with no batches at all, and the\nonly case where it can happen.\n\nTHROUGH `ctx.batch_df`, NOT `frame(ctx, 'batch', ...)`, and the indirection is the whole\nof what makes the yard family honest at site scope.  A site's batch record is BOTH\nleaves' frames concatenated, so this bound becomes the SITE end \u2014 the union across two\nclocks \u2014 which is what a censoring bound over one shared yard has to be.  The three\nsibling-frame `extra` hooks below deliberately do NOT go through `ctx`: each wants this\narm's own frame, and that difference was load-bearing before this table and still is.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key) -> float"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_batch": {
      "decorators": [
        "request('batch', 'config')"
      ],
      "doc": "Every strategy's batch frame, materialized (memoised \u2014 the render re-reads for free).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_breakdown": {
      "decorators": [
        "request('breakdown', 'config')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_deny_absent": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_deny_absent_site": {
      "decorators": [],
      "doc": "Every file a SITE request reads has to be on disk: the site DB AND both leaves'.\n\nTwo separate absences with one message shape.  A missing site DB is a coupled run that\nreceived nothing (or was never coupled at all); a missing leaf DB is the same condition\n`_deny_absent` reports one scope down \u2014 and the site's denominators come from the\nleaves, so half a site is not a smaller site, it is a wrong one.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_missed": {
      "decorators": [
        "request('missed', 'config')"
      ],
      "doc": "Per-arm demand-service frames.  Granted with ZEROS in them, unlike `yard`.\n\nAn arm with no missed rows genuinely served all its demand, and that is a result rather\nthan an absence \u2014 so a frame of zeros is a grant, and `_cdf` builds one row per BATCH\nrather than one per carryover row precisely so a perfectly-served batch is present at\nzero instead of missing.\n\nThat is also why this compose cannot be the era check. A pre-`carryover` vintage\nproduces the identical frame of zeros \u2014 `load_carryover` returns `[]` for \"no table\" and\nfor \"nothing missed\" alike \u2014 so row-emptiness cannot tell a perfect run from an old one,\nand reading zeros off the old one would publish a plausible wrong number. The\nCAPABILITY does tell them apart (it probes the table for rows), both quantities name it,\nand `era_shortfall` runs before this. The check below therefore only ever fires when no\narm recorded a batch at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_run_catalogue": {
      "decorators": [
        "request('catalogue', 'run')"
      ],
      "doc": "The run's own frozen catalogue \u2014 what the inventory model REALLY produced.\n\nNot the generator's parameters and not the config's averages: the per-SKU rows the\nsimulation actually stocked, which is the only population a published distribution\nmay be computed over.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_run_runtime": {
      "decorators": [
        "request('runtime', 'run')"
      ],
      "doc": "Per-arm wall-clock compute rows from the run root's runtime metrics DB.\n\nDenied when the DB is absent \u2014 a re-analysis of a run made before it existed is a\nnormal thing to do, and the cost figures should be skipped with a reason rather than\nrendered empty.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_run_whatif": {
      "decorators": [
        "request('whatif', 'run')"
      ],
      "doc": "The cross-cell what-if comparison rows \u2014 the only place a scheduler pair is joined.\n\nDenied on a single-cell run, where there is no second cell to compare against and the\nwhat-if writers correctly emit nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_series": {
      "decorators": [
        "request('series', 'config')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_shift": {
      "decorators": [
        "request('shift', 'config')"
      ],
      "doc": "Every arm's per-day ledger frame -- `{key: shift_df}`.\n\nERA-UNMET, not Denied, when no arm closed a single day: the files were read and hold\nno drain-or-cap ledger, which is every run before the calibrated era and every\nflag-off run since.  A throughput audit over that would report \"every day drained\"\nabout a site that never had a day.  The `days_capped` quantity names the `shift_days`\ncapability, so the probe refuses the audit before this compose runs; this check is\nthe same fact found the other way round, for a caller that declared no quantity.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_site_batch": {
      "decorators": [
        "request('batch', 'site')"
      ],
      "doc": "Every arm pair's SITE batch frame \u2014 both leaves', concatenated.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_site_yard": {
      "decorators": [
        "request('yard', 'site')"
      ],
      "doc": "Both yard frames for every arm pair, read from `<pair>/_site/inbound_*.db`.\n\nThe SAME composes as the config namespace \u2014 `yard_frame` and `drain_frame` key on\n`db_path` and `run_id` and nothing else, so site-ness lives in what those keys point\nAT rather than in a second implementation (07 section 1).  The denial and the\nera-unmet halves say the same thing they say one scope down, for the same reasons.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_strategy_travel_handling": {
      "decorators": [],
      "doc": "Per-strategy (travel, handling) picker-time totals over a sample of steady-state\nbatches, reconstructed from picker_events.  Returns {key: (travel, handling)}.\n(Moved from core/context.py \u2014 the broker owns resource navigation.)",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, ss_lo, max_b, n_sample=8)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_task": {
      "decorators": [
        "request('task', 'config')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_work": {
      "decorators": [
        "request('work', 'config')"
      ],
      "doc": "Every arm's per-batch production-labour frame \u2014 `{key: work_df}`.\n\nERA-UNMET, not Denied, when no arm has a single work-event row. The files were opened\nand read and simply have no production timeline in them, which no re-run of this stage\ncan change: the run predates `work_events`, or predates the 2026-08-24 refactor that\ngave put-away a duration at all. A `total_production_time` figure built on that would\nread exactly the pick leg while claiming to be the sum of three \u2014 which is the\nsubstitution this whole quantity exists to prevent.\n\nThe `_METRICS` consumers (`tables.stats`, `tables.vs_baseline`, `sig.suite`) do NOT\ndeclare this need, and that is deliberate rather than an oversight in their `needs=`\ntuples. They iterate every declared metric and already drop the ones whose series\ncannot be aligned, so an era shortfall costs them three rows; declaring the need would\ncost them the whole CSV, on every run in the archive, to protect rows that degrade\ncorrectly on their own. Figure evaluations, which cannot degrade a panel that way,\ndeclare it.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::_yard": {
      "decorators": [
        "request('yard', 'config')"
      ],
      "doc": "Both yard frames for every arm \u2014 `{key: (trailers_df, drains_df)}`.\n\nDENIED when no arm has a single yard row, and that denial is the honest one: an\ninbound-off run is not a run whose yard was empty, it is a run with no yard, and a\nfamily of figures asserting zero trailer-days would be a claim about a model that did\nnot exist. The driver logs it and skips the render, which is exactly what should\nhappen to every run in the archive.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::batch_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::breakdown_dict": {
      "decorators": [],
      "doc": "{key: (travel, handling)}, memoised in ctx._breakdown; {} (logged) on failure so\ndependent graphs degrade gracefully \u2014 EvalContext.breakdown delegates here.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::carry_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::drain_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::era_shortfall": {
      "decorators": [],
      "doc": "`EraUnmet` when this run cannot answer a quantity this evaluation draws, else None.\n\nTHE RUNTIME HALF OF THE ERA GATE. `core/era.py` proves statically that every quantity's\nread is either version-free or names a capability; this asks the far narrower question\nthat only a file can answer \u2014 does THIS run carry it. The gap between the two is a\nvetted vintage that has the table and no rows, which is common enough to have been\nmeasured (`reorder_queue`: 68 of 166 arms) and which nothing else would notice: the\nfigure would simply not appear, with an INFO line either way.\n\nOnly config-scope evaluations are checked, because only they read a sim DB \u2014 the\naggregate scope consumes series documents this same analysis wrote, and the run scope\nreads the run root. A scope with no capability probe available answers None rather than\nguessing, which is the same rule `_deny_absent` follows for a file it cannot see.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, ev)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::frame": {
      "decorators": [],
      "doc": "One strategy's frame of `kind`, memoised per kind and key.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, kind: str, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::free_index_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::metric_frames": {
      "decorators": [],
      "doc": "The {kind: frame} mapping `frames._metric_series` resolves a metric source against.\n\nOne place builds it, so a consumer iterating `_METRICS` cannot serve some kinds and\nsilently starve another \u2014 which is what a positional frame list did until a fourth\nframe made the omission likely rather than merely possible.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key) -> dict"
    },
    "Optimization/Performance_Evaluations/core/requests.py::missed_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::record_error": {
      "decorators": [],
      "doc": "Record that an evaluation's render raised.  Called only by `driver._run_one`.\n\nStores `(count, last_repr)` so the run-end summary can name both how often and what.",
      "is_async": false,
      "kind": "function",
      "signature": "(eval_key: str, exc: BaseException) -> None"
    },
    "Optimization/Performance_Evaluations/core/requests.py::request": {
      "decorators": [],
      "doc": "Decorator: register `fn` as the compose function for (scope, name).",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, scope: str)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::request._wrap": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fn)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::resolve_needs": {
      "decorators": [],
      "doc": "Resolve every request `ev.needs` declares against `ctx`.  Returns {} when all granted\n(resources are materialized into the context's caches as a side effect), else\n{need: Denied} for exactly the requests that could not be served.  Tallies either way.\n\nThe ERA check runs first and short-circuits: composing a resource this run cannot answer\nwould pay for every arm's frames to discover what the capability probe already knows.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, ev) -> dict"
    },
    "Optimization/Performance_Evaluations/core/requests.py::series_dict": {
      "decorators": [],
      "doc": "The composed series dict, memoised in ctx._series \u2014 EvalContext.series delegates here.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::shift_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::site_batch_frame": {
      "decorators": [],
      "doc": "A SITE's batch frame: every leaf's, concatenated.  Memoised in the shared frame\ncache under 'batch', so for a site context it REPLACES the per-leaf frame rather\nthan sitting beside it.\n\nONE CONCAT, AND IT ANSWERS BOTH OF THE DENOMINATOR QUESTIONS 07 asked, with no new\narithmetic anywhere:\n\n  * DOOR UTILIZATION takes the SITE CALENDAR SPAN \u2014 `min(batch_start_time)` to\n    `max(start + duration)` \u2014 and over the concatenated frame that is exactly the union\n    of the two leaves' clocks.  A door is occupied on the site's calendar whatever\n    channel's packs sit behind it.\n  * RECEIVER BUSY takes both leaves' `recv_seconds` over DISTINCT `work_day`, and over\n    the concatenated frame that is exactly what `nansum` and `nunique` return.  Still\n    WORK days and never the calendar span (memory `calendar-span-is-not-work-days`,\n    which printed 184% once); the leaves share days by construction, because\n    `staffing` refuses to sum channels that ran different batch counts.\n\nThe per-leaf `recv_seconds` are each leaf's OWN share of the site dock \u2014 the coupled\ndriver writes them from the coordinator's decomposition \u2014 so the sum is the site total\nrather than either channel's number counted twice.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::sql": {
      "decorators": [],
      "doc": "The composed SQL statement that would serve named query `name` on `schema_id` \u2014 for\nconsumers that want the statement rather than frames (the run_whatif_* trio pattern).\nDelegates to `Schema.dataset.sql_for`; raises `UnsupportedQuery` rather than guessing.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, schema_id: str, family: str='sim_db') -> str"
    },
    "Optimization/Performance_Evaluations/core/requests.py::tally_snapshot": {
      "decorators": [],
      "doc": "A picklable copy of this process's access tally; optionally reset (per-job snapshots).\n\nThe reset is load-bearing at `granularity='graph'`: a worker is reused across jobs, so\nwithout it every later job re-reports the earlier jobs' counts.",
      "is_async": false,
      "kind": "function",
      "signature": "(reset: bool=False) -> dict"
    },
    "Optimization/Performance_Evaluations/core/requests.py::task_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::work_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/core/requests.py::yard_frame": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/cost/__init__.py": {
      "decorators": [],
      "doc": "cost.* evaluations \u2014 what a placement rule costs to RUN, in real wall-clock seconds.\n\nThe only family that measures the SIMULATOR rather than the warehouse.  Every other\nfamily reports modeled seconds from the cost model \u2014 an effort figure for comparing\nplacement rules.  These report seconds a CPU actually spent, which is a different\nquestion with a different audience: a WMS engineer asking whether a rule is cheap enough\nto run per arriving unit, and whether it carries an offline job to schedule.\n\nRun scope, because the runtime metrics DB lives at the run root and spans every cell \u2014 no\nleaf and no per-cell aggregate can see it.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/cost/compute.py": {
      "decorators": [],
      "doc": "cost.compute \u2014 three views of what a placement rule costs to RUN.\n\nTHE QUESTION THIS ANSWERS, and who asked it.  A WMS engineer reviewing the published\nexperiment could not tell whether the winning rules are cheap enough to run: the site\ndescribed the `Map` family's offline address-map build without a number, and nothing said\nwhat per-arrival scoring costs either.  Both were measured all along \u2014 the per-arrival\ncost sat in the run's runtime DB, unread by anything but a set of absolute-second bar\ncharts nobody staged; the offline build was outside every clock until the setup span was\nadded.\n\nWHY THE PRIMARY VIEW IS A MULTIPLE.  Absolute seconds do not survive leaving the machine\nthat produced them: they are contended against the sweep's worker pool, they scale with\ncatalogue size, and on a 210-second bar the difference between the best and second-best\nrule is invisible.  The FIFO floor is the right denominator because FIFO does the same\nreorder bookkeeping and then picks a slot at random \u2014 so the RATIO isolates the scoring.\n\nTHE ONE THING NOT TO DO.  `precomp_s` is not a slice of `total_s` (the batch loop's clock\nstarts after setup), so it must never be stacked onto the section breakdown.  Every view\nhere keeps the two spans visibly separate; `runtime_metrics.OUTSIDE_TOTAL` is the\ndeclaration that makes that checkable rather than remembered.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_CONTROL_HATCH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'///'"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_absolute": {
      "decorators": [],
      "doc": "The cost in units a WMS reader can size against, INCLUDING the offline build.\n\nThree panels per channel, because the family's charter asks two questions and the old\ntwo-panel form answered one and a half.  Left: absolute milliseconds of placement per\narriving unit \u2014 absolute, not the delta over FIFO, which is a different quantity and\nwas previously drawn in an `absolute` view (FIFO showed a real per-wave bar beside a\nzero-width per-unit one, in the same figure).  Middle: seconds per wave.  Right: the\nOFFLINE build, the one span no view carried at all despite being the reason half this\nfamily exists \u2014 a rule with a bar there is a job somebody has to schedule.\n\nEvery bar carries its value, because the interesting rules are the cheap ones and a\nlinear axis scaled to the most expensive rule compresses them into a sliver.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, rows, out)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_bar_style": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(r)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_by_channel": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_delta": {
      "decorators": [],
      "doc": "Where the extra seconds go, as a DIFFERENCE from the floor rather than a stack.\n\nA stacked absolute breakdown is the natural chart here and the wrong one twice over:\nevery rule's stack is dominated by the sections it shares with FIFO, and the setup\nspan is not in the total the stack adds up to.  Differences against the floor show\nonly what the rule changed, and the setup span gets its own marker beside them.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, rows, out)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_in_order": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sub, order, key)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_percent": {
      "decorators": [],
      "doc": "Scoring cost as a multiple of the do-nothing floor \u2014 the view that travels.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, rows, out)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::_rule_order": {
      "decorators": [],
      "doc": "One rule order for every panel of the family, cheapest-first by the FIFO multiple.\n\nSorting each panel by its own values scrambles cross-channel reading: a reader\nfollowing one rule has to hunt for it again in the next panel.  The multiple is the\nright key because it is the family's primary view and it is unit-free.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/Performance_Evaluations/cost/compute.py::render": {
      "decorators": [
        "evaluation(key='cost.compute', label='What each placement rule costs to run', scope='run', needs=('runtime',), family='cost', shape='ranked', quantities=('reord_ms_per_unit', 'scoring_ms_per_unit', 'x_reord_vs_fifo'))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py": {
      "decorators": [],
      "doc": "cost.rollup \u2014 the per-rule compute-cost table, and the two spans it must keep apart.\n\nWHAT IS BEING MEASURED.  Two disjoint wall-clock spans per arm:\n\n  * the SETUP span (`precomp_s`) \u2014 `strat.build()`, where the map family solves its\n    offline address map.  Once per arm, before the batch loop's clock starts.\n  * the LOOP sections (`reord_s`, `sim_s`, \u2026) \u2014 a strict partition of `total_s`.\n\n`reord_s` is the one that answers \"what does this rule cost to run\": it is the per-arrival\nplacement scoring, and FIFO's value is the floor every other rule is measured against,\nbecause FIFO does the same reorder bookkeeping and then picks a slot at random.  The\nDIFFERENCE over FIFO is the scoring itself.\n\nWHY NOT RAW SECONDS.  They are contended (the sweep ran a worker pool), machine-specific,\nand dominated by scale \u2014 the store and fulfillment channels differ by 3x on the same rule\nfor reasons that have nothing to do with the rule.  Two normalisations travel with every\nrow instead: the multiple of the FIFO floor, and milliseconds per unit placed, which is\nthe number a WMS engineer can compare against their own dock throughput.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::BASE_RULE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_baseline.BASELINE.value"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::_f": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(row, key)"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::_med": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(vals)"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::_ratio": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(x, base)"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::cost_rows": {
      "decorators": [],
      "doc": "One row per (channel, rule), with both spans and both normalisations.\n\nGrouped by CHANNEL, not by leaf: a rule's compute cost is a property of the rule and\nthe catalogue it scores against, and the two channels have genuinely different\ncatalogues.  Cells and pairs collapse into the median, which is what makes `n_arms`\nworth printing beside it.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx) -> list"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::render": {
      "decorators": [
        "evaluation(key='cost.rollup', label='Per-rule compute cost (real wall seconds)', scope='run', needs=('runtime',), out_subdir='tables')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/cost/rollup.py::units_placed": {
      "decorators": [],
      "doc": "{(cell, pair, config, channel): units put away per wave} from the per-run tables.\n\nThe denominator that turns seconds into an engineering number.  Read from the leaf\nsummaries rather than assumed, and averaged across arms within a leaf: every arm faces\nthe same demand, so they agree to rounding, and taking the mean makes a missing arm a\nsmall error rather than a wrong one.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx) -> dict"
    },
    "Optimization/Performance_Evaluations/diagnostics/__init__.py": {
      "decorators": [],
      "doc": "diagnostics.* evaluations \u2014 raw operational read-outs (small-multiple grids and\nper-arm scorecards) whose job is inspection, not comparison.  Submodules are\nimported by core.discovery; this package declares nothing itself.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/diagnostics/metric_grids.py": {
      "decorators": [],
      "doc": "diagnostics.metric_grids \u2014 small-multiple grids, one panel per arm.\n\nFour grids (batch duration over time, layout efficiency, task-duration distribution,\ntask duration over time), ported from the retired per-strategy grids MINUS the churn\ngrid, which moved to the layout family as a single overlay.  What changed beyond the\nmove: every grid now FORCES one shared y scale across its panels \u2014 the retired grids\nlet every facet autoscale, the cardinal small-multiples sin, so two arms whose panels\nlooked identical could differ by half their range.  Each duration grid picks ONE time\nunit from its own pooled values (`chartkit.time_units`) and names it in the axis label \u2014\na batch is hours-ish while a task is seconds, so no single fixed unit serves both \u2014 only\nedge panels carry axis labels, the histogram grid additionally bins every arm on one\ncommon edge set IN THAT UNIT, and any line-role encoding (median / mean / IQR) gets one\nshared legend below the panels instead of thirty in-panel copies.\n\nRaw operational read-outs for inspection, not comparison \u2014 the diagnostics exemption\nin the family grammar.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/diagnostics/metric_grids.py::_eff_series": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df, optimal)"
    },
    "Optimization/Performance_Evaluations/diagnostics/metric_grids.py::_finish_panels": {
      "decorators": [],
      "doc": "Arm-name panel titles + the edge-only labeling rule.",
      "is_async": false,
      "kind": "function",
      "signature": "(ch, ncols, n, strategies, xlabel, ylabel)"
    },
    "Optimization/Performance_Evaluations/diagnostics/metric_grids.py::_grid_chart": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(n, legend_labels=())"
    },
    "Optimization/Performance_Evaluations/diagnostics/metric_grids.py::_pool": {
      "decorators": [],
      "doc": "One flat array of every value that will share a grid's axis \u2014 the input to the\ngrid's single `chartkit.time_units` call.  Empty in, harmless zero out.",
      "is_async": false,
      "kind": "function",
      "signature": "(arrays)"
    },
    "Optimization/Performance_Evaluations/diagnostics/metric_grids.py::render": {
      "decorators": [
        "evaluation(key='diagnostics.metric_grids', label='Per-arm metric grids (shared scales)', scope='per_strategy', needs=('batch', 'task', 'series'), family='diagnostics', shape='inspection')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/diagnostics/scorecards.py": {
      "decorators": [],
      "doc": "diagnostics.scorecards \u2014 one three-panel read-out per arm.\n\nBatch duration (rolling mean) \u00b7 layout efficiency (optimal \u00f7 realised total f\u00b7D when\nthe optimal floor is known, raw f\u00b7D otherwise) \u00b7 inventory churn (% of bins moved per\nbatch).  Ported from the retired per-strategy scorecards; the shared panel helpers that\nmodule imported are inlined here (their home module is gone).  A raw operational\nread-out for inspecting one arm in isolation \u2014 the diagnostics exemption in the family\ngrammar.\n\nThe duration panel's unit is chosen once, from every arm's batch durations pooled\ntogether, so all the arms' scorecards in a leaf read in the SAME named unit and stay\ncomparable at a glance \u2014 and so a run whose batches are seconds does not print 0.0008.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/diagnostics/scorecards.py::_churn_series": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df, total_bins)"
    },
    "Optimization/Performance_Evaluations/diagnostics/scorecards.py::_eff_series": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df, optimal)"
    },
    "Optimization/Performance_Evaluations/diagnostics/scorecards.py::render": {
      "decorators": [
        "evaluation(key='diagnostics.scorecards', label='Per-arm scorecards', scope='per_strategy', needs=('batch',), family='diagnostics', shape='inspection')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/dossier/__init__.py": {
      "decorators": [],
      "doc": "dossier \u2014 the run-root index a page loads first.\n\nOne evaluation, and it exists so that every output in the run tree has exactly ONE owner.\n\n`cost.rollup` declares `out_subdir='tables'` and wrote the run-root index to the parent of\nthat, through `os.path.dirname(out)`.  Nothing stopped it: `artifact_map.save_in_bounds`\nchecks a path against the evaluation's declared subdir, and an evaluation that writes\noutside its own bounds is invisible to a check keyed on those bounds.  So the index \u2014 the\ndocument every dossier macro loads before anything else \u2014 was written by an evaluation\nthat did not declare it, and `artifact_map.findings()` could not see the discrepancy.\n\nThe obvious fix is worse than this one: giving `cost.rollup` `out_subdir=('', 'tables')`\nmakes `save_in_bounds` unmatchable on the `''` member, which converts a silent gap into a\nsilent pass.  A root-scope evaluation of its own is what the `catalog.*` evaluations\nalready do, and it costs one module.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/dossier/index.py": {
      "decorators": [],
      "doc": "dossier.index \u2014 the one document a page loads first.\n\nRun identity, the worker count the timings were taken under, and the cost rows: enough\nthat no macro has to know the layout of the tree it is reading from.\n\nIt used to be written by `cost.rollup`, which declares `out_subdir='tables'` and reached\nits parent with `os.path.dirname(out)` \u2014 see this package's docstring for why that made\nthe file invisible to `artifact_map`.  The rows come from the same shared `cost_rows`\nhelper `cost.rollup` uses, so the CSV and the index cannot disagree about a number; the\nbroker has already served the runtime metrics by the time the second of the two runs.\n\nThe basename comes from the HEAD run-tree contract rather than being spelled here, so the\ndeclaration and the writer cannot disagree and the filename appears in this module as no\ntext at all.\n\n**HEAD, not the run's own contract.**  `ctx.rt` resolves against the document the RUN was\nmade under, which is right for a CONSUMER and wrong for a writer: this run predates the\ndossier artifacts entirely, so `ctx.rt.path('dossier_json')` raises `KeyError` on it.  A\nwriter emits the tree the current code declares; the resolver exists to read a tree that\nwas already written.  (The failure was silent-shaped and only visible because the render\nerror tally now names an evaluation that raised.)",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/dossier/index.py::_ARTIFACT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'dossier_json'"
    },
    "Optimization/Performance_Evaluations/dossier/index.py::_BASENAME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_head_basename(_ARTIFACT)"
    },
    "Optimization/Performance_Evaluations/dossier/index.py::_SPAN_NOTE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'Wall-clock compute cost of the SIMULATOR, not modeled warehouse labor. Setup and loop spans are disjoint: precomp_s is measured before the batch loop starts and is not part of total_s.'"
    },
    "Optimization/Performance_Evaluations/dossier/index.py::_head_basename": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(artifact: str) -> str"
    },
    "Optimization/Performance_Evaluations/dossier/index.py::render": {
      "decorators": [
        "evaluation(key='dossier.index', label='Run dossier index', scope='run', needs=('runtime',), out_subdir='')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/driver.py": {
      "decorators": [],
      "doc": "Driver \u2014 scope dispatch + output-dir preparation for the registry.\n\nThe flat-pool worker in run_analysis.py builds an EvalContext (config stage) or an\nAggregateContext (aggregate stage) and calls run_config / run_aggregate (config granularity)\nor run_one (graph granularity).  Directory preparation is done ONCE by the parent pre-pass\n(prepare_config_dirs / prepare_aggregate_dir) so no worker races to wipe a shared dir; each\nevaluation only writes its own files into the shared figures/ and tables/ tops (the old\nsingle-owner self-wipe pattern is retired \u2014 no render wipes anything).\n\n`_run_one` is the sole render entry, which makes it the access-control choke point: every\nevaluation's declared `needs=` is resolved through the request broker (core/requests.py)\nBEFORE its render runs.  All granted -> render exactly as before (the resolution warmed the\ncontext caches the render reads).  Any denied -> the render is SKIPPED, gracefully, and the\n`[access]` log line carries the reason the broker actually hit \u2014 a missing resource is an\nanswerable event, never a crash and never a silent half-figure.\n\nIt also catches every render exception, so one broken evaluation cannot sink the rest.\nThat swallow is deliberate and it was also, for a long time, invisible: the only trace was\na log call from inside a worker whose logger reaches no file.  Every caught exception is\nnow recorded in the broker's tally and reported at the end of the run, because a grant is\na statement about an evaluation's INPUTS and says nothing about whether anything came out.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/driver.py::_CONFIG_SCOPES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('per_strategy', 'config')"
    },
    "Optimization/Performance_Evaluations/driver.py::_run_one": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, ev, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::aggregate_keys": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(preset)"
    },
    "Optimization/Performance_Evaluations/driver.py::config_keys": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(preset)"
    },
    "Optimization/Performance_Evaluations/driver.py::prepare_aggregate_dir": {
      "decorators": [],
      "doc": "Wipe + recreate one _aggregate/<pickcfg>/ root once (parent pre-pass).",
      "is_async": false,
      "kind": "function",
      "signature": "(out_dir)"
    },
    "Optimization/Performance_Evaluations/driver.py::prepare_config_dirs": {
      "decorators": [],
      "doc": "Wipe + recreate the per-config shared output dirs exactly once (parent pre-pass).\n\nThe dir list is DERIVED from the registry's `out_subdir` declarations (see\n`artifact_map.config_dirs` for the rule); a golden test pins it to the historical\nliterals so the derivation can never silently relocate an output.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir)"
    },
    "Optimization/Performance_Evaluations/driver.py::prepare_run_dir": {
      "decorators": [],
      "doc": "Wipe + recreate the run-root dossier root, then every declared run-scope subdir.\n\nNo shared-top/single-owner rule here, unlike `prepare_config_dirs`: run-scope\nevaluations execute serially in the parent after every cell has finished, so there is\nno worker to race.  One wipe of the root, then `artifact_map.run_dirs()`.",
      "is_async": false,
      "kind": "function",
      "signature": "(out_dir)"
    },
    "Optimization/Performance_Evaluations/driver.py::prepare_site_dir": {
      "decorators": [],
      "doc": "Wipe + recreate one pair's `site_dir` analysis root, then its declared subdirs.\n\nThe site DB lives in this same directory and is the SIMULATION's output, not the\nanalysis's \u2014 so this wipes the analysis SUBDIRS and never the root, unlike\n`prepare_run_dir`.  Wiping the root here would delete `inbound_<arm-pair>.db` and every\nlater re-analysis of the same run would find no yard at all, which reads exactly like\nan inbound-off run.",
      "is_async": false,
      "kind": "function",
      "signature": "(out_dir)"
    },
    "Optimization/Performance_Evaluations/driver.py::resolve_params": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ev, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::run_aggregate": {
      "decorators": [],
      "doc": "Run all aggregate evaluations named in `keys` (config granularity).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, keys, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::run_at_root": {
      "decorators": [],
      "doc": "Run all run-scope evaluations named in `keys`, over the whole run root.\n\nCalled by analyze_run AFTER every cell and after the cross-cell what-if writers: these\nevaluations read what those stages produced, so ordering is a data dependency, not a\npreference.  Serial in the parent \u2014 there are a handful of them and each one reads the\nentire run, so a pool would buy nothing and cost the wipe-race rule.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, keys, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::run_config": {
      "decorators": [],
      "doc": "Run all per_strategy + config evaluations named in `keys` (config granularity).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, keys, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::run_one": {
      "decorators": [],
      "doc": "Run a single evaluation by key (graph granularity).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::run_root_keys": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(preset)"
    },
    "Optimization/Performance_Evaluations/driver.py::run_site": {
      "decorators": [],
      "doc": "Run all site-scope evaluations named in `keys` (config granularity).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, keys, overrides, cli_set)"
    },
    "Optimization/Performance_Evaluations/driver.py::site_keys": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(preset)"
    },
    "Optimization/Performance_Evaluations/headline/__init__.py": {
      "decorators": [],
      "doc": "headline.* evaluations \u2014 the stakeholder-first answer figures (top arms vs FIFO,\nthe all-arm delta panels, per-run rollup dot plots, the throughput-vs-labor thesis).\nSubmodules are imported by core.discovery; this package declares nothing itself.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/headline/all_arms.py": {
      "decorators": [],
      "doc": "headline.all_arms \u2014 every arm's % improvement vs the FIFO baseline, at a glance.\n\nTwo horizontal-bar panels (throughput improvement, duration improvement) that SHARE\none y category order \u2014 arms sorted by duration improvement, best on top \u2014 so a reader\nscans one ranking, not two.  The panels also share the x scale when the magnitudes\nallow it; when one panel's swings would flatten the other, each keeps its own scale\nand says so out loud.  Sign is carried by the zero reference line and the sort, not\nby red/green bars: bars wear their arm's suite color.\n\nNO LEGEND, DELIBERATELY.  Every arm is already named on the y axis, so a colour key is\none row of restatement per arm \u2014 a third of the canvas spent telling the reader what\nthe tick label beside the bar just told them.  The single thing the axis cannot say is\nwhich row IS the reference, and that is marked IN PLACE on the FIFO row (dotted rule,\nbold tick label, a note at the zero line).  The gutter those rows used to occupy goes\nback to the panels.\n\nPorted data logic: the retired delta-bars chart \u2014 throughput \u0394% off the steady-state\nthroughput scalar and duration improvement % off the steady-state batch-makespan\nscalar, both against strategies[0] (the FIFO arm).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/headline/all_arms.py::_SHARE_RATIO": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3.0"
    },
    "Optimization/Performance_Evaluations/headline/all_arms.py::render": {
      "decorators": [
        "evaluation(key='headline.all_arms', label='All arms vs FIFO (% improvement panels)', scope='config', needs=('series',), family='headline', shape='ranked', quantities=('throughput', 'makespan'), views_suppressed=(('absolute', \"this is the 34-bar wall headline/rollup.py retired: at full arm count the absolute values differ by a few percent, so every bar is the same length and the figure carries no information the rollup does not. The absolute level of these two quantities is headline.rollup's job, at top-N.\"),))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/headline/rollup.py": {
      "decorators": [],
      "doc": "headline.rollup \u2014 production-time-first per-run rollups as sorted dot plots.\n\nThe per-arm aggregations are ported from the retired per-run report (total production\ntime = \u03a3 task time over every batch, production-time-per-item's siblings, mean\ncompletion rate, put-away queue depth \u2014 the honesty metric: a strategy that defers\nplacement carries a standing queue).  The table outputs moved to another module; this\none draws ONLY figures, and replaces the unreadable 34-bar walls with sorted dot\nplots: one dot per arm, FIFO as the zero reference line on the percent views, best at\nthe top, panel height computed from the category count so labels stay readable.\n\nThe queue-depth figure is written only when any arm ever carried a queue (the ported\nconditional) \u2014 an all-zero chart would claim an honesty check that never ran.\nBaseline = strategies[0] (the FIFO arm).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/headline/rollup.py::_dot_chart": {
      "decorators": [],
      "doc": "One sorted horizontal dot plot: rows = [(strategy, value)], best already first.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, xlabel, title, subtitle, path, view, *, strategies, base_key=None, zero_line=False)"
    },
    "Optimization/Performance_Evaluations/headline/rollup.py::_summaries": {
      "decorators": [],
      "doc": "Per-arm rollup scalars, ported from the retired per-run report.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/headline/rollup.py::render": {
      "decorators": [
        "evaluation(key='headline.rollup', label='Per-run rollup dot plots', scope='per_strategy', needs=('batch', 'task'), family='headline', shape='ranked', quantities=('production_time', 'throughput'))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py": {
      "decorators": [],
      "doc": "headline.throughput_vs_labor \u2014 the four success metrics, and the adoption question.\n\nThree scatter panels, one point per strategy arm, built from the per-arm series\nscalars.  Vocabulary: TASK makespan = \u03a3 task time = total labor (the serial makespan\na lone picker would incur); BATCH makespan = last-picker finish (parallel wall-clock).\nThe two throughputs are items \u00f7 each makespan.\n\n  * Panel A: x = task makespan  vs  y = throughput / batch makespan.  Marker size \u221d\n    batch makespan.  Best corner is UP-LEFT.\n  * Panel B: x = batch makespan  vs  y = throughput / batch makespan \u2014 the\n    mechanistic link.\n  * Panel C: x = throughput / task makespan  vs  y = throughput / batch makespan.\n    Separates the two win types: a pure SCHEDULING win (batch makespan \u2193 at flat task\n    makespan) moves a point straight UP; a LABOR win (less task time per item) moves\n    it RIGHT.\n\nThe thesis stays: throughput does NOT track task makespan (total labor) \u2014 a placement\nthat lowers \u03a3 task labor can pile work onto the busiest picker, raising batch\nmakespan and lowering throughput.  So throughput tracks 1/batch-makespan (Panel B,\ntight) \u2014 not task makespan (Panel A, a scattered cloud).  The Spearman \u03c1 triplet in\nthe subtitle quantifies it.\n\nA fourth figure, the PERCENT view, answers the question the three scatter panels set up\nbut do not close: which arm to adopt.  Labor gain against throughput gain, both against\nthe baseline, so the corner where an arm beats FIFO on both is a place on the chart\nrather than a comparison the reader has to carry between two other figures.\n\nUNITS.  Throughput is items/hour on its face.  The two makespan axes are not: a makespan\nis whatever the run makes it (a small cell's batch finishes in seconds, a full run's in\nhours), so both resolve ONE shared time unit from their pooled magnitudes and name it in\nthe axis label \u2014 panels A and B measure the same kind of quantity and must never be read\nin different units.  The \u03c1 triplet is rank-based, hence unit-invariant.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py::_both_levers": {
      "decorators": [],
      "doc": "percent view \u2014 each arm's labor gain against its throughput gain, vs the baseline.\n\nThe question two independent reviewers asked and no artifact answered: WHICH ONE DO\nWE ADOPT.  Every other headline figure ranks on one quantity, and the podium is not\nthe same on both \u2014 an arm can save labor by spreading work to the least-loaded aisle\nand finish the day later for it.  Here the baseline is the origin, the shaded corner\nis \"better on both\", and an arm in an off-diagonal quadrant is trading one for the\nother in the direction its position names.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, S, base, bd)"
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py::_f": {
      "decorators": [],
      "doc": "Coerce to float, mapping None/missing to NaN.",
      "is_async": false,
      "kind": "function",
      "signature": "(v)"
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py::_panel": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ax, xs, ys, colors, labels, base_xy, xlabel, title, sizes=None)"
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py::_rho": {
      "decorators": [],
      "doc": "Spearman rank correlation over finite pairs; NaN if too few / degenerate.",
      "is_async": false,
      "kind": "function",
      "signature": "(xs, ys)"
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py::_sizes": {
      "decorators": [],
      "doc": "Map makespan \u2192 marker area in [40, 300]; flat 90 when all equal / non-finite.",
      "is_async": false,
      "kind": "function",
      "signature": "(durs)"
    },
    "Optimization/Performance_Evaluations/headline/throughput_vs_labor.py::render": {
      "decorators": [
        "evaluation(key='headline.throughput_vs_labor', label='Throughput vs total labor (scatter + Spearman \u03c1)', scope='config', needs=('series',), family='headline', shape='scatter', quantities=('throughput', 'production_time'))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py": {
      "decorators": [],
      "doc": "headline.top_vs_baseline \u2014 how the best arms compare to the FIFO baseline.\n\nTwo figures in the headline family:\n\n  * the percent view \u2014 grouped bars of % improvement of each top arm vs the FIFO\n    baseline across the headline metric groups (positive = better, one-decimal data\n    labels).  Metric groups whose swings are an order of magnitude larger than the\n    rest (layout f\u00b7D, typically) move to a SECOND panel that says out loud its scale\n    is independent, so they stop crushing the 2\u20133% labor effects \u2014 the flaw of the\n    retired single-axis chart.  Each metric group is annotated with the best arm's\n    paired effect vs FIFO: matched-pairs rank-biserial r plus a Holm-corrected\n    Wilcoxon p.\n  * the table view \u2014 one row per top arm: labor and throughput % vs FIFO, paired\n    Hedges g, a 95% interval for the labor figure, and the Wilcoxon p LAST \u2014 demoted,\n    because with n\u224875 paired batches essentially every p is three-star; the subtitle\n    says exactly that and the effect columns carry the discrimination.  The labor\n    figure and its interval are the same estimator (the median of the per-batch\n    improvements, and the bootstrap of that median), so the interval always contains\n    the number it sits beside.\n\nBoth figures pair each arm against FIFO over the batches the two runs share\n(`_paired`, ported verbatim from the retired compare module), so labor and\nthroughput answer one question with one method.  Baseline = strategies[0] (the\nFIFO/uniform-random arm).  Params: top_n, top_by.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_METRIC_GROUPS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[(q.label, q.source.steady_state, q.lower_is_better, *q.source.per_batch, q.source.per_batch[0] in _quantities.PAIRED_KINDS) for q in (_quantities.BY_KEY[k] for k in _quantities.HEADLINE_ORDER)]"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_UNSHARED_RATIO": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "8.0"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_drawable": {
      "decorators": [],
      "doc": "({label: values} the figure can draw, [labels dropped]) \u2014 ORDER PRESERVED.\n\nA group with nothing finite in it is dropped, not drawn empty.  Every group could once\nbe relied on to produce numbers, so this was unreachable code; the funnel's two\nappended entries (ticket 08) make it the normal case \u2014 an inbound-off run has no yard,\nso the fee's scalar is NaN on every arm, and an archived run predating `work_events`\nhas no put-away hours.  Both are the overwhelming majority of what gets re-analysed.\n\nRendering those as a blank labelled panel would put an unanswered question on published\nevidence, and a reader who sees a labelled group with no bar in it concludes the wrong\nthing: not \"this run cannot answer that\" but \"it was measured, and it was nothing\".\nThat is the same substitution the era gate exists to prevent, arriving one layer lower\nwhere the gate cannot see it \u2014 this figure survives its optional quantities on purpose,\nso nothing else is going to refuse on its behalf.",
      "is_async": false,
      "kind": "function",
      "signature": "(vals)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_impr": {
      "decorators": [],
      "doc": "Signed % improvement vs baseline (always oriented so higher = better).",
      "is_async": false,
      "kind": "function",
      "signature": "(val, base, lower)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_pair_diffs": {
      "decorators": [],
      "doc": "Per-batch paired % improvement (positive = better) for an aligned pair, with the\npairs where the comparison is undefined dropped by the shared rule.",
      "is_async": false,
      "kind": "function",
      "signature": "(b, v, lower)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_paired": {
      "decorators": [],
      "doc": "(pct_change, wilcoxon_p, n_batches, base_vals, strat_vals) for one per-batch\nmetric vs the FIFO baseline, paired over the batches the two runs share.\n\npct = (median(strat) \u2212 median(base))/median(base) \u00b7 100 \u2014 the RAW sign; callers\nreorient it through the one improvement convention before display.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, s, source, col)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_percent_figure": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, selected, S, baseline, out, top_n, top_by)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::_table_figure": {
      "decorators": [],
      "doc": "Rows are labelled with _stitle (initial|assignment|reslot), not the bare\nassignment name \u2014 Opt|Rank_labor and Uni|Rank_labor are different arms and must\nnot collapse to one label.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, selected, S, baseline, out)"
    },
    "Optimization/Performance_Evaluations/headline/top_vs_baseline.py::render": {
      "decorators": [
        "evaluation(key='headline.top_vs_baseline', label='Top arms vs FIFO baseline (% bars + effect table)', scope='config', needs=('series', 'batch', 'task'), defaults={'top_n': 3, 'top_by': 'initial'}, family='headline', shape=('ranked', 'table'), quantities=('production_time', 'makespan', 'throughput', 'throughput_task', 'sigma_fd'), quantities_optional=('total_production_time', 'yard_overage_days'), views_pending=(('absolute', \"the top-N arms' own steady-state levels beside the baseline's, which is what a reader asks for after seeing a 2.6% bar and wanting to know 2.6% of what. The table view carries the numbers today; a bar of them is not written yet\"),))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/labor/__init__.py": {
      "decorators": [],
      "doc": "labor.* \u2014 the labor-cost figure family: per-batch labor hours, task-time deltas vs\nthe FIFO baseline (top-N and the one-panel-per-arm grid), all through chartkit.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/labor/delta_grid.py": {
      "decorators": [],
      "doc": "labor.delta_grid \u2014 the task-time delta vs FIFO, one small panel per arm.\n\nThe retired per-strategy pair drew two separate grids (a line grid and a bar grid) over\nthe SAME `series._prodtime_delta` numbers, wasted panels on the baseline's identically\nflat delta-vs-itself facets, and \u2014 worst \u2014 let every facet autoscale independently, so a\n\u00b10.3% arm looked exactly like a \u00b12.8% arm.  This merge fixes all three:\n\n  * one grid, one panel per NON-baseline arm \u2014 bold cumulative % saved over the light\n    per-batch % line, so level and swing read together;\n  * a FORCED shared y scale across every panel (`chartkit.shared_ylim`), which is the\n    small-multiples rule: panel-to-panel comparison IS the chart;\n  * a zero reference per panel \u2014 the FIFO baseline appears as that line, not as a facet.\n\nPanel titles are the compact arm names (`style._stitle`); only edge panels carry axis\nlabels, and the bottom legend explains the bold/light encoding once for the whole grid.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/labor/delta_grid.py::_LEGEND_ROWS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('cumulative % saved (bold)', 'per-batch % (light)')"
    },
    "Optimization/Performance_Evaluations/labor/delta_grid.py::render": {
      "decorators": [
        "evaluation(key='labor.delta_grid', label='Task-time delta vs FIFO, one panel per arm', scope='per_strategy', needs=('series',), family='labor', shape='facet', quantities=('production_time',), views_pending=(('absolute', 'the level of each arm over batches, one panel per arm. trajectories.overtime already draws it overlaid; the facet version is worth having at 34 arms, where the overlay is one opaque ribbon, and is not written yet'), ('percent', 'the same facet expressed as improvement rather than as hours saved \u2014 the ratio a reader compares across arms of different absolute size; not written yet')))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/labor/delta_topn.py": {
      "decorators": [],
      "doc": "labor.delta_topn \u2014 the task-time delta story vs FIFO, told once, in one figure.\n\nThe retired suite told it twice: one chart plotted the cumulative % of baseline task\ntime saved but anchored its y-axis at zero, so a 2\u20133% treatment effect occupied the top\ntenth of the canvas; a second chart re-plotted the same per-batch deltas as a bar forest.\nMerged here into two panels sharing the batch axis:\n\n  TOP     the HONEST cumulative measure \u2014 running task-time saved over running baseline\n          total (`series._prodtime_delta`'s cum series, not a mean of percents) \u2014 with y\n          clipped to the data (`chartkit.data_ylim`); the zero reference appears only\n          when it is within or near the data window, because forcing it in is exactly\n          the canvas-wasting anchor this figure exists to fix.\n  BOTTOM  the per-batch % trend: the raw line kept faint so the demand swing stays\n          visible, a small rolling mean carrying the policy level, and a rolling \u00b11 std\n          band around it (`chartkit.draw_ci`) so the spread is drawn, not implied.\n\nArm selection is the shared top-N rule (`series._select_top`).  The FIFO baseline is\nexcluded from selection \u2014 its delta against itself is identically zero \u2014 and appears as\nthe zero reference line instead of a redundant flat series or a duplicate legend row:\nthe legend carries strategy names only.\n\nParams: top_n (int), top_by ('global' | 'initial' | 'assignment' | 'reslot').",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/labor/delta_topn.py::_ylim_zero_aware": {
      "decorators": [],
      "doc": "Clip y to the data; draw the FIFO zero reference only when zero is already\nwithin \u2014 or near (25% of the data span beyond) \u2014 the data window.  A far-away zero\nwould re-anchor the axis and compress the effect, which is the old chart's bug.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, values)"
    },
    "Optimization/Performance_Evaluations/labor/delta_topn.py::render": {
      "decorators": [
        "evaluation(key='labor.delta_topn', label='Task-time saved vs FIFO \u2014 cumulative + per-batch (top-N)', scope='config', needs=('series',), defaults={'top_n': 3, 'top_by': 'initial'}, family='labor', shape='serial', quantities=('production_time',), views_pending=(('absolute', \"the top-N arms' own labor hours over batches, which labor.per_batch draws for the same arm selection; consolidating the two is the fix, not a third figure\"), ('percent', 'the cumulative saving as a fraction rather than in hours; not written yet')))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py": {
      "decorators": [],
      "doc": "labor.per_batch \u2014 labor cost of the scheduled tasks, batch by batch, in two views.\n\nWHAT \"LABOR\" IS HERE.  The per-batch sum of task durations \u2014 the serial makespan a lone\npicker would incur, i.e. total work, independent of how many pickers ran in parallel.\nThat is the quantity the assignment function actually moves; the scheduler does not\nchange it.  Durations are sim milliseconds and render in hours via `chartkit.to_hours`,\nthe same conversion the what-if layer uses, so figures reconcile across the suite.\nHOURS IS FIXED HERE, not chosen from the data like the duration axes elsewhere in the\nsuite: \"labor hours per batch\" is the reader-facing quantity itself \u2014 the staffing\nnumber the what-if layer, the tables and the site all quote \u2014 so the axis names the unit\nthe question is asked in, and a run whose batches happen to be small simply reads small.\n\nWHY IT IS PLOTTED AGAINST BATCH NUMBER.  Labor per batch is demand-driven: a batch with\nmore items costs more, whatever the policy.  Plotting over batch order shows whether a\npolicy's advantage HOLDS as the warehouse fills and reorders churn, or decays.  The\npercent view removes the demand signal entirely by expressing each batch against FIFO on\nthat same batch, so what remains is the policy effect alone.\n\nBoth views draw the raw line faint UNDER the smoothed line, in the strategy's own color\nAND its own dash (the retired chart drew raw lines in one unmapped tan, so they could not\nbe attributed): hiding the raw spread would overstate how cleanly separated the arms are.\nThe raw stroke was still too pale and too thin to read as its arm's color, and nothing on\nthe canvas said what it was \u2014 a reader saw beige lines swinging twice as far as anything\nin the legend.  So: the raw stroke carries the SAME color and dash as the arm's smoothed\nline and differs only in weight, and ONE legend row states that convention.  Everything\nelse in the legend is a strategy name \u2014 the retired chart printed six near-identical stat\nrows there; the compact steady-state h/batch listing lives in the absolute view's subtitle.\n\nTWO THINGS NOT TO READ INTO IT.  (1) There is no warm-up transient: batch 0 measured 70%\nof median on a real run, well inside the family, so the opening batches are not excluded.\n(2) One batch in the store channel carries ~4 items; its FIFO ratio divides two tiny\nnumbers, so the percent view is scaled by percentile to the real signal and the offending\nbatches are marked with triangles at the axis bottom edge \u2014 identified, never removed.\nThe batch sequence is shared across arms, so it biases no comparison.\n\nParams: top_n (int), top_by ('global' | 'initial' | 'assignment' | 'reslot'),\nwin (steady-state tail for the subtitle means).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_RAW_KW": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "dict(lw=0.9, alpha=0.45)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_RAW_LABEL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "f'faint = raw \u00b7 bold = {_SMOOTH}-batch mean'"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_RAW_TITLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'strategy \u00b7 line weight'"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_absolute": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, arms, curves, base_curve, win, top_n, top_by, out)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_percent": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, arms, curves, base_curve, top_n, top_by, out)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_raw_handle": {
      "decorators": [],
      "doc": "The one legend row that explains every faint stroke on the canvas.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_smooth": {
      "decorators": [],
      "doc": "Centred moving average over the window the over-time trajectories use.\n\nPromoted to `chartkit.rolling_mean` \u2014 this module's implementation was the only\nedge-correct one of the three in the package, and the primitive carries the reason\n(a `mode='same'` convolution pads with zeros and fabricates a collapse at exactly the\ntwo places a reader looks for a trend to start or finish).  Kept as a named alias\nbecause `_SMOOTH` is this family's window and the call sites read better for it.",
      "is_async": false,
      "kind": "function",
      "signature": "(y, win=_SMOOTH)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_sub_note": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(top_n, top_by)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::_with_raw_note": {
      "decorators": [],
      "doc": "Place the legend with the raw/smoothed row appended after the strategy rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, ch, **kw)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::labor_by_batch": {
      "decorators": [],
      "doc": "(batch_ids, labor_hours) \u2014 \u03a3 task duration per batch, in hours, by batch order.",
      "is_async": false,
      "kind": "function",
      "signature": "(task_df)"
    },
    "Optimization/Performance_Evaluations/labor/per_batch.py::render": {
      "decorators": [
        "evaluation(key='labor.per_batch', label='Labor hours per batch + % saved vs FIFO, over batch order', scope='config', needs=('task', 'series'), defaults={'top_n': 3, 'top_by': 'initial', 'win': _WIN}, family='labor', shape='serial', quantities=('production_time',), views_pending=(('delta', 'hours saved per batch against the baseline, in hours rather than as a percentage. labor.delta_topn draws this for the top N arms only; the full-arm version belongs here and is not written yet'),))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/labor/production_legs.py": {
      "decorators": [],
      "doc": "labor.production_legs \u2014 the three legs of production time, per arm.\n\nTHE FIGURE THE OBJECTIVE WAS MISSING.  Ticket 10 fixed the inbound objective as expected\nfuture WORK \u2014 the put + pick hours a placement generates \u2014 and ticket 08 made total\nproduction hours phase 1's selection metric.  Until the `work` frame landed, only the pick\nleg was reportable, and the honest consequence was stated in the ticket: a comparison on\npick hours alone systematically favours arms that buy pick time with put-away time, which\nis the exact trade the inbound effort exists to measure.\n\nOne stacked bar per arm, three segments:\n\n    unload   receiving labour  (`work_events`, role='receive')\n    put      put-away labour   (`work_events`, role='put')\n    pick     picking labour    (`task_stats.duration`, summed per batch)\n\nRead it for the SPLIT, not the total \u2014 the total already has a headline panel.  An arm\nwhose bar is shorter than its neighbour's while its put segment is longer bought its\npicking win with put-away hours, and the put share printed on each bar is what makes that\ncomparison possible across arms of different sizes.\n\nDeliberately ABSOLUTE-only, which the `composite` mark enforces: the meaning here is the\ndecomposition, and a decomposition has no single value to compare against a baseline.  The\nper-leg comparison against the baseline arm is a real question and it is answered by the\n`tables.vs_baseline` evaluation, which now emits `putaway_time` and `unload_time` as their\nown rows with paired intervals \u2014 numbers, where this is a shape.\n\nUnload is near-absent inbound-off: no trailers, no receiving crew, no unload hours.  That\nsegment being invisible on an archive run is the model saying it had no inbound, not a\nrendering fault.  A run that recorded no work events at all does not reach here \u2014 all three\nquantities name the `work_events` capability, so the era gate refuses the render with a\nreason rather than drawing pick hours under a label promising three legs.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/labor/production_legs.py::_LEGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('unload_seconds', 'unload_time', 'unload (receiving)', '#dd8452'), ('put_seconds', 'putaway_time', 'put-away', '#c44e52'), ('pick_seconds', 'production_time', 'pick', '#4c72b0'))"
    },
    "Optimization/Performance_Evaluations/labor/production_legs.py::_totals": {
      "decorators": [],
      "doc": "[(strategy, [seconds per leg])] over the arms with a populated work frame.\n\nSummed over every batch rather than the steady-state window, and that is the one\nchoice worth stating: the legs are FLOWS of seconds, the put-away queue drains on its\nown schedule, and a trailing window can cut a put-away burst away from the picking it\nwas serving.  The whole run is the only cut where the three legs describe the same\nwork.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames)"
    },
    "Optimization/Performance_Evaluations/labor/production_legs.py::render": {
      "decorators": [
        "evaluation(key='labor.production_legs', label='Production time by leg (unload / put / pick) per arm', scope='config', needs=('work',), family='labor', shape='composite', quantities=('unload_time', 'putaway_time', 'production_time'))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/layout/__init__.py": {
      "decorators": [],
      "doc": "layout.* evaluations \u2014 placement-layout dynamics (inventory churn and its\ncross-arm spread).  Submodules are imported by core.discovery; this package\ndeclares nothing itself.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/layout/churn.py": {
      "decorators": [],
      "doc": "layout.churn \u2014 inventory churn over time, all arms on ONE overlay.\n\nReplaces the retired 34-facet churn grid.  The churn series is ported unchanged:\n(reload moves + reorder placements) per batch, as a % of total bins when the bin\ncount is known, rolled with the steady-state window.  The overlay draws the\ncross-arm MEDIAN as one bold line and the cross-arm IQR as a band; an individual\narm is drawn (and labelled) ONLY when its curve leaves the median by more than 2\u00d7\nthe IQR somewhere AND by enough of the plotted range to be findable on the canvas\n\u2014 everything conforming stays inside the band, and a corner note says how many arms\nthat is, so 30 indistinguishable facets become one statement plus its exceptions.\n\nA LEGEND ROW IS A PROMISE.  The IQR test alone cannot keep it: churn is identical\nacross most arms on most runs, so the cross-arm IQR collapses to ~0 and `dev > 2 \u00d7\n0` promotes any arm that differs in the last float digit.  Seven such arms were\nlegended and drawn \u2014 at the median's own values, underneath the median's own bold\nline, invisible.  Hence the second test (`_VISIBLE_FRAC`) and the raised z-order\nwith a light halo: whatever the legend names, the reader can point at.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/layout/churn.py::_MEDIAN_COL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'#1a4d7a'"
    },
    "Optimization/Performance_Evaluations/layout/churn.py::_VISIBLE_FRAC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.02"
    },
    "Optimization/Performance_Evaluations/layout/churn.py::_churn_series": {
      "decorators": [],
      "doc": "Ported: bins moved per batch (reloads + reorder placements), % of bins when\nthe total is known, rolling-mean smoothed.",
      "is_async": false,
      "kind": "function",
      "signature": "(df, total_bins)"
    },
    "Optimization/Performance_Evaluations/layout/churn.py::render": {
      "decorators": [
        "evaluation(key='layout.churn', label='Inventory churn overlay (median + IQR + outliers)', scope='config', needs=('batch', 'series'), family='layout', shape='ranked', quantities=('reorder_churn',), views_suppressed=(('percent', 'churn is exactly zero on every arm when reslotting is off, which is the common case, and an improvement percent against a zero baseline is NaN for every arm \u2014 a chart of thirty-four empty rows under a caption asserting a comparison'),))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/layout/travel.py": {
      "decorators": [],
      "doc": "layout.travel \u2014 travel cost per arm against the baseline, the layout family's\ncomparison work.\n\nThe layout family owned only the churn overlay, and churn is flat (often exactly zero\nwhen reslotting is off), so the family that should answer \"did this placement rule\nactually shorten the walking?\" answered nothing.  The quantity that does differentiate\narms \u2014 \u03a3 f\u00b7D, demand-weighted distance, the objective the assignment functions optimise \u2014\nwas reachable only through a diagnostics small-multiples grid, and diagnostics is the\nfamily explicitly exempted from comparison work.\n\nTwo views of one quantity, and the set is DERIVED (`ranked` mark \u00d7 `sigma_fd`), not\nchosen here.  A ranked mark's categories are ARMS, which do not pair with themselves, so\nthe derivation gives no delta \u2014 the per-batch difference lives with the serial marks:\n\n  absolute  each arm's steady-state \u03a3 f\u00b7D beside the layout floor when the run publishes\n            one, which turns a unitless objective into \"how much of the achievable\n            improvement did this rule capture\".\n  percent   per-arm improvement against the baseline, sorted, with the bootstrap interval\n            of the PAIRED per-batch improvements, so the ranking says which gaps are\n            separable and which are not: where an interval crosses zero, the arm has not\n            been shown to move travel at all.\n\n**The percent view used to be called `delta`.**  It computed `improvement_pct_series`,\nlabelled its axis through `pct_axis`, and saved as `delta_travel_vs_baseline.png` under\n`view='delta'` \u2014 a percent under a delta's name, which five save-time checks passed\nbecause every one of them compared a declaration against another declaration and none of\nthem ever saw a number.  It was not carelessness: the `layout` family's allow-list had no\n`percent` in it, so there was nowhere honest to put the figure.  Both halves are fixed \u2014\nthe view set comes from the quantity and the mark, and `common/marks.py` computes the\nstance from raw values, so the filename and the arithmetic come from one decision.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/layout/travel.py::QUANTITY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'sigma_fd'"
    },
    "Optimization/Performance_Evaluations/layout/travel.py::_TITLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'absolute': ('Travel cost per arm', 'steady-state demand-weighted distance; baseline shaded'), 'percent': ('Travel cost vs baseline', 'median of the per-batch improvements in \u03a3 f\u00b7D, with the boots..."
    },
    "Optimization/Performance_Evaluations/layout/travel.py::_entries": {
      "decorators": [],
      "doc": "(entries, paired) \u2014 the absolute view's scalars, and the pairing for the rest.\n\n`entries`  [(strategy, steady-state \u03a3 f\u00b7D)], for the absolute bars.\n`paired`   {key: (arm per-batch \u03a3 f\u00b7D, baseline per-batch \u03a3 f\u00b7D)} over the batches the\n           two arms SHARE, in \u03a3 f\u00b7D.  `marks` derives the comparison point and its\n           interval from this, both in the same stance, so the dot and the whiskers\n           estimate one quantity \u2014 and the interval is the interval of the comparison\n           rather than of the arm's own spread wearing a percentage.\n\nAn arm sharing fewer than three batches with the baseline appears in `entries` and not\nin `paired`: it keeps its bar and loses its dot, which is the honest pair of answers.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, S, baseline)"
    },
    "Optimization/Performance_Evaluations/layout/travel.py::_figure": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, entries, paired, baseline, view, out)"
    },
    "Optimization/Performance_Evaluations/layout/travel.py::_per_batch": {
      "decorators": [],
      "doc": "\u03a3 f\u00b7D per batch for one arm, indexed by batch.\n\nRead straight off the batch frame rather than through the generic metric-series\nhelper: that helper takes a task frame it does not consult for a batch-scoped\ncolumn, and asking the broker for the task frame would both cost a real read per arm\nand make this evaluation's `needs=` declaration a lie.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key)"
    },
    "Optimization/Performance_Evaluations/layout/travel.py::render": {
      "decorators": [
        "evaluation(key='layout.travel', label='Travel cost per arm vs the baseline', scope='config', needs=('batch', 'series'), family='layout', shape='ranked', quantities=(QUANTITY,))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/presets.py": {
      "decorators": [],
      "doc": "Presets \u2014 the manifest that decides which graphs run and with what params.\n\nA preset bundles: `keys` (the enabled evaluation keys, in run order), `overrides`\n(per-graph param overrides keyed by evaluation key), and `focus` (a context-level\nstrategy filter, 'uni' | 'opt' | 'all').  Toggle a graph by adding/removing its key;\ntune a graph via its `overrides` entry \u2014 no central argparse to grow.\n\nKeys are grouped by chart family (core/families.py): the tables group writes the tidy\nCSV surface, the figure groups each own one folder of the leaf's figures tree.  The two\nstats flavours stay mutually exclusive per preset, exactly as before the redesign:\n'assignment' runs the flat suite, 'initial' the uni-vs-opt per-assignment fork.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/presets.py::PRESETS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'DEFAULT': {'keys': _keys('assignment'), 'overrides': _top(1, 'global') | _AGG_FLAT, 'focus': 'uni'}, 'NO_STATS': {'keys': _keys(None), 'overrides': _top(1, 'global') | _AGG_FLAT, 'focus': 'uni'},..."
    },
    "Optimization/Performance_Evaluations/presets.py::_AGG": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['agg.traj', 'agg.tables', 'agg.sig']"
    },
    "Optimization/Performance_Evaluations/presets.py::_AGG_FLAT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'agg.tables': {'by_initial': False}, 'agg.sig': {'by_initial': False}}"
    },
    "Optimization/Performance_Evaluations/presets.py::_DETAIL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['task_time.duration', 'task_time.breakdown', 'labor.production_legs', 'layout.travel', 'layout.churn', 'yard.binding', 'yard.detention', 'yard.fee', 'yard.scorecard', 'diagnostics.metric_grids', '..."
    },
    "Optimization/Performance_Evaluations/presets.py::_GROUPS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(_TABLES, _HEADLINE, _TRENDS, _DETAIL, _AGG, _SITE, _RUN)"
    },
    "Optimization/Performance_Evaluations/presets.py::_HEADLINE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['headline.top_vs_baseline', 'headline.all_arms', 'headline.rollup', 'headline.throughput_vs_labor']"
    },
    "Optimization/Performance_Evaluations/presets.py::_RUN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['cost.compute', 'cost.rollup', 'dossier.index', 'tables.census', 'catalog.rules', 'catalog.inventory', 'catalog.fixed']"
    },
    "Optimization/Performance_Evaluations/presets.py::_SITE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['yard.site_binding', 'yard.site_detention', 'yard.site_fee', 'yard.site_scorecard']"
    },
    "Optimization/Performance_Evaluations/presets.py::_STATS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'assignment': ['tables.stats', 'sig.suite'], 'initial': ['tables.by_initial', 'sig.by_initial']}"
    },
    "Optimization/Performance_Evaluations/presets.py::_TABLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['config.series', 'tables.per_run', 'tables.tidy', 'tables.vs_baseline']"
    },
    "Optimization/Performance_Evaluations/presets.py::_TOP_KEYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('labor.delta_topn', 'labor.per_batch', 'throughput.volume')"
    },
    "Optimization/Performance_Evaluations/presets.py::_TRENDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['trajectories.overtime', 'labor.delta_topn', 'labor.per_batch', 'labor.delta_grid', 'throughput.volume', 'throughput.missed', 'throughput.audit']"
    },
    "Optimization/Performance_Evaluations/presets.py::_keys": {
      "decorators": [],
      "doc": "stats: 'assignment' (flat suite) | 'initial' (uni-vs-opt per fn) | None.",
      "is_async": false,
      "kind": "function",
      "signature": "(stats)"
    },
    "Optimization/Performance_Evaluations/presets.py::_top": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(n, by, *extra)"
    },
    "Optimization/Performance_Evaluations/presets.py::check_groups": {
      "decorators": [],
      "doc": "Every registered evaluation is named by exactly one group or one stats flavour.\n\nCalled from the PACKAGE `__init__`, immediately after `import_all()` \u2014 not at this\nmodule's own import, because `presets` is itself one of the modules that walk\ndiscovers, so at that moment the registry holds whatever happens to have been imported\nalphabetically before it.  Calling it there and not from a test is deliberate: a\nnewly registered evaluation must not be able to reach a run without someone deciding\nwhere it belongs, and the cost of finding out otherwise lands 21 minutes later as a\nfigure nobody notices is absent.\n\nThe message names the key and lists the groups, because \"add it somewhere\" is not\nactionable and \"which of these six\" is.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/Performance_Evaluations/significance/__init__.py": {
      "decorators": [],
      "doc": "significance \u2014 the effect-size figure family (merged panels, heatmap, forests).\n\nKey namespace 'sig.*'; the numbers these figures draw live in the tables evaluations.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/significance/panels.py": {
      "decorators": [],
      "doc": "Shared significance painters (not evaluations) \u2014 the three figure shapes every\nsignificance eval renders through:\n\n  * `merged_effect_panel` \u2014 ONE figure per metric replacing the retired four-file set\n    (distribution boxes, p heatmap, effect heatmap, rank bars).  Left: the per-strategy\n    distribution on a data-hugging axis in presentation units.  Right: the pairwise\n    rank-biserial ladder, dot-with-CI, each row annotated via chartkit.effect_label with\n    the Holm-corrected p baked into the stars \u2014 the label carries the significance, so\n    the separate p-matrix and rank-bar figures are gone on purpose.\n  * `effect_heatmap` \u2014 assignments \u00d7 metrics, fill = % improvement (diverging colormap\n    centred at 0, positive = better per chartkit.improvement_pct), non-significant\n    cells greyed/hatched.\n  * `forest_panel` \u2014 metrics as rows, % improvement with 95% CI whiskers.  Each file\n    hugs its OWN x range (one assignment's rows span \u00b10.1%, another's \u22125%\u2026+20%), so it\n    carries chartkit.annotate_unshared: dot position is comparable down a panel, never\n    between panels.\n\nEverything draws through chartkit (reserved geometry, no legend in the data region) and\nsaves with view='effect'; jitter is deterministic (np.random.default_rng(0)).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/significance/panels.py::_rb_ci": {
      "decorators": [],
      "doc": "95% CI for the matched-pairs rank-biserial of (a, b), resampling the PAIRS.\n\nReplaces a closed form that used the signed-rank NULL variance: that expression\ndepends only on n, so every row of a panel got an identical whisker regardless of\nwhat the runs showed \u2014 widest, absurdly, where the effect was most decisive.  See\nstats_core._rank_biserial_ci.",
      "is_async": false,
      "kind": "function",
      "signature": "(a, b)"
    },
    "Optimization/Performance_Evaluations/significance/panels.py::effect_heatmap": {
      "decorators": [],
      "doc": "Assignments \u00d7 metrics improvement heatmap.\n\nFill = % improvement (positive = better, per chartkit.improvement_pct \u2014 callers\norient before passing).  Each significant cell shows the % (1 decimal) with the\noriented rank-biserial beneath; cells with p >= 0.05 (or no p) are greyed and\nhatched with 'ns'.  Returns the saved path, or None on an empty grid.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, cols, pct_matrix, effect_matrix, p_matrix, out_path, *, title, cbar_label='% improvement (opt vs uni)')"
    },
    "Optimization/Performance_Evaluations/significance/panels.py::forest_panel": {
      "decorators": [],
      "doc": "Forest plot: one row per metric \u2014 % improvement dot, 95% CI whiskers, and an\neffect_label annotation (oriented rank-biserial + significance stars).\n\n`metric_rows` is a list of dicts with keys name / pct / lo / hi / p / effect\n(pct, lo, hi already improvement-oriented: positive = better).  Returns the saved\npath, or None when no row is drawable.\n\nTHE X AXIS IS PER FILE and says so.  One forest is written per assignment function\nand their effect magnitudes differ by orders of magnitude \u2014 a family whose rows all\nsit inside \u00b10.1% next to one spanning \u22125%\u2026+20%.  A shared scale would flatten the\nfirst into a vertical stack of dots, so each panel keeps its own range and carries\nthe `independent x scale` note: a reader flipping between files must compare the\nprinted effect labels, never the dot positions.",
      "is_async": false,
      "kind": "function",
      "signature": "(metric_rows, out_path, *, title, xlabel='% improvement (opt vs uni)')"
    },
    "Optimization/Performance_Evaluations/significance/panels.py::merged_effect_panel": {
      "decorators": [],
      "doc": "One metric, one figure: distribution (left) + pairwise effect ladder (right).\n\n`box_values` are RAW per-key samples (what the boxes show); `conv` maps them into\npresentation units for the left panel only (effects are unitless).  `tests` is a\nstats_core._run_tests document over the same keys.  `paired` is the ALIGNED\nblock \u00d7 key matrix those tests were computed over \u2014 the ladder's resampled interval\nneeds it, because a bootstrap of a PAIRED statistic has to resample pairs, and\nbox_values are not aligned with each other.  Without it the dots still draw and the\nwhiskers are simply omitted rather than invented.\n\nWith more pairs than `max_ladder` (a 17-arm family gives 136), the ladder keeps only\nbaseline-vs-each \u2014 every pairwise number still lives in the tests JSON beside this\nfigure.  Returns the saved path, or None when there is nothing to draw.",
      "is_async": false,
      "kind": "function",
      "signature": "(keys, colors, box_values, tests, out_path, *, title, lower_is_better, conv=None, unit_label='', max_ladder=28, paired=None)"
    },
    "Optimization/Performance_Evaluations/significance/suite.py": {
      "decorators": [],
      "doc": "sig.suite / sig.by_initial \u2014 the per-config significance FIGURES.\n\nBoth draw from the SAME computation the tables evaluations write (imported from the\ntables stats module), so figure and table can never disagree.  The retired layout was\n4 PNGs \u00d7 11 metrics for the all-strategies suite plus a full per-assignment-function\ndirectory tree for the by-initial fork (~748 files); this pair replaces it with:\n\n  * `sig.suite`      \u2014 ONE merged panel per metric (distribution + pairwise\n                       rank-biserial ladder, Holm-p in the row labels);\n  * `sig.by_initial` \u2014 the headline assignments \u00d7 metrics improvement heatmap, plus one\n                       forest plot per assignment function (metrics as rows, the MEDIAN\n                       paired % improvement with the bootstrap interval of that same\n                       median, so the dot and its whiskers estimate one quantity and the\n                       forest agrees with the heatmap cell above it; improvement-oriented,\n                       positive = better).\n\nPresentation units on distribution axes only: rates in items/hour, and every DURATION in\nthe time unit resolved at render time from that metric's own pooled distribution \u2014 a\nmetric whose median is seconds gets a seconds axis, not a column of 0.0008 hours\n(effects and % improvements are unitless).  ss_lo=0, as the tables use.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/significance/suite.py::render_by_initial": {
      "decorators": [
        "evaluation(key='sig.by_initial', label='Opt-vs-uni heatmap + per-fn forests', scope='config', needs=('batch', 'task', 'breakdown'), family='significance', shape='effect', by_initial=True)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/significance/suite.py::render_suite": {
      "decorators": [
        "evaluation(key='sig.suite', label='Merged effect panels (all strategies)', scope='config', needs=('batch', 'task', 'breakdown'), family='significance', shape='effect')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/tables/__init__.py": {
      "decorators": [],
      "doc": "tables.* evaluations \u2014 every per-config CSV/JSON data product (no figures).\n\nThe package name is the key namespace; all outputs land FLAT in the declared tables dir.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/tables/census.py": {
      "decorators": [],
      "doc": "tables.census \u2014 the count/span claims the pages keep making, computed instead of typed.\n\n\"Positive in all 68 same-rule comparisons per channel\", \"median +44.8 %, range 28.5\u201339.8 %\",\n\"positive in all 136 comparisons\".  Every one of those was hand-counted at authoring time\nand then RESTATED as prose, so the only way to check one was to re-derive it from the\nstaged what-if rows.  That is the shape of claim `stats_core.census` generalises, and this\nis where it gets emitted so a page can render the sentence from the artifact rather than\nassert it beside one.\n\nWHAT IS COUNTED.  Each row of the cross-cell what-if table is one same-rule comparison: an\narm under the treatment scheduler against the identical arm under the reference, on the\nsame catalogue and the same demand.  A census over those rows, grouped four ways, answers\n\"how broadly does this hold\" with an n, a win rate, an exact sign test and a span \u2014 the\nfour things a reader needs before quoting a median.\n\nWHY A SIGN TEST AND NOT THE PAIRED TEST THE LEAF TABLES USE.  Different question.  The leaf\ntables ask whether ONE arm beats the baseline, over 75 paired batches.  This asks how many\nARMS moved in the claimed direction, over 68 of them \u2014 the unit of observation is the arm,\nthe comparison is already a difference, and the only distributional assumption worth making\nabout a set of 68 arm-level deltas is which side of zero each one fell.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/tables/census.py::_CLAIMS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('thr_gain_vs_ref_pct', 'higher', 'throughput uplift vs the reference scheduler'), ('auc_gain_vs_ref_pct', 'higher', 'area between the cumulative volume curves'), ('labor_delta_vs_ref_pct', 'highe..."
    },
    "Optimization/Performance_Evaluations/tables/census.py::_GROUPINGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('channel', ('channel',)), ('channel_x_rule', ('channel', 'assignment')), ('scheduler', ('scheduler',)))"
    },
    "Optimization/Performance_Evaluations/tables/census.py::_num": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(row, key)"
    },
    "Optimization/Performance_Evaluations/tables/census.py::census_blocks": {
      "decorators": [],
      "doc": "[{metric, better, grouping, rows:[census...]}] over the what-if comparison rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows) -> list"
    },
    "Optimization/Performance_Evaluations/tables/census.py::comparisons": {
      "decorators": [],
      "doc": "The rows that ARE comparisons \u2014 the reference cell's own are not.\n\nThe what-if table carries every arm in every cell, reference included, and a reference\narm's \"gain vs reference\" is exactly 0 because it is being compared with itself.  Left\nin, those rows double the count, halve the win rate, and drag the median toward zero:\nthe first render of this census reported 136 comparisons per channel with 68 ties and a\nstore median of +19.9 %, against a true 68 comparisons, no ties, and +44.8 %.  A tie in\na sign test means \"the two were equal\"; a row compared with itself means nothing at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, reference)"
    },
    "Optimization/Performance_Evaluations/tables/census.py::render": {
      "decorators": [
        "evaluation(key='tables.census', label='How broadly each comparison holds (counts + sign test)', scope='run', needs=('whatif',), out_subdir='tables')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/tables/per_run.py": {
      "decorators": [],
      "doc": "tables.per_run \u2014 the two per-run CSV data products, ported verbatim from the retired\nper-strategy report module (schemas unchanged; the bar charts died in the redesign \u2014 the\nsignificance and headline families own those comparisons now).\n\nTwo documents:\n\n  * the raw long-format per-batch table \u2014 every batch \u00d7 strategy with all headline\n    metrics, written to the LEAF ROOT so external analysis (and the docs ingest) finds\n    it beside the series document, exactly where it has always lived;\n  * the per-run rollup \u2014 one row per strategy (production-time-first, with the\n    gaming-resistant per-item variant, the put-away queue honesty columns, and the\n    strategy's published color hex so charts and tables agree), written into the flat\n    tables dir the registration declares.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/tables/per_run.py::_per_run_rows": {
      "decorators": [],
      "doc": "(long_rows, summary_rows): the per-(strategy, batch) long records and the\nper-strategy rollup records.  Column names and dtypes are the OLD schemas \u2014\ndownstream consumers (docs ingest, notebooks) key on them.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, df_b, df_t, title)"
    },
    "Optimization/Performance_Evaluations/tables/per_run.py::render": {
      "decorators": [
        "evaluation(key='tables.per_run', label='Per-run rollup + long per-batch CSV', scope='per_strategy', needs=('batch', 'task'), out_subdir='tables')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/tables/stats_csv.py": {
      "decorators": [],
      "doc": "tables.stats / tables.by_initial \u2014 the per-config significance DATA products.\n\nThe retired stats suite computed the paired tests once and wrote CSVs, JSON and four PNGs\nper metric into per-assignment subdirectories.  Post-redesign the split is clean: the\nNUMBERS live here (flat, in the declared tables dir), the FIGURES live in the\nsignificance family \u2014 and both are built from the same shared computation in this module\n(`compute_config_stats` / `compute_by_initial`), so the panels can never disagree with\nthe tables they sit beside.\n\n  * `tables.stats`      \u2014 per-strategy descriptives (the exact retired columns,\n                          including sem and the 95% CI bounds) plus the omnibus+pairwise\n                          test document over all strategies.\n  * `tables.by_initial` \u2014 the uni-vs-opt contrast per assignment function: the combined\n                          summary CSV (exact retired columns) plus ONE consolidated test\n                          document keyed by assignment function, replacing the retired\n                          17 per-function copies.\n\nss_lo=0 throughout: every batch is a fair paired observation, so the tests use the full\nn (~100), not just the steady-state tail window (the retired suite's deliberate choice).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/tables/stats_csv.py::by_initial_tests_doc": {
      "decorators": [],
      "doc": "The consolidated by-initial test document: one JSON-ready dict keyed by\nassignment function (replaces the retired one-file-per-function layout).",
      "is_async": false,
      "kind": "function",
      "signature": "(per_fn, ss_lo)"
    },
    "Optimization/Performance_Evaluations/tables/stats_csv.py::compute_by_initial": {
      "decorators": [],
      "doc": "The uni-vs-opt contrast per assignment function, computed once.\n\nReturns (combined, per_fn):\n  combined  list of rows with EXACTLY the retired summary columns\n            (assignment, metric, n, uni_median, opt_median,\n             pct_change_opt_vs_uni, p_wilcoxon, better) \u2014 pct_change is the raw\n            median shift, sign NOT improvement-oriented, as the retired CSV had it;\n  per_fn    {fn: {'keys': {'uni','opt'}, 'metrics': {metric_name: detail}}} where\n            detail carries the paired arrays (u, o), the direction flag, the full\n            pairwise test document, and the summary scalars \u2014 the raw material for\n            the consolidated test JSON and the significance heatmap/forest panels.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, frames, ss_lo)"
    },
    "Optimization/Performance_Evaluations/tables/stats_csv.py::compute_config_stats": {
      "decorators": [],
      "doc": "The full per-config suite over all strategies, computed once.\n\nReturns (summary_rows, per_metric):\n  summary_rows  list of {'strategy', 'metric', **descriptives} \u2014 the CSV body,\n                including the travel/handling decomposition rows when provided;\n  per_metric    {metric_name: {'box_values', 'paired', 'tests', 'lower'}} \u2014\n                everything the merged effect panels need, in _METRICS order.\n                `box_values` are each key's own observations (what the boxes show);\n                `paired` is the aligned block \u00d7 key matrix (what every paired\n                statistic and its resampled interval must be computed over).  They\n                are different objects on purpose: a key with a missing block belongs\n                in its own box but not in a pair.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, frames, ss_lo, travel_handling=None)"
    },
    "Optimization/Performance_Evaluations/tables/stats_csv.py::render_by_initial": {
      "decorators": [
        "evaluation(key='tables.by_initial', label='Uni-vs-opt tables per assignment fn', scope='config', needs=('batch', 'task', 'breakdown'), out_subdir='tables', by_initial=True)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/tables/stats_csv.py::render_stats": {
      "decorators": [
        "evaluation(key='tables.stats', label='Significance descriptives + tests (all strategies)', scope='config', needs=('batch', 'task', 'breakdown'), out_subdir='tables')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/tables/tidy.py": {
      "decorators": [],
      "doc": "tables.tidy \u2014 tidy long-format replacements for the retired 103-column MultiIndex\nsummary pivots.\n\nThe old pair of wide tables put every (strategy \u00d7 statistic) in its own column, which no\ndownstream tool could consume without bespoke unpivoting.  These two documents are the\ntidy re-statement of the same data:\n\n  * the batch table \u2014 one row per (strategy, batch_id, metric) over every numeric\n    batch-frame column, carrying the strategy's identity split (initial / assignment /\n    reslot) and the upstream Tukey outlier flag so a consumer can filter honestly;\n  * the task table \u2014 one row per (strategy, batch_id) with the task count, the\n    mean/median/summed task duration, and the share of tasks flagged as outliers.\n\nValues stay in raw sim units on purpose: these are DATA products for downstream\ncomputation, not presentation surfaces \u2014 unit policy is applied where a figure is drawn.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/tables/tidy.py::_NON_METRIC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'is_outlier', 'work_day')"
    },
    "Optimization/Performance_Evaluations/tables/tidy.py::_batch_long": {
      "decorators": [],
      "doc": "One row per (strategy, batch_id, metric) over the numeric batch-frame columns.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, df_b)"
    },
    "Optimization/Performance_Evaluations/tables/tidy.py::_task_long": {
      "decorators": [],
      "doc": "One row per (strategy, batch_id): task count, duration stats, outlier share.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies, df_t)"
    },
    "Optimization/Performance_Evaluations/tables/tidy.py::render": {
      "decorators": [
        "evaluation(key='tables.tidy', label='Tidy long-format batch/task tables', scope='config', needs=('batch', 'task'), out_subdir='tables')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/tables/vs_baseline.py": {
      "decorators": [],
      "doc": "tables.vs_baseline \u2014 every arm against the baseline, as auditable numbers.\n\nThe headline table figure prints the claim the whole study turns on: this arm saves this\nmuch labor against FIFO, with this effect size, this interval and this p.  Those numbers\nexisted only as pixels.  The by-initial documents beside them answer a DIFFERENT question\n(optimal vs uniform placement WITHIN one assignment function), so a reader who tried to\ncheck a headline figure against the tables would find no row for it \u2014 and the docs site's\nrule that every published number cites a committed file could not be satisfied for the\nmost important numbers in the run.\n\nOne row per (strategy, metric), each carrying the same quantities the headline figure\nshows and computed by the same rules:\n\n    pct_median   the median of the per-batch improvements, improvement-oriented\n                 (positive = better), which is the figure's point estimate\n    ci_lo/ci_hi  the bootstrap interval OF THAT MEDIAN \u2014 same estimator, so the\n                 interval always contains the point\n    hedges_g     the paired effect size on the raw per-batch values\n    rank_biserial  its distribution-free companion\n    p_wilcoxon / p_holm   raw and family-corrected across arms within a metric\n    n_batches    the paired batches the two arms actually share\n\nEvery arm appears, not just the podium: the figure shows the top N, and a reader\nchecking why an arm did NOT make the podium needs its row too.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/tables/vs_baseline.py::_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('strategy', 'initial', 'assignment', 'reslot', 'metric', 'n_batches', 'baseline_median', 'arm_median', 'pct_median', 'ci_lo', 'ci_hi', 'hedges_g', 'rank_biserial', 'p_wilcoxon', 'p_holm', 'better')"
    },
    "Optimization/Performance_Evaluations/tables/vs_baseline.py::_paired_values": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key, source, col)"
    },
    "Optimization/Performance_Evaluations/tables/vs_baseline.py::compute_vs_baseline": {
      "decorators": [],
      "doc": "[row dicts] \u2014 every arm against ctx.base, over every declared metric.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx)"
    },
    "Optimization/Performance_Evaluations/tables/vs_baseline.py::render": {
      "decorators": [
        "evaluation(key='tables.vs_baseline', label='Every arm vs the baseline (auditable numbers)', scope='config', needs=('batch', 'task'), out_subdir='tables')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/task_time/__init__.py": {
      "decorators": [],
      "doc": "task_time.* evaluations \u2014 where picker time goes: per-arm task-duration\ndistributions and deltas, and the travel/handling + picking/traveling decompositions.\nSubmodules are imported by core.discovery; this package declares nothing itself.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/task_time/breakdown.py": {
      "decorators": [],
      "doc": "task_time.breakdown \u2014 where picker time goes, in two complementary cuts.\n\nOne render, two figures, ONE shared category order (the strategy declaration order),\nso a reader can scan the same arm across both without re-finding it:\n\n  * the absolute view \u2014 the stacked per-arm picker-time decomposition (travel vs\n    handling, reconstructed from picker events over the steady-state sample), in the\n    time unit `chartkit.time_units` picks from both components of every bar pooled\n    together, each bar annotated with its travel share.\n  * the second absolute view \u2014 the picking% / traveling% split of aggregate picker time\n    (already percent-of-time in the series scalars), as horizontal stacked bars.\n\nMovement is orange in both figures and stationary work is blue, so the two cuts read\nas one vocabulary.  Data logic ported from the retired travel-vs-handling and\npick-vs-travel charts.  The travel/handling decomposition degrades gracefully: an\nempty reconstruction skips that figure and the percent split still renders.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/task_time/breakdown.py::_MOVE_COL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'#dd8452'"
    },
    "Optimization/Performance_Evaluations/task_time/breakdown.py::_WORK_COL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'#4c72b0'"
    },
    "Optimization/Performance_Evaluations/task_time/breakdown.py::_breakdown_figure": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, out)"
    },
    "Optimization/Performance_Evaluations/task_time/breakdown.py::_split_figure": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, out)"
    },
    "Optimization/Performance_Evaluations/task_time/breakdown.py::render": {
      "decorators": [
        "evaluation(key='task_time.breakdown', label='Picker-time decomposition (two cuts)', scope='config', needs=('breakdown', 'series'), family='task_time', shape='composite')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/task_time/duration.py": {
      "decorators": [],
      "doc": "task_time.duration \u2014 per-arm task-duration distributions, absolute and vs FIFO.\n\nTwo figures in the task_time family:\n\n  * the absolute view \u2014 one box per arm of steady-state task durations, in the time\n    unit `chartkit.to_time` picks from every arm's values pooled together (a per-task\n    duration is seconds, so a fixed hours axis read 0.0008 and nothing else),\n    arms sorted by median (best on the left), the y-range trimmed to the pooled\n    2\u201398 percentile band (stated in a corner note) so a handful of outlier tasks\n    cannot flatten the boxes.  The FIFO baseline's box is edge-marked in the\n    baseline style and is the ONLY legend entry \u2014 the x labels already name every\n    arm, so a color legend would restate them.\n  * the delta view \u2014 per-arm % improvement vs FIFO as a dot with a 95% CI whisker:\n    per-batch MEAN task durations are paired against FIFO's on the batches the two\n    runs share, each pair becomes a % improvement, and the t-interval of those\n    paired diffs is the whisker.  Sorted, with the zero line as the FIFO reference.\n\nData logic ported from the retired steady-state task-duration box chart (the `win`\nsteady-state tail and the per-arm duration pulls).  Baseline = strategies[0].\nParams: win (steady-state tail width in batches).",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/task_time/duration.py::_absolute_figure": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, out, win)"
    },
    "Optimization/Performance_Evaluations/task_time/duration.py::_delta_figure": {
      "decorators": [],
      "doc": "Pair per-batch MEAN task durations vs FIFO's over shared batches.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, out)"
    },
    "Optimization/Performance_Evaluations/task_time/duration.py::_room_below": {
      "decorators": [],
      "doc": "Deepen the reserved bottom margin so rotated category labels are not\nclipped \u2014 chartkit sizes the grid from panel geometry and cannot see\ntick-label extents.  The provenance footer band stays clear below them.",
      "is_async": false,
      "kind": "function",
      "signature": "(ch, labels)"
    },
    "Optimization/Performance_Evaluations/task_time/duration.py::_ss_durations": {
      "decorators": [],
      "doc": "Steady-state task durations for one arm, RAW sim milliseconds (ported tail\nselection).  Unit conversion waits for the caller, which pools every arm on the\naxis before choosing one.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, s, win)"
    },
    "Optimization/Performance_Evaluations/task_time/duration.py::render": {
      "decorators": [
        "evaluation(key='task_time.duration', label='Task duration: ranked boxes + paired deltas', scope='config', needs=('task',), defaults={'win': 50}, family='task_time', shape=('ranked', 'serial'), quantities=('task_mean_duration',), views_pending=(('percent', 'the ranked improvement in mean task duration, which is the view a reader compares arms with when the absolute durations differ by a few percent; not written yet'),))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/throughput/__init__.py": {
      "decorators": [],
      "doc": "throughput.* \u2014 the throughput figure family: cumulative pick-volume curves and the\npercent lead over the FIFO baseline at matched elapsed time.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/throughput/audit.py": {
      "decorators": [],
      "doc": "throughput.audit \u2014 declared throughput vs delivered: the calibrated era's audit.\n\nThe second caller of the equilibrium check (\"Declare the equilibrium bands\", decision 6):\n`Optimization/simconfig/equilibrium.check_rows` judged over EVERY day the ledger closed,\nper arm, and rendered as realized-vs-expected utilization per department plus the\ndrained / capped day count.  The first caller, the reference-run driver, uses the same\nfunction as a PRECONDITION; this one REPORTS (decision 7):\n\n  - a CAPPED day is flagged \"declared throughput not delivered\";\n  - a picking utilization BELOW the band on a campaign arm is the arm's travel saving --\n    the effect being measured -- and is labelled as such, never as a failure;\n  - a nonzero `released_late` on a drained day still RAISES (`InstrumentError`): that is\n    the instrument contradicting itself, and the driver records it as an error for this\n    evaluation rather than a figure quietly not appearing.\n\nNothing here fails a run.  The sim never judges itself, and neither does the audit.\n\nA DRAINED day is a LABOUR verdict (`equilibrium.is_drained`, amending decision 4 by \"Choose\nthe coverage floor\", decision 7): the supply carry -- demand no bin could serve -- is stock\nnot delivered, the `supply` clause's quantity, and never keeps a day from draining.  The\nper-day frame carries both halves (`standing_carry_labour` / `standing_carry_supply`) and\nthe labour clause's `drained` reading lists the days that closed with supply carry standing.\n\nSince \"Split the missed-share clause into supply and labour\" (2026-09-09) the check judges\nthe two causes apart, both as `carryover` FLOWS over FRESH demand (`equilibrium.demand_flows`,\nfed from `ctx.carry_df`): the `labour` clause reads the realized cut share against the\nstamped expected cut share (ADR-0004) with the standing labour carry bounded and not\ntrending, and the `supply` clause reads the first-attempt supply share against the stamped\n`1 - fill`.  \"Every day drained\" is a reading inside the labour clause, never a verdict.\nThe two shares ride the inspection table as two more rows per arm beside the departments.\n\nSince \"Band the own-bin share and the free-index depth\" (2026-09-10) the `rework` clause\njudges the tier spill, the own-bin top-up and the repack at exactly zero and reads the\nfree-index depth PER BUCKET off the `free_index` table (`ctx.free_index_df`) against the\nrecord's per-bucket setup free; the table carries the three events and the depth (the\nsection and its driest bucket) as five more rows per arm.\n\nOvertime caps a day (\"Overtime behind a drained day raises the instrument\", 2026-09-07):\na day whose last task finished past its cap is labour that did not fit the day, and the\nloader serves a pre-amendment ledger's `drained` with that term folded in -- which is what\nlets the store leaf of the line-floor check render at all (its day 3, stamped drained with\n138 s of overtime, raised the released-late clause behind it).\n\n## Two marks, one quantity\n\n`days_capped` is the ONE quantity (ranked, absolute + percent vs baseline): a capped day\nhas an unambiguous direction.  Realized vs expected utilization has none -- the same\nbelow-band read is a saving on one arm and a broken derivation on another -- so it lives\nin an INSPECTION table, the family grammar's slot for a read-out with no baseline in it,\nexactly as `yard.scorecard` holds door utilization.  Expected values, `band_tol`, the\ncrews and the calibration stamps come off the `staffing` record stamped onto\n`sim_result` (03's sixth seam) through `ctx.staffing_expectations()`; the audit needs no\nseam work and reads nothing from CONFIG.\n\n## Gated by the ledger\n\nThe quantity names the `shift_days` capability, so the era gate refuses the render on\nevery run without a drain-or-cap ledger -- the whole archive, and every flag-off run since\n-- with a reason in the log.  A run that HAS a ledger but no staffing expectations (its\nrecord carries no derived block) still renders the day verdicts; the utilization rows\nread \"n/a\" rather than zero.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('arm', 'days', 'drained', 'capped', 'overtime', 'reading', 'crew', 'expected', 'realized', 'band', 'read')"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_COL_W": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(1.4, 0.5, 0.6, 1.2, 0.7, 0.9, 0.5, 0.8, 0.8, 0.6, 1.5)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_DEPT_LABEL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'pick': 'picking', 'put': 'put-away', 'recv': 'receiving'}"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_REWORK_ROWS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('spills', 'tier spills'), ('topups', 'own-bin top-ups'), ('repacked_packs', 'repacked packs'))"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_SHARE_ROWS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('supply', 'supply share', 3), ('labour', 'cut share', 4))"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_flags": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(expectations)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_lead_row": {
      "decorators": [],
      "doc": "The order-to-shelf lead row under the supply share: the STAMPED lead as the expected\ncell, the REALIZED lead (Little's law over the window, `equilibrium.realized_lead`) as\nthe realized cell, no band, and the read naming the EXPLAINED supply level\n`1 - fill(realized)` off the record's curve (\"Declare the coverage against the inbound\nlead\", decision 10) -- reported, never judged.",
      "is_async": false,
      "kind": "function",
      "signature": "(head, reading)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_read": {
      "decorators": [],
      "doc": "The one-word reading of a department's band position, in the audit's vocabulary.",
      "is_async": false,
      "kind": "function",
      "signature": "(dept, reading, is_base, tol)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_read_share": {
      "decorators": [],
      "doc": "The one-word reading of a flow clause's level, plus its trend and (labour) its carry.\n\n`in_band` is None when the record carries no expectation for the level -- the reading\nsays so rather than pretending; a trend past its tolerance or a carry past a day's\ncapacity is named even when the level sits in band, because each fails the clause on\nits own.",
      "is_async": false,
      "kind": "function",
      "signature": "(reading)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_rework_rows": {
      "decorators": [],
      "doc": "The rework clause's rows of the inspection table for one arm, after the shares: the\nthree events judged at zero, then the per-bucket depth as a report.",
      "is_async": false,
      "kind": "function",
      "signature": "(head, verdict)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_rows": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, s, sdf, verdict, expectations)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_share_rows": {
      "decorators": [],
      "doc": "The supply and cut-share rows of the inspection table for one arm, after the\ndepartments: expected / realized / band / read, the crew cell blank.",
      "is_async": false,
      "kind": "function",
      "signature": "(head, verdict)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_short_bucket": {
      "decorators": [],
      "doc": "`handling/category/size` for the table: the `unit` segment is implied by the size\nclass in this repo (`ff_*` is fulfillment, `singleton` is singleton, the rest pallet)\nand the full label does not fit the cell.  Display only -- the reading keeps the key.",
      "is_async": false,
      "kind": "function",
      "signature": "(label: str) -> str"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::_verdict_for": {
      "decorators": [],
      "doc": "The check over every day this arm's ledger closed, or None without expectations.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key, sdf, expectations)"
    },
    "Optimization/Performance_Evaluations/throughput/audit.py::render": {
      "decorators": [
        "evaluation(key='throughput.audit', label='Throughput audit: declared vs delivered', scope='config', needs=('shift', 'batch'), family='throughput', shape=('ranked', 'inspection'), quantities=('days_capped',))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/throughput/missed.py": {
      "decorators": [],
      "doc": "throughput.missed \u2014 demand the floor was asked for and could not pick.\n\nThe availability axis. It was floated as the inbound OBJECTIVE and lost to expected future\nwork, and it is here on the terms of that decision: a REPORTED axis, never a target and\nnever a selection metric. Nothing ranks arms by it and nothing optimises toward it. It\nexists because a policy that wins hours by starving the shelf has to show that somewhere,\nand this is the somewhere.\n\n## Two supply reasons, and one that is not\n\n`unpicked_unstocked` (nothing on the shelf) and `unpicked_unavailable` (stock exists, the\nbin could not be reached) are what inbound can move. `unpicked_daycut` is NOT included and\nis not an oversight: the whistle stopping a picker mid-shift is a staffing fact, so folding\nit in would let a longer working day read as better inbound. `unpicked_notasks` is out for\nthe same kind of reason \u2014 no task was built for it, which is scheduling.\n\n## A per-batch share, not the equilibrium check's\n\n`missed_share` here is per EFFECTIVE batch: `items_demanded` under the era is the sampled\ndemand plus the previous batch's carry, and a unit the shelf fails on two consecutive days\nis in two batches' rows. The calibrated era's equilibrium check judges supply on a\ndifferent arithmetic \u2014 the same two reasons over FRESH demand with each unit counted once\n(`Optimization/simconfig/equilibrium.py`, `demand_flows`, the `supply` clause) \u2014 because its\nexpectation, the stamped `1 - fill`, is a first-pass quantity. The two agree only with\nrollover off. This figure keeps its definition because it draws on every vintage, and a run\nalone does not say whether it re-offered anything.\n\n## Ungated by the yard, gated by carryover\n\nDemand service is meaningful with no inbound model at all, so these draw on every run that\nCAN answer them. What they cannot outrun is the `carryover` table, which postdates most of\nthe archive \u2014 hence the `carryover` capability on both quantities and the broker's denial\nwhen the table is absent. An arm with no missed rows served all its demand, which is a\nresult and renders normally.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/throughput/missed.py::_PANELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('missed_pieces', lambda df: float(df['missed_pieces'].sum())), ('missed_share', lambda df: float(np.nanmean(df['missed_share'].values))))"
    },
    "Optimization/Performance_Evaluations/throughput/missed.py::_entries": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames, fold)"
    },
    "Optimization/Performance_Evaluations/throughput/missed.py::render": {
      "decorators": [
        "evaluation(key='throughput.missed', label='Demand missed, per arm', scope='config', needs=('missed',), family='throughput', shape='ranked', quantities=tuple((k for k, _f in _PANELS)))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py": {
      "decorators": [],
      "doc": "throughput.volume \u2014 cumulative pick volume against elapsed time, in two views.\n\nWHY A CURVE AND NOT A BAR.  Throughput as a single number hides *when* the work\nhappened.  Plotting running items against running elapsed hours makes the rate itself\nthe visible thing: the SLOPE of the curve IS throughput, so a steeper line that\nterminates further left finished the same demand sooner.  That is the whole \"better\nthroughput at equal labor\" story in one picture \u2014 the arms do not do less work, they do\nit in less elapsed time.\n\nTHE TIME AXIS IS SYNTHESIZED, AND HAS TO BE.  There is no global clock in the data:\nevery batch's sim clock restarts at zero, so elapsed time is the running sum of batch\nmakespans \u2014 exactly the quantity run_whatif_labor calls `batch_hours`, converted with\nthe same `chartkit.to_hours`, so numbers reconcile across the suite.  These are modeled\npick-time hours, not wall-clock; the axis label carries the qualifier.  HOURS IS FIXED\non this x-axis, not chosen from the data like the duration axes elsewhere in the suite:\nelapsed run time is the one quantity the whole comparison is denominated in \u2014 the\nwhat-if layer, the finish annotations and the site all say \"hours\" \u2014 and a cumulative\naxis that renamed its unit per run would stop reconciling with them.\n\nTHE PRIMARY VIEW IS THE PERCENT LEAD.  The cumulative curves are near-identical straight\nlines (measured shape index ~1.0007 on a real 100-batch arm), so overlaying them shows\nalmost nothing, and the retired companion panel plotted the lead in absolute items \u2014\n\u00b1150k reads as huge while being a few percent.  The percent view interpolates each arm\nand FIFO onto a shared hour grid spanning only the hours BOTH actually cover (never\nextrapolating past the shorter run) and plots the lead as a % of FIFO's cumulative at\nthat same moment \u2014 FIFO is the zero line, and each arm's finish dot is annotated with\nits finish hour, because the finish point is where arms genuinely differ.  The absolute\nview keeps the raw curves as the honest-units companion, decluttered: names in the\nlegend, the per-arm rate/finish numbers in one compact subtitle block, and x-limits\nidentical across both files so the views superimpose mentally.\n\nParams: top_n (int), top_by ('global' | 'initial' | 'assignment' | 'reslot').",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::_HOURS_LABEL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'elapsed pick-time hours (modeled, not wall-clock)'"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::_absolute": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, curves, base_curve, xlim, top_n, top_by, out)"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::_percent": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, curves, base_curve, xlim, top_n, top_by, out)"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::_sub_note": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(top_n, top_by)"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::cumulative_curve": {
      "decorators": [],
      "doc": "(hours, items) running totals over batches, ordered by batch_id.\n\nhours[i] is elapsed time at the END of batch i, so the pair is the point \"this much\nwork had been finished by this much elapsed time\".",
      "is_async": false,
      "kind": "function",
      "signature": "(df)"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::lead_pct_curve": {
      "decorators": [],
      "doc": "(grid_hours, lead_pct) \u2014 this arm's lead over FIFO at matched elapsed time, as a\n% of FIFO's cumulative at that time.\n\nBoth curves are interpolated onto a shared grid spanning only the hours BOTH\nactually cover, so the comparison never extrapolates past the shorter run.  Grid\npoints where FIFO has finished nothing yet are dropped (a ratio needs a base).",
      "is_async": false,
      "kind": "function",
      "signature": "(hrs, items, base_hrs, base_items, npts=200)"
    },
    "Optimization/Performance_Evaluations/throughput/volume.py::render": {
      "decorators": [
        "evaluation(key='throughput.volume', label='Cumulative pick volume vs elapsed time + % lead over FIFO', scope='config', needs=('batch', 'series'), defaults={'top_n': 3, 'top_by': 'initial'}, family='throughput', shape='curve', quantities=('pick_volume',))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/trajectories/__init__.py": {
      "decorators": [],
      "doc": "trajectories.* \u2014 the over-time figure family: each shared metric's absolute and\npercent-vs-baseline trajectory, rendered through the one painter in common/painters so\nthe config stage and the cross-profile aggregate stage stay visually identical.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/trajectories/overtime.py": {
      "decorators": [],
      "doc": "trajectories.overtime \u2014 every over-time metric in both measurement stances.\n\nDeliberately a THIN driver: the drawing lives in `common/painters.paint_overtime`, which\nis also what the aggregate stage renders with \u2014 that shared body is what keeps the\nper-config and cross-profile trajectory charts visually identical, so nothing here may\nstyle, size, or name anything.  This module's whole job is to walk the shared metric\nvocabulary (`painters.overtime_metrics`) and request each metric in each declared view;\nthe painter owns units, the baseline treatment, filenames, and saving.\n\nThe ctx access idiom is the retired overlay module's, verbatim: the strategy list and the\nbaseline come off the context facade (`ctx.strategies` / `ctx.base` \u2014 focus-filtered once\nat context build, baseline = first strategy), and the series dict via `ctx.series()`.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/trajectories/overtime.py::SERIES_QUANTITIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "SERIES_ORDER"
    },
    "Optimization/Performance_Evaluations/trajectories/overtime.py::render": {
      "decorators": [
        "evaluation(key='trajectories.overtime', label='Over-time trajectories, absolute + % vs baseline', scope='config', needs=('series',), family='trajectories', shape='serial', quantities=SERIES_QUANTITIES)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/__init__.py": {
      "decorators": [],
      "doc": "yard \u2014 did the yard bind, and what did the inbound policy cost in trailer-days.\n\nThe tenth chart family, and inbound's FIRST figure coverage of any kind: the receiving\nsurface (`batch_stats.recv_*`) has been carried into frames and one diagnostics report\nsince it landed and is declared by zero quantities.\n\n## What this family answers, and what it deliberately does not\n\nThe campaign behind it asks one two-part question \u2014 does space-aware inbound beat FIFO,\nand at what fee cost \u2014 so this folder holds the second half. The first half is hours, and\nhours live in `labor`/`headline` where they always have. **The two never meet in a\nnumber.** Labour seconds and detention days are different instruments; the only place they\ncompose in this whole effort is the `gain_gated` arm's urgency GATE, which is a yes/no\ntest inside the simulation and not a report. A single score blending them would be exactly\nthe arithmetic the fee proxy was designed to avoid.\n\n## The fee is a PROXY, and the stamps are raw\n\nNothing in the database is a fee. `yard_trailers` holds arrival, staging and emptying\nstamps; the detention span, the overage past `INBOUND_FEE_THRESHOLD_DAYS` and the door\nutilization are all derived here, at read time, against the threshold the RUN recorded.\nThat is what makes a finished run re-reportable under a different threshold without\nre-simulating \u2014 and it is why the calibration probe (\"where does the threshold sit for a\nnonzero, non-saturated overage?\") is an analysis pass over one FIFO run rather than a\nsweep. Overage days are never converted to money.\n\n## An empty folder here is a result\n\nA run without `INBOUND_STANDING_YARD` has no yard \u2014 not an empty one. The `yard` request\nis DENIED when no arm carries a trailer row, so these evaluations render nothing and the\ndriver logs why. Zeroed bars would be a claim about a model that did not run.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/yard/binding.py": {
      "decorators": [],
      "doc": "yard.binding \u2014 did the yard actually bind, and how often did a drain leave work behind.\n\nThe prior question of the whole effort. A policy that reorders trailers can only matter\nwhere there is contention: with doors free and nothing standing, every ordering rule picks\nthe same trailers in the same drain and the arms are byte-identical by construction. So\nthis family's first figure is not a comparison at all \u2014 it is the evidence that the\ncomparison is licensed.\n\nTwo marks, one question:\n\n  `yard_depth`    trailers standing at each drain's ctx-freeze, over the run. Read BEFORE\n                  the door fill, because \"did the yard bind\" is a question about the moment\n                  of choice and after the fill there is nothing left to choose.\n  `binding_cuts`  how many drains ENDED with a trailer unreached or units still on a staged\n                  one. A COUNT OF DRAINS, never a sum of the levels: summing them would\n                  re-count the same standing trailer once per batch it waits, which is the\n                  mistake `recv_cut` made at 101x on a published headline.\n\nTHE PILOT GATE READS THIS FIGURE'S ABSOLUTE VALUE UNDER `fifo`, not its ranking. A config\nthat shows neither contention nor binding cuts is a declared stop for the campaign \u2014 the\narms would be measuring nothing \u2014 so the absolute panel is the load-bearing one here and\nthe comparison views are secondary.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/yard/binding.py::CUTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'binding_cuts'"
    },
    "Optimization/Performance_Evaluations/yard/binding.py::DEPTH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'yard_depth'"
    },
    "Optimization/Performance_Evaluations/yard/binding.py::_contention_note": {
      "decorators": [],
      "doc": "One sentence on whether the yard bound at all \u2014 the pilot gate's own reading.\n\nDeliberately computed over every arm rather than the baseline alone: an arm-specific\nabsence would be a policy result, and a universal one is a configuration that cannot\nanswer the question at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(frames) -> str"
    },
    "Optimization/Performance_Evaluations/yard/binding.py::_cut_entries": {
      "decorators": [],
      "doc": "[(strategy, drains that left inbound work)] \u2014 a COUNT of drains, not of trailers.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames)"
    },
    "Optimization/Performance_Evaluations/yard/binding.py::_series": {
      "decorators": [],
      "doc": "{key: {'batch': [...], 'yard_start': [...]}} \u2014 the shape `marks.serial` takes.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames)"
    },
    "Optimization/Performance_Evaluations/yard/binding.py::render": {
      "decorators": [
        "evaluation(key='yard.binding', label='Yard depth over drains, and binding cuts per arm', scope='config', needs=('yard', 'batch'), family='yard', shape=('serial', 'ranked'), quantities=(DEPTH, CUTS))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/binding.py::render_site": {
      "decorators": [
        "evaluation(key='yard.site_binding', label='Site yard binding (coupled)', scope='site', needs=('yard', 'batch'), family='yard', shape=('serial', 'ranked'), quantities=(DEPTH, CUTS))"
      ],
      "doc": "Did the yard bind \u2014 the SITE's yard, for a coupled run.\n\nThe render body is `render`, unchanged: a `SiteContext` populates the same\n`_by_key` shape an `EvalContext` does, so every frame broker works over it\nuntouched and site-ness lives in what the keys point AT (\"Re-scope the analysis\nsurfaces to the site\", section 1).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/detention.py": {
      "decorators": [],
      "doc": "yard.detention \u2014 how long the site held each trailer, and how that time is spread.\n\nDETENTION IS arrived -> emptied: the whole time the carrier's trailer is on site,\nINCLUDING its hours at a door. That definition is what makes it a fair substrate for a\nfee \u2014 it is monotone under any ordering, so staging a trailer early and then unloading it\nslowly sheds nothing, and no policy can win the fee axis by shuffling where the waiting\nhappens rather than shortening it.\n\n## Why a distribution, not just a mean\n\nThe adversarial control arm (`lifo`) does not RAISE mean detention much \u2014 it serves the\nsame trailers with the same crew \u2014 it CONCENTRATES it, holding a few trailers a very long\ntime while the rest move normally. A mean beside a mean would show that arm as roughly\ntied and the fee total as mysteriously worse. The box per arm is where the mechanism\nbecomes visible, and the threshold is drawn on it as a fixed mark so a reader can see the\ntail crossing the line rather than infer it from a number elsewhere.\n\nThe mean is still reported, because it is the quantity the ranked comparison is defined\non and the one a percent view can be taken of. The two figures answer different halves.\n\n## Censored rows are IN, and marked\n\nA trailer still standing when the run ends contributes its lower bound. Excluding it would\nremove exactly the longest-held trailers \u2014 the ones the fee is about \u2014 and understate the\narm that produced them. Each arm's censored share is annotated so the bound is legible.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/yard/detention.py::QUANTITY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'yard_detention_days'"
    },
    "Optimization/Performance_Evaluations/yard/detention.py::_distribution": {
      "decorators": [],
      "doc": "The box-per-arm figure: THE view of concentration, and a bespoke mark.\n\n`marks.ranked` draws one value per arm; this draws a whole sample per arm, which is a\ndifferent mark and is why it is in the bespoke ledger rather than routed through the\nlibrary. Everything else is the house style \u2014 the same category axis, the same\nbaseline shading, the quantity's own converter for the axis label.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames, threshold, out)"
    },
    "Optimization/Performance_Evaluations/yard/detention.py::_entries": {
      "decorators": [],
      "doc": "[(strategy, mean detention in days)] over arms with at least one trailer.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames)"
    },
    "Optimization/Performance_Evaluations/yard/detention.py::render": {
      "decorators": [
        "evaluation(key='yard.detention', label='Detention per trailer, mean and distribution', scope='config', needs=('yard', 'batch'), family='yard', shape='ranked', quantities=(QUANTITY,))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/detention.py::render_site": {
      "decorators": [
        "evaluation(key='yard.site_detention', label='Site trailer detention (coupled)', scope='site', needs=('yard', 'batch'), family='yard', shape='ranked', quantities=('yard_detention_days',))"
      ],
      "doc": "Trailer detention \u2014 the SITE's yard, for a coupled run.\n\nThe render body is `render`, unchanged: a `SiteContext` populates the same\n`_by_key` shape an `EvalContext` does, so every frame broker works over it\nuntouched and site-ness lives in what the keys point AT (\"Re-scope the analysis\nsurfaces to the site\", section 1).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/fee.py": {
      "decorators": [],
      "doc": "yard.fee \u2014 what the inbound ordering cost the carrier, per arm.\n\nTwo quantities, one ranked mark, and they are deliberately reported side by side:\n\n  `yard_overage_days`            \u03a3 max(0, detention \u2212 threshold) over every trailer. A\n                                 carrier charges per trailer per day held, so this is the\n                                 quantity that is actually billed, and it SUMS.\n  `yard_over_threshold_trailers` how many trailers accrued any overage at all.\n\nOne trailer held a fortnight and a fortnight of trailers each held a day are the same\nnumber of trailer-days and are not the same operational problem \u2014 the first is one carrier\nrelationship in trouble, the second is a systemic dwell. The total alone cannot tell them\napart, so the count rides beside it rather than being derivable-in-principle from a chart\nnobody will do the arithmetic on.\n\nCENSORED TRAILERS ARE IN BOTH. A trailer still standing when the run stops has been held\nat least until the run ended, and under an adversarial ordering (`lifo`) that is exactly\nwhere the concentrated overage sits. Dropping those rows would report `lifo`'s fee as\nclipped rather than concentrated, which inverts the signal the arm exists to produce; the\nsubtitle says how many rows are bounded rather than observed.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/yard/fee.py::_PANELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('yard_overage_days', lambda df: float(df['overage_days'].sum())), ('yard_over_threshold_trailers', lambda df: float(df['over_threshold'].sum())))"
    },
    "Optimization/Performance_Evaluations/yard/fee.py::_TITLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'absolute': ('Yard overage per arm', 'trailer-days held past the free threshold, censored trailers included'), 'percent': ('Yard overage vs baseline', 'improvement against the baseline arm; an arm..."
    },
    "Optimization/Performance_Evaluations/yard/fee.py::_censored_note": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(frames) -> str"
    },
    "Optimization/Performance_Evaluations/yard/fee.py::_entries": {
      "decorators": [],
      "doc": "[(strategy, per-arm scalar)] over the arms that have any trailer at all.\n\nNo `paired` companion, and that is a decision rather than an omission: the fee's\ninstances are TRAILERS, and trailer `seq` n of one arm is the same physical trailer as\n`seq` n of another (leads are seq-keyed draws, common random numbers across arms) \u2014 but\nonly when both arms ran the same catalogue to the same depth. Pairing on that\nassumption without checking it would put a bootstrap interval on a comparison that may\nbe lining up different shipments. A ranked mark without `paired` draws points with no\ninterval, which is honest about what it does not know.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, frames, fold)"
    },
    "Optimization/Performance_Evaluations/yard/fee.py::render": {
      "decorators": [
        "evaluation(key='yard.fee', label='Yard overage and over-threshold trailers per arm', scope='config', needs=('yard', 'batch'), family='yard', shape='ranked', quantities=tuple((k for k, _f in _PANELS)))"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/fee.py::render_site": {
      "decorators": [
        "evaluation(key='yard.site_fee', label='Site detention fee exposure (coupled)', scope='site', needs=('yard', 'batch'), family='yard', shape='ranked', quantities=tuple((k for k, _f in _PANELS)))"
      ],
      "doc": "Detention fee exposure \u2014 the SITE's yard, for a coupled run.\n\nThe render body is `render`, unchanged: a `SiteContext` populates the same\n`_by_key` shape an `EvalContext` does, so every frame broker works over it\nuntouched and site-ness lives in what the keys point AT (\"Re-scope the analysis\nsurfaces to the site\", section 1).",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py": {
      "decorators": [],
      "doc": "yard.scorecard \u2014 the read-outs that have no honest direction, in one table.\n\nThree numbers the yard produces that a reader needs and that must NOT be ranked:\n\n  DOOR UTILIZATION       \u03a3 door spans \u00f7 (doors \u00d7 the arm's span). Neither high nor low is\n                         good. Low says the doors were never the constraint; high says they\n                         were saturated and adding one would change the answer. Both are\n                         findings about the CONFIGURATION, not scores for the policy.\n  THE CONTENTION PAIR    trailers standing and doors free at each drain's freeze. The\n                         licence for the whole comparison \u2014 with doors free and nothing\n                         standing, every ordering rule picks the same trailers and the arms\n                         are byte-identical by construction.\n  THE CENSORED SHARE     what fraction of trailer rows are bounded rather than observed. A\n                         provenance fact about the other figures, not a result.\n\n`DIRECTIONS` admits only 'lower' and 'higher', so declaring any of these as a Quantity\nwould force a direction none of them has \u2014 and a false direction is worse than no\ndeclaration, because every derived view downstream would then assert it. They live here\ninstead, in an INSPECTION mark, which is the family grammar's own slot for a read-out with\nno baseline in it. The column semantics they are built from are declared where the reads\nactually happen, in `common/frames.py`.\n\n## The door count is DERIVED, and exactly\n\nNothing records `INBOUND_DOCK_DOORS` where the analysis can see it (that is the run-shape\nlayer's job). But `free_doors_start = doors \u2212 staged`, and the first drain of a run always\nfreezes with nothing staged, so `max(free_doors_start)` IS the door count whenever the\nrun's first drain is in the frame. On a resumed arm whose early drains are missing it\nbecomes a lower bound, which makes utilization an UPPER bound \u2014 stated in the table rather\nthan left for a reader to assume away.",
      "kind": "module",
      "signature": null
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('arm', 'trailers', 'censored', 'doors', 'door util', 'receiver busy', 'dock ceiling', 'yard depth (mean/max)', 'free doors (mean)', 'drains bound')"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_SITE_AT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_COLS.index('receiver busy') + 1"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_SITE_COL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'per channel'"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_arm_span_days": {
      "decorators": [],
      "doc": "The arm's own wall of sim time, in days \u2014 utilization's denominator.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key) -> float"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_busy_share": {
      "decorators": [],
      "doc": "One frame's receiver-busy share against the SITE crew and the SITE's work days.\n\nFactored out of `_receiver_busy` so the site number and the per-channel shares are\ncomputed by ONE function against ONE denominator.  That is the whole point of printing\nthem together: a share is of a whole the channel does not own (ADR-0005 says so in as\nmany words), so it is only readable beside the total it is a share OF.",
      "is_async": false,
      "kind": "function",
      "signature": "(bdf, crew: int, S: float, n_days: int) -> str"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_cols": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(site: bool) -> tuple"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_receiver_busy": {
      "decorators": [],
      "doc": "The crew's BUSY SHARE: receiver-seconds charged \u00f7 (crew \u00d7 day_seconds \u00d7 WORK DAYS).\n\nThe door-team cap's own read-out (\"Decide the contention regime under the derived crew\",\n4), and deliberately beside door utilization rather than in place of it: doors can be\nsaturated while the crew idles (a capped dock cannot seat everyone) and the crew can be\nsaturated with doors to spare. Neither direction is better, which is why this lives in\nthe inspection table and is not a Quantity.\n\nTHE DENOMINATOR IS WORK DAYS, not the arm's calendar span, and the distinction is not\npedantic: the sim clock only advances through working hours, so an arm of 40 work days on\nan 8-hour day spans 13.3 CALENDAR days. Dividing a crew's seconds by the calendar span\nover-reads the share by exactly the ratio of the two \u2014 it reported 184% busy on the first\nreal run this was rendered against, which is how it was caught. `_arm_span_days` is the\nright denominator for DOOR utilization (door spans are calendar time) and the wrong one\nhere, which is why the two read-outs beside each other do not share one.\n\nCounted as DISTINCT `work_day` values rather than batches, so it stays right if a day\never releases more than one batch, and it is the same count\n`equilibrium._utilization_clause` grants against \u2014 the two reports cannot mean different\nthings by \"busy\". '-' when the run derived no crew (flag-off) or recorded no receiving\nseconds; never 0%, which is a different and stronger claim.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key) -> str"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_receiver_split": {
      "decorators": [],
      "doc": "The per-channel receiver shares, beside the site number and never instead of it.\n\n`a-right-site-total-hides-two-wrong-shares` is exact about why this column exists:\nput-away's site load was 0.9% off while both per-channel bands failed in OPPOSITE\ndirections.  A site total is not evidence for the split, and the split is the only\nplace the crews' fairness is visible.\n\nCalled only from the SITE registration \u2014 an uncoupled leaf's yard IS that channel's, so\nthe column does not exist there at all rather than printing 100%.  Both shares use the\nSITE crew and the SITE's work-day count: they are shares of one whole, so they sum to the\nsite number and are not utilizations in their own right.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, key) -> str"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::_row": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, s, tdf, ddf, site: bool=False)"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::render": {
      "decorators": [
        "evaluation(key='yard.scorecard', label='Yard read-outs with no direction', scope='config', needs=('yard', 'batch'), family='yard', shape='inspection')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params, site: bool=False)"
    },
    "Optimization/Performance_Evaluations/yard/scorecard.py::render_site": {
      "decorators": [
        "evaluation(key='yard.site_scorecard', label='Site yard read-outs (coupled)', scope='site', needs=('yard', 'batch'), family='yard', shape='inspection')"
      ],
      "doc": "Yard read-outs with no direction \u2014 the SITE's yard, for a coupled run.\n\nThe render body is `render`, unchanged: a `SiteContext` populates the same\n`_by_key` shape an `EvalContext` does, so every frame broker works over it\nuntouched and site-ness lives in what the keys point AT (\"Re-scope the analysis\nsurfaces to the site\", section 1).\n\nThe ONE thing the site table adds is the per-channel SHARE column, which exists only\nhere: a right site total hides two wrong shares, and an uncoupled leaf has no share to\nreport because its yard is its own.",
      "is_async": false,
      "kind": "function",
      "signature": "(ctx, params)"
    },
    "Optimization/__init__.py": {
      "decorators": [],
      "doc": "Optimization \u2014 the run harness as a package.\n\nSimulation runner (run_simulation / strategy_runner), analysis\n(run_analysis + Performance_Evaluations), channel rollup, and the SQLite\npersistence layers.  Import style is package-absolute\n(``from Optimization import channels``); the repo root must be on sys.path \u2014\nentry scripts self-bootstrap, pytest gets it from Tests/conftest.py.\n\nDeliberately side-effect-free: no imports here (Performance_Evaluations keeps\nits own deliberate registry side effects in ITS __init__, fired only when the\nanalysis package itself is imported).",
      "kind": "module",
      "signature": null
    },
    "Optimization/analyze_run.py": {
      "decorators": [],
      "doc": "analyze_run.py \u2014 one analysis hub for a whole run (every run is a cell matrix).\n\n`run_simulation` calls this in-process after the sim (unless ``--no-analyze``), so ONE command\nproduces the simulation AND every graph.  It can also be run standalone to (re)analyze a finished\nrun without re-simulating:\n\n    python -m Optimization.analyze_run <base_dir> [--reference k1_off_rr] [--workers 8]\n\nFor each cell under ``base_dir`` it runs the per-cell registry suite (`run_analysis.run_analysis`)\nand the whole-warehouse rollup (`run_channel_rollup.rollup`); when the run has >1 cell it also runs\nthe cross-cell what-if steps (`run_whatif_delta.run`, `run_whatif_labor.run`,\n`run_whatif_volume.run`) at the run root, and finally the RUN DOSSIER \u2014 the registry's\nrun-scope evaluations, over the whole tree.  The dossier goes last because it reads what\nevery earlier stage produced (the runtime DB, the what-if rows, the per-leaf tables): the\nordering is a data dependency, not a preference.  Each\nstep is isolated in try/except so one failure never sinks the rest \u2014 the sim's DBs are already safe\non disk, so analysis is best-effort.",
      "kind": "module",
      "signature": null
    },
    "Optimization/analyze_run.py::_run_root_stage": {
      "decorators": [],
      "doc": "Render the run-scope evaluations into the run root's dossier.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, log, preset_name)"
    },
    "Optimization/analyze_run.py::_step": {
      "decorators": [],
      "doc": "Run one analysis step, isolating failures (DBs are already persisted).",
      "is_async": false,
      "kind": "function",
      "signature": "(log, what, fn)"
    },
    "Optimization/analyze_run.py::analyze_run": {
      "decorators": [],
      "doc": "Analyze every cell of a run, then (for a multi-cell run) the cross-cell what-if summaries.\n\ncells: explicit [cell_name, \u2026] from the driver (avoids re-scanning); else discovered from disk.\nreference: the round-robin/baseline cell for the cross-cell delta (from the run's spec).\ngranularity: 'config' emits one job per channel-run (a handful per cell, so a large\n    --workers is mostly idle); 'graph' emits one job per (channel-run, evaluation),\n    which is what actually saturates a big pool on a re-analysis.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, log, *, cells=None, workers=1, preset='BY_INITIAL', reference=None, cross_cell=True, granularity='config')"
    },
    "Optimization/analyze_run.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None)"
    },
    "Optimization/config/__init__.py": {
      "decorators": [],
      "doc": "Optimization.config \u2014 the run's tunable inputs, in one place.\n\nCONFIG (sim_config) is the single source of truth for every knob; `channels` describes the two\nindependent warehouse sections; `strategies` is the assignment-function registry; `whatif_config`\nholds the cell-matrix specs.  See README.md for what does and does not belong here.\n\nsim_config and channels import each other and must stay co-located.",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/channels.py": {
      "decorators": [],
      "doc": "channels.py \u2014 the Channel abstraction: one operation over a shared warehouse.\n\nA *Channel* bundles the three things that differ between the store and fulfillment\noperations while the pick MECHANICS stay shared:\n\n  * regime      \u2014 which SKU family / bins it uses (BinKey routing; see regime.py)\n  * batch stream \u2014 its own order-generation rate/size (a BatchConfig, built per run once\n                   the channel's SKU-subset size is known) + an independent seed offset\n  * picker pool \u2014 its own worker count + cost regression (a PickerProfile wrapping a\n                   PickConfig)\n\nThe channel list is the single source of truth the runner uses to (a) generate a batch\nstream per channel, (b) simulate each channel with its own picker pool + cost, and (c)\nderive ``WP_BY_REGIME`` for the per-regime cost routing in placement/labor.  Adding a new\noperation later = adding one Channel.\n\nBackward-compatible: a run with a single ``store`` channel reproduces today's behavior\nexactly (one batch stream, one picker pool, one cost, one DB subtree).",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/channels.py::Channel": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One operation (store | fulfillment | \u2026) over the shared warehouse.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/config/channels.py::Channel.batch_config": {
      "decorators": [],
      "doc": "Build this channel's BatchConfig for its SKU-subset size.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, inventory_size: int) -> BatchConfig"
    },
    "Optimization/config/channels.py::FF_BATCH_SEED_OFFSET": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1000000"
    },
    "Optimization/config/channels.py::PickerProfile": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "A pool of one kind of picker: a name, its pick-time cost regression (a PickConfig\ncarrying coefficients + travel speeds), how many work concurrently, and what KIND of\nactor they are.\n\n`mode` is not a new idea here -- the two pools have been called `store_machine` and\n`fulfillment_walker` for as long as they have existed, and `simconfig/constants.py`\ncomments them as \"machine order-picker pool\" and \"human-walker pool\".  Nothing read\neither name.  The distinction was real, written down in prose, and inert; now it is a\nvalue, and `crew()` turns this pool into the domain's own actor objects.\n\nBoth fields default to today's values (a picking crew on foot), so a construction that\ndoes not care -- there are several in tests and Diagnostics -- is unchanged.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/config/channels.py::PickerProfile.crew": {
      "decorators": [],
      "doc": "This pool as the domain's `Crew`, with the speeds its PickConfig carries.\n\nThe bridge from harness configuration to the actor model: `Warehouse/operations/`\nmay not import `Optimization/`, so the conversion belongs on this side.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> Crew"
    },
    "Optimization/config/channels.py::build_channels": {
      "decorators": [],
      "doc": "Assemble the run's channel list.\n\nThe STORE channel's cost is the run's own (swept) PickConfig, so store results are\nunchanged.  The FULFILLMENT channel is appended only when the inventory has fulfillment\nitems (``include_fulfillment``); it uses the walker cost + its own picker pool.\n\n``store_restocks`` restricts the store channel to a subset of restock rules (None \u21d2 full\nsuite); fulfillment always runs the full suite.",
      "is_async": false,
      "kind": "function",
      "signature": "(store_pick_cfg: PickConfig, store_num_pickers: int, *, include_fulfillment: bool, ff_pick_cfg: PickConfig | None=None, ff_num_pickers: int=_FF_PICKERS, store_restocks: tuple[str, ...] | None=None) -> list[Channel]"
    },
    "Optimization/config/channels.py::fulfillment_pick_config": {
      "decorators": [],
      "doc": "The fulfillment channel's default walker pick-time regression.\n\nA thin reader of ``sim_config.FULFILLMENT_CONFIGS[0]`` \u2014 the SINGLE source of\ntruth for the walker numbers.  (This function used to carry its own copy, which\nsilently diverged once the sweep entries were tuned; folding it onto CONFIG ended\nthe two-sources-of-truth hazard.)  num_pickers stays 1 here \u2014 the PickerProfile\npool size overrides it.  Imported lazily to avoid the sim_config \u2192 channels\nimport cycle (FF_BATCH_SEED_OFFSET lives in this module).",
      "is_async": false,
      "kind": "function",
      "signature": "() -> PickConfig"
    },
    "Optimization/config/channels.py::make_channel": {
      "decorators": [],
      "doc": "Build a single Channel from a picker cost + pool size.\n\nThe one-channel primitive the runner uses to sweep each section's configs\nindependently; build_channels() composes the standard store+fulfillment pair on\ntop of it.  ``batch_seed_offset`` gives a channel an INDEPENDENT batch stream\n(store uses 0; fulfillment a large offset so its stream never overlaps store's).\n``batch_mean_fraction``/``batch_std_fraction`` set the channel's batch-stream shape\n(each channel may differ; the runner sources these from CONFIG).\n\n``mode`` is the pick crew's travel MODE and the runner must pass it.  This is the ONLY\nchannel builder the run path uses -- `build_channels` below is reached from tests only\n-- so a default here is what every real run gets.  It defaulted silently to `foot`\nwhile the store pool ran at the machine speed (3, 2), which put `mode='foot'` on every\nstore `work_events` row beside a machine-speed duration.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, regime: str, pick_cfg: PickConfig, num_pickers: int, *, restocks: tuple[str, ...] | None=None, batch_seed_offset: int=0, batch_mean_fraction: float=0.2, batch_std_fraction: float=0.05, sampler: str='v1', mode: str | Mode=Mode.FOOT) -> Channel"
    },
    "Optimization/config/channels.py::wp_by_regime": {
      "decorators": [],
      "doc": "{regime: WorkloadParams} derived from each channel's picker cost \u2014 the map the\nplacement/labor cost routing keys on (attached to the primary WorkloadParams as\n``.by_regime`` so it rides through the assignment builders without signature churn).",
      "is_async": false,
      "kind": "function",
      "signature": "(channels: list[Channel]) -> dict"
    },
    "Optimization/config/envfile.py": {
      "decorators": [],
      "doc": "envfile.py \u2014 the ONE `.env` reader, and the one path-value cleaner.\n\n`load_env` and `clean_path` existed four times, byte-for-byte equivalent, in\n`Optimization/config/sim_config.py`, `Warehouse/generation/generate_mixed_profile.py`,\n`Warehouse/generation/generate_profile_suite.py` and `docs/experiments/ingest.py`. Every copy\nhad the same skip rules, the same `r\"...\"` stripping and the same shell-wins precedence, so\nthey were one function stored four times \u2014 the state `Warehouse/operations/putaway.py` records\nthe cost of: \"this project has already paid for two default sets that drifted 55x apart.\"\n\nWHY IT LIVES HERE, in `Optimization/config/`, and not somewhere more neutral:\n\n* `Warehouse/kernel/` is declared \"zero-dependency VALUE OBJECTS \u2014 the primitives everything\n  else in the DOMAIN is built from\". An `.env` reader is harness plumbing and would be the\n  first non-domain thing in it.\n* A module at the repo root would be STRUCTURALLY INVISIBLE. `context/arch/extract.py`'s\n  `GRAPH_ROOTS` are all directories, so a bare top-level `.py` is never walked: no `files.yml`\n  entry, no layer, and nothing to notice \u2014 the same class of hole this package has been\n  fixing elsewhere.\n* Reading `.env` IS configuration, and `generation -> opt_config` and `docs -> opt_config` are\n  both permitted by `context/architecture.yml` (only the reverse directions are forbidden).\n\nTHE IMPORT MUST STAY FREE. `docs/experiments/ingest.py` loads `.env` BEFORE importing anything\nthat reads it, and `Tests/architecture/test_ingest_env_bootstrap.py` pins that ordering. So\nthis module imports `os` and nothing else, and both package `__init__` files above it are\ndocstring-only (`Optimization/__init__.py` says so in as many words: \"Deliberately\nside-effect-free: no imports here\"). Adding an import here can break a bootstrap two packages\naway without failing anything local \u2014 do not.",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/envfile.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('load_env', 'clean_path')"
    },
    "Optimization/config/envfile.py::clean_path": {
      "decorators": [],
      "doc": "Strip `r\"...\"` / `r'...'` notation or plain quotes from an env-var path value.\n\nApplied after `os.getenv` as well as during parsing, so a value set directly in a Windows\nsession environment (with literal `r\"...\"` text, which the shell does not interpret) is\nnormalised exactly like one read from the file.",
      "is_async": false,
      "kind": "function",
      "signature": "(val: str) -> str"
    },
    "Optimization/config/envfile.py::load_env": {
      "decorators": [],
      "doc": "Inject `KEY=VALUE` pairs from *path* into `os.environ`.\n\nShell-set variables are never overwritten: a value already in the environment wins, which\nis what lets a one-off `COMPARISON_OUTPUT_DIR=... python -m ...` override the file without\nediting it. A missing file is not an error \u2014 every caller treats `.env` as optional.\n\nBlank lines, `#` comments and lines with no `=` are skipped. Values go through\n`clean_path`, so `r\"D:\\runs\"` and `\"D:\\runs\"` and `D:\\runs` all arrive the same.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> None"
    },
    "Optimization/config/objectives.py": {
      "decorators": [],
      "doc": "objectives.py \u2014 what each restock rule OPTIMISES, in one machine-readable place.\n\nWHY THIS EXISTS.  The objective of a placement rule used to live in three places that\ndrifted independently: the Python closure that implements it, a hand-written LaTeX table\nin `docs/macros.py::assignment_formulas` (top-3 only), and a second hand-written copy in\neach experiment's `formula-reference.md` (all 17).  The macro's own docstring named the\nfix \u2014 \"expose each builder's objective \u2026 so this macro can read it like the others\" \u2014\nand this module is it.  The catalog is emitted INTO each run, so the objective published\nbeside a result is the objective that ran, versioned with it.\n\nWHAT THIS CANNOT DO, stated plainly: the LaTeX is not derived from the Python.  No\ninspection recovers an objective from a closure.  What the registry buys is that the\ntranscription exists exactly ONCE and is machine-anchored \u2014 `Tests/architecture/\ntest_rule_catalog.py` asserts every rule has an entry, every named symbol exists, and\nevery builder actually calls the symbol its entry claims.  A rename or a re-pointed\nbuilder fails a test instead of silently making a published formula a lie.\n\nKEYED BY RESTOCK KEY, not by arm.  `Strategy.restock` already carries it, so the tie is\n`OBJECTIVES[s.restock]` and `Optimization/config/strategies.py` needs no edit \u2014 which\nalso keeps LaTeX out of the strategy grid's import path and keeps that file (a\nrun-contract SHAPE_SOURCE) out of unrelated diffs.\n\nThe shared machinery each objective is written against \u2014 the placement primitive\n`ell(b) = M(y_b)(t0 + h) + D_b`, the pick-effort priority that orders a ranked wave, and\nthe tie-breaking / full-aisle rules \u2014 is documented once on the experiment site's\nformula reference; the entries below state only what distinguishes them.",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/objectives.py::FAMILIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'baseline': 'Baseline', 'ranked': 'Ranked (effort / labor)', 'map': 'Map (optimal-map score matching)', 'cluster_map': 'Cluster-map (map + cohesion)', 'travel': 'Travel bracket', 'affinity': 'Affi..."
    },
    "Optimization/config/objectives.py::Objective": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One restock rule's identity: what it is called, what code it is, what it optimises.\n\nrule       restock key, matching `Strategy.restock` and `_RESTOCKS[i][0]`\nlabel      display label, matching `_RESTOCKS[i][1]`\nfamily     which bracket it belongs to (see `FAMILIES` below)\nsense      'min' | 'max' | 'none' \u2014 the direction of its argument\nstage      'per_unit' | 'ranked_wave' | 'precomputed_map' \u2014 WHEN it decides\nsymbol     the placement function the builder actually calls\nmodule     repo-relative file that DEFINES `symbol`\nbuilder    the `_build_*` in the strategy grid that wires it up\nprecompute an offline build step run once per arm before the sim, or '' for none.\n           This is the field a WMS reader needs: a rule with a precompute has a job\n           to schedule; a rule without one is pure per-arrival scoring.\nlatex      the objective, one expression, `$\u2026$`-delimited\nnotes      the mechanism prose that belongs with the formula rather than the page\ncontrol    True for the deliberate worst-case bracket controls (designed to lose)",
      "kind": "class",
      "signature": ""
    },
    "Optimization/config/objectives.py::Objective.anchor": {
      "decorators": [
        "property"
      ],
      "doc": "The heading anchor the published rule section uses.\n\nLoad-bearing: `full-results.md` and `glossary.md` link to these fragments and\n`context/guards/docref_guard.py` checks them, so the generated section headings\nmust keep reproducing exactly this string.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/config/objectives.py::_AF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'Warehouse/placement/Assignment_Functions.py'"
    },
    "Optimization/config/objectives.py::_ENTRIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[Objective(rule='fifo', label='FIFO', family='baseline', sense='none', stage='per_unit', symbol='_uniform_assignment', module='Warehouse/inventory/inventory_common.py', builder=f'_build_uniform@{_S..."
    },
    "Optimization/config/objectives.py::_MAP_BUILD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'build_optimal_map@Warehouse/inventory/inventory_optimal.py'"
    },
    "Optimization/config/objectives.py::_STRAT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'Optimization/config/strategies.py'"
    },
    "Optimization/config/objectives.py::as_dicts": {
      "decorators": [],
      "doc": "The registry as plain dicts, in declaration order \u2014 the emitted catalog's body.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Optimization/config/objectives.py::objective_for": {
      "decorators": [],
      "doc": "The Objective behind a `Strategy` (or a bare restock key).",
      "is_async": false,
      "kind": "function",
      "signature": "(strategy) -> Objective"
    },
    "Optimization/config/settings.py": {
      "decorators": [],
      "doc": "settings \u2014 every tunable a run has, as a flat list of named values.\n\nThe authoring surface. One name per knob, at module level, grouped by what it governs and\nannotated with what changing it costs. `sim_config.CONFIG` is DERIVED from this file, so\nthis is the one place a value is written down and the nested dict is a view of it.\n\n    settings.py          you edit this                        (declaration)\n        |\n        v\n    sim_config.CONFIG    the nested dict every reader knows   (run state)\n        |\n        v\n    resolve at run time  + CLI flags + the what-if cell       (this run)\n\n## Reading this file\n\nEvery value below is a DEFAULT. Three things override it, in order:\n\n1. a CLI flag (`--n-batches`, `--seed-world`, \u2026) \u2014 see `run_simulation`\n2. a what-if cell, for the three axes it sweeps \u2014 see `simdriver.cells`\n3. a `--resume`, which restores whatever the original run recorded in its run spec\n\nOverrides are written into `CONFIG` and read back through the accessors in `sim_config`\n(`n_batches()`, `seed_world()`, \u2026). **Never snapshot one of these at import**: a module\nscalar cannot see a CLI override, which is a bug this project has now shipped twice \u2014 once\nfor `_INITIAL_FILL`, which made a run misreport its own sizing in its warehouse DB, and once\nfor five more that were caught before they could.\n\n## What does NOT live here\n\n- **Pick-config coefficients.** Those are a swept AXIS, not a setting: each variant is a\n  self-registering module under `Optimization/simconfig/configs/`, and the run sweeps all of\n  them. A number that varies within one run is not a default.\n- **What-if cell definitions.** `Optimization/config/whatif_config.SPECS`.\n- **Restock-rule membership.** `Optimization/config/strategies`.\n- **Anything derived.** `checkpoint_every` is computed from `n_batches`; the shift's\n  hour-length is `REPORTING_FRAME_SECONDS / 3600`. A derived value declared beside its input is two\n  things to keep in step.\n\nAdding a setting: declare it here, thread it into `CONFIG` below, and \u2014 if it changes\nresults \u2014 give it a CLI flag, record it in the run spec, and restore it in both\n`_apply_run_spec` and `run_analysis._apply_run_shape`.\n\n**And then the FIFTH seam, which this list used to omit:** carry the value in\n`workunits._shared`, the picklable payload a spawned worker actually receives. A worker\nre-imports this module and gets the PRISTINE defaults above \u2014 it never sees the parent's\nmutated `CONFIG` \u2014 so a setting that reaches all four sites and not the payload is accepted\non the command line and then silently ignored for the whole run. That is not hypothetical:\nit is why the working day, the two crews, the crew price and the staffing record are each\ncarried as an explicit payload key rather than re-derived worker-side.\n\nMiss any one of those five and the flag is accepted and then silently ignored.\n`Tests/unit/test_run_shaping_params.py` guards the first four per knob;\n`Tests/unit/test_config_reaches_the_worker.py` guards the fifth generically \u2014 it proves a\nworker cannot import this package at all, so a value it uses must have arrived in the\npayload.",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/settings.py::AISLE_COLUMNS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "50"
    },
    "Optimization/config/settings.py::AISLE_LEVELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "10"
    },
    "Optimization/config/settings.py::BAND_TOL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.1"
    },
    "Optimization/config/settings.py::CHECKPOINT_FRAC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.1"
    },
    "Optimization/config/settings.py::COUPLE_CHANNELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "False"
    },
    "Optimization/config/settings.py::COVERAGE_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "10.0"
    },
    "Optimization/config/settings.py::CUT_AT_DAY_END": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "False"
    },
    "Optimization/config/settings.py::FF_BATCH_MEAN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.2"
    },
    "Optimization/config/settings.py::FF_BATCH_STD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.05"
    },
    "Optimization/config/settings.py::FF_CART": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'FulfillmentCart'"
    },
    "Optimization/config/settings.py::FF_DEMAND": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.018138"
    },
    "Optimization/config/settings.py::FF_FILL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_TARGET_FILL"
    },
    "Optimization/config/settings.py::FF_PICKERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_FF_PICKERS"
    },
    "Optimization/config/settings.py::FF_PICK_MODE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'foot'"
    },
    "Optimization/config/settings.py::FIRST_TIME_CONFIDENCE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.95"
    },
    "Optimization/config/settings.py::FLOOR_LINES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::F_PUT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1.0"
    },
    "Optimization/config/settings.py::F_RECV": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1.0"
    },
    "Optimization/config/settings.py::F_REPACK": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.0"
    },
    "Optimization/config/settings.py::INBOUND_CREW_ALLOCATION": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'split'"
    },
    "Optimization/config/settings.py::INBOUND_DOCK_DOORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_DOCK_DOORS"
    },
    "Optimization/config/settings.py::INBOUND_DOCK_POLICY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fifo'"
    },
    "Optimization/config/settings.py::INBOUND_DOOR_TEAM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_FEE_THRESHOLD_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_FEE_THRESHOLD_DAYS"
    },
    "Optimization/config/settings.py::INBOUND_FUTURESIGHT_BATCHES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_LEAD_MINUTES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.0"
    },
    "Optimization/config/settings.py::INBOUND_LEAD_SPREAD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.0"
    },
    "Optimization/config/settings.py::INBOUND_LOCAL_POLICY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fifo'"
    },
    "Optimization/config/settings.py::INBOUND_STANDING_YARD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "False"
    },
    "Optimization/config/settings.py::INBOUND_TRAILER_BOUND": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_TRAILER_TYPE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_UNLOAD_INTERCEPT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_UNLOAD_VOLUME_COEF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_UNLOAD_WEIGHT_COEF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::INBOUND_URGENCY_HORIZON_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_URGENCY_HORIZON_DAYS"
    },
    "Optimization/config/settings.py::INBOUND_YARD_POLICY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fifo'"
    },
    "Optimization/config/settings.py::KEYFRAME_INTERVAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "25"
    },
    "Optimization/config/settings.py::MAX_SKUS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::MIN_HEADROOM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.05"
    },
    "Optimization/config/settings.py::N_BATCHES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "100"
    },
    "Optimization/config/settings.py::PUT_CART_CREW": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1"
    },
    "Optimization/config/settings.py::PUT_CART_STAGING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::PUT_CREW_MODE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'foot'"
    },
    "Optimization/config/settings.py::PUT_CREW_SIZE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1"
    },
    "Optimization/config/settings.py::PUT_FF_CREW": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1"
    },
    "Optimization/config/settings.py::PUT_FF_STAGING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::PUT_FOOT_X": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "2.0"
    },
    "Optimization/config/settings.py::PUT_FOOT_Y": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "4.0"
    },
    "Optimization/config/settings.py::PUT_INTERCEPT_SCALE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_PUT_INTERCEPT_SCALE"
    },
    "Optimization/config/settings.py::PUT_ITEM_RATIO": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_PUT_ITEM_RATIO"
    },
    "Optimization/config/settings.py::PUT_MACHINE_X": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3.0"
    },
    "Optimization/config/settings.py::PUT_MACHINE_Y": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "2.0"
    },
    "Optimization/config/settings.py::PUT_PALLET_CREW": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1"
    },
    "Optimization/config/settings.py::PUT_PALLET_STAGING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::PUT_QUEUE_SPLIT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "False"
    },
    "Optimization/config/settings.py::PUT_SWAP_COEF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.0"
    },
    "Optimization/config/settings.py::RECV_CREW_SIZE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0"
    },
    "Optimization/config/settings.py::RECV_DAY_ORIGIN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.0"
    },
    "Optimization/config/settings.py::RECV_DAY_SECONDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::RECV_INTERCEPT_SCALE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_RECV_INTERCEPT_SCALE"
    },
    "Optimization/config/settings.py::RELEASES_PER_DAY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::REPORTING_FRAME_SECONDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_SHIFT_SECONDS"
    },
    "Optimization/config/settings.py::RHO_PICK": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.85"
    },
    "Optimization/config/settings.py::RHO_PUT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.85"
    },
    "Optimization/config/settings.py::RHO_RECV": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.85"
    },
    "Optimization/config/settings.py::ROLL_OVER_UNPICKED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "False"
    },
    "Optimization/config/settings.py::SAFETY_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "2.0"
    },
    "Optimization/config/settings.py::SAMPLER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'v3'"
    },
    "Optimization/config/settings.py::SEED_BATCHES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1337"
    },
    "Optimization/config/settings.py::SEED_WORLD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "42"
    },
    "Optimization/config/settings.py::SHIFT_DRAIN_OR_CAP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "False"
    },
    "Optimization/config/settings.py::STORE_BATCH_MEAN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.15"
    },
    "Optimization/config/settings.py::STORE_BATCH_STD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.05"
    },
    "Optimization/config/settings.py::STORE_CART": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'StoreCart'"
    },
    "Optimization/config/settings.py::STORE_DEMAND": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.00245335"
    },
    "Optimization/config/settings.py::STORE_FILL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "DEFAULT_TARGET_FILL"
    },
    "Optimization/config/settings.py::STORE_PICKERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_STORE_PICKERS"
    },
    "Optimization/config/settings.py::STORE_PICK_MODE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'machine'"
    },
    "Optimization/config/settings.py::S_PICK_FF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::S_PICK_STORE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::S_PUT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::WORKERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1"
    },
    "Optimization/config/settings.py::WORK_DAY_SECONDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/config/settings.py::ZONING_OFF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'enabled': False, 'n_bands': DEFAULT_ZONING_BANDS, 'mode': 'equal', 'abc': {'mass_thresholds': [0.7, 0.9]}}"
    },
    "Optimization/config/sim_config.py": {
      "decorators": [],
      "doc": "sim_config.py \u2014 the run's single source of tunable truth.\n\nEverything a run can be tuned by lives here: the .env-backed output/profile dirs,\nthe per-channel pick-config sweeps (STORE_CONFIGS / FULFILLMENT_CONFIGS), the nested\nCONFIG dict (global + per-channel sections), the run-shaping accessors, and the\nconfig\u2192PickConfig conversion.  run_simulation re-exports the public names so\n`rs.CONFIG` / `rs.REGRESSION_CONFIGS` consumers (tests, Diagnostics/bucket_fill)\nkeep working.\n\nCONFIG identity rule: the re-export binds the SAME dict object \u2014 tests mutate\n`rs.CONFIG['global'][...]` in place \u2014 so nobody may ever REBIND CONFIG (or the\nmutation would silently detach from internal readers).\n\nCorollary, and the reason the run-shaping values are FUNCTIONS rather than module\nscalars: a value snapshotted at import cannot see that mutation.  Every consumer of a\ntunable reads it at call time.  See the block above `seed_world()`.",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/sim_config.py::CONFIG": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'global': {'seed_world': _s.SEED_WORLD, 'seed_batches': _s.SEED_BATCHES, 'n_batches': _s.N_BATCHES, 'workers': _s.WORKERS, 'checkpoint_frac': _s.CHECKPOINT_FRAC, 'keyframe_interval': _s.KEYFRAME_I..."
    },
    "Optimization/config/sim_config.py::FULFILLMENT_CONFIGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[dict(s.cfg) for s in _ACTIVE_PICK_CONFIGS if s.channel == 'fulfillment']"
    },
    "Optimization/config/sim_config.py::Knob": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One tunable of CONFIG['global'], declared once.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/config/sim_config.py::REGRESSION_CONFIGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "STORE_CONFIGS"
    },
    "Optimization/config/sim_config.py::STORE_CONFIGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[dict(s.cfg) for s in _ACTIVE_PICK_CONFIGS if s.channel == 'store']"
    },
    "Optimization/config/sim_config.py::_ACTIVE_PICK_CONFIGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "sorted((s for s in PICK_CONFIGS if s.enabled), key=lambda s: (s.channel, s.order, s.name))"
    },
    "Optimization/config/sim_config.py::_CART_TYPES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'StoreCart': StoreCart, 'FulfillmentCart': FulfillmentCart}"
    },
    "Optimization/config/sim_config.py::_CATEGORIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['food', 'clothing', 'electronic', 'furniture', 'seasonal', 'chemical']"
    },
    "Optimization/config/sim_config.py::_COERCERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{None: lambda v: v, 'bool': bool, 'or_one': lambda v: v or 1}"
    },
    "Optimization/config/sim_config.py::_DEFAULT_PROFILES_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_clean_path(os.getenv('PROFILE_INPUT_DIR', os.path.normpath(os.path.join(_REPO_ROOT, 'Warehouse', 'generated', 'profiles'))))"
    },
    "Optimization/config/sim_config.py::_HANDLINGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['conveyable', 'non-conveyable']"
    },
    "Optimization/config/sim_config.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Optimization/config/sim_config.py::_OUTPUT_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_clean_path(os.getenv('COMPARISON_OUTPUT_DIR', _HERE))"
    },
    "Optimization/config/sim_config.py::_PICK_CONFIG_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset((f.name for f in _dataclass_fields(PickConfig)))"
    },
    "Optimization/config/sim_config.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(_HERE, '..', '..'))"
    },
    "Optimization/config/sim_config.py::_build_pick_cfg": {
      "decorators": [],
      "doc": "Turn a config dict (store or fulfillment) into a PickConfig.\n\nThe one canonical dict\u2192PickConfig conversion shared by both channels' sweeps.  Callers\npass the channel's DECLARED picker count (`channel_pickers(name)`; a config dict's own\n'num_pickers', if present, must agree -- `workunits._channel_runs_for` raises otherwise) and\nthe channel's default_cart (StoreCart / FulfillmentCart); a 'cart' key overrides it.\n\nA MISSING key falls through to `PickConfig`'s own dataclass default, and that is the whole\npoint of the shape here.  This function used to restate a fallback per key \u2014 a SECOND\ndefault set, which had drifted from the first: `pick_weight_coef` 1.1 against the\ndataclass's 0.02 (55x), `pick_volume_coef` 1e-3 against 1e-4 (10x), `cart_swap_coef` 10.0\nagainst 5.0 (2x).  Nothing caught it because every registered pick-config declares all\nthree, so neither set was ever exercised on the run path \u2014 and `run_map_precompute`\nrebuilds a `PickConfig` from an ARCHIVED leaf config by field-filtering, which has always\ntaken the dataclass defaults.  Two default sets and two reconstruction paths is a silent\n55x waiting for the first archive vintage that omits a key.\n\nKeys the dataclass does not declare (`name`) are dropped; `cart` is resolved separately\nbecause the dict stores its NAME and the dataclass wants the class.",
      "is_async": false,
      "kind": "function",
      "signature": "(cfg: dict, *, num_pickers: int, default_cart=StoreCart) -> PickConfig"
    },
    "Optimization/config/sim_config.py::_checkpoint_every": {
      "decorators": [],
      "doc": "Batches between per-strategy checkpoints (floor(n_batches * checkpoint_frac), \u22651) \u2014\nmatches the legacy ``max(1, N_BATCHES // 10)`` cadence at the default 0.1 fraction.",
      "is_async": false,
      "kind": "function",
      "signature": "(n_batches: int) -> int"
    },
    "Optimization/config/sim_config.py::_config_name": {
      "decorators": [],
      "doc": "A config's directory/identity name (explicit 'name', else a coeff fingerprint).",
      "is_async": false,
      "kind": "function",
      "signature": "(cfg: dict) -> str"
    },
    "Optimization/config/sim_config.py::_futuresight_batches": {
      "decorators": [],
      "doc": "Normalize INBOUND_FUTURESIGHT_BATCHES for the spec: None | 'all' | int >= 0.\n\nAnything else raises here, at spec build, rather than reaching a worker: the knob\ndenominates a batch COUNT, so a negative, fractional, bool, or unrecognized-string\nvalue is a config error, never a silent clamp \u2014 and the error names the knob\n(a bare `int('oracle')` message would refuse loudly but signpost nothing).",
      "is_async": false,
      "kind": "function",
      "signature": "(raw)"
    },
    "Optimization/config/sim_config.py::_knobs": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(names, **kw)"
    },
    "Optimization/config/sim_config.py::_setup_logging": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(log_path: str) -> logging.Logger"
    },
    "Optimization/config/sim_config.py::aisle_geometry": {
      "decorators": [],
      "doc": "(width, height) of one aisle in physical units, read at CALL time.\n\nA FUNCTION, not two module scalars, and deliberately: the pair used to be\n`aisle_width_for(50)` / `aisle_height_for(10)` evaluated at import, so a CLI override\ncould never reach them. That is the `_INITIAL_FILL` defect this project has already\nshipped once -- a run misreporting its own sizing in its own warehouse DB --\nand `Tests/unit/test_config_reaches_the_worker.py` mutates the CONFIG key and calls\nevery accessor twice to keep it from coming back.\n\nPARENT-SIDE. The warehouse is planned before any worker exists, so this is not in\nthe worker payload; it is listed in that test's `PARENT_ONLY` with the reason.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple[int, int]"
    },
    "Optimization/config/sim_config.py::apply_cli_overrides": {
      "decorators": [],
      "doc": "Write every declared knob's CLI value back onto CONFIG['global'].\n\nTHE write-back.  Replaces ~35 hand-typed assignments plus two family loops; a knob that\nis declared above cannot now be accepted at the command line and then silently ignored\nfor the whole run, which is the defect this registry exists to make unrepresentable.\n\nCONFIG is mutated in place and never rebound (see the module head): every accessor reads\nat call time, so this reaches the warehouse build, the channel build and every worker\npayload built after it.",
      "is_async": false,
      "kind": "function",
      "signature": "(args) -> None"
    },
    "Optimization/config/sim_config.py::channel_demand": {
      "decorators": [],
      "doc": "The DECLARED demand of channel `name` under the era, in the sampler's unit (the\nfraction of the section's SKUs drawn per day), read from CONFIG at call time; None\nflag-off, where the script's content is the channel's `batch.mean` and nothing reads\nthis.  A None IN the key under the era (a spec restored by `_apply_run_shape` from a\nrun that predates the declaration) resolves to the settings default.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> float | None"
    },
    "Optimization/config/sim_config.py::channel_pickers": {
      "decorators": [],
      "doc": "The DECLARED picker count of channel `name`, read from CONFIG at CALL time.\n\nThe `recv_crew_spec` pattern: a flag writes CONFIG, a resume and a re-analysis restore\ninto CONFIG, so CONFIG is the only place this may be read from.\nA None (a pre-record run spec restored by `run_analysis._apply_run_shape`) resolves to the\nleaf default -- that run fielded the compile-time constant of its day, which is what the\nconstant still is.  An unknown channel is a KeyError, deliberately: a third channel needs\nits own declared knob, not a silent share of someone else's.\n\nUNDER THE ERA THIS IS A PLACEHOLDER (ADR-0004): the channel is built from it and then\nthe derivation replaces the crew with the solved one (`workunits._derive_staffing_for_pair`),\nthe record carries the picker keys as None, and every reader that needs a crew reads it\nthrough `staffing.channel_crew`, which prefers the derived block.  Nothing sizes from\nthis value under the era.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> int"
    },
    "Optimization/config/sim_config.py::couple_channels": {
      "decorators": [],
      "doc": "Whether this run's two channels share one site: one dock, one receiving crew, one pool\nof putters \u2014 a COUPLED work unit per arm pair instead of one unit per channel leaf.\n\nA DECLARATION, never an inference. The obvious rule \u2014 \"coupling rides the inbound flag\" \u2014\ndoes not hold: site-dock 06 couples every cell of the inbound campaign including its\ninbound-OFF pole, because the matrix's own deltas are what it publishes and an uncoupled\nanchor inside a coupled matrix would not be one. So the run says whether it is a site.\n\nRead from CONFIG at call time, like `era_on()` and for the same reason: a spawned worker\nre-imports this module and would get the pristine default. The flag reaches the run tree\nas `run_layout.json`'s `coupled`, which is what a downstream tool reads (a coupled root is\nrefused by `run_restock_selection.select`, whose validity argument IS channel independence).",
      "is_async": false,
      "kind": "function",
      "signature": "() -> bool"
    },
    "Optimization/config/sim_config.py::crew_cost_spec": {
      "decorators": [],
      "doc": "The other crews' PRICE as a picklable record:\n`{put_intercept_scale, put_item_ratio, recv_intercept_scale}`.\n\nPut-away and receiving keep picking's cost shape and coefficients by reference\n(`PutawayCost.from_pick`, `UnloadCost.from_putaway`); these three scalars are the only\nplace their numbers may differ.  Read from CONFIG at CALL time -- never from `_s.`\ndirectly, which was the trap `put_crew_spec` above carried until the era build -- and handed to the\nworker in its payload (`workunits._shared['crew_cost']`), because a spawned worker\nre-imports this module and would get pristine defaults.\n\nA None (a pre-field run spec restored by `run_analysis._apply_run_shape`) resolves to the\nkernel default: that run priced its put crew at the pre-charge literal, which no scale\ncan reproduce, so the default is the honest reconstruction of \"no scale was declared\".",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/config/sim_config.py::crew_cost_spec._f": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(key, default)"
    },
    "Optimization/config/sim_config.py::era_on": {
      "decorators": [],
      "doc": "Whether this run is under the calibrated era: the drain-or-cap shift is on.\n\nONE predicate for \"derive the crews, refuse the legacy crew flags, price the script from\nthe calibration record\", read from CONFIG at call time.  The same key `work_day_spec()`\ncarries to the worker as `drain_or_cap`, so the parent and the worker cannot disagree\nabout which regime a run is in.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> bool"
    },
    "Optimization/config/sim_config.py::ff_fill": {
      "decorators": [],
      "doc": "The fulfillment regime's fill headroom, read at call time.  Same rule as store_fill,\nflag-off only for the same reason.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> float"
    },
    "Optimization/config/sim_config.py::inbound_lead_law": {
      "decorators": [],
      "doc": "The trailer pipeline's LEAD LAW alone -- `{'trailer_type', 'lead_s', 'lead_sigma'}`,\nor **None** when no trailer type is named (the pipeline is off, structurally).\n\nThe three keys `inbound_spec()` reads for the lead, factored out because the coverage\nrecord needs them BEFORE the receiving crew exists: `era_coverage.lead_block` derives\nthe day-grid transit at setup (`coverage.transit_day_law`), and `inbound_spec()`'s\n\"a yard nobody can unload\" guard would refuse that read under the era, where the crew\nis derived only after the fixed point has declared the levels.  No guard here: this\naccessor answers what the lead IS; `inbound_spec()` still refuses every contradiction\nthe moment the worker payload is built.  Reads CONFIG at call time; the median is\nconverted from minutes to seconds here and nowhere else (the same conversion\n`inbound_spec()` records as `lead_s`).",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict | None"
    },
    "Optimization/config/sim_config.py::inbound_spec": {
      "decorators": [],
      "doc": "The trailer pipeline's configuration as a picklable record, or **None** for off.\n\n`recv_crew_size` is the DERIVED receiving crew under the calibrated era\n(`staffing.derive`), handed in by the caller that holds the derived block -- the\n`recv_crew_spec(size=)` pattern, and for the same reason: derived values are never\nCONFIG keys.  None reads the declared `recv_crew_size` key, which is every flag-off\nrun.  It is read by exactly one guard, the standing yard's \"a yard nobody can unload\"\nrefusal below; nothing else about the record depends on it.\n\nNone rather than a dict with a flag, for the same reason `recv_crew_spec` returns None:\nthe worker tests `args.get('inbound') is None` and skips the whole build -- no\nTrailerTransit, no trailer objects, and the manager keeps the batch lead queue it\nconstructed for itself, byte-identically.\n\nReads CONFIG at CALL time (the `recv_crew_spec` pattern, never `put_crew_spec`'s\nsettings snapshot).  The lead MEDIAN is authored in MINUTES and converted to the sim's\nseconds exactly once, here -- the minutes-at-the-surface decision.  The SPREAD is\ndimensionless and crosses as-is; the draw itself lives at trailer creation\n(`Inbound.transit.TrailerTransit.lead_for`), keyed by the world seed carried below,\nso no RNG object ever has to cross the worker payload.\n\nTHE STANDING YARD'S CONTRADICTIONS FAIL HERE, LOUDLY.  `INBOUND_STANDING_YARD` with\nno trailer type is a yard with no trailers; with no receiving crew it is a yard\nnobody can ever unload -- merchandise would stand deferred forever, the run would\ncomplete, and nothing would raise.  And a non-fifo yard/dock policy WITHOUT the\nstanding yard is a policy nothing reads: the run would complete as v1 fifo under\nthe policy's name -- the fake-arm hazard, worn as configuration.  So is a lead\nSPREAD without the standing yard, or over a zero median (see the guard below).  All\nare configuration errors, never a silent no-op: the flag's OFF state is the only\ninert one.",
      "is_async": false,
      "kind": "function",
      "signature": "(recv_crew_size: int | None=None) -> dict | None"
    },
    "Optimization/config/sim_config.py::k_pickers": {
      "decorators": [],
      "doc": "The store channel's DECLARED picker count, read at call time (the diagnostics' entry\npoint).  Flag-off only in meaning: under the era this is the placeholder, not the solved\ncrew -- a probe that models an era run's crew reads `staffing.channel_crew` off that\nrun's staffing record instead.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> int"
    },
    "Optimization/config/sim_config.py::min_headroom": {
      "decorators": [],
      "doc": "The minimum free share every bin bucket keeps at setup under the era, read from CONFIG\nat call time; None flag-off, where the typed `store_fill()` / `ff_fill()` size the\nwarehouse and nothing reads this.  A None IN the key under the era (a spec restored from\na run that predates the derived fill) resolves to the settings default.  Refuses a value\noutside [0, 1): a headroom of one is a bucket sized for nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> float | None"
    },
    "Optimization/config/sim_config.py::n_batches": {
      "decorators": [],
      "doc": "The configured batch horizon, read at call time (`--n-batches` writes CONFIG).",
      "is_async": false,
      "kind": "function",
      "signature": "() -> int"
    },
    "Optimization/config/sim_config.py::put_crew_spec": {
      "decorators": [],
      "doc": "The put crew as a picklable record: `{size, mode, x_speed, y_speed}`.\n\nReads CONFIG at CALL time -- `put_crew_size` / `put_crew_mode` -- like every other\naccessor in this file.  It used to read `_s.PUT_CREW_SIZE` / `_s.PUT_CREW_MODE`\ndirectly with no CONFIG key at all, which was THE trap the other accessors' docstrings\nwarn about: a flag writing CONFIG would have been accepted and ignored forever, and a\nstandalone re-analysis would have sized against this checkout's settings instead of\nthe run's own.  Closed by \"Design the staffing record\", decision 7.\n\n`size` is the DERIVED crew under the calibrated era (`staffing.derive`), handed in by\nthe caller that holds the derived block; None reads the declared key, which is every\nflag-off run.  A None IN the key (a pre-field run spec restored by\n`run_analysis._apply_run_shape`) resolves to the settings default -- the one walker\nevery such run fielded.\n\nThe speed comes from the crew's MODE, which is the point of having a mode: a crew\nlabelled `foot` costed at the store's machine speed would write rows whose mode and\nduration contradict each other.  The mode is a DECLARED staffing input (it rides\nSTAFFING_KEYS), so it is read through `staffing_spec()`'s resolution.",
      "is_async": false,
      "kind": "function",
      "signature": "(size: int | None=None) -> dict"
    },
    "Optimization/config/sim_config.py::put_queues_spec": {
      "decorators": [],
      "doc": "The split put-away configuration as a picklable record, or **None** for one queue.\n\nNone rather than a dict with `split=False`, for the same reason `recv_crew_spec` returns\nNone: the worker tests `args.get('put_queues') is None` and skips the assignment\nentirely, so the no-op is \"the manager keeps the `single_queue()` it built in\n`__init__`\" rather than \"something reconstructed the default\".\n\nReads CONFIG at CALL time. Copying the `put_crew_spec` of its day would have been the\nnatural move and was a trap: it read `_s.PUT_CREW_SIZE` directly, there was no\n`CONFIG['global']['put_crew_*']` key at all, and a CLI flag writing CONFIG would\ntherefore have been accepted and ignored forever.  (Since fixed.)\n\nTHE CREW SIZES ARE NOT A DETAIL. Each queue gets its own crew and its own clock, so\nthree queues of size 1 is three putters where the default is one -- roughly 3x the\nput-away throughput before any other difference. A sweep comparing split against single\nmust size these against the single-queue total, or it is measuring headcount.\n\nStaging is carried as-is including None: `None` means an unbounded floor, and that is a\nmeaningfully different configuration from a large one, because it is what decides\nwhether the held list and the refill loop ever execute at all.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict | None"
    },
    "Optimization/config/sim_config.py::recv_crew_spec": {
      "decorators": [],
      "doc": "The receiving crew as a picklable record, or **None** when there is no crew.\n\n`size` is the DERIVED crew under the calibrated era (`staffing.derive`), handed in by\nthe caller that holds the derived block; None reads the declared `recv_crew_size` key,\nwhich is every flag-off run.  Derived values are never CONFIG keys, so the derivation\ncannot leak into a flag-off run by leaving a key behind.\n\nNone rather than an empty dict, and that distinction is the feature's off-switch. The\nworker tests `args.get('recv_crew') is None` and skips its whole receive block: no Crew,\nno Workers, no clocks, no Dock, no WorkDay. The no-op is therefore \"nothing was\nconstructed\" rather than \"an empty thing exists\" -- which matters because an empty dock\nwould still be an object something could fold into a clock or a snapshot.\n\nCOPIED FROM `work_day_spec`, NOT FROM the `put_crew_spec` of its day. That one read\n`_s.PUT_CREW_SIZE` directly with no `CONFIG['global']['put_crew_*']` key at all -- so a\nCLI flag, which writes CONFIG, would have been accepted and ignored forever, and a\nstandalone re-analysis would have sized against this checkout's `settings.py` instead\nof the run's own. Reading CONFIG at CALL time is what makes the other three seams\nreachable.  (`put_crew_spec` has since been fixed the same way.)\n\n`day_seconds` is resolved with an explicit `is not None`, never `or`: `--recv-day-seconds\n0` means \"the crew has no day today\", and `or` would silently turn that into \"no whistle\nat all\" -- the inverse. (The parser rejects 0 as well, so this is the second of two\nguards on the same mistake.)\n\nSpeeds come from the PUT crew's foot table. Not laziness: an unload has no travel term,\nso no speed is consumed by the cost model at all, and declaring receiving-specific\nconstants would assert a distinction the model cannot express. They are carried only so a\n`Crew` can be built and its rows labelled.",
      "is_async": false,
      "kind": "function",
      "signature": "(size: int | None=None) -> dict | None"
    },
    "Optimization/config/sim_config.py::regime_sizing_from_config": {
      "decorators": [],
      "doc": "Assemble the per-regime warehouse-sizing dict from CONFIG (both regimes sized from the\ndeclared levels; store composition + caps; ff aisle shape), each with its own fill\nheadroom -- the share of each bucket those levels occupy at setup.  Used\nby both the run (main) and the analysis rebuild (run_analysis) so the warehouse SHAPE \u2014 ff\naisle layout + total_bins \u2014 matches; sizing the two paths differently would misgroup ff\naisle stats and skew churn %.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/config/sim_config.py::run_spec_record": {
      "decorators": [],
      "doc": "Every declared knob a run records, each from the source it declares.\n\n`spec_from='config'` reads POST-overlay so a value that came from CONFIG rather than the\ncommand line is recorded too -- otherwise two runs with different docks look identical\nafter the fact.  `spec_from='args'` reads the namespace, because a few knobs are recorded\nas TYPED rather than as resolved (`workers` resolves to 1 in CONFIG and records None).\n\nThe caller adds the entries that are not plain knobs: argv, the sizing caps (which live on\n`args` and never in CONFIG), the era-conditional per-channel fills, the store composition,\nthe nested staffing record, and the CLI-only run shaping.",
      "is_async": false,
      "kind": "function",
      "signature": "(args) -> dict"
    },
    "Optimization/config/sim_config.py::seed_batches": {
      "decorators": [],
      "doc": "The base batch-stream seed, read at call time.\n\nA CHANNEL's seed is this plus its `batch_seed_offset` (see `channels.Channel`), so\nstore and fulfillment draw independent streams from one configured base.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> int"
    },
    "Optimization/config/sim_config.py::seed_world": {
      "decorators": [],
      "doc": "The world seed (warehouse + catalogue construction), read at call time.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> int"
    },
    "Optimization/config/sim_config.py::shift_seconds": {
      "decorators": [],
      "doc": "Shift length in seconds, read at call time.  Default: an eight-hour day.\n\nA REPORTING FRAME, not a scheduler.  It labels `work_events.shift_index`; nothing\ndispatches against it, work does not pause at the whistle, and no task is split at a\nboundary -- see `Warehouse.kernel.timeline.shift_index`.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> float"
    },
    "Optimization/config/sim_config.py::staffing_provenance": {
      "decorators": [],
      "doc": "The staffing record's PROVENANCE block, one entry per input the regime reads.\n\n`declared` when a flag chose the value (`explicit` is the set of flag names typed on\nthe command line), `assumed` when a settings default stood; the picker keys are\n`derived` under the era (ADR-0004), and a key the regime does not read -- None in\n`staffing_spec()` -- carries no provenance at all, because \"assumed\" would claim a\nsettings default was in force when nothing read one.  `floor_lines` under the era is\n`derived` unless typed (the coverage loop solves it).",
      "is_async": false,
      "kind": "function",
      "signature": "(explicit) -> dict"
    },
    "Optimization/config/sim_config.py::staffing_spec": {
      "decorators": [],
      "doc": "The staffing record's INPUTS as a picklable dict, one entry per STAFFING_KEYS key.\n\nInputs ONLY -- the derived block (batch content, the picking crew, put crew, receiving\ncrew, expected utilization) is the pure module `Optimization/simconfig/staffing.py`, run\nby `workunits._derive_staffing_for_pair` after batch precompute, and is never a CONFIG\nkey.  Read from CONFIG at call time for the reason every accessor in this file is, and\ncarried in `workunits._shared` so a spawned worker can check that the crews it was\nhanded are the crews the record declares.\n\nTHE REGIME DECIDES WHICH KEYS ARE INPUTS (ADR-0004).  Flag-off the pickers resolve\nthrough `channel_pickers` (a None is the leaf default) and the ERA_ONLY_KEYS are None;\nunder the era the demand and the confidence resolve to their defaults and the\nFLAG_OFF_ONLY_KEYS are None -- the crew is derived, `rho_pick` is not read.  The shared\nscalars resolve a None to their settings default (`_SCALAR_DEFAULTS`); `floor_lines`\nand the CALIBRATION_KEYS keep None, because \"solve it\" / \"no override\" is a value the\ncoverage loop and the derivation read.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/config/sim_config.py::store_fill": {
      "decorators": [],
      "doc": "The store's sizing fill headroom, read from CONFIG at CALL time.\n\nWas `_INITIAL_FILL`, an import-time scalar \u2014 which quietly broke this module's own rule\nthat CONFIG is the single source of truth and is mutated in place: a runtime override\n(a CLI flag, a test) never reached the snapshot, and `sim_assets` writes this value into\nthe warehouse DB as `target_fill`, so the run's own provenance recorded the stale\nnumber.\n\nFLAG-OFF ONLY since \"Derive the fill headroom from the fragmentation\": under the era the\nplanner is handed a per-bucket hold map derived from the stationary fragmentation and\nfloored at `min_headroom()`, `--store-fill` is refused, and the run spec records this\nkey as None.  The value still stands in CONFIG there, read by nothing that sizes.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> float"
    },
    "Optimization/config/sim_config.py::store_restocks": {
      "decorators": [],
      "doc": "The store channel's restock-rule subset, read at call time.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple"
    },
    "Optimization/config/sim_config.py::work_day_spec": {
      "decorators": [],
      "doc": "The working day as a picklable record: `{seconds, releases_per_day, cut_at_day_end}`.\n\nRead at call time and handed to the worker in its payload rather than re-imported there:\na spawned worker re-imports this module and would get pristine defaults, so a day\nconfigured on the command line would be accepted and then silently ignored.\n\nONE accessor and ONE payload key for three values, because three of each is how two of\nthem end up disagreeing.  `seconds` falls back to the shift length so a run that asks for\na cut without naming a day gets the eight hours it already reports against.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/config/strategies.py": {
      "decorators": [],
      "doc": "strategies.py \u2014 data-driven registry of placement (assignment) strategies.\n\nEach Strategy names an assignment policy and knows how to wire it onto an\nInventory_Manager for the reorder phase.  The whole comparison pipeline\n(run_simulation orchestration, strategy_runner worker, run_analysis/Comparison_Plots\nlabels via sim_meta) iterates this list, so adding a strategy is one entry here.\n\nStrategies share the same uniform INITIAL stocking by default (done in the worker\nbefore build() is called) and differ in how reorder waves are placed.  A strategy\nmay override stocking with a `stock` hook (e.g. optimal_reslot stocks at the\npure-global-W optimum) and may enable bounded per-batch re-slotting via `reslot_frac`.",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/strategies.py::Strategy": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/config/strategies.py::StrategyContext": {
      "decorators": [
        "dataclass"
      ],
      "doc": "Everything a strategy's build() may need, assembled by the worker.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/config/strategies.py::_INITIALS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[('uni', 'Uni', 'uniform'), ('opt', 'Opt', 'policy')]"
    },
    "Optimization/config/strategies.py::_N_STRATEGIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "len(_INITIALS) * len(_RESTOCKS) * len(_RESLOTS)"
    },
    "Optimization/config/strategies.py::_RANKED3": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('sku_sets', 'idx_sets', 'demand_sum')"
    },
    "Optimization/config/strategies.py::_RANKED3_POS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_RANKED3 + ('member_pos',)"
    },
    "Optimization/config/strategies.py::_RESLOTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[('norsl', 'noRSL', 0.0, 'rebalance')]"
    },
    "Optimization/config/strategies.py::_RESLOT_FRAC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.005"
    },
    "Optimization/config/strategies.py::_build_cluster_map": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_cluster_map_rank": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_compaction": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_expansion": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_map": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_map_rank": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_max_cluster": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_min_cluster": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_rank_cartlabor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_rank_labor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_rank_maxlabor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_rank_minlabor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_rank_popularity": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_trip_max": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_trip_min": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_uniform": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_build_uniform_trip_min_ranked": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext) -> None"
    },
    "Optimization/config/strategies.py::_hsv_hex": {
      "decorators": [],
      "doc": "A distinct-ish hex colour for index i of n (HSV hue sweep).",
      "is_async": false,
      "kind": "function",
      "signature": "(i: int, n: int) -> str"
    },
    "Optimization/config/strategies.py::_key": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "f'{_ik}_{_pol.key}_{_sk}'"
    },
    "Optimization/config/strategies.py::_stock_optimal": {
      "decorators": [],
      "doc": "Place initial stock at the pure-global-W optimal layout (hottest SKUs in the\nlowest-W bins).  Stores nothing on mgr; reorders use the normal placement.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, ctx: StrategyContext, inventory) -> None"
    },
    "Optimization/config/strategies.py::restocks_for": {
      "decorators": [],
      "doc": "The restock-rule subset a channel sweeps (None \u21d2 full suite).  Unknown channel \u21d2\nNone (full suite), so a new channel runs everything until curated here.",
      "is_async": false,
      "kind": "function",
      "signature": "(channel: str) -> tuple[str, ...] | None"
    },
    "Optimization/config/strategies.py::strategies_for": {
      "decorators": [],
      "doc": "Subset of STRATEGIES whose restock rule is in `restocks` (None \u21d2 all), IN THE ORDER\n`restocks` states it \u2014 initial (uni/opt) outer, the caller's rule order inner.\n\nLets one channel run only a subset of restock rules (e.g. store: fifo + rank_labor)\nwhile another runs the full suite, without perturbing the global grid used elsewhere.\n\nTHE ORDER IS LOAD-BEARING UNDER COUPLING, and it was not before.  `_prepare_site_run`\n(`Optimization/simdriver/workunits.py`) builds the site's arm PAIRS by zipping the two\nchannels' lists as this function returns them, so position i of the store list is run\nagainst position i of the fulfillment list.  A filter that re-imposed the GRID's order \u2014\nwhich the old `[s for s in STRATEGIES if s.restock in restocks]` did \u2014 would throw away\nthe rank order `run_restock_selection` spent a whole phase-1 run computing and pair rank 1\nagainst whichever of the other channel's rules happens to sit earliest in `_RESTOCKS`.\nSame shape as site-dock 06 section 0's `sorted(set(arms))` finding, one level down: a sort\nthat destroys the only thing the diagonal reads, with nothing raising.\n\nBYTE-IDENTICAL when the caller's order IS the grid order, which every committed\n`CHANNEL_RESTOCKS` value and every registered spec's arm set is (asserted in\nTests/unit/test_funnel_spec_pairs.py) \u2014 so this re-ordering moves no run that exists today.\n\nThe inner order comes from ITERATION, so an unordered container has no defined arm order\nand is refused: a `set` would pair the two channels by whatever its hash order happened to\nbe, which is a different campaign on a different Python build.",
      "is_async": false,
      "kind": "function",
      "signature": "(restocks) -> list[Strategy]"
    },
    "Optimization/config/whatif_config.py": {
      "decorators": [],
      "doc": "Experiment definition for `run_simulation --whatif`: an aisle-reconstruction \u00d7 velocity-zoning \u00d7\npicker-scheduler matrix.\n\nEditing + committing this file is how a what-if sweep is DEFINED \u2014 the same way the strategies sweep\nlives in `strategies.CHANNEL_RESTOCKS` and the base warehouse knobs live in `sim_config.CONFIG`.\n`run_simulation` consumes `WHATIF`; `--whatif` is just the flag that turns it on.  The matrix\nsimulates the SAME frozen inventory + SAME batch stream across every cell, so cells differ ONLY in\nthe swept knobs \u2014 an apples-to-apples comparison.\n\nCells are the combinatorial product schedulers \u00d7 zoning \u00d7 ks \u00d7 losses (loss collapses to 0 when\nk == 1), named `k{k}[_l{loss%}]_{zone}[_{sched}]`.  The scheduler suffix (`_rr` / `_lpt`) is added\nonly when more than one scheduler is swept; the (k=1, off, round_robin) cell is the reference.\n\nCURRENT SCENARIO \u2014 picker-scheduler A/B with NO ABC aisle splitting and NO ABC velocity zoning: hold\nthe layout fixed (whole aisles, no A/B/C banding) and sweep the task\u2192picker scheduler `round_robin`\n(naive static i%num_pickers) vs `lpt` (makespan-MINIMIZING load balancer) across the full\nassignment-function suite \u2014 measuring the throughput (batch-makespan) gain of load-balancing at\nunchanged task makespan (total labor).  The ABC aisle-split and ABC velocity-zoning axes are preserved\nbelow as commented-out lines; uncomment them to sweep layout too.\n\nA spec also DECLARES ITS OWN SHAPE and is checked against it at `get_spec` \u2014 before the run\ndirectory exists, before a worker is forked, before anything is written.  The checks live here\nrather than in the driver because this file is where a campaign is authored, and every one of\nthem guards a defect whose only other symptom is a run that completes and means something else\n(`validate_spec`).",
      "kind": "module",
      "signature": null
    },
    "Optimization/config/whatif_config.py::CAMPAIGN_DEPTH_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "40"
    },
    "Optimization/config/whatif_config.py::CAMPAIGN_WINDOW_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(20, 39)"
    },
    "Optimization/config/whatif_config.py::COUPLED_CHANNELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('store', 'fulfillment')"
    },
    "Optimization/config/whatif_config.py::ERA_RUN_DEFAULTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'shift_drain_or_cap': True, 'releases_per_day': 1, 'roll_over_unpicked': True, 'cut_at_day_end': True}"
    },
    "Optimization/config/whatif_config.py::INBOUND_ARRIVAL_REGIME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'inbound_trailer_type': '53', 'inbound_standing_yard': True, 'inbound_dock_doors': PHASE2_DOCK_DOORS, 'inbound_door_team': PHASE2_DOOR_TEAM, 'inbound_lead_minutes': PHASE2_LEAD_MINUTES, 'inbound_l..."
    },
    "Optimization/config/whatif_config.py::PHASE1_RUN_DEFAULTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{**ERA_RUN_DEFAULTS, **INBOUND_ARRIVAL_REGIME, 'n_batches': CAMPAIGN_DEPTH_DAYS}"
    },
    "Optimization/config/whatif_config.py::PHASE2_DOCK_DOORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "4"
    },
    "Optimization/config/whatif_config.py::PHASE2_DOOR_TEAM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "10"
    },
    "Optimization/config/whatif_config.py::PHASE2_FINITE_W": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "5"
    },
    "Optimization/config/whatif_config.py::PHASE2_H_MULTIPLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(0.25, 0.5, 1.0)"
    },
    "Optimization/config/whatif_config.py::PHASE2_LEAD_MINUTES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "480.0"
    },
    "Optimization/config/whatif_config.py::PHASE2_LEAD_SPREAD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.7"
    },
    "Optimization/config/whatif_config.py::PHASE2_PAIRS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[('rank_cartlabor', 'rank_minlabor'), ('rank_minlabor', 'tmin'), ('rank_labor', 'rank_labor'), ('tmin', 'rank_cartlabor'), ('rank_random', 'rank_popularity'), ('fifo', 'fifo')]"
    },
    "Optimization/config/whatif_config.py::PHASE2_RECV_CREW_SIZE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "4"
    },
    "Optimization/config/whatif_config.py::PHASE2_RECV_DAY_SECONDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "43200.0"
    },
    "Optimization/config/whatif_config.py::PHASE2_RIDER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('fifo', 'fifo')"
    },
    "Optimization/config/whatif_config.py::PHASE2_RUN_DEFAULTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{**ERA_RUN_DEFAULTS, **INBOUND_ARRIVAL_REGIME, 'couple_channels': True, 'n_batches': CAMPAIGN_DEPTH_DAYS}"
    },
    "Optimization/config/whatif_config.py::PHASE2_STAFFING_PIN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'mixed_20260816_131535__mixed_realistic_bell_lt0': '0ed2dd1582af'}"
    },
    "Optimization/config/whatif_config.py::PHASE2_THRESHOLD_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.4"
    },
    "Optimization/config/whatif_config.py::PILOT_RUN_DEFAULTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{**ERA_RUN_DEFAULTS, **INBOUND_ARRIVAL_REGIME, 'couple_channels': True, 'n_batches': CAMPAIGN_DEPTH_DAYS}"
    },
    "Optimization/config/whatif_config.py::SPECS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'single': {'ks': [1], 'losses': [0.0], 'zoning': [('off', {'enabled': False})], 'schedulers': ['round_robin'], 'arms': None, 'reference': 'k1_off'}, 'scheduler_ab': WHATIF, 'inbound_select': {'ks'..."
    },
    "Optimization/config/whatif_config.py::WHATIF": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'ks': [1], 'losses': [0.0], 'zoning': [('off', {'enabled': False})], 'schedulers': ['round_robin', 'lpt'], 'arms': 'all', 'reference': 'k1_off_rr'}"
    },
    "Optimization/config/whatif_config.py::_cells_naming": {
      "decorators": [],
      "doc": "The axis suffixes that name any of `policies`, for a message that points at CELLS \u2014\nwhich is what a reader has to go and edit \u2014 rather than at policy names.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec, policies) -> list"
    },
    "Optimization/config/whatif_config.py::_refuse_unrunnable_cells": {
      "decorators": [],
      "doc": "Refuse a spec whose DECLARED cells the run would decline at their first drain.\n\nTHE CLASS OF DEFECT (inbound-optimization 31, 33): two honest files that nothing\nchecks against each other.  `phase2_inbound_axis()` declared ten cells and\n`Inbound/site_space.py` refused two of them under coupling; both were right, both were\ntested, and the campaign carried two dead cells from the day site-dock closed until a\nprobe run paid its freeze and watched 24 units fail.  The shape reaches this spec\nregistry from two directions and both are checked here:\n\n* **The inbound axis** names a yard/dock policy that reads a `SpaceView` structure a\n  COUPLED composition does not carry (`uncomposable_policies`).  Conditional on\n  coupling: a single-leaf composition is the view by identity, so the same policy is\n  perfectly runnable uncoupled and refusing it there would be false.\n* **The arm axis** names a restock rule the gain evaluator has no faithful bundle for\n  (`FAITHFUL_GAIN_FAMILIES`), while some cell names a gain policy.  `rule_pairs` is a\n  HAND-COPIED artifact field and the artifact legitimately reports families that still\n  need `_gain_bundle_for` extended (`needs_bundle_extension`), so copying a ranking\n  before that extension lands is a reachable edit, not a typo.\n\nNeither is visible to the refusals above it: `validate_spec` reads the spec's SHAPE,\nand both of these are properties of what the named POLICY and the named RULE can do.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec, name: str) -> None"
    },
    "Optimization/config/whatif_config.py::_rule_pairs": {
      "decorators": [],
      "doc": "Normalise a declared rule-pair list to a tuple of `(store_rule, fulfillment_rule)`,\nraising ValueError on every shape a hand-edited spec can reach.\n\n`where` names the spec in the message, because the reader is looking at a registry of\nseven and needs to be told which one they broke.",
      "is_async": false,
      "kind": "function",
      "signature": "(pairs, where: str) -> tuple[tuple[str, str], ...]"
    },
    "Optimization/config/whatif_config.py::channel_restocks_for": {
      "decorators": [],
      "doc": "The per-channel restock subsets a spec installs into `strategies.CHANNEL_RESTOCKS`,\nkeyed by channel name \u2014 or None meaning \"leave it exactly as committed\".\n\n`channels` is the run's channel names (the driver passes `CONFIG['channels']`), because\nwhich channels EXIST is a property of the catalogue, not of the spec.\n\nFour shapes, and the last is the new one:\n\n  * `arms` absent/None  -> None.  The committed per-channel subsets stand untouched; this\n    is what makes `--spec single` the old flat run.\n  * `arms` = 'all'      -> {ch: None} \u2014 the full assignment-function suite everywhere.\n  * `arms` = (rules\u2026)   -> {ch: rules} \u2014 the SAME subset on every channel, which is the\n    only thing a flat arm set can say.\n  * `rule_pairs`        -> {'store': store column, 'fulfillment': fulfillment column},\n    each in RANK ORDER, derived from the pairing and never authored beside it.\n\nTHE COLUMNS ARE ORDERED, NOT SETS.  Site-dock 06 wrote the derivation as `{p[0] for p in\npairs}`; a set destroys rank, `strategies_for` hands the arm list to `_prepare_site_run` in\nexactly this order, and the zip there IS the pairing \u2014 so the one thing the derivation\nexists to preserve would be dropped on the way out.  Ordered tuples, and `_rule_pairs`\nrefuses the repeat that would make a column shorter than its pair list.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec, channels) -> dict | None"
    },
    "Optimization/config/whatif_config.py::get_spec": {
      "decorators": [],
      "doc": "Return the cell-matrix spec dict for a registered name (KeyError if unknown).\n\nShape-checked on the way out (`validate_spec`): `run_simulation._resolve_spec` catches the\nKeyError to print the choices, so a shape refusal is raised as a ValueError and propagates.",
      "is_async": false,
      "kind": "function",
      "signature": "(name)"
    },
    "Optimization/config/whatif_config.py::inbound_policies_of": {
      "decorators": [],
      "doc": "Every yard/dock policy the spec NAMES, distinct and in declaration order \u2014 from its\ninbound axis and its run defaults alike.\n\nTwo key spellings, because the two seams have two: an inbound axis entry states\n`yard_policy` (a `CONFIG['global']` `inbound_*` name with the prefix dropped, which is\nwhat `_inbound_axis` validates against), while `run_defaults` states the full\n`inbound_yard_policy`.  A gate that read only one of them would pass a spec that names\nits policy at the other seam.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec) -> tuple[str, ...]"
    },
    "Optimization/config/whatif_config.py::phase2_inbound_axis": {
      "decorators": [],
      "doc": "The ten-entry inbound axis for phase 2, as [(name_suffix, overrides), \u2026].\n\nEvery entry states EVERY key the axis touches, including the ones it is not exercising.\nThat is not verbosity: `_apply_cell` mutates a process-wide CONFIG that is never reset\nbetween cells, so a key one cell writes and the next omits leaves the second running the\nfirst's policy under its own name.  `_inbound_axis` refuses a partial entry for exactly\nthis reason, and stating the full record here is how that refusal is satisfied.\n\nThe arrival regime rides the axis rather than the command line for the same reason: the\ninbound-OFF anchor must ALSO turn the lead spread off, because `inbound_spec()` refuses a\nspread without the standing yard \u2014 and it refuses it ABOVE the trailer-type early return,\nso an anchor inheriting a run-level `--inbound-lead-spread` would raise instead of running.\nCarrying the regime here makes the spec the whole experimental condition, reproducible\nwithout a remembered command line.\n\nTHE ANCHOR IS NOT A CROSS-PHASE VALIDITY CHECK, and saying so here was false (site-dock 06\nsection 3, which rewrote this paragraph rather than amending it \u2014 a stale comment claiming a\nguarantee the campaign does not have is worse than no comment).  EVERY cell of phase 2 runs\nthe COUPLED site model, `inb_off` included, because coupling is a property of the RUN and is\nDECLARED (`--couple-channels`, carried by `PHASE2_RUN_DEFAULTS`), never inferred from the\ninbound flag.  So `inb_off` is the inbound-OFF POLE INSIDE the coupled model \u2014 the control\nfor \"does running an inbound pipeline at all change the answer, versus which policy runs it\",\nand the only cell with no yard, which makes it the structural zero for every yard quantity.\n\nWhat that trades away is the comparison with phase 1, and the trade is deliberate.  Phase 1\nis UNCOUPLED and, since department-calibration decision 11, inbound-ON, so it ranks\nplacement arms under 2x the site put labour: an\nuncoupled leaf is handed the WHOLE derived site crew, twice over (site-dock 19 measured the\nbreak).  Under the old leaf model `inb_off` was comparable with phase 1 but confounded\nagainst its own nine siblings; coupling every cell inverts that, and the matrix's own deltas\nare what the campaign publishes, so that is the side kept whole.  The asymmetry is not a\nclean scaling either \u2014 what doubles is put and receiving CAPACITY, not labour seconds, and\nunder drain-or-cap a larger crew cuts less and completes more per day by an amount that\ndiffers per arm \u2014 so a RANK comparison across the boundary is no more invariant than an\nhours one.  The caveat is carried as a STAMP (`restock_selection.json`'s `put_regime:\n'per-leaf'`) and published with the campaign; there is no runtime gate because nothing in\nthe codebase reads two run roots, so there is no join to gate.  The one reachable mistake \u2014\npointing the selector at a coupled root \u2014 is refused by `run_restock_selection.select`.\n\nThe reference is `fifo`, so every delta reads \"versus FIFO\", which is the campaign's own\nquestion.  The anchor is present as a cell, never the reference.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, threshold_days=PHASE2_THRESHOLD_DAYS, lead_minutes=PHASE2_LEAD_MINUTES, lead_spread=PHASE2_LEAD_SPREAD, doors=PHASE2_DOCK_DOORS, door_team=PHASE2_DOOR_TEAM, finite_w=PHASE2_FINITE_W, h_multiples=PHASE2_H_MULTIPLES)"
    },
    "Optimization/config/whatif_config.py::phase2_inbound_axis._policy": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name, **over)"
    },
    "Optimization/config/whatif_config.py::rule_pairs_of": {
      "decorators": [],
      "doc": "The spec's declared rule pairs, normalised \u2014 or None when it declares none.\n\nShape-checked on the way out, so no caller can read a half-valid pair list.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec) -> tuple[tuple[str, str], ...] | None"
    },
    "Optimization/config/whatif_config.py::swept_rules_of": {
      "decorators": [],
      "doc": "The restock rules a run SWEEPS, flat \u2014 `run_layout.json`'s `arms` field.  None means\n\"the committed suite, whatever that is\", which is what a spec declaring no arm set says.\n\nA rule-pair spec has to answer this too, and `None` would be a lie: the descriptor would\nclaim the run swept the full 34-arm suite while it swept twelve rules in a diagonal.  The\nunion, in first-seen order (the store column's ranks, then whatever the fulfillment column\nadds), is the honest flat answer; the PAIRING itself lives in the spec, not the descriptor.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec) -> list | None"
    },
    "Optimization/config/whatif_config.py::validate_spec": {
      "decorators": [],
      "doc": "Refuse a cell-matrix spec that cannot mean, or cannot DO, what it says.  ValueError.\n\nCalled from `get_spec`, which is the single door every run comes through \u2014 so this fires\nbefore the run directory exists, before the catalogue is read and before a worker is\nforked.  Cheap, total, and pure: it reads the spec against declared registries and\nnothing else \u2014 the rule grid, and (since inbound-optimization 33) the two that say what\na named policy and a named rule can actually be run with, never the simulation.\n\nThe shape refusals come first and `_refuse_unrunnable_cells` last; both kinds guard the\nsame thing, a run that completes, publishes, and measures something nobody chose \u2014 or,\nfor the last one, one that pays its freeze and then dies at its first drain.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec, name: str='<spec>') -> None"
    },
    "Optimization/gpu/__init__.py": {
      "decorators": [],
      "doc": "Optimization.gpu \u2014 GPU offload prototypes (auction solver, broker, client shim).\n\nExperimental: not wired into the production pipeline; exercised only by\nTests/gpu/ (correctness tests + benches).  See docs/gpu_assessment.md and\ndocs/gpu_auction_assessment.md for the findings.",
      "kind": "module",
      "signature": null
    },
    "Optimization/gpu/gpu_auction.py": {
      "decorators": [],
      "doc": "gpu_auction.py \u2014 parallel (Jacobi) auction solver for the placement linear-assignment problem.\n\nPROTOTYPE (no production wiring).  Models one placement wave as a linear assignment of U units to\nB>=U bins minimising total cost, and solves it with Bertsekas' auction algorithm in parallel form:\nall unassigned units bid at once for their best bin; a contested bin keeps the highest bidder and\nRAISES ITS PRICE; displaced units re-bid against the new prices.  Prices only ever rise and the total\nrise is bounded, so it terminates \u2014 resolving bin clashes WITHOUT the eviction cascade a naive\nparallel swap would create.  The bid step is a row-wise top-2 over the U x B cost matrix, which is the\nGPU-parallel reduction we want.\n\nWhat's here:\n  * build_static_cost(...)  \u2014 the exact bilinear placement cost `f_s*(M_b*(intercept+v_s) + D_b)`\n    (same form as inventory_optimal.py's scipy LAP); a pure U x B matrix.\n  * auction_assign(...)     \u2014 numpy + torch backends, tiled top-2 (peak memory bounded by tile, not\n    B), eps-scaling, deterministic index tie-breaks.  Returns assign[U] (bin per unit) + prices.\n  * greedy_assign(...)      \u2014 sequential argmin-and-consume baseline (the structure the production\n    greedy uses) for a quality reference.\n  * auction_place_wave(...) \u2014 folds the path-dependent AFFINITY reward in via a few-round fixed point\n    (solve -> update aisle memberships -> re-solve), the linear relaxation of the quadratic objective.\n\nThe auction MINIMISES total cost optimally (within U*eps); it is a *replacement* for the sequential\ngreedy (re-baselined), not a bit-for-bit mimic.",
      "kind": "module",
      "signature": null
    },
    "Optimization/gpu/gpu_auction.py::_TILE_BINS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "32768"
    },
    "Optimization/gpu/gpu_auction.py::_auction_phase_numpy": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(benefit, price, assign, owner, eps, tile, max_rounds)"
    },
    "Optimization/gpu/gpu_auction.py::_auction_phase_torch": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(torch, benefit, price, assign, owner, eps, tile, max_rounds)"
    },
    "Optimization/gpu/gpu_auction.py::_top2_numpy": {
      "decorators": [],
      "doc": "Per-row (rows = un) running top-2 of (benefit[un] - price) over B columns, in tiles.\nReturns (best_idx[n], best_val[n], second_val[n]).",
      "is_async": false,
      "kind": "function",
      "signature": "(benefit, un, price, tile)"
    },
    "Optimization/gpu/gpu_auction.py::_top2_torch": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(torch, benefit, un, price, tile)"
    },
    "Optimization/gpu/gpu_auction.py::affinity_correction": {
      "decorators": [],
      "doc": "correction[u,b] = -lam * sum_{partners p placed in bin b's aisle} L[u,p].  Built as L @ M\nwhere M[p,a]=1 if unit p is assigned to aisle a (a dense matmul \u2014 GPU-friendly).",
      "is_async": false,
      "kind": "function",
      "signature": "(L, lam, assign, aisle_of_bin, n_aisles)"
    },
    "Optimization/gpu/gpu_auction.py::assignment_cost": {
      "decorators": [],
      "doc": "Total cost of an assignment (assign[u] = bin index, or -1 for unassigned).",
      "is_async": false,
      "kind": "function",
      "signature": "(cost, assign)"
    },
    "Optimization/gpu/gpu_auction.py::auction_assign": {
      "decorators": [],
      "doc": "Dispatch: 'numpy', 'torch', or 'auto' (torch+CUDA if available, else numpy).",
      "is_async": false,
      "kind": "function",
      "signature": "(cost, backend='auto', **kw)"
    },
    "Optimization/gpu/gpu_auction.py::auction_assign_numpy": {
      "decorators": [],
      "doc": "Optimal (within U*eps) assignment minimising total cost via the parallel auction (single eps).\n\nCORRECT reference, but SLOW on realistic *structured* costs: many bins are near-tied for a unit, so\nthe eps-sized price increments take ~1e5 rounds (measured ~193k rounds / 23s at U=200,B=2000).\neps-scaling would cut rounds but a correct *rectangular* (B>U) eps-scaling is non-trivial (a coarse\nphase inflates used-bin prices, then unused price-0 bins look best and units flee to them).  This is\nthe central reason the prototype's verdict is NO-GO vs scipy.linear_sum_assignment (which solves the\nidentical problem optimally in ~0.04-0.09s) \u2014 see docs/gpu_auction_assessment.md.",
      "is_async": false,
      "kind": "function",
      "signature": "(cost, eps=None, tile=_TILE_BINS, max_rounds=2000000)"
    },
    "Optimization/gpu/gpu_auction.py::auction_assign_torch": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cost, eps=None, tile=_TILE_BINS, max_rounds=1000000, device='cuda')"
    },
    "Optimization/gpu/gpu_auction.py::auction_place_wave": {
      "decorators": [],
      "doc": "Fixed-point: solve static LAP, then re-solve with the affinity correction implied by the\ncurrent assignment, a few rounds.  Returns (assign, telemetry) where telemetry lists per-round\n(changed_units, objective).",
      "is_async": false,
      "kind": "function",
      "signature": "(static_cost, L, lam, aisle_of_bin, rounds=4, backend='numpy', **kw)"
    },
    "Optimization/gpu/gpu_auction.py::build_static_cost": {
      "decorators": [],
      "doc": "Static per-(unit,bin) placement cost matrix, shape (U, B):\n    cost[u,b] = f[u] * ( M[b]*(intercept + v[u]) + D[b] )\nf,v are per-unit (U,); D,M are per-bin (B,).  Mirrors Assignment_Functions._ranked_minlabor_impl\n/ inventory_optimal._optimal_work_assign (minus the path-dependent affinity terms).",
      "is_async": false,
      "kind": "function",
      "signature": "(f, v, D, M, intercept)"
    },
    "Optimization/gpu/gpu_auction.py::greedy_assign": {
      "decorators": [],
      "doc": "Sequential argmin-and-consume baseline (the production greedy's structure on a static cost):\nprocess units in `order` (default: row index), each takes its lowest-cost still-free bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(cost, order=None)"
    },
    "Optimization/gpu/gpu_auction.py::wave_objective": {
      "decorators": [],
      "doc": "Full wave objective to MINIMISE: total static cost minus lam * affinity reward earned by\nco-aisle partners.  L[u,p] = (lift(u,p)-1)*f_p (>=0), aisle_of_bin[b] = bin b's aisle id.",
      "is_async": false,
      "kind": "function",
      "signature": "(static_cost, L, lam, assign, aisle_of_bin)"
    },
    "Optimization/gpu/gpu_broker.py": {
      "decorators": [],
      "doc": "gpu_broker.py \u2014 one process, one CUDA context: the shared GPU server for placement argmin.\n\nCPU workers (gpu_client.GpuClient) send (request_id, response_queue, v, D, M, intercept, tile,\nest_bytes) over a Manager request queue.  The broker admits each request against a measured VRAM\nbudget (so concurrency trades off against per-job size), runs the tiled cost-matrix argmin on the\nGPU across a small thread+stream pool, and returns (request_id, idx | None).  EVERY GPU op is\nwrapped so a bad/oversized request degrades to None (the client then computes on CPU) \u2014 the broker\nprocess never dies, so an unattended run can't be taken down by GPU trouble.\n\ntorch is the broker backend (own process, single context \u2014 no cupy/torch nvrtc ordering concern).",
      "kind": "module",
      "signature": null
    },
    "Optimization/gpu/gpu_broker.py::SHUTDOWN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/gpu/gpu_broker.py::_HEADROOM_BYTES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "512 * 1024 * 1024"
    },
    "Optimization/gpu/gpu_broker.py::broker_main": {
      "decorators": [],
      "doc": "Broker entry point (run in its own process).  `status` is a Manager dict; we set\nstatus['ready']/'budget'/'error' so the parent can confirm startup.",
      "is_async": false,
      "kind": "function",
      "signature": "(request_q, status, vram_frac=0.6, max_inflight=8, log_q=None)"
    },
    "Optimization/gpu/gpu_broker.py::broker_main._handle": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rid, resp_q, v, D, M, intercept, tile, est, sidx)"
    },
    "Optimization/gpu/gpu_broker.py::broker_main._release": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(est)"
    },
    "Optimization/gpu/gpu_broker.py::broker_main._reserve": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(est, deadline=2.0)"
    },
    "Optimization/gpu/gpu_broker.py::compute_budget": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(torch, vram_frac: float) -> int"
    },
    "Optimization/gpu/gpu_broker.py::gpu_argmin_torch": {
      "decorators": [],
      "doc": "Tiled cost-matrix argmin on the GPU (mirror of gpu_client._argmin_tiled).  Peak working\nset ~ U*tile, so VRAM is bounded by the tile, not C.  Returns int64 idx[U] on the host.",
      "is_async": false,
      "kind": "function",
      "signature": "(torch, v, D, M, intercept, tile, stream=None)"
    },
    "Optimization/gpu/gpu_broker.py::start_broker": {
      "decorators": [],
      "doc": "Start the broker process.  Returns (proc, request_q, status) or (None, None, status)\nwith status['error'] set if the GPU is unavailable.  Caller passes request_q to workers.",
      "is_async": false,
      "kind": "function",
      "signature": "(manager, vram_frac=0.6, max_inflight=8)"
    },
    "Optimization/gpu/gpu_broker.py::stop_broker": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(proc, request_q)"
    },
    "Optimization/gpu/gpu_client.py": {
      "decorators": [],
      "doc": "gpu_client.py \u2014 worker-side GPU offload shim + the shared tiled cost-matrix kernel.\n\nThe placement drain scores, for a wave of U queued units over C candidate bins of one BinKey,\nthe static marginal cost  cost[u,b] = v[u]*M[b] + (M[b]*intercept + D[b])  and takes argmin over\nbins per unit.  This module owns:\n\n  * `estimate_bytes(U, C, tile)` \u2014 the allocation calculator (admission + startup ceiling).\n  * `cpu_argmin(...)`           \u2014 the numpy CPU reference / fallback (tiled; bounded memory).\n  * `GpuClient` / `placement_argmin(...)` \u2014 offload to the broker; returns None \u21d2 caller does CPU.\n\n`placement_argmin` NEVER raises and NEVER blocks indefinitely: a missing/over-budget/timed-out/\nerrored broker yields None so the worker silently runs the CPU path \u2014 an unattended run can't\ncrash on GPU trouble.  The broker (gpu_broker.py) reuses the same tiling math on the GPU.",
      "kind": "module",
      "signature": null
    },
    "Optimization/gpu/gpu_client.py::DEFAULT_TILE_BINS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "32768"
    },
    "Optimization/gpu/gpu_client.py::GpuClient": {
      "decorators": [],
      "doc": "Talks to the GPU broker over Manager queues.  Constructed in the pool initializer;\n`placement_argmin` returns None on any trouble so the caller falls back to CPU.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/gpu/gpu_client.py::GpuClient.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, request_q, response_q, budget_bytes: int, timeout: float=5.0)"
    },
    "Optimization/gpu/gpu_client.py::GpuClient.placement_argmin": {
      "decorators": [],
      "doc": "Offload one wave's argmin to the broker.  Returns int64 idx[U] or None (do CPU).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, v, D, M, intercept, tile: int=DEFAULT_TILE_BINS)"
    },
    "Optimization/gpu/gpu_client.py::_CLIENT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "Optimization/gpu/gpu_client.py::_ITEMSIZE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "8"
    },
    "Optimization/gpu/gpu_client.py::_TEMP_FACTOR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3.0"
    },
    "Optimization/gpu/gpu_client.py::_argmin_tiled": {
      "decorators": [],
      "doc": "Backend-generic tiled argmin (numpy or torch via the passed callables).\ncost[u,b] = v[u]*M[b] + M[b]*intercept + D[b];  returns int idx[U] of the min bin per unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(v, D, M, intercept, tile, xp, argmin, full, arange, where)"
    },
    "Optimization/gpu/gpu_client.py::cpu_argmin": {
      "decorators": [],
      "doc": "Numpy CPU reference / fallback.  v,D,M are 1-D float64 arrays; returns int64 idx[U].",
      "is_async": false,
      "kind": "function",
      "signature": "(v, D, M, intercept, tile: int=DEFAULT_TILE_BINS)"
    },
    "Optimization/gpu/gpu_client.py::estimate_bytes": {
      "decorators": [],
      "doc": "Worst-case device bytes for a tiled argmin of a U x C cost matrix.\nPeak working set ~ U*min(C,tile) (the live chunk + temps) + the C-sized D/M/base vectors.",
      "is_async": false,
      "kind": "function",
      "signature": "(u: int, c: int, tile: int=DEFAULT_TILE_BINS) -> int"
    },
    "Optimization/gpu/gpu_client.py::get_client": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/gpu/gpu_client.py::placement_argmin": {
      "decorators": [],
      "doc": "Module-level convenience: GPU via the active client, else CPU.  Always returns idx[U].",
      "is_async": false,
      "kind": "function",
      "signature": "(v, D, M, intercept, tile: int=DEFAULT_TILE_BINS)"
    },
    "Optimization/gpu/gpu_client.py::set_client": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(client)"
    },
    "Optimization/metrics/Simulation_Analytics.py": {
      "decorators": [],
      "doc": "Simulation_Analytics.py \u2014 per-batch and per-task statistics from PickSimulation events.\n\nPublic API\n----------\navg_concurrent_pickers(events)\n    Time-weighted average number of pickers simultaneously in \"picking\" state.\n\n    Aggregate picking vs traveling fractions across all pickers.\n\nextract_batch_stats(events, batch_id, k_pickers, run_id)\n    Summarise one PickSimulation run into a BatchStats record.\n\nextract_task_stats(events, tasks, batch_id, affinity, wp, run_id)\n    Extract per-aisle TaskStats from one PickSimulation run.\n\nflag_batch_outliers(stats, iqr_factor)\n    IQR outlier detection on batch duration.\n\nflag_task_outliers(stats, iqr_factor)\n    IQR outlier detection on task duration.\n\nbuild_placed_affinity(warehouse, inventory, max_per_group)\n    Sparse lift matrix for only the SKUs that have a physical bin.\n\nsum_lift(skus, affinity)\n    Demand-weighted co-occurrence helper for a dict-based lift matrix (AffinityStore\n    objects carry their own faster .sum_lift; this is the dict fallback).",
      "kind": "module",
      "signature": null
    },
    "Optimization/metrics/Simulation_Analytics.py::_group_events_by_picker": {
      "decorators": [],
      "doc": "Partition events into per-picker lists in a single O(N) pass.\n\nPick.py generates events in time order within each picker's timeline so\nno sort is needed \u2014 the per-picker lists are already time-ordered.\n\nAn actor outside `[0, k_pickers)` RAISES.  It used to be dropped silently, and\nevery statistic downstream \u2014 `total_items`, `task_makespan`, the picking split \u2014\nwould then report a smaller crew's numbers as if they were the whole batch's.\nThat is the precise failure a second work stream produces: a putter, an unloader\nor any actor from another crew carries an id from a different space, and the\n`0 <= pid < k_pickers` test is exactly the shape that swallows it.\n\nThis function's id space is DENSE and PER-CREW by contract \u2014 `assign_tasks`\npartitions into `k_pickers` buckets and the bucket index is the id, so a\nconforming pick run can never trip this.  A globally-unique actor id belongs on\nthe merged event stream, not here; see `_ProgressAPIMixin.progress_at`, which\nenumerates the same `range(num_pickers)` and is the other half of the contract.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list, k_pickers: int) -> list[list]"
    },
    "Optimization/metrics/Simulation_Analytics.py::_pick_lines": {
      "decorators": [],
      "doc": "(weight, volume, qty, y_phys) per bin stop that has inventory for this task.\ny_phys lets aisle_workload apply the height-bracket handling multiplier.\n\nPrefer the snapshot captured at task build (Task.pick_lines) \u2014 the live path bins are\ndepleted by the sim's pick application before extract_task_stats runs, which would\notherwise drop emptied bins and zero out W.  Falls back to the live read for any task\nobject that predates the snapshot.",
      "is_async": false,
      "kind": "function",
      "signature": "(task) -> list[tuple[int, int, int, float]]"
    },
    "Optimization/metrics/Simulation_Analytics.py::_picker_time_breakdown_grouped": {
      "decorators": [],
      "doc": "Compute picking/traveling breakdown from pre-grouped picker event lists.\n\nOFFSET-INVARIANT: a picker contributes the SPAN from its own first event to its\n`done`, not the `done` timestamp.  The two are the same number only while every\npicker's clock restarts at zero every batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(grouped: list[list]) -> dict[str, float]"
    },
    "Optimization/metrics/Simulation_Analytics.py::avg_concurrent_pickers": {
      "decorators": [],
      "doc": "Time-weighted average number of pickers simultaneously in 'picking' state.\n\nA picker is \"picking\" from the moment they arrive at a bin (arrive event)\nuntil they finish handling it (pick event).  Cart-swap overhead is folded\ninto that interval because both arrive and pick share the same pick_time span.\n\nAlgorithm\n---------\nBuild a (time, +1/-1) change-list, sort it, then sweep to integrate the\nconcurrent-count curve.  Divide by total simulation span.\n\nOFFSET-INVARIANT.  The integration window is `[t_start, t_end]` measured from the\nbatch's own first event, not from absolute zero \u2014 so a batch whose pickers start at\nan absolute instant scores exactly what the same batch scores starting at zero.  It\nused to anchor `prev_t` and the divisor at a literal `0.0`, which is the same number\nonly while every picker's clock is reborn each batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list) -> float"
    },
    "Optimization/metrics/Simulation_Analytics.py::build_placed_affinity": {
      "decorators": [],
      "doc": "Build a sparse lift matrix covering only placed SKUs.\n\nCalling inventory.affinity_matrix() on 50 000+ SKUs with 5 lift groups\nof ~10 000 each generates ~250 M pairs \u2014 impractical.  This function caps\neach lift group at max_per_group placed SKUs, yielding a bounded dict\n(5 * C(300,2) * 2 \u2248 450 k entries at the default) while still producing\nnon-zero lift_sums for tasks that contain two or more eligible SKUs.\n\nParameters\n----------\nwarehouse      : built Warehouse object (bins already stocked)\ninventory      : Inventory object whose orders have .sku and .lift_group\nmax_per_group  : SKUs with affinity per lift group; higher \u2192 denser lift\n                 coverage but more memory (~150 MB at 1 000 per group)",
      "is_async": false,
      "kind": "function",
      "signature": "(warehouse, inventory, max_per_group: int=300) -> dict"
    },
    "Optimization/metrics/Simulation_Analytics.py::build_pre_snapshot": {
      "decorators": [],
      "doc": "Capture bin quantities before a batch simulation runs.\n\nCall immediately after check_reorders() and before DeferredPickSimulation: the instant it\ndescribes is \"start of this batch, after the restock pass, before any pick\".\n\nReturns a dict keyed by id(bin).  Two consumers, both live:\n  - the keyframe writer (`save_bin_keyframe`) \u2014 the independent audit of the log;\n  - the per-batch conservation ledger in `strategy_runner`, which sums `pre_qty`.\n\nIts third consumer, `snapshot_bin_inventory`, is GONE \u2014 `bin_inventory` was pure redundancy\nonce `bin_placement` + `bin_eviction` + `picks` existed, and is no longer written.\n\nOnly non-empty bins are captured; empty bins are implicitly quantity=0 and are not written.",
      "is_async": false,
      "kind": "function",
      "signature": "(manager) -> dict"
    },
    "Optimization/metrics/Simulation_Analytics.py::expected_task_labor": {
      "decorators": [],
      "doc": "E[task labor] for a placement, scored ANALYTICALLY from the realised tasks.\n\nThe optimization objective is the expected labor of a randomly-assigned task =\nmean over tasks of W = D + P + C (see Workload.aisle_workload).  Computed straight\nfrom each Task's path/items via the same bracket-aware formula the sim bills, so it\nis independent of sim wall-timing (queue waits, parallelism, reorder starvation).\n\nReturns {'objective': mean W, 'handling': mean P, 'travel': mean (D+C), 'n_tasks': n}.\n'travel' folds the cart-swap term C in with travel D since both are placement/route\neffects rather than per-unit handling; 'handling' is the placement-modulated P term.",
      "is_async": false,
      "kind": "function",
      "signature": "(tasks: list, wp: WorkloadParams) -> dict"
    },
    "Optimization/metrics/Simulation_Analytics.py::extract_batch_stats": {
      "decorators": [],
      "doc": "Summarise one PickSimulation.run() result into a BatchStats record.\n\nduration      : BATCH MAKESPAN \u2014 first picker starting to last finishing (parallel wall-clock)\ntask_makespan : \u03a3 per-picker spans = total labor (the serial / single-picker makespan)\nnum_tasks     : unique aisles that received a task_start\ntotal_items   : sum of items_picked from each picker's done event\nthr_batch     : throughput / batch makespan = total_items / duration\nthr_task      : throughput / task makespan  = total_items / task_makespan\navg_concurrent_pickers : time-weighted mean (see avg_concurrent_pickers)\npicking/traveling pct  : aggregate fractions (see _picker_time_breakdown_grouped)\n\ntask_makespan == \u03a3 task_stats.duration by construction: a picker accrues\ntravel+pick+cart-swap back-to-back with no idle gaps, so the span from its first event to\nits done telescopes to the sum of its task durations; summing over pickers gives the\nbatch's total task time.\n\nOFFSET-INVARIANT.  Both `duration` and `task_makespan` are SPANS measured from a start\ninstant, never raw timestamps.  They read the same while every picker's clock is reborn at\nzero each batch \u2014 which is exactly the assumption a shared clock removes.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list, batch_id: int, k_pickers: int, run_id: int=0) -> BatchStats"
    },
    "Optimization/metrics/Simulation_Analytics.py::extract_picker_events": {
      "decorators": [],
      "doc": "Convert a PickSimulation event list into PickerEventRecord rows for DB storage.\n\nEvery event is stored verbatim so the full picker timeline can be replayed\nfrom SQL.  Location (bayX, bayY) is populated for arrive / cart_swap / pick\nevents; it is NULL for task_start, task_end, and done events.\n\nVisualization query \u2014 state of all pickers at sim-time t in batch b:\n\n    SELECT picker_id, aisle_id, bayX, bayY, event_type, items_picked\n    FROM   picker_events\n    WHERE  run_id = ? AND batch_id = ? AND time <= ?\n    GROUP  BY picker_id\n    HAVING time = MAX(time)\n    ORDER  BY picker_id;",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list, batch_id: int, run_id: int=0) -> list"
    },
    "Optimization/metrics/Simulation_Analytics.py::extract_picks": {
      "decorators": [],
      "doc": "Extract one PickRecord per 'pick' event from the picker event stream.\n\nProvides a compact, join-friendly picks table (run_id, batch_id, sku, qty,\nlocation, sim_time) without the full event stream overhead of picker_events.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list, batch_id: int, run_id: int=0) -> list[PickRecord]"
    },
    "Optimization/metrics/Simulation_Analytics.py::extract_task_stats": {
      "decorators": [],
      "doc": "Extract per-aisle TaskStats from one PickSimulation.run() result.\n\nduration         : task_end.time - task_start.time for this aisle\nW              : analytical workload via aisle_workload()\nlift_sum         : sum_lift for SKUs present in this task\nnum_bins_visited : bins on the task path -- the PLANNED visit count\ntotal_items      : sum of quantities across this aisle's SKUs -- the PLAN\nitems_realized   : items actually picked in this aisle\nbins_realized    : bins actually picked from\n\nThe last two are read off the event stream rather than off the task, because the task\nonly ever knows the plan.  They diverge from it whenever the live-stock clamp bites --\nboth loops take `min(task.planned[i], what the bin holds)` -- and a truncated task would\notherwise report a full workload against partial time.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list, tasks: list, batch_id: int, affinity, wp: WorkloadParams, run_id: int=0, lift_cache: dict | None=None) -> list[TaskStats]"
    },
    "Optimization/metrics/Simulation_Analytics.py::flag_batch_outliers": {
      "decorators": [],
      "doc": "Return new list with is_outlier set via Tukey IQR fences on duration.",
      "is_async": false,
      "kind": "function",
      "signature": "(stats: list[BatchStats], iqr_factor: float=1.5) -> list[BatchStats]"
    },
    "Optimization/metrics/Simulation_Analytics.py::flag_task_outliers": {
      "decorators": [],
      "doc": "Return new list with is_outlier set via Tukey IQR fences on duration.",
      "is_async": false,
      "kind": "function",
      "signature": "(stats: list[TaskStats], iqr_factor: float=1.5) -> list[TaskStats]"
    },
    "Optimization/metrics/Simulation_Analytics.py::fused_pre_snapshot": {
      "decorators": [],
      "doc": "One pass over the occupied bins: occupancy always, keyframe rows only on demand.\n\nThe fused replacement for `build_pre_snapshot` on the strategy_runner hot path.  That\nfunction materialises an id(bin)-keyed dict of ~400k row dicts EVERY batch (measured\n96 MiB/batch of churn at 8k SKUs), yet on a non-keyframe batch \u2014 the common case \u2014\nits only consumer is the conservation ledger's `sum(pre_qty)`.  This walks\n`manager._unavailable.values()` once at the same instant (start of batch, after the\nrestock pass, before any pick) and returns\n\n    (occupancy, rows)   \u2014 occupancy: \u03a3 quantity over occupied bins (the ledger term);\n                          rows: `save_bin_keyframe`-shaped dicts, or None unless\n                          `want_rows` (keyframe batches only).\n\nRow order is the `_unavailable` dict's iteration order minus the `storage is None`\nskip \u2014 the SAME order `build_pre_snapshot` produced, so keyframe rowids are\nunchanged.  `unit_type` comes from the storage UNIT's class and `storage_size` from\nthe BIN's tier, exactly as before (a FulfillmentBin holding a Singleton is\n'singleton'; the bin's tier is what the viewer bins by).\n\n`build_pre_snapshot` itself stays: Tests/integration/test_visualization_data.py\nreads `bin_ref` and post-mutation `pre_qty` off the materialised dict, and the\narchived bin_inventory writer it documents is a never-improve.",
      "is_async": false,
      "kind": "function",
      "signature": "(manager, want_rows: bool) -> tuple[int, list[dict] | None]"
    },
    "Optimization/metrics/Simulation_Analytics.py::snapshot_aisle_metrics": {
      "decorators": [],
      "doc": "Capture the trip-cost equation state for every aisle in the manager.\n\nCall once per batch after check_reorders() and before picks \u2014 the same\nmoment as build_pre_snapshot() \u2014 so the snapshot reflects the current\nwarehouse layout after any reorder placements.\n\nFields captured\n---------------\nn_skus     : len(_aisle_sku_sets[aid])            \u2014 unique SKUs in aisle\nn_bins     : sum(_aisle_sku_counts[aid].values())  \u2014 occupied bin count\ndemand_sum : _aisle_demand_sum[aid]               \u2014 \u03a3 f_i * q_i secondary score\n\nStrategy A note\n---------------\nFor uniform placement (no affinity), _aisle_sku_sets / _aisle_demand_sum\nare never populated.  Only aisles touched by B/C reorders\nwill have non-zero values.  Strategy A produces no rows here by design \u2014\nit has no structured placement state to track.",
      "is_async": false,
      "kind": "function",
      "signature": "(manager: object, batch_id: int, run_id: int=0) -> list"
    },
    "Optimization/metrics/Simulation_Analytics.py::sum_lift": {
      "decorators": [],
      "doc": "Sum of pairwise lift over ordered pairs (i, j), i != j, in `skus`.",
      "is_async": false,
      "kind": "function",
      "signature": "(skus: list[int], affinity: dict) -> float"
    },
    "Optimization/metrics/Simulation_Analytics.py::task_time_breakdown": {
      "decorators": [],
      "doc": "Decompose picker time into (travel, handling, other) from a picker-event stream.\n\nEach picker's timeline is walked in time order; the gap *ending* at an event is\ncharged by that event's type: a gap ending at 'arrive' is travel (move to a bin),\na gap ending at 'cart_swap' is travel too (return the full cart / fetch an empty one \u2014 a\nroute/depot cost, matching the analytical objective which folds cart into travel), a gap\nending at 'pick' is handling (= _pick_time, at-location only), everything else\n(task_start/end, done) is 'other' (\u22480 / inter-task).  Works on PickEvent objects or\nPickerEventRecord rows (both have .picker_id/.time/.event_type).\ntravel + handling \u2248 \u03a3 task durations (productivity hours).",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list) -> tuple[float, float, float]"
    },
    "Optimization/metrics/Simulation_Analytics.py::task_travel_axes": {
      "decorators": [],
      "doc": "The x-vs-y split of POSITIONAL travel (excludes non-axis cart_move), in seconds:\n(travel_x, travel_y) = (\u03a3 pick_x+nonpick_x, \u03a3 pick_y+nonpick_y).  Evidence for the\n'x drives fulfillment / y drives store' thesis.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list) -> tuple[float, float]"
    },
    "Optimization/metrics/Simulation_Analytics.py::task_travel_breakdown": {
      "decorators": [],
      "doc": "Sibling of task_time_breakdown that reads the EXPLICIT per-event travel decomposition\nstamped by the sim, returning (pick_travel, non_pick_travel, cart_move) in seconds.\n\n  pick_travel     = \u03a3 (pick_travel_x + pick_travel_y)      \u2014 inter-pick within-aisle sweep\n  non_pick_travel = \u03a3 (non_pick_travel_x + non_pick_travel_y)  \u2014 aisle entry (+ one-way exit)\n  cart_move       = \u03a3 cart_move                            \u2014 cart-swap seconds (non-pick)\n\nSo total travel = pick_travel + non_pick_travel + cart_move, matching the `travel` bucket of\ntask_time_breakdown (which folds cart into travel).  Also exposes the x/y split via\ntask_travel_axes().  Works on PickEvent objects or PickerEventRecord rows.  Unlike the\ngap-based task_time_breakdown, this is exact even across skipped (no-event) bins because the\nsim folds their travel into the next stamped event.",
      "is_async": false,
      "kind": "function",
      "signature": "(events: list) -> tuple[float, float, float]"
    },
    "Optimization/metrics/Workload.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/metrics/Workload.py::WorkloadParams": {
      "decorators": [
        "dataclass"
      ],
      "doc": "Coefficients that define how physical effort is estimated for one aisle.\n\nMirrors the relevant fields of PickConfig (Warehouse/picking/Pick.py) so the\nOptimization layer can compute W without importing simulation internals.\n\nFields\n------\nx_speed      : horizontal travel speed in ft/s (positions are inches)\ny_speed      : vertical travel speed in ft/s (positions are inches)\npick_intercept   : fixed overhead per P stop (one pick LINE), never per item\npick_per_item    : fixed charge per UNIT handled (cost_model.DEFAULT_PICK_PER_ITEM)\npick_weight_coef : time added per (weight \u00d7 quantity) unit\npick_volume_coef : time added per (volume \u00d7 quantity) unit\ncart_swap_coef   : penalty per additional cart needed beyond the first\n\nThe handling defaults are the KERNEL's, by reference \u2014 the same declaration\n`PickConfig` and `PutawayCost` read, so the mirror cannot drift.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/metrics/Workload.py::WorkloadParams.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/metrics/Workload.py::WorkloadParams.from_pick_config": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Build WorkloadParams from a PickConfig instance (duck-typed).",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, cfg: object) -> WorkloadParams"
    },
    "Optimization/metrics/Workload.py::WorkloadParams.speed": {
      "decorators": [
        "property"
      ],
      "doc": "These params' travel speeds as one value \u2014 the analytical mirror of\n`PickConfig.speed`, and equal to it whenever this was built `from_pick_config`.\n\nA property for the same reason: `x_speed`/`y_speed` stay fields because they are a\nserialization contract, and this is the derived view of them.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> SpeedProfile"
    },
    "Optimization/metrics/Workload.py::aisle_workload": {
      "decorators": [],
      "doc": "Estimate W = D + P + C, the total time (workload) for one aisle's picks.\nSee aisle_workload_components for the term definitions.",
      "is_async": false,
      "kind": "function",
      "signature": "(x_traversed: int, y_traversed: int, carts_required: int, pick_lines: list[tuple], params: WorkloadParams) -> float"
    },
    "Optimization/metrics/Workload.py::aisle_workload_components": {
      "decorators": [],
      "doc": "The (D, P, C) decomposition of one aisle's workload W = D + P + C.\n\n  D (travel)   = x_traversed * sec_per_inch(x_speed) + y_traversed * sec_per_inch(y_speed)\n                 (x_speed/y_speed are ft/s; x_traversed/y_traversed are inches walked)\n  P (handling) = \u03a3_stops  height_mult(y) * (intercept + qty * per_item + qty\n                                    * (weight_coef*ln(weight) + volume_coef*ln(volume)))\n  C (cart)     = cart_swap_coef * max(0, carts_required - 1)\n\nMirrors Warehouse/Pick._pick_time, including the height-bracket multiplier on the\nper-unit weight/volume handling.  Split out so callers can report the\nhandling-vs-travel breakdown of the analytical objective (see expected_task_labor).\n\nParameters\n----------\nx_traversed    : total bayX distance walked in the aisle (from Task)\ny_traversed    : total bayY distance walked in the aisle (from Task)\ncarts_required : number of carts the aisle's picks fill (from Task)\npick_lines     : one (weight, volume, qty[, y_phys]) tuple per pick stop\nparams         : WorkloadParams coefficients",
      "is_async": false,
      "kind": "function",
      "signature": "(x_traversed: int, y_traversed: int, carts_required: int, pick_lines: list[tuple], params: WorkloadParams) -> tuple[float, float, float]"
    },
    "Optimization/metrics/__init__.py": {
      "decorators": [],
      "doc": "Optimization.metrics \u2014 pick-cost and per-batch/per-task statistics over sim events.\n\nSits between the simulation and both consumers (the worker, which records stats, and the analysis\nsuite, which plots them) \u2014 which is why it is here and not under Performance_Evaluations.",
      "kind": "module",
      "signature": null
    },
    "Optimization/metrics/bin_recorder.py": {
      "decorators": [],
      "doc": "bin_recorder.py \u2014 capture every bin mutation the simulation makes.\n\nCloses the gap that made spatial reconstruction lossy. `bin_inventory` records picks and NEVER\nrestocks \u2014 `check_reorders()` runs before the pre-batch snapshot, so a restocked bin is already in\nit at its post-restock quantity and the `post_qty == pre_qty` skip drops it. Measured on a\nproduction arm: **0 rows** with `post_qty > pre_qty` against 20k-42k `reorder_placements` per\nbatch, so replaying the delta stream only ever decays \u2014 losing 59% of the warehouse within five\nbatches of a keyframe.\n\nWhat makes this complete\n------------------------\nBin state is `Aisle.Bin.storage` / `.storage.quantity`, and a repo-wide search for writes to\neither finds seven lines across five sites, one of them dead code:\n\n    Inventory_Management._execute_placement   `bin_.storage = unit`    -> PLACE  (recorded here)\n    Inventory_Management._execute_topup       `storage.quantity += n`  -> PLACE  (recorded here)\n    inventory_reorder.requeue_bin             `bin_.storage = None`    -> EVICT  (recorded here)\n    fast_pick.py / Pick.py                    qty -= / storage = None  -> PICK   (already `picks`)\n    Storage_Primitive.StorageCart             zero callers repo-wide   -> n/a\n\n`_execute_topup` is ADR-0003's own-bin rung and the only site that ADDS to an occupied bin;\nits rows carry `bin_state='occupied'`, every other PLACE row `'empty'`.\n\nSo PLACE + EVICT + PICK is complete **by construction**, and picks are already fully recorded \u2014\n`SUM(pre_qty - post_qty)` equals `SUM(picks.quantity)` exactly on every arm tested.\n`Tests/integration/test_bin_log_replay.py` checks that construction argument against a live\nsimulation: folding the log reproduces the warehouse's own bins at every batch and at every\nsim-time within a batch. `Tests/architecture/test_bin_mutation_sites.py` fails the build if a\nsixth mutation site ever appears.\n\nHow it attaches\n---------------\nBy rebinding three bound methods on the manager **instance** \u2014 no edits to `Warehouse/`, and a\nstrategy swapping `mgr.placement` cannot detach it, because `_execute_placement` is the chokepoint\nall three placement call sites reach through `self.`. Same technique as\n`Diagnostics/trace_lifecycle.py`.\n\nTime resolution\n---------------\n`(batch_id, seq)`, and that is the TRUE resolution rather than a compromise: `check_reorders()`\nruns entirely between one batch's picks and the next batch's simulation, so no finer ordering\nexists to lose. Picks keep their `sim_time`, so intra-batch animation stays exact.\n\nChecked at runtime, not only in tests\n-------------------------------------\n`units_placed` / `units_evicted` feed the per-batch conservation ledger in\n`Optimization/simdriver/strategy_runner.py`:\n\n    units_placed - units_evicted - units_picked  ==  units currently sitting in bins\n\non every batch of every real run. The completeness argument above is structural; that ledger is\nwhat turns it into something a production sweep re-proves for itself.",
      "kind": "module",
      "signature": null
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder": {
      "decorators": [],
      "doc": "Accumulates PLACE/EVICT records for one arm, drained by the checkpoint flush.\n\nRows are appended to plain lists and handed to the existing per-checkpoint flush, so this\nadds no new write path and no new connection.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, run_id: int)"
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.attach": {
      "decorators": [],
      "doc": "Wrap the two PLACE commit points and `requeue_bin` on this manager instance.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr) -> None"
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.attach._execute_placement": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit, bin_, *, source=None, score=None, score_rank=None, policy=None)"
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.attach._execute_topup": {
      "decorators": [],
      "doc": "ADR-0003's own-bin rung: a PLACE into a bin that was already occupied.\n\nShares `_place_seq` with `_execute_placement` deliberately -- both are places,\nthe sequence is what orders them within a batch, and two counters would make\n`(run_id, batch_id, seq)` collide on the primary key.  `score` stays NULL: no\npolicy chose this bin, the SKU's own on-hand did, and a zero would claim a\nplacement was scored perfectly when it was not scored at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, bin_, n, *, source=None, queue=None)"
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.attach.requeue_bin": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bin_, *args, **kwargs)"
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.begin_batch": {
      "decorators": [],
      "doc": "Roll onto a new batch.\n\nThe sequence counters are deliberately NOT reset here.  They used to be, and that was a\nsilent data-loss bug: initial stocking happens before the loop and records at\n`batch_id = 0`, then the loop's first `begin_batch(0)` restarted `seq` at 0 \u2014 so a batch-0\nreorder placement collided with an initial fill on the primary key\n`(run_id, batch_id, seq)` and `INSERT OR REPLACE` quietly dropped the initial row.  It\nwent unnoticed because no shipped arm reorders at batch 0.\n\nA run-scoped monotonic counter makes the collision structurally impossible while\npreserving every ordering property that matters: `seq` is still ascending within a batch,\nso `ORDER BY batch_id, seq` is still the application order.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch_id: int) -> None"
    },
    "Optimization/metrics/bin_recorder.py::BinRecorder.drain": {
      "decorators": [],
      "doc": "Hand over the accumulated rows and clear, mirroring the other checkpoint lists.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[list, list]"
    },
    "Optimization/metrics/work_events.py": {
      "decorators": [],
      "doc": "work_events \u2014 both work streams, as rows on one absolute axis.\n\nThe pick simulation and the put-away crew are simulated INDEPENDENTLY and merged\nafterwards. That is the model the user specified \u2014 *\"assume non-intersection or\nnon-interference among physical objects, so simulated on merge similar to fast_pick\nsimulating sequential inventory access\"* \u2014 and it is the same shape `fast_pick` already\nuses across its own pickers: phase 1 runs each actor against a snapshot, phase 2 reconciles.\nOne level up, the actors are crews rather than pickers.\n\nSo this module does not schedule anything. It takes what each stream produced and stamps\nboth onto one timeline:\n\n    pick rows   from the batch's `PickEvent`s, whose `.time` is already absolute\n                (the arm hands every picker the batch epoch as its start)\n    put rows    from `Inventory_Manager.drain_putaway_records()`, whose clock is the put\n                crew's own and is offset onto the axis here\n\n## Two things that are decisions, not details\n\n**`qty` is signed.** A pick removes inventory and a put adds it, so one row shape reads in\nboth directions and `SUM(qty)` over a bin is its net movement. A pick with no quantity (a\n`task_start`, a `done`) carries NULL, not 0 \u2014 absent and zero are different.\n\n**The put crew's clock starts at the batch epoch, not at zero.** The crew works through the\nbatch's placements back to back; nothing in this model makes it wait for a picker, and\nnothing makes a picker wait for it.",
      "kind": "module",
      "signature": null
    },
    "Optimization/metrics/work_events.py::_QTY_EVENTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset({'pick'})"
    },
    "Optimization/metrics/work_events.py::merged": {
      "decorators": [],
      "doc": "The rows in the order `work_events_merged` declares.\n\nKept here as well as in the view so an in-memory caller and a SQL caller cannot\ndisagree about what \"merged\" means. Instant, then BATCH, then role, then mode, then\nactor, then emission order within that actor \u2014 `PickEvent.__lt__` compares time alone,\nand with two streams that undeclared tie-break decides whether a pick or a put is read\nfirst at the same instant.\n\n`batch_id` is in the key because `seq` restarts at 0 every batch, and consecutive\nbatches genuinely TOUCH: the arm advances to `batch_start + duration`, which is exactly\nthe last `done` instant, so batch i's final `done` and batch i+1's first `task_start`\nfor the same picker share a t_abs, a role, a mode and an actor. Without the batch the\ntie fell through to `seq` \u2014 large for the `done`, near 0 for the `task_start` \u2014 and\nordered them backwards, the reverse of the emission order this key promises.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/metrics/work_events.py::pick_rows": {
      "decorators": [],
      "doc": "Merged-stream rows for one batch's pick events.\n\n`events` carry ABSOLUTE times already \u2014 the arm hands every picker the batch epoch as\nits start \u2014 so `t_abs` is `e.time` and `t_local` is the offset from `batch_start`.\n\n`crew` supplies the role, the mode and the uid space; `e.picker_id` is the dense\nper-crew id and becomes `actor_local`, with `actor_uid` its crew-offset twin.  A picker\nid outside the crew raises rather than being written under someone else's uid.",
      "is_async": false,
      "kind": "function",
      "signature": "(events, batch_id, batch_start, crew, *, shift_seconds: float=DEFAULT_SHIFT_SECONDS, first_seq: int=0)"
    },
    "Optimization/metrics/work_events.py::put_rows": {
      "decorators": [],
      "doc": "Merged-stream rows for one batch's put-away.\n\n`records` are `Inventory_Manager.drain_putaway_records()` tuples\n`(t_start, dur, sku, qty, aisle_id, x_phys, y_phys, source, worker, queue)`, whose\n`t_start` runs on that WORKER's own clock from 0 within this batch (the drain restarts\nthem all).\n\nONE QUEUE PER CALL.  Each put-away stream has its own crew, so its worker indices mean\nsomething only against that crew's roster -- worker 0 of the cart crew and worker 0 of\nthe forklift crew are different people.  Handing this function a mixed list would\nsilently attribute one stream's work to the other's actors, so it refuses.\n\nTWO ORIGINS, and they are not the same instant:\n\n  `crew_start`  where the crew actually picks the work up, and what `t_abs` is measured\n                from. The crew is CONTINUOUS: it cannot start batch i's queue before\n                the wave is released, and it cannot start before it finished batch\n                i-1's, so the caller passes `max(batch_start, previous_finish)`.\n                Defaults to `batch_start` for a caller with no carry.\n  `batch_start` the wave's release, and what `t_local` is measured from \u2014 so `t_local`\n                on a put row reads as \"how far into this wave the crew got to it\",\n                and EXCEEDS the batch's duration exactly when the crew is running\n                behind. That is information, not an error.\n\nWhy the crew must carry: one putter placing a whole wave's restock takes longer than\nthe parallel pick crew takes to pick it, so batch i's put-away genuinely overruns batch\ni+1's release. Restarting at each wave would have the same single worker doing two\nbatches at the same instant \u2014 measured on a store arm before this carry existed, 16-33\nrows per DB overlapped.\n\nEach record names the WORKER that did it. The manager gives every put to whichever of\nits crew is free earliest, so the worker is a scheduling outcome, not a function of\nposition in the list. This used to round-robin on `seq % len(workers)` while the\ndurations came from a single serial clock \u2014 so a crew of two reported two people each\ndoing every other put, at instants that said one person did all of them. `qty` is\npositive.\n\n`event_type` is a PARAMETER and not the literal it used to be. `role` comes from the\nworker, so a second stream reusing this body would otherwise write `role='receive'` with\n`event_type='put'`, and `SUM(duration) WHERE role='put'` would disagree with the same\nquery on `event_type` with nothing to point at. `recv_rows` below is that second stream;\nit shares this body precisely so the two-origin split, the worker bounds check and the\none-stream-per-call refusal exist once.",
      "is_async": false,
      "kind": "function",
      "signature": "(records, batch_id, batch_start, crew, *, shift_seconds: float=DEFAULT_SHIFT_SECONDS, first_seq: int=0, crew_start=None, event_type: str='put')"
    },
    "Optimization/metrics/work_events.py::recv_rows": {
      "decorators": [],
      "doc": "Merged-stream rows for one batch's RECEIVING, through `put_rows`' body.\n\n`records` are `Inventory_Manager.drain_receiving_records()` tuples\n`(t_start, dur, sku, qty, worker)`, widened here to the 10-slot put shape with `None`\nfor the aisle and 0.0 for the coordinates -- a dock has no aisle and no position. The\n`source` slot carries `'dock'`, which is where the work happened; the merchandise's own\norigin is already on the put row that follows it.\n\nA THIN WRAPPER ON PURPOSE. Everything that is easy to get subtly wrong lives in\n`put_rows`: `crew_start` for `t_abs` against `batch_start` for `t_local`, the worker\nbounds check, and the refusal to mix streams in one call. A second implementation would\nhave its own copy of each, and the copies would drift somewhere nobody looks.\n\nRefuses a `Crew` OBJECT rather than its pre-offset worker tuple. `Crew.workers()`\ndefaults `first_uid=0`, so passing the crew here would silently re-allocate uids from\nzero and collide with the pick crew -- and nothing downstream could tell: a collision\npasses the bounds check, the DDL has no uniqueness constraint, and the merged view still\nsorts. Any per-actor rollup would then merge two people, and the failure is quietest\nexactly when the receiving crew is small, which is the likely configuration.",
      "is_async": false,
      "kind": "function",
      "signature": "(records, batch_id, batch_start, crew, *, shift_seconds: float=DEFAULT_SHIFT_SECONDS, first_seq: int=0, crew_start=None, event_type: str='receive')"
    },
    "Optimization/metrics/work_events.py::repack_rows": {
      "decorators": [],
      "doc": "`repack` rows for one batch's PUT-AWAY REWORK (ADR-0003), through `recv_rows`' body.\n\n`records` are `Inventory_Manager.drain_repack_records()` tuples, identical in shape to\nthe unload ones and charged on the same crew clock -- a repack IS receiving work, done\nby the receiving crew at the dock's own per-pack price.\n\n`role` is therefore `'receive'` (it comes from the worker) and only `event_type` differs.\nThat split is the whole reason `put_rows` takes the type as a parameter: a reader asking\n\"what did receiving cost\" sums `role='receive'` and gets unloads AND rework, while one\nasking \"how much rework was there\" filters `event_type='repack'`. Folding repacks into\nthe `'receive'` event type would make the second question unanswerable; giving them\ntheir own role would make the first one wrong.\n\nA SEPARATE CALL from `recv_rows` rather than a widened one, because `put_rows` writes a\nsingle event type per call by construction.",
      "is_async": false,
      "kind": "function",
      "signature": "(records, batch_id, batch_start, crew, *, shift_seconds: float=DEFAULT_SHIFT_SECONDS, first_seq: int=0, crew_start=None)"
    },
    "Optimization/persistence/Picking_Data.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/Picking_Data.py::AisleMetricRecord": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::BATCH_UNKNOWN_ON_OLDER_VINTAGES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('free_bins', 'put_spills')"
    },
    "Optimization/persistence/Picking_Data.py::BatchStats": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::BinEvictionRecord": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::BinInventoryRecord": {
      "decorators": [
        "dataclass"
      ],
      "doc": "One archived `bin_inventory` row.  **ARCHIVE-ONLY \u2014 no run writes this table any more.**\n\nKept, with `load_bin_inventory`, because the ~500 GB archive predates the bin-mutation log\nand `bin_inventory` is the only depletion record those files will ever have.  New runs record\n`bin_placement` + `bin_eviction` + `picks` instead, which reconstruct bin state exactly at\nevery batch rather than approximately between keyframes.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::BinPlacementRecord": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::CAP_AISLE_METRICS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'aisle_metrics'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_BIN_INVENTORY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'bin_inventory'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_BIN_LOG": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'bin_log'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_BIN_SCORES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'bin_scores'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_CARRYOVER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'carryover'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_KEYFRAMES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'keyframes'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_REORDER_QUEUE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'reorder_queue'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_SHIFT_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'shift_days'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_SKU_SCORES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'sku_scores'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_VIZ_CACHE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'viz_cache'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_WORK_EVENTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'work_events'"
    },
    "Optimization/persistence/Picking_Data.py::CAP_YARD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'yard'"
    },
    "Optimization/persistence/Picking_Data.py::CONDITIONAL_READS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'load_bin_inventory': 'bin_inventory', 'load_bin_placements': 'bin_placement', 'load_bin_evictions': 'bin_eviction', 'run_identity': 'simulation_runs.sim_schema_id', 'load_yard_trailers': 'yard_tr..."
    },
    "Optimization/persistence/Picking_Data.py::KEYFRAME_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='keyframes_db', declared_shape=declared_keyframe_shape, meta_table='schema_meta', known_ids=('e1149f95dfed',)))"
    },
    "Optimization/persistence/Picking_Data.py::OCCUPANCY_LADDER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple((SIM_CAPABILITIES[n] for n in (CAP_BIN_LOG, CAP_AISLE_METRICS, CAP_BIN_INVENTORY)))"
    },
    "Optimization/persistence/Picking_Data.py::PRE_CARRY_SPLIT_SIM_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'487a65bf83a9'"
    },
    "Optimization/persistence/Picking_Data.py::PRE_FREE_INDEX_SIM_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'02a78953886c'"
    },
    "Optimization/persistence/Picking_Data.py::PRE_REALIZED_SIM_SCHEMA_IDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('1a594605a10e', '23d0c7f167bc', '2b7913bcd7e6', '6ad0b34af9f1', '8114cc4332eb', '96b8e37f158d', 'c33feeed3975', 'ee5ebabe74fb', 'f43d8b5931a4')"
    },
    "Optimization/persistence/Picking_Data.py::PRE_REWORK_SIM_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'798778f4fae1'"
    },
    "Optimization/persistence/Picking_Data.py::PRE_STAMP_SIM_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'23d0c7f167bc'"
    },
    "Optimization/persistence/Picking_Data.py::PickRecord": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::PickerEventRecord": {
      "decorators": [
        "dataclass(slots=True)"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='Picking_Data shared read layer', tables={'batch_stats': ('run_id', 'batch_id', 'duration', 'num_tasks', 'total_items', 'avg_concurrent_pickers', 'picking_pc..."
    },
    "Optimization/persistence/Picking_Data.py::SIM_CAPABILITIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{c.name: c for c in (_capability.Capability(name=CAP_SHIFT_DAYS, table='shift_days', exact=True, phase=\"per working day, at close-out (the next day's first batch; the final day flushed at run end)\"..."
    },
    "Optimization/persistence/Picking_Data.py::SIM_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='sim_db', declared_shape=declared_sim_schema_shape, meta_table=None, stamp_reader=_read_sim_stamp, known_ids=('c37b50bf2b84', 'c6bacfdb5c77', 'b87cfbb8d041'..."
    },
    "Optimization/persistence/Picking_Data.py::TASK_UNKNOWN_ON_OLDER_VINTAGES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('items_realized', 'bins_realized')"
    },
    "Optimization/persistence/Picking_Data.py::TaskStats": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/Picking_Data.py::UNVETTED_ARCHIVE_SIM_SCHEMA_IDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('5d8a78b74466', '89c7b2babf22', 'e110afa222e3')"
    },
    "Optimization/persistence/Picking_Data.py::_BATCH_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('run_id', 'batch_id', 'duration', 'num_tasks', 'total_items', 'avg_concurrent_pickers', 'picking_pct', 'traveling_pct', 'is_outlier', *_BATCH_OPTIONAL)"
    },
    "Optimization/persistence/Picking_Data.py::_BATCH_OPTIONAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'task_makespan': 0.0, 'thr_task': 0.0, 'thr_batch': 0.0, 'batch_start_time': 0.0, 'batch_end_time': 0.0, 'sigma_fd': 0.0, 'reload_moves': 0, 'reorder_placements': 0, 'skus_reordered': 0, 'units_or..."
    },
    "Optimization/persistence/Picking_Data.py::_BATCH_WRITE_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('run_id', 'batch_id', 'duration', 'num_tasks', 'total_items', 'task_makespan', 'thr_task', 'thr_batch', 'avg_concurrent_pickers', 'picking_pct', 'traveling_pct', 'batch_start_time', 'batch_end_tim..."
    },
    "Optimization/persistence/Picking_Data.py::_BIN_SCORE_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('aisle_id', 'bayX', 'bayY', 'travel_d', 'height_mult', 'layout_score', 'map_pref')"
    },
    "Optimization/persistence/Picking_Data.py::_CARRYOVER_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'reason', 'sku', 'qty')"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_AISLE_METRICS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS aisle_metrics (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n aisle_id INTEGER NOT NULL,\\n n_skus INTEGER NOT NULL DEFA..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_AISLE_METRICS_AISLE_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_am_run_aisle\\n ON aisle_metrics (run_id, aisle_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_AISLE_METRICS_BATCH_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_am_run_batch\\n ON aisle_metrics (run_id, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BATCH_STATS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS batch_stats (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n duration REAL NOT N..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_EVICTION": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS bin_eviction (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n seq INTEGER NOT NULL, -- run-scoped monotonic; ascending w..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_EVICTION_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_be_bin\\n ON bin_eviction (run_id, aisle_id, bayX, bayY, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_KEYFRAME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS bin_keyframe (\\n run_id INTEGER NOT NULL,\\n batch_id INTEGER NOT NULL,\\n aisle_id INTEGER NOT NULL,\\n bayX INTEGER NOT NULL,\\n bayY INTEGER NOT NULL,\\n sku INTEGER NO..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_KEYFRAME_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_kf_run_batch ON bin_keyframe (run_id, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_PLACEMENT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS bin_placement (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n seq INTEGER NOT NULL, -- run-scoped monotonic; ascending ..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_PLACEMENT_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_bp_bin\\n ON bin_placement (run_id, aisle_id, bayX, bayY, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_SCORES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS bin_scores (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n aisle_id INTEGER NOT NULL,\\n bayX INTEGER NOT NULL,\\n bayY INTEGER NOT NULL,\\n travel_d R..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_BIN_SCORES_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_bs_run ON bin_scores (run_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_CARRYOVER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS carryover (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL, -- the batch it carried OUT of\\n reason TEXT NOT NULL, -- WHY i..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_FREE_INDEX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS free_index (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL, -- pairs 1:1 with batch_stats.batch_id; read at the same\\n -- ..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PICKER_EVENTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS picker_events (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n picker_id INTEGER..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PICKER_EVENTS_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_pe_run_batch\\n ON picker_events (run_id, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PICKER_EVENTS_TIME_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_pe_run_batch_time\\n ON picker_events (run_id, batch_id, time)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PICKS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS picks (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n picker_id INTEGER NOT NUL..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PICKS_BATCH_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_picks_run_batch ON picks (run_id, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PICKS_SKU_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_picks_run_sku ON picks (run_id, sku)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_PUT_QUEUE_STATE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS put_queue_state (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n queue TEXT NOT NULL, -- the PutQueueSpec name\\n depth I..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_REORDER_QUEUE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS reorder_queue (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n kind TEXT NOT NULL, -- 'lead' (in-transit) | 'stock' (awa..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_REORDER_QUEUE_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_rq_run_batch ON reorder_queue (run_id, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_RUNS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS simulation_runs (\\n run_id INTEGER PRIMARY KEY AUTOINCREMENT,\\n run_type TEXT NOT NULL,\\n created TEXT NOT NULL,\\n strategy_key TEXT, -- identity (rename-proof): same..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_SHIFT_DAYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS shift_days (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n day INTEGER NOT NULL, -- the working day, 0-based, = batch_stats.work_day\\n cap_end REAL ..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_SITE_RECEIVING": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS site_receiving (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch INTEGER NOT NULL, -- pairs 1:1 with each leaf's batch_stats.batch_id;\\n -- spel..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_SKU_SCORES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS sku_scores (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n sku INTEGER NOT NULL,\\n map_target REAL, -- optimal-map target pref (map/map_rank only)\\n..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_SKU_SCORES_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_ss_run ON sku_scores (run_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_TASK_STATS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS task_stats (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n aisle_id INTEGER NOT..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_WORK_EVENTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS work_events (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch_id INTEGER NOT NULL,\\n seq INTEGER NOT NUL..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_WORK_EVENTS_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_we_run_batch\\n ON work_events (run_id, batch_id)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_WORK_EVENTS_MERGED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE VIEW IF NOT EXISTS work_events_merged AS\\n SELECT * FROM work_events\\n ORDER BY t_abs, batch_id, role, mode, actor_uid, seq\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_WORK_EVENTS_TIME_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_we_run_tabs\\n ON work_events (run_id, t_abs)\\n'"
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_YARD_DRAINS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS yard_drains (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n batch INTEGER NOT NULL, -- pairs 1:1 with batch_stats.batch_id. Its own\\n -- table rathe..."
    },
    "Optimization/persistence/Picking_Data.py::_CREATE_YARD_TRAILERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS yard_trailers (\\n run_id INTEGER NOT NULL REFERENCES simulation_runs(run_id),\\n seq INTEGER NOT NULL, -- the trailer's dispatch ordinal: run-scoped, dense,\\n -- and t..."
    },
    "Optimization/persistence/Picking_Data.py::_EVENT_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('run_id', 'batch_id', 'picker_id', 'time', 'event_type', 'aisle_id', 'bayX', 'bayY', 'sku', 'quantity', 'bins_completed', 'total_bins', 'items_picked', 'total_items', *_EVENT_OPTIONAL)"
    },
    "Optimization/persistence/Picking_Data.py::_EVENT_OPTIONAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'pick_travel_x': 0.0, 'pick_travel_y': 0.0, 'non_pick_travel_x': 0.0, 'non_pick_travel_y': 0.0, 'cart_move': 0.0}"
    },
    "Optimization/persistence/Picking_Data.py::_FREE_INDEX_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'handling', 'category', 'size', 'unit', 'free')"
    },
    "Optimization/persistence/Picking_Data.py::_IDENTITY_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('strategy_key', 'pair_label', 'config_label', 'warehouse_fingerprint', 'inventory_label', 'channel', 'sim_schema_id')"
    },
    "Optimization/persistence/Picking_Data.py::_PICK_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('sku', 'quantity', 'timestamp', 'aisle_id', 'bayX', 'bayY', 'handling_type', 'category_type')"
    },
    "Optimization/persistence/Picking_Data.py::_PLAIN_INSERT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset({'batch_stats', 'picker_events', 'picks', 'reorder_queue', 'site_receiving', 'task_stats', 'work_events'})"
    },
    "Optimization/persistence/Picking_Data.py::_RECEIVE_EVENT_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'sku', 'qty', 'duration')"
    },
    "Optimization/persistence/Picking_Data.py::_RUN_ADDED_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('sim_schema_id', 'TEXT'),)"
    },
    "Optimization/persistence/Picking_Data.py::_RUN_PARAM_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('num_pickers', 'x_speed', 'y_speed', 'pick_intercept', 'pick_weight_coef', 'pick_volume_coef', 'cart_swap_coef', 'k_pickers', 'n_batches', 'seed_world', 'keyframe_interval', 'optimal_sigma_fd', 'o..."
    },
    "Optimization/persistence/Picking_Data.py::_SHIFT_DAY_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('day', 'cap_end', 'end_s', 'drained', 'standing', 'standing_put', 'standing_dock', 'standing_carry', 'standing_carry_labour', 'standing_carry_supply', 'last_finish')"
    },
    "Optimization/persistence/Picking_Data.py::_SHIFT_DAY_DRAINED_SQL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'CASE WHEN last_finish > cap_end THEN 0 ELSE drained END AS drained'"
    },
    "Optimization/persistence/Picking_Data.py::_TASK_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('run_id', 'batch_id', 'aisle_id', 'picker_id', 'task_start_time', 'task_end_time', 'duration', 'W', 'lift_sum', 'num_bins_visited', 'total_items', 'items_realized', 'bins_realized', 'is_outlier')"
    },
    "Optimization/persistence/Picking_Data.py::_TASK_OPTIONAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'W': 0.0, 'items_realized': None, 'bins_realized': None}"
    },
    "Optimization/persistence/Picking_Data.py::_TASK_ROW_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'aisle_id', 'picker_id', 'task_start_time', 'task_end_time', 'duration', 'W', 'lift_sum', 'num_bins_visited', 'total_items', 'is_outlier')"
    },
    "Optimization/persistence/Picking_Data.py::_TIMELINE_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('time', 'picker_id', 'event_type', 'aisle_id', 'bayX', 'bayY', 'sku', 'quantity', 'bins_completed', 'total_bins', 'items_picked', 'total_items')"
    },
    "Optimization/persistence/Picking_Data.py::_VIEWER_BATCH_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'duration', 'num_tasks', 'total_items', 'reorder_placements')"
    },
    "Optimization/persistence/Picking_Data.py::_WORK_EVENT_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'seq', 't_abs', 't_local', 'shift_index', 'actor_uid', 'actor_local', 'role', 'mode', 'event_type', 'aisle_id', 'sku', 'qty', 'duration', 'source')"
    },
    "Optimization/persistence/Picking_Data.py::_WORK_HOURS_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch_id', 'role', 'seconds', 'n_rows', 'n_timed', 'units')"
    },
    "Optimization/persistence/Picking_Data.py::_WRITE_ONLY_BY_DESIGN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset({'is_outlier'})"
    },
    "Optimization/persistence/Picking_Data.py::_YARD_DRAIN_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('batch', 'yard_start', 'free_doors_start', 'yard_end', 'staged_remainder_end')"
    },
    "Optimization/persistence/Picking_Data.py::_YARD_TRAILER_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('seq', 'arrived_s', 'staged_s', 'emptied_s', 'status')"
    },
    "Optimization/persistence/Picking_Data.py::_apply_run_schema": {
      "decorators": [],
      "doc": "Issue every CREATE for the run DB on an already-open connection.\n\nSplit out of init_run_db so sim_schema_id() can build the schema in memory and hash what\nthe writer ACTUALLY creates \u2014 the declared id is never a hand-maintained list of columns,\nso it cannot drift from this function.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_batch_frame_sql": {
      "decorators": [],
      "doc": "The `batch_frame` select list minus the columns a vintage lacks -- an override for a\npure column addition, so the OPTIONAL fill (not the legacy dataclass default) answers\nfor the absent ones.  Every name in `omit` must be optional, or the contract check in\n`dataset.query` raises for the vintage.",
      "is_async": false,
      "kind": "function",
      "signature": "(*omit: str) -> str"
    },
    "Optimization/persistence/Picking_Data.py::_batch_row": {
      "decorators": [],
      "doc": "One `batch_stats` row, in `_BATCH_WRITE_COLS` order.\n\nZipped from the same declaration the column list is built from, so the two cannot get\nout of step -- which is the whole point: a hand-kept pair shifts every value after the\ninserted column by one, and SQLite accepts it as long as the types line up.\n\n`is_outlier` is the one column with an expression rather than a read: SQLite has no\nboolean, and `int()` says which integer a `bool` becomes rather than leaving it to the\nadapter.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_id: int, r) -> tuple"
    },
    "Optimization/persistence/Picking_Data.py::_insert_aisle_metrics": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_batch_stats": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_bin_evictions": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_bin_placements": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_carryover": {
      "decorators": [],
      "doc": "Write the carryover rows, refusing a batch that would destroy one of its own.\n\nThe primary key is (run_id, batch_id, reason, sku) and the insert is OR REPLACE, so two\nrows sharing a key silently collapse to the last one written. That is correct for a\nRESUME -- re-running a batch should replace its rows -- and catastrophic within a single\nflush, where it means two producers disagreed about what a reason means and the bigger\nnumber lost.\n\nRAISES rather than logs. The conservation ledger logs because it measures something that\ncan legitimately drift; this is not a measurement, it is rows about to be deleted.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_free_index": {
      "decorators": [],
      "doc": "`(batch_id, handling, category, size, unit, free)` tuples -- the runner's\n`(i, *BinKey, free)` off `free_bin_depth_by_bucket`.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_picker_events": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_picks": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_put_queue_state": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_reorder_queue": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_shift_days": {
      "decorators": [],
      "doc": "`(day, cap_end, end_s, drained, standing, standing_put, standing_dock, standing_carry,\nstanding_carry_labour, standing_carry_supply, last_finish)` tuples -- `strategy_runner`'s\nclose-out row, one per working day.  `standing_carry` is the labour and supply halves\nsummed; the halves are what the drained verdict and the missed-share report read.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_site_receiving": {
      "decorators": [],
      "doc": "`(batch, recv_depth, recv_unloaded, recv_cut, recv_seconds)` tuples \u2014 the SITE dock's\nown per-site-day counters, one row per batch of a coupled run.\n\nA PLAIN INSERT, where every neighbour here uses `INSERT OR REPLACE`, and the difference\nis deliberate.  This table has exactly ONE producer (`_SiteDock.collect`, draining the\ncoordinator once per batch) and it is written ONCE at run end, so a duplicate `batch` is\nnot a re-flush \u2014 it is two site days collected under one index, which the coupled grid\nrefuses upstream (one batch IS one site day).  `INSERT OR REPLACE` would silently keep\nthe second and drop the first, which is `carryover-two-producers-one-key` exactly: a\ntable that raises is the fix that memory records.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_sql": {
      "decorators": [],
      "doc": "The INSERT for `table`, generated from its declared write surface.\n\nThe column list and the placeholder count come from one tuple, so they cannot disagree --\nthe misalignment a hand-kept pair invites shifts every value after the added column by\none, and SQLite accepts it whenever the types happen to line up.",
      "is_async": false,
      "kind": "function",
      "signature": "(table: str) -> str"
    },
    "Optimization/persistence/Picking_Data.py::_insert_task_stats": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_work_events": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, rows: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_yard_drains": {
      "decorators": [],
      "doc": "`(batch, yard_start, free_doors_start, yard_end, staged_remainder_end)` tuples.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_insert_yard_trailers": {
      "decorators": [],
      "doc": "`(seq, arrived_s, staged_s, emptied_s, status)` tuples, as the transit stamps them.\n\nA trailer reaches this table exactly once in a clean run \u2014 the finished stamps are\nDRAINED per batch and the censored tail is read once, at run end, from trailers the\ndrain never saw.  OR REPLACE is therefore idempotency insurance, matching the other log\ntables: it makes re-running an arm over an existing DB a correction rather than a PK\ncollision.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, run_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_migrate_run_columns": {
      "decorators": [],
      "doc": "Add any simulation_runs column this build expects but an older file lacks.\n\nALTER TABLE ADD COLUMN is O(1) in SQLite (it only rewrites the schema), and adding the\ncolumn also lifts the file's observed shape onto the current declared id \u2014 which is exactly\nwhat the reader registry wants.  Existing rows keep NULL, and a NULL stamp is the documented\n\"derive it\" path.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection) -> None"
    },
    "Optimization/persistence/Picking_Data.py::_open_db": {
      "decorators": [],
      "doc": "Open *path* with WAL journal mode and a generous busy timeout.\n\nWAL allows multiple concurrent readers and one writer without blocking\nreaders.  Writers that arrive while another write is in progress wait up\nto *timeout* seconds before raising OperationalError, giving the three\nparallel strategy workers enough headroom to avoid spurious lock errors\nwhen their 100-batch checkpoints happen to coincide.\n\n\u2500\u2500 Why NONE of this module's 28 closes goes through `Schema.connect.close` \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nDeliberate, and measured \u2014 not an oversight.  The closes here fall into three groups:\n\n  13 are `load_*` / `find_run` / `run_identity` / the two `declared_*_shape` helpers.\n     Read-only or `:memory:`.  There is no WAL to fold back; converting them is noise.\n\n   3 are the per-flush writers \u2014 `save_bin_keyframe` and the two score writers.  (This\n     read TWELVE until ticket 07: nine of them were `save_<table>` wrappers with no\n     production caller, and they are gone.  The checkpoint path's own open/close now\n     lives in `checkpoint_buffer._write`, one per flush for ALL fifteen channels instead\n     of one per table, and the reasoning below applies to it unchanged.)  Each opens and\n     closes ONCE PER CHECKPOINT FLUSH (default every 10 batches) against a sim DB that\n     reaches ~1 GB.  A `wal_checkpoint(TRUNCATE)` there would fold the whole WAL into the\n     main file every flush, on the hot path of a 40-minute arm, for no benefit \u2014 see below.\n\n   3 are one-time setup: `init_run_db`, `create_run`, `init_keyframe_db`.  The connection\n     is finished but the FILE is not; the run writes to it for the next 40 minutes, so a\n     checkpoint changes nothing about the state that is left behind.\n\nAnd the benefit really is nil, because a plain close already does the job here.  Measured:\non a clean single-process write-then-close, SQLite removes `-wal`/`-shm` itself, and\n`connect.close` removes exactly the same set.  What actually strands them is a READER \u2014\na `mode=ro` connection on a WAL database creates `-shm` and cannot delete it on the way\nout, so every archived sim DB grows sidecars the moment anything fingerprints or plots it.\nNo change on the WRITE side can prevent that; only `immutable=1` on the read side can, and\nthat is a promise about the file that a live run cannot make.  The one writer here whose\nclose is genuinely the file's last is in `Warehouse_Data`, which does convert.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, timeout: float=60.0) -> sqlite3.Connection"
    },
    "Optimization/persistence/Picking_Data.py::_query_rows": {
      "decorators": [],
      "doc": "Rows via the named-query registry when this file's vintage is servable, else None.\n\nNone routes the caller to its FROZEN LEGACY body: `Diagnostics/replay_run.py` deliberately\nreads UNVETTED cold-archive vintages (`UNVETTED_ARCHIVE_SIM_SCHEMA_IDS`), which `bind`\nrefuses by design \u2014 for those, the pre-registry `SELECT *` + `row.keys()` behavior is kept\nverbatim, and a golden test asserts both paths agree on every vetted vintage.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, path: str, **params)"
    },
    "Optimization/persistence/Picking_Data.py::_read_sim_stamp": {
      "decorators": [],
      "doc": "The `simulation_runs.sim_schema_id` a run stamped, or None.\n\nThe sim DB's stamp is a COLUMN VALUE (written by `create_run`), not a meta table, so the\ngeneric `identity.read_stamp` cannot find it without this.  THE single implementation:\nthe viewer once kept its own byte-equivalent copy and now resolves through\n`identity.resolve` with this family, so every consumer dates the archive identically.",
      "is_async": false,
      "kind": "function",
      "signature": "(con) -> str | None"
    },
    "Optimization/persistence/Picking_Data.py::_shift_day_select": {
      "decorators": [],
      "doc": "The select list for a `shift_days` query: every column by name, `drained` amended.",
      "is_async": false,
      "kind": "function",
      "signature": "(cols) -> str"
    },
    "Optimization/persistence/Picking_Data.py::_task_frame_sql": {
      "decorators": [],
      "doc": "The `task_frame` select list minus the columns a vintage lacks -- `_batch_frame_sql`'s\nrule, one table over. Every name in `omit` must be optional, or the contract check in\n`dataset.query` raises for the vintage.",
      "is_async": false,
      "kind": "function",
      "signature": "(*omit: str) -> str"
    },
    "Optimization/persistence/Picking_Data.py::_unreadable": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{t: sorted(set(WRITE_SURFACES[t]) - set(read) - _WRITE_ONLY_BY_DESIGN) for t, read in _READ_SURFACES.items() if set(WRITE_SURFACES[t]) - set(read) - _WRITE_ONLY_BY_DESIGN}"
    },
    "Optimization/persistence/Picking_Data.py::create_run": {
      "decorators": [],
      "doc": "Insert a new simulation run row; return the assigned run_id.\n\nparams (optional): run configuration recorded for reconstruction \u2014\nkeys from _RUN_PARAM_COLS (num_pickers, x_speed, \u2026, optimal_work).\nidentity (optional): rename-proof association \u2014 keys from _IDENTITY_COLS\n(strategy_key, pair_label, config_label, warehouse_fingerprint, inventory_label).\nMissing keys are stored NULL.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_type: str, params: dict | None=None, identity: dict | None=None) -> int"
    },
    "Optimization/persistence/Picking_Data.py::declared_keyframe_shape": {
      "decorators": [],
      "doc": "Canonical shape of the sibling keyframe DB.\n\nIncludes the `schema_meta` stamp table: a family that declares `meta_table` must DECLARE the\ntable in its own shape, or the observed shape (with it, once stamped) would never match the\ndeclaration (see `identity.META_TABLE_DDL`).",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/persistence/Picking_Data.py::declared_sim_schema_shape": {
      "decorators": [],
      "doc": "Canonical shape of a run DB as `_apply_run_schema` creates it, built in memory.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/persistence/Picking_Data.py::find_run": {
      "decorators": [],
      "doc": "Resolve a run_id WITHOUT relying on the file name.\n\nMatches on the stored strategy_key/run_type (which equal the strategy key) so a\nrenamed sim_*.db still resolves; falls back to the first run in the DB.  Returns\nNone if the DB has no runs.  Tolerates the older schema (no strategy_key column).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, strategy_key: str | None=None) -> int | None"
    },
    "Optimization/persistence/Picking_Data.py::init_keyframe_db": {
      "decorators": [],
      "doc": "Create the bin_keyframe table + index (and the schema_meta stamp) if absent.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> None"
    },
    "Optimization/persistence/Picking_Data.py::init_run_db": {
      "decorators": [],
      "doc": "Create all tables and indexes if they don't already exist, and enable WAL mode.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> None"
    },
    "Optimization/persistence/Picking_Data.py::keyframe_db_path": {
      "decorators": [],
      "doc": "Sibling keyframe-DB path for a strategy's run DB (run.db \u2192 run.keyframes.db).",
      "is_async": false,
      "kind": "function",
      "signature": "(run_db_path: str) -> str"
    },
    "Optimization/persistence/Picking_Data.py::load_aisle_metrics": {
      "decorators": [],
      "doc": "Load AisleMetricRecord rows, optionally filtered to one batch or aisle.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int | None=None, aisle_id: int | None=None) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_batch_stats": {
      "decorators": [],
      "doc": "One BatchStats per batch, version-adaptive.\n\nA VETTED file is served by the `batch_frame` named query (per-vintage overrides and\noptional-fill included), so a consumer of this loader is version-free without knowing it.\nAn unvetted file \u2014 the cold archive `Diagnostics/replay_run.py` deliberately reads \u2014 falls\nthrough to the frozen pre-registry body below, byte-for-byte the historical behavior.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list[BatchStats]"
    },
    "Optimization/persistence/Picking_Data.py::load_bin_evictions": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int | None=None) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_bin_inventory": {
      "decorators": [],
      "doc": "Load BinInventoryRecord rows from an ARCHIVED run, optionally filtered to batch/aisle.\n\nLEGACY/ARCHIVE-ONLY.  The table is no longer written by any run; a DB produced by this\nbuild has no `bin_inventory` at all and this raises `sqlite3.OperationalError`.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int | None=None, aisle_id: int | None=None) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_bin_placements": {
      "decorators": [],
      "doc": "PLACE events, ordered as applied.\n\n`bin_state` ('empty' | 'occupied', ADR-0003) is selected only when the file HAS it and\nfilled with 'empty' otherwise -- the `row.keys()` guard this file's raw loaders have\nalways used, and the honest value for a pre-ADR run, where put-away could not add to an\noccupied bin.  Selecting it unconditionally would make every archived file raise.\nThe tier pair (`unit_size` / `bin_size`) is guarded the same way and filled with NULL,\nbecause a file without the columns did not record a fit and 'empty' has no analogue.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int | None=None) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_bin_scores": {
      "decorators": [],
      "doc": "All per-bin scores for a run; empty list if the table is absent (older runs).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list[dict]"
    },
    "Optimization/persistence/Picking_Data.py::load_carryover": {
      "decorators": [],
      "doc": "Every carryover row this run recorded.  `[]` on a vintage without the table.\n\nRAW ROWS, reasons unfolded.  The three reason families are LEVELS and FLOWS sharing one\nphysical column, so there is no legal `SUM(qty)` over the whole table and this loader\ndeliberately does not offer one: the caller selects its reasons and knows which kind it\nthen holds (`sim_semantics`' ByDiscriminator on `reason` is the declaration).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_free_index": {
      "decorators": [],
      "doc": "The free index per bucket per batch, in (batch, key) order.  `[]` on a pre-table\nvintage, which is \"the depth was never recorded per bucket\", never \"no bucket had a\nfree bin\".\n\nEvery `free` is a LEVEL re-measured per batch: `sum()` over a bucket's rows restates the\nsame free bins once per batch (the `recv_cut` scar).  The statistics that mean something\nare per bucket over a window -- minimum, mean, and the drawdown from the record's stamped\nsetup `free` -- which is how the rework clause reads it.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_picker_events": {
      "decorators": [],
      "doc": "Load PickerEventRecord rows for *run_id*, optionally filtered to one batch.\n\nReturns records ordered by (batch_id, picker_id, time) for sequential replay.\nVersion-adaptive; see `load_batch_stats`.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int | None=None) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_picker_events._g": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(row, col)"
    },
    "Optimization/persistence/Picking_Data.py::load_receive_events": {
      "decorators": [],
      "doc": "Every pack the receiving crew unloaded -- `{batch_id, sku, qty, duration}` per row,\none row per storage unit -- for a check that prices each pack exactly.  `[]` on a\npre-`work_events` vintage and on a run with no receiving crew alike; the caller must\nnot read either as \"nothing was received\" without the capability saying so.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_reorder_queue": {
      "decorators": [],
      "doc": "Queue contents at the start of one batch.  Empty list if the table is absent\n(older runs predate it) so the viewer degrades gracefully.  Falls back to the\npre-enrichment columns when unit_type/storage_size are missing.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int) -> list[dict]"
    },
    "Optimization/persistence/Picking_Data.py::load_shift_days": {
      "decorators": [],
      "doc": "Every working day's close-out, in day order.  `[]` on a pre-era vintage AND on an\nera-less run of the new vintage -- both mean \"no day was ever closed out\".\n\n`drained` is the verdict the equilibrium check reads per day -- LABOUR-ONLY\n(`simconfig.equilibrium.is_drained`): the carry is split into `standing_carry_labour`\n(the cut's, standing work) and `standing_carry_supply` (the shelf's, stock not\ndelivered), and only the first can keep a day from draining.  On the pre-split vintage\n(`PRE_CARRY_SPLIT_SIM_SCHEMA_ID`) both halves read as None and `drained` is as that\nrunner judged it.  The `standing_*` columns are LEVELS at close-out and never sum\nacross days -- the two carry halves are what the day's LAST batch rolled forward and\nequal the day's `carryover` flows only under one batch per day, so the check's flow\nclauses read `load_carryover` and treat these as a cross-check (\"Split the missed-share\nclause into supply and labour\").  `end_s < cap_end` with `drained` is a day the crews got off the clock\nearly; `last_finish > cap_end` is START-gate overtime (a task begun before the whistle\nfinished after it) -- and such a day is CAPPED: overtime is labour that did not fit the\nday (\"Overtime behind a drained day raises the instrument\", 2026-09-07).  A ledger\nstamped before that amendment wrote an overtime day DRAINED; the query serves every\nvintage's `drained` with the term folded in (`_SHIFT_DAY_DRAINED_SQL`), so a row here\nnever reads drained with a finish past its cap.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_sku_scores": {
      "decorators": [],
      "doc": "All per-SKU scores for a run; empty list if the table is absent (older runs).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list[dict]"
    },
    "Optimization/persistence/Picking_Data.py::load_task_stats": {
      "decorators": [],
      "doc": "One TaskStats per (batch, aisle) task \u2014 version-adaptive; see `load_batch_stats`.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list[TaskStats]"
    },
    "Optimization/persistence/Picking_Data.py::load_work_hours": {
      "decorators": [],
      "doc": "Seconds of production labour per (batch, role).  `[]` on a pre-`work_events` vintage.\n\nTHE PUT LEG'S ONLY ROUTE.  Put-away hours exist nowhere else: `putaway_seconds` is an\nin-sim property on `Inventory_Management` and was never written to `batch_stats`, so\nevery consumer that wanted them had to read this table -- and until now none did.\n\nThe unload leg is here too, deliberately, even though `batch_stats.recv_seconds`\ncarries the same measurement.  That column sits OUTSIDE the guaranteed sim-DB surface\nand postdates `work_events`, so a quantity reading it would need a capability that\n`work_events` cannot honestly supply, while an unguarded read would fill a pre-dock\nvintage's unload leg with a plausible zero.  Reading both legs here puts the whole\nobjective behind one capability that is true when it says it is.  The two surfaces are\nwritten by different code from different state and are reconciled seconds-for-seconds\nby `Diagnostics/receiving_report.py`'s first check, which is what makes them\nsubstitutable rather than merely similar.\n\nRows are aggregates, not events, so the fold happens in SQL: a 200-batch arm holds\n~30k rows and the consumer wants ~3 numbers a batch.  `role` selection stays with the\ncaller for the same reason `load_carryover` leaves `reason` there -- 'pick' rows carry\nNULL durations by design and summing across every role would produce a number with no\nmeaning.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_yard_drains": {
      "decorators": [],
      "doc": "Every drain's yard levels, in batch order.  `[]` on a pre-yard vintage.\n\nAll four columns are LEVELS re-measured per drain: `sum()` over any of them restates\nthe same standing trailers once per batch.  The additive statistic over the binding-cut\npair is a COUNT OF DRAINS with either above zero.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::load_yard_trailers": {
      "decorators": [],
      "doc": "Every trailer row this run recorded, in dispatch order.  `[]` on a pre-yard vintage.\n\nRAW STAMPS.  No span, no detention day, no overage \u2014 those derive at analysis against\nthe run's recorded threshold, which is what makes a finished run's fee axis\nre-reportable without re-simulating.  An empty list is also the honest answer for an\ninbound-off run: the table exists, and nothing stood in a yard.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> list"
    },
    "Optimization/persistence/Picking_Data.py::run_identity": {
      "decorators": [],
      "doc": "Return the stored identity + key params for a run (rename-proof metadata).\nMissing columns (older schema) are simply omitted from the dict.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int) -> dict"
    },
    "Optimization/persistence/Picking_Data.py::save_bin_keyframe": {
      "decorators": [],
      "doc": "Write one full occupied-bin snapshot (keyframe) for (run_id, batch_id).\n\nrecords: iterable of dicts with keys aisle_id, bayX, bayY, sku, unit_type,\nstorage_size, qty.  INSERT OR REPLACE so re-running a batch overwrites cleanly.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, batch_id: int, records: list) -> None"
    },
    "Optimization/persistence/Picking_Data.py::save_bin_scores": {
      "decorators": [],
      "doc": "Per-bin static scores.  records: (aisle_id, bayX, bayY, travel_d, height_mult,\nlayout_score, map_pref); map_pref None for non-map strategies.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, records: list[tuple]) -> None"
    },
    "Optimization/persistence/Picking_Data.py::save_sku_scores": {
      "decorators": [],
      "doc": "Per-SKU placement scores.  records: (sku, map_target, labor_cost, handle_var,\nexpected_popularity, expected_labor, equilibrium_qty, reorder_point, lead_time_mean).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, records: list[tuple]) -> None"
    },
    "Optimization/persistence/Picking_Data.py::sim_schema_id": {
      "decorators": [],
      "doc": "The id this build stamps into new run DBs.  Computed once, then cached.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> str"
    },
    "Optimization/persistence/Warehouse_Data.py": {
      "decorators": [],
      "doc": "Warehouse_Data.py \u2014 SQLite persistence for warehouse sizing stats and aisle distributions.\n\nWritten once per inventory pair (shared across A/B/C strategies) to\npair_dir/warehouse.db.  Separate from the per-strategy sim_*.db files.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/Warehouse_Data.py::PRE_FINGERPRINT_WAREHOUSE_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'342d31313d08'"
    },
    "Optimization/persistence/Warehouse_Data.py::PRE_STAMP_WAREHOUSE_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'46464b5dfff6'"
    },
    "Optimization/persistence/Warehouse_Data.py::WAREHOUSE_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='warehouse_db', declared_shape=declared_warehouse_shape, meta_table='schema_meta', known_ids=(PRE_STAMP_WAREHOUSE_SCHEMA_ID, PRE_FINGERPRINT_WAREHOUSE_SCHEM..."
    },
    "Optimization/persistence/Warehouse_Data.py::_ALL_DDL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(_CREATE_WAREHOUSE_STATS, _CREATE_AISLE_TYPE_STATS, _CREATE_AISLE_IDX, _CREATE_AISLE_LAYOUT, _identity.meta_ddl('schema_meta'))"
    },
    "Optimization/persistence/Warehouse_Data.py::_CREATE_AISLE_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_at_warehouse_id\\n ON aisle_type_stats (warehouse_id)\\n'"
    },
    "Optimization/persistence/Warehouse_Data.py::_CREATE_AISLE_LAYOUT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS aisle_layout (\\n aisle_id INTEGER PRIMARY KEY,\\n handling_type TEXT NOT NULL,\\n category TEXT NOT NULL,\\n unit_type TEXT NOT NULL,\\n storage_size TEXT NOT NULL,\\n bay..."
    },
    "Optimization/persistence/Warehouse_Data.py::_CREATE_AISLE_TYPE_STATS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS aisle_type_stats (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n warehouse_id INTEGER NOT NULL REFERENCES warehouse_stats(id),\\n handling_type TEXT NOT NULL,\\n category T..."
    },
    "Optimization/persistence/Warehouse_Data.py::_CREATE_WAREHOUSE_STATS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS warehouse_stats (\\n id INTEGER PRIMARY KEY AUTOINCREMENT,\\n inventory_db TEXT NOT NULL,\\n timestamp TEXT NOT NULL,\\n n_skus INTEGER NOT NULL,\\n n_pallet_units INTEGER..."
    },
    "Optimization/persistence/Warehouse_Data.py::_GEOMETRY_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('aisle_id', 'handling_type', 'category', 'unit_type', 'storage_size', 'bay_x', 'bay_y')"
    },
    "Optimization/persistence/Warehouse_Data.py::_open_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> sqlite3.Connection"
    },
    "Optimization/persistence/Warehouse_Data.py::compute_warehouse_fingerprint": {
      "decorators": [],
      "doc": "Stable hash of a warehouse's geometry + inventory profile.\n\nRename-proof identity: stored in warehouse_stats and in every sim run row so a run\ncan be matched to its warehouse.db regardless of folder/file names.  Keyed on the\nper-aisle layout (id, handling, category, unit_type, size, bays) which uniquely\nfixes the geometry the picker_events/aisle_ids were recorded against.",
      "is_async": false,
      "kind": "function",
      "signature": "(aisle_rows: list[dict], inventory_label: str='') -> str"
    },
    "Optimization/persistence/Warehouse_Data.py::declared_warehouse_shape": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/persistence/Warehouse_Data.py::init_warehouse_db": {
      "decorators": [],
      "doc": "Create warehouse_stats, aisle_type_stats, and aisle_layout tables.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> None"
    },
    "Optimization/persistence/Warehouse_Data.py::save_aisle_layout": {
      "decorators": [],
      "doc": "Replace aisle_layout with one row per aisle of the built warehouse.\n\nrows: dicts with keys aisle_id, handling_type, category, unit_type,\nstorage_size, bay_x, bay_y.  Rewrite-fresh so it always reflects the latest\nplan/warehouse for this pair.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, rows: list[dict]) -> None"
    },
    "Optimization/persistence/Warehouse_Data.py::save_warehouse_stats": {
      "decorators": [],
      "doc": "Insert one warehouse_stats row + one aisle_type_stats row per aisle type.\n\naisle_rows: list of dicts with keys\n    handling_type, category, unit_type, replica_count, eff_bins_per_aisle,\n    total_bins, size_small_pct, size_medium_pct, size_large_pct, size_xlarge_pct\n\nReturns the warehouse_stats.id of the inserted row.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, inventory_db: str, n_skus: int, n_pallet: int, n_singleton: int, total_aisles: int, total_bins: int, expected_fill: float, target_fill: float, max_aisles: int | None, max_bins: int | None, avg_eq_qty: float, avg_rp: float, aisle_rows: list[dict], warehouse_fingerprint: str | None=None) -> int"
    },
    "Optimization/persistence/__init__.py": {
      "decorators": [],
      "doc": "Optimization.persistence \u2014 SQLite schemas and read/write for a run's DBs.\n\nLeaf modules by design: none of them imports anything else in Optimization, so persistence can\nnever drag configuration or orchestration into a worker.  See README.md.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/checkpoint_buffer.py": {
      "decorators": [],
      "doc": "checkpoint_buffer \u2014 accumulate, flush, clear: one object instead of thirteen lists.\n\n`strategy_runner` declared THIRTEEN bare lists, appended to them across ~40 scattered sites,\nflushed them through a 15-keyword call, cleared them with four statements naming all thirteen,\nand flushed them again with the same 15 keywords at run end. `save_checkpoint_bundle` is the\nother end: sixteen parameters over a body that is fifteen lines of\n`if rows: _insert_x(con, run_id, rows)`. **The interface was as complex as the implementation.**\n\nIts own docstring names the characteristic failure:\n\n> A bundle argument that is accepted and never inserted is this function's characteristic\n> failure: `work_events` was one for a while, and the reconciliation that was supposed to\n> catch it passed over 68 databases holding zero rows.\n\nThat cannot happen here. A channel IS its insert function \u2014 `CHANNELS` is the only place a\nname exists \u2014 so an accepted-but-never-inserted argument is not expressible, and\n`Tests/unit/test_checkpoint_buffer.py` walks the table and reads every channel back off a\nreal file.\n\n## `close()` is the design decision\n\n**A run-end close ALWAYS writes**, open window or not. `SectionTimers` made the same call for\nthe same reason and states it the same way.\n\nThe old form guarded its run-end flush with `if pb:` \u2014 \"is there an unflushed window\",\ninferred from one of the thirteen buffers. That proxy has been escaped twice, and each escape\nleft a paragraph behind explaining why its writer sits OUTSIDE the guard: the final day's\nclose-out, and the censored yard tail, which \"would report `lifo`'s fee as CLIPPED rather than\nconcentrated, which inverts the signal the arm exists to produce\". Memory\n`run-end-writers-miss-the-final-flush` records the general shape: the tail does not fire when\n`n_batches` divides the checkpoint cadence.\n\nWith `close()` writing unconditionally there is nothing to sit outside.\n\n## The channel table is the seam, and its ORDER is load-bearing\n\n`CHANNELS` is iterated in declared order, and that order is `save_checkpoint_bundle`'s\nhistorical call order. Per-table rowids and the run digest depend on it: two tables written in\nthe other order get different `id` sequences on the same rows. Reordering this tuple is a\nbehaviour change, not tidying.\n\n`skip_when_empty` mirrors what the individual writers did: three of them early-returned on an\nempty list and the rest did not. It moves no row either way (the tables all pre-exist from\n`init_run_db`); it is preserved because an empty `executemany` still costs a statement\npreparation on a ~1 GB WAL file, and because parity is cheaper to keep than to re-argue.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer": {
      "decorators": [],
      "doc": "Rows waiting for a checkpoint, and the three things anybody does with them.\n\n`add` accumulates, `pending()` asks EVERY channel rather than one of them, `flush` writes\nan open window, `close` writes at run end whether or not one is open.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, channels=CHANNELS) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer._write": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, path: str, run_id: int) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.add": {
      "decorators": [],
      "doc": "Extend one channel. An unknown name is a REFUSAL, not a silently dropped row.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, channel: str, rows) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.append": {
      "decorators": [],
      "doc": "One row. `add` for the common case of many.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, channel: str, row) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.clear": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.close": {
      "decorators": [],
      "doc": "Run end: ALWAYS write, open window or not.\n\nThis is the design decision (see the module docstring). A caller may have put rows in\nafter the last flush that belong to no window at all \u2014 the final day's close-out, the\ncensored yard tail \u2014 and under the old `if pb:` guard those were lost whenever\n`n_batches` divided the checkpoint cadence.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, path: str, run_id: int) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.flush": {
      "decorators": [],
      "doc": "Write an open window and clear it. A no-op when nothing is pending.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, path: str, run_id: int) -> None"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.pending": {
      "decorators": [],
      "doc": "Is ANYTHING unflushed?\n\nThe old form asked `if pb:` \u2014 one buffer as a proxy for all thirteen \u2014 and two\nrun-end writers had to be lifted out of that guard by hand. This asks every channel.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/persistence/checkpoint_buffer.py::CheckpointBuffer.rows": {
      "decorators": [],
      "doc": "The live list for one channel \u2014 for a caller that must read what it accumulated\n(the batch count the checkpoint cadence compares against).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, channel: str) -> list"
    },
    "Optimization/persistence/checkpoint_buffer.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['CheckpointBuffer', 'CHANNELS', 'SITE_CHANNELS', 'write_rows']"
    },
    "Optimization/persistence/checkpoint_buffer.py::write_rows": {
      "decorators": [],
      "doc": "Write rows a caller ALREADY HAS, in one connection and one commit. No window.\n\nFifteen named writers stood here until ticket 07 -- eleven `save_<table>` wrappers with\nzero production callers, plus `save_checkpoint_bundle`, `save_site_inbound`,\n`save_shift_days` and `save_yard_trailers` -- and every one of them was the same four\nlines with its channel name baked into its own name:\n\n    con = _open_db(path); _insert_<table>(con, run_id, rows); con.commit(); con.close()\n\nHere the channel is DATA. That is the difference that matters, not the line count: the\nbundle's characteristic failure was an argument ACCEPTED AND NEVER INSERTED (`work_events`\nwas one over 68 databases holding zero rows), and a 16-parameter signature is what made\nthat expressible. An unknown name raises here, because the only place a channel exists is\nthe table it is inserted from.\n\nThe last four had docstrings arguing at length that they must be separate from the bundle\n-- \"the bundle only fires when a batch window is unflushed, and a run whose batch count\ndivides evenly by its checkpoint interval has no such window\". That was always an argument\nabout `if pb:`, and `close()` writing unconditionally is what retired it.\n\n`None` is SKIPPED rather than refused, which is what the bundle's seven optional keywords\ndid: a caller that predates a channel passes nothing and writes no rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, run_id: int, *, channels=CHANNELS, **rows) -> None"
    },
    "Optimization/persistence/runtime_metrics.py": {
      "decorators": [],
      "doc": "runtime_metrics.py \u2014 per-arm RUNTIME (wall-clock compute cost) capture, written by the PARENT.\n\nEach worker RETURNS its whole-arm wall time + per-section totals (reorder / batch-build / pre /\nsim / extract / inv / DB-save) + warehouse identity; the supervisor (parent process) inserts one\nrow per arm into ``<run_root>/runtime_metrics.db``.  Writing from the single parent avoids SQLite\nwrite contention under the flat pool, and puts the DB at the highest run dir so it spans every cell.\n\nThis is RUNTIME (how long an arm took to COMPUTE) \u2014 orthogonal to the sim-modeled batch_stats\ndurations.  The `cost` chart family (Performance_Evaluations/cost/, a RUN-scope evaluation) reads it\nto rank rules by what they cost to run and to show WHERE the time goes, so recurring hot-paths (e.g.\na reorder/reslot-dominated arm \u2014 the valid-aisle recompute suspicion) stay visible \u2014 and so a WMS\nreader can be told what scoring one arriving unit costs.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/runtime_metrics.py::OUTSIDE_TOTAL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[('precomp_s', 'map precompute (setup, once per arm)')]"
    },
    "Optimization/persistence/runtime_metrics.py::PRE_STAMP_RUNTIME_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'a683d2d07c72'"
    },
    "Optimization/persistence/runtime_metrics.py::RUNTIME_DB": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'runtime_metrics.db'"
    },
    "Optimization/persistence/runtime_metrics.py::RUNTIME_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='runtime_metrics_db', declared_shape=declared_runtime_shape, meta_table='schema_meta', known_ids=('397b7e750e1d', 'c033ff9c85a5', PRE_STAMP_RUNTIME_SCHEMA_I..."
    },
    "Optimization/persistence/runtime_metrics.py::SECTIONS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[(s.column, s.label) for s in SPANS if s.label is not None]"
    },
    "Optimization/persistence/runtime_metrics.py::Span": {
      "decorators": [],
      "doc": "One measured span, and ALL FOUR of its spellings in one place (ticket 14).\n\nThe chain used to be: `SectionTimers.SECTIONS` -> `totals()` -> a result-dict key ->\npickled across the process seam -> a hand-written column in `record_arm`'s INSERT. A\nsection added to the first flowed automatically as far as the result dict and then\n**vanished at the INSERT** -- writing nothing and raising nothing. Here a span declares\nits spellings once and `record_arm` builds its statement from them, so the last hop is\nderived rather than remembered.\n\n`column` IS A HARD CONTRACT. Renaming one shifts the `runtime_metrics` schema id for a\nrelabelling and breaks archived rows' comparability with themselves, which is why\n`label` exists: the human name tracks what a span measures, the column never moves.\n`inv_s` is the standing example -- it predates the bin-mutation log, when that section\nwrote the `bin_inventory` snapshot; the table is gone and the section is now the\nper-batch conservation ledger, but the column is still `inv_s`.\n\n`label` is also the PARTITION flag. A span with a label is one of the seven that tile an\narm's wall clock; a span with `None` is an OVERLAY carved out of one of them, and putting\nan overlay in the stacked graph double-counts its seconds.",
      "kind": "class",
      "signature": "(NamedTuple)"
    },
    "Optimization/persistence/runtime_metrics.py::_ALL_DDL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(_DDL, _identity.meta_ddl('schema_meta'))"
    },
    "Optimization/persistence/runtime_metrics.py::_DDL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\nCREATE TABLE IF NOT EXISTS runtime (\\n cell TEXT NOT NULL,\\n pair TEXT NOT NULL,\\n config TEXT NOT NULL,\\n channel TEXT NOT NULL,\\n arm TEXT NOT NULL,\\n initial TEXT NOT NULL,\\n assignment TEXT ..."
    },
    "Optimization/persistence/runtime_metrics.py::_SETUP_COLUMNS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(('precomp_s', 'REAL'), ('precomp_src', 'TEXT'), ('map_lap_pct', 'REAL'))"
    },
    "Optimization/persistence/runtime_metrics.py::_migrate_setup_columns": {
      "decorators": [],
      "doc": "Add the setup-phase columns to a DB written before they existed; True if it changed.\n\n`CREATE TABLE IF NOT EXISTS` cannot widen an existing table, so a finished run whose\narms predate these columns needs an explicit ALTER before a backfill can write to it.\nEvery added column is NULLABLE with no default, so existing rows read NULL \u2014 \"never\nmeasured\", which is exactly what they are.\n\nRe-stamping afterwards is not optional: the file's recorded schema id describes its\nold shape, and leaving the two disagreeing turns every later read into an unvetted\nwarning.  A migrated run legitimately IS the current shape.",
      "is_async": false,
      "kind": "function",
      "signature": "(con) -> bool"
    },
    "Optimization/persistence/runtime_metrics.py::_parse_arm": {
      "decorators": [],
      "doc": "'uni_rank_labor_norsl' -> ('uni', 'rank_labor'); assignment may contain '_'.",
      "is_async": false,
      "kind": "function",
      "signature": "(arm: str)"
    },
    "Optimization/persistence/runtime_metrics.py::declared_runtime_shape": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/persistence/runtime_metrics.py::load_rows": {
      "decorators": [],
      "doc": "All runtime rows as dicts (empty if the DB is absent) \u2014 for the runtime graphs.\n\nWARNS rather than raising on an unvetted shape, unlike the sim-DB check in\n`Performance_Evaluations/core/context.py`.  The difference is what the number is FOR: these\nrows are wall-clock diagnostics about how long an arm took to compute, never a published\nresult, and this is a `SELECT *` into dicts that the graphs index defensively.  A runtime DB\nfrom a shape we have not seen is worth a named line in the log; it is not worth sinking the\nanalysis of a sweep that has already finished computing.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_root: str) -> list[dict]"
    },
    "Optimization/persistence/runtime_metrics.py::record_arm": {
      "decorators": [],
      "doc": "Insert one arm's runtime row (idempotent by (cell,pair,config,channel,arm) \u2014 a re-run/resume\nof the arm overwrites its row).  `res` = the worker return dict.\n\nThe four key fields are KEYWORD-ONLY, and deliberately.  This took a work-unit uid and\nunpacked it positionally, which was safe only while every uid meant\n`(pair, config, channel, arm)`.  The coupled unit's uid is `(label, 'coupled', arm_store,\narm_ful)` \u2014 same arity, different meanings \u2014 so the positional read would have written\n`channel = <arm_store>` into a TEXT NOT NULL column whose IntegrityError the supervisor\nswallows with a warning: neither the wrong value nor a rejected row would announce itself.\nA caller now has to say which is which, and a caller holding a differently-shaped uid gets\na TypeError at the call site instead of a wrong row.\n\nBest-effort: callers wrap this so a runtime-DB hiccup never sinks a real run.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_root: str, cell: str, res: dict, *, pair: str, config: str, channel: str, arm: str) -> None"
    },
    "Optimization/persistence/runtime_metrics.py::record_precompute": {
      "decorators": [],
      "doc": "Fill an existing arm's setup-phase measurement; True when a row was updated.\n\nThe inline path (`record_arm`) writes these at simulation time.  This is the other\ndoor: a finished run whose arms predate the columns can be measured afterwards by\nrebuilding the arm's warehouse and timing the precompute alone, without re-simulating\nhundreds of GB.  `source` separates the two so nothing downstream has to guess \u2014\n'inline' seconds are contended against the sweep's whole worker pool, a 'backfill'\nmeasurement is not, and the two are not comparable as ratios.\n\nUpdates only; a missing row means the arm never ran, and inventing one here would put\na measurement in the table with no simulation behind it.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_root: str, cell: str, uid, seconds: float, source: str, map_lap_pct=None) -> bool"
    },
    "Optimization/persistence/runtime_metrics.py::runtime_db_path": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_root: str) -> str"
    },
    "Optimization/persistence/runtime_semantics.py": {
      "decorators": [],
      "doc": "runtime_semantics \u2014 the runtime-metrics family's column semantics, beside its DDL.\n\nOne row per ARM, and every `*_s` here is a SPAN of WALL-CLOCK COMPUTE seconds \u2014 the same\nword \"seconds\" as the sim tables and a completely different instrument (incident class 11:\nsumming a deep-ladder mean against a phase wall cost a day and a wrong committed claim).\nThe clock axis on every time column is what makes that mistake refusable now.\n\nTags are metadata: importing this module changes no DDL, no shape id, no result.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/runtime_semantics.py::_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'enum', 'arm')"
    },
    "Optimization/persistence/runtime_semantics.py::_wall": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(note=None, null_means=None)"
    },
    "Optimization/persistence/sim_semantics.py": {
      "decorators": [],
      "doc": "sim_semantics \u2014 the sim_db (and keyframes) column semantics, declared beside their DDL.\n\nThe structured form of every warning `Picking_Data.py`'s DDL comments carry in prose: kind,\nunit, unit of account, grain, clock, null-meaning and the scar that earned each tag.  Keyed\nexactly as `SIM_DB_FAMILY.declared_shape()['tables']` keys its columns, so the completeness\ngate (`Tests/architecture/test_column_semantics.py`) is a dict diff with nothing to drift.\n\nEvery table of both families is COVERED \u2014 the convention pass left no remainder.\n\nTags are metadata: importing this module changes no DDL, no shape id, no result.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/sim_semantics.py::_BAY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'coordinate', 'row')"
    },
    "Optimization/persistence/sim_semantics.py::_ENUM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'enum', 'row')"
    },
    "Optimization/persistence/sim_semantics.py::_KEY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'id', 'row')"
    },
    "Optimization/persistence/warehouse_semantics.py": {
      "decorators": [],
      "doc": "warehouse_semantics \u2014 the warehouse-DB family's column semantics, beside its DDL.\n\nBuild-time counts, target shares and identity fingerprints; no time columns, low risk \u2014\ntagged anyway, because \"leave no remainder\" is the rule that keeps the next column honest.\n\nTags are metadata: importing this module changes no DDL, no shape id, no result.",
      "kind": "module",
      "signature": null
    },
    "Optimization/persistence/warehouse_semantics.py::_ENUM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'enum', 'row')"
    },
    "Optimization/persistence/warehouse_semantics.py::_KEY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'id', 'row')"
    },
    "Optimization/run_analysis.py": {
      "decorators": [],
      "doc": "run_analysis.py \u2014 registry-driven graph generator for completed simulation runs.\n\nReads the sim_meta docs written by run_simulation.py, rebuilds warehouse aisle maps via\nbuild_shared_assets, then runs the graphs/analyses registered under Performance_Evaluations\n(selected by a preset).  Each graph is a self-registering module; add/remove/tune graphs by\nediting Performance_Evaluations/presets.py \u2014 not this file.\n\nPer-leaf filenames (sim_meta / series) and the cross-profile aggregate subtree render through the\nrun-tree contract (`rt.leaf_path` / `rt.aggregate_dir`), never a basename join \u2014 the resolver is\nrooted at the RUN root (this module receives a CELL dir from analyze_run), and\nTests/integration/test_writer_paths_golden.py pins the rendered strings to the old literals.\n\nParallelism is a single FLAT worker pool (mirrors run_simulation): one global job list across\nall pairs \u00d7 configs fed to one ProcessPoolExecutor, then a second flat pool for the\ncross-profile aggregate stage.  `--granularity config` (default) = one job per config (context\nloaded once, shared across its graphs); `--granularity graph` = one job per (config, graph)\nfor maximum core utilization on sparse runs.\n\nUsage:\n  python run_analysis.py <base_dir>                        # default preset BY_INITIAL\n  python run_analysis.py <base_dir> --preset DEFAULT        # uni-only (drops opt_* arms)\n  python run_analysis.py <base_dir> --granularity graph --set labor.delta_topn.top_n=3\n\nThe default preset is BY_INITIAL (focus=all): it keeps BOTH the uniform (uni_*) and optimum\n(opt_*) initial-assignment arms, so the optimum-vs-uniform comparison is produced and the\ndownstream run_channel_rollup sees the full suite.  Pass --preset DEFAULT for the older\nuniform-only view.",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_analysis.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_analysis.py::_SLIM_KEYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('aisle_unittype_map', 'aisle_handling_map', 'total_bins')"
    },
    "Optimization/run_analysis.py::_aggregate_jobs": {
      "decorators": [],
      "doc": "Group every config's series doc by leaf pick-config name across profiles, prepare\neach aggregate group dir once, and emit the flat aggregate-stage job list.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, rt, cell, preset_name, granularity, cli_set, log, only=())"
    },
    "Optimization/run_analysis.py::_apply_run_shape": {
      "decorators": [],
      "doc": "Restore the RUN's own run-shaping params onto CONFIG before rebuilding its warehouse.\n\nA standalone re-analysis used to size the warehouse from whatever CONFIG this checkout\nhappens to hold \u2014 no max_skus, this build's fill \u2014 while the comment at the rebuild claimed\nit used \"the SAME per-regime sizing the run used\".  For any capped run that silently\nrebuilt a DIFFERENT warehouse than the sim ran on: wrong aisle count, wrong total_bins,\nand (per `regime_sizing_from_config`'s own docstring) misgrouped ff aisle stats and skewed\nchurn %.  The in-process analysis never had the bug because `run_simulation` had already\nmutated CONFIG in the same process; only the standalone path was affected.\n\nReturns the run's `max_skus` (None when unrecorded).  A run with no recorded spec \u2014 every\nrun before that file existed \u2014 warns loudly and keeps the old behaviour, which is the\nhonest answer: those runs never recorded what they were shaped with.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, log: logging.Logger) -> int | None"
    },
    "Optimization/run_analysis.py::_assignment_of": {
      "decorators": [],
      "doc": "The assignment rule inside an arm key \u2014 what `core.baseline` matches the baseline on.\n\nAn arm key is `<initial>_<assignment>[_<reslot>]`, and the baseline declaration selects\non `assignment == 'fifo'`.  A site's members are arm PAIRS, which carry no such field of\ntheir own, so the store half's rule is lifted onto the pair: the diagonal is rank against\nrank and the reference pair is `fifo`/`fifo` (site-dock 06), so the half naming `fifo`\nnames the reference pair.  Falls back to the whole key, which makes the baseline\nresolution fall back POSITIONALLY and say so \u2014 never silently onto another pair.",
      "is_async": false,
      "kind": "function",
      "signature": "(arm_key: str) -> str"
    },
    "Optimization/run_analysis.py::_coerce": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(v: str)"
    },
    "Optimization/run_analysis.py::_config_jobs": {
      "decorators": [],
      "doc": "Parent pre-pass: build slim shared assets per pair, prepare each config's output\ndirs once, and emit the flat config-stage job list.\n\n`only` restricts the run to a subset of keys AND suppresses the directory pre-pass:\na partial re-render replaces the artifacts it produces and must leave every other\nartifact where it is, so wiping the shared tops would be exactly wrong.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, rt, preset_name, granularity, cli_set, log, max_skus=None, only=())"
    },
    "Optimization/run_analysis.py::_coverage_record": {
      "decorators": [],
      "doc": "The coverage block this run recorded for `pair`, or None when it recorded none.\n\nA rebuild re-declares the run's stock levels from it before planning: the catalogue\ncarries none (ADR-0002) and the warehouse is sized from levels, so without this a\nre-analysis would size from nothing.  None is the honest answer for a run written before\nthe record existed, and `build_shared_assets` refuses rather than fabricating.",
      "is_async": false,
      "kind": "function",
      "signature": "(pair: str) -> dict | None"
    },
    "Optimization/run_analysis.py::_drain": {
      "decorators": [],
      "doc": "Run jobs on the flat pool (or inline if no pool); log per-job errors.  Returns the\nmerged access tally {'granted': {eval: n}, 'denied': {eval: n}} across all jobs.",
      "is_async": false,
      "kind": "function",
      "signature": "(pool, jobs, log) -> dict"
    },
    "Optimization/run_analysis.py::_merge_tally": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(total: dict, part: dict) -> None"
    },
    "Optimization/run_analysis.py::_parse_set": {
      "decorators": [],
      "doc": "Parse repeated --set KEY.PARAM=VALUE into {eval_key: {param: coerced_value}}.",
      "is_async": false,
      "kind": "function",
      "signature": "(items: list) -> dict"
    },
    "Optimization/run_analysis.py::_run_job": {
      "decorators": [],
      "doc": "Picklable unit of work.  job['stage'] is 'config' or 'aggregate'; job['eval_keys']\nis the list of evaluation keys to run against the job's context.  Returns\n(target, error, access_tally) \u2014 the tally is this job's broker grant/denial counts,\ncarried back to the parent for the run-end `[access]` summary.",
      "is_async": false,
      "kind": "function",
      "signature": "(job: dict)"
    },
    "Optimization/run_analysis.py::_sim_result_from_meta": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(meta: dict) -> dict"
    },
    "Optimization/run_analysis.py::_site_jobs": {
      "decorators": [],
      "doc": "Parent pre-pass for the SITE stage: one job per `(cell, pair)` on a COUPLED run.\n\n`[]` on every uncoupled run, and that is a structural return rather than a guard: the\nsite DBs simply do not exist, `site_inbound_dbs` finds none, and no job is emitted.  A\nsite stage reading an empty scope reports zeros rather than refusing \u2014 but it is worth\nsaying which zero this is, because `a-grant-is-not-an-output` is exactly the trap here:\nthe `[access]` summary counts INPUTS, so only the `[render]` run summary says whether\nanything was written.  Hence the log line below naming the pair count either way.\n\nONE JOB PER PAIR, not per arm pair, and `SiteContext` says why: the yard family's marks\nare `ranked` and `serial`, so every arm pair has to be in one context to be ranked\nagainst the others, and the declared output tree is one directory per pair.\n\nEVERY PATH IS RESOLVED THROUGH THE CONTRACT.  The site DBs come from\n`rt.site_inbound_dbs`, the arm pair from `rt.arm_pair_of`, and each leaf's sim DB from\n`rt.leaf_path(run, 'sim_db', strategy=)` \u2014 never a joined string, and never a tree level\nconsumed by NAME (`<pair>/store/store/` is a real path).",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, rt, cell, preset_name, granularity, cli_set, log, only=())"
    },
    "Optimization/run_analysis.py::_staffing_record": {
      "decorators": [],
      "doc": "The staffing record to stamp onto sim_result: the run's own when it recorded one,\nelse a reconstruction from the restored inputs with every value marked `assumed` --\nwhich is what a pre-record run's crew was, a compile-time constant nobody chose.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/run_analysis.py::_tree_for": {
      "decorators": [],
      "doc": "(RunTree, cell_name) for the run this CELL directory belongs to.\n\nrun_analysis receives a CELL dir (analyze_run calls it once per cell), so the resolver is\nrooted at the PARENT \u2014 where the run descriptor lives \u2014 and the cell name is the dir's own\nbasename.  A tree with NO descriptor (the e2e scratch trees, ad-hoc analyses) falls back to\nthe HEAD contract rooted at the same parent: the head templates render exactly the strings\nthe old hand-joins assumed, so the fallback is behavior-preserving while keeping the tree\nshape declared in one place.",
      "is_async": false,
      "kind": "function",
      "signature": "(cell_dir: str)"
    },
    "Optimization/run_analysis.py::_worker_log": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> logging.Logger"
    },
    "Optimization/run_analysis.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/run_analysis.py::run_analysis": {
      "decorators": [],
      "doc": "Re-run analysis on all completed sims under *base_dir* via the registry.\n\nTwo sequential flat-pool stages (config, then cross-profile aggregate); each stage is a\nsingle ProcessPoolExecutor sized by `workers` (workers<=1 runs inline).",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, log: logging.Logger, workers: int=1, preset: str='BY_INITIAL', granularity: str='config', cli_set: dict | None=None, only=()) -> None"
    },
    "Optimization/run_channel_rollup.py": {
      "decorators": [],
      "doc": "run_channel_rollup.py \u2014 combine per-channel best plans into a whole-warehouse saving.\n\nUNDER THE INBOUND-OFF MODEL the simulation treats store and fulfillment as two INDEPENDENT\nsections of the warehouse, each sweeping its OWN set of pick-time regression configs and\nanalyzed on its own by run_analysis.py (its own `fifo` baseline, its own winner).  This\npost-analysis step is the ONE place that combines them: for each inventory it reads every\nchannel's series JSON, computes each plan's absolute production-time saving vs that channel's\n`fifo` baseline, picks the best (config, plan) per channel across ALL that channel's configs,\nand SUMS the per-channel best savings into a cumulative whole-warehouse saving.\n\n**That summing is only valid while the channels are independent, and this script REFUSES a run\nwhere they are not.**  Under the inbound-off model each channel fields its own dock, its own\nreceiving crew and its own pool of putters, so absolute savings (sim-unit `ss_prod_hours`\ndeltas) are ADDITIVE \u2014 any (store-plan, fulfillment-plan) pairing is just the sum of the two\nrows, and the per-plan CSV doubles as a mix-and-match table with no cross-product simulation\nneeded.  Under a COUPLED run (`--couple-channels`) one dock and one pool of putters serve both\nchannels, so a store plan changes what the fulfillment crew can do and back: the premise is\nvoid, not merely the plumbing.  `rollup()` raises `ValueError` on such a run and\n`analyze_run` skips the step outright.  The whole published archive is inbound-off, so\nrefusing costs no history.\n\nRun AFTER run_analysis.py (which writes the series JSONs).  The analysis default preset is\nBY_INITIAL (focus='all'), so BOTH uni_* and opt_* arms land in the series and the rollup sees\nthe full suite.  If you instead analyzed with --preset DEFAULT (focus='uni'), every opt_* arm\nis dropped and the rollup picks the best from only half the suite (it warns when it detects\nthis \u2014 re-run the analysis without --preset DEFAULT):\n  python run_analysis.py <base_dir>\n  python run_channel_rollup.py <base_dir>\nOutputs (under <base_dir>, rendered by the run-tree contract as the `channel_rollup_csv` /\n`channel_rollup_summary_csv` artifacts \u2014 this module is their declared writer, and\nTests/integration/test_writer_paths_golden.py pins the rendered strings to the old literals):\n  the per-plan rollup CSV     \u2014 one row per (inventory, config, channel, plan): absolute + % saving\n  the rollup summary CSV      \u2014 best plan per channel + the cumulative whole-warehouse saving",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_channel_rollup.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_channel_rollup.py::_baseline_entry": {
      "decorators": [],
      "doc": "The channel's baseline plan \u2014 the fifo arm (uni_fifo), matching run_analysis's\nctx.base = strategies[0].  Prefer an explicit fifo key, then any fifo key.\n\nNO fallback to `strategies[0]`.  That fallback was silent and its output was plausible:\nevery `saving_abs` in both CSVs would be measured against an arbitrary arm, the summary\nwould name it `baseline_fifo_ss`, and nothing in the numbers would look wrong.  An arm set\nwithout a fifo rule is a configuration error made upstream \u2014 `_run_whatif_matrix` refuses\none before the simulation starts \u2014 so by the time a series doc reaches here, its absence\nmeans the analysis is reading something other than what the driver ran.  Failing here is\nthe only way that difference is ever visible.",
      "is_async": false,
      "kind": "function",
      "signature": "(strategies: list[dict]) -> dict | None"
    },
    "Optimization/run_channel_rollup.py::_channel_rows": {
      "decorators": [],
      "doc": "Return (channel_info, per-plan rows) for one channel's series doc.",
      "is_async": false,
      "kind": "function",
      "signature": "(meta: dict, series: dict) -> tuple[dict, list[dict]]"
    },
    "Optimization/run_channel_rollup.py::_find_series": {
      "decorators": [],
      "doc": "(sim_meta, series) JSON path pairs under base_dir \u2014 one per ANALYZED channel run.\n\nTraversal stays with `iter_channel_runs` (the layout owner, default marker = the sim_meta\nbasename); the per-leaf filenames render through `rt.leaf_path`, never a basename join.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, base_dir: str) -> list[tuple[str, str]]"
    },
    "Optimization/run_channel_rollup.py::_is_num": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(x) -> bool"
    },
    "Optimization/run_channel_rollup.py::_tree_for": {
      "decorators": [],
      "doc": "(RunTree, cell_name) for the run this CELL directory belongs to.\n\nThe rollup receives a CELL dir (analyze_run calls it once per cell), so the resolver is\nrooted at the PARENT \u2014 where the run descriptor lives \u2014 and the cell name is the dir's own\nbasename.  A tree with NO descriptor (scratch trees in tests, ad-hoc analyses) falls back to\nthe HEAD contract rooted at the same parent: the head templates render exactly the strings\nthe old hand-joins assumed, so the fallback is behavior-preserving while keeping the shape in\none place.  A descriptor that names an UNKNOWN schema still fails loudly (resolver_for\nraises before the fallback is consulted only for the missing-descriptor case).",
      "is_async": false,
      "kind": "function",
      "signature": "(cell_dir: str)"
    },
    "Optimization/run_channel_rollup.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/run_channel_rollup.py::rollup": {
      "decorators": [],
      "doc": "Roll one CELL directory's per-channel winners up into a whole-warehouse saving.\n\n`base_dir` is a cell dir; both output CSVs render through the run-tree contract\n(`rt.path('channel_rollup_csv' / 'channel_rollup_summary_csv', cell=...)`).\n\nREFUSES A COUPLED RUN with a `ValueError`, and the TYPE is the decision (site-dock 07\nsection 4).  `SystemExit` is the house style for a CLI shape refusal and is what\n`run_restock_selection` uses -- but `analyze_run._step` catches `Exception`, and\n`SystemExit` is not one, so a `SystemExit` here would abort `analyze_run` MID-RUN from\ninside its cell loop, killing every remaining cell's analysis and the cross-cell what-if\nstage.  `run_restock_selection` gets away with it because nothing calls it from the hub.\nA `ValueError` matches this file's own in-library precedent (`_baseline_entry`) and is\ncaught and logged per cell.  `analyze_run` additionally SKIPS the step on a coupled run,\nso the log reads `skipped: coupled run` rather than `analysis step failed` once per cell\n-- a failure line on every cell of a healthy run is how a reader learns to ignore\nfailure lines.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, log=print) -> dict"
    },
    "Optimization/run_map_precompute.py": {
      "decorators": [],
      "doc": "run_map_precompute.py \u2014 measure the map family's OFFLINE build on a finished run.\n\nWHY A SEPARATE CLI.  `strat.build()` now carries a timer, so every future run records its\nsetup span inline.  A run already on disk cannot be re-simulated to get one \u2014 that is\nhundreds of GB and hours \u2014 but it CAN be re-measured: the build is a pure function of the\nrun's own catalogue and warehouse shape, both of which the run kept.  This rebuilds those,\ntimes `build_optimal_map` alone, and writes the result back into the run's runtime table\nstamped `backfill` so nothing downstream mistakes it for an inline measurement.\n\n    python -m Optimization.run_map_precompute <run_root>\n\nRUN IT AFTER `analyze_run`, not before.  The measurements themselves go into the run's\nruntime table and survive anything, but the per-class detail lands under the dossier tree,\nand the dossier stage WIPES that tree on every analysis pass (it is a derived directory \u2014\na stale figure from a previous suite is worse than a missing one).  Backfill first and the\ncensus is silently gone by the time the site stages.\n\nTHE GATE, and why it is not optional.  A hand-rolled rebuild of the worker's setup phase is\na SECOND implementation of something the simulator already does \u2014 the exact drift class the\nrun dossier exists to end, reintroduced by the fix for it.  So the measurement is refused\nunless the reconstruction reproduces the warehouse identity the run itself recorded\n(`n_bins`, `regime_bins`, `n_aisles`, SKU count).  A number measured on a warehouse nobody\nchecked is an unfalsifiable claim, and this file would be where it entered.\n\nWHAT THE NUMBER IS AND IS NOT.  An UNCONTENDED, single-arm wall time: this process builds\none map at a time, where the sweep ran a 20-worker pool.  It is therefore not comparable as\na ratio against the loop seconds in the same table, which is why `precomp_src` exists and\nwhy every consumer must read it.  It also carries the per-class solver split \u2014 how much of\nthe assignment was solved exactly rather than greedily \u2014 which is the other thing the\npublished pages asserted without measuring.",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_map_precompute.py::MAP_RULES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple(sorted((k for k, o in OBJECTIVES.items() if o.precompute)))"
    },
    "Optimization/run_map_precompute.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_map_precompute.py::_identity_matches": {
      "decorators": [],
      "doc": "The gate: does the rebuild reproduce what the run recorded about its warehouse?\n\nA refusal here is the DESIGNED outcome, not a failure of this script.  It already\ncaught the real thing once: without the run's own shaping params applied first, the\nrebuild produced 5,814 aisles / 6.2 M bins against a run that recorded 384 / 398,500,\nand the timing taken on it would have been published as if it meant something.\n\n`regime_bins` is deliberately not checked: it is the per-CHANNEL slice, and the whole\nwarehouse is what the map is built over.",
      "is_async": false,
      "kind": "function",
      "signature": "(log, rec, shared) -> bool"
    },
    "Optimization/run_map_precompute.py::_metas_by_pair": {
      "decorators": [],
      "doc": "{pair: [(cell, ChannelRun, sim_meta), ...]} across the WHOLE run.\n\n`rt.channel_runs()` spans cells; the raw walker takes one cell directory, which is the\nshape the per-cell analysis uses and the wrong one here \u2014 it silently yields nothing\nwhen handed a run root.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, rt) -> dict"
    },
    "Optimization/run_map_precompute.py::_recorded": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rows, cell, pair, config, channel, arm) -> dict | None"
    },
    "Optimization/run_map_precompute.py::_workload_params": {
      "decorators": [],
      "doc": "WorkloadParams from the leaf's own committed config, or None when it is absent.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell, run)"
    },
    "Optimization/run_map_precompute.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None)"
    },
    "Optimization/run_map_precompute.py::measure_pair": {
      "decorators": [],
      "doc": "Time `build_optimal_map` once per (channel, map rule) for one inventory pair.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, rt, pair, metas, rows, log, max_skus=None) -> list"
    },
    "Optimization/run_map_precompute.py::run": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, log=None) -> None"
    },
    "Optimization/run_restock_selection.py": {
      "decorators": [],
      "doc": "run_restock_selection.py \u2014 phase 1's hand-off: rank the restock rules, pair them for phase 2.\n\nThe inbound funnel is two phases, not one product sweep (\"Design the phased funnel\",\n`.scratch/inbound-optimization`).  Phase 1 is a fresh, inbound-OFF run of the full assignment\nsuite whose ONLY job is to rank the restock rules; phase 2 sweeps the ten inbound policies over\nthe rules phase 1 chose.  This module is the seam between them, and it writes the one artifact\nthat carries the decision:\n\n    <phase-1 run root>/restock_selection.json\n\n**The unit is a restock RULE, not an arm.**  `strategies.CHANNEL_RESTOCKS` filters on\n`Strategy.restock`, so choosing `tmin` necessarily takes both `uni_tmin_norsl` and\n`opt_tmin_norsl`: 17 rules, each costing 2 arms.  \"Top-k arms\" is not an expressible unit, and\na selector that ranked arms would hand phase 2 an arm set it cannot configure.\n\n**The metric is TOTAL PRODUCTION HOURS** \u2014 unload + put + pick, the objective the whole effort\nis about (\"Define the inbound objective\").  It reaches this module as `ss_prod_total` in each\nchannel run's `series_json` artifact: a steady-state MEAN of `production_seconds` per batch,\nresolved through the contract rather than by name.  Ranking on\npick hours alone \u2014 all that `channel_rollup` and `whatif_labor` offer \u2014 would systematically\nfavour arms that buy pick time with put-away time, which is the exact trade phase 2 measures.\n\nThree things about that metric decide the shape of the code below:\n\n* **Do NOT read the cross-profile summary CSV.**  `_aggregate_series` normalizes each profile to\n  its own baseline, so that file holds RATIOS; `total_production_time` is deliberately absent\n  from `AGGREGATE_ORDER` for the same reason.  Hours are additive and physical, ranks are not,\n  so this module sums `ss_prod_total` over the profiles' series documents itself.\n* **Absence must be LOUD.**  The metric is capability-gated: a run with no `work_events` rows\n  produces no `total_production_time` at all and `ss_prod_total` comes out NaN \u2014 correctly, an\n  unmeasured leg is not a leg measured at zero.  A selector that read a missing value as a tie\n  would rank all seventeen rules on nothing, so a rule with any missing or non-finite reading is\n  EXCLUDED from the ranking and reported by name.\n* **`fifo` is mandatory, and is not one of the k.**  `run_channel_rollup._baseline_entry` needs\n  it as the analysis baseline, and `uni_fifo`/`opt_fifo` are byte-identical runs, which makes\n  the same row phase 2's order-blind negative control.  It rides along whether or not it ranks.\n\n**Phase 2 runs the COUPLED site dock, so the hand-off is a diagonal of RULE PAIRS.**  Under\none site dock a work unit finalizes both channel leaves at once, so phase 2 cannot sweep the\ntwo channels' rule sets independently: it needs to be told which store rule runs beside which\nfulfillment rule.  The pairing is the rank diagonal \u2014 store's 1st with fulfillment's 1st, and\nso on \u2014 because the campaign's question is \"does space-aware inbound beat FIFO\"; the arm pair\nis a CONTROL, not an axis, and the cross product would spend ~4x the wall confounding the\ninbound comparison with a placement interaction nobody asked about.  Hence `rule_pairs` in the\nartifact, built from the ORDERED `chosen` lists; `arms` keeps its per-channel meaning, for the\nleaf projections (\"Re-shape the funnel for arm pairs\", `.scratch/site-dock`).\n\n**Two consequences of that coupling land here rather than in phase 2:**\n\n* **The extension cap counts the UNION of families across both channels.**  Extending\n  `_gain_bundle_for` is work per FAMILY, site-wide \u2014 both channels choosing `map` costs one\n  extension, not two \u2014 so a per-channel cap of 3 could silently commit the project to six.\n  Channels are consumed in this module's existing deterministic order and that order is\n  RECORDED, because an alphabetical tiebreak between two unrelated hour scales is a decision,\n  not an implementation detail.\n* **Phase 1 is uncoupled and phase 2 is not, so hours do not cross the boundary.**  The\n  artifact stamps `put_regime: 'per-leaf'` to say so.  Nothing in the codebase reads two run\n  roots, so there is no join to gate \u2014 the stamp and the published caveat are the whole of how\n  the asymmetry is carried.  The one reachable mistake IS gated: pointed at a coupled run root,\n  `select()` refuses rather than emitting a hand-off indistinguishable from a real one.\n\nRun AFTER `analyze_run` (which writes the series JSONs):\n\n    python -m Optimization.analyze_run <phase-1 run root>\n    python -m Optimization.run_restock_selection <phase-1 run root>\n\nThen copy `rule_pairs.chosen` into phase 2's rule-pair list and run phase 2 with\n`--spec inbound_policies`.  The copy is deliberate and manual: the rule set is a decision a\nperson signs off on, and a spec that read a JSON file at import would make it invisible.",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_restock_selection.py::BASELINE_RULE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fifo'"
    },
    "Optimization/run_restock_selection.py::BASELINE_RULE_PAIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(BASELINE_RULE, BASELINE_RULE)"
    },
    "Optimization/run_restock_selection.py::DEFAULT_EXTENSION_CAP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3"
    },
    "Optimization/run_restock_selection.py::DEFAULT_K": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "5"
    },
    "Optimization/run_restock_selection.py::FULFILLMENT_CHANNEL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fulfillment'"
    },
    "Optimization/run_restock_selection.py::METRIC_FIELD": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'ss_prod_total'"
    },
    "Optimization/run_restock_selection.py::METRIC_QUANTITY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'total_production_time'"
    },
    "Optimization/run_restock_selection.py::PUT_REGIME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'per-leaf'"
    },
    "Optimization/run_restock_selection.py::RULE_PAIR_ORDER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(STORE_CHANNEL, FULFILLMENT_CHANNEL)"
    },
    "Optimization/run_restock_selection.py::STORE_CHANNEL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'store'"
    },
    "Optimization/run_restock_selection.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_restock_selection.py::_choose": {
      "decorators": [],
      "doc": "The top-k rules, with the extension cap applied and the `fifo` rider added.\n\nThe cap is 08's: only `FAITHFUL_GAIN_FAMILIES` have a faithful gain bundle today, and every\nother family refuses at the first drain by design.  If the top k holds more unfaithful\nfamilies than the cap allows, take the highest-ranked `extension_cap` of them and backfill\nfrom the next faithful rules down \u2014 so the cost of phase 2 is bounded by a decision made\nhere rather than discovered when an arm dies mid-sweep.\n\n**`committed` is the UNION, and it is mutated.**  It carries the distinct unfaithful\nfamilies already committed by the channels consumed before this one, and this channel adds\nto it.  Extending `_gain_bundle_for` is work per FAMILY, site-wide, so a family the other\nchannel already paid for is free here, and the cap bounds the total bill rather than one\nchannel's share of it \u2014 a per-channel cap of N can commit 2N, which is not bounding the\nthing the cap exists to bound.  The caller passes the same list to every channel and\nrecords both the union and the ORDER the channels were consumed in: one channel's choice\nnow depends on the other's through an order that means nothing physically, and there is no\norder-free alternative, because the two rankings share units but not scales and so admit no\nglobal rank to allocate against.  Declared and recorded beats arbitrary and hidden.",
      "is_async": false,
      "kind": "function",
      "signature": "(ranking, k: int, extension_cap: int, log, committed: list[str]) -> dict"
    },
    "Optimization/run_restock_selection.py::_finite": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(x) -> bool"
    },
    "Optimization/run_restock_selection.py::_rank_channel": {
      "decorators": [],
      "doc": "Rank every restock rule in one channel.  Lower hours win.\n\nAn arm's score is its `ss_prod_total` SUMMED over the channel's (profile, config) leaves \u2014\na steady-state per-batch mean of production seconds, summed, then reported in hours.  Every\narm is measured on the identical leaf set, so the sum is like-for-like; a MIN over leaves\nwould let two rules be scored on different profiles and is exactly the \"ranks are not\nadditive\" mistake one level down.\n\nA RULE's score is the minimum over its own arms, because selecting a rule takes both of them\nand what makes the rule worth carrying into phase 2 is that one of its arms performs.  Both\narm totals are recorded, so a reader can see when the two disagree.\n\nAn arm missing from a leaf, or reading non-finite there, disqualifies its RULE: a partial sum\nwould silently rank a rule that was measured on fewer profiles than its rivals as cheaper\nthan all of them.",
      "is_async": false,
      "kind": "function",
      "signature": "(leaves, log) -> dict"
    },
    "Optimization/run_restock_selection.py::_read_leaves": {
      "decorators": [],
      "doc": "[(ChannelRun, meta, series)] for every analyzed channel run under one cell dir.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell_dir: str)"
    },
    "Optimization/run_restock_selection.py::_rule_of": {
      "decorators": [],
      "doc": "The restock rule an arm key belongs to, from the strategy grid rather than by parsing.\n\n`uni_rank_labor_norsl` splits into three fields whose middle one contains underscores, so\nevery hand-rolled parse of an arm key is a guess.  `STRATEGY_BY_KEY` already holds the\nanswer the driver used when it built the arm, so unknown keys come back None and are\nreported rather than mis-bucketed into a rule that exists.",
      "is_async": false,
      "kind": "function",
      "signature": "(arm_key: str) -> str | None"
    },
    "Optimization/run_restock_selection.py::_rule_pairs": {
      "decorators": [],
      "doc": "The diagonal: rank-aligned `(store_rule, fulfillment_rule)`, store first.\n\nBuilt from the two channels' ORDERED `chosen` lists, never from `arms` \u2014 `arms` is\n`sorted(set(...))` and the sort destroys rank order, which is the only thing a diagonal\nreads.  The ranking itself stays per channel: the two hour scales are not comparable (the\nchannels' receiving loads differ ~7x, the same fact behind `PHASE2_THRESHOLD_DAYS`), so the\ndiagonal is applied AFTER ranking rather than by merging the rankings.\n\n**Ragged rankings zip to the common length and the artifact says so.**  The two lists\ngenuinely can differ in length \u2014 a rule disqualified for a missing reading, a cap that\nbackfills on one side only, or `len(chosen) < k`.  The zip takes the common length, logs\nloudly and stamps `complete: false` with the reason; the caller still WRITES, because the\nranking is the valuable part and a human needs to read it to decide between re-running\nphase 1 and accepting a shorter campaign.  The refusal that costs money belongs at the\nphase-2 launcher, not here.\n\n**A one-channel run produces no pairs at all, and does not fabricate the rider.**  A\ndiagonal needs two rankings; a pair list holding nothing but a manufactured\n`('fifo', 'fifo')` would look like a (very short) campaign rather than like the\nstore-only run it came from.",
      "is_async": false,
      "kind": "function",
      "signature": "(channels: dict, log) -> dict"
    },
    "Optimization/run_restock_selection.py::_tree_for": {
      "decorators": [],
      "doc": "(RunTree, [(cell_name, cell_dir)]) for a phase-1 RUN ROOT.\n\nRooted at the run root, not a cell dir \u2014 unlike `run_channel_rollup`, whose artifacts are\ncell-scoped.  The selection is a property of the RUN: it names the arm set a second run will\ncarry, so it belongs beside the run's own spec and layout descriptors rather than inside the\ncell whose numbers produced it.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str)"
    },
    "Optimization/run_restock_selection.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/run_restock_selection.py::select": {
      "decorators": [],
      "doc": "Rank the restock rules of a phase-1 run and write the selection artifact at its root.\n\n`base_dir` is the phase-1 RUN ROOT.  Phase 1 is one cell by design \u2014 ranking across two\nlayouts would rank two different warehouses on one scale \u2014 so a multi-cell run is refused\nrather than silently summed or silently reduced to its first cell.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, k: int=DEFAULT_K, extension_cap: int=DEFAULT_EXTENSION_CAP, log=print) -> dict"
    },
    "Optimization/run_simulation.py": {
      "decorators": [],
      "doc": "Warehouse assignment strategy simulation runner.\n\nRuns A/B/C strategy workers for each (pair \u00d7 regression-config) combination\nand saves all results to per-strategy SQLite DBs.  Graph generation is a\nseparate step: run_analysis.py <base_dir>.\n\nModes:\n  python run_simulation.py                          # new run, sequential\n  python run_simulation.py --workers 15             # flat pool, no idle\n  python run_simulation.py --resume <base_dir>      # resume a crashed run",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_simulation.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Optimization/run_simulation.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(_HERE, '..'))"
    },
    "Optimization/run_simulation.py::_apply_run_defaults": {
      "decorators": [],
      "doc": "Overlay a cell-matrix spec's `run_defaults` onto args, for a NEW run.\n\nA spec may carry run-level knobs (whatif_config.ERA_RUN_DEFAULTS: the calibrated era)\nbeside its cell axes.  They are DEFAULTS: a flag the user typed wins, with a note, so a\nspec cannot silently override a command line.  A resumed run never reaches this -- its\nrun spec recorded the resolved values and `_apply_run_spec` restores them.\nReturns the notes.",
      "is_async": false,
      "kind": "function",
      "signature": "(args, spec_dict: dict, explicit: set) -> list[str]"
    },
    "Optimization/run_simulation.py::_apply_run_spec": {
      "decorators": [],
      "doc": "Overlay a saved run_spec onto args for --resume: the saved value is the base; a flag the\nuser explicitly typed on the resume command overrides it (with a warning).  Returns\n(store_composition_override, notes) \u2014 the resolved store composition is injected directly so\na since-deleted --s-composition file can't break resume.",
      "is_async": false,
      "kind": "function",
      "signature": "(args, spec, explicit)"
    },
    "Optimization/run_simulation.py::_build_parser": {
      "decorators": [],
      "doc": "Every flag `run_simulation` accepts, and nothing else.\n\nSplit out of `main`, which was 875 lines holding four unrelated things: this parser, the\nCONFIG override loop, the two contract prechecks, and the run.  Only the last three have\nanything to do with each other.\n\nDEFAULTS COME FROM `CONFIG`, NEVER FROM A LITERAL.  That is the invariant the whole\nfive-seam chain rests on -- a flag whose default were written out here would disagree\nwith `settings.py` the moment the setting moved, and the run would be shaped by\nwhichever of the two the reader happened to trust.  `Tests/unit/test_cli_surface.py`\nchecks the pairs against the live CONFIG rather than against this file's text.\n\nReads CONFIG; never writes it.  A parser can therefore be built by a test without\nreshaping the run that follows.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> argparse.ArgumentParser"
    },
    "Optimization/run_simulation.py::_check_era_flags": {
      "decorators": [],
      "doc": "Enforce and complete the calibrated era's regime on the parsed args.\n\nUnder `--shift-drain-or-cap` (\".scratch/department-calibration\", \"Define the calibrated\nera\"):\n  * the legacy crew flags (`_ERA_DERIVED_FLAGS`) typed explicitly are an ERROR -- the\n    put and receiving crews are derived from the pickers, never declared;\n  * `--put-queue-split` is an error -- single queue only for this map; per-stream sizing\n    is the out-of-scope swept-axis effort;\n  * a nonzero `--put-swap-coef` is an error -- the put crew is sized by a closed form\n    with no cart-swap term (`staffing.implied_reorders`), and the single queue the era\n    runs never reads the coefficient anyway, so accepting it would record a knob the\n    run ignores and the derivation cannot price;\n  * the cadence is pinned at ONE release per day: an unset `--releases-per-day` is\n    completed to 1 (with a note), an explicit other value is an error, because the\n    derivation makes one batch one day's demand;\n  * the roll-over and the cut are completed to on (with a note): a day that reads\n    \"drained\" because cut demand was dropped is not equilibrium, and the worker forces\n    the cut anyway -- the record should say what ran.\nRaises `SystemExit` with the reason; returns the completion notes otherwise.\nModule-level so a test can hand it a Namespace.",
      "is_async": false,
      "kind": "function",
      "signature": "(args, explicit: set) -> list[str]"
    },
    "Optimization/run_simulation.py::_free_share": {
      "decorators": [],
      "doc": "An argparse `type=` for a free share of a bin bucket: a number in [0, 1).\n\nZero is a real setting (\"trust the fragmentation chain, no floor\"); one is a bucket\nsized for nothing, and the derivation would divide by `1 - 1`.  Fails at the parser,\nnaming the flag, rather than as a bare ValueError at run-spec build after the run dir\nexists (`sim_config.min_headroom`).",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str) -> float"
    },
    "Optimization/run_simulation.py::_futuresight_window": {
      "decorators": [],
      "doc": "An argparse `type=` for the futuresight window: `'all'` or a non-negative int.\n\nThe knob is a batch COUNT with a string sentinel for the oracle, and `_futuresight_batches`\n(the spec-build normalizer) rejects `'5'` as firmly as it rejects `'oracle'` \u2014 its\n`w != raw` test is what stops a fractional value, and a numeric STRING fails it too.  So\nthe conversion has to happen at the parser: without it, `--inbound-futuresight-batches 5`\nwould parse cleanly and then refuse at spec build, naming the settings constant rather\nthan the flag the user actually typed.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str)"
    },
    "Optimization/run_simulation.py::_nonneg_float": {
      "decorators": [],
      "doc": "An argparse `type=` for a coefficient where 0 is the default and negative is nonsense.\n\n`swap_coef` is validated nowhere else -- not in `PutQueueSpec.__post_init__`, not in\n`PutQueue`. A negative value makes a cart swap REDUCE the duration of the put it precedes,\nand a negative total moves the crew clock BACKWARD, which makes `crew_clock.can_start`\ntrue again after the whistle has already blown. None of that raises anywhere.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str) -> float"
    },
    "Optimization/run_simulation.py::_nonneg_int": {
      "decorators": [],
      "doc": "An argparse `type=` that rejects negatives but ALLOWS zero.\n\n`--recv-crew-size 0` is meaningful -- it is the off switch, and the default -- so unlike\n`_positive_int` below, zero passes.  Negative does not: it would reach\n`crew_clock.new_clocks` and raise three layers down, or worse, be swallowed by a\ntruthiness guard on the way.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str) -> int"
    },
    "Optimization/run_simulation.py::_positive_float": {
      "decorators": [],
      "doc": "An argparse `type=` for a DURATION that is meaningless at zero.\n\n`--recv-day-seconds 0` would mean \"the crew has a day of no length\", which is not the\nsame as \"the crew has no whistle\" (that is omitting the flag) and is not a configuration\nanyone wants.  Rejected at the parser for the same reason `--n-batches 0` is: argparse\nprints the flag name and exits 2, instead of the value being quietly turned into its\nopposite by an `or`.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str) -> float"
    },
    "Optimization/run_simulation.py::_positive_int": {
      "decorators": [],
      "doc": "An argparse `type=` that rejects zero and negatives.\n\n`--n-batches 0` was accepted, discarded by a truthiness guard, and the run went ahead\non CONFIG's value \u2014 so a smoke run asking for nothing quietly became a full one.  A\ncount that is meaningless at zero should fail at the parser, where argparse prints the\nflag name and exits 2, rather than three layers down or not at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str) -> int"
    },
    "Optimization/run_simulation.py::_unit_fraction": {
      "decorators": [],
      "doc": "An argparse `type=` for a utilization target: a number in (0, 1].\n\n\u03c1 is worked \u00f7 granted, so 0 means \"no work is ever done\" and above 1 means \"the crew\nworks more than the day it is granted\" -- both are not a scenario but a typo, and the\nderivation would divide by the first.  Fails at the parser, naming the flag.",
      "is_async": false,
      "kind": "function",
      "signature": "(text: str) -> float"
    },
    "Optimization/run_simulation.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/run_simulation.py::main._resolve_spec": {
      "decorators": [],
      "doc": "Selected cell-matrix (name, dict).  --whatif is the deprecated alias for scheduler_ab.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/run_whatif_delta.py": {
      "decorators": [],
      "doc": "run_whatif_delta.py \u2014 compare what-if scenarios against a reference cell.\n\nReads the four steady-state success metrics straight from each scenario's sim_*.db (no run_analysis\nneeded), keyed by (pair, pick-config, channel, arm), and diffs every cell against the reference cell:\n\n    task makespan  (\u03a3 task time = total labor)          batch makespan (last-picker finish)\n    throughput / task makespan (items / \u03a3 task time)    throughput / batch makespan (items / makespan)\n\nEmits the whatif_delta CSV (all four deltas), JSON (the docs site's matrix table), and a\nlabor-saving-vs-throughput-gain scatter (one point per cell\u00d7channel\u00d7arm) plus a median summary.  This\nis what makes a scheduling win legible: LPT should show \u0394batch-makespan \u2193 and \u0394throughput/batch \u2191\nwhile \u0394task-makespan (labor) and \u0394throughput/task stay \u2248flat.\n\n    python -m Optimization.run_whatif_delta <comparison_whatif_...>   # --reference defaults from config\n\nTree access goes through the versioned run-tree resolver (Optimization/runschema), never raw globs:\nthe previous relpath-splitting scan required a `<channel>` segment and therefore silently dropped\nEVERY store-only run, whose sim DBs sit directly under `<config>/`.  Output paths render through the\nsame contract (`rt.path(...)`) \u2014 this module is the declared writer of the three whatif_delta\nartifacts, and the golden test (Tests/integration/test_writer_paths_golden.py) pins the rendered\nstrings to the literals the old hand-joins produced.",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_whatif_delta.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='run_whatif_delta cross-cell reader', tables={'batch_stats': ('batch_id', 'duration', 'task_makespan', 'total_items'), 'task_stats': ('batch_id', 'duration')})"
    },
    "Optimization/run_whatif_delta.py::WIN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "50"
    },
    "Optimization/run_whatif_delta.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_whatif_delta.py::_bind": {
      "decorators": [],
      "doc": "Bind a sim DB to its OWN schema vintage (`Schema.dataset.bind`), or None if unvetted.\n\nNone routes the caller to the frozen pre-registry read path (`connect.read_only` + a PRAGMA\nprobe) \u2014 synthetic test fixtures and cold-archive vintages keep exactly their historical\nbehavior, the same vetted/unvetted split `Picking_Data._query_rows` uses.  A VETTED file that\ncannot serve the declared read raises `RequirementUnmet` naming the missing column instead of\ndegrading: failing loudly at open is the whole point of the declaration.",
      "is_async": false,
      "kind": "function",
      "signature": "(db: str)"
    },
    "Optimization/run_whatif_delta.py::_cell_labels": {
      "decorators": [],
      "doc": "(layout, zoning_label) for one descriptor cell \u2014 the prose the docs matrix shows.",
      "is_async": false,
      "kind": "function",
      "signature": "(layout_cell: dict) -> tuple[str, str]"
    },
    "Optimization/run_whatif_delta.py::_channel_of": {
      "decorators": [],
      "doc": "The channel a run belongs to \u2014 `ChannelRun.channel_key`, kept as a name here because\nthree what-if writers import it.  The rule itself now lives on the dataclass that owns the\nambiguity, so there is one answer rather than one per reader.",
      "is_async": false,
      "kind": "function",
      "signature": "(cr) -> str"
    },
    "Optimization/run_whatif_delta.py::_delta_scatter": {
      "decorators": [],
      "doc": "The cross-cell picture: labor saving (x) vs throughput/batch gain (y), one point per\ncell \u00d7 channel \u00d7 arm, drawn through the chartkit contract \u2014 gutter legend, reserved\nfooter band, short title with the reading aid demoted to the subtitle.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, reference, out_path)"
    },
    "Optimization/run_whatif_delta.py::_metrics": {
      "decorators": [],
      "doc": "Steady-state means (last WIN batches) of the four success metrics from batch_stats:\n\n    task_ms   = task makespan  = \u03a3 task time = total labor        (metric a)\n    batch_ms  = batch makespan = last-picker finish (wall-clock)  (metric b)\n    thr_batch = items / batch makespan                            (metric d)\n    thr_task  = items / task makespan                             (metric c)\n\nNew runs carry task_makespan/thr_task as columns; legacy DBs fall back to the task_stats join.\nA VETTED file binds to its own vintage (`_bind`) and the column probe is answered from the\nBOUND SHAPE (`ds.has`), never a PRAGMA; only an unvetted file (synthetic fixtures, cold\narchives) keeps the frozen PRAGMA probe.  The aggregate SQL itself is untouched either way \u2014\nfloat drift on published CSVs is forbidden.\n\nRead-only + immutable, never a plain `sqlite3.connect`: these DBs are written in WAL mode, so\nany writable open \u2014 including a bare read \u2014 makes SQLite drop `-wal`/`-shm` sidecars beside a\n~1 GB archived file.  That is exactly what `scripts/archive_cells.py::_quick_check` exists to\ndefend against (`_bind` passes the same immutable promise through `dataset.bind`).",
      "is_async": false,
      "kind": "function",
      "signature": "(db: str)"
    },
    "Optimization/run_whatif_delta.py::_pct": {
      "decorators": [],
      "doc": "Signed % change of b vs reference a; + always = better.  NaN on missing/zero ref/cell.",
      "is_async": false,
      "kind": "function",
      "signature": "(a, b, higher_better)"
    },
    "Optimization/run_whatif_delta.py::_scan": {
      "decorators": [],
      "doc": "{(pair, pickcfg, channel, arm): metrics} for one cell, via the run-tree resolver.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str) -> dict"
    },
    "Optimization/run_whatif_delta.py::_write_delta_json": {
      "decorators": [],
      "doc": "Write the whatif_delta_json artifact \u2014 the per-cell \u00d7 per-channel MEDIAN table the docs\nsite renders.\n\nPreviously this file had no producer at all: docs/macros.py:whatif_matrix read it, and the\nonly way to get one was to hand-copy and rename the whatif_labor summary JSON.  The\nfour-metric key names (dthr_batch / dtask_ms / dbatch_ms / dthr_task) are the ones that\nmacro prefers.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, rows, reference, cell_names) -> str"
    },
    "Optimization/run_whatif_delta.py::_write_delta_json._med": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(vs, key)"
    },
    "Optimization/run_whatif_delta.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/run_whatif_delta.py::run": {
      "decorators": [],
      "doc": "Engine: diff every cell under base_dir vs the reference cell; write the whatif_delta CSV +\nthe labor-vs-throughput scatter, and return the CSV path.  Importable so the analysis hub\ncalls it in-process (no argv).  A single-cell run has nothing to diff and is a no-op.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, reference=None, log=None)"
    },
    "Optimization/run_whatif_labor.py": {
      "decorators": [],
      "doc": "run_whatif_labor.py \u2014 cross-cell THROUGHPUT / LABOR-HOURS what-if (re-analyze, no re-sim).\n\nReads the SAME per-cell sim_*.db as run_whatif_delta, but tells the story in MODELED HOURS and\nadds the picker-scheduler axis that lives only in the CELL directory name (k1_off_rr / k1_off_lpt).\n\n  per (channel, scheduler, assignment-fn, initial):\n    labor_hours  = \u03a3 task_makespan / 3600      total serial labor over the run (all batches)\n    batch_hours  = \u03a3 duration      / 3600      parallel wall-clock makespan\n    thr_items_hr = mean thr_batch  \u00d7 3600      items per hour\n    labor_saved  = labor(fifo) \u2212 labor(arm)    absolute hours a better assignment fn saves vs FIFO\n\nTwo levers: the ASSIGNMENT FUNCTION sets total labor (labor_saved vs FIFO); the LPT SCHEDULER\nconverts leftover picker imbalance into throughput at ~flat labor (batch_hours \u2193, labor flat).\n\n*** CAVEAT: hours here are SIM-MODELED pick-time (batch_stats SECONDS \u00f7 3600), NOT wall-clock\n    or staffing hours.  They are a modeled-effort figure for comparing arms, not a schedule.\n    This text is the module docstring AND the `--help` description, so a stale formula here\n    is printed to users; it read `/ 3.6e6` while the code divided by 3600, because the sim\n    emits seconds and the analysis layer had declared milliseconds. ***\n\nReuses run_whatif_delta's engine (resolver-driven scan + _metrics steady-state means); adds full-run\nsums for the true total labor.  Outputs to the run root: the whatif_labor CSV + JSON\n(whatif_matrix()-compatible), and four PNGs.  All output paths render through the run-tree\ncontract (`rt.path(...)` / the whatif PNG glob's directory) \u2014 this module is the declared writer\nof those artifacts, and Tests/integration/test_writer_paths_golden.py pins the rendered strings\nto the literals the old hand-joins produced.\n\n  python -m Optimization.run_whatif_labor <comparison_whatif_...> [--baseline fifo]\n         [--baseline-initial match|uni|opt] [--reference k1_off_rr] [--pairs sum|median]",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_whatif_labor.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='run_whatif_labor hours reader', tables={'batch_stats': ('duration', 'task_makespan', 'total_items')})"
    },
    "Optimization/run_whatif_labor.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': {'batch_stats.task_makespan': 'sum', 'batch_stats.duration': 'sum', 'batch_stats.total_items': 'sum'}}"
    },
    "Optimization/run_whatif_labor.py::_ARM_HUE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'#5b7fa6'"
    },
    "Optimization/run_whatif_labor.py::_HOURS_NOTE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'modeled sim pick-time hours (batch_stats seconds / 3600) \u2014 not wall-clock'"
    },
    "Optimization/run_whatif_labor.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_whatif_labor.py::_SCHED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'rr': 'round_robin', 'lpt': 'lpt'}"
    },
    "Optimization/run_whatif_labor.py::_SCHED_COLOR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'round_robin': '#4c78a8', 'lpt': '#f58518'}"
    },
    "Optimization/run_whatif_labor.py::_agg": {
      "decorators": [],
      "doc": "Aggregate `field` over rows grouped by keyfn: how in {'sum','mean'}.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, keyfn, field, how)"
    },
    "Optimization/run_whatif_labor.py::_batch_hours_bars": {
      "decorators": [],
      "doc": "Per-arm RR-vs-LPT batch-makespan as a DUMBBELL \u2014 dot per scheduler, connecting\nline, and the LPT improvement labelled at the row end.  Zero-anchored paired bars\nmade the few-percent deltas this figure exists to show invisible; the x-window hugs\nthe data and declares its truncation instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, out_path, order=())"
    },
    "Optimization/run_whatif_labor.py::_channels": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rows)"
    },
    "Optimization/run_whatif_labor.py::_data_xlim": {
      "decorators": [],
      "doc": "chartkit.data_ylim's x-axis counterpart for the horizontal bar/dumbbell panels:\nhug the data (+pad fraction), fold in the `include` anchors, and declare the\ntruncation whenever the window excludes zero.  `pad_hi` widens only the right side\n(room for row-end labels).",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, values, *, pad=0.08, pad_hi=None, include=())"
    },
    "Optimization/run_whatif_labor.py::_headroom": {
      "decorators": [],
      "doc": "Raise the y ceiling by `frac` of the current span \u2014 reserved space for the panel\ntag, so it can never sit on data.  Applied equally per panel, it preserves a shared\nscale's identical limits.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, frac)"
    },
    "Optimization/run_whatif_labor.py::_hours": {
      "decorators": [],
      "doc": "Full-run SUMS over ALL batches (the true totals): labor/batch hours + items + n_batches.\n\nRead-only + immutable for the same reason as `run_whatif_delta._metrics`: a WAL-mode DB opened\nany other way leaves `-wal`/`-shm` sidecars beside an archived ~1 GB file.",
      "is_async": false,
      "kind": "function",
      "signature": "(db: str)"
    },
    "Optimization/run_whatif_labor.py::_labor_saved_bars": {
      "decorators": [],
      "doc": "ONE panel: the channel where the assignment function moves real hours.  The other\nchannels' spreads are minutes-scale noise, and ranking noise with confident bars was\nthe audit finding this figure fixes \u2014 they are omitted (and named in the subtitle)\nrather than drawn at 100\u00d7 zoom.  Neutral single hue: sign is the zero line's job.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, baseline, out_path, order=())"
    },
    "Optimization/run_whatif_labor.py::_med": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(vals)"
    },
    "Optimization/run_whatif_labor.py::_ordered": {
      "decorators": [],
      "doc": "`names` arranged in the canonical arm order; anything the order does not know\n(defensive \u2014 should not happen on real rows) goes last, alphabetically.",
      "is_async": false,
      "kind": "function",
      "signature": "(names, order)"
    },
    "Optimization/run_whatif_labor.py::_panel_tag": {
      "decorators": [],
      "doc": "Panel caption INSIDE the axes (top-left, in the reserved headroom) \u2014 the figure's\ntitle band is sized for the suptitle + subtitle only, so per-panel `ax.set_title`\nwould collide with it.",
      "is_async": false,
      "kind": "function",
      "signature": "(ax, text)"
    },
    "Optimization/run_whatif_labor.py::_parse_arm": {
      "decorators": [],
      "doc": "'uni_rank_labor_norsl' -> ('uni', 'rank_labor', 'norsl'); assignment may contain '_'.",
      "is_async": false,
      "kind": "function",
      "signature": "(arm: str)"
    },
    "Optimization/run_whatif_labor.py::_pct": {
      "decorators": [],
      "doc": "Signed % change of b vs reference a; + always = better.  NaN on missing/zero/NaN.",
      "is_async": false,
      "kind": "function",
      "signature": "(a, b, higher_better)"
    },
    "Optimization/run_whatif_labor.py::_scan_labor": {
      "decorators": [],
      "doc": "{(pair, pickcfg, channel, arm): {**steady means (whatif_delta), **full-run hours}}.\n\nWalks via the versioned run-tree resolver.  The previous relpath scan required a `<channel>`\nsegment (`len(rel) < 4: continue`) and so returned NOTHING for a store-only run.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str) -> dict"
    },
    "Optimization/run_whatif_labor.py::_scheduler_of": {
      "decorators": [],
      "doc": "'k1_off_lpt' -> 'lpt'; 'k1_off_rr' -> 'round_robin' (the swept scheduler is the cell suffix).",
      "is_async": false,
      "kind": "function",
      "signature": "(cell: str) -> str"
    },
    "Optimization/run_whatif_labor.py::_throughput_scatter": {
      "decorators": [],
      "doc": "One point per (scheduler, assignment fn): total labor vs mean throughput, per\nchannel.  Channels are different workloads, so each panel keeps its own scale and\nsays so.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, out_path)"
    },
    "Optimization/run_whatif_labor.py::_uplift_bars": {
      "decorators": [],
      "doc": "Median LPT-vs-reference throughput uplift per arm \u2014 vertical bars on ONE shared\nvalue scale across panels, with a 95% t-interval whisker wherever an arm has two or\nmore per-(pair \u00d7 config) samples.  `samples` is {(channel, assignment): [uplift %]}.",
      "is_async": false,
      "kind": "function",
      "signature": "(samples, out_path, order=())"
    },
    "Optimization/run_whatif_labor.py::canonical_arm_order": {
      "decorators": [],
      "doc": "THE one arm ordering every what-if figure shares (this writer and the volume writer\nalike): assignment functions sorted by mean labor hours saved vs `baseline` in the\n`reference` cell, largest saving first; arms with no reference sample sort last,\nalphabetically.  Computed once per run and passed to every chart, so a reader can track\na strategy across figures by position alone.\n\nWorks off the long-row fields both writers emit (cell / pair / pickcfg / channel /\ninitial / assignment / labor_hours); the saving is matched-initial (uni vs uni,\nopt vs opt), isolating the assignment function.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, reference, baseline='fifo')"
    },
    "Optimization/run_whatif_labor.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/run_whatif_labor.py::run": {
      "decorators": [],
      "doc": "Engine: cross-cell throughput / labor-hours over base_dir; write the whatif_labor CSV/JSON\n+ the four PNGs, and return the CSV path.  Importable so the analysis hub calls it in-process\n(no argv).  `pairs` in {'sum','median'} blends the catalogs; a single-cell run still emits\nthe CSV/JSON (its cross-cell deltas are empty).",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, baseline='fifo', baseline_initial='match', reference=None, pairs='sum', log=None)"
    },
    "Optimization/run_whatif_volume.py": {
      "decorators": [],
      "doc": "run_whatif_volume.py \u2014 cross-cell CUMULATIVE-VOLUME what-if: how much work each scheduler\nfinishes per unit of elapsed time (re-analyze, no re-sim).\n\nThe per-config `throughput.volume` graph can only see ONE channel-run leaf, so it compares\nassignment functions inside a single cell.  The scheduler axis lives only in the CELL directory name\n(k1_off_rr / k1_off_lpt), so the round-robin-vs-LPT comparison has to happen here, at the run root,\nalongside run_whatif_delta and run_whatif_labor.\n\n  per (channel, scheduler, assignment-fn, initial):\n    elapsed_hours      = \u03a3 duration      / 3600     running wall-clock the work took\n    labor_hours        = \u03a3 task_makespan / 3600     running serial labor (what the work COST)\n    mean_thr_items_hr  = items / elapsed_hours      the chord slope of the cumulative curve\n    auc_gain_vs_rr_pct area between this cell's cumulative curve and the reference cell's,\n                       over the reference's own area, on a shared hour grid\n\nWHY THIS SEPARATES THE TWO LEVERS.  The assignment function changes labor_hours (less work).  The\nscheduler does not \u2014 it changes elapsed_hours (the same work, less picker idle time).  Plotting\ncumulative volume against elapsed hours makes that visible as a steeper line reaching the same\nvolume sooner, which is the honest picture of \"more throughput at flat labor\".\n\n*** CAVEAT: hours are SIM-MODELED pick-time (batch_stats SECONDS / 3600), NOT wall-clock or\n    staffing hours.  A modeled-effort figure for comparing arms, not a schedule. ***\n\n  python -m Optimization.run_whatif_volume <comparison_whatif_...> [--reference k1_off_rr]",
      "kind": "module",
      "signature": null
    },
    "Optimization/run_whatif_volume.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='run_whatif_volume series reader', tables={'batch_stats': ('batch_id', 'duration', 'task_makespan', 'total_items')})"
    },
    "Optimization/run_whatif_volume.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': {'batch_stats.duration': 'ratio', 'batch_stats.total_items': 'read', 'batch_stats.task_makespan': 'read'}}"
    },
    "Optimization/run_whatif_volume.py::_FIELDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['cell', 'scheduler', 'pair', 'pickcfg', 'channel', 'arm', 'initial', 'assignment', 'items', 'elapsed_hours', 'labor_hours', 'mean_thr_items_hr', 'auc_item_hours', 'shape_index', 'auc_gain_vs_ref_p..."
    },
    "Optimization/run_whatif_volume.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Optimization/run_whatif_volume.py::_curves_png": {
      "decorators": [],
      "doc": "The headline curves: cumulative items vs elapsed hours for ONE arm under both\nschedulers, per channel.  Elapsed hours and item counts are commensurate across the\npanels, so both axes share ONE honest scale; the per-scheduler rate/finish figures\nlive in the gutter legend, off the axes; the wall-clock caveat is the subtitle.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, scans, cells, out_path)"
    },
    "Optimization/run_whatif_volume.py::_gain": {
      "decorators": [],
      "doc": "% area between curve `a` and reference `b`, over b's area, on the hours BOTH cover.",
      "is_async": false,
      "kind": "function",
      "signature": "(a, b)"
    },
    "Optimization/run_whatif_volume.py::_gain_png": {
      "decorators": [],
      "doc": "Median LPT-over-RR gain in whole-run mean throughput (the chord slope), one\nhorizontal bar per arm in the canonical order, ONE shared x scale across panels.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, out_path, order=())"
    },
    "Optimization/run_whatif_volume.py::_metrics_of": {
      "decorators": [],
      "doc": "Endpoint + area metrics for one cumulative series.  See volume_curve for why shape_index is\na stability diagnostic and not a score.",
      "is_async": false,
      "kind": "function",
      "signature": "(s)"
    },
    "Optimization/run_whatif_volume.py::_pick_best": {
      "decorators": [],
      "doc": "Highest-throughput arm in the reference cell for this channel \u2014 the one worth drawing.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, channel)"
    },
    "Optimization/run_whatif_volume.py::_series": {
      "decorators": [],
      "doc": "(hours, items) cumulative arrays for one arm, ordered by batch.\n\nA VETTED file binds to its own schema vintage (`Schema.dataset.bind`, immutable=True \u2014 the\nescape-hatch `.con` still carries the same aggregate SQL, unchanged); an unvetted one\n(synthetic fixtures, cold archives) opens through `Schema.connect.read_only` with the same\nread-only + immutable promise, replacing the hand-built `?mode=ro&immutable=1` URI this\nfunction used to assemble.  Either way no `-wal`/`-shm` sidecar is ever left beside an\narchived copy.",
      "is_async": false,
      "kind": "function",
      "signature": "(db: str)"
    },
    "Optimization/run_whatif_volume.py::_writer_tree": {
      "decorators": [],
      "doc": "The resolver this module's OUTPUTS render through \u2014 `rt` itself, or the head contract.\n\nA re-analysis reads an OLD run through the run's OWN contract (`rt`), but the artifacts this\nmodule WRITES are declared by whatever contract is current: one committed vintage\n(e69b6d392929, the 2026-07-29 scheduler sweeps) predates whatif_volume_csv/_json entirely,\nand `rt.path` on it would KeyError.  For such runs the outputs follow the HEAD document at\nthe same root \u2014 exactly the strings the old hand-joins produced (today's basenames onto the\nold run root), so nothing moves; every READ still goes through the run's own document.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt)"
    },
    "Optimization/run_whatif_volume.py::build_rows": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cells, reference)"
    },
    "Optimization/run_whatif_volume.py::log": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "logging.getLogger('analysis')"
    },
    "Optimization/run_whatif_volume.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Optimization/run_whatif_volume.py::run": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, reference: str | None=None, log=log) -> list"
    },
    "Optimization/run_whatif_volume.py::scan": {
      "decorators": [],
      "doc": "{(pair, config, channel, arm): series} for one cell, via the versioned resolver.\n\nThe key deliberately EXCLUDES the cell, so the same key in two cells is the same arm under two\nschedulers \u2014 the join run_whatif_delta and run_whatif_labor already use.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell)"
    },
    "Optimization/runschema/__init__.py": {
      "decorators": [],
      "doc": "runschema \u2014 the CONTENT-ADDRESSED contract for a simulation run's on-disk tree.\n\nDownstream tools (analysis, graphs, docs ingest, the replay viewer) resolve paths through a resolver\nfrom this package instead of joining strings.  A structural change to the tree means editing\n``schema.py``; its identity follows automatically.\n\n    from Optimization import runschema\n    rt = runschema.resolver_for(base_dir)        # the schema the run ITSELF was written with\n    for cell, cr, sim_db in rt.sim_dbs():\n        ...\n\nWHY THERE IS NO VERSION NUMBER\n------------------------------\nA schema is identified by the sha256 of its own declared shape\n(``Optimization/schemas/run_tree/<short>.json``), so:\n\n  * nothing to pick, register, or increment \u2014 the id is derived, and re-derivable by anyone;\n  * two branches that change the tree differently get different ids instead of both calling\n    themselves \"v2\";\n  * a run records the exact schema that produced it, so an OLD run stays resolvable forever;\n  * there is no ``LATEST`` to compute \u2014 \"current\" is a named pointer (``INDEX.json``'s `head`),\n    because hashes have no natural order.\n\nCompatibility is negotiated by FEATURE, not by comparing numbers: a contract lists the template\nvocabulary it uses and ``resolver.SUPPORTED_FEATURES`` says what this build implements, so an\nunreadable contract fails by naming the missing feature.\n\nThere is deliberately no support for runs predating the cell-matrix refactor \u2014 they have no\ndescriptor at all.  ``preflight.py`` keeps the committed store honest by proving it against real\ncanary runs before a simulation.",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/__init__.py::UnsupportedRunTree": {
      "decorators": [],
      "doc": "Raised for a run whose tree schema this build cannot resolve.",
      "kind": "class",
      "signature": "(Exception)"
    },
    "Optimization/runschema/__init__.py::_resolver_for_contract": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base: str, doc: dict, layout: dict | None) -> RunTree"
    },
    "Optimization/runschema/__init__.py::analysis_path": {
      "decorators": [],
      "doc": "Render an ANALYSIS artifact's path for `rt`'s run \u2014 HEAD contract first.\n\n`reader_for` in one call, because the two-step dance was written out at three separate\ncall sites and got forgotten at each of them in turn: the analysis writes into\ndirectories a finished run's own contract has never heard of, so `rt.path(...)` raises\n`KeyError` for a location that is perfectly legal to write.\n\nNone when no contract declares the name, so a caller can log that rather than crash a\nbest-effort analysis step.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt: RunTree, artifact: str, **parts) -> str | None"
    },
    "Optimization/runschema/__init__.py::head": {
      "decorators": [],
      "doc": "The schema id a NEW run stamps (INDEX.json's `head`), or None if the store is empty.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> str | None"
    },
    "Optimization/runschema/__init__.py::reader_for": {
      "decorators": [],
      "doc": "A resolver for reading an ANALYSIS artifact out of `rt`'s run \u2014 HEAD contract first.\n\nA run is resolved through the contract it was written with, which is right for\neverything the SIMULATION produced: those bytes are on disk exactly where that era's\ndocument says.  It is wrong for anything the ANALYSIS produced, because the analysis\nthat wrote them is today's, and today's contract is where today's evaluations put\ntheir output.\n\nAn absence-only fallback is not enough, and the failure is silent.  When a name exists\nin BOTH documents but the artifact MOVED, the run's older document wins, the reader\nlooks in a directory the current suite no longer writes, `os.path.exists` says no, and\nnothing reports a problem \u2014 the file simply never appears.  That happened to the\nper-arm summary when the analysis suite grew its `tables/` layout, and only the\nexperiment guard noticed, one layer downstream.\n\nReturns None when neither document declares the name, so a caller can distinguish\n\"not in this vintage\" from \"declared but absent on disk\".",
      "is_async": false,
      "kind": "function",
      "signature": "(rt: RunTree, artifact: str) -> RunTree | None"
    },
    "Optimization/runschema/__init__.py::resolve_base_dir": {
      "decorators": [],
      "doc": "Resolve a CLI run argument: an absolute path as-is, a bare name against COMPARISON_OUTPUT_DIR.\n\nEvery downstream CLI over a run tree shares this, so `python -m Optimization.run_whatif_delta\ncomparison_20260728_120000` behaves the same as the analysis hub instead of only resolving\nrelative to the current directory.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> str"
    },
    "Optimization/runschema/__init__.py::resolver_for": {
      "decorators": [],
      "doc": "Return a resolver bound to the schema the run's own ``run_layout.json`` records.\n\nRaises UnsupportedRunTree when the descriptor is missing, carries no `schema_id`, or names a\nschema this checkout has no document for \u2014 a loud failure is the point.  Silently guessing the\nlayout is what made every downstream tool drift in the first place.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> RunTree"
    },
    "Optimization/runschema/contract.py": {
      "decorators": [],
      "doc": "runschema.contract \u2014 derive, store and diff CONTENT-ADDRESSED run-tree contracts.\n\nA contract document is generated from ``runschema/schema.py``'s LEVELS + ARTIFACTS tables and\ncommitted at ``Optimization/schemas/run_tree/<short>.json``, so consumers that aren't Python (the JS\nviewer, notebooks, any future ETL) read the same declaration the resolver enforces.  Nothing here\ninspects a run directory \u2014 that's ``preflight.observe``; this module only deals with the DECLARED\nshape.\n\nIDENTITY IS DERIVED, NOT CHOSEN\n-------------------------------\n``schema_id = \"sha256:\" + sha256(canonical_json(shape))`` where `shape` is the projection in\n``_shape_only``.  Consequences worth understanding:\n\n  * The id cannot be an INPUT to its own hash, so `schema_id` is excluded from `_shape_only`.\n  * The document is IMMUTABLE: its filename is its own content hash.  Mutable state (the\n    source-change trigger, the current head, provenance) therefore lives in ``INDEX.json``, never in\n    the document.\n  * Prose (`note`/`condition`/`writer`) is excluded, so documenting the tree better never mints a\n    new schema; anything a path resolver depends on is included, so a real change always does.\n\nTwo fingerprints answer different questions:\n\n  * ``schema_id``          \u2014 hash of the DECLARED shape. Identity. Changes only on a real change.\n  * ``source_fingerprint`` \u2014 hash of the shape-DEFINING source files (in INDEX.json). A cheap\n    *trigger*, not proof: a docstring edit trips it, which is exactly why the preflight then runs\n    canaries before concluding anything.\n\nRun standalone:\n    python -m Optimization.runschema.contract --write   # mint/refresh the document for schema.py\n    python -m Optimization.runschema.contract --check   # exit 1 if the committed store is stale",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/contract.py::SHAPE_SOURCES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('Optimization/runschema/runlayout.py', 'Optimization/runschema/sim_manifest.py', 'Optimization/simdriver/sim_assets.py', 'Optimization/simdriver/__init__.py', 'Optimization/simdriver/cells.py', 'O..."
    },
    "Optimization/runschema/contract.py::SHAPE_SOURCE_DIRS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('Optimization/simconfig/configs',)"
    },
    "Optimization/runschema/contract.py::SHORT_LEN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "12"
    },
    "Optimization/runschema/contract.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Optimization/runschema/contract.py::_INDEX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_TREE_DIR, 'INDEX.json')"
    },
    "Optimization/runschema/contract.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(_HERE, '..', '..'))"
    },
    "Optimization/runschema/contract.py::_SCHEMA_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_REPO_ROOT, 'Optimization', 'schemas')"
    },
    "Optimization/runschema/contract.py::_TREE_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_SCHEMA_DIR, 'run_tree')"
    },
    "Optimization/runschema/contract.py::_git_head": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> str"
    },
    "Optimization/runschema/contract.py::_now": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> str"
    },
    "Optimization/runschema/contract.py::_sha": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(payload: bytes) -> str"
    },
    "Optimization/runschema/contract.py::_shape_only": {
      "decorators": [],
      "doc": "The projection that DEFINES the tree \u2014 the exact material `schema_id` hashes.\n\nExcluded on purpose:\n  * `schema_id` \u2014 an identity cannot be an input to its own hash.\n  * `note` / `condition` / `description` \u2014 documenting the tree better must never mint a new\n    schema, so prose is stripped before hashing.\n  * `writer` \u2014 WHICH function creates a file is attribution, not tree shape; moving a producer\n    does not move the file.\n  * `source_fingerprint` / provenance \u2014 mutable, and lives in INDEX.json.\n\nIncluded: everything a path resolver depends on.  Note `axes`, `levels`, `tables` and\n`resolves_via` are LISTS, so their ORDER is significant \u2014 reordering them is a real change.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> dict"
    },
    "Optimization/runschema/contract.py::adopt": {
      "decorators": [],
      "doc": "Store `doc` and make it the head, recording its provenance.  Returns its schema id.\n\nIdempotent: adopting the current head only refreshes the mutable `source_fingerprint`.  The\nprevious head becomes the new entry's `parent`, which is what restores the ordering that hashes\ninherently lack.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict, *, label: str | None=None, source_fp: str | None=None, commit: str | None=None, created: str | None=None) -> str"
    },
    "Optimization/runschema/contract.py::build": {
      "decorators": [],
      "doc": "Generate the contract document for the CURRENT declaration in runschema/schema.py.\n\nIts `schema_id` is derived from the tables \u2014 there is nothing to choose and nothing to register.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str=_REPO_ROOT) -> dict"
    },
    "Optimization/runschema/contract.py::contract_path": {
      "decorators": [],
      "doc": "Where the document for a schema id lives.  Accepts a full id or an already-short form.",
      "is_async": false,
      "kind": "function",
      "signature": "(schema_id: str) -> str"
    },
    "Optimization/runschema/contract.py::diff_shape": {
      "decorators": [],
      "doc": "Human-readable differences between two contracts' SHAPES (a = old, b = new).\n\nEmpty list \u21d4 identical schema_id.  Under content addressing this is the ONLY explanation of why\nan id changed, so it must cover every field `_shape_only` hashes \u2014 including list ORDER, which\nan earlier set-based comparison silently ignored (reordering axes flipped the hash but produced\nno diff, i.e. \"STALE\" with no reason given).",
      "is_async": false,
      "kind": "function",
      "signature": "(a: dict, b: dict) -> list[str]"
    },
    "Optimization/runschema/contract.py::head": {
      "decorators": [],
      "doc": "The schema id a NEW run stamps.  A named pointer, because hashes have no natural order.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> str | None"
    },
    "Optimization/runschema/contract.py::load": {
      "decorators": [],
      "doc": "The committed document for a schema id (full or short), or None when absent.",
      "is_async": false,
      "kind": "function",
      "signature": "(sid: str) -> dict | None"
    },
    "Optimization/runschema/contract.py::load_all": {
      "decorators": [],
      "doc": "Every committed contract document, keyed by full schema id.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict[str, dict]"
    },
    "Optimization/runschema/contract.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Optimization/runschema/contract.py::read_index": {
      "decorators": [],
      "doc": "The schema store's index, or an empty skeleton when it doesn't exist yet.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/runschema/contract.py::schema_id": {
      "decorators": [],
      "doc": "The content-addressed identity of a contract: sha256 over its canonical shape.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> str"
    },
    "Optimization/runschema/contract.py::short_id": {
      "decorators": [],
      "doc": "'sha256:a412c613ba76\u2026' -> 'a412c613ba76' \u2014 the display/filename form.\n\nNB the full id contains a colon, which is an illegal filename character on Windows, so the\nshort form is what names files; the full id lives inside the document.",
      "is_async": false,
      "kind": "function",
      "signature": "(schema_id: str) -> str"
    },
    "Optimization/runschema/contract.py::source_fingerprint": {
      "decorators": [],
      "doc": "Hash of every shape-defining source file (path + content), in declared order.\n\nLine endings are NORMALISED to \\n before hashing.  On Windows a checkout, stash/pop, or\nautocrlf change rewrites CRLF<->LF without touching a single statement; hashing raw bytes made\nthat look like a structural change and cost a needless canary pair.  Content changes still\nregister, which is the whole point.\n\nA missing file contributes its path plus a MISSING marker rather than being skipped, so\nDELETING a shape source is detected instead of silently matching.\n\n`SHAPE_SOURCE_DIRS` is then hashed by NAME LIST and content, which is how an\nADDED file registers -- a path tuple can only ever describe files someone already\nthought of, and `simconfig/configs/` is auto-discovered.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str=_REPO_ROOT) -> str"
    },
    "Optimization/runschema/contract.py::verify_store": {
      "decorators": [],
      "doc": "Integrity of the whole committed store.  Empty list = sound.\n\nChecks the properties that make content addressing trustworthy without any registry:\n  * every stored document re-hashes to its own `schema_id` AND to its filename;\n  * the index head resolves to a stored document;\n  * every `parent` link resolves (or is null), with exactly one root.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list[str]"
    },
    "Optimization/runschema/contract.py::write": {
      "decorators": [],
      "doc": "Write a contract document to its versioned path, atomically.  Returns the path.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> str"
    },
    "Optimization/runschema/contract.py::write_index": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(index: dict) -> str"
    },
    "Optimization/runschema/hook_check.py": {
      "decorators": [],
      "doc": "hook_check.py \u2014 Stop-hook backstop that nudges a run-tree schema resync on drift.\n\nWired via .claude/settings.json (Stop hook), alongside context/arch/hook_check.py.  Runs ONLY the\ncheap stage of the preflight \u2014 a source-fingerprint comparison plus a staleness check of the\ncommitted contract.  It NEVER runs a canary simulation and ALWAYS exits 0: this is advisory and\nmust not block a turn, slow it down, or fail when something upstream is half-edited.\n\nThe nag it prints is the whole point.  Discovering that the output tree moved at the START of the\nnext simulation is cheap; discovering it after a multi-hour run, from broken graphs, is not.\n\nRun standalone:  python Optimization/runschema/hook_check.py",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/hook_check.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', '..'))"
    },
    "Optimization/runschema/hook_check.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> int"
    },
    "Optimization/runschema/preflight.py": {
      "decorators": [],
      "doc": "runschema.preflight \u2014 prove the run-tree contract BEFORE a simulation, not after.\n\nThe problem this solves: every structural change to the output tree used to be discovered\ndownstream, weeks later, as hand-written throwaway extraction code.  This runs at the FRONT of a\nrun instead.\n\nFive stages, cheapest first:\n\n  1. DETECT   \u2014 hash the shape-defining sources (contract.SHAPE_SOURCES) against the\n                `source_fingerprint` in the committed contract.  Unchanged \u21d2 return immediately.\n                This is the common path: no canary, no cost.\n  2. CANARY   \u2014 only on a source change.  Two tiny REAL runs into a temp dir:\n                  A: mixed catalog + 2-cell sweep  -> `<channel>/` present, `_frozen/`, what-if\n                  B: store-only    + 1-cell        -> `<channel>/` ABSENT, no `_frozen/`\n                One canary alone would encode \"channel always present\" \u2014 the exact assumption\n                behind the store-only bugs this package exists to prevent.\n  3. OBSERVE  \u2014 generalize every produced path back to a template, consuming levels POSITIONALLY\n                against run_layout.json's axis lists (never by directory name: the store config is\n                named `store` and so is the store channel, so `<pair>/store/store/` is real).\n  4. VALIDATE \u2014 compare observed templates against the committed contract.  Produced-but-undeclared\n                is the finding that matters; declared-but-never-produced and wrong optionality are\n                caught too.\n  5. ADOPT    \u2014 the schema's identity is the hash of `schema.py`'s declaration, so there is no\n                number to pick and no module to scaffold.  When the declaration ALREADY covers the\n                observed tree, store the new document, make it the head, and refresh the downstream\n                orchestrator files (context/artifacts.yml, Visualization/static/schema.json).  When\n                the code moved but `schema.py` didn't, print the exact ARTIFACTS entries to add\n                (`--apply` inserts them) and adopt the resulting id.\n\nCLI:\n    python -m Optimization.runschema.preflight            # detect; canary + prompt on change\n    python -m Optimization.runschema.preflight --check    # stage 1 only; exit 1 on drift, no writes\n    python -m Optimization.runschema.preflight --yes      # unattended: adopt without prompting\n    python -m Optimization.runschema.preflight --apply    # also write observed entries into schema.py\n    python -m Optimization.runschema.preflight --force    # run the canaries even with no change\n\nEnvironment:\n    ILO_SKIP_PREFLIGHT=1   skip entirely (set for the canary subprocesses so they cannot recurse)",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/preflight.py::CANARY_ARGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['--n-batches', '2', '--max-skus', '200', '--coverage-days', '1', '--keyframe-interval', '1', '--workers', '1']"
    },
    "Optimization/runschema/preflight.py::SKIP_ENV": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'ILO_SKIP_PREFLIGHT'"
    },
    "Optimization/runschema/preflight.py::_BATCHES_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('^_batches_[0-9a-f]+\\\\.pkl$')"
    },
    "Optimization/runschema/preflight.py::_CKPT_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('^_ckpt_(?P<strategy>.+)\\\\.pkl$')"
    },
    "Optimization/runschema/preflight.py::_EXT_FORMAT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'.db': 'sqlite', '.json': 'json', '.csv': 'csv', '.png': 'png', '.pkl': 'pickle', '.log': 'text', '.txt': 'text'}"
    },
    "Optimization/runschema/preflight.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Optimization/runschema/preflight.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "contract._REPO_ROOT"
    },
    "Optimization/runschema/preflight.py::_STRATEGY_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('^sim_(?P<strategy>.+?)(?P<kf>\\\\.keyframes)?\\\\.db$')"
    },
    "Optimization/runschema/preflight.py::_artifact_key_for": {
      "decorators": [],
      "doc": "A stable snake_case key for a newly-observed template.",
      "is_async": false,
      "kind": "function",
      "signature": "(tmpl: str) -> str"
    },
    "Optimization/runschema/preflight.py::_axis_values": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(layout: dict) -> dict[str, set]"
    },
    "Optimization/runschema/preflight.py::_collapse_channel_variants": {
      "decorators": [],
      "doc": "Fold `\u2026/{channel}/x` + `\u2026/x` into one `\u2026/{channel?}/x` entry.\n\nThe two canaries observe the SAME artifact under both shapes \u2014 mixed catalogs put it under a\nchannel dir, store-only ones don't.  Emitting two scaffold entries would hard-code exactly the\n\"channel is always there\" assumption this whole package exists to prevent, so an artifact seen\nin both shapes becomes one required entry with an optional segment.",
      "is_async": false,
      "kind": "function",
      "signature": "(undeclared: dict[str, str]) -> dict[str, str]"
    },
    "Optimization/runschema/preflight.py::_confirm": {
      "decorators": [],
      "doc": "Interactive gate.  Non-interactive without --yes is a refusal, never a silent write.",
      "is_async": false,
      "kind": "function",
      "signature": "(question: str, assume_yes: bool, echo) -> bool"
    },
    "Optimization/runschema/preflight.py::_declared_templates": {
      "decorators": [],
      "doc": "contract path template -> artifact key, normalized to the observe() vocabulary.\n\n`{channel?}` expands to BOTH shapes (with and without the segment) because both are legitimate\n\u2014 that is precisely the optionality being modelled.\n\nSkips artifacts that are not FILE templates: aliases (a `resolves_via` entry has no path of its\nown) and directory entries.  Matching observed files against those would be meaningless, and\nwould make them permanently show up as \"declared but never produced\".",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> dict[str, str]"
    },
    "Optimization/runschema/preflight.py::_format_of": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(tmpl: str) -> str"
    },
    "Optimization/runschema/preflight.py::_generalize_file": {
      "decorators": [],
      "doc": "Turn a run-root-relative path into a template, consuming levels POSITIONALLY.\n\nPositional consumption is the whole point: `<pair>/store/store/` is a real path where the\nconfig and the channel share a name, so any name-based guess is wrong. Each level is taken only\nat its own depth, and only if the segment is a known value for that axis.",
      "is_async": false,
      "kind": "function",
      "signature": "(segments: list[str], axes: dict[str, set]) -> str"
    },
    "Optimization/runschema/preflight.py::_report": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(findings: dict, echo) -> None"
    },
    "Optimization/runschema/preflight.py::_run_canary": {
      "decorators": [],
      "doc": "Run one canary simulation in a SUBPROCESS; return its run-root dir (None on failure).\n\nIsolation is by environment, not new flags: PROFILE_INPUT_DIR / COMPARISON_OUTPUT_DIR are\nalready the driver's input/output roots (sim_config), so the real results drive is never\ntouched.  ILO_SKIP_PREFLIGHT stops the child from re-entering this module.",
      "is_async": false,
      "kind": "function",
      "signature": "(workdir: str, name: str, *, spec: str, mixed: bool, echo=print) -> str | None"
    },
    "Optimization/runschema/preflight.py::_scope_of": {
      "decorators": [],
      "doc": "Which level a newly-observed template belongs to \u2014 the deepest axis it consumes.\n\n`_aggregate/` is per-CELL even though it carries {config}/{channel} beneath it: those are the\ngrouping key of a cross-profile rollup, not the run's own config/channel levels.",
      "is_async": false,
      "kind": "function",
      "signature": "(tmpl: str) -> str"
    },
    "Optimization/runschema/preflight.py::_template_regex": {
      "decorators": [],
      "doc": "Compile a declared path template into a matcher for OBSERVED templates.\n\nObserved templates carry placeholders as literal text (`{cell}`), so those compare exactly; only\nthe glob forms are fuzzy:  `**/` spans any number of segments, `*` stays within one.",
      "is_async": false,
      "kind": "function",
      "signature": "(declared: str) -> re.Pattern"
    },
    "Optimization/runschema/preflight.py::_write_canary_catalog": {
      "decorators": [],
      "doc": "Generate one tiny inventory+affinity pair in the profiles-tree layout the driver discovers.\n\nMirrors Tests/test_channel_runner_smoke.py's catalog: a real (if small) affinity matrix over\nconsecutive SKUs, because affinity-driven arms refuse to run on an empty one.  `mixed=False`\nomits the fulfillment family, which is what makes the run store-only (no `<channel>` level).",
      "is_async": false,
      "kind": "function",
      "signature": "(profiles_root: str, profile: str, *, mixed: bool, num_skus: int=300)"
    },
    "Optimization/runschema/preflight.py::_yaml_pattern": {
      "decorators": [],
      "doc": "contract template -> the angle-bracket pattern style context/artifacts.yml uses.",
      "is_async": false,
      "kind": "function",
      "signature": "(tmpl: str) -> str"
    },
    "Optimization/runschema/preflight.py::apply_entries": {
      "decorators": [],
      "doc": "Insert proposed ARTIFACTS entries into runschema/schema.py.  Returns the path.\n\nEditing the ONE declaration is the whole point of dropping per-version modules: there is no\n`v<N+1>.py` to scaffold, so the new schema id falls out of the edited tables automatically.",
      "is_async": false,
      "kind": "function",
      "signature": "(entries: list[tuple[str, str]]) -> str"
    },
    "Optimization/runschema/preflight.py::ensure": {
      "decorators": [],
      "doc": "The full preflight.  Returns 0 to proceed with the run, non-zero to stop.\n\nStage 1 short-circuits when nothing shape-defining changed, which is the common case.",
      "is_async": false,
      "kind": "function",
      "signature": "(echo=print, *, assume_yes: bool=False, force: bool=False, keep_workdir: bool=False, apply_entries_to_schema: bool=False) -> int"
    },
    "Optimization/runschema/preflight.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Optimization/runschema/preflight.py::observe": {
      "decorators": [],
      "doc": "Derive the on-disk shape of a finished run: the set of generalized path templates.\n\nReturns {'templates': {template: count}, 'levels_seen': {...}, 'layout': {...}}.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> dict"
    },
    "Optimization/runschema/preflight.py::proposed_entries": {
      "decorators": [],
      "doc": "[(artifact_key, source_text), \u2026] \u2014 the ARTIFACTS entries the canaries say are missing.\n\npath/format/scope/optional are derived mechanically from the observed template.  `writer` and\n`condition` stay TODO because no script can infer which function creates a file or why it is\nconditional; those TODOs are deliberately loud.\n\nChannel variants collapse first, so an artifact seen under BOTH tree shapes becomes one\n`{channel?}` entry rather than two \u2014 otherwise the proposal would hard-code the very\n\"channel is always present\" assumption this package exists to prevent.",
      "is_async": false,
      "kind": "function",
      "signature": "(findings: dict) -> list[tuple[str, str]]"
    },
    "Optimization/runschema/preflight.py::run_canaries": {
      "decorators": [],
      "doc": "Both canaries.  A = mixed catalog + 2-cell sweep; B = store-only + single cell.",
      "is_async": false,
      "kind": "function",
      "signature": "(workdir: str, echo=print) -> dict[str, str | None]"
    },
    "Optimization/runschema/preflight.py::sources_changed": {
      "decorators": [],
      "doc": "(changed, recorded_fingerprint, current_fingerprint) for the shape-defining sources.\n\nThe recorded value lives in INDEX.json, NOT in a contract document: a document is named by its\nown content hash, so it must never carry a mutable field.  An empty store counts as changed \u2014\nthere is nothing to compare against yet.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple[bool, str, str]"
    },
    "Optimization/runschema/preflight.py::update_artifacts_yml": {
      "decorators": [],
      "doc": "Rewrite context/artifacts.yml `path_pattern:` lines from the contract.\n\nLine-level edit (not a YAML round-trip) so the file's comments and ordering survive \u2014 they are\nthe documentation.  Only ids that exist in BOTH the contract and the file are touched.\nReturns the list of ids whose pattern changed.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict, path: str | None=None) -> list[str]"
    },
    "Optimization/runschema/preflight.py::update_viewer_schema": {
      "decorators": [],
      "doc": "Emit Visualization/static/schema.json \u2014 the slice of the contract the front end reads.\n\nThe viewer builds its cascading run selectors from `axes` and `levels`, so adopting a new schema\nchanges the UI's navigation without a single JS edit.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict, path: str | None=None) -> str"
    },
    "Optimization/runschema/preflight.py::validate": {
      "decorators": [],
      "doc": "Compare both canaries against the committed contract.\n\nReturns {'undeclared': {...}, 'never_produced': [...], 'optionality': [...], 'ok': bool}.\n`undeclared` is the finding that matters most: a file the tree produces that no consumer can\nbe told about.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict, obs_a: dict, obs_b: dict) -> dict"
    },
    "Optimization/runschema/preflight.py::validate._match": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(tmpl: str) -> str | None"
    },
    "Optimization/runschema/resolver.py": {
      "decorators": [],
      "doc": "runschema.resolver \u2014 ONE generic resolver that interprets ANY run-tree contract document.\n\nThere is deliberately no per-schema Python.  A contract is pure data (levels + path templates), so\nthis class reads it the way a template engine reads a template: change the tree by editing\n``schema.py``, not by writing a new resolver.  That is what makes a content-addressed schema id\nworkable \u2014 the id names a *document*, and this module is the single interpreter of all of them.\n\nEverything that used to be hardcoded here now comes from the contract:\n\n  * the frozen-vs-cell-local planned inventory fallback  -> ``resolves_via`` (first existing wins)\n  * ``sim_<arm>.db`` -> arm                              -> inverse-render of the ``{strategy}``\n                                                            placeholder in the sim_db template\n  * the cross-cell what-if artifact list                 -> ``group: 'whatif'``\n  * the ``_aggregate`` literal                           -> the ``aggregate_dir`` template\n\nTree TRAVERSAL still belongs to ``Optimization/runschema/runlayout.py`` \u2014 this class delegates to it and adds\nthe cell level, optional-segment handling, and the axis inventory the viewer navigates by.\n\nFeature negotiation replaces a version number: a contract lists the template vocabulary it uses and\n``SUPPORTED_FEATURES`` says what this build implements, so an unreadable contract fails by NAMING\nthe missing feature instead of comparing integers.",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/resolver.py::RunTree": {
      "decorators": [],
      "doc": "Resolver for one run directory.  Construct via ``runschema.resolver_for(base_dir)``.\n\nEvery accessor returns an ABSOLUTE path (or an iterator of them).  Existence is NOT implied \u2014\noptional artifacts legitimately do not exist on some runs (see each artifact's `condition`), so\ncallers check `os.path.exists` where it matters.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/runschema/resolver.py::RunTree.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, base_dir: str, contract: dict, layout: dict | None=None)"
    },
    "Optimization/runschema/resolver.py::RunTree.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree._owner_of": {
      "decorators": [],
      "doc": "Which artifact a concrete path belongs to \u2014 MOST-SPECIFIC template wins.\n\nA wildcarded `{strategy}` happily glob-matches `sim_x.keyframes.db`, because a template\nplaceholder cannot know that no arm name contains a dot.  Rather than encoding that\n(false-in-general) fact, ownership is decided the way preflight's observe stage decides\nit: every template that matches is a candidate, and the one with the MOST literal\ncharacters \u2014 the most specific claim \u2014 owns the path.  `sim_{strategy}.keyframes.db`\nout-literals `sim_{strategy}.db`, so a keyframe sidecar is never a sim DB.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, abs_path: str) -> str | None"
    },
    "Optimization/runschema/resolver.py::RunTree._parts_from_leaf": {
      "decorators": [],
      "doc": "Split a channel-run's relative dir into level parts, POSITIONALLY.\n\nNever by name: the store config and the store channel share the name `store`, so\n`<pair>/store/store/` is real and name-matching would mis-assign it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, leaf_rel: str) -> dict"
    },
    "Optimization/runschema/resolver.py::RunTree.aggregate_dir": {
      "decorators": [],
      "doc": "<cell>/_aggregate/<group_key> \u2014 the cross-profile aggregate subtree.\n\ngroup_key is ChannelRun.group_key: `<config>` on a store-only run, `<config>/<channel>` on a\nmixed one, so the optional channel level is already folded in by the caller.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, group_key: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.arm_pair_halves": {
      "decorators": [],
      "doc": "`{channel: arm_key}` for one arm pair \u2014 `{}` when it cannot be resolved.\n\nThe other half of `arm_pair_of`: that inverts the FILENAME to the pair, this splits\nthe pair into its two channels' arms.  Both live here because both are inversions of\none declaration, and a consumer that re-derived either would be a second copy to fix\nwhen the joiner or the channel-order source moves.\n\n`have` is `{channel: {arm keys that channel actually ran}}` \u2014 whatever the caller\nalready has on disk (a leaf's recorded arm list, a directory's sim DBs).\n\nPOSITIONAL, against `run_layout.json`'s declared `channels`: half k belongs to\nchannel k, because that is the order `workunits._site_db_path(pair_dir, arm_store,\narm_ful)` BUILT the stem in (`zip(_sa_s, _sa_f)`, store first, off the same list).\nMembership then decides only whether the positionally-named arm was RUN.\n\nRESOLVING BY MEMBERSHIP ALONE IS WRONG, and both consumers of this had that bug\nindependently (site-dock 25, found by mutation).  Both channels draw restock rules\nfrom ONE vocabulary, so on a real campaign a leaf holds arms named like BOTH halves;\n\"the channel that holds exactly one of the two names\" then matches neither, every\npair is skipped, and the site stage runs on nothing.  A one-arm probe cannot see it.\n\nEvery split position is tried rather than `split('__')`: an arm name may contain the\njoiner, which is why the arm pair is ONE capture in the declared template.\n\nNO FALLBACK when the layout declares no channel order.  `write_run_layout` writes\n`channels` on every run, so that is unreachable in production \u2014 and a fallback would\nhave to GUESS which half is which channel, where a wrong guess reads one leaf's rows\nunder the other channel's name with nothing to say so.\n\nA TWO-PLACE AGREEMENT WITH NO THIRD WITNESS, recorded because nothing enforces it:\n`run_simulation` passes the channel list to `write_run_layout`, and\n`workunits._prepare_site_run` zips its leaves in the order `_channel_runs_for`\nproduced them.  Both are store-first today and the stem is built from the second\nwhile this reads the first.  Emit them in different orders and the mis-pairing is\nSILENT *only* where both channels ran arms with the same names (the membership check\nbelow catches every other shape by failing to resolve at all) \u2014 which is exactly the\ncampaign case.  Move either and move the other.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, arm_pair: str, have: dict) -> dict"
    },
    "Optimization/runschema/resolver.py::RunTree.arm_pair_of": {
      "decorators": [],
      "doc": "'\u2026/_site/inbound_uni_fifo__opt_lpt.db' -> 'uni_fifo__opt_lpt'.\n\nInverts the declared template the way `strategy_of` inverts `sim_db`'s, and for the\nsame reason: renaming the site DB pattern in `schema.py` must not leave a consumer\nslicing fixed prefix and suffix lengths behind.  The arm pair is ONE capture, not\ntwo \u2014 the store and fulfillment halves are joined by `__` inside the stem, and the\npair, not either half, is what a site evaluation is keyed by.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, site_db: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.axes": {
      "decorators": [],
      "doc": "Sorted distinct values per axis, observed from the tree and backfilled from the\ndescriptor.  `channel` is [] on a store-only run \u2014 that emptiness is meaningful, and the UI\nshould hide the channel selector rather than invent a value.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict[str, list[str]]"
    },
    "Optimization/runschema/resolver.py::RunTree.by_group": {
      "decorators": [],
      "doc": "Artifact names tagged with `group` \u2014 lets callers ask for a family (e.g. every cross-cell\nwhat-if output) without hardcoding a name list that goes stale.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, group: str) -> list[str]"
    },
    "Optimization/runschema/resolver.py::RunTree.by_scope": {
      "decorators": [],
      "doc": "Artifact names at one scope ('run' | 'cell' | 'config' | 'channel_run').",
      "is_async": false,
      "kind": "function",
      "signature": "(self, scope: str) -> list[str]"
    },
    "Optimization/runschema/resolver.py::RunTree.cell_dir": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.cells": {
      "decorators": [],
      "doc": "[(cell_name, cell_dir), \u2026] in descriptor order (so partial/crashed cells are included).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[tuple[str, str]]"
    },
    "Optimization/runschema/resolver.py::RunTree.channel_runs": {
      "decorators": [],
      "doc": "Yield (cell_name, ChannelRun) for every analyzed leaf.  `cell=None` spans the whole run.\n\nChannelRun.channel is None on a store-only run \u2014 do NOT assume a channel segment.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str | None=None) -> Iterator[tuple[str, runlayout.ChannelRun]]"
    },
    "Optimization/runschema/resolver.py::RunTree.config_json": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, pair: str, config: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.dossier_dir": {
      "decorators": [],
      "doc": "<run>/_dossier \u2014 the run-scope stage root, the twin of `aggregate_dir`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.exists": {
      "decorators": [],
      "doc": "`os.path.exists` over `path(...)` \u2014 a convenience that reads as a contract question.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str, **parts) -> bool"
    },
    "Optimization/runschema/resolver.py::RunTree.family_of": {
      "decorators": [],
      "doc": "The `Schema.identity` family a sqlite artifact belongs to, or None.\n\nNone for non-sqlite artifacts AND for contract documents that predate the `family` key \u2014\nthe key is unhashed attribution (like `writer`), so old runs' documents simply lack it and\ncallers must tolerate that.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str) -> str | None"
    },
    "Optimization/runschema/resolver.py::RunTree.glob": {
      "decorators": [],
      "doc": "Existing paths for an artifact, with UNSUPPLIED parts wildcarded.\n\nGeneralizes `whatif_outputs()`: any `{part}` not passed becomes `*`; template `*`/`**`\npass through; the optional `{channel?}` level expands to BOTH variants (with and without\nthe segment) because a single glob cannot express an optional directory.  Sorted,\ndeduped, absolute; only existing paths return.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str, **parts) -> list[str]"
    },
    "Optimization/runschema/resolver.py::RunTree.group_outputs": {
      "decorators": [],
      "doc": "Every EXISTING file declared under a contract group tag, globs expanded.\n\nThe tag is the unit of discovery, so a producer that emits a new document gets it\nstaged and consumed with no edit at either end \u2014 which is what separates the\ngroup-driven stages from the ones that name their artifacts one at a time.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, group: str) -> list[str]"
    },
    "Optimization/runschema/resolver.py::RunTree.is_sweep": {
      "decorators": [
        "property"
      ],
      "doc": "True when the run has >1 cell \u2014 the condition that produces _frozen/ and the\ncross-cell what-if outputs.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/runschema/resolver.py::RunTree.keyframe_db": {
      "decorators": [],
      "doc": "The keyframe DB beside a sim DB, via the contract's keyframes_db template.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sim_db: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.leaf_path": {
      "decorators": [],
      "doc": "Absolute path of a channel-run-scoped artifact INSIDE `cr`'s own directory.\n\nFor callers holding a `ChannelRun` but not its cell (the common shape in analysis code):\nthe template's tail BELOW the tree levels is rendered with `extra` (e.g. `strategy=`)\nand joined to `cr.path`, which already IS the leaf directory.  Only legal for artifacts\nwhose template bottoms out in the leaf \u2014 asserted, so a run-scoped artifact cannot be\nsilently mis-rooted.\n\nThe tail is everything after the leading level placeholders, NOT merely the basename:\nan artifact that lives in a subfolder of the leaf (the analysis tables and the per-family\nfigure folders) must keep that folder, or every consumer of it would resolve one\ndirectory too high \u2014 silently, since the join would still look plausible.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cr, artifact: str, **extra) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.parts_of": {
      "decorators": [],
      "doc": "Level parts for one analysis leaf, from the `(cell, ChannelRun)` pair the iterators\nyield.  This is the SCOPE BINDING that lets `path()` be called for any artifact at a\nleaf without the caller re-joining basenames (ChannelRun carries pair/config/channel but\nnot its cell, so the pair travels together).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, cr) -> dict"
    },
    "Optimization/runschema/resolver.py::RunTree.path": {
      "decorators": [],
      "doc": "Absolute path of any contract artifact, e.g. path('sim_db', cell=\u2026, pair=\u2026, config=\u2026,\nchannel=None, strategy='uni_fifo_norsl').\n\nAn artifact declaring `resolves_via` resolves to the first CANDIDATE THAT EXISTS; when none\ndo, the last candidate is returned so the caller gets the canonical location to create.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str, **parts) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.planned_inventory_db": {
      "decorators": [],
      "doc": "The planned inventory for (cell, pair).\n\nMulti-cell runs freeze it once at _frozen/<pair>/ and share it across cells; single-cell runs\nwrite it under <cell>/<pair>/.  The precedence is declared in the contract\n(`planned_inventory.resolves_via`), not branched on here.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, pair: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.rollup_csv": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.run_layout_json": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.run_manifest": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.run_spec_json": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.runtime_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.schema_id": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.schema_short": {
      "decorators": [
        "property"
      ],
      "doc": "The 12-hex display form \u2014 what goes in logs, the viewer badge, and filenames.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.series_json": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cr: runlayout.ChannelRun) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.sim_dbs": {
      "decorators": [],
      "doc": "Yield (cell_name, ChannelRun, sim_db_path) across the run (or one cell).\n\nStructural \u2014 finds arms whose sim_meta.json was never finalized (crashed runs) too.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str | None=None) -> Iterator[tuple[str, runlayout.ChannelRun, str]]"
    },
    "Optimization/runschema/resolver.py::RunTree.sim_meta": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cr: runlayout.ChannelRun) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.site_dir": {
      "decorators": [],
      "doc": "<cell>/<pair>/_site \u2014 the site stage root, the pair-scope twin of `dossier_dir`.\n\nPresent on COUPLED runs only.  A consumer asks whether it exists rather than\nassuming: `exists('site_dir', cell=, pair=)`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, pair: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.site_inbound_dbs": {
      "decorators": [],
      "doc": "Every arm pair's site inbound DB under one pair \u2014 `[]` on an uncoupled run.\n\nThrough `glob`, so the arm-pair part is wildcarded by the CONTRACT rather than by a\njoined `_site/inbound_*.db` literal: the segment, the prefix and the extension all\nlive in one template and a consumer that spelled them again would be a second\ndeclaration to keep in step.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, pair: str) -> list[str]"
    },
    "Optimization/runschema/resolver.py::RunTree.strategy_of": {
      "decorators": [],
      "doc": "'\u2026/sim_uni_fifo_norsl.db' -> 'uni_fifo_norsl', by inverting the sim_db template.\n\nDerived from the contract rather than slicing fixed prefix/suffix lengths, so renaming the\nsim DB pattern in schema.py cannot leave this behind.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sim_db: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.warehouse_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cell: str, pair: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.warehouse_db_of": {
      "decorators": [],
      "doc": "The warehouse DB governing a sim DB's leaf \u2014 `keyframe_db`'s mirror for the pair level.\n\nSplits the leaf positionally (never by name \u2014 the store config and channel share a name)\nand renders the pair-scoped template, honouring the frozen-vs-cell-local `resolves_via`\nprecedence.  Replaces the walk-up-N-directories guess for contract runs.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sim_db: str) -> str"
    },
    "Optimization/runschema/resolver.py::RunTree.whatif_outputs": {
      "decorators": [],
      "doc": "Existing cross-cell what-if artifacts at the run root (empty on a single-cell run).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[str]"
    },
    "Optimization/runschema/resolver.py::SUPPORTED_FEATURES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset({'optional-segments', 'globs', 'resolves-via', 'strategy-capture'})"
    },
    "Optimization/runschema/resolver.py::_LEVEL_SEG": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('\\\\{\\\\w+\\\\??\\\\}')"
    },
    "Optimization/runschema/runlayout.py": {
      "decorators": [],
      "doc": "runlayout.py \u2014 single owner of the on-disk run-tree layout.\n\nTwo trees, one vocabulary:\n\n  * profiles tree (generation output):   <profiles>/<run>/<profile>/{inventory,affinity}/*.db\n  * comparison tree (simulation output): <base>/<cell>/<pair>/<config>[/<channel>]/{sim_*.db,\n                                          sim_meta.json, series.json, ...}\n\nEvery run is a CELL matrix: even a plain run is one cell (``k1_off``).  A cell dir is\nitself a self-contained comparison root (``<cell>/<pair>/<config>[/<channel>]/\u2026``), so the\nper-cell walkers below take a *cell dir* (or a legacy flat base) and are unchanged; only the\ntop-level ``cells()`` iterator is cell-aware.  ``iter_channel_runs`` / ``iter_sim_dbs`` therefore\nrun PER CELL \u2014 pass a cell dir, or loop ``cells(base_dir)`` to span a whole run.\n\nEvery walker that used to re-encode these shapes (run_simulation's pair discovery +\nblank-arm scan, run_analysis's config/aggregate walks, run_channel_rollup's series\nwalk, Visualization/db_reader's run discovery) now goes through here, so a layout\nchange is a one-file edit.  All listings are ``sorted(os.listdir(...))`` \u2014\ndeterministic order, matching the original sites.\n\nA *channel run* is the leaf directory an analyzed run lives in: ``<config>/`` itself\nfor store-only runs, or ``<config>/<channel>/`` per channel on mixed-catalog runs.",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/runlayout.py::ChannelRun": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One analyzed run directory: <base>/<pair>/<config>[/<channel>].",
      "kind": "class",
      "signature": ""
    },
    "Optimization/runschema/runlayout.py::ChannelRun.channel_key": {
      "decorators": [
        "property"
      ],
      "doc": "The channel this run SIMULATED, spelled as every writer's `channel` column spells it.\n\n`channel` is the run-tree LEVEL, and it is None on a store-only layout because there is\nno `<channel>/` directory to name.  The channel itself is not absent: a non-mixed\ncatalogue yields store runs only (`workunits._channel_runs_for`), and those arms wrote\n`channel = 'store'`.  So the missing level means STORE, never 'no channel', and a reader\nthat spells it `''` silently matches no row \u2014 which is what\n`run_map_precompute`'s runtime lookup and its `WHERE channel=?` backfill did.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/runlayout.py::ChannelRun.group_key": {
      "decorators": [
        "property"
      ],
      "doc": "Aggregate grouping key: config, or config/channel on mixed runs.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/runschema/runlayout.py::_profile_pairs": {
      "decorators": [],
      "doc": "(label, inventory_db, affinity_db) for every valid profile under one run dir.\n\nDescriptor-first since the profiles-tree contract (`Schema/profile_tree.py`): a run carrying\n`profile_layout.json` is served through `ProfileTree` (which existence-checks the\ndescriptor's claims); a pre-contract run \u2014 every catalogue generated before the contract,\nforever, per the no-backfill decision \u2014 takes the legacy walk below unchanged.  Signature\nand label format are load-bearing (`context/` anchors; the run tree's `<pair>` level).",
      "is_async": false,
      "kind": "function",
      "signature": "(run_path: str, run_name: str) -> list[tuple[str, str, str]]"
    },
    "Optimization/runschema/runlayout.py::_reserved": {
      "decorators": [],
      "doc": "True for a driver bookkeeping subtree (`_aggregate`, `_frozen`, `_site`, `_viz`, ...).\n\nThe run-tree contract DECLARES the prefix (`schema.RESERVED_PREFIX`); every walker below\nreads it rather than retyping `_`, and applies it at EVERY level.  The skip used to exist\nat the pair level only, so a reserved subtree the contract owns one level deeper --\n`<pair>/_site/` -- walked as a phantom CONFIG, and any `sim_*.db` beneath it surfaced as\nan extra ARM.  A reserved name is never an axis value at any depth.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> bool"
    },
    "Optimization/runschema/runlayout.py::_sim_dbs_in": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run: ChannelRun) -> Iterator[tuple[ChannelRun, str]]"
    },
    "Optimization/runschema/runlayout.py::cells": {
      "decorators": [],
      "doc": "Yield (cell_name, cell_dir) for every cell under a run root.\n\nEvery run is a cell matrix (a plain run is the single cell ``k1_off``).  When the run has a\n``run_layout.json`` descriptor, its cells are yielded IN DESCRIPTOR ORDER with NO disk probe \u2014\nso a partial/crashed cell (dir exists, no ``sim_*.db`` yet) is still yielded and analyzes on\nresume.  When the descriptor is ABSENT (legacy runs), fall back to walking the tree: the run\nroot's immediate non-``_`` children that contain a ``<pair>/<config>/sim_*.db`` subtree are the\ncells; a legacy FLAT run (no cell level \u2014 DBs directly under ``<base>/<pair>/<config>``) is\nyielded as one implicit cell named after the base dir.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> Iterator[tuple[str, str]]"
    },
    "Optimization/runschema/runlayout.py::discover_db_pairs": {
      "decorators": [],
      "doc": "Scan profiles_dir and return (label, inventory_db, affinity_db) for every valid pair.",
      "is_async": false,
      "kind": "function",
      "signature": "(profiles_dir: str) -> list[tuple[str, str, str]]"
    },
    "Optimization/runschema/runlayout.py::find_latest_db_pairs": {
      "decorators": [],
      "doc": "Return DB pairs from the most recently generated profile run only.\n\n\"Latest\" is `ProfileTree.latest()` \u2014 descriptor `created` timestamps for contract runs,\nthe historical lexicographic name order for pre-contract ones, a logged warning when the\ntwo disagree.  On a tree with no descriptors anywhere this is byte-for-byte the old\nbehavior (the timestamped names make the lexicographic order correct).",
      "is_async": false,
      "kind": "function",
      "signature": "(profiles_dir: str) -> list[tuple[str, str, str]]"
    },
    "Optimization/runschema/runlayout.py::iter_channel_runs": {
      "decorators": [],
      "doc": "Yield every channel-run dir under base_dir that contains *marker*.\n\nStore-only runs put the marker directly at <config>/; mixed-catalog runs put one\nper channel at <config>/<channel>/.  Reserved entries (`_reserved`) are skipped at\nEVERY level, not just the top.  Order: sorted pair, then config, then channel.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, marker: str='sim_meta.json') -> Iterator[ChannelRun]"
    },
    "Optimization/runschema/runlayout.py::iter_sim_dbs": {
      "decorators": [],
      "doc": "Yield (ChannelRun, sim_db_path) for every per-strategy result DB under base_dir.\n\nStructural walk over <pair>/<config>[/<channel>]/sim_<strategy>.db \u2014 keyframe DBs\nexcluded.  Channel subdirs are found even when sim_meta.json was never finalized\n(crashed runs), so blank-arm scans see everything.  Reserved entries (`_reserved`) are\nskipped at every level: `<pair>/_site/` holds the coupled unit's site DB, and a walker\nthat descended into it would report that DB as an extra arm of a phantom config.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> Iterator[tuple[ChannelRun, str]]"
    },
    "Optimization/runschema/schema.py": {
      "decorators": [],
      "doc": "runschema.schema \u2014 THE DECLARATION of the run tree.  Edit this file to change the tree.\n\nThis module is the single source of truth for what a run directory looks like.  Three things live\nhere and nowhere else:\n\n  * ``FEATURES``  \u2014 the template vocabulary this declaration uses; a resolver must support all of it.\n  * ``LEVELS``    \u2014 the ordered directory levels, each flagged optional + why.\n  * ``ARTIFACTS`` \u2014 every file a run produces, as a path TEMPLATE plus its format/scope/schema.\n\nThere is deliberately NO version number here.  A schema's identity is the SHA-256 of these\ndeclarations (``runschema.contract.schema_id``), so it is derived, never chosen: editing the tables\nmints a new id automatically, two branches that make different edits get different ids instead of\ncolliding on \"v2\", and any consumer can re-derive the id and check it.  The generated document lands\nat ``Optimization/schemas/run_tree/<short>.json`` and is committed, so non-Python consumers (the JS\nviewer, notebooks) read the same contract.\n\nProse (``note`` / ``condition``) is EXCLUDED from the hash \u2014 documenting a level or artifact more\nclearly must never mint a new schema.  Anything a path resolver actually depends on is included.\n\n``runschema.resolver.RunTree`` interprets these tables generically; there is no per-schema Python.\nThe walkers themselves stay in ``Optimization/runschema/runlayout.py`` (still the single owner of tree\ntraversal); the resolver delegates to them and adds the cell level, the optional-segment handling,\nand the axis inventory the viewer navigates by.\n\nWHY LEVELS ARE CONDITIONAL \u2014 read before adding a level\n-------------------------------------------------------\nTwo levels are NOT always present, and assuming otherwise is the bug this package exists to stop:\n\n  * ``<channel>/`` appears only on a MIXED catalog (store + fulfillment).  A store-only run puts\n    ``sim_*.db`` directly under ``<config>/``.  The pre-existing ``len(rel) < 4`` relpath checks in\n    run_whatif_delta/run_whatif_labor silently dropped every store-only run for exactly this reason.\n  * ``_frozen/<pair>/`` appears only on a MULTI-cell run, where the sampled inventory is frozen once\n    and reshaped per cell.  A single-cell run samples fresh, so its ``planned_inventory.db`` sits at\n    ``<cell>/<pair>/`` instead.\n\nA second trap: the store *config* is named ``store`` and the store *channel* is also ``store``, so a\nreal path reads ``<cell>/<pair>/store/store/sim_*.db``.  Never infer a level by matching a directory\nNAME \u2014 consume levels positionally against the axis lists in ``run_layout.json``.",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/schema.py::ARTIFACTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'run_layout': {'path': 'run_layout.json', 'format': 'json', 'scope': 'run', 'writer': 'write_run_layout@Optimization/runschema/sim_manifest.py', 'note': 'the cell-tree descriptor; carries schema_i..."
    },
    "Optimization/runschema/schema.py::AXES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('cell', 'pair', 'config', 'channel', 'strategy')"
    },
    "Optimization/runschema/schema.py::FEATURES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('optional-segments', 'globs', 'resolves-via', 'strategy-capture')"
    },
    "Optimization/runschema/schema.py::LEVELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[{'name': 'cell', 'optional': False, 'note': 'what-if cell: aisle-split x zoning x scheduler. Every run is a cell matrix; a plain run is the single cell k1_off.'}, {'name': 'pair', 'optional': Fals..."
    },
    "Optimization/runschema/schema.py::RESERVED_PREFIX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'_'"
    },
    "Optimization/runschema/schema.py::ROOT_PREFIXES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('comparison_', 'comparison_whatif_')"
    },
    "Optimization/runschema/schema.py::_FIGURE_DIR_WRITER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'prepare_config_dirs@Optimization/Performance_Evaluations/driver.py'"
    },
    "Optimization/runschema/schema.py::_LEAF_FIGURE_FAMILIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('headline', 'trajectories', 'labor', 'throughput', 'task_time', 'layout', 'significance', 'yard', 'diagnostics')"
    },
    "Optimization/runschema/schema.py::_SITE_FIGURE_FAMILIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('yard',)"
    },
    "Optimization/runschema/schema.py::_figure_globs": {
      "decorators": [],
      "doc": "`figures_<family>_pngs` for every family whose figures land in a per-leaf tree.\n\nOne glob per family rather than one for the whole tree, so preflight can name WHICH\nfamily an undeclared file violated.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/runschema/schema.py::_site_figure_globs": {
      "decorators": [],
      "doc": "`figures_site_<family>_pngs` for every family that renders into a site tree.\n\nOPTIONAL and conditioned on coupling, the same shape the per-leaf `yard` glob already\ncarries for the standing yard: a run that is not coupled has no site tree at all, which\nis an absence of a model rather than an empty folder.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/runschema/sim_manifest.py": {
      "decorators": [],
      "doc": "sim_manifest.py \u2014 run bookkeeping artifacts.\n\nResume state (resume.pkl) + the top-level run_manifest.json schema index that the\ndocs ingest (docs/experiments/ingest.py) auto-discovers a run from.\n\nIt also stamps CODE provenance into run_spec.json (`repo_commit` / `repo_dirty`).  That is a\ndifferent question from the two ids already on a run and neither of them answers it:\n``run_layout.json``'s ``schema_id`` names the run-TREE contract and ``sim_meta.json``'s\n``sim_schema_id`` names the DB TABLE contract, so a change to the simulator that moves no path and\nno column \u2014 `753d01e` moved absolute throughput ~1.4 % without touching either \u2014 leaves both\nbyte-identical and the two runs indistinguishable.",
      "kind": "module",
      "signature": null
    },
    "Optimization/runschema/sim_manifest.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', '..'))"
    },
    "Optimization/runschema/sim_manifest.py::_load_resume": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str)"
    },
    "Optimization/runschema/sim_manifest.py::_load_run_spec": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str)"
    },
    "Optimization/runschema/sim_manifest.py::_pair_bindings": {
      "decorators": [],
      "doc": "{label: binding | None} \u2014 WHICH catalogue version each pair label resolves to.\n\nThe binding comes from the pair's profile run's own `profile_layout.json` (via\n`ProfileTree.binding_of`); `None` records that discovery used the pre-contract legacy walk \u2014\nwith descriptors forward-only, that is every catalogue generated before the profiles\ncontract, and recording the absence honestly beats inventing provenance.  A regenerated-in-\nplace catalogue mints new params digests, so a recorded binding is what makes silent\nre-pointing DETECTABLE after the fact.",
      "is_async": false,
      "kind": "function",
      "signature": "(pairs) -> dict"
    },
    "Optimization/runschema/sim_manifest.py::_resume_path": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str) -> str"
    },
    "Optimization/runschema/sim_manifest.py::_run_layout_path": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> str"
    },
    "Optimization/runschema/sim_manifest.py::_run_spec_path": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> str"
    },
    "Optimization/runschema/sim_manifest.py::_save_resume": {
      "decorators": [],
      "doc": "Persist per-strategy batch counters and run IDs for crash recovery.\n\nrun_ids and starts are keyed by strategy key (e.g. 'uniform', 'trip_min').\nWritten atomically (tmp + os.replace) so a crash mid-write can't corrupt resume state.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, run_ids: dict, starts: dict) -> None"
    },
    "Optimization/runschema/sim_manifest.py::_write_run_spec": {
      "decorators": [],
      "doc": "Persist the fully-resolved run spec (argv + resolved run-shaping params + the resolved\ninventory pairs) to <base>/run_spec.json, atomically.  On --resume this is read back so a\nbare `python run_simulation.py --resume DIR` reconstructs the run with zero retyped flags\nand no find_latest_db_pairs drift.\n\n`repo_commit` / `repo_dirty` are added HERE rather than by the caller so that every producer of\na run_spec gets them without opting in \u2014 the point of the stamp is that it cannot be forgotten.\nThey are provenance, not run shape: `_apply_run_spec` overlays an explicit whitelist of fields\nonto argv, so a resume ignores them and keeps the ORIGINAL run's commit on record.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, spec: dict) -> None"
    },
    "Optimization/runschema/sim_manifest.py::read_run_layout": {
      "decorators": [],
      "doc": "Load <base>/run_layout.json, or None if absent/malformed (legacy runs have no descriptor).",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str)"
    },
    "Optimization/runschema/sim_manifest.py::write_run_layout": {
      "decorators": [],
      "doc": "Write <base>/run_layout.json \u2014 the descriptor of a run's unified cell tree\n``<base>/<cell>/<pair>/<config>[/<channel>]/sim_*.db``.  Lets tools INFER the tree (cells,\nreference, configs, pairs) instead of directory-guessing, and lets analysis of a partial/crashed\nor OLD run enumerate + label cells without re-importing the (possibly since-changed) whatif spec.\nAtomic (tmp + os.replace), same pattern as _write_run_spec.\n\ncells = the _build_cells() `Cell` records (name, split, zoning, scheduler); split is None or\n{'k','capacity_loss'}.  `created` (ISO-8601) is passed in so the caller owns the clock.\n\n`schema_id` stamps which RUN-TREE CONTRACT this run's directory layout follows \u2014 the sha256 of\nthat contract's own shape, stored at Optimization/schemas/run_tree/<short>.json.  Downstream\ntools select their resolver from it (runschema.resolver_for), so an OLD run keeps analyzing\ncorrectly after the tree shape moves on.  It is a content address, not a sequence number: there\nis nothing to increment, and anyone can re-derive it from the declaration to check it.\n\nDistinct from `version`, which versions THIS descriptor file's own field set.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, *, spec, reference, cells, pairs, store_cfgs, ff_cfgs, channels, arms, created, coupled=False) -> None"
    },
    "Optimization/runschema/sim_manifest.py::write_run_manifest": {
      "decorators": [],
      "doc": "Write <base>/run_manifest.json \u2014 a schema index of this run (inventories \u00d7\nconfigs \u00d7 strategies) so the docs ingest can auto-discover what to pull.\nSee docs/experiments/ingest.py.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, pairs, store_cfgs, ff_cfgs, strategies) -> None"
    },
    "Optimization/runschema/sim_manifest.py::write_run_manifest._brackets_json": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(hb)"
    },
    "Optimization/runschema/sim_manifest.py::write_run_manifest._cfg_json": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(c, channel)"
    },
    "Optimization/simconfig/__init__.py": {
      "decorators": [],
      "doc": "simconfig \u2014 self-registering pick-config registry (the sim analogue of the graph suite).\n\nImporting this package fires import_all() so every module under configs/ self-registers its\npick-config via @pick_config; Optimization/config/sim_config.py then rebuilds STORE_CONFIGS /\nFULFILLMENT_CONFIGS from the registry (filtering by channel + enabled).  Adding a pick-config =\ndrop a module in configs/ \u2014 no central edit.  Whatif cell-matrix specs stay in whatif_config.SPECS\nfor now; the same registry pattern can absorb them later.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/configs/__init__.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/configs/ful_calibrated.py": {
      "decorators": [],
      "doc": "Fulfillment channel \u2014 base human-walker pick-config (FulfillmentCart, one-way lanes).\n\nThe FIRST/only fulfillment entry is the single source of truth for the default walker cost \u2014\nchannels.fulfillment_pick_config() reads it back via sim_config.FULFILLMENT_CONFIGS[0].  Keep the\nname DISTINCT from any store config name (config.json is written per config dir; a shared name\nwould collide \u2014 see the runner's _prepare_channel_run).",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/configs/ful_calibrated.py::ful_calibrated": {
      "decorators": [
        "pick_config(name='ful_calibrated', channel='fulfillment', order=0)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/simconfig/configs/store.py": {
      "decorators": [],
      "doc": "Store channel \u2014 base ergonomics pick-config (StoreCart). The single active store calibration.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/configs/store.py::store": {
      "decorators": [
        "pick_config(name='store', channel='store', order=0)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/simconfig/configs/store_high_height.py": {
      "decorators": [],
      "doc": "Store variant \u2014 base weight, steeper height brackets (menu entry, not in the active sweep).\nRegistered with enabled=False; flip enabled=True to add it to the store sweep.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/configs/store_high_height.py::store_high_height": {
      "decorators": [
        "pick_config(name='store_high_height', channel='store', order=11, enabled=False)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/simconfig/configs/store_high_weight_high_height.py": {
      "decorators": [],
      "doc": "Store variant \u2014 steeper weight exponent + steeper height brackets (menu entry, not in the\nactive sweep).  Registered with enabled=False so it is discoverable/togglable without running;\nflip enabled=True to add it to the store sweep.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/configs/store_high_weight_high_height.py::store_high_weight_high_height": {
      "decorators": [
        "pick_config(name='store_high_weight_high_height', channel='store', order=10, enabled=False)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Optimization/simconfig/constants.py": {
      "decorators": [],
      "doc": "Leaf constants shared by the pick-config modules, sim_config, AND the staffing records.\n\nKept here (importing nothing from sim_config) to break the circular-import trap: sim_config\nimports the simconfig registry, which import_all()s the config modules, so anything both\nsides need has to live below both of them.\n\n`_STORE_PICKERS` / `_FF_PICKERS` are the DEFAULTS of the two declared picker knobs -- the\none staffing input of the calibrated era (.scratch/department-calibration, \"Define the\ncalibrated era\").  They reach a run through `settings.STORE_PICKERS` / `FF_PICKERS`, then\n`CONFIG['global']['store_pickers']` / `['ff_pickers']`, and are read at CALL time by\n`sim_config.channel_pickers(name)` -- never snapshotted, never restated.  The pick-config\nmodules under configs/ no longer name them at all: a per-arm `num_pickers` that disagrees\nwith the channel's declared count raises at setup (`workunits._channel_runs_for`), so the only\nthing a module could legally say is the channel value, and saying nothing says exactly that.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/constants.py::_FF_PICKERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "20"
    },
    "Optimization/simconfig/constants.py::_STORE_PICKERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "25"
    },
    "Optimization/simconfig/core/__init__.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/core/discovery.py": {
      "decorators": [],
      "doc": "Auto-discovery: import every simconfig submodule so @pick_config decorators fire.\n\nMirrors Optimization/Performance_Evaluations/core/discovery.py.  Used instead of a hand-maintained\nimport list so dropping a new pick-config file into configs/ registers it with zero edits elsewhere.\n\nCritical for multiprocessing: the package __init__ calls import_all() at import time, so each\nspawned worker that (transitively) imports Optimization.simconfig repopulates the registry in its\nown process before any run.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/core/discovery.py::import_all": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/simconfig/core/registry.py": {
      "decorators": [],
      "doc": "PickConfigSpec descriptor + @pick_config decorator + module-level registry.\n\nMirrors Optimization/Performance_Evaluations/core/registry.py: a frozen dataclass per item, a\nflat list, and a by-key dict, populated by a decorator so a pick-config module self-registers on\nimport.  A pick-config is the plain dict Optimization/sim_config._build_pick_cfg consumes; the spec\nmakes explicit the CHANNEL split (store vs fulfillment) that used to be implicit in WHICH list the\ndict lived in, plus a sweep ``order`` (list order is load-bearing \u2014 it sets config-dir identity and\nsweep order, so it must NOT depend on filesystem walk order) and an ``enabled`` flag so a variant\ncan be kept as a discoverable menu entry without running.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/core/registry.py::PickConfigSpec": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/core/registry.py::pick_config": {
      "decorators": [],
      "doc": "Decorator: register the wrapped zero-arg builder's returned dict as a PickConfigSpec.\n\nReturns the builder unchanged so it stays directly callable/unit-testable.  Raises on a\nduplicate name or an unknown channel.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, name, channel, order=0, enabled=True)"
    },
    "Optimization/simconfig/core/registry.py::pick_config._wrap": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fn)"
    },
    "Optimization/simconfig/coverage.py": {
      "decorators": [],
      "doc": "coverage.py \u2014 stock coverage as a RUNTIME declaration in days, never a generator knob.\n\nThe catalogue carries NO stock level (ADR-0002, department-calibration \"Field the floor\",\ndecision 5): a level is a RUN's declaration, and this module is where every run makes it.  It\nused to be authored at generation in GENERATION batches (`equilibrium_qty = round(\ncoverage_batches x freq x qty)`), and a generation batch is not a unit of time -- it is \"each\nSKU appears with probability `freq`\", which is worth however many site days the run's pickers\nmake it worth (1,771 of them on the reference catalogue's store section).  The demand a SKU\nsees per DAY is known at setup (.scratch/department-calibration, \"Derive the expected-travel\nclosed form\", decision 2; the derivation note's section 5):\n\n    d_s = n \u00b7 \u03c0_s \u00b7 E[q_s]                    units of SKU s per day\n\n`n` is the pair's fixed point (lines per day the declared crew fills its capacity at), `\u03c0_s`\nthe SKU's line share within its channel section (`freq / \u03a3 freq`) and `E[q_s]` the mean of\nthe SKU's STAMPED line law (`Demand.line.mean()`; today `max(1, Poisson(\u03bb_s))`, so\n`\u03bb_s + e^{-\u03bb_s}`) -- read off the SKU, never re-derived here (\"Stamp the line distribution\non the SKU\").  This module re-derives every\nSKU's stock levels by the GENERATOR'S OWN FORMULA with the day as the unit, floored at ONE\nLINE of the SKU's own demand (\"Choose the coverage floor\", decisions 1-2):\n\n    L_s             = ceil(floor_lines \u00b7 E[q_s])                  the LINE FLOOR, >= 1\n    equilibrium_qty = max(round(coverage_days \u00b7 d_s), L_s)\n    reorder_point   = min(Q - 1, max(round(d_s \u00b7 (lead_days + safety_days)), L_s))   (Q > 1)\n                    = 1                                                              (Q = 1)\n    pipeline_qty    = round(d_s \u00b7 lead_days)                      the lead PIPELINE, stamped\n\nThe floor is a physical number per SKU -- a pick's worth of itself, so no SKU stocks out on a\nsingle line -- and it is what replaced the generator's unit floor (`Q >= 1`), under which a\nslow mover held one unit against ten-unit lines and the store section of the reference pair\nwas 100% floored (\"Rescale stock coverage at setup\", the finding).  A SKU whose coverage is\nshorter than the interval between its lines lands on the floor with `rp = Q - 1`: every pick\nfires an order-up-to for exactly what it took -- the textbook BASE-STOCK (S-1, S) policy,\nwhich `inventory_reorder._fire_reorders` already implements on inventory position.  Coverage\nis nominal AND real only for SKUs above the floor; `rescale_section` returns the shares so the\nrecord can say how much of the section, and of its demand, sits on it.\n\nThe reorder point's lead-time content is stamped, not inferred: `pipeline_qty` is the\nin-transit allowance `_fire_reorders` adds to the order-up-to (decision 6).  The manager's\n`rp \u00d7 lead \u00f7 (lead + 1)` heuristic assumed `rp` encodes lead-time demand; under the line floor\n`rp` encodes a line, and a floored SKU with a two-day lead would have ordered up to ~1.7 lines.\nThe stamp rides the SKU (`Order.pipeline_qty`, a `cartons` column of the planned inventory) so\nthe spawned workers read it; a flag-off run never stamps it and the heuristic stands.\n\nTHE LEAD IS TWO ADDITIVE STAGES (\"Declare the coverage against the inbound lead\", decisions\n1-6).  A SKU's `lead_time_mean` is its SUPPLIER lead -- order to ready-to-ship, authored in\nBATCHES and read here as days through `lead_unit_days` (`1 / releases_per_day`: one batch is\none day under the era; 1.0 flag-off, where a batch was always read as a day) -- and the\ntrailer pipeline's transit is the site's, one number per pair, rounded UP to the day grid\nbecause a trailer is observed only at the next drain:\n\n    transit_days = E[ceil(L / D)] = 1 + \u03a3_{k>=1} (1 - \u03a6(ln(k\u00b7D / m) / \u03c3))        (`transit_day_law`)\n    lead_days_s  = lead_time_mean_s \u00b7 lead_unit_days + transit_days\n\nwith `m` the lead median in seconds, `\u03c3` the spread and `D` the site day; exactly 1 at spread\n0 over a one-day median, 0 with no pipeline.  The first-time promise holds AT the lead\n(decision 5): `fill_rate` prices the shelf a line meets as the order-up-to POSITION\n`S = Q + pipeline_qty` less the units of the SKU's own prior lines still in transit, the\nprior lines counted over the integer days the observed lead spans, and `solve_floor_lines`\nabsorbs it unchanged.  At lead zero every term reduces to the lead-free expression EXACTLY --\nthe same code path, not a tolerance -- so a record with no pipeline is byte-identical to one\ndeclared before the lead existed.\n\nTHIS MODULE IS PURE: orders, a line count and three declared scalars in; the orders'\nstock DECLARATION written through `Order.declare_stock` (the one mutation site for the four\nlevel slots) and a stats dict out.  It imports no CONFIG and touches no file.  The harness seam\nthat drives it -- the pair-level fixed point Q(n) -> plan_warehouse -> geometry -> n -- is\n`Optimization/simdriver/era_coverage.py`, and it runs in EVERY mode: the calibrated era decides\nwhether the clock cuts and caps, never whether a run declares its stock (ADR-0002, decision 6).",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/coverage.py::_line_pmf": {
      "decorators": [],
      "doc": "`P(q_s = i)` for `i = 0 .. upto-1`, one row per line law -- 0 at `i = 0` (a line\nalways demands at least one unit).  The founding family is evaluated in one vectorised\nlog-space pass (`max(1, Poisson(\u03bb))`: `P(q = 1) = e^{-\u03bb}(1 + \u03bb)`, the Poisson mass above);\nany other family falls back to the law's own `pmf`, row by row, so a second family\nprices correctly the day it is stamped and merely loses the fast path.",
      "is_async": false,
      "kind": "function",
      "signature": "(lines: list, upto: int) -> np.ndarray"
    },
    "Optimization/simconfig/coverage.py::_phi": {
      "decorators": [],
      "doc": "The standard normal cdf, by `erf`.",
      "is_async": false,
      "kind": "function",
      "signature": "(x: float) -> float"
    },
    "Optimization/simconfig/coverage.py::_served_under_lead": {
      "decorators": [],
      "doc": "`E[min(q, (S - D_K)^+)]` per SKU: the units a line is expected to take off a shelf\nwhose order-up-to POSITION is `S` while the SKU's own prior lines are still in transit\n(\"Declare the coverage against the inbound lead\", decision 5).\n\nThe observed lead is `K = a + k` whole days on the site grid: `a` the SKU's supplier lead\nrounded to the grid (the ledger's own `round(lead_time_mean)`) and `k` the trailer's\ntransit under `pmf_k` (`transit_day_law`; `{0: 1}` with no pipeline).  The prior lines\ninside those `K` days number `N ~ Poisson(K \u00b7 rate_s)` at the SKU's line rate `n \u00b7 \u03c0_s`\n(the batch is one Poisson draw a day under the era -- the same convention the\nfragmentation chain prices with), each of the stamped line law, so `D_K` is compound\nPoisson and its mass on `0 .. S-1` follows Panjer's recursion\n\n    g(0) = e^{-\u03bb},   g(j) = (\u03bb / j) \u00b7 \u03a3_{i=1..j} i \u00b7 f(i) \u00b7 g(j - i)         \u03bb = K \u00b7 rate_s\n\nThen, `q` and `D_K` being independent,\n`E[min(q, (S - D)^+)] = \u03a3_{u<S} P(q > u) \u00b7 P(D <= S - 1 - u)`.  Vectorised over SKUs in\ngroups by shelf size (the recursion is quadratic in `S`, and a floored section is almost\nentirely small shelves), exact to the truncation at `S` -- nothing above the position\nis ever needed -- and to the transit tail `SERVED_TAIL_TOL` leaves unpriced.  The\nrecursion is linear in `g`, so a row whose mass outgrows `_PANJER_RESCALE` is divided\ndown and its log-scale carried, which keeps a seed that underflows (\u03bb above ~745) from\nzeroing a shelf that serves everything.  At `K = 0` the mass sits at zero and the sum\nis `E[min(q, S)]`.\n\n`a` is the supplier lead ROUNDED TO GRID DAYS after the batch-to-day conversion; at one\nrelease a day (the era, which pins it) that is the ledger's own `round(lead_time_mean)`\nin batches, and at any other release schedule the two roundings differ -- a limitation\nstated, not solved, because nothing declares another schedule.",
      "is_async": false,
      "kind": "function",
      "signature": "(lines: list, S: np.ndarray, rate: np.ndarray, a: np.ndarray, pmf_k: dict) -> np.ndarray"
    },
    "Optimization/simconfig/coverage.py::converged": {
      "decorators": [],
      "doc": "Every channel's fixed-point `n` moved by less than `tol` (relative) since the last\nround.  A channel absent from either side, or a zero previous value, is not converged.",
      "is_async": false,
      "kind": "function",
      "signature": "(prev: dict, cur: dict, tol: float=DEFAULT_TOL) -> bool"
    },
    "Optimization/simconfig/coverage.py::daily_demand": {
      "decorators": [],
      "doc": "`{sku: d_s}` for one channel section at `lines_per_day` lines a day.\n\nThe line share is `freq / \u03a3 freq` over exactly the orders given -- the section the\nchannel picks from (`staffing.regime_orders`), which is also how `expected_travel\n.accumulate` weights them, so the two agree by construction.  An empty section or a\nzero weight sum yields zeros.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, lines_per_day: float) -> dict"
    },
    "Optimization/simconfig/coverage.py::fill_rate": {
      "decorators": [],
      "doc": "The expected FIRST-PASS fill rate of one section at the levels the orders carry now:\nthe units a shelf serves off a line, over the units the line asks for, weighted by each\nSKU's lines per day (\"Choose the coverage floor\", decision 5):\n\n    \u03a3_s  lines_s \u00b7 E[min(q_s, (S_s - D_s)^+)]  \u00f7  \u03a3_s  lines_s \u00b7 E[q_s]     lines_s = n \u00b7 \u03c0_s\n\n`S_s = Q_s + pipeline_qty_s` is the order-up-to POSITION and `D_s` the units of the SKU's\nown prior lines still in transit when this one arrives -- zero at lead zero, where the\nprevious line's order has landed and the shelf sits at `Q_s`, and priced by\n`_served_under_lead` otherwise (\"Declare the coverage against the inbound lead\", decision\n5: the promise holds AT the lead).  `transit` is `transit_day_law`'s result for the pair\n(None = no pipeline) and `lead_unit_days` reads the SKU's supplier lead as days.  The\ndenominator is the section's daily units; the numerator the units served before a\nrestock, so `1 - fill_rate` is the expected missed share the audit's `supply` clause\nreads.  A SKU with no lead and no pipeline stamp takes the lead-free expression through\nITS OWN code path (`expected_min`), so a record with no pipeline is byte-identical to one\npriced before the lead existed.  Call it on the PLANNED orders: the shelf that serves the\nline is the one the run fields, which since \"Field the requirement\" is the declared one\nexactly, so pre-plan and post-plan price the same number.  Returns\n`{'fill_rate', 'expected_missed_share', 'units_per_day', 'served_per_day',\n'base_stock_skus', 'base_stock_share', 'n_skus', 'pipeline_units', 'transit_days',\n'lead_days'}` -- `pipeline_units` is \u03a3 `pipeline_qty` over the section and `lead_days`\nthe units-weighted mean lead, both 0 when the lead-free premise holds exactly.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, lines_per_day: float, *, transit: dict | None=None, lead_unit_days: float=1.0) -> dict"
    },
    "Optimization/simconfig/coverage.py::line_floor": {
      "decorators": [],
      "doc": "`L_s = ceil(floor_lines \u00d7 E[q_s])`, at least 1: the least stock a SKU ever holds, in\nunits, read off its stamped line law.  `floor_lines` is the declared number of lines\n(1.0 by default: one pick's worth); the mean is rounded UP so the shelf is never below\nthe expected line.  Refuses a non-positive `floor_lines` -- a zero would be the unit\nfloor by the back door.",
      "is_async": false,
      "kind": "function",
      "signature": "(line, floor_lines: float) -> int"
    },
    "Optimization/simconfig/coverage.py::pipeline_qty": {
      "decorators": [],
      "doc": "`round(d_s \u00d7 lead)`: the units expected in transit over the lead -- the allowance\n`_fire_reorders` adds to the order-up-to so on-hand returns to the target when the lot\nlands.  Zero at lead 0, as the heuristic it replaces was.",
      "is_async": false,
      "kind": "function",
      "signature": "(d_s: float, lead_days: float) -> int"
    },
    "Optimization/simconfig/coverage.py::rescale_section": {
      "decorators": [],
      "doc": "Re-derive every order's stock levels for one section and return the floor shares.\n\nMUTATES the orders through `Order.declare_stock`: the order-up-to, the reorder point and\nthe stamped lead `pipeline_qty`, with `stock_plan` reset to None -- a plan the warehouse\nplanner wrote for the OLD quantity would otherwise be honoured by `viable_storage_units` at\nthe new one, and the planner re-packs the line-sized quantity.  Each SKU's lead is\n`sku_lead_days` -- its supplier lead in days plus the pair's `transit_days` (0 with no\npipeline; `lead_unit_days` 1.0 is the flag-off reading of a batch as a day).  Returns\n\n    {'n_skus', 'line_families', 'lines_per_day', 'coverage_days', 'safety_days',\n     'floor_lines', 'transit_days', 'lead_unit_days',\n     'lead_days',                      # the units-weighted mean lead over the section\n     'sum_q', 'units_per_day',\n     'floor_line_skus', 'floor_line_share', 'floor_line_demand_share',   # Q == L_s\n     'base_stock_skus', 'base_stock_share',                              # rp == Q - 1\n     'floor_rp_skus', 'floor_rp_share',           # above the floor, rp lifted to L_s\n     'above_floor_skus', 'realized_coverage_days'}   # \u03a3 Q / \u03a3 d over the SKUs above\n\n`floor_line_demand_share` is the share of the section's daily UNITS that lands on a SKU\nholding exactly its line floor -- the number that says whether the floor is a tail or the\nsection.  `realized_coverage_days` is the coverage the SKUs above the floor actually carry\n(their rounded Q against their demand), which the nominal number only approximates.  The\nexpected first-pass fill rate is NOT here: it depends on the levels the planner finally\nfields, so `fill_rate` prices it on the planned orders once (`era_coverage.fixed_point`).",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, lines_per_day: float, *, coverage_days: float, safety_days: float, floor_lines: float, transit_days: float=0.0, lead_unit_days: float=1.0) -> dict"
    },
    "Optimization/simconfig/coverage.py::sku_lead_days": {
      "decorators": [],
      "doc": "`lead_days_s = lead_time_mean_s \u00b7 lead_unit_days + transit_days`: the SKU's expected\norder-to-shelf lead in days -- its supplier lead (authored in batches, one batch a day\nunder the era) plus the pair's day-grid transit (\"Declare the coverage against the\ninbound lead\", decisions 2 and 4).  Never negative; 0 on a lead-free catalogue with no\npipeline, which is every catalogue the generator has authored so far.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, transit_days: float=0.0, lead_unit_days: float=1.0) -> float"
    },
    "Optimization/simconfig/coverage.py::solve_floor_lines": {
      "decorators": [],
      "doc": "The shelf side of the first-time guarantee (ADR-0004): the smallest `floor_lines`\nat which the section's expected first-pass fill clears `fill_min` (`sqrt(c)`), with\nthe line count DECLARED.\n\n`fill(f)` is `fill_rate` over the levels `rescale_section(f)` declares -- a pure\ncatalogue closed form while every SKU sits on the floor, and the same root when a\nsection is not fully floored (a SKU above its floor takes `coverage_days \u00d7 d_s`, which\nthe formula already reads, so `coverage_days` enters the fill through the levels and\nnothing here changes).  It is a non-decreasing STEP function of `f` (`L_s = ceil(f \u00d7\nE[q_s])` moves one SKU at a time), so the root is bracketed by doubling from `lo` and\nbisected to `tol` lines, and the answer is the bracket's UPPER end rounded up to the\ntolerance -- always at or above the true threshold, never below it.  `lo` is the\none-line floor: a SKU never holds less than a pick's worth of itself, so a confidence\none line already clears solves to exactly `lo` and says so (`at_lower_bound`).\n\nTHE FLOOR IS SOLVED AT THE LEAD (\"Declare the coverage against the inbound lead\",\ndecision 5): `transit` (the pair's `transit_day_law`, None = no pipeline) and\n`lead_unit_days` reach both the levels and the fill, so the same declared confidence\nbuys a higher floor under a pipeline -- the shelf that clears `sqrt(c)` while the\nprevious line's lot is still on the road.  The fill stays a non-decreasing step\nfunction of `f` (the position rises with the floor, the in-transit law does not move).\n\nMUTATES the orders (every evaluation re-declares through `rescale_section`) and leaves\nthem declared at the SOLVED floor.  Refuses a `fill_min` outside [0, 1) and a section\nthat cannot reach it by `hi_max` lines.  Returns\n\n    {'floor_lines', 'fill_rate', 'fill_min', 'evaluations', 'at_lower_bound',\n     'bracket': (lo, hi)}",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, lines_per_day: float, *, coverage_days: float, safety_days: float, fill_min: float, transit: dict | None=None, lead_unit_days: float=1.0, lo: float=DEFAULT_FLOOR_LINES, tol: float=FLOOR_SOLVE_TOL, hi_max: float=FLOOR_SOLVE_MAX) -> dict"
    },
    "Optimization/simconfig/coverage.py::solve_floor_lines.fill": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(f: float) -> float"
    },
    "Optimization/simconfig/coverage.py::stock_levels": {
      "decorators": [],
      "doc": "`(equilibrium_qty, reorder_point)` for one SKU: the generator's formula in days,\nfloored at `floor_units` (the SKU's line floor, `line_floor`) on BOTH levels.\n\nThe order-up-to is `coverage x demand`, never less than the floor; the reorder point is\ndemand over the lead plus the safety buffer, never less than the floor, capped one below\nthe equilibrium so the trigger always fires before the target.  A SKU whose coverage\nterm falls below its floor therefore lands at `rp = Q - 1` -- base stock.  A SKU at\n`Q = 1` (a floor of one unit) takes the generator's `rp = 1` branch, as `Order.build`\nclamps it.",
      "is_async": false,
      "kind": "function",
      "signature": "(d_s: float, lead_days: float, coverage_days: float, safety_days: float, floor_units: int) -> tuple[int, int]"
    },
    "Optimization/simconfig/coverage.py::transit_day_law": {
      "decorators": [],
      "doc": "The trailer pipeline's transit on the SITE-DAY GRID: the law of `ceil(L / D)` for a\nlognormal lead `L` (median `lead_s` seconds, spread `lead_sigma`) against a day of\n`day_seconds`, and its expectation (\"Declare the coverage against the inbound lead\",\ndecision 3 -- transit and the grid only, no doors, no queue):\n\n    transit_days = E[ceil(L / D)] = 1 + \u03a3_{k>=1} (1 - \u03a6(ln(k\u00b7D / m) / \u03c3))\n    P(ceil(L / D) = k) = \u03a6(ln(k\u00b7D / m) / \u03c3) - \u03a6(ln((k-1)\u00b7D / m) / \u03c3)          k >= 1\n\nReturns `{'transit_days', 'pmf': {k: p}, 'lead_s', 'lead_sigma', 'day_seconds'}`.  The\nceiling is the model, not a rounding choice: a reorder fires and dispatches inside one\ndrain, `arrived = dispatched + L`, and the yard is read once per site day, so a lead of\nany positive length lands at the next drain at the earliest (1.766 days at the pilot's\n480 min / 0.7 / 8 h; the continuous mean, 1.28, is the wrong number).  Spread 0 is taken\nliterally (`ceil(m / D)` with probability one, exactly 1 for a median of one day) and a\nzero median is no transit at all (`{0: 1.0}`, 0 days): a trailer that arrives inside the\ndrain that dispatched it.  Refuses a non-positive day or a negative median or spread.",
      "is_async": false,
      "kind": "function",
      "signature": "(lead_s: float, lead_sigma: float, day_seconds: float) -> dict"
    },
    "Optimization/simconfig/equilibrium.py": {
      "decorators": [],
      "doc": "equilibrium.py \u2014 the calibrated era's pre-registered EQUILIBRIUM CHECK, as a pure module.\n\n\"Declare the equilibrium bands\" (.scratch/department-calibration, decision 4) turned \"the crews\nhave something to do, within bounds\" into one function of (db, day_lo, day_hi) returning a\nverdict with reasons: five clauses over a window of working days.  \"Split the missed-share\nclause into supply and labour\" (2026-09-09, ADR-0004's instrument) replaced two of the\noriginal five -- the strict `drained` and the level-free `missed_share` -- with the two\nclauses the old pair was silently conflating:\n\n    labour         the picking crew's queue is in equilibrium.  THREE terms, all judged on the\n                   `carryover` FLOWS over FRESH demand (`demand_flows`, below): the realized\n                   cut share (`unpicked_daycut` units over fresh units, a ratio of sums over\n                   the window) sits within a band of the STAMPED expected cut share (ADR-0004,\n                   `guarantee.crew.cut_share`; the band is `CUT_SHARE_Z` sampling sds of the\n                   declared day law, `staffing.cut_share_sd`); the standing labour carry at\n                   any day's close stays under ONE day's capacity in units (crew x S / s_pick);\n                   and the per-day cut share does not trend (half-window means within\n                   `CUT_SHARE_Z` sds of their difference under the same law -- the typed\n                   `TREND_TOL` is half a day's spread and failed a healthy window a third of\n                   the time).  A day the ledger never closed still fails it.  \"Every day\n                   drained\" is GONE as a verdict: the day's line count is a Gaussian draw with a\n                   declared cv (a third on the store), so 14 of 40 days exceed a full shift at\n                   ANY headroom and a strict drained count was never a property of equilibrium\n                   (\"Fit the store's window to its own steady state\", decision 9).  `drained`\n                   survives as a READING inside this clause -- days drained, days capped, the\n                   overtime and early days, the supply carry standing -- read off the ledger's\n                   own verdict (`is_drained`, below, still the ONE definition the runner's\n                   close-out calls).\n    released_late  = 0 behind every drained day.  A self-consistency assertion, not a clause\n                   that can fail: release waits on the picker clock and the cut is a\n                   between-bins START gate, so a day that drained leaves exactly 0 lag on the\n                   NEXT release, and lag there -- or on the first day of the run -- is an\n                   INSTRUMENT BUG.  It RAISES `InstrumentError` rather than failing.\n                   Lag behind a CAPPED day is that day's overrun and is recorded.\n    utilization    realized utilization (worked \u00f7 granted, a RATIO OF SUMS over the window,\n                   never a mean of per-day ratios) inside `band_tol` of the EXPECTED value the\n                   derivation recorded per department per leaf -- never of \u03c1, which integer\n                   site crews and single-channel leaves undercut by construction.  Under\n                   ADR-0004 the picking expectation comes from the DERIVED crew.\n    supply         the shelf's first-pass service.  The two SUPPLY reasons only\n                   (`unpicked_unstocked`: no bin held the SKU; `unpicked_unavailable`: the bin\n                   held less), each unit counted ONCE -- on the first batch its SKU's supply\n                   failure rose -- over FRESH demand; LEVEL (a ratio of sums) within\n                   `SUPPLY_LEVEL_TOL` of the record's `1 - fill_rate` (the solved floor's\n                   expected first-pass missed share), TREND as before (half-window means of the\n                   per-batch share within `TREND_TOL`; memories `knees-hide-from-r-squared`,\n                   `per-batch-series-are-autocorrelated`).\n    rework         THREE EVENTS JUDGED AT EXACTLY ZERO, the depth REPORTED PER BUCKET (\"Band\n                   the own-bin share and the free-index depth\", 2026-09-10).  A TIER SPILL\n                   (`put_spills`: the candidate chain moved up a size tier because the unit's\n                   own bucket was dry), a TOP-UP into an occupied bin (`put_topups`: the whole\n                   tier chain was dry), and a REPACK (the own bins were full too, judged\n                   against the record's stamped `f_repack = 0`) are the same finding at three\n                   rungs -- the warehouse's SIZING, never a cost to absorb into a band -- and\n                   each fails the clause with the bucket(s) that read dry.  The zero for the\n                   spill and the top-up is ADR-0003's own claim, hard-coded, NO KNOB\n                   (decision 4): nothing on the record prices either differently from a\n                   placement, so a tolerance would have no number to derive from.  The\n                   free-index DEPTH is read per bucket off the `free_index` table (the leaf's\n                   own section by construction; `batch_stats.free_bins` is the whole\n                   geometry) against the setup `free` the record stamps per bucket, and is\n                   reported -- minimum, mean and drawdown over the window -- never judged:\n                   a typed level floor certifies nothing on a store whose slide outlives the\n                   window (decision 6), and the trajectory band waits for the stationary\n                   fragmentation closed form (34).\n\nWHY THE SPLIT.  The old `missed_share` read `(items_demanded - total_items) / items_demanded`\nper batch.  Under the era that difference is the day cut PLUS the stockout, and\n`items_demanded` is the EFFECTIVE batch -- the sampled demand plus everything the previous\nbatch rolled forward -- so a re-offered unit was counted again in both terms (cumulative\n312,302 demanded against 255,817 fresh on the store leaf of `comparison_20260908_094846`).\nThe clause read a labour overflow (0.037 -> 0.159 across the halves) while the supply share it\nclaimed to judge was flat at 0.070 against an expected 0.078.  Now every share here is a FLOW\nover FRESH demand, and the two causes are two clauses with two expectations.\n\nONE PURE FUNCTION, and the sim never judges itself (decision 6).  It was designed with two\ncallers; the reference-run driver that used it as a PRECONDITION is retired (\"Derive the\nexpected-travel closed form\": there are no calibration simulations), so the callers left are\nthe throughput audit (`Performance_Evaluations/throughput/audit.py`), which calls it on every\nrun and REPORTS -- on a campaign arm a picking utilization below the band is the arm's travel\nsaving, the effect being measured, and a capped day is \"declared throughput not delivered\";\nneither fails the run (decision 7) -- and `Diagnostics/equilibrium_report.py`, which prints\nthe same verdict per leaf off a finished run tree.\n\nThe arithmetic lives in `check_rows` over already-loaded rows so a test can prove every\nclause CAN fail without a database; `check` is the thin loader in front of it.  What the\ncheck reads, and where each number comes from:\n\n    the ledger      `load_shift_days`   day, drained, cap_end, end_s, last_finish, and the\n                                        close-out LEVELS (reported, cross-checked, never judged)\n    picking         `load_batch_stats`  task_makespan (\u03a3 task time = the crew's worked\n                                        seconds), work_day, released_late, items_demanded,\n                                        total_items\n    the flows       `load_carryover`    (batch_id, reason, sku, qty): the pick side's four\n                                        FLOWS, from which `demand_flows` recovers fresh demand\n                                        and the two cause families per batch\n    put / receiving `load_work_hours`   seconds per (batch, role), joined to the day\n                                        through `batch_stats.work_day`\n    the free index  `load_free_index`   (batch_id, handling, category, size, unit, free):\n                                        the depth PER BUCKET, joined to the day through\n                                        the batch; `[]` on a vintage before the table,\n                                        which the rework clause reports as unrecorded\n\nLEVELS VERSUS FLOWS.  The ledger's `standing_carry_labour` / `standing_carry_supply` are\nLEVELS at close-out: what the day's LAST batch rolled forward.  They equal the day's flows only\nunder one batch per day (which the era completes `--releases-per-day` to), and they can never\nsay which units are re-attempts.  The two clauses therefore read the `carryover` flows and\nreport the ledger's levels beside them as a cross-check.\n\nThe GRANT is the whole declared day for every crew (crew \u00d7 S per day): a day that drained\nearly still granted S, and utilization is against the grant, not the shift end.\n\nNo CONFIG, no settings, no run tree -- the same discipline `staffing.py` keeps.  The expected\nvalues arrive in an `expectations` dict built by `expectations_for` from the staffing record\n(the run spec's, or the copy stamped onto `sim_result` -- 03's sixth seam), so every caller\nreads the same numbers through the same function.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/equilibrium.py::Clause": {
      "decorators": [
        "dataclass"
      ],
      "doc": "One clause's verdict: pass/fail, every reading it was judged on, and why.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/equilibrium.py::Clause.as_dict": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::InstrumentError": {
      "decorators": [],
      "doc": "The instrument contradicts itself: a nonzero `released_late` on a DRAINED day, or a\nbatch whose inherited carry exceeds its stated demand.\n\nA drained day means nothing was cut and nothing stood at close-out, and release waits\non the picker clock, so a batch released late into such a day is impossible unless the\nledger or the release clamp is wrong.  `items_demanded` is the sampled demand PLUS the\nprevious batch's carry, so a carry larger than the demand is impossible unless the\nrunner or the rollover is wrong.  Raised, never returned as a failed clause: a failed\nclause says the SITE is out of equilibrium; this says the MEASUREMENT is.",
      "kind": "class",
      "signature": "(RuntimeError)"
    },
    "Optimization/simconfig/equilibrium.py::RecordError": {
      "decorators": [],
      "doc": "The staffing record does not carry what the check needs (no derived block for the\npair, no section for the channel): the run is not an era run, or its record is broken.",
      "kind": "class",
      "signature": "(ValueError)"
    },
    "Optimization/simconfig/equilibrium.py::Verdict": {
      "decorators": [
        "dataclass"
      ],
      "doc": "The check's answer over one window: pass/fail with every clause's reading.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/equilibrium.py::Verdict.as_dict": {
      "decorators": [],
      "doc": "A JSON-ready copy: what the calibration record and the audit carry.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::Verdict.reasons": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[str]"
    },
    "Optimization/simconfig/equilibrium.py::_WORK_ROLE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'put': 'put', 'recv': 'receive'}"
    },
    "Optimization/simconfig/equilibrium.py::_dept_seconds": {
      "decorators": [],
      "doc": "One department's seconds inside the window, for one leaf.\n\nThe SAME arithmetic `_utilization_clause` does inline, factored out so the leaf clause\nand the site clause cannot mean different things by \"worked\" -- which is the drift the\nequilibrium report exists to prevent (\"the two cannot disagree\").",
      "is_async": false,
      "kind": "function",
      "signature": "(batch_rows, work_rows, days: list[int], dept: str) -> float"
    },
    "Optimization/simconfig/equilibrium.py::_expected_repacked_packs": {
      "decorators": [],
      "doc": "The `f_repack` the record stamped, or None if it carries none.\n\nPrefers the DERIVED block's constant (where `derive` records it with its provenance)\nand falls back to the raw input, so a record written before the constant was recorded\nbut after the input existed still answers.  Neither present means a pre-ADR-0003\nrecord, and the clause must report rather than judge.",
      "is_async": false,
      "kind": "function",
      "signature": "(derived: dict, inputs: dict) -> float | None"
    },
    "Optimization/simconfig/equilibrium.py::_get": {
      "decorators": [],
      "doc": "A field off a dataclass row or a dict row -- the loaders return both shapes.",
      "is_async": false,
      "kind": "function",
      "signature": "(row, name, default=0)"
    },
    "Optimization/simconfig/equilibrium.py::_guarantee_crew": {
      "decorators": [],
      "doc": "The stamped guarantee's crew block (ADR-0004) as the labour clause reads it, or None\non a record that predates it: `{'pickers', 'load_s', 'sd_s', 'cut_share', 'cut_share_sd'}`.\n\n`cut_share` is the stamped expectation; `cut_share_sd` is derived from the same law\n(`staffing.cut_share_sd`) so the band around it is the law's, not a typed number.  The\ncrew in the block is the solved K; `pickers` (the derived crew off `channel_crew`) is\nthe same number on every record `derive` wrote, and is what the sd is priced at.",
      "is_async": false,
      "kind": "function",
      "signature": "(section: dict, pickers: int, S: float) -> dict | None"
    },
    "Optimization/simconfig/equilibrium.py::_halves": {
      "decorators": [],
      "doc": "(first-half mean, second-half mean, second - first) of an ordered series of >= 2.",
      "is_async": false,
      "kind": "function",
      "signature": "(series: list[float]) -> tuple[float, float, float]"
    },
    "Optimization/simconfig/equilibrium.py::_isnan": {
      "decorators": [],
      "doc": "A pandas frame hands a NULL level back as NaN; the loaders hand it back as None.",
      "is_async": false,
      "kind": "function",
      "signature": "(v) -> bool"
    },
    "Optimization/simconfig/equilibrium.py::_labour_clause": {
      "decorators": [],
      "doc": "The picking queue in equilibrium: the cut share at its stamped expectation, the\nstanding labour carry under a day's capacity, and the per-day cut share not trending.\n\nLEVEL.  `cut_share` = \u03a3 cut \u00f7 \u03a3 fresh over the window, a ratio of sums.  Judged against\n`expected_cut_share` (the guarantee's `E[(W - K\u00b7S)^+] / E[W]`, ADR-0004) within\n`CUT_SHARE_Z` sampling sds of the declared law over `n_days` days; REPORTED, not judged,\non a record with no guarantee (the 2026-09-08 runs).  The closed form is for a day that\nstarts clean, so a queue carrying overflow forward reads ABOVE it -- which is the finding.\n\nCARRY.  The standing labour carry at a day's close is the cut flow of the day's LAST\nbatch (what rolls into the next day); it must stay under ONE day's capacity in units,\n`crew \u00d7 S \u00f7 s_pick` (the arm's re-centred `s_pick` when stamped).  A carry past a full\nday is a queue that cannot clear, whatever the share reads.  Reported, not judged, when\nno `s_pick` is on the record to price the cap.  The ledger's own level\n(`standing_carry_labour`) rides the reading as a cross-check.\n\nTREND.  Half-window means of the per-day cut share within `CUT_SHARE_Z` sds of their\ndifference under the day law (`_trend_tol`: the stamped sd, else the window's pooled\nwithin-half sd).  Under one batch per day the share is the carry over fresh demand, so a\nqueue that is building or draining across the window shows here even when its mean sits\nin band.\n\nA day the ledger never closed fails the clause outright (an unmeasured day is not a\ndrained one); a CAPPED day does not -- it is the `drained` reading's business.",
      "is_async": false,
      "kind": "function",
      "signature": "(shift_rows, flows: dict, days: list[int], expectations: dict) -> Clause"
    },
    "Optimization/simconfig/equilibrium.py::_ledger_reading": {
      "decorators": [],
      "doc": "The ledger's per-day verdicts as a READING (written by `is_drained`), for the labour\nclause: never a verdict of its own since the split.\n\n`capped` are the days that ended with labour standing or a task past the cap; `missing`\nthe days the ledger never closed (those DO fail the clause -- an unclosed day is not a\nmeasured one).  `overtime_days` are CAPPED days since the overtime amendment; through the\nloaders a drained day never appears on that list (a pre-amendment ledger is served with\nthe term folded in).  `overtime_only_days` are the capped days that overtime ALONE capped\n-- nothing standing at close-out, the last task a few seconds past the whistle -- named\napart because \"declared throughput not delivered\" overstates such a day: it delivered\neverything, late.  A day whose labour carry the vintage did not record (NULL) is never\ncounted there.  The SUPPLY carry is reported: `supply_standing_days` closed with demand\nthe shelf could not serve still rolling forward, `supply_standing_max_units` the largest\nsuch LEVEL (never summed across days); `supply_split_recorded` is False on the pre-split\nvintage (487a65bf83a9), whose rows carry the halves as NULL.  `labour_standing_max_units`\nis the ledger's own labour LEVEL at its worst close-out, the cross-check for the flows.",
      "is_async": false,
      "kind": "function",
      "signature": "(shift_rows, days: list[int]) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::_ledger_reading._level": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(r, name)"
    },
    "Optimization/simconfig/equilibrium.py::_ledger_reading._nothing_standing": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(r) -> bool"
    },
    "Optimization/simconfig/equilibrium.py::_num": {
      "decorators": [],
      "doc": "A number the summary can format: an int or a float that is not NaN (`_isnan`).",
      "is_async": false,
      "kind": "function",
      "signature": "(v) -> bool"
    },
    "Optimization/simconfig/equilibrium.py::_opt_int": {
      "decorators": [],
      "doc": "An optional count: None for a NULL the loader handed back as None or a frame handed\nback as NaN (the `free_bins` / `put_spills` rule: unknown, never zero).",
      "is_async": false,
      "kind": "function",
      "signature": "(v) -> int | None"
    },
    "Optimization/simconfig/equilibrium.py::_packs_budget": {
      "decorators": [],
      "doc": "`f_repack` (packs repacked PER PACK RECEIVED) as a pack COUNT for the window.\n\nAt 0.0 the rate and the count coincide, which is the only value that has ever shipped.\nAny other value needs the window's received-pack count to convert, and nothing here has\nit -- so this REFUSES rather than comparing a rate to a count and silently judging a run\nagainst a number three orders of magnitude off.  Whoever gives `f_repack` a real value\nwrites the conversion here; that is the one line that has to learn it.",
      "is_async": false,
      "kind": "function",
      "signature": "(f_repack: float) -> float"
    },
    "Optimization/simconfig/equilibrium.py::_released_late_clause": {
      "decorators": [],
      "doc": "`released_late` = 0 on every drained day -- attributed to the day that CAUSED it.\n\nA batch is released late when the crew is still working the previous one, and the\nclamp erases everything but this column.  So the lag lands on the batch released INTO\nday d, but the day that overran is d - 1: a capped day 1 leaves its tail on day 2's\nrelease even when day 2 then drains (measured on the first era smoke run: 69.9 s on a\ndrained day 2 behind a capped day 1).  The self-consistency assertion is therefore that\nlag in day d exists only behind a CAPPED day d - 1; lag behind a drained day, or on the\nfirst day of the run (nothing to overrun), is the instrument contradicting itself.  A\nday d - 1 the ledger never closed cannot be judged here; the labour clause reports it.",
      "is_async": false,
      "kind": "function",
      "signature": "(shift_rows, batch_rows, days: list[int]) -> Clause"
    },
    "Optimization/simconfig/equilibrium.py::_rework_clause": {
      "decorators": [],
      "doc": "ADR-0003's rework, per day.  THREE EVENTS JUDGED AT ZERO; the depth REPORTED PER BUCKET.\n\nTHE THREE EVENTS ARE ONE FINDING AT THREE RUNGS of the put-away chain, and each fails\nthe clause with a SIZING message (\"Band the own-bin share and the free-index depth\",\ndecisions 1, 2 and 4 -- ADR-0003 is the decision record; this docstring does not\nre-decide):\n\n  * a TIER SPILL (`put_spills`): `_candidates_raw` moved up a size tier because the\n    unit's own bucket had no free bin.  The FIRST deviation from the put pricer's\n    per-class assumption, and the one event that fires while the other two still read 0.\n  * a TOP-UP into an occupied bin (`put_topups`): the whole tier chain was dry.\n  * a REPACK (`recv_repacked_packs`): the own bins were full too.  Judged against the\n    record's stamped `f_repack = 0` (provenance `assumed`); `expected` None means no\n    record was carried and the repack is reported without judging, because a missing\n    expectation is not a passing one.\n\nThe zero for the spill and the top-up is the ADR's own claim, hard-coded here and NOT a\nstaffing key: nothing on the record prices a spill or a top-up differently from a\nplacement, so a tolerance would have no number to derive from.  A run that wants a tight\nwarehouse gets the finding it asked for.\n\nTHE DEPTH IS REPORTED PER BUCKET, NEVER JUDGED (decisions 3 and 6).  `free_rows` are the\n`free_index` table's rows -- one LEVEL per BinKey per batch, the leaf's own section by\nconstruction -- read over the window against `setup_free`, the `free` the record stamps\nper bucket at setup (`coverage.final.<ch>.fielded.buckets`).  Per bucket: the window's\nfirst and last reading, minimum, mean, the drawdown (first - last), and the batches that\nread dry.  A typed level floor was rejected: it certifies nothing on a store whose slide\noutlives the window and fails a healthy run the moment the window moves; the trajectory\nband waits for the stationary fragmentation closed form (34).  When the record carries\n`setup_free`, only ITS buckets are reported -- the other section's rows are exactly the\nartefact `batch_stats.free_bins` cannot separate.  The readings this decision was drawn\nfrom are the corrected 2026-09-09 numbers (`comparison_20260909_204522`): 18.0% / 15.2%\nof the section free at setup (the 0.85 fill plus aisle rounding), falling 6,763 / 18,279\nbins over 40 days -- NOT the 57-59% the whole-geometry total showed.\n\nBATCH 0 CARRIES INITIAL STOCKING.  The runner's first snapshot folds every event since\nthe manager was built into batch 0's row, so a spill or a top-up during initial\nstocking is judged like any other -- deliberately: the planner fields every bucket at\nexactly its declaration, so a spill at setup is that promise broken one bucket early,\nand on the reference pair batch 0 reads 0 / 0 (`comparison_20260910_100637`).  Batch\n0's `own_bin_share` is the one reading this skews (its denominator,\n`reorder_placements`, excludes setup), which is why the share is reported and the\ncount is judged.\n\nVINTAGES.  `put_topups` and the two repack flows are 0 on every pre-ADR vintage BY\nCONSTRUCTION (put-away could not add to an occupied bin), so those terms pass trivially\non an old run -- the reading's `n` says how many batches it saw.  `put_spills` and\n`free_bins` are NOT in that group: an older run spilled and had free bins and simply\nnever recorded either, so both read None (unknown) rather than 0, and an unknown spill\ncount cannot fail a clause judged at zero -- the reading says `spills: None` and the\nreason names it.  `free_rows` is `[]` on a vintage before the table, reported as\n\"depth unrecorded per bucket\", never as a warehouse with no free bins.",
      "is_async": false,
      "kind": "function",
      "signature": "(batch_rows, days: list[int], expected_repack_packs: float | None, free_rows=None, setup_free: dict | None=None) -> Clause"
    },
    "Optimization/simconfig/equilibrium.py::_rework_clause._where": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> str"
    },
    "Optimization/simconfig/equilibrium.py::_supply_clause": {
      "decorators": [],
      "doc": "The shelf's first-pass service: the supply share at its stamped level, not trending.\n\nLEVEL.  `\u03a3 supply_new \u00f7 \u03a3 fresh` over the window -- each unit counted once, on the first\nbatch its SKU's shortfall rose (`demand_flows`) -- judged within `SUPPLY_LEVEL_TOL` of\n`expected`, the record's `1 - fill_rate` (the solved floor's expected first-pass missed\nshare under base stock, \"Choose the coverage floor\", decision 5, priced AT the stamped\nlead since \"Declare the coverage against the inbound lead\").  A ratio of sums, because\nthe fill rate is one (`\u03a3 served \u00f7 \u03a3 units`, units-weighted).  REPORTED, not judged, on a\nrecord with no stamped fill.\n\nTHE LEAD, reported and never banded (decision 10 of the lead ticket): the stamped\norder-to-shelf lead (`lead['lead_days']`), the REALIZED one off the batch rows\n(`realized_lead`), and the EXPLAINED level `1 - fill(realized)` read off the record's\nfill-versus-transit curve at the realized transit (the realized lead less the section's\nsupplier-lead days) -- so a binding yard reads as supply moved by the yard's detention\n(the level sits near the explained value, above the band) and a model error as a gap the\nrealized lead cannot explain.  The band itself is unchanged: `SUPPLY_LEVEL_TOL` around the\nstamped expectation.  `reading['lead']` is `{'stamped_days', 'realized_days',\n'explained', 'in_transit_mean', 'ordered_mean', 'transit_days'}`, Nones where the record\nor the rows carry nothing.\n\nTREND.  Half-window means of the per-batch share within `TREND_TOL`, as decision 4 had it.\n`units.supply` is the raw flow with re-attempts; `units.reattempts` is what the old\nclause was double-counting.",
      "is_async": false,
      "kind": "function",
      "signature": "(flows: dict, days: list[int], expected: float | None, *, batch_rows=(), lead: dict | None=None) -> Clause"
    },
    "Optimization/simconfig/equilibrium.py::_trend_tol": {
      "decorators": [],
      "doc": "The labour trend's band: `(tol, source, sd)`, `CUT_SHARE_Z` sds of the DIFFERENCE of\ntwo half-window means, `sd \u00b7 sqrt(1/n1 + 1/n2)`.\n\n`sd` is the declared law's per-day spread (`expected_cut_share_sd`, source `declared`)\nwhen the record stamps one; without it (the 2026-09-08 runs) the window's own pooled\nWITHIN-half sample sd (source `empirical`) -- within, not overall, so a real step does not\nwiden the band that judges it.  Fewer than four days leave nothing to pool, and the typed\n`TREND_TOL` stands in (source `typed`).  The typed number alone was the review's critical\nfinding: half a day's spread, failing a healthy window a third of the time.",
      "is_async": false,
      "kind": "function",
      "signature": "(series: list[float], sd_day: float | None) -> tuple[float, str, float | None]"
    },
    "Optimization/simconfig/equilibrium.py::_unrecorded": {
      "decorators": [],
      "doc": "The reason a flow clause cannot be judged: no carryover rows, yet demand went unserved.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows) -> str"
    },
    "Optimization/simconfig/equilibrium.py::_utilization_clause": {
      "decorators": [],
      "doc": "Realized-vs-expected utilization per department over one LEAF's window.\n\n`departments` is what a coupled leaf narrows.  Under one site dock and one site pool of\nputters, put and receiving are not this channel's crews at all: a leaf's seconds over a\nSITE crew is a share of a whole the channel does not own (ADR-0005 says exactly that),\nand banding it per leaf would publish two verdicts on one crew that can disagree with\neach other -- the double count reappearing as a reporting artefact after being fixed by\nconstruction.  SITE BANDS, LEAVES REPORT: the leaf keeps `pick`, and\n`site_utilization_clause` carries put and recv once against the SUMMED expectation.",
      "is_async": false,
      "kind": "function",
      "signature": "(batch_rows, work_rows, days: list[int], expectations: dict, departments: tuple=DEPARTMENTS) -> Clause"
    },
    "Optimization/simconfig/equilibrium.py::arm_expectations": {
      "decorators": [],
      "doc": "The expectations for ONE ARM: the pair's, with the picking band -- and the labour\nclause's expected cut share -- re-centred on the arm's own expected seconds per unit\nunder its initial placement.\n\nThe pair's pick expectation was drawn at the class-uniform `s_pick` (the demand's\nfixed point).  An arm whose placement makes a unit cheaper is EXPECTED to run below it\n-- by the ratio of the two expectations, since the demand is shared -- and the band\nbelongs around that number (\"Derive the expected-travel closed form\": the per-arm\nexpectation is stamped for the report only).  The same ratio scales the day's load and\nits sd, so the arm's expected cut share is `cut_share(load\u00b7r, sd\u00b7r, K, S)` -- a cheaper\nunit is cut less often -- and its sampling sd follows.  Without a stamped\n`expected_pick` (a flag-off arm, an older run) the pair's expectations are returned\nunchanged; the pick department is left alone when either `s_pick` is unknown.",
      "is_async": false,
      "kind": "function",
      "signature": "(expectations: dict, expected_pick: dict | None) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::bucket_label": {
      "decorators": [],
      "doc": "The BinKey as ONE string, `handling/category/size/unit` -- the key the rework clause's\nper-bucket reading is keyed by, so `Verdict.as_dict` stays JSON-ready.  One constructor\nfor the label, shared by the record side (`setup_free_by_bucket`) and the row side\n(`_rework_clause`), so the two cannot spell a bucket differently.",
      "is_async": false,
      "kind": "function",
      "signature": "(handling, category, size, unit) -> str"
    },
    "Optimization/simconfig/equilibrium.py::check": {
      "decorators": [],
      "doc": "The check over one arm's sim DB: load the five sources, judge the window.\n\n`expectations` is `expectations_for(...)` for this leaf.  The loaders are the\nversion-negotiating ones (`shift_days`, `work_events`, `carryover` and `free_index` are\nconditional tables: a pre-era vintage answers `[]`, which the labour clause reports as a\nwindow the ledger never closed rather than as a plausible pass, and the rework clause\nas a depth never recorded per bucket).",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str, run_id: int, day_lo: int, day_hi: int, *, expectations: dict, departments: tuple=DEPARTMENTS) -> Verdict"
    },
    "Optimization/simconfig/equilibrium.py::check_rows": {
      "decorators": [],
      "doc": "The five clauses over already-loaded rows.  See the module docstring for each.\n\n`shift_rows` are `load_shift_days` dicts; `batch_rows` are `BatchStats` (or dicts with\nthe same fields); `work_rows` are `load_work_hours` dicts; `carry_rows` are\n`load_carryover` rows (dicts or dataclasses with batch_id, reason, sku, qty) -- REQUIRED,\nnot defaulted, because an empty list reads as \"everything served\" and a caller that\nforgot to load the table must not get that answer by accident.  `free_rows` are\n`load_free_index` rows and ARE defaulted: an absent table means \"the depth was never\nrecorded per bucket\", which the rework clause reports as exactly that -- the reading is\nnever judged, so a missing list cannot manufacture a pass.  Raises `InstrumentError`\nfrom the released-late clause and from `demand_flows`; every other outcome is a `Verdict`.\n\n`departments` narrows the UTILIZATION clause alone, and a COUPLED leaf is the one\ncaller that narrows it: put and receiving are the SITE's crews there, banded once\nby `site_utilization_clause` against the SUMMED expectation. Site bands, leaves\nreport -- two verdicts on one crew can disagree with each other, and that is the\ndouble count reappearing as a reporting artefact after being fixed by construction.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, shift_rows, batch_rows, work_rows, carry_rows, day_lo: int, day_hi: int, expectations: dict, free_rows=None, departments: tuple=DEPARTMENTS) -> Verdict"
    },
    "Optimization/simconfig/equilibrium.py::demand_flows": {
      "decorators": [],
      "doc": "Per batch, the demand ledger the two flow clauses read: `{batch_id: {...}}`.\n\n`items_demanded` is the EFFECTIVE batch -- the sampled demand PLUS everything the\nprevious batch rolled forward (`strategy_runner`: `sum(_eff_batch.items.values())`) --\nand nothing records the sampled half.  It is recovered exactly from the flows: the\nprevious batch's carry is its four pick-side `carryover` reasons summed (the cut's, the\ntwo supply ones, and a skipped batch's `unpicked_notasks`), which is precisely\n`_pending`, so\n\n    fresh_i = items_demanded_i - \u03a3 carry_{i-1}\n\nholds whenever the carry is re-offered -- `roll_over_unpicked`, which the era completes\nto on (`run_simulation._check_era_flags`).  Flag-off the rows are recorded and NOT\nre-offered, and this identity does not hold; the check is the era's, and a caller with\nexpectations has one (`expectations_for` refuses a record with no derived block).\nA carry larger than the demand is the runner contradicting itself and RAISES\n`InstrumentError`.\n\nPer batch:\n\n    demanded    the effective batch (`items_demanded`)\n    carry_in    the previous batch's whole pick-side carry\n    fresh       demanded - carry_in\n    picked      `total_items`\n    cut         `unpicked_daycut` units this batch (the labour flow, re-cuts included:\n                a re-offered unit the whistle stops again is overflow again -- the\n                queue's cost, which the labour clause is there to see)\n    supply      the two supply reasons this batch, re-attempts INCLUDED\n    supply_new  the same, each unit counted ONCE: per SKU, the rise in the family's\n                failure count over the previous batch (`max(0, x_i - x_{i-1})`).  A LOWER\n                BOUND on the first-attempt failures: exact when the whole SKU demand fails\n                (a stockout: `x_i = fresh_i + x_{i-1}`), LOW when a restock lands\n                mid-shortage (10 carried + 6 fresh against 8 received, 8 fail -> 0 new,\n                though up to 6 fresh units failed first time), because demand has no unit\n                identity and per-SKU fresh demand is not recorded -- only the batch total\n                is.  Recording it (a schema change) is what would make this exact; until\n                then the supply level can read low against `1 - fill` when the shelf is\n                under-stocked, and a reader should weigh `units.reattempts` beside it.\n                The fill-rate expectation is a FIRST-PASS quantity, and a re-offered line\n                meets a restocked shelf -- its second failure is a second draw, not the\n                same miss twice.\n    notasks     a skipped batch's demand rolling forward whole (neither family)\n    recorded    False when no PICK-SIDE row exists (after the reason filter, so a table\n                holding only the put side's levels or the pre-2026-08-25 `unplaced` rows\n                does not count): the flows are unrecorded on such a vintage, and a batch\n                that served everything and a batch the table never saw both read zero\n                here -- the clauses tell them apart by whether `demanded - picked` is\n                nonzero",
      "is_async": false,
      "kind": "function",
      "signature": "(batch_rows, carry_rows) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::demand_flows._family": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(b: int, reasons) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::expectations_for": {
      "decorators": [],
      "doc": "The expected utilization per department for ONE channel leaf, off the staffing record.\n\n`staffing` is the record as the run spec holds it (`{inputs, provenance, derived,\ncalibration}`) -- the same object `run_analysis._sim_result_from_meta` stamps onto\n`sim_result`, so the audit passes `ctx.sim_result['staffing']` and the reference-run\ndriver passes `run_spec['staffing']`.  `derived` and `calibration` are keyed BY PAIR\nbecause the catalogue decides the load; `pair` is the inventory label\n(`sim_result['inventory']`, `ChannelRun.pair`).  `channel` is None on a store-only run.\n\nReturns\n\n    {'pair', 'channel', 'day_seconds', 'band_tol',\n     'departments': {dept: {'crew': int, 'expected': float}},   # only those that apply\n     'absent': {dept: why},\n     'fill_rate', 'expected_missed_share',      # the supply clause's level (None pre-floor)\n     'lead_days', 'transit_days', 'fill_vs_transit',   # the lead it was priced at (None pre-lead)\n     'expected_cut_share', 'expected_cut_share_sd', 'guarantee',   # the labour clause's\n     'expected_repacked_packs',\n     'flags': {'overridden', 'saturated'}}\n\nA department with no crew on this site, or whose expected value the record does not\ncarry for this channel, is ABSENT rather than expected at zero: a band around 0.0 would\npass a crew that did nothing and fail one that did anything, and neither is a finding.\nRaises `RecordError` on a record with no derived block for the pair (a flag-off run) or\nno section for the channel.",
      "is_async": false,
      "kind": "function",
      "signature": "(staffing: dict, *, pair: str, channel: str | None) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::fill_at": {
      "decorators": [],
      "doc": "The record's fill-versus-transit curve (`era_coverage.fill_curve`, `[{'transit_days',\n'fill_rate'}, ...]` sorted by transit) read at `transit_days`, linearly between points\nand held at the ends; None without a curve.  The one interpolation the audit does: the\ncurve is the closed form evaluated at the stamp's own law with the median scaled, so\nbetween two points it is the same section under a slightly faster or slower road.",
      "is_async": false,
      "kind": "function",
      "signature": "(curve: list | None, transit_days: float) -> float | None"
    },
    "Optimization/simconfig/equilibrium.py::is_drained": {
      "decorators": [],
      "doc": "THE drained verdict: did working day end with its LABOUR done?\n\nTrue when nothing was cut in the day, no standing labour survives it -- no storage\nunits in the put queues or held, none on the dock floor, and no demand the CUT left\nunpicked (`unpicked_daycut`, the labour carry) -- and no task finished past the cap\n(`overtime`: `last_finish > cap_end`, START-gate overtime).  Overtime is labour that\ndid not fit the day, so it caps the day exactly as standing work does (\"Overtime\nbehind a drained day raises the instrument\", 2026-09-07, amending 18): a day stamped\ndrained with a finish past the whistle leaves lag on the next release, and the\nreleased-late clause then raises on a healthy run -- the store leaf of the line-floor\ncheck, day 3, 138 s of overtime with nothing standing.  The SUPPLY carry -- demand no bin could\nserve (`unpicked_unavailable`, `unpicked_unstocked`) -- is deliberately NOT an argument:\nit is stock not delivered, the `supply` clause's quantity, and a crew that realized every\ntask it was given has drained its day whatever the shelf held.  Counting it made the\nverdict unreachable: under lumpy lines some SKU is always short, so 0/20 days drained on\nthe reference run with pickers in band (\"Choose the coverage floor\", decision 7).\n\nThe lead queue is never standing work either (transit is calendar, not labour), and\nreleases are exhausted by construction at a day boundary, so neither is read.\n\nONE definition, two readers: `strategy_runner._shift_close_out` writes the ledger's\n`drained` with it; the `labour` clause READS that column into its `drained` reading\nrather than re-deriving, so a pre-split vintage's verdicts stand as its runner judged\nthem -- and since the split of 2026-09-09 a capped day is a READING, never a failed\nclause.  The overtime term is the one exception, and it lives in the LOADER, not the\nclause: the amendment moved no column, so no vintage separates a ledger stamped before\nit from one stamped after, and `Picking_Data.load_shift_days` serves `drained` with the\nterm folded in off the row's own `last_finish` / `cap_end` -- a no-op on a ledger this\ndefinition wrote.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, cut: bool, standing_put: int, standing_dock: int, standing_carry_labour: int, overtime: bool) -> bool"
    },
    "Optimization/simconfig/equilibrium.py::realized_lead": {
      "decorators": [],
      "doc": "The realized ORDER-TO-SHELF lead of one leaf over a window, by Little's law:\nmean pieces in transit over mean units ordered per day, both read per batch off\n`batch_stats` (`in_transit_qty`, a LEVEL at batch close; `units_ordered`, the FLOW that\nentered transit that batch) -- the flows inbound-optimization 23 summed by hand.  One\nbatch is one day under the era, so the per-batch mean is the per-day rate.  Returns\n`{'days': lead | None, 'in_transit_mean', 'ordered_mean', 'n'}`; `days` is None when\nthe window ordered nothing (no flow to divide by) or holds no batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(batch_rows, days: list[int]) -> dict"
    },
    "Optimization/simconfig/equilibrium.py::setup_free_by_bucket": {
      "decorators": [],
      "doc": "`{bucket_label: free}` off one channel's `coverage.final.<ch>` block, or None when the\nrecord carries no `fielded.buckets` (a run before \"Field the requirement\").",
      "is_async": false,
      "kind": "function",
      "signature": "(final_ch: dict) -> dict | None"
    },
    "Optimization/simconfig/equilibrium.py::site_utilization_clause": {
      "decorators": [],
      "doc": "ONE band over the SITE's put and receiving crews, and the leaves' shares beside it.\n\nTHE WHOLE ARGUMENT FOR ONE SITE CLAUSE IS THAT `expected_utilization` IS LINEAR IN LOAD.\nA department's expectation is `load / (crew x S)` by construction, and crew and day are\nthe SITE's -- one crew, one day -- so the two channels' expectations SUM to the site's,\nexactly, with no reweighting.  The realized side sums the same way because the loads do.\nThat identity is asserted by `Tests/unit/test_site_equilibrium.py`, not assumed, because\neverything this clause claims rests on it.\n\nAND IT SITS AT rho RATHER THAN BELOW IT, for put, on a coupled run.  `expected_utilization`\ncarries a standing caveat -- \"integer crews and single-channel leaves undercut rho by\nconstruction\" -- whose SECOND half is what the site put pool removed: a leaf undercut rho\nbecause it fielded the whole site crew against one channel's load.  Under one pooled crew\nover both channels' volume that cause is gone, and the caveat is retired FOR PUT ON A\nCOUPLED RUN.  It is kept verbatim flag-off and kept whole for the integer-crew half, which\nthe pool does not touch.\n\n`rows_by_channel` is `{channel: (batch_rows, work_rows)}`; `expectations_by_channel` is\n`{channel: expectations_for(...)}`.  Two channels' rows in, one banded reading out, with\neach channel's realized share carried INSIDE it as an unbanded number -- printed beside\nthe site figure and never instead of it, because a right site total hides two wrong\nshares (put-away's site load was 0.9% exact while both per-channel bands failed in\nopposite directions).\n\nA pure rows-in function, like every other clause here: no CONFIG, no settings, no run\ntree.  The REPORT walks the tree and accumulates; this only bands.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows_by_channel: dict, days: list[int], expectations_by_channel: dict) -> Clause"
    },
    "Optimization/simconfig/equilibrium.py::summarize": {
      "decorators": [],
      "doc": "One log line per clause, for the equilibrium report and the audit's INFO output.",
      "is_async": false,
      "kind": "function",
      "signature": "(verdict: Verdict) -> str"
    },
    "Optimization/simconfig/equilibrium.py::window_of": {
      "decorators": [],
      "doc": "(first day, last day) the ledger closed, or None when no day was ever closed out.",
      "is_async": false,
      "kind": "function",
      "signature": "(shift_rows) -> tuple[int, int] | None"
    },
    "Optimization/simconfig/expected_travel.py": {
      "decorators": [],
      "doc": "expected_travel.py \u2014 the calibrated era's pick and put-away seconds per unit as a\nCLOSED-FORM EXPECTATION over the demand distribution and the built geometry.\n\nThere are no calibration simulations (.scratch/department-calibration, \"Derive the\nexpected-travel closed form\", user decision 2026-09-06).  Every input is known at setup --\nthe catalogue's line shares and quantity rates, the warehouse the run just built, the\nplacement distribution, the travel speeds, the handling coefficients, the cart -- so the\nseconds a day of picking costs is an expectation computed here, and the staffing derivation\nbalances around it (`staffing.py`).  The derivation of every formula, its assumptions and\nthe development-time residual against the reference passes are in\n`.scratch/department-calibration/assets/expected-travel-derivation.md`; this docstring only\nrestates what the code needs a reader to know.\n\n## What the simulator charges (the rules this reproduces -- `fast_pick`, `putaway`)\n\nA TASK is one aisle visit.  The picker starts at the mouth (0, 0), walks the bins in\n`_plan_aisle_path` order (columns ascending in x; inside a column toward the nearer end\nfirst), pays `|\u0394x|\u00b7x_pace + |\u0394y|\u00b7y_pace` per segment -- to EVERY bin on the path, empty\nones included -- a `cart_swap_coef` per next-fit swap, `M(y)\u00b7(intercept + q\u00b7per_item +\nq\u00b7var)` per non-empty bin, and on a one-way lane an exit to the far end and down to the\nground.  No travel between tasks.  A put is costed from the mouth with no travel between\nplacements.\n\nTwo identities follow and are what the tests pin: on a two-way lane a task's x travel is\nexactly `x_max` of its bins (the path is monotone in x); on a one-way lane it is exactly\nthe aisle length.\n\n## The expectation, in one paragraph\n\nA day is `N ~ Normal(n, cv\u00b7n)` i.i.d. lines over the section's SKUs (share `\u03c0_s`), each\ndemanding units drawn from the SKU's STAMPED line law (`Demand.line`; today\n`max(1, Poisson(\u03bb_s))`), drained through the SKU's bins in the sim's drain\norder (forward-pick first, then location).  Bin k of a SKU is REACHED when the line's\nquantity exceeds the stock before it -- the law's tail, `line.survival` -- so every bin carries a visit RATE\nper line.  Poissonisation makes bins independent given N, and each aisle then yields, in\nclosed form: its visit probability (a task), `E[x_max]` (a product-sum over columns), the\nexpected y walk (a Markov chain over height levels, one transition per column) and the\none-way exit.  Swaps are the renewal count of next-fit over per-visit volumes.  Handling is\nthe catalogue priced line by line at the bins' heights.  Seconds per unit is the ratio of the\nday's expected seconds to its expected units, and the fixed point `n\u00b7s(n)\u00b7\u2026 = K\u00b7S\u00b7\u03c1` is one\nscalar root-find.\n\n## The seam: `PlacementDist`\n\nThe same formula takes two placement distributions, because two genuinely exist:\n`initial` (an arm's own bin map, after its initial stock -- what the arm's report is\njudged against) and `uniform` (each unit spread evenly over the free bins of its class --\nwhat the pair's DEMAND derives from, arm-independent, and the long-run steady state of a\nFIFO restock, which reorders onto a uniformly random free bin).  The check that separated\nthem is in the derivation note, \u00a73.\n\nTHIS MODULE IS PURE: geometry, orders, a pick/put config and numbers in; dicts out.  It\nimports no CONFIG and touches no file.  `scipy` is already a dependency (the affinity CSR).",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/expected_travel.py::AisleGeom": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One aisle's grid: `C` columns of `x_step`, `R` rows of `y_step`, a `key` (BinKey).",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/expected_travel.py::AisleGeom.length": {
      "decorators": [
        "property"
      ],
      "doc": "The one-way exit's far end (`Aisle.aisle_width`).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Optimization/simconfig/expected_travel.py::AisleGeom.x_of": {
      "decorators": [],
      "doc": "Bin centre x of 1-based column `col` -- `Aisle.Bin.x_phys`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, col: int) -> float"
    },
    "Optimization/simconfig/expected_travel.py::AisleGeom.y_of": {
      "decorators": [],
      "doc": "Bin centre y of 1-based row `row` -- `Aisle.Bin.y_phys` of a uniform aisle.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, row: int) -> float"
    },
    "Optimization/simconfig/expected_travel.py::Geometry": {
      "decorators": [],
      "doc": "The built warehouse as the expectation reads it: aisles grouped by BinKey.\n\n`from_warehouse` reads a `Warehouse` (the pair-level `warehouse_meta` or a worker's\nown build); `from_layout_rows` reads the archived `aisle_layout` table rows, so an\nanalysis can recompute an expectation off the archive without rebuilding.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/expected_travel.py::Geometry.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisles: list[AisleGeom]) -> None"
    },
    "Optimization/simconfig/expected_travel.py::Geometry.class_aisles": {
      "decorators": [],
      "doc": "The aisles of BinKey `key`, of which there must be at least one with a bin:\nan unbuilt class raises `UnbuiltClass`, never prices as empty space.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key: tuple) -> list"
    },
    "Optimization/simconfig/expected_travel.py::Geometry.class_bins": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key: tuple) -> int"
    },
    "Optimization/simconfig/expected_travel.py::Geometry.class_mean_height_mult": {
      "decorators": [],
      "doc": "The height multiplier averaged over every bin of the class (uniform placement).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key: tuple, brackets) -> float"
    },
    "Optimization/simconfig/expected_travel.py::Geometry.class_mean_travel": {
      "decorators": [],
      "doc": "Mouth-to-bin seconds averaged over every bin of the class (a put's travel).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, key: tuple, x_pace: float, y_pace: float) -> float"
    },
    "Optimization/simconfig/expected_travel.py::Geometry.from_layout_rows": {
      "decorators": [
        "classmethod"
      ],
      "doc": "`rows`: dicts with aisle_id, handling_type, category, unit_type, storage_size,\nbay_x, bay_y (the `aisle_layout` table / `save_aisle_layout` rows).",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, rows) -> 'Geometry'"
    },
    "Optimization/simconfig/expected_travel.py::Geometry.from_warehouse": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, warehouse) -> 'Geometry'"
    },
    "Optimization/simconfig/expected_travel.py::PlacementDist": {
      "decorators": [],
      "doc": "`sites[sku] -> [Site, ...]` in the sim's drain order, plus which kind it is.\n\n`initial(bin_map, geometry)`: `bin_map[sku] = [(aisle_id, bayX, bayY, qty), ...]` as an\narm's manager holds them after the initial stock; sorted forward-pick first, then\nlocation -- `Task.from_batch`'s order.\n`uniform(units_by_sku)`: `units_by_sku[sku] = [StorageUnit, ...]` (the packs\n`viable_storage_units` produces for the SKU's stock); each unit is spread over its\nclass.  Forward-pick units first, then the given order.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/expected_travel.py::PlacementDist.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, kind: str, sites: dict) -> None"
    },
    "Optimization/simconfig/expected_travel.py::PlacementDist.initial": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, bin_map: dict, geometry: Geometry) -> 'PlacementDist'"
    },
    "Optimization/simconfig/expected_travel.py::PlacementDist.uniform": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, units_by_sku: dict) -> 'PlacementDist'"
    },
    "Optimization/simconfig/expected_travel.py::SectionRates": {
      "decorators": [
        "dataclass"
      ],
      "doc": "Everything a day of `n` lines scales linearly from.  Per LINE: expected units,\nbin visits, handling seconds, per-visit volume moments; and the visit rates -- per\naisle (C, R) matrices for `initial`, one scalar per class for `uniform`.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/expected_travel.py::Site": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One storage unit of a SKU, in drain order: its quantity and WHERE it is -- a concrete\nbin (`initial`: aisle_id, col, row set) or only its class (`uniform`).",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/expected_travel.py::UnbuiltClass": {
      "decorators": [],
      "doc": "A BinKey the built warehouse has NO aisle for was asked for a class mean.\n\n`class_mean_travel` used to answer 0.0 and `class_mean_height_mult` 1.0 here, so a\nreorder pack whose class the planner never built was priced as a FREE put and a pick\nfrom it as a travel-less visit, with no warning (\"Close the put closed form's three\nknown gaps\", gap 3).  The planner sizes every bucket from the same packer this module\nreads (`viable_storage_units`), so on a healthy run the class always exists; when it\ndoes not, the run itself would have to rescue that pack, and a silent zero here hid\nexactly that.  Raised rather than warned: a price of zero is a number the crews get\nsized on.",
      "kind": "class",
      "signature": "(ValueError)"
    },
    "Optimization/simconfig/expected_travel.py::_aisle_routing": {
      "decorators": [],
      "doc": "(P(aisle visited), E[x travel], E[y travel]) in INCHES for visit-rate matrix `m`.",
      "is_async": false,
      "kind": "function",
      "signature": "(m: np.ndarray, a: AisleGeom, one_way: bool) -> tuple[float, float, float]"
    },
    "Optimization/simconfig/expected_travel.py::_nodes": {
      "decorators": [],
      "doc": "(scale, weight) pairs averaging over N ~ Normal(n, cv\u00b7n) -- one pair when cv = 0.",
      "is_async": false,
      "kind": "function",
      "signature": "(cv: float) -> list[tuple[float, float]]"
    },
    "Optimization/simconfig/expected_travel.py::_steps_for": {
      "decorators": [],
      "doc": "(x_step, y_step) of a uniform aisle, by the same rule `Aisle.__init__` applies.",
      "is_async": false,
      "kind": "function",
      "signature": "(unit_type: str, storage_size: str) -> tuple[int, int]"
    },
    "Optimization/simconfig/expected_travel.py::accumulate": {
      "decorators": [],
      "doc": "Stage 1: walk the section once and accumulate the per-line quantities.\n\n`orders` is the channel section (`staffing.regime_orders`); `pick_cfg` is the channel's\n`PickConfig` (duck-typed: `pick_intercept`, `pick_per_item`, the four handling\ncoefficients, `height_brackets`).  A SKU with no sites (nothing placed) contributes its\nline share to nothing and is counted in `unplaced_skus`; a `uniform` site whose class\nthe geometry never built raises `UnbuiltClass` (it used to carry handling and no travel).",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, pick_cfg, dist: PlacementDist, geometry: Geometry) -> SectionRates"
    },
    "Optimization/simconfig/expected_travel.py::expected_pick": {
      "decorators": [],
      "doc": "The expected day at `n` lines: every component per day, and `s_pick`.\n\n`pick_cfg` supplies `x_speed`/`y_speed` (ft/s), `one_way`, `cart_swap_coef` and\n`cart` (its `capacity()`).  `units == 0` yields `s_pick = 0.0` (an empty section).",
      "is_async": false,
      "kind": "function",
      "signature": "(rates: SectionRates, geometry: Geometry, pick_cfg, n: float, cv: float) -> dict"
    },
    "Optimization/simconfig/expected_travel.py::expected_swaps": {
      "decorators": [],
      "doc": "Next-fit swaps per day as a renewal count over per-visit volumes: the cart holds\n`(cap - E[v] + E[v\u00b2]/2E[v]) / E[v]` visits in expectation (Wald + the residual).",
      "is_async": false,
      "kind": "function",
      "signature": "(vol_per_day: float, e_v: float, e_v2: float, cart_cap: float) -> float"
    },
    "Optimization/simconfig/expected_travel.py::put_site_pricer": {
      "decorators": [],
      "doc": "A `(unit) -> (travel_s, height_mult)` for `staffing.implied_reorders`: the put's\ntravel from the mouth and its height multiplier under the placement distribution --\nthe class-uniform mean for `uniform`; for `initial`, the mean over the SKU's own bins\n(a reorder refills the SKU's locations).  A unit whose class the geometry never built\nraises `UnbuiltClass` on either branch rather than pricing the put at zero travel.",
      "is_async": false,
      "kind": "function",
      "signature": "(geometry: Geometry, dist: PlacementDist, put_cost, put_speed, brackets=None)"
    },
    "Optimization/simconfig/expected_travel.py::put_site_pricer.price": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit)"
    },
    "Optimization/simconfig/expected_travel.py::routing": {
      "decorators": [],
      "doc": "Stage 2 at `n` lines a day: `{tasks, x_inches, y_inches}` per day, averaged over the\nday-to-day line count.  `initial` walks every aisle with rates; `uniform` walks each\nclass once and multiplies by its replica count.",
      "is_async": false,
      "kind": "function",
      "signature": "(rates: SectionRates, geometry: Geometry, n: float, cv: float, one_way: bool) -> dict"
    },
    "Optimization/simconfig/expected_travel.py::solve_n": {
      "decorators": [],
      "doc": "The fixed point: the `n` lines a day at which the expected day exactly fills the\ncrew's capacity `K\u00b7S\u00b7\u03c1`.  `total_s(n)` is increasing in n, so the root is unique;\nBrent's method finds it to 1e-6 relative in ~10 evaluations.  A capacity the section\ncannot absorb even at `n_max` lines (every SKU every day) returns the day at `n_max`\nwith `saturated = True` -- the same clamp `staffing.batch_content` applies.",
      "is_async": false,
      "kind": "function",
      "signature": "(rates: SectionRates, geometry: Geometry, pick_cfg, cv: float, *, capacity_s: float, n_max: float) -> dict"
    },
    "Optimization/simconfig/expected_travel.py::solve_n.g": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(n)"
    },
    "Optimization/simconfig/fragmentation.py": {
      "decorators": [],
      "doc": "fragmentation.py -- the stationary bin fragmentation of a replenished SKU, in closed form.\n\nUnder the calibrated era every SKU on the line floor runs base stock at a lead of zero\n(`coverage.py`): a line of `q` serves `min(q, Q)` off a shelf holding `Q`, the order-up-to\nfires exactly what it took, and the lot lands before the next batch.  Three decided rules\nthen fragment the shelf across bins (ADR-0003; \".scratch/department-calibration\",\n\"Let a base-stock top-up reach the shelf\" and \"Band the own-bin share and the free-index\ndepth\"):\n\n  * a top-up lands in an EMPTY bin first (`Inventory_Manager._candidates_raw`), so a\n    partial line leaves a remnant AND opens a bin for the lot that replaces it;\n  * picks drain the SKU's SMALLEST bin first (`Workload_Builder.drain_sku`), which is what\n    clears a remnant and hands its bin back;\n  * the lot is packed by the planner's `stock_plan` -- its LEADING slots filled in order\n    (`Storage_Primitive.viable_storage_units`), so a partial lot of `r` is `r // per`\n    units of `per` and one of `r % per`, and a pallet of fewer items can sit in a smaller\n    size tier than the full one it replaces.\n\nSo each SKU is a Markov chain over the multiset of units on its shelf, driven by its own\nstamped line law (`Demand.line`).  This module derives the chain's STATIONARY distribution\nand its TRANSIENT from a fresh fielding, and sums the expected units per bucket (BinKey)\nover a section -- the closed form the record's `fielded.buckets[].expected_extra` stamps\nand the fill headroom is derived from (\"Derive the stationary fragmentation closed form\",\n\"Derive the fill headroom from the fragmentation\").\n\nTHE CHAIN.  A unit is `(q_now, single, n0)`: the items it holds now, and the kind it was\npacked as (singleton or pallet, and the quantity it was packed with -- the KIND decides\nthe size tier, which the bin keeps even as picks thin the unit).  A state is the sorted\ntuple of units; sorted ascending it IS the drain order.  With `S` the items on the shelf,\n`Q` the order-up-to and `rp` the reorder point, a line of `q`:\n\n  * `q < S`: drain `q` smallest-first; if the shelf is then at or below `rp`, the\n    position rule fires `Q - on_hand` and `packing(Q - on_hand)` lands in fresh bins\n    (base stock is `rp = Q - 1`: every line fires exactly what it took);\n  * `q >= S`: the shelf is emptied and refilled to the fielded state `f`, the remainder\n    re-offered next day against a full shelf again (nothing is lost, ADR-0004), so the\n    state after the whole line is `f` with the residual `(q - S) mod Q` drained and\n    repacked -- `staffing.fired_lots` prices the same line as lots of `Q` and one of the\n    residual.\n\nThe stationary law is `pi = pi P` over the states reachable from `f` (a sparse power\niteration on the lazy chain); the transient after `j` lines is the forward iterate from `f`.\nA SKU that sees `lambda_s` lines a day is at day `t` the Poisson(`lambda_s t`) mixture\nover `j`; `lambda_s` is `n * pi_s` with `pi_s` the line share `freq / sum freq` as\n`coverage.daily_demand` weights it, unless a caller hands a rate per SKU.\n\nWHAT IS AND IS NOT MODELLED (stated so the check's residual has a name):\n\n  * the supply jitter (`_fire_reorders`' `max(1, round(N(lot, lot * cv)))`) is not: the\n    lot is taken as exactly what the position rule ordered;\n  * a lead is taken as zero: the lot lands before the next line.  Every catalogue the\n    generator authors today has lead 0; with a lead the multiset a lot lands as is the\n    same, only its timing against the lines between is not, and the record names the\n    SKUs priced this way (`positive_lead_skus`);\n  * two lines for one SKU in one batch are aggregated by the sampler into one quantity;\n    the chain treats each line alone (on the reference pair a SKU sees a second line in the\n    same day on under 1% of its days);\n  * a location tie between units of EQUAL quantity and DIFFERENT kind is split uniformly\n    over the tied units: `drain_sku` breaks ties by `(aisle_id, bayX, bayY)`, and the built\n    layout interleaves every tier's aisles across the whole id range, so the tie carries\n    no information;\n  * the top-up always finds an empty bin of its own tier -- a spill or an own-bin top-up\n    is judged at zero by the equilibrium check, not modelled here.\n\nChecked (2026-09-10, `.scratch/department-calibration/assets/fragmentation-derivation.md`)\nagainst the two finished era runs on the reference pair: scored at each SKU's realized\nline count the chain reproduces both leaves' bin change over 25 days to -3%, the one-line\nmean to a third of a percent; the store's 39-day drawdown at the declared line share to\n-4%; the fulfillment section's transient over-reads because the batch sampler's affinity\nlift spreads its lines over fewer SKUs than the line share says (the seam below).\n\nTHIS MODULE IS PURE: orders with a declared level and a `stock_plan` in, floats out.  It\nimports no CONFIG and touches no file.  The size tier of a kind is read off the packer's\nown unit classes (`Pallet`, `Singleton`, `FulfillmentBin`) through `binkey_of`, so the\nbucket a unit lands in is the one the sim would build for it.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/fragmentation.py::Kind": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple[bool, int]"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain": {
      "decorators": [
        "dataclass"
      ],
      "doc": "The fragmentation chain of one SKU CLASS -- every SKU sharing a line law, a\n`stock_plan` and a reorder point shares it, whatever its size tier.\n\n`kinds` is the fixed list of unit kinds the plan can ever produce (every `packing(r)`\nfor `r = 1..Q`); `stationary_units[k]` the expected units of kind `k` on the shelf\nunder the stationary law, `trajectory_units[j, k]` the same after exactly `j` lines\nfrom the fielded state, `fielded_units[k]` what the plan fields.  `stationary_bins`\nis the expected bin count, `bins_law` its distribution `{bins: p}`, `rho` the share\nof lines that empty a full shelf (`P(q >= Q)`), `states` the reachable state count.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/fragmentation.py::SkuChain.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._direct": {
      "decorators": [
        "staticmethod"
      ],
      "doc": "`(I - P^T) pi = 0` with `pi[pin] = 1`, the pinned row and column dropped, then\nnormalised; None when the solve fails or does not return a distribution.",
      "is_async": false,
      "kind": "function",
      "signature": "(PT, n: int, pin: int) -> np.ndarray | None"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._kernel": {
      "decorators": [],
      "doc": "`{state: prob}` after one line at `st`, over the whole line law.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, st: State) -> dict"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._landing": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, m: int) -> dict"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._partial": {
      "decorators": [],
      "doc": "`{state: weight}` after a line of `d` items that leaves stock on the shelf.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, st: State, d: int) -> dict"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._solve": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._stationary": {
      "decorators": [],
      "doc": "`pi = pi P` by power iteration on the LAZY chain `(P + I) / 2` from the fielded\nstate -- the same stationary law, and aperiodic whatever cycle the drain runs in.\nThe chain regenerates whenever a line empties the shelf, so the reachable set holds\none recurrent class and the iterate converges geometrically; it stops when the\nL1 step is below `POWER_TOL`.  A chain still moving at `POWER_SWITCH` steps is a\nslow mixer and goes to the pinned direct solve (`_direct`); `solver` records which\npath answered, `iterations` how many steps it took.\n\nA direct solve with a normalisation ROW was tried first and dropped: the dense row\nfills the LU factors in, and a 9,000-state class cost four seconds against a\ntwentieth of that by iteration; the pinned form keeps the system sparse.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, PT, n: int) -> np.ndarray"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain._units": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, st: State) -> np.ndarray"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain.stationary_extra": {
      "decorators": [
        "property"
      ],
      "doc": "Expected bins beyond the fielding, in steady state.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain.units_at_days": {
      "decorators": [],
      "doc": "Expected units by kind at each of `days` for a SKU seeing `lines_per_day`\nlines a day: the Poisson mixture over the lines count, the tail past `j_max`\nat the stationary law.  Returns `(len(days), n_kinds)`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, lines_per_day: float, days) -> np.ndarray"
    },
    "Optimization/simconfig/fragmentation.py::SkuChain.units_at_lines": {
      "decorators": [],
      "doc": "Expected units by kind after exactly `j` lines (stationary past `j_max`).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, j: int) -> np.ndarray"
    },
    "Optimization/simconfig/fragmentation.py::State": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple[Unit, ...]"
    },
    "Optimization/simconfig/fragmentation.py::Unit": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple[int, bool, int]"
    },
    "Optimization/simconfig/fragmentation.py::_LGAMMA": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "np.array([math.lgamma(j + 1.0) for j in range(J_MAX + 1)])"
    },
    "Optimization/simconfig/fragmentation.py::_add": {
      "decorators": [],
      "doc": "The state after the lot's units land, each in its own fresh bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(state: State, kinds: tuple[Kind, ...]) -> State"
    },
    "Optimization/simconfig/fragmentation.py::bucket_of": {
      "decorators": [],
      "doc": "The BinKey a unit of `kind` lands in for this SKU, cached on `tier_prefix` + kind\n(pass `prefix` when calling for several kinds of one order).",
      "is_async": false,
      "kind": "function",
      "signature": "(order, kind: Kind, cache: dict | None=None, prefix: tuple | None=None) -> BinKey"
    },
    "Optimization/simconfig/fragmentation.py::class_key": {
      "decorators": [],
      "doc": "What a SKU's chain depends on: regime, line law, plan and reorder point.  Two SKUs\nwith the same key share one chain; their size tiers may still differ (that is a\nper-SKU lookup).",
      "is_async": false,
      "kind": "function",
      "signature": "(order) -> tuple"
    },
    "Optimization/simconfig/fragmentation.py::drain_all": {
      "decorators": [],
      "doc": "`{d: {state: weight}}` for every `d = 1..d_max`: the shelf after `d` items are picked\nsmallest-first, in ONE pass (the drain of `d + 1` is the drain of `d` and one more).\n\nA tie between units of equal quantity and different kind is split uniformly over the\ntied UNITS (the layout's location order is uninformative across tiers -- module\ndocstring -- so each tied unit is equally likely to come first); branches are merged\nafter every whole unit, so a shelf of many tied units costs its distinct multisets,\nnot its orderings.  A `d` no unit can satisfy yields nothing for that `d` (the caller\nonly asks below the shelf's total).",
      "is_async": false,
      "kind": "function",
      "signature": "(state: State, d_max: int) -> dict"
    },
    "Optimization/simconfig/fragmentation.py::line_pmf": {
      "decorators": [],
      "doc": "`P(q = k)` for `k = 0..q_max` off the stamped law's `cdf` (zero at k = 0: a line\ndemands at least one unit).  `q_max` defaults to the law's `1 - LINE_TAIL` quantile;\nthe truncated tail is renormalised away.",
      "is_async": false,
      "kind": "function",
      "signature": "(line, q_max: int | None=None) -> np.ndarray"
    },
    "Optimization/simconfig/fragmentation.py::packing": {
      "decorators": [],
      "doc": "The units a lot of `r` items packs into: the plan's LEADING slots filled in order,\n`take = min(per, remaining)` per unit -- `viable_storage_units`' plan branch, for a\nlot at or below the plan's total (a lot never exceeds the order-up-to).  Returns the\nkinds `(single, n0)` in packing order; `r <= 0` packs nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(plan: tuple, r: int) -> tuple[Kind, ...]"
    },
    "Optimization/simconfig/fragmentation.py::plan_slots": {
      "decorators": [],
      "doc": "The order's `stock_plan` as `((single, per, count), ...)`, refusing an order that\ncarries none: under the one planner contract every fielded SKU has one (\"Field the\nrequirement\"), and the packer's plan-less branch packs by volume, which this chain\ndoes not reproduce.",
      "is_async": false,
      "kind": "function",
      "signature": "(order) -> tuple[tuple[bool, int, int], ...]"
    },
    "Optimization/simconfig/fragmentation.py::plan_total": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(plan: tuple) -> int"
    },
    "Optimization/simconfig/fragmentation.py::poisson_weights": {
      "decorators": [],
      "doc": "`P(J = j)` for `j < j_max` and `P(J >= j_max)` in the last column, for each Poisson\nmean in `mean` (shape `(len(mean), j_max + 1)`).",
      "is_async": false,
      "kind": "function",
      "signature": "(mean, j_max: int) -> np.ndarray"
    },
    "Optimization/simconfig/fragmentation.py::section_fragmentation": {
      "decorators": [],
      "doc": "The expected extra units per bucket over one channel section: the closed form the\nrecord stamps.\n\n    {'buckets': {BinKey: {'fielded': int, 'expected_units': float,\n                          'expected_extra': float,\n                          'expected_extra_at': [per day in `days`]}},\n     'expected_extra': float,          # the section sum, stationary\n     'expected_extra_at': [...],       # the section sum per day in `days`\n     'n_skus', 'n_classes', 'capped_classes', 'positive_lead_skus',\n     'stationary_bins_per_sku', 'fielded_bins_per_sku',\n     'chains': {class_key: SkuChain}}\n\n`expected_extra` is `expected_units - fielded` and can be NEGATIVE for a bucket: a\npartial pallet packs into a smaller tier than the full one it replaces, so a plan's\nsingleton or small-tier remainder migrates down a tier under churn (the reference\nstore's `singleton` bucket empties toward `small`).  The section sum is what the\nshelf needs beyond its declaration.\n\n`days` (with `lines_per_day`, the section's declared line count) adds the transient:\nthe expected extra at each day after a fresh fielding, the line share per SKU being\n`freq / sum freq` as `coverage.daily_demand` weights it -- or, when\n`lines_per_day_by_sku` is given, that map's rate per SKU (a SKU absent from it sees\nno line).  The seam exists because the stationary law needs no line rate while the\ntransient is only as good as its rate: the batch sampler's affinity lift spreads a\nsection's lines over SKUs differently from the line share (the fulfillment section\nof the reference pair touches about a quarter fewer SKUs by day 25 than the share\npredicts), and a trajectory band would read the sampler's own rate through this\nseam rather than the share.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, *, lines_per_day: float | None=None, days=(), lines_per_day_by_sku: dict | None=None, j_max: int=J_MAX) -> dict"
    },
    "Optimization/simconfig/fragmentation.py::section_fragmentation.grow": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(arrs, n)"
    },
    "Optimization/simconfig/fragmentation.py::tier_prefix": {
      "decorators": [],
      "doc": "What decides the bucket of every kind of this SKU: regime, handling, category and\ndimensions -- the cache key `bucket_of` completes with the kind.",
      "is_async": false,
      "kind": "function",
      "signature": "(order) -> tuple"
    },
    "Optimization/simconfig/fragmentation.py::unit_for": {
      "decorators": [],
      "doc": "The storage unit the packer builds for `n0` items of this SKU packed as `single`:\nits own classes, so the size tier is the sim's.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, single: bool, n0: int)"
    },
    "Optimization/simconfig/staffing.py": {
      "decorators": [],
      "doc": "staffing.py \u2014 the calibrated era's staffing DERIVATION, as a pure module.\n\nDEMAND per channel is the declared staffing input of the calibrated era (ADR-0004,\n.scratch/department-calibration, \"Fit the store's window to its own steady state\" --\nreversing \"Define the calibrated era\", which declared the pickers): a mean day is a\ndeclared fraction of the section's SKUs drawn as lines, and everything a run fields is\nDERIVED from that and ONE more declared scalar, the joint FIRST-TIME CONFIDENCE `c` -- the\nprobability a pick is completed the first time, reached on its day AND filled from its\nshelf -- live at setup:\n\n    lines per day      n = demand \u00d7 N_skus                     (the declared day, mean)\n    day law            N ~ Normal(n, cv\u00b7n), cv the declared batch spread   (declared)\n    line floor         the smallest floor_lines whose first-pass fill >= sqrt(c)\n                                                              (`coverage.solve_floor_lines`)\n    pick load          E[W] = demanded units/day \u00d7 s_pick      (DEMANDED, never served)\n    picking crew       the smallest integer K with E[(W - K\u00b7S)^+] / E[W] <= 1 - sqrt(c)\n                                                              (`solve_pickers`, per channel)\n    batch content      one batch = one day's demand          (a BatchConfig mean fraction)\n    put load           units \u00d7 f_put                         (units put per unit picked)\n    put crew           ceil(\u03a3_ch put load_ch \u00d7 s_put_ch \u00f7 (S \u00d7 \u03c1_put))   (ONE site crew)\n    receive load       packs the script's LOTS pack into \u00d7 f_recv\n    receiving crew     ceil(receive seconds \u00f7 (S \u00d7 \u03c1_recv))  (ONE site crew)\n\nNOTHING IS LOST under the era (memory `nothing-is-lost-under-the-era`): a pick the day cut\nor the shelf could not fill is re-offered next day and a lead-0 top-up lands before the\nre-attempt, so the crew picks ALL of demand and is sized on DEMANDED units.  The previous\nderivation priced pickers on SERVED units (demand \u00d7 fill rate) and under-staffed both\nchannels by exactly that factor; `test_first_time_guarantee` pins the sabotage.  Put-away\nand receiving keep their declared utilization targets: a receiving backlog on a heavy\ntrailer is a legitimate scenario the inbound campaign exists to study.\n\n`s_pick` and `s_put` are BOTH per channel, and both are seconds per unit: CLOSED-FORM\nEXPECTATIONS over the catalogue's demand distribution and the warehouse geometry the run\nbuilt (`Optimization/simconfig/expected_travel.py`, \"Derive the expected-travel closed form\").\nThere are no calibration simulations and no calibration record.  The harness computes them\nat setup and hands them in as resolved constants (a declared `--s-pick-*` / `--s-put`\noverride replaces the expectation and is recorded `declared`).  `s_recv` is EXACT from the\nscript: an unload has no travel term, so every pack's seconds are computable from the\ncatalogue and the cost model alone.\n\n**s_put IS PER CHANNEL** (\"Give put-away a per-channel expected travel\", 2026-09-08).  It was\none site value until then, and the two sections do not share a geometry: the era re-read\nmeasured 98.5 s/unit put away on the store against 29.2 on fulfillment, a 3.4\u00d7 spread that a\nsingle site price averaged away.  The site TOTAL stayed right -- so the crew was correctly\nSIZED -- while both leaves' expected utilization sat ~0.21 outside the band, in opposite\ndirections.  Picking never had this defect (its constant was always per channel) and neither\ndid receiving (its per-channel seconds come straight from the script).  There is now no\nsite-wide put price anywhere in the derived record: `put.s_put` is a map keyed by channel.\n\nTHIS MODULE IS PURE.  Inputs, the resolved constants and the catalogue / script totals go\nin; the derived dict comes out.  It imports no CONFIG, reads no settings and\ntouches no file, so the arithmetic is testable against a hand computation without a run\n(`Tests/unit/test_staffing_derivation.py`).  The harness seam that feeds it is\n`Optimization/simdriver/workunits._derive_staffing_for_pair`, which runs AFTER batch\nprecompute because the receiving crew needs the packs the script implies -- and the script\nis itself derived from the pickers, which is why the derivation has two stages:\n\n    stage A  (the catalogue and the geometry)  s_pick at the declared day, the picking crew\n                               (solved), batch content -- BEFORE precompute\n    stage B  (the script)      put load, packs, the two site crews -- AFTER precompute\n\nEvery scalar default below is a declared ASSUMPTION of the era, never a measurement, and the\nrecord says so (`PROVENANCE`, \"Design the staffing record\", decision 5).  The derived block\ncarries `derived`; a copied constant keeps the provenance it arrived with.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simconfig/staffing.py::PIN_SIGFIGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "6"
    },
    "Optimization/simconfig/staffing.py::PricingConfig": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "The handling coefficients one channel's analytic prediction is priced with.\n\nThe subset of a `PickConfig` the script-only prediction needs -- no speeds, no height\nbrackets, because the prediction is at GROUND height with no travel term (the travel\nshare is what a reference run adds).  `from_pick_config` lifts them off the channel's\ncanonical pick config; `put()` / `recv()` derive the other two crews' prices by the\nsame chain the runner uses (`PutawayCost.from_pick`, `UnloadCost.from_putaway`).",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/staffing.py::PricingConfig.from_pick_config": {
      "decorators": [
        "classmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cls, cfg, name: str='') -> 'PricingConfig'"
    },
    "Optimization/simconfig/staffing.py::PricingConfig.put": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, intercept_scale: float, item_ratio: float) -> PutawayCost"
    },
    "Optimization/simconfig/staffing.py::PricingConfig.recv": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, put_intercept_scale: float, put_item_ratio: float, recv_intercept_scale: float) -> UnloadCost"
    },
    "Optimization/simconfig/staffing.py::PricingConfig.var": {
      "decorators": [],
      "doc": "The per-unit weight/volume handling term for `order` under these coefficients.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, order) -> float"
    },
    "Optimization/simconfig/staffing.py::ScriptTotals": {
      "decorators": [
        "dataclass"
      ],
      "doc": "What one channel's precomputed batch script demands, summed over its batches.\n\n`units` / `lines` are the demand itself; `analytic_pick_s` is the same script priced\nwith `analytic_pick`'s formula line by line (so the record can hold the script-only\nprediction NEXT to the measured value, and their ratio is the travel share).  The\nput and receiving totals come from `implied_reorders`: `lots` the reorders the\nscript's lines fire, `put_units` the merchandise units put away (= units received),\n`put_s` their put-away seconds, `packs` the storage units the lots pack into, `recv_s`\ntheir EXACT unload seconds.  `unknown_skus` counts script lines whose SKU the catalogue\nmap did not hold -- zero on a healthy run, and reported rather than silently skipped.\n`per_sku_lines` keeps every line's quantity per SKU IN SCRIPT ORDER, because the\norder-up-to rule's lots depend on the sequence (`fired_lots`).",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simconfig/staffing.py::ScriptTotals.per_day": {
      "decorators": [],
      "doc": "A total as a per-DAY quantity under the era's one batch per day.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, value: float) -> float"
    },
    "Optimization/simconfig/staffing.py::_Phi": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(z: float) -> float"
    },
    "Optimization/simconfig/staffing.py::_levels": {
      "decorators": [],
      "doc": "`(Q, rp, P)` -- the order-up-to target, the reorder point and the pipeline allowance\n-- as `inventory_reorder._fire_reorders` reads them off the SKU's DECLARATION.\n\nThe declaration, not a default: `equilibrium_qty -> 1` / `reorder_point -> 0` sent an\nundeclared order down the base-stock branch and returned a one-line lot, so the era's\nput-away and receiving crews would have been sized off a lot no SKU was ever declared to\norder.  The one caller (`implied_reorders`) runs after the fixed point has declared, so an\nundeclared order here is a bug in the caller.  `stock_qty` stays: it is the documented\nduck-typed legacy contract.  The pipeline is `Order.pipeline_allowance()` (the era's\nstamp, else the `round(rp \u00d7 lead \u00f7 (lead + 1))` heuristic) -- a duck-typed order without\none takes the heuristic.",
      "is_async": false,
      "kind": "function",
      "signature": "(order) -> tuple[int, int, int]"
    },
    "Optimization/simconfig/staffing.py::_phi": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(z: float) -> float"
    },
    "Optimization/simconfig/staffing.py::_round_sig": {
      "decorators": [],
      "doc": "`x` to `sig` significant figures; 0.0 and non-finite values pass through unchanged.",
      "is_async": false,
      "kind": "function",
      "signature": "(x: float, sig: int=PIN_SIGFIGS) -> float"
    },
    "Optimization/simconfig/staffing.py::analytic_pick": {
      "decorators": [],
      "doc": "The script-only prediction of a channel's picking labour per UNIT, from the catalogue.\n\nWeighted the way the sampler weights: a SKU enters a batch in proportion to its\n`relative_frequency` (affinity lift ignored -- it redistributes lines, it does not add\nthem), and a line demands the MEAN of the SKU's stamped line law (`Demand.line.mean()`).\nBefore the stamp this read `max(1, quantity_rate)` -- a drift from the `\u03bb + e^-\u03bb` the\nsampler and the expectation use, corrected by \"Stamp the line distribution on the SKU\":\nthe recorded `analytic` block moves by the `e^-\u03bb` term; the era's batch content never read\nit (it reads `expected_travel`'s `units_per_line`).  One intercept per LINE\n(one bin visit for one SKU) is the line-count floor (\"Choose the calibration\nprocedure\", decision 7); the per-item charge and `qty \u00d7 handle_var` are per unit; the\nheight multiplier is M(0) = 1 (ground) and there is no travel term.  Returns\n\n    {'seconds_per_unit', 'units_per_line', 'n_skus', 'pricing_config', 'line_families'}\n\n`seconds_per_unit` is what a travel share scales into a seeded `s_pick`.  Empty\norders yield zeros rather than raising: a store-only catalogue has no fulfillment\nsection, and the caller records that as an absent channel.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, pricing: PricingConfig) -> dict"
    },
    "Optimization/simconfig/staffing.py::batch_content": {
      "decorators": [],
      "doc": "One batch = one day's demand, as the sampler's (mean_fraction, std_fraction).\n\nThe sampler draws a LINE count ~ N(mean\u00b7N, std\u00b7N) and each line demands its stamped\nlaw's mean units (`units_per_line`), so the mean fraction that delivers `demand_units` is\n`(demand \u00f7 units_per_line) \u00f7 N`.  The spread keeps the DECLARED coefficient of\nvariation (the flag-off `std \u00f7 mean`, 1/3 for the store, 1/4 for fulfillment) so a\nderived script has the same relative day-to-day variability the archive's did.  A\nmean above 1.0 -- every SKU every day, and then some -- is CLAMPED and flagged: the\nsampler cannot draw more lines than SKUs, and the declared crew is then oversized for\nthe catalogue (the verifier will show the idle capacity; `saturated` says why).",
      "is_async": false,
      "kind": "function",
      "signature": "(demand_units: float, units_per_line: float, n_skus: int, declared_mean: float, declared_std: float) -> dict"
    },
    "Optimization/simconfig/staffing.py::channel_crew": {
      "decorators": [],
      "doc": "This channel's picking crew off a staffing record -- THE ONE READER (ADR-0004).\n\nPrefers the DERIVED block (`derived.channels[<channel>].pickers`: the solved crew under\nthe era, the declared one restated on an older era record), and falls back to the\ndeclared input (`inputs.<store|ff>_pickers`, flag-off).  Two record shapes are read:\nthe run spec / `sim_result` shape, where `derived` is keyed by PAIR (pass `pair`), and\nthe worker payload, where it is the pair's block itself; a pre-derivation payload is the\ninputs dict alone.  Raises `KeyError` naming the channel when neither holds a crew,\nbecause a reader that fell through to a literal here is the store-crew-on-every-\nfulfillment-leaf trap (\"Build the picker staffing seam\").",
      "is_async": false,
      "kind": "function",
      "signature": "(record: dict, *, channel: str | None, pair: str | None=None) -> int"
    },
    "Optimization/simconfig/staffing.py::constant": {
      "decorators": [],
      "doc": "One recorded constant: `{value, provenance, ...}`.  Provenance must be one of the\nfive agreed values; a copied constant keeps the one it arrived with.",
      "is_async": false,
      "kind": "function",
      "signature": "(value: float, provenance: str, **extra) -> dict"
    },
    "Optimization/simconfig/staffing.py::crew_size": {
      "decorators": [],
      "doc": "ceil(load \u00f7 (S \u00d7 \u03c1)), floored at 1 when there is load and 0 when there is none.\n\nA department with work to do fields at least one person even when the arithmetic says\n0.3; a department with NO work (a store-only catalogue's absent channel contributes\nnothing, but the site crew still forms from the other) fields nobody only when the\nwhole site has nothing for it.",
      "is_async": false,
      "kind": "function",
      "signature": "(load_seconds_per_day: float, day_seconds: float, rho: float) -> int"
    },
    "Optimization/simconfig/staffing.py::cut_share": {
      "decorators": [],
      "doc": "The expected share of a day's work the cut leaves standing under a crew of `pickers`:\n`E[(W - K\u00b7S)^+] / E[W]`, `W ~ Normal(load_s, sd_s)`.  The per-PICK guarantee: units\ncut over units demanded, in expectation over the declared day law.  Zero when there\nis no load.",
      "is_async": false,
      "kind": "function",
      "signature": "(load_s: float, sd_s: float, pickers: int, day_seconds: float) -> float"
    },
    "Optimization/simconfig/staffing.py::cut_share_sd": {
      "decorators": [],
      "doc": "The standard deviation of ONE DAY's cut share under the declared law -- the spread\n`cut_share` is the mean of, so a measured window can be read against the stamped\nexpectation with a tolerance the law implies rather than one somebody typed\n(\"Split the missed-share clause into supply and labour\", the labour clause).\n\nFor `W ~ Normal(mean, sd)` and `X = (W - C)^+` at `C = K\u00b7S`:\n\n    E[X\u00b2] = sd\u00b2\u00b7[(1 + z\u00b2)(1 - \u03a6(z)) - z\u00b7\u03c6(z)]        z = (C - mean) / sd\n\n(integrate `(t - z)\u00b2` against \u03c6 from z: `\u222bt\u00b2\u03c6 = (1 - \u03a6) + z\u03c6`, `\u222bt\u03c6 = \u03c6`, `\u222b\u03c6 = 1 - \u03a6`),\nand the share's sd is `sqrt(E[X\u00b2] - E[X]\u00b2) / E[W]`.  A window of `n` iid days then has\na sampling sd of `cut_share_sd / sqrt(n)` on its mean share; the clause's band is a\ndeclared multiple of that.  Zero at `sd = 0` (the day is exactly the mean, the share is\nexact) and with no load.",
      "is_async": false,
      "kind": "function",
      "signature": "(load_s: float, sd_s: float, pickers: int, day_seconds: float) -> float"
    },
    "Optimization/simconfig/staffing.py::daily_demand": {
      "decorators": [],
      "doc": "Units per day the channel's capacity buys at `s_pick` seconds per unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(capacity_s: float, s_pick: float) -> float"
    },
    "Optimization/simconfig/staffing.py::derive": {
      "decorators": [],
      "doc": "The derived block, from declared inputs + resolved constants + the script.\n\n`inputs` is the staffing record's INPUTS (`sim_config.staffing_spec()` shape:\n`rho_put/rho_recv`, `f_put/f_recv`, `band_tol`, `put_crew_mode`, plus the three\ncrew-cost scales the harness copies in; `rho_pick` is NOT read here since ADR-0004).\n`constants` is `{'s_pick': {channel: constant}, 's_put': {channel: constant}}` as the\nharness resolves them (the expectation, or a declared override).  BOTH are keyed by\nchannel: a site-wide put price is what this derivation used to get wrong, and there is\nno longer anywhere to put one.  `channels` maps a channel name to its stage-A dict:\n`pickers` (the crew stage A SOLVED from the first-time confidence -- `solve_pickers`),\n`daily_demand_units` (the declared day's DEMANDED units), `analytic`, `batch`,\n`n_skus`, `expected` (the expected day at the declared line count, recorded beside the\nconstant), and optionally `demand` and `guarantee` (the declaration and the solve's\nstamp, carried through verbatim); `scripts` maps it to its `ScriptTotals`.  Channels\nabsent from `channels` (a store-only catalogue) are recorded as absent and contribute\nnothing.\n\nTHE PICKING LOAD IS THE SAMPLED SCRIPT'S DEMANDED UNITS at `s_pick` (\"Fit the store's\nwindow to its own steady state\", decision 5: the derivation prices the actual script,\nthe declared law is what the guarantee was made against, both are stamped and named\napart).  Its expected utilization is that load against the granted day `K \u00d7 S` -- a\nDERIVED number near 0.72 on the reference store, never a target.\n\nThe put and receiving crews are SITE totals: one crew each, summing both channels'\nper-day loads.  Each channel leaf's expected utilization for those two departments is\nits own load against the WHOLE site crew, which is why it sits well below \u03c1.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, inputs: dict, constants: dict, day_seconds: float, channels: dict, scripts: dict, pricing_names: dict) -> dict"
    },
    "Optimization/simconfig/staffing.py::derived_differs": {
      "decorators": [],
      "doc": "The paths at which two derived blocks disagree, for the restore drift check.\n\nFloats compare with a tolerance, never `==` (the repo rule); everything else exactly.\nAn empty list means \"the same derivation\".  Used on resume (raise) and on re-analysis\n(warn and stamp) -- \"Design the staffing record\", decision 3.",
      "is_async": false,
      "kind": "function",
      "signature": "(a: dict | None, b: dict | None, *, rel_tol: float=1e-09) -> list[str]"
    },
    "Optimization/simconfig/staffing.py::derived_differs._walk": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(x, y, path)"
    },
    "Optimization/simconfig/staffing.py::expected_utilization": {
      "decorators": [],
      "doc": "worked \u00f7 granted for one department on one leaf: load \u00d7 s \u00f7 (crew \u00d7 S).\n\nAFTER `ceil` and AFTER the leaf's share of the site crew (\"Declare the equilibrium\nbands\", decision 3): integer crews and single-channel leaves undercut \u03c1 by\nconstruction, so the band is drawn around THIS number, never around \u03c1.",
      "is_async": false,
      "kind": "function",
      "signature": "(load_seconds_per_day: float, crew: int, day_seconds: float) -> float"
    },
    "Optimization/simconfig/staffing.py::fired_lots": {
      "decorators": [],
      "doc": "The lots the order-up-to rule fires while `quantities` -- one script's lines for this\nSKU, in script order -- are picked against its declared levels: `{lot: expected count}`.\n\nReplaces the one ROUNDED lot this derivation used to pack (\"Close the put closed form's\nthree known gaps\"): every pack-proportional term -- the whole put travel term, the put\nintercept, the receiving intercept -- is per PACK, and the packer packs whole units, so\npricing one rounded lot and scaling it by a fractional reorder count under-counted packs\nby 7% on the store and 3% on fulfillment.  Two regimes, exactly as\n`inventory_reorder._fire_reorders` fires them:\n\n  * BASE STOCK (`rp >= Q - 1`; every SKU on the line floor, \"Choose the coverage\n    floor\"): the shelf holds `Q` at the start of every day (a lead-0 top-up lands before\n    the next batch), so a line of `q` units SERVES `min(q, Q)`, fires a lot of exactly\n    what it took, and re-offers the remainder next day against a full shelf again\n    (`unpicked_unstocked`, \"Fit the store's window to its own steady state\": nothing is\n    lost).  A line is therefore `q // Q` lots of `Q` and one of `q % Q` -- the reference\n    pair fires 1.3 lots per line.  With a pipeline `P > 0` a lot below `P + 1` does not\n    fire alone (the position only drops to the trigger once `P + 1` units are gone), so\n    such a lot is priced as `P + 1` at a count that conserves units -- a FLOOR on the\n    pack count, exact at `P = 0`, which is every catalogue the generator authors today.\n  * ABOVE THE FLOOR: the expected lot is the position at the reorder point,\n    `Q + P - rp`, and the reorders are `\u03a3q \u00f7 lot`, kept FRACTIONAL (a steady-state\n    expectation, not this window's transient -- a 40-day script would fire nothing for\n    a SKU whose demand never crosses `Q - rp`).  The lot is handed on as a fraction and\n    `received_law` prices it between its two integer neighbours, so the rounding bias\n    is gone here too.  The crossing line's overshoot (about half a mean line) is not\n    modelled; no fielded catalogue has an above-floor SKU to measure it on.\n\nThe lot is what the ledger ORDERS; what arrives is `received_law`'s business.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, quantities) -> dict"
    },
    "Optimization/simconfig/staffing.py::first_time_split": {
      "decorators": [],
      "doc": "Each side's share of the joint first-time confidence: `sqrt(c)`, split EQUALLY\nbetween the shelf (first-pass fill >= sqrt(c)) and the crew (expected cut share of\nunits <= 1 - sqrt(c)) -- ADR-0004, the one declared scalar and nothing else authored.\nRefuses a confidence outside (0, 1): 0 promises nothing and 1 is staffing to the\npeak by another name.",
      "is_async": false,
      "kind": "function",
      "signature": "(confidence: float) -> float"
    },
    "Optimization/simconfig/staffing.py::implied_reorders": {
      "decorators": [],
      "doc": "Stage B's supply side: the put-away and receiving work the script implies.\n\nSteady state (f = 1.0) replenishes every unit picked.  Each SKU's script lines fire the\nlots `fired_lots` says they do, each lot arrives as `received_law` says it does, and\neach arrived quantity packs into the storage units the sim's own packer would produce\n(`Inbound.pack.receive` -> `viable_storage_units`, honouring a `stock_plan`).  Each\npack costs\n\n    put-away   travel + M(y) \u00b7 (put_intercept + qty \u00b7 put_per_item + qty \u00b7 var)\n    receiving  recv_intercept + recv_per_item + qty \u00b7 var                (EXACT)\n\n`put_site`, when given, is `(unit) -> (travel_s, height_mult)` -- the expected travel\nfrom the aisle mouth and the height multiplier of the pack's destination under the run's\nplacement distribution (`expected_travel.put_site_pricer`).  None prices every put at\nthe ground with no travel (the script-only prediction).\n\nThe packer is asked once per (SKU, arrived quantity) and the answer cached: a floored\nSKU's lots never exceed its level plus the jitter, so the distinct questions per SKU are\nfew.  There is NO cart-swap term: the era runs one uncarted put queue at\n`swap_coef = 0` and the harness REFUSES anything else (`run_simulation._check_era_flags`,\n`workunits.refuse_unpriceable_put`) rather than under-price it here.  `f_put` scales the\nput load and `f_recv` the receiving load (\"units put per unit picked\" / packs received\nper pack demanded); at 1.0 both are the steady state, any other value models a growing\nor shrinking warehouse, which is exactly why they are knobs.  Mutates and returns\n`totals`.",
      "is_async": false,
      "kind": "function",
      "signature": "(totals: ScriptTotals, orders_by_sku: dict, pricing: PricingConfig, *, f_put: float, f_recv: float, put_intercept_scale: float, put_item_ratio: float, recv_intercept_scale: float, put_site=None) -> ScriptTotals"
    },
    "Optimization/simconfig/staffing.py::line_moments": {
      "decorators": [],
      "doc": "The first two moments of ONE line's units over a section, weighted the way the\nsampler weights (a SKU's line share `\u03c0_s = freq / \u03a3 freq`), off each SKU's STAMPED\nline law: `{'m1': E[q], 'm2': E[q\u00b2], 'var': Var[q], 'n_skus'}`.\n\n`E[q\u00b2] = \u03a3_j (2j + 1) P(q > j)` from `line.survival`, summed to the law's 1 - 1e-12\nquantile (the tail past it is below float noise on a Poisson).  Zeros on an empty\nsection or a zero weight sum.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders) -> dict"
    },
    "Optimization/simconfig/staffing.py::partial_expectation": {
      "decorators": [],
      "doc": "`E[(W - cap)^+]` for `W ~ Normal(mean, sd)`: `sd\u00b7[\u03c6(z) - z\u00b7(1 - \u03a6(z))]` at\n`z = (cap - mean) / sd` -- the expected overflow of a day's work past the crew's cap.\nDegenerates to `max(0, mean - cap)` at `sd = 0`.",
      "is_async": false,
      "kind": "function",
      "signature": "(mean: float, sd: float, cap: float) -> float"
    },
    "Optimization/simconfig/staffing.py::pick_capacity": {
      "decorators": [],
      "doc": "Seconds of picking one channel's crew is expected to deliver per day: K \u00d7 S \u00d7 \u03c1.\n\nFLAG-OFF ONLY since ADR-0004: the era declares demand and solves the crew from the\nfirst-time confidence (`solve_pickers`), and its capacity is the granted day `K \u00d7 S`\nwith the headroom a consequence rather than a target.",
      "is_async": false,
      "kind": "function",
      "signature": "(pickers: int, day_seconds: float, rho_pick: float) -> float"
    },
    "Optimization/simconfig/staffing.py::picker_key": {
      "decorators": [],
      "doc": "The staffing-inputs key holding this channel's DECLARED picker count (flag-off).",
      "is_async": false,
      "kind": "function",
      "signature": "(channel: str | None) -> str"
    },
    "Optimization/simconfig/staffing.py::pin_digest": {
      "decorators": [],
      "doc": "A 12-hex identity for `pin_of(derived)` -- the one string a campaign spec carries.\n\nThe projection itself is recorded on `restock_selection.json` so a refusal can be\nDIAGNOSED (which crew moved, which price), and only this travels into the spec: a pin the\nsize of the projection would be twenty hand-copied floats in a tracked file, and a typo in\none of them is indistinguishable from the drift it exists to catch.",
      "is_async": false,
      "kind": "function",
      "signature": "(derived: dict) -> str"
    },
    "Optimization/simconfig/staffing.py::pin_of": {
      "decorators": [],
      "doc": "The part of a derivation a CAMPAIGN pins across phases: the crews it fields, the day's\ndemand, the two prices and the script depth, rounded to `PIN_SIGFIGS`.\n\nNOT the whole derived block, on purpose.  `derived_differs` compares two derivations of\nthe SAME run and rightly fails on anything; this answers a different question -- \"is phase\n2 running the warehouse phase 1 ranked?\" -- and what decides that is what a run FIELDS and\nwhat its script contains.  Reporting-only keys (`expected_utilization`, `analytic`,\n`guarantee`) move when the reporting moves and would refuse a campaign over a comment.\n\n`script.batches` is in the projection because the derivation reads the sampled script: two\nphases at different depths derive different per-day loads from the same catalogue, which\nis the drift a depth typed on a command line produces.  `CAMPAIGN_DEPTH_DAYS` is what\nstops it; this is what NOTICES.",
      "is_async": false,
      "kind": "function",
      "signature": "(derived: dict) -> dict"
    },
    "Optimization/simconfig/staffing.py::received_law": {
      "decorators": [],
      "doc": "What arrives for a lot the ledger ordered: `{quantity: probability}`.\n\n`_fire_reorders` receives `max(1, round(N(lot, lot \u00d7 cv)))` when the SKU carries a\n`supply_cv`, the lot itself otherwise.  The Gaussian is discretised on the unit cells\n(`r - \u00bd, r + \u00bd`], everything below 1\u00bd arriving as one; \u00b15\u03c3 covers the tail to 1e-6.\nA FRACTIONAL lot with no jitter (the above-floor expectation) is the mixture of its two\ninteger neighbours weighted by its fractional part, so the packer -- which packs whole\nunits -- is asked two whole questions instead of one rounded one.  Always sums to one;\na lot at or below one arrives as one unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(lot: float, supply_cv: float) -> dict"
    },
    "Optimization/simconfig/staffing.py::regime_orders": {
      "decorators": [],
      "doc": "The catalogue section one channel picks from: `regime_of(order) == regime`, or the\nwhole catalogue when `regime` is None (the store-only path).",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, regime: str | None) -> list"
    },
    "Optimization/simconfig/staffing.py::script_totals": {
      "decorators": [],
      "doc": "Stage B's demand side: units, lines and the analytic pick seconds of a batch list.\n\n`batches` are `Workload_Builder.Batch` objects (or anything with `.items: {sku: qty}`);\n`orders_by_sku` maps a SKU to its `Order` for the handling term.  Each SKU's line\nquantities are kept, in script order, for `implied_reorders`.",
      "is_async": false,
      "kind": "function",
      "signature": "(batches, orders_by_sku: dict, pricing: PricingConfig) -> ScriptTotals"
    },
    "Optimization/simconfig/staffing.py::solve_pickers": {
      "decorators": [],
      "doc": "The smallest integer crew whose expected cut share is at or under `cut_share_max`.\n\n`load_s` is E[W], the day's expected picking seconds at the DECLARED demand priced at\nthe channel's expected seconds per unit; `cv` the day's unit cv (`units_cv`); the day\nlaw is `W ~ Normal(load_s, cv\u00b7load_s)`.  `cut_share(K)` is decreasing in K, and it is\nnever below the plain excess `(E[W] - K\u00b7S) / E[W]` (the overflow of a spread-less day),\nso no crew under `(1 - bound)\u00b7E[W] / S` can meet the bound: the search walks up from\nthat crew and stops at the first K inside it, which monotonicity makes the SMALLEST.\n(Walking up from the crew that fits the MEAN day would skip feasible crews below it\nwhenever the spread is small -- under ~0.06 at the 0.95 confidence.)  Returns\n\n    {'pickers', 'cut_share', 'cut_share_max', 'load_s', 'sd_s', 'cv',\n     'expected_utilization'}                              (utilization = E[W] / (K\u00b7S))\n\nA section with no load fields nobody (`pickers = 0`); `k_max` guards a runaway (a\nbound of 0 with a positive sd is unreachable and says so).",
      "is_async": false,
      "kind": "function",
      "signature": "(load_s: float, cv: float, day_seconds: float, cut_share_max: float, *, k_max: int=100000) -> dict"
    },
    "Optimization/simconfig/staffing.py::units_cv": {
      "decorators": [],
      "doc": "The coefficient of variation of a DAY's units under the declared law: `N ~ Normal(n,\ncv\u00b7n)` lines, each demanding iid units with the section's moments, so\n\n    E[U] = n\u00b7m1        Var[U] = n\u00b7Var[q] + (cv\u00b7n)\u00b2\u00b7m1\u00b2\n\n(\"Fit the store's window to its own steady state\", decision 8: the Gaussian line count\ncarries all but ~0.001 of the unit cv on the reference pair, so the day's work is one\nGaussian draw to promise on).  Zero when the day has no units.",
      "is_async": false,
      "kind": "function",
      "signature": "(lines_per_day: float, cv_lines: float, moments: dict) -> float"
    },
    "Optimization/simdriver/__init__.py": {
      "decorators": [],
      "doc": "simdriver \u2014 the simulation orchestration driver (cells + work units + supervisor +\nscenario), extracted from run_simulation.py.  run_simulation re-exports these so tests,\nDiagnostics, and the arch graph keep referencing them as rs.<name>.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/audit_ledgers.py": {
      "decorators": [],
      "doc": "audit_ledgers.py \u2014 the two run-long audits an arm keeps, and their two report-once rules.\n\nWHAT IT REPLACES.  `strategy_runner._build_leaf` carried these as four closure variables:\n`cons_picked`, `cons_breaks`, `cons_residual` (the conservation ledger) and `demand_breaks`\n(the demand ledger).\n\n# \u2500\u2500 what each audit proves \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nCONSERVATION is the runtime proof that the bin-mutation log is COMPLETE.  Over the arm,\n\n    units placed - units evicted - units picked  ==  units sitting in bins right now\n\nand the right-hand side is read from the WAREHOUSE, not from any counter the recorder keeps,\nso a mutation that bypasses the log breaks it -- which is the entire point.  It is asserted\ncumulatively rather than per-batch because the cumulative form needs no special case for the\nfirst measured batch (a resumed arm re-stocks from empty) and localises a break just as well.\n\nDEMAND is the proof that no pick exceeded the demand that asked for it.  Its docstring in the\nrunner records what it would have caught: both picker loops read the per-AISLE\n`task.items[sku]` once per bin, so a SKU in several bins of one aisle was picked once per\nbin, inflating every throughput figure by ~6.7% for as long as the model had existed.\n\nNEITHER EVER RAISES.  A residual can move for a benign scheduling coincidence -- two pickers\ncontending for one bin drain slightly differently than the depletion applied -- and turning\nthat into a run-killer would be strictly worse than reporting it.\n\n# \u2500\u2500 the two report-once rules, which are NOT the same rule \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nBoth suppress repeats, because ~100 identical log lines is how a real defect gets scrolled\npast.  They suppress them differently, and this object exists to make that difference\naddressable:\n\n    `observe()`           reports when the residual MOVES.  The ledger is cumulative, so one\n                          bad batch leaves a residual forever; only a batch that moves it\n                          INTRODUCED anything.  Movement in EITHER direction counts -- a\n                          ledger that reported only growth would go quiet exactly when a\n                          second defect cancelled the first.\n    `note_demand_break()` reports only the FIRST.  There is no residual here, so \"movement\"\n                          has no meaning: the first line is the diagnosis and the count is\n                          the summary.\n\n# \u2500\u2500 what stays in the runner \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nTHE LOG WORDING.  This object decides WHETHER a batch should be reported and hands back the\ndrift; `strategy_runner` owns the messages, which name files and columns that a unit test\nshould not be pinned to.\n\nSlotted and picklable, like the other `_build_leaf` state objects.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers": {
      "decorators": [],
      "doc": "The conservation ledger and the demand ledger, with their report-once rules.\n\naudit = AuditLedgers()\naudit.note_picked(sum(p.quantity for p in picks_b))\nbroke = audit.observe(placed=..., evicted=..., occupancy=...)\nif broke is not None:\n    drift, residual = broke\n    log.error(...)\n...\nif bs.total_items > bs.items_demanded and audit.note_demand_break():\n    log.error(...)\n...\nresult.update(audit.totals())",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers.note_demand_break": {
      "decorators": [],
      "doc": "Count a batch that picked more than was demanded.  True only for the FIRST.\n\nEvery break is counted; only the first is worth a line, because there is no residual\nwhose movement could distinguish a later one.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers.note_picked": {
      "decorators": [],
      "doc": "Add this batch's picked units to the cumulative term.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, units: int) -> None"
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers.observe": {
      "decorators": [],
      "doc": "Test the ledger against the warehouse.  Returns `(drift, residual)` if this batch\nINTRODUCED a break, else None.\n\n`drift` is INCREMENTAL -- how far the residual moved this batch -- and is computed\nbefore the new residual is stored, so the caller can report \"new drift X; cumulative\nY\" exactly as the runner always did.  A residual that has not moved returns None and\ndoes not count a second break.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, placed: int, evicted: int, occupancy: int)"
    },
    "Optimization/simdriver/audit_ledgers.py::AuditLedgers.totals": {
      "decorators": [],
      "doc": "The audit half of the worker's result dict.\n\nReal zeroes on a clean arm, never None: `cons_breaks == 0` means MEASURED AND ZERO,\nand a NULL there would read as \"not measured\" -- which is the opposite claim.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/simdriver/batch_precompute.py": {
      "decorators": [],
      "doc": "batch_precompute.py \u2014 compute each warehouse family's batch sequence ONCE and share it.\n\nBatch sampling (`Workload_Builder.Batch`) is a PURE function of (batch_cfg, inventory, affinity,\nseed_batches + i): it weights SKUs by static demand frequency x the immutable affinity-lift CSR and\ndraws from a dedicated per-batch RNG `random.Random(seed_batches + i)`.  Today every strategy arm of a\nwarehouse family re-samples the identical sequence inside its own worker process \u2014 the heavy\nO(k*|inventory|) sampling (the bulk of the per-batch \"build\" section) is paid ~32x for bit-identical\nresults.  This module computes the sequence ONCE (optionally in parallel chunks) so every arm of the\nfamily reads it instead, while the genuinely sequential `Task.from_batch` stays per-arm in the loop.\n\nCorrectness rests on two things:\n  * `load_inventory_from_db` selects `ORDER BY sku`, so the candidate order \u2014 which\n    `_lift_weighted_sample` indexes positionally \u2014 is deterministic; loading the SAME db with the SAME\n    limit/allowlist the worker uses reproduces the worker's candidates exactly.\n  * a content FINGERPRINT over exactly the fields a batch consumes (the ordered\n    (sku, relative_frequency, quantity_rate) triples + batch_cfg + seeds + the affinity CSR digest)\n    keys the on-disk list.  Families of different sizes get different fingerprints (so they never share\n    a list), and a worker accepts a file only when its own recomputed fingerprint matches \u2014 never\n    another family's.  On any mismatch/miss the caller falls back to inline sampling (still correct).",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/batch_precompute.py::_MIN_PARALLEL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "16"
    },
    "Optimization/simdriver/batch_precompute.py::_affinity_digest": {
      "decorators": [],
      "doc": "Hash the immutable affinity-lift CSR (the values batch selection multiplies in).  Distinguishes\ndifferent affinity DBs; identical for the same DB loaded twice (parent precompute vs worker).",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity) -> str"
    },
    "Optimization/simdriver/batch_precompute.py::_load_worker_inventory": {
      "decorators": [],
      "doc": "Load inventory EXACTLY as strategy_runner._run_strategy_worker does (same db, limit,\nallowlist, THEN regime filter) so the candidate list \u2014 and therefore every sampled batch \u2014\nis identical to the worker's.  ``channel_regime`` (store/fulfillment) keeps a mixed-catalog\nchannel's stream regime-pure; None \u21d2 the whole (allowlisted) inventory (store-only).",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_db: str, max_skus, sku_allowlist, channel_regime=None)"
    },
    "Optimization/simdriver/batch_precompute.py::_sample_range": {
      "decorators": [],
      "doc": "Module-level (spawn-picklable) chunk worker: load inv+aff once, sample batches [lo, hi).",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_db, max_skus, sku_allowlist, aff_db, batch_cfg, seed_batches, lo, hi, channel_regime=None)"
    },
    "Optimization/simdriver/batch_precompute.py::batch_fingerprint": {
      "decorators": [],
      "doc": "Stable hash over EXACTLY the inputs that determine the batch sequence (order included).",
      "is_async": false,
      "kind": "function",
      "signature": "(inventory, batch_cfg, seed_batches: int, n_batches: int, affinity) -> str"
    },
    "Optimization/simdriver/batch_precompute.py::ensure_batches": {
      "decorators": [],
      "doc": "Compute-or-reuse this family's batch file under out_dir.  Returns (path, fingerprint).\n\nThe file is named by the fingerprint so different families never collide and identical families\n(e.g. configs that don't change inventory) share one file.  Existence \u21d2 reuse (write is atomic);\nthe worker re-verifies the full fingerprint on load.\n\n``channel_regime`` (store/fulfillment) makes the precompute regime-pure so a mixed-catalog\nchannel shares ONE list across all its configs (store and fulfillment necessarily get distinct\nfiles: different regime SKUs, seed, and batch fraction all feed the fingerprint).",
      "is_async": false,
      "kind": "function",
      "signature": "(out_dir: str, inv_db: str, max_skus, sku_allowlist, aff_db, batch_cfg, seed_batches: int, n_batches: int, workers: int=1, log=None, channel_regime=None)"
    },
    "Optimization/simdriver/batch_precompute.py::load_batches": {
      "decorators": [],
      "doc": "Return the stored batch list IFF the stored fingerprint matches expected; else None so the\ncaller falls back to inline sampling (never the wrong family's list).\n\nImportError/AttributeError guard: a cache written before a module rename/move (e.g. the\nflat->package conversion) embeds the OLD class paths \u2014 unpicklable now.  Treat it exactly\nlike a corrupt file (fall back inline; delete stale _batches_*.pkl to regain the dedup).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, expected_fingerprint: str) -> list | None"
    },
    "Optimization/simdriver/batch_precompute.py::precompute_batches": {
      "decorators": [],
      "doc": "Return the full list of `Batch` objects for i in [0, n_batches).\n\nSerial when workers<=1 or n_batches<_MIN_PARALLEL; otherwise splits the range across a transient\nspawn pool (each chunk loads inv+aff once, then samples its slice).  Result order is always 0..n-1.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_db, max_skus, sku_allowlist, aff_db, batch_cfg, seed_batches: int, n_batches: int, workers: int=1, channel_regime=None) -> list"
    },
    "Optimization/simdriver/batch_precompute.py::write_batches": {
      "decorators": [],
      "doc": "Persist {fingerprint, batches} atomically (tmp + os.replace) so a half-written file is never\nobserved as complete.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, fingerprint: str, batches: list) -> None"
    },
    "Optimization/simdriver/batch_state.py": {
      "decorators": [],
      "doc": "batch_state \u2014 the five names a batch's two halves share, and nothing that outlives one.\n\n`_build_leaf` binds 113 distinct names at one indent level. Five of them are here because of\na language rule and nothing else: `_replenish` and `_step` are the two halves of one batch,\nthey must share these values, and `nonlocal` can only rebind a name that already exists in\nthe enclosing scope. So five values whose LIFETIME IS ONE BATCH were bound in a scope whose\nlifetime is one ARM, and three `nonlocal` declarations existed to reach back up to them.\n\nThat mismatch is the smell ticket 06 points at, in plain sight:\n\n    _late = 0.0              # how late this batch was against its release slot\n    _day_end = None          # the whistle for the day this batch was released into\n    _put_base = None         # the put crew's epoch; the SITE's when pooled\n    _batch_early = None      # the batch, sampled early for the standing-demand feed\n    triggered = ()           # the SKUs this batch reordered\n\nOne object instead. The closures mutate its fields rather than rebinding names, so the\n`nonlocal` lines go, and the arm's scope carries one name where it carried five.\n\n## What is NOT here\n\nNo `begin()` or `reset()`. Every field is unconditionally re-seeded by `_replenish` before\n`_step` can read it \u2014 `late` and `day_end` by assignment, `put_base` and `early` by an\nexplicit `= None` followed by a conditional set, `triggered` by `check_reorders` or by\n`note_triggered` under coupling. A reset method would be a second writer of the same fact\nthat happens to be a no-op, which is the shape this whole effort is about deleting.\n\n## Why a dataclass and not a namedtuple\n\nThese are WRITTEN by one half and READ by the other, several fields at a time, at different\npoints in a 600-line body. A frozen record would mean rebuilding it on every write, which\nputs the rebinding problem back and adds allocation to the per-batch path.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/batch_state.py::BatchState": {
      "decorators": [
        "dataclass"
      ],
      "doc": "Everything one batch's replenish half must hand its pick half. Lives one batch.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/batch_state.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['BatchState']"
    },
    "Optimization/simdriver/cells.py": {
      "decorators": [],
      "doc": "simdriver.cells \u2014 the what-if cell matrix: build/apply/inspect cells.\n\nA cell = a fixed choice of aisle-split \u00d7 zoning \u00d7 scheduler \u00d7 inbound policy over one frozen\ninventory.  _apply_cell mutates the SAME sim_config.CONFIG object (never rebound) so the\nchange is seen everywhere; _build_cells names cells\nk{k}[_l{loss}]_{zone}[_{inbound}][_{sched}].\n\nThe inbound suffix sits BEFORE the scheduler suffix on purpose: `run_whatif_labor._scheduler_of`\nrecovers the swept scheduler as the cell name's LAST underscore token, and appending a fifth\nsegment after it would silently relabel every row of that module's CSV.\n\nCONFIG is run STATE and the simconfig registry is the DECLARATION; _apply_cell writes to\nthe former only.  Cell.overrides() reports the same values without applying them.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/cells.py::Cell": {
      "decorators": [],
      "doc": "One point in the what-if matrix: a fixed choice on every swept axis.\n\nA NamedTuple rather than a dataclass, deliberately.  This was a bare 4-tuple unpacked\npositionally in five files \u2014 `cells.py`, `scenario.py`, `run_simulation.py`,\n`sim_manifest.py` and `run_whatif_delta.py` \u2014 so `c[1]`, `c[3]` and\n`(name, split, zoning, sched) = c` all had to keep working.  They do, which is why\nthis change cannot break a consumer by being half-applied; named access is simply\nadded beside them.\n\nThe FIFTH axis (the inbound policy) is now one field and one line in `_build_cells`\nrather than five positional unpacks to find and renumber.  The names here are the same\nones the run-tree descriptor records, so it and the producer cannot drift apart.",
      "kind": "class",
      "signature": "(NamedTuple)"
    },
    "Optimization/simdriver/cells.py::Cell.is_reference": {
      "decorators": [
        "property"
      ],
      "doc": "The natural baseline: no split, no zoning, the legacy scheduler, inbound untouched.\n\nThis predicate was written out twice, verbatim, in `scenario.py` and\n`run_simulation.py` \u2014 and those two MUST agree, because one picks the cell the\ndriver treats as the baseline and the other writes the reference name into\nthe run-tree descriptor for every downstream what-if to diff against.  Two copies of a\npredicate that must agree is a bug with a delay on it.\n\nA cell carrying an inbound record is never the NATURAL baseline: an inbound matrix has\nno order-blind default cell to fall into, so the spec must DECLARE its reference (and\n`_build_cells` refuses one that does not).  Without this clause every cell of a\nten-policy matrix would answer True and `reference_cell` would silently take the first.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Optimization/simdriver/cells.py::Cell.overrides": {
      "decorators": [],
      "doc": "What this cell CHANGES, as a plain record.\n\nThe same four values `_apply_cell` writes, named rather than positional, so a\ncaller can see a cell's effect without applying it \u2014 which is what a resolution\npass needs, and what makes the effect diffable between two cells.\n\n`scheduler` is a pick-config field (like `one_way`), so it lands on every one of a\nchannel's pick-config dicts; `aisle_split` and `velocity_zoning` are channel-level.\n`inbound` is the odd one out and stays named as itself: the yard and dock policies are\nwhole-RUN settings on `CONFIG['global']`, which is exactly why they had to become a\ncell axis instead of six separate runs.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/simdriver/cells.py::_SCHED_SHORT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'round_robin': 'rr', 'lpt': 'lpt'}"
    },
    "Optimization/simdriver/cells.py::_apply_cell": {
      "decorators": [],
      "doc": "Mutate CONFIG for one cell: same aisle_split + velocity_zoning + picker scheduler on every\nchannel, plus this cell's inbound policy on `CONFIG['global']`.  The scheduler is a\npick-config field (like one_way), so set it on each channel's pick-config dicts.\n\nWrites into CONFIG and ONLY into CONFIG.  Those pick-config dicts used to be the very\nobjects `PICK_CONFIG_BY_KEY[\u2026].cfg` holds, so this loop permanently edited the registry:\n`scheduler` is absent from every config module as written, and after one cell ran the\nfrozen spec carried whichever value that cell chose, for the life of the process.\n`sim_config` now copies the dicts when it assembles CONFIG, which is where that boundary\nlives \u2014 see the comment beside STORE_CONFIGS.  Do not reintroduce a path that hands the\nregistry's own dict to a mutator.\n\n`inbound` is global rather than per-channel because the yard and the dock are one site's,\nnot one channel's; `inbound_spec()` reads `CONFIG['global']` at CALL time inside\n`_prepare_channel_run`, so a value written here reaches every spawned worker's payload.\nNone writes NOTHING \u2014 the inert default that keeps a pre-inbound spec byte-identical.\n\nSee `Cell.overrides()` for the same four values as a record, when you want to inspect a\ncell's effect rather than apply it.",
      "is_async": false,
      "kind": "function",
      "signature": "(aisle_split, zoning, scheduler='round_robin', inbound=None) -> None"
    },
    "Optimization/simdriver/cells.py::_build_cells": {
      "decorators": [],
      "doc": "Combinatorial (scheduler \u00d7 zoning \u00d7 k \u00d7 capacity_loss \u00d7 inbound) cells from a WHATIF spec.\nk=1 = no split (loss collapses to 0).  Returns [(name, aisle_split|None, zoning_spec,\nscheduler, inbound|None), \u2026]; the (k=1, off, round_robin, no inbound) cell is the natural\nreference.  ``schedulers`` defaults to ['round_robin'] (byte-identical, no name suffix); a\nmulti-value list adds a `_rr`/`_lpt` suffix so the picker-scheduler becomes a sweep axis\nalongside aisle_split and zoning.  ``inbound`` defaults to one inert entry and behaves the\nsame way \u2014 see `_inbound_axis` for its shape and the four collapses it refuses.\n\nA swept inbound axis must also DECLARE `reference`: `Cell.is_reference` answers False for\nevery cell carrying an inbound record, so an undeclared reference would fall through to\n`cells[0]` \u2014 the same silent-arbitrary-baseline failure `run_channel_rollup._baseline_entry`\nwas caught doing, one level up.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec)"
    },
    "Optimization/simdriver/cells.py::_cell_complete": {
      "decorators": [],
      "doc": "A cell is done when it has \u22651 sim_meta.json per pair (all its configs finalized).",
      "is_async": false,
      "kind": "function",
      "signature": "(scenario_base: str, pairs: list) -> bool"
    },
    "Optimization/simdriver/cells.py::_inbound_axis": {
      "decorators": [],
      "doc": "The spec's inbound axis as [(name_suffix, overrides|None)], validated.\n\nDefaults to the single inert entry `[('', None)]`, which is what every spec written before\nthe inbound axis existed resolves to: no suffix, no CONFIG write, byte-identical names and\nbehaviour.  A real axis is a list of `(suffix, {inbound_key: value})` pairs \u2014 keys are\n`CONFIG['global']` `inbound_*` names with the prefix dropped.\n\nFour refusals, each closing a failure with no error message:\n\n* **An unknown key.**  A misspelt name creates a new `CONFIG['global']` entry that\n  `inbound_spec()` never reads, so the cell runs the DEFAULT policy under the swept\n  policy's name.  Checked on every axis, inert or not.\n* **A missing suffix on a real axis.**  `_build_cells` dedupes by name, so inbound-on and\n  inbound-off collapse into ONE cell \u2014 the matrix comes out smaller than it was asked for\n  and nothing says so.\n* **Duplicate suffixes.**  The same collapse by another route.\n* **A key one entry sets and another does not.**  `_apply_cell` mutates a process-wide\n  CONFIG that is never reset between cells, so a key written by cell 3 and omitted by cell 4\n  leaves cell 4 running cell 3's policy under its own name.  Requiring every entry to cover\n  the union makes the carryover unreachable rather than merely unlikely.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec)"
    },
    "Optimization/simdriver/cells.py::_tightest_split": {
      "decorators": [],
      "doc": "The split with the largest capacity_loss.  Freezing the sampled inventory to it is\nhow a multi-cell sweep guarantees every cell can hold the same inventory.\n\n\"Largest loss\" no longer means \"fewest bins\", and has not since \"Field the requirement\":\n`_split_inflation` adds compensatory aisles at sizing time, so a lossier split can emit\nMORE bins than no split at all.  The freeze is still correct, and now for a stronger\nreason -- every cell is sized from the same declaration and REFUSES if it cannot hold it,\nso which split we freeze against no longer decides whether the inventory fits.  Kept as\nthe freeze point because the choice must be deterministic and cell-order-independent.",
      "is_async": false,
      "kind": "function",
      "signature": "(cells)"
    },
    "Optimization/simdriver/cells.py::reference_cell": {
      "decorators": [],
      "doc": "The name of the reference cell, or `fallback`, or the first cell.",
      "is_async": false,
      "kind": "function",
      "signature": "(cells, fallback: str | None=None) -> str"
    },
    "Optimization/simdriver/draw_probability.py": {
      "decorators": [],
      "doc": "draw_probability.py \u2014 the DRAW PROBABILITY `p_s`: what the declared sampler actually does.\n\nThe coverage record prices a SKU's prior lines at its **line share** `pi_s = freq / sum freq`\n(`Optimization/simconfig/coverage.py`).  That is the sampler's BASE WEIGHT, not its inclusion\nprobability.  `Workload_Builder._lift_weighted_sample` draws `k` DISTINCT SKUs per batch WITHOUT\nreplacement and multiplies every survivor's weight by `prod lift(A, B)` over the partners already\ndrawn, so the probability a SKU lands in a batch is a different function of the catalogue, not a\nscaled one (.scratch/department-calibration, \"Close the fulfillment fill-law gap\", decision 1).\n\nThis module computes that probability -- the **draw probability** `p_s` -- by running the declared\nsampler and nothing else.  No warehouse is planned, no inventory is placed, no simulation is\nstepped: exactly `Batch(batch_cfg, inventory, affinity, rng=random.Random(seed_batches + i))` for\n`i` in `[0, M)`, which is the identical call `batch_precompute._sample_range` makes.  Because the\nrun's own script uses the SAME base seed, the run's `n_batches` batches are a PREFIX of this sample\n(decision 7) -- there is no second demand stream to reconcile.\n\nWHY THIS IS AFFORDABLE, and why it may run BEFORE the coverage fixed point.  Under the era the two\n`units_per_line` cancel in `era_coverage.stage_a`, so a channel's `mean_fraction` reduces to the\ndeclared `STORE_DEMAND` / `FF_DEMAND` constant: `k` is a declared fraction times the section size\nand does NOT depend on the fixed point's `n`.  `p_s` is therefore a pure function of\n`(inventory, affinity, batch_cfg, seed)` -- all declared before the solve runs -- and is drawn ONCE\nrather than re-drawn per round.  (`batch_precompute` runs AFTER the solve, from the converged\ndeclaration, which is why its file cannot serve this purpose: it holds `n_batches` batches, and\n`n_batches` is a window length, not a precision.)\n\nWHAT COMES BACK, and why it is COUNTS rather than probabilities.  A chunk returns the per-SKU\ncount `c_s` of batches that included it, positionally over `load_inventory_from_db`'s\n`ORDER BY sku` -- the same candidate order `_lift_weighted_sample` indexes, so the vector aligns\nwith the inventory the caller already holds.  Counts are kept rather than `c_s / M` because the\nconsumer needs BOTH estimators and only counts can give the unbiased one:\n\n    p_s               ->  c_s / M                                       (the plug-in)\n    p_s^j (1-p_s)^i   ->  c_s^(j) \u00b7 (M - c_s)^(i) / M^(j+i)             (falling factorials)\n\nthe second being exact for `c_s ~ Binomial(M, p_s)`.  This matters because the fill law's\nline-weighted functional is CONVEX in `p_s`, so a plug-in carries an upward Jensen bias that\nshrinks only as `M` grows -- the identical artifact that, at the 20-batch window, was worth two\nthirds of the apparent movement in \"Close the fulfillment fill-law gap\".  `M` is DECLARED by\nrequiring the two to agree (`prior_line_probability` below, and `--ladder` on the CLI), which makes\nthe precision a measurement rather than a guess.  The plug-in is what the record carries; the\nunbiased form is the diagnostic that sizes `M`.\n\nChunk counts are stored SEPARATELY and in range order, so any prefix `M_j` is a partial sum: one\ndraw answers the whole ladder instead of one draw per rung.\n\nThe fingerprint is `batch_precompute.batch_fingerprint` unchanged -- the same inputs decide both\nsequences, and `M` enters it through the `n_batches` slot.  Files are named `_drawp_*.npz`, so a\ndraw can never be served from (or poison) a `_batches_*.pkl`.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/draw_probability.py::_assets": {
      "decorators": [],
      "doc": "Load-or-reuse this process's (inventory, affinity, position map).",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_db, max_skus, sku_allowlist, aff_db, channel_regime)"
    },
    "Optimization/simdriver/draw_probability.py::_count_range": {
      "decorators": [],
      "doc": "Module-level (spawn-picklable) chunk worker: draw batches `[lo, hi)` and return the\nper-SKU inclusion COUNT over the inventory's own order.\n\nBuilds the full `Batch` rather than calling the sampler directly, deliberately: `Batch`\nalso draws each selected SKU's line quantity from the same `rng`, and reproducing the\nrun's stream exactly means reproducing every draw that stream makes, not only the ones\nthis module reads.  The batches themselves are dropped as soon as they are counted, so\npeak memory is the memoized assets plus one batch and the count vector.\n\nMEMORY, not compute, sets the worker count.  The partner map is ~14M entries of Python\ntuples on the reference catalogue -- well over a gigabyte resident -- so each worker is\nexpensive to hold and cheap to keep busy (~0.22 s/batch on the fulfillment section).  A\nhandful of workers each drawing thousands of batches is the shape that fits; one worker\nper core is how this thrashes.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_db, max_skus, sku_allowlist, aff_db, batch_cfg, seed_batches, lo: int, hi: int, channel_regime=None) -> np.ndarray"
    },
    "Optimization/simdriver/draw_probability.py::_main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv: list) -> int"
    },
    "Optimization/simdriver/draw_probability.py::ensure_draw_probability": {
      "decorators": [],
      "doc": "Compute-or-reuse this (pair, channel)'s draw file under `out_dir`.  Returns\n`(path, fingerprint)`.\n\nNamed by the fingerprint, so two channels never collide and an identical declaration is a\nfile-exists hit; the write is atomic and the loader re-verifies the fingerprint.  Exactly\nthe contract `batch_precompute.ensure_batches` offers, over the same inputs, with `M` in\nthe `n_batches` slot.",
      "is_async": false,
      "kind": "function",
      "signature": "(out_dir: str, inv_db: str, max_skus, sku_allowlist, aff_db, batch_cfg, seed_batches: int, n_draws: int, workers: int=1, chunks: int=_DEFAULT_CHUNKS, log=None, channel_regime=None) -> tuple"
    },
    "Optimization/simdriver/draw_probability.py::ladder": {
      "decorators": [],
      "doc": "The prefix ladder: `(M_j, K, plug_in, unbiased, abs_gap, rel_gap)` for every chunk\nprefix and every `K`.  One draw answers the whole table -- which is the point of keeping\nthe chunk counts apart.",
      "is_async": false,
      "kind": "function",
      "signature": "(blob: dict, ks=(1, 2, 3, 4)) -> list"
    },
    "Optimization/simdriver/draw_probability.py::load_counts": {
      "decorators": [],
      "doc": "Return `{sku, counts, bounds, M, seed_batches}` IFF the stored fingerprint matches\n(or no expectation is given); else None, so a caller falls back to drawing.\n\n`counts` is the per-chunk matrix, NOT the total -- summing is the caller's, because the\nprefix ladder is the reason the chunks are kept apart.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, expected_fingerprint: str | None=None) -> dict | None"
    },
    "Optimization/simdriver/draw_probability.py::precompute_counts": {
      "decorators": [],
      "doc": "Draw `n_draws` batches and return `(bounds, counts)`.\n\n`bounds` is the list of `(lo, hi)` chunk ranges in order and `counts` the matching\n`(len(bounds), n_skus)` int64 matrix, so `counts[:j].sum(0)` is the count vector at\nprefix `bounds[j-1][1]` -- the ladder, from one draw.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_db, max_skus, sku_allowlist, aff_db, batch_cfg, seed_batches: int, n_draws: int, workers: int=1, chunks: int=_DEFAULT_CHUNKS, channel_regime=None) -> tuple"
    },
    "Optimization/simdriver/draw_probability.py::prior_line_probability": {
      "decorators": [],
      "doc": "`P(>= 1 prior line for the same SKU within K days | a line)`, line-weighted, both ways.\n\nThe fill law on a floored section reduces to exactly this event, so it is the quantity\nwhose precision decides `M`.  A line belongs to SKU `s` with probability proportional to\n`p_s`, and the days are independent (each batch draws from its own\n`random.Random(seed_batches + i)`; no cross-batch state), so\n\n    Q(K) = sum_s p_s \u00b7 (1 - (1 - p_s)^K) / sum_s p_s\n\nReturns `(plug_in, unbiased)`.  The plug-in substitutes `c_s / M`; the unbiased form uses\nthe binomial falling-factorial identity\n\n    E[ c^(j) \u00b7 (M - c)^(i) ] = M^(j+i) \u00b7 p^j \u00b7 (1 - p)^i\n\nwith `j = 1`, `i = K`, which is exact rather than an alternating expansion and therefore\nstable at every `K` the transit law puts mass on.  Their DIFFERENCE is the plug-in's\nJensen bias at this `M`; agreement is the criterion that declares `M`.",
      "is_async": false,
      "kind": "function",
      "signature": "(counts: np.ndarray, M: int, K: int) -> tuple"
    },
    "Optimization/simdriver/draw_probability.py::write_counts": {
      "decorators": [],
      "doc": "Persist the draw atomically (tmp + os.replace) so a half-written file is never\nobserved as complete.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, fingerprint: str, skus: np.ndarray, bounds: list, counts: np.ndarray, seed_batches: int) -> None"
    },
    "Optimization/simdriver/era_coverage.py": {
      "decorators": [],
      "doc": "era_coverage.py \u2014 the calibrated era's stage-A expectation per channel, and the\npair-level FIXED POINT that sizes the warehouse from stock levels denominated in days.\n\nTwo things live here because they are one loop:\n\n  * `stage_a` -- for each channel, the class-uniform placement distribution over the\n    section's packs and the expected day over it.  UNDER THE ERA (ADR-0004) the day is\n    DECLARED: `n = demand \u00d7 N_skus` lines, the expected seconds per unit is read AT that\n    day (`expected_travel.expected_pick`), and the picking crew is SOLVED from the\n    first-time confidence (`staffing.solve_pickers`: the smallest integer whose expected\n    cut share of the demanded units is under `1 - sqrt(c)`).  FLAG-OFF the crew is declared\n    and the day is the fixed point at which it fills its capacity\n    (`expected_travel.solve_n`), or the demand a declared `--s-pick-*` override buys.  The\n    regime is read off `inputs` (`staffing_spec()` records the other regime's keys as\n    None), never off a flag.  This is the first half of\n    `workunits._derive_staffing_for_pair` (\"Derive the expected-travel closed form\"), factored\n    out so the coverage loop and the derivation compute it with ONE function -- two copies\n    would be two answers.\n\n  * `fixed_point` -- \"Rescale stock coverage at setup\" (.scratch/department-calibration,\n    decision 2 of the closed form): a stock level is a RUN's declaration in declared days.\n    Every SKU's `equilibrium_qty` / `reorder_point` follow from its daily demand `d_s`, which\n    needs `n`; the warehouse is sized from those quantities (`plan_warehouse`); `n` depends on\n    the built geometry.  So\n\n        Q(n) -> plan_warehouse -> geometry -> expected_travel.solve_n -> n\n\n    is iterated at PAIR level until every channel's `n` moves by less than `tol`, or\n    `max_rounds` is spent and the residual is recorded.  ROUND 0 IS A SEED, NOT A PLAN\n    (`seed_lines`): the catalogue carries no level to plan (ADR-0002), so the loop opens with\n    the line count the crew fills at the section's ANALYTIC seconds per unit -- catalogue\n    only, no geometry, no travel term.  Each later round rescales the WHOLE catalogue at the\n    previous round's `n` and re-plans.  The last plan stands: its sampled orders, its geometry\n    and its stage-A expectation are what the run fields, so the derivation reuses them rather\n    than pricing the section twice.\n\n    UNDER THE ERA THE LOOP COLLAPSES TO ONE PASS: the line count is declared, so the seed\n    IS the fixed point, round 1 declares at it, plans once, and stage A answers with the\n    same `n` (converged by construction; a `--max-skus` sample that shrinks a section costs\n    one more round).  The levels no longer depend on the crew -- only the crew reads the\n    geometry.  Before round 1 the LINE FLOOR is solved per channel (`coverage\n    .solve_floor_lines`: the smallest floor whose stamped first-pass fill clears\n    `sqrt(c)`), the shelf side of the same guarantee; a typed `--floor-lines` is accepted\n    at or above it and refused below.  Flag-off the floor is one line and the loop\n    iterates as before.\n\n  * THE DERIVED FILL (\"Derive the fill headroom from the fragmentation\").  UNDER THE ERA\n    (`inputs['min_headroom']` is not None) the planner's fill is not typed: each round,\n    after the levels are declared and before the warehouse is planned, every SKU's plan is\n    stamped and the stationary fragmentation chain (`simconfig/fragmentation.py`) prices\n    the extra bins each bucket grows into under base stock; the bucket is then sized to\n    HOLD `max(requirement + E[extra], requirement / (1 - min_headroom))` bins\n    (`derive_fill`, handed to the planner as `bucket_hold`).  The record's\n    `fielded.buckets[]` carries `fill` and `hold` beside `expected_extra`, and a rebuild\n    reads the holds back (`holds_at`) rather than re-deriving them.  Flag-off the typed\n    `store_fill` / `ff_fill` size the warehouse exactly as before and the record says so\n    (`fielded.fill.provenance = 'assumed'`, `derived = None`).\n\nThe loop is driven from `sim_assets.build_shared_assets` WHEREVER IT SAMPLES -- in every mode,\nnot only under the era, because a level is now a run's declaration in every mode (ADR-0002,\ndecision 6); the era flag decides only whether the clock cuts and caps, and flag-off the day\nthe loop declares against is the reporting frame.  `plan_fn` is injected (a closure over the\nplanner's arguments) so the loop is testable with a fake planner and knows nothing about sizing.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/era_coverage.py::ChannelSpec": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "What stage A needs to know about one channel: its name, the regime its section is\nfiltered by (None on a store-only catalogue: the whole catalogue), the pick config it is\npriced with and the name of that config (the channel's FIRST config).",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/era_coverage.py::MissingCoverageRecord": {
      "decorators": [],
      "doc": "A rebuild asked to re-declare a run's levels, and the run recorded no coverage.",
      "kind": "class",
      "signature": "(RuntimeError)"
    },
    "Optimization/simdriver/era_coverage.py::_Floors": {
      "decorators": [],
      "doc": "A per-channel floor map whose missing channels read the record's fallback.",
      "kind": "class",
      "signature": "(dict)"
    },
    "Optimization/simdriver/era_coverage.py::_Floors.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, values: dict, default: float) -> None"
    },
    "Optimization/simdriver/era_coverage.py::_Floors.__missing__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, key)"
    },
    "Optimization/simdriver/era_coverage.py::_guarantee_inputs": {
      "decorators": [],
      "doc": "`(fill_min, cut_share_max)` from the declared first-time confidence -- both sides at\n`sqrt(c)` -- or None flag-off.",
      "is_async": false,
      "kind": "function",
      "signature": "(inputs: dict) -> tuple[float, float] | None"
    },
    "Optimization/simdriver/era_coverage.py::_lines": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sa: dict) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::channel_specs": {
      "decorators": [],
      "doc": "`(mixed, [ChannelSpec, ...])` for a catalogue, one spec per channel, from the same\nchannel plan the run's work units are built from (`workunits._channel_runs_for`).",
      "is_async": false,
      "kind": "function",
      "signature": "(inventory) -> tuple[bool, list[ChannelSpec]]"
    },
    "Optimization/simdriver/era_coverage.py::declare_from_record": {
      "decorators": [],
      "doc": "Re-declare a catalogue's stock levels EXACTLY as one finished run declared them, from\nthat run's own `staffing.calibration[<pair>].coverage` record.  Returns the per-channel\nrescale stats.\n\nThis is the REBUILD path, and it exists because a rebuild has a different job from a run.\n`run_analysis` and `run_map_precompute` re-plan a finished run's warehouse from its\noriginal catalogue to recover the shape it ran on -- and since ADR-0002 that catalogue\ncarries no level, while the warehouse is sized from levels on every path (`sample=False`\nskips only the SKU sampling, never `bucket_requirements`).  So a rebuild must declare\nbefore it plans, and it must declare THE SAME THING, or it silently rebuilds a different\nwarehouse -- the exact failure `run_map_precompute._identity_matches` refuses on, and the\none `run_analysis` would swallow into \"Config stage: 0 job(s)\".\n\nRe-running the fixed point would be both expensive (~8 minutes a pair) and WRONG: it would\nre-derive levels from THIS checkout's geometry and closed form, not the ones the run\nfielded.  The record already holds the only thing that varies -- the line count the run\nDECLARED at -- and the levels are a pure function of it and the three declared scalars, so\nONE rescaling pass reproduces the run's declaration exactly, in a second.\n\nWHICH line count, and it is not the obvious one.  A round declares at `prev` and sizes the\nwarehouse from that declaration; the stage A that follows ANSWERS with a different `n`, and\nthat answer is what `record['lines_per_day']` holds.  On a converged run the two differ by\nup to `tol` (1%) and on one that spent `max_rounds` by whatever the residual says -- enough\nto move `bucket_requirements` by tens of buckets and flip a `_demand_replicas` ceil, which\nis a rebuilt warehouse the run never built.  `run_map_precompute` would refuse every row;\n`run_analysis` has no such gate and would ship the wrong `total_bins` into every evaluation\nin silence.  So the INPUT is the one we want -- `declared_at`.\n\nRaises `MissingCoverageRecord` rather than guessing when the record is absent or does not\nname a channel the rebuild needs: a fabricated level here is a fabricated warehouse.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders_all: list, specs: list, record: dict | None, *, log: logging.Logger) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::declared_at": {
      "decorators": [],
      "doc": "`{channel: the line count this run DECLARED its levels at}` from a coverage record.\n\nNOT `record['lines_per_day']`, which is the fixed point's OUTPUT: the `n` stage A answered\nwith AFTER the last declaration was made and the warehouse sized from it.  Declaring at the\noutput re-derives levels the run never fielded -- see `declare_from_record`.\n\n`final[<channel>]` IS the stats dict `coverage.rescale_section` returned, so its\n`lines_per_day` is by construction the value the declaration was made at.\n`rounds[-1]['rescaled_at']` is the same number recorded from the loop's side, and serves as\nthe fallback so a record written by either shape reads correctly.",
      "is_async": false,
      "kind": "function",
      "signature": "(record: dict) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::declared_lines": {
      "decorators": [],
      "doc": "The DECLARED lines per day of channel `name` over `section`, or None flag-off.\n\nThe declaration is in the sampler's unit -- the fraction of the section's SKUs drawn\nper day -- so the line count is `fraction \u00d7 N` over exactly the orders handed in: the\nwhole catalogue at the seed, the sampled set at stage A.  The two agree whenever the\nplanner samples nothing away, which is every run without `--max-skus`.",
      "is_async": false,
      "kind": "function",
      "signature": "(inputs: dict, name: str, section) -> float | None"
    },
    "Optimization/simdriver/era_coverage.py::derive_fill": {
      "decorators": [],
      "doc": "The bins each bucket must HOLD under the era, and the fill that implies:\n\n    hold_b = max(requirement_b + E[extra_b],  requirement_b / (1 - min_headroom))\n    fill_b = requirement_b / hold_b                    (1 - min_headroom on an empty bucket)\n\n-- \"Band the own-bin share and the free-index depth\", decision 7: the warehouse holds\nits declaration AND the stationary fragmentation base stock creates\n(`fragmentation.section_fragmentation`, the `expected_extra` per bucket), floored at a\ndeclared minimum headroom for what the chain does not model (settings `MIN_HEADROOM`).\n`expected_extra` can be NEGATIVE (a remainder unit migrating down a tier), and a bucket\ncan carry extra with no requirement at all (the tier it migrates INTO); both read\ncorrectly here, the first taking the headroom floor, the second sized to its extra.\n\nPure: `{bucket: {'requirement', 'expected_extra', 'hold', 'fill', 'headroom_floored'}}`\nover the union of both maps' buckets, `headroom_floored` saying the minimum bound (the\nfragmentation alone would have left less free).  Refuses a headroom outside [0, 1).",
      "is_async": false,
      "kind": "function",
      "signature": "(req: dict, extra: dict, min_headroom: float) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::derived_holds": {
      "decorators": [],
      "doc": "The era's per-bucket hold map for a DECLARED catalogue, before the warehouse is\nplanned: `(holds, fills, frags)` -- `{bucket: bins}` for the planner, `derive_fill`'s\ntable, and each channel's `section_fragmentation` result (keyed by channel name).\n\nThe chain packs a lot by the SKU's `stock_plan`, which the planner writes when it\nfields -- AFTER sizing.  So every SKU's plan is stamped here first (one packing pass,\n`declared_packing` + `field_requirement` with its slots); the planner's own packing\nthen honours the stamped plan (`viable_storage_units`' plan branch) and fields the same\nthing, which the loop's fielded-equals-declared check states rather than assumes.  The\nchain computed here is the one the record stamps (`stamp_fragmentation(frag=)`): same\ndeclaration, same plan, one derivation.\n\nCOST: one packing pass plus the chain per channel, EVERY round of the loop (the chain\nused to run once, after convergence).  Under the era the loop is one round, so setup\ngrows by one packing and the chain (about a minute on the 400k-SKU reference pair);\nthe `[coverage] fill DERIVED ... [Ns]` line below is where that time shows.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders_all: list, specs: list, *, min_headroom: float, log: logging.Logger) -> tuple[dict, dict, dict]"
    },
    "Optimization/simdriver/era_coverage.py::fielded_block": {
      "decorators": [],
      "doc": "What the plan actually FIELDED for one channel section, as the record's proof that the\nline floor's promise was kept (\"Field the floor\", decision 11).\n\n    {'n_skus', 'fielded_sum_q', 'declared_sum_q',\n     'below_floor_skus',        # fielded below the SKU's own line floor: the promise BROKEN\n     'above_declaration_skus',  # fielded above its declaration: the retired phase-2 growth\n     'buckets': [{handling, category, size, unit, requirement, capacity, free}, ...]}\n\nThe growth counter is `above_DECLARATION_skus`, not \"above floor\" as decision 11 sketched\nit.  On the pair that produced this map the two coincided -- 100% of both sections sat ON\nthe floor, so a level above the floor WAS a grown one -- but on any catalogue where\ncoverage exceeds the floor, \"SKUs above their floor\" is nearly all of them and reads as an\nalarm.  What is worth zero is the count of levels the planner moved.\n\nBoth counts are read off each order's recorded `stock_plan` -- the packing the warehouse\nholds -- not off the level it was asked for, so they can disagree with the declaration and\nsay so.  Under the one planner contract they are zero by construction; the point of\nstamping them is that a reader can see the promise was TESTED rather than asserted, and\nthe pair that produced this map (30.3% of the fulfillment section below floor, 64,989 SKUs\ngrown above it) is exactly what a nonzero reading would look like.",
      "is_async": false,
      "kind": "function",
      "signature": "(section: list, plan, regime: str | None, floor_lines: float) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::fill_curve": {
      "decorators": [],
      "doc": "The section's expected first-pass fill as a function of the pair's TRANSIT, at the\nlevels the orders carry now: `[{'transit_days', 'fill_rate'}, ...]` sorted by transit,\none point per distinct day-grid expectation the median scaled by `scales` produces\n(the stamp itself, scale 1, always among them); None when the block names no pipeline.\n\nStamped so the audit can price `1 - fill(realized lead)` -- the EXPLAINED supply level\nof \"Declare the coverage against the inbound lead\", decision 10 -- without the\ncatalogue: an analysis worker holds the record, never the orders.  The levels are held\nFIXED (they were declared at the stamp); only the in-transit law moves, which is what a\nyard that binds does to the shelf.  The law's SHAPE is kept (same spread, the median\nscaled), so the point at scale 1 is the stamped fill exactly and the curve reads as\n\"the same regime, slower or faster\".  Linear between points (`equilibrium.fill_at`).",
      "is_async": false,
      "kind": "function",
      "signature": "(section: list, lines_per_day: float, lead: dict, scales: tuple=FILL_CURVE_SCALES) -> list | None"
    },
    "Optimization/simdriver/era_coverage.py::fixed_point": {
      "decorators": [],
      "doc": "Iterate Q(n) -> plan -> geometry -> n at pair level.  Returns\n`(plan, warehouse_meta, stage_a_result, record)` for the LAST round.\n\n`plan_fn()` plans `orders_all` (which this loop rescales in place before each call) and\nbuilds the warehouse: `(plan, warehouse_meta)`.  UNDER THE ERA it is called as\n`plan_fn(bucket_hold=holds)` with the derived hold map (`derived_holds`); flag-off with\nno argument at all, so a planner closure that never learned the keyword is still the\nflag-off path.  `orders_all` is the whole catalogue; the\nplan's `sampled` subset (or the whole list when the planner samples nothing) is what\nstage A prices.  `floor_lines` is the INPUT as declared: None means the one-line default\nflag-off and \"solve it\" under the era (`resolve_floors`).  The derived fill is on when\n`inputs['min_headroom']` is not None (`staffing_spec()` records it so under the era and\nNone flag-off).  `lead` is the pair's `lead_block` -- the day-grid transit every level,\npipeline stamp and fill is priced at (\"Declare the coverage against the inbound lead\");\nNone is no pipeline and a batch read as a day, byte-identically the record every run\nbefore the block declared.  The record is JSON-shaped and\nrides `staffing.calibration[<pair>]['coverage']`:\n\n    {'coverage_days', 'safety_days', 'floor_lines',   # the INPUT (None = solved)\n     'lead': {'transit_days', 'provenance', 'trailer_type', 'lead_s', 'lead_sigma',\n              'day_seconds', 'releases_per_day', 'lead_unit_days'},   # `lead_block`\n     'floor': {channel: {'floor_lines', 'provenance', 'solved'}},   # what each declares at\n     'tol', 'max_rounds', 'rounds': [...],\n     'seed': {'method': 'analytic_pick' | 'declared', 'lines_per_day': {channel: n}},\n     'final': {channel: rescale stats + 'fill' + 'fielded'},   # the declared levels, the\n                                                    #   fill rate at them, and the proof\n                                                    #   the plan fielded exactly them --\n                                                    #   with, per bucket, what it was\n                                                    #   sized to hold (`stamp_fill`)\n     'planned_sum_q', 'lines_per_day': {channel: n}, 'residual': {channel: n/n_prev - 1},\n     'converged': bool}\n\n`final[<channel>]['fill']` is `coverage.fill_rate` over the orders the last plan fields:\nthe expected first-pass fill rate the audit reads `missed_share` against, and the\nbase-stock share after planning -- priced at the pair's lead, with `lead_days` (the\nsection's units-weighted lead) beside `pipeline_units`, and under a pipeline\n`vs_transit`, the fill as a curve over the transit (`fill_curve`) for the audit's\nexplained level.  Priced post-plan for a reason that no longer bites --\nthe planner used to grow a level into leftover capacity, so the shelf that served a line\nwas not the one declared -- and it now equals the pre-plan price by construction, which\nis what `final[<channel>]['fielded']` (`fielded_block`) states and this loop RAISES on.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders_all: list, plan_fn, specs: list, *, coverage_days: float, safety_days: float, floor_lines: float | None, inputs: dict, day_seconds: float, log: logging.Logger, tol: float=_cov.DEFAULT_TOL, max_rounds: int=_cov.DEFAULT_MAX_ROUNDS, lead: dict | None=None) -> tuple"
    },
    "Optimization/simdriver/era_coverage.py::floors_at": {
      "decorators": [],
      "doc": "`{channel: the line floor this run DECLARED its levels at}` from a coverage record.\n\nPer channel since ADR-0004 (each section's floor is solved from its own fill), read off\n`final[<channel>]['floor_lines']` -- the rescale stats, so by construction the value the\ndeclaration was made at -- and falling back to the record's scalar for a run that\nrecorded one floor for both sections (every run before the flip), then to the flag-off\ndefault.  A channel absent from `final` takes the same fallback.",
      "is_async": false,
      "kind": "function",
      "signature": "(record: dict) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::holds_at": {
      "decorators": [],
      "doc": "`{bucket: bins to hold}` a run DERIVED its warehouse from, read off its coverage\nrecord's `final[<ch>].fielded.buckets[]` -- or None when the run sized at a typed fill\n(flag-off, or a record written before the derived fill existed).\n\nThe REBUILD path's counterpart of `declare_from_record`: a rebuild re-plans the run's\nwarehouse from its catalogue and must size every bucket exactly as the run did.\nRe-running the chain would be both slow and a re-derivation from THIS checkout's code;\nthe record already holds the only thing the planner needs.  A row sized to hold\nnothing (an empty structural bucket the fill rule gave its one replica) is not a hold\nthe run named, and is skipped as `derived_holds` skips it.",
      "is_async": false,
      "kind": "function",
      "signature": "(record: dict | None) -> dict | None"
    },
    "Optimization/simdriver/era_coverage.py::lead_block": {
      "decorators": [],
      "doc": "The record's `lead` block: the pair's expected order-to-shelf TRANSIT on the day\ngrid and the unit a SKU's supplier lead is read in (\"Declare the coverage against the\ninbound lead\", decisions 2-4).  JSON-shaped, stamped on the coverage record, and the\nonly thing a rebuild needs to re-declare the same levels (`declare_from_record`):\n\n    {'transit_days',                       # E[ceil(L / D)]  (`coverage.transit_day_law`)\n     'provenance': 'derived',\n     'trailer_type', 'lead_s', 'lead_sigma',   # the law it was read from (None/0/0 off)\n     'day_seconds',                        # the site day D it was gridded against\n     'releases_per_day', 'lead_unit_days'}  # a supplier lead's batch, in days\n\n`lead_law` is `sim_config.inbound_lead_law()` (None = no pipeline: transit 0) and `day`\nis `work_day_spec()`.  `lead_unit_days` is `1 / releases_per_day` -- one batch is one day\nunder the era, which completes `releases_per_day` to 1 -- and 1.0 when no release\nschedule is declared, the flag-off reading of a batch as a day (byte-identical to every\nrecord before this block existed).  DERIVED, never a CONFIG key: the regime's median and\nspread move the record through the closed form, and nothing else does.",
      "is_async": false,
      "kind": "function",
      "signature": "(lead_law: dict | None, day: dict) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::next_guess": {
      "decorators": [],
      "doc": "The line count to rescale at next, from this channel's `(n_in, n_out)` history.\n\n`n_out = g(n_in)` is monotone DECREASING: more lines a day means more stock, a bigger\nwarehouse, more travel per pick and so fewer lines the crew can fill -- and its gain is\nabove one on a real catalogue (the reference pair's fulfillment section went 1,818 ->\n14,832 -> 4,048 -> 10,797 under plain iteration).  So the plain iterate is used only\nuntil the root of `f(n) = log g(n) - log n` is BRACKETED (one point with `f > 0`, one\nwith `f < 0`); from then on the next guess is the secant of `f` in log-space between\nthe two bracket ends, pulled to the log-midpoint when it lands within 5% of either end\n(the one-sided stagnation regula falsi is known for).  Every later evaluation shrinks\nthe bracket, so the loop converges on any monotone map.",
      "is_async": false,
      "kind": "function",
      "signature": "(points: list) -> float"
    },
    "Optimization/simdriver/era_coverage.py::resolve_floors": {
      "decorators": [],
      "doc": "The line floor each channel declares at, and the record's `floor` block.\n\nFLAG-OFF (no first-time confidence in `inputs`): the typed `floor_lines`, else the\none-line default; nothing is solved.  UNDER THE ERA (ADR-0004, the shelf side): each\nsection's floor is SOLVED at its declared line count so the stamped first-pass fill\nclears `sqrt(c)` (`coverage.solve_floor_lines`), and a typed `--floor-lines` is\naccepted at or above every channel's solved value (stamped `declared`, the solved one\nrecorded beside it) and REFUSED below it -- a floor under the solved one is a smaller\npromise than the confidence makes, and raising it silently would be the second authored\nknob that moves the crew, the pattern that hid the fill-rate defect.  The solve runs AT\nthe pair's lead (`lead`, a `lead_block`; None = no pipeline, a batch read as a day).\n\nReturns `({channel: floor}, {channel: {'floor_lines', 'provenance', 'solved': {...}}})`.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders_all: list, specs: list, n: dict, *, coverage_days: float, safety_days: float, floor_lines: float | None, inputs: dict, log: logging.Logger, lead: dict | None=None) -> tuple[dict, dict]"
    },
    "Optimization/simdriver/era_coverage.py::seed_lines": {
      "decorators": [],
      "doc": "Round 0's `{channel: lines per day}`, from the CATALOGUE ALONE.\n\nThe loop needs a line count before it can declare a level, and it cannot get one the way\nevery later round does: `stage_a` prices a placement distribution over each SKU's storage\nunits at its order-up-to, and at round 0 no SKU has one (ADR-0002 -- the catalogue carries\nno level, so there is nothing to plan and no geometry to price against).\n\nSo the seed is the ANALYTIC prediction (`staffing.analytic_pick`): one intercept per line,\nthe per-item charge and the handling term per unit, at ground height with NO travel term.\nThat is a strict UNDER-estimate of seconds per unit, so `n` starts too HIGH and the first\nrescaling asks for too much stock.  The DIRECTION is not load-bearing -- `next_guess` needs\nthe root bracketed, one point each side, and the plain iterate delivers that after one round\nfrom either side -- but a seed of the right MAGNITUDE is what the loop is owed, and pricing\nthe real catalogue is the only honest way to get one without geometry.  A declared\n`--s-pick-*` override replaces the analytic value, because a run that names its constant\nmeans it from the first round.\n\nZero for a channel whose section is empty or unpriceable; the loop then declares every SKU\nat its line floor, which is what an empty demand implies.\n\nUNDER THE ERA there is nothing to seed: the line count is DECLARED (`declared_lines`),\nso the seed is the declaration itself and the loop converges on it in one round.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders_all: list, specs: list, *, inputs: dict, day_seconds: float, log: logging.Logger) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::stage_a": {
      "decorators": [],
      "doc": "Stage A of the staffing derivation for every channel: `{name: {...}}`.\n\nPer channel the dict carries `orders` (the section), `pricing`, `dist` (class-uniform),\n`pick_cfg`, `rates`, `analytic`, `expected` (the expected day at the section's line\ncount), `pickers` (SOLVED under the era, declared flag-off), `pickers_provenance`,\n`daily_demand_units`, `batch`, `s_pick` (the recorded constant), and under the era\n`demand` (the declaration and the day law's two cvs) and `guarantee` (the crew solve's\nstamp) -- exactly what `_derive_staffing_for_pair` reads.  `n` is the section's lines\nper day.  Warnings, never failures: an empty section prices nothing; a saturated batch\nis flagged.\n\nUNDER THE ERA (`inputs` carries a demand for the channel, ADR-0004) the line count is\nthe declaration, the expected day is priced AT it, and the crew is the smallest integer\nwhose expected cut share of the DEMANDED units -- `n \u00d7 E[q]` at the expected seconds per\nunit, under the day's unit cv -- is within `1 - sqrt(c)`.  A declared `--s-pick-*`\nreplaces the expected price in that load and is recorded `declared`.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders, geometry: _et.Geometry, specs: list, *, inputs: dict, day_seconds: float, log: logging.Logger) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::stamp_fill": {
      "decorators": [],
      "doc": "Stamp how one channel's `fielded` block was SIZED (in place) and return the block.\n\nEvery `buckets[]` row (already carrying `fill` and `hold` from the plan) gains\n`headroom_floored`, and the block gains\n\n    'fill': {'provenance': 'derived' | 'assumed',\n             'min_headroom': h | None,           # the era's declared floor\n             'typed': f | None,                  # the flag-off fill the rows share\n             'derived': {'section_fill',         # sum requirement / sum hold\n                         'expected_extra',       # the section's stationary extra\n                         'headroom_floored_buckets'} | None}\n\n`fills` is `derive_fill`'s table under the era and None flag-off, where the derived\nblock is None and the typed fill is what the rows say (\"flag-off keeps the typed fill\nand records the derived one as None\").",
      "is_async": false,
      "kind": "function",
      "signature": "(fielded: dict, fills: dict | None, min_headroom: float | None, *, name: str='') -> dict"
    },
    "Optimization/simdriver/era_coverage.py::stamp_fragmentation": {
      "decorators": [],
      "doc": "Stamp the stationary fragmentation onto one channel's `fielded` block (in place) and\nreturn the closed form's summary.  `frag` is a `section_fragmentation` result already\ncomputed for exactly this section and declaration (the era's `derived_holds` prices it\nbefore the plan); None prices it here.\n\nEvery `buckets[]` row gains `expected_extra` (the expected units of that bucket in\nsteady state beyond its `requirement`; 0.0 on a bucket no SKU's plan can reach), and\nthe block gains\n\n    'fragmentation': {'expected_extra',          # the section sum, provenance `derived`\n                      'provenance', 'method', 'n_classes', 'capped_classes',\n                      'positive_lead_skus', 'fielded_bins_per_sku',\n                      'stationary_bins_per_sku', 'seconds'}\n\nA kind that maps to a bucket the plan never built RAISES: the planner sizes a bucket\nfor every tier the packer can produce, so a miss is the planner and the packer having\ndrifted apart -- the same contract the fielded-equals-declared check above states.",
      "is_async": false,
      "kind": "function",
      "signature": "(fielded: dict, section: list, log: logging.Logger, *, name: str='', frag: dict | None=None) -> dict"
    },
    "Optimization/simdriver/era_coverage.py::transit_of": {
      "decorators": [],
      "doc": "The `transit_day_law` a lead block was derived from -- None when the block names no\npipeline -- so the fill can be priced at the same law the levels were declared at.\n`scale` multiplies the median (the fill curve's axis); 1.0 is the block itself.",
      "is_async": false,
      "kind": "function",
      "signature": "(lead: dict | None, scale: float=1.0) -> dict | None"
    },
    "Optimization/simdriver/leaf_scope.py": {
      "decorators": [],
      "doc": "leaf_scope \u2014 who answers a leaf's inbound and put-away questions: it, or the site.\n\n`_build_leaf` asked `site is None` / `pool is None` twenty-eight times, and every pair was the\nsame dispatch under a different name:\n\n| the question | solo | site-docked |\n|---|---|---|\n| the transit census | `mgr.transit_snapshot()` | `coord.transit_census_for(mgr)` |\n| the receiving snapshot | `mgr.receiving_snapshot()` | `coord.snapshot_for(mgr)` |\n| this batch's receipts | `mgr.drain_receiving_records()` | `coord.drain_records_for(mgr)` |\n| the dock depth | `mgr.dock_depth` | `coord.dock_depth_for(mgr)` |\n| the yard rows | `mgr.drain_yard_*()` | nothing \u2014 the SITE collects them |\n| the receiving roster | `_recv_crew.workers(uid)` | `site.workers` |\n| the put roster | `_put_crew.workers(uid)` | `pool.workers` |\n\nTwo adapters already existed \u2014 \"the leaf's own manager answers\" and \"the coordinator answers on\nits behalf\" \u2014 with no interface between them, so the dispatch was re-decided at every call site\nand correctness was held up by REFUSALS IN THE DOMAIN LAYER. Eight accessors under `Warehouse/`\nand `Inbound/` raise under a site scope, which means `Warehouse/` had to learn that a DRIVER\nconcept exists. Those refusals are still there and still right, but they are a backstop now\nrather than the mechanism.\n\n## The three parameters are a LADDER, not eight combinations\n\nThe ticket that asked for this counted `pool`, `site_gain` and `site` as three independent\n`None`-able parameters \u2014 eight nominal combinations, \"~3 legal, enforced by guards scattered\nacross `_build_site_dock`, `_build_put_pool` and `_bind_put_crews`\". Reading those guards, the\nreason only three are legal is stronger than scattering: **they form a ladder.**\n\n    SOLO      no pool, no site      an uncoupled run\n      |       `_build_put_pool` is only called on a coupled unit\n    POOLED    pool, no site         a coupled unit with inbound OFF (site-dock 06)\n      |       `_build_site_dock` reads `pool.workers[-1].uid`, so it cannot exist without one\n    DOCKED    pool and site         a coupled unit with the standing yard\n\n`_build_put_pool` never returns `None` \u2014 it raises or it builds \u2014 so `site` implies `pool`\nimplies coupled. The fourth corner (a site dock with no put pool) is not one of eight\ncombinations that happens to be guarded: it is unreachable, and here it is UNNAMEABLE, because\nthere is no class for it. `scope_for` is the only constructor and it walks the ladder.\n\n`site_gain` is not on this ladder at all. It is read at exactly one place \u2014 binding the gain\nbundle into the transit \u2014 so it stays an argument to `_build_arm` rather than becoming a fourth\nrung; a scope member consulted once is a parameter wearing a costume.\n\n## What each rung overrides\n\n`PooledScope` changes the PUT half only; a coupled unit with inbound off still answers every\ninbound question out of its own manager, byte-identically to a solo run. `SiteScope` adds the\ninbound half on top. That is why this is an inheritance chain rather than three siblings: each\nrung is the one below it with one more group of answers moved to the site, and writing it any\nother way would duplicate the put half into two classes.\n\n## The instants are preserved, including where the two poles disagree\n\n`lead_depth` and `in_transit` are read at DIFFERENT MOMENTS by the two poles, and that is kept\nexactly. The site takes all three transit numbers off one partitioning pass before\n`check_reorders` fires; a solo leaf reads them off the manager afterwards, so its values include\nthis batch's own reorders and the site's do not. That asymmetry predates this module (see\n`transit_census`' own note) and moving either read would move published numbers, so `lead_depth`\nand `in_transit` stay METHODS taking the census value rather than becoming fields resolved at\ncensus time, which is the shape that would silently align them.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope": {
      "decorators": [],
      "doc": "SOLO \u2014 the leaf's own manager answers every question, and it owns its put crew.\n\nThe base is a real rung, not an abstract interface: an uncoupled run uses this class\nunchanged, so every method here is the production answer rather than a stub to override.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.bind_put": {
      "decorators": [],
      "doc": "Nothing to bind: this leaf's manager owns its own put-away.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, channel_name: str) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.bind_receiving": {
      "decorators": [],
      "doc": "Nothing to bind: this leaf's manager owns its own dock.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, channel_regime) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.dock_depth": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.in_transit": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, census_qty)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.lead_depth": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, census_depth)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.note_put": {
      "decorators": [],
      "doc": "Nothing to report: this leaf's clocks are its own.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, put_clock) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.note_recv": {
      "decorators": [],
      "doc": "Nothing to report: an unpooled leaf's `drain_receiving_records` already reset its own\ncrew clock.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, recv_clock) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.put_base_for_batch": {
      "decorators": [],
      "doc": "The epoch this batch's put rows are stamped from.\n\nSolo the crew picks this wave's queue up when the wave is RELEASED or when it finished\nthe last one, whichever is later. Pooled it was already fixed when the site day opened,\nso the value handed in is the answer and this must not recompute it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, site_base, batch_start, put_clock)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.put_clock_for": {
      "decorators": [],
      "doc": "Which clock a caller that must name ONE epoch should use.\n\nTwo callers want this and they want the same answer: the skipped-batch close-out, and\nthe `_put_recs` empty case where a pooled leaf still owes the pool a report. Solo the\nanswer is the leaf's own clock, which makes the second one an assignment to itself --\ndeliberately, because \"nothing changes here\" is the solo behaviour being preserved.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, put_clock, put_base)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.put_uid_after": {
      "decorators": [],
      "doc": "Where the uid cursor sits after the put crews are minted.\n\nSolo, the per-queue loop already advanced it. Pooled, it must jump to the POOL's block\nend -- the pool's block starts above BOTH channels' dense picker uids, so a cursor left\nat this leaf's own `k_pickers + put_size` would put the smaller leaf's receivers inside\nthe putters' block and merge two crews in one DB.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, uid: int, put_crews: dict, first_queue: str) -> int"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.put_window": {
      "decorators": [],
      "doc": "`(base, deadline)` \u2014 the epoch put-away is charged from, and its whistle.\n\nSolo there is no base (the manager keeps its own clock) and the whistle is what is left\nof THIS leaf's day.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, day, day_end, arm_clock, put_clock)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.receiving_snapshot": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.recv_drain": {
      "decorators": [],
      "doc": "`(receipts, repacks, base)` for this batch.\n\nBoth streams must be taken in the SAME batch or the one left behind lands against the\nnext batch's epoch. `drain_repacks` deliberately does not reset the crew clocks and both\nstreams' `t0` were stamped when the work happened, so their order does not matter.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, *, arm_clock, recv_clock, day)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.standing_yard": {
      "decorators": [],
      "doc": "The censored tail at run end. `standing_yard_trailers` does not drain, so two leaves\nreading it would bill every standing trailer twice -- which is why it refuses under a\nsite scope and why this returns nothing there.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.transit_census": {
      "decorators": [],
      "doc": "`(rows, in_transit_qty, lead_queue_depth)` \u2014 the second and third are None here.\n\nA solo leaf reads those two off the manager LATER, after `check_reorders` has fired\nthis batch's reorders, so they cannot be resolved at census time without moving them.\nSee the module docstring: the two poles genuinely read at different instants and this\nmodule preserves that rather than tidying it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::LeafScope.yard_rows": {
      "decorators": [],
      "doc": "`(trailer stamps, drain levels)` for THIS leaf to record.\n\nSite-docked this is empty and the site collects instead: a trailer's load is mixed by\nconstruction and a door is occupied by the trailer rather than by either channel's share\nof it (ADR-0005). Both leaf accessors REFUSE under a site scope, so the empty pair here\nis the reason nothing calls them, not a duplicate of the refusal.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, batch_id: int)"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope": {
      "decorators": [],
      "doc": "COUPLED, INBOUND OFF \u2014 one site put crew, but every inbound question is still the leaf's.\n\nThe middle rung, and a real configuration rather than a degenerate one: the funnel runs the\ncoupled inbound-off pole in every cell (site-dock 06). Nothing inbound is overridden here, so\nsuch a run answers `transit_census`, `receiving_snapshot` and the rest exactly as a solo run\ndoes -- which is the byte-identity that pole is supposed to have, now by construction.",
      "kind": "class",
      "signature": "(LeafScope)"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, pool) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.bind_put": {
      "decorators": [],
      "doc": "Both ways: the manager routes phase 5 to the pool, and the pool learns which channel\nthis leaf is so it can hand it its share of the day.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, channel_name: str) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.note_put": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, put_clock) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.put_base_for_batch": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, site_base, batch_start, put_clock)"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.put_clock_for": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, put_clock, put_base)"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.put_clocks": {
      "decorators": [
        "property"
      ],
      "doc": "The POOL's list, shared by identity: `crew_clock.charge` books every put to whichever\nputter is free earliest across BOTH channels, and `reset` mutates in place -- which is\nwhy the pool, and not either manager, owns the reset (`reset_put_clocks`).",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.put_uid_after": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, uid: int, put_crews: dict, first_queue: str) -> int"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.put_window": {
      "decorators": [],
      "doc": "BOTH NUMBERS ARE THE SITE'S and neither is this leaf's to compute. One shared clock\nlist cannot carry two epochs, so the base is the site day's start and the whistle is what\nis left of THAT day -- never `arm_clock`, which would idle the site's putters whenever\nEITHER pick crew overran its day and would misattribute a picking overrun to put-away's\ncut. `open_batch` is idempotent per day, so both leaves get the same answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, day, day_end, arm_clock, put_clock)"
    },
    "Optimization/simdriver/leaf_scope.py::PooledScope.put_workers": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope": {
      "decorators": [],
      "doc": "COUPLED, STANDING YARD \u2014 the coordinator answers for inbound as well as put-away.\n\nThe top rung. It extends `PooledScope` rather than `LeafScope` because a site dock cannot\nexist without a put pool (`_build_site_dock` reads `pool.workers[-1].uid`), so \"site-docked\nbut unpooled\" is not a state this class can be in.",
      "kind": "class",
      "signature": "(PooledScope)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, pool, site) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.bind_receiving": {
      "decorators": [],
      "doc": "The SECOND bind, which stamps `site_scoped` on the leaf and turns the six leaf\naccessors into refusals. It must run after the transit is set and after intake has filled\n`_originals`: `bind` reads this leaf's whole catalogue partition to build the\n`{sku: leaf}` owner dict, and asserts the leaf holds the coordinator's own transit.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, channel_regime) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.coord": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.dock": {
      "decorators": [
        "property"
      ],
      "doc": "ONE dock, built above both leaves with a per-regime PRICE LIST (site-dock 27): the\nunload price is a statement about the merchandise, so the site charges each channel's own\nconstant.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.dock_depth": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.in_transit": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, census_qty)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.lead_depth": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, census_depth)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.note_recv": {
      "decorators": [],
      "doc": "THE SITE CARRY IS COMMITTED ONCE PER SITE DAY, after EVERY leaf has stamped -- and so\nis the shared crew's clock reset, which `note_records` owns because\n`drain_receiving_records` (its uncoupled owner) refuses here. `None` when this leaf\nrecorded nothing: the crew is where it was, exactly as an unpooled leaf leaves\n`recv_clock` alone.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, recv_clock) -> None"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.receiving_snapshot": {
      "decorators": [],
      "doc": "`snapshot_for` partitions once and serves each leaf once. The leaf accessor refuses\nhere because three of its four values RESET, so the first caller would take the site's\nwhole batch and leave the other channel reporting an idle dock.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.recv_drain": {
      "decorators": [],
      "doc": "BOTH STREAMS COME OFF THE SITE'S ONE DRAIN, partitioned by the owner of each row's\nsku -- the same `{sku: leaf}` dict step 1 packs by, so an unload row, a repack row and\nthe lot that produced them cannot land in different channels.\n\nAnd the BASE is the SITE DAY's, not this leaf's `arm_clock`: one crew on one dock has one\nepoch, and basing it on a pick crew's release would idle the site's receivers whenever\nEITHER channel overran its day. `open_batch` is idempotent per day, so this reads the\nvalue the drain was already run against rather than computing a second one.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, *, arm_clock, recv_clock, day)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.standing_yard": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.transit": {
      "decorators": [
        "property"
      ],
      "doc": "ONE site is ONE yard. Two transits behind one dock would give each leaf its own\ntrailers while the drain ranked only the coordinator's, and `_advance_lead_queue` would\ntick each channel's copy of the SUPPLIER lead queue -- an order placed with a 3-batch\nlead arriving in 2, on every coupled run, silently.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.transit_census": {
      "decorators": [],
      "doc": "THE CENSUS IS THE SITE'S and this leaf takes its own share. All three reads come off\nONE pass so they cannot disagree, and the split is by SKU -- the pack rule (ADR-0005) --\nso the rows and the units sum back to the site's own census exactly.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.workers": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/leaf_scope.py::SiteScope.yard_rows": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, mgr, batch_id: int)"
    },
    "Optimization/simdriver/leaf_scope.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['LeafScope', 'PooledScope', 'SiteScope', 'scope_for']"
    },
    "Optimization/simdriver/leaf_scope.py::scope_for": {
      "decorators": [],
      "doc": "The ONE constructor, and the only place the ladder is walked.\n\nA site dock with no put pool is refused rather than silently degraded: `_build_site_dock`\nreads `pool.workers[-1].uid` to mint the receiving block, so this state cannot arise from the\nbuilders -- and if it ever does, the symptom without this check is a receiving crew whose\nuids sit inside the putters' block, which merges two crews in one DB and no table says so.",
      "is_async": false,
      "kind": "function",
      "signature": "(pool, site) -> LeafScope"
    },
    "Optimization/simdriver/scenario.py": {
      "decorators": [],
      "doc": "simdriver.scenario \u2014 one scenario + the cell-matrix driver.\n\n_run_scenario is the inner pipeline for a SINGLE warehouse cell (manifest + shared\nassets + flat pool + blank-arm check); _run_whatif_matrix loops _build_cells over one\nfrozen inventory so every run is a cell matrix (a plain run is the single cell k1_off).",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/scenario.py::_run_scenario": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, pairs, regime_sizing, workers, log, *, cell='', cell_index=1, cell_total=1, frozen_by_pair=None, skip_completed=False, max_tasks_per_child=1, max_retries=2, resume_granularity='strategy')"
    },
    "Optimization/simdriver/scenario.py::_run_whatif_matrix": {
      "decorators": [],
      "doc": "Drive a cell-matrix run from a spec (see whatif_config.SPECS): every run is a matrix, so a\nplain run is the single cell ``k1_off``.  A MULTI-cell matrix freezes the sampled inventory once\n(tightest cell) and reshapes it per cell (apples-to-apples); a SINGLE-cell run skips the freeze\nand samples fresh \u2014 bit-identical to the old flat run, just nested under its cell dir.  Returns\n{'cells': [names], 'reference': name}.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir, pairs, log, spec, resume=False, max_retries=2, resume_granularity='strategy', max_tasks_per_child=1)"
    },
    "Optimization/simdriver/scenario.py::_warn_blank_arms": {
      "decorators": [],
      "doc": "Scan every sim_*.db under base_dir and log a prominent WARNING for any that\nrecorded zero batches (blank result DB).  Returns the list of blank db paths.\n\nA blank arm means the whole strategy produced no metrics \u2014 almost always a\nplacement/stocking failure (units never binned \u2192 no pick tasks \u2192 all batches\nskipped).  Downstream analysis silently omits such arms, so we flag them here.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, log: logging.Logger) -> list"
    },
    "Optimization/simdriver/section_timers.py": {
      "decorators": [],
      "doc": "section_timers.py \u2014 the per-section wall accumulator an arm keeps while it runs.\n\nWHAT IT REPLACES.  `strategy_runner._build_leaf` carried this as twenty-three closure\nvariables: eleven `t_*_ckpt` / `p*_sum_ckpt` window accumulators, the eleven `t_*_run` /\n`p*_run` whole-arm totals they fold into, and `t_save_run`, which never had a window\nbecause a checkpoint's DB write is timed inside the checkpoint block itself.  Three sites\nhad to name every section and stay in step \u2014 the declaration block, the checkpoint\nfold-and-reset, and `_finish`'s fold of the final unflushed window.\n\n# \u2500\u2500 the one design decision \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n**A TOTAL ALWAYS INCLUDES THE OPEN WINDOW.**  `roll()` closes the window; it does not\nmove a number anybody is waiting on.  That is the whole point, and it is chosen against a\nrecorded defect rather than for tidiness: a run-end fold that sits behind a condition\n(`if pb:` \u2014 true only when an unflushed batch window exists, i.e. NOT when `n_batches`\ndivides the checkpoint cadence) discards the final window in silence, and the same class\nof loss is on record against the `p1`/`p2` split, whose last window went unwritten until\nthe runtime_metrics column add.  Here there is nothing to forget: the total is right\nbefore the first roll and after the last one.\n\n`roll()` therefore exists for ONE reader \u2014 the per-checkpoint log line, which reports the\nwindow rather than the arm \u2014 and is an observability operation, not a correctness one.\n\n# \u2500\u2500 float discipline \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nThe old code was a chain of `+=`, and an archived `runtime_metrics` row has to keep\nreproducing, so the association is preserved exactly rather than approximately:\n\n    window  = running sum of this window's deltas          (one add per `add`)\n    base    = running sum of the CLOSED windows            (one add per `roll`)\n    total   = base + window                                (one add, at read time)\n\nNothing sums a list, nothing re-associates, and `roll()` on an empty window adds 0.0 \u2014\nwhich is an exact identity for every float a timer can hold.  `Tests/unit/\ntest_section_timers.py` pins all three against a plain running sum.\n\n# \u2500\u2500 the vocabulary is a contract \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`SECTIONS` names the twelve spans an arm measures.  **FOUR spellings of each span coexist**,\nand it is worth writing them out because three of the four look like each other:\n\n    span        accumulator key   log-line token   result-dict key   runtime_metrics column\n    sampling    'sample'          smpl=            't_sample'        smpl_s\n    extraction  'extract'         extr=            't_extract'       extract_s\n    bin ledger  'inv'             cons=            't_inv'           inv_s\n    reorder     'reord'           reord=           't_reord'         reord_s\n    fast_pick   'p1' / 'p2'       p1= / p2=        'p1_s' / 'p2_s'   p1_s / p2_s\n\n`totals()` emits the THIRD column \u2014 the worker RESULT-DICT keys \u2014 not the DB column names.\n`p1_s` / `p2_s` are the single case where the key and the column coincide, which is exactly\nwhy calling `t_<name>` \"the column\" reads as plausible and is wrong.\n\nTHE LAST HOP USED TO BE A HAND-WRITTEN CALL.  `record_arm` mapped result-dict keys onto\ncolumns in a 32-column INSERT written out by hand, so a section added to `SECTIONS` flowed\nautomatically into `totals()` and then **silently vanished** \u2014 writing nothing, raising\nnothing, and leaving a column reading 0.0, which is a legal measurement.  Ticket 14 made the\nfour spellings one record: `runtime_metrics.SPANS`, from which this module's `SECTIONS` and\n`COLUMNS` are both derived and from which `record_arm` builds its statement.\n`Tests/unit/test_runtime_span_table.py` asserts the table and the DDL cover each other AND\nwrites a real row to prove each span reaches its column.\n\nWHY THE TABLE LIVES IN `runtime_metrics` AND NOT HERE.  The DB column is the hard contract and\nit belongs beside the DDL \u2014 and `opt_persistence -> opt_simdriver` is a declared boundary\n(\"storing results must not depend on orchestration\"), so the dependency can only run this way.\nThis module owns the ACCUMULATOR KEY and reads the other three.\n\nThe tuple's ORDER is its own: it is neither the log line's (which puts `kf=` last where this\nhas it fifth, and prints p1/p2/db earlier still) nor the DDL's.\n\nWHAT IS AND IS NOT A CONTRACT.  The DB column must never move \u2014 a rename would shift the\n`runtime_metrics` schema id for a relabelling and break archived rows' comparability with\nthemselves \u2014 which is why `Span.label` exists: the human name tracks what a span measures and\nthe column never moves.  What is pinned HERE is the ACCUMULATOR KEY:\n`Tests/calltree/test_calltree_anchors.py` checks `SECTION_MAP` against this tuple and that\nevery declared section has a real `timers.add(...)` site.  `COLUMNS` (badly named for\nhistory) holds the two result-dict keys that are not simply `t_<name>`.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow": {
      "decorators": [],
      "doc": "The COUNTER half of the checkpoint window, beside `SectionTimers`' timer half.\n\n    win = CheckpointWindow(opened_at=time.perf_counter())\n    win.add('placed', batch_rp)          # inside the batch loop\n    ckpt_wall = win.wall(now)            # at a checkpoint: the log line's three numbers\n    ckpt_rate = win.rate(ckpt_wall)\n    avg_dur   = win.avg_dur()\n    win.roll(now)                        # close the window\n\n# -- why this one is free, and `SectionTimers` was not ---------------------------\n\nEVERY VALUE HERE DIES AT THE LOG LINE.  The five counters, the open instant, and the\nthree numbers derived from them are consumed by exactly one reader -- the checkpoint\n`log.info(...)` f-string -- and reach no database, no result dict and no\n`runtime_metrics` column.  They are not simulation numbers, so no arrangement of them\ncan move one.  `SectionTimers` could not say that: its totals are persisted.\n\n`Tests/unit/test_checkpoint_window.py` asserts the guarantee rather than trusting it --\nthe old loose names must stay gone and the window must not reach any writer -- because\nthe moment one of these values is persisted, the argument above stops holding silently.\n\n# -- why it is a second object and not folded into `SectionTimers` ----------------\n\nThe two always roll together, which normally argues for one object.  They are kept\napart because their contracts differ: a timer carries a WHOLE-ARM total that outlives\nevery window and lands in a DB column; a counter here has no run total at all.  Folding\nthem would give half the members a `total()` that means nothing, and would reopen the\n`SectionTimers` interface that `Tests/calltree/test_calltree_anchors.py` pins.\n\n# -- the arithmetic is preserved exactly -----------------------------------------\n\n`dur_sum` is a running sum in batch order (the original was a chain of `+=`); `avg_dur`\nkeeps the original's `if dur_count else 0.0` guard; and `rate` takes the ALREADY-COMPUTED\nwall rather than re-reading the clock, because the original reused its `ckpt_wall` local\n-- a `rate()` that read the clock itself would divide by a slightly later wall than the\none printed beside it.  `rate` deliberately does NOT guard a zero wall: the original had\nno guard, and adding one would hide a clock that failed to advance.\n\nSlotted and picklable, for the same reasons as `SectionTimers`.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, opened_at: float) -> None"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.add": {
      "decorators": [],
      "doc": "Add to one counter.  An undeclared name is a KeyError, never a sixth counter.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, counter: str, n) -> None"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.avg_dur": {
      "decorators": [],
      "doc": "Mean batch duration this window, or 0.0 when it timed no batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.get": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, counter: str)"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.rate": {
      "decorators": [],
      "doc": "Batches per second over `wall` -- the caller's already-computed wall.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, wall: float) -> float"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.roll": {
      "decorators": [],
      "doc": "Zero every counter and reopen the window at `now`.\n\nAll five together, as the original's one reset block did: a partial reset would\ncarry one counter into the next line and read as a spike.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now: float) -> None"
    },
    "Optimization/simdriver/section_timers.py::CheckpointWindow.wall": {
      "decorators": [],
      "doc": "Seconds since this window opened.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now: float) -> float"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers": {
      "decorators": [],
      "doc": "Two-level accumulator: an open window, and the whole-arm total that contains it.\n\n    st = SectionTimers()\n    st.add('sim', dt)          # inside the batch loop\n    st.window('sim')           # the checkpoint log line's number\n    st.roll()                  # at a checkpoint: close the window\n    st.totals()                # at run end: the runtime_metrics payload\n\nSlotted, so a mistyped attribute raises instead of shadowing a section, and picklable,\nbecause the leaf that owns one is built inside a spawned worker.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.add": {
      "decorators": [],
      "doc": "Add one measured span to the open window.  An undeclared section is a KeyError.\n\nRefused rather than created: a typo that opened a thirteenth section would\naccumulate seconds nothing ever reads and subtract them from nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, section: str, seconds: float) -> None"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.roll": {
      "decorators": [],
      "doc": "Close the open window: fold it into the base and zero it for the next one.\n\nNothing depends on this having happened \u2014 `total()` already counted the window.\nRolling an empty window is an exact no-op (`x + 0.0 is x` for every float a timer\nholds), so an extra roll cannot perturb a total.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.split": {
      "decorators": [],
      "doc": "Close the lap: charge the elapsed time to EVERY named section, reopen at the same\ninstant, and return the delta.\n\nSEVERAL SECTIONS, ONE CLOCK READ.  `build` is the sum of `sample` and `task`, so both\nof those sites charged two sections from a single `_dt` -- never two reads, which\nwould have made `build` disagree with its own parts by a few microseconds per batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, *sections: str, now: float=None) -> float"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.start": {
      "decorators": [],
      "doc": "Open a lap.  Called once at the top of each batch, before any `split`.\n\nThe cursor is the TIMERS' state, not the caller's.  `_build_leaf` used to keep it as\na seventh closure variable `_t`, and every one of the eight timing sites repeated\n`_now = time.perf_counter(); timers.add(s, _now - _t); _t = _now` -- eight chances to\nforget the advance, which silently charges the next section its own time PLUS\neverything before it.\n\n`now` is injectable so a test need not sleep; production passes nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now: float=None) -> None"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.total": {
      "decorators": [],
      "doc": "Whole-arm seconds, **open window included**.  Correct at every instant.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, section: str) -> float"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.totals": {
      "decorators": [],
      "doc": "The `runtime_metrics` half of the worker's result dict.\n\nA plain `dict` of plain `float`s: it crosses the process boundary in the worker\nresult, so no views, no defaultdicts, nothing that needs the class to unpickle.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Optimization/simdriver/section_timers.py::SectionTimers.window": {
      "decorators": [],
      "doc": "This checkpoint window's seconds \u2014 the number the per-batch log line prints.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, section: str) -> float"
    },
    "Optimization/simdriver/shift_ledger.py": {
      "decorators": [],
      "doc": "shift_ledger.py \u2014 the drain-or-cap working day's close-out state.\n\nWHAT IT REPLACES.  `strategy_runner._build_leaf` carried this as four closure variables --\n`_shift_prev_day`, `_shift_cut_today`, `_shift_last_finish`, `_shift_standing` -- written in\none block per batch and read again at run end for the final day, which has no next-day\nboundary to close it.\n\n# \u2500\u2500 the ordering rule IS the specification \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    the boundary is tested BEFORE this batch's clocks, cut and depths are folded in, so the\n    previous day closes on what ITS last batch left behind and this batch -- the first of the\n    new day -- is attributed to the new day.\n\nThat sentence was a comment in the batch loop and nothing enforced it.  Here it is the\nINTERFACE: `advance_to(day)` answers \"is there a day to close, and with what?\", and `note()`\nfolds this batch in.  Calling them in the other order mis-attributes every day's first batch,\nand `Tests/unit/test_shift_ledger.py` pins the rule with a sabotage proving the wrong order\nis actually visible.\n\n# \u2500\u2500 what each field is, and why they reset differently \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    prev_day     the day currently open; None until the first batch\n    cut_today    STICKY within a day (`cut or <this batch>`), reset at a boundary -- once a\n                 day has cut work it stays cut however many later batches fit\n    last_finish  a running MAX over the three crew clocks, reset at a boundary -- a batch\n                 whose crews finished earlier must not pull the day's finish backwards\n    standing     a LEVEL, REPLACED each batch and NOT reset at a boundary: it is re-measured\n                 every batch, so the carried value is simply the last reading until this\n                 day's first batch overwrites it.  Summing a level across batches is the\n                 `recv_cut` scar; this one is only ever assigned.\n\n# \u2500\u2500 what is deliberately NOT here \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`_shift_close_out` -- the pure function that turns a snapshot into a DB row and decides\n`drained` through `equilibrium.is_drained` -- stays in `strategy_runner`.  This object holds\nthe state that function is CALLED WITH; it makes no judgement about a day, and moving the\njudgement here would put `equilibrium` behind a second door.\n\nFLAG-OFF IS STRUCTURAL.  Every call site sits inside `if _drain_or_cap:`, so a run without\nthe era flag never constructs one and never calls it.  That is what makes this extraction\nbyte-identical by construction rather than by argument.\n\nSlotted and picklable, like the other `_build_leaf` state objects: the leaf that owns one is\nbuilt inside a spawned worker.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/shift_ledger.py::ShiftLedger": {
      "decorators": [],
      "doc": "Per-day close-out state for the drain-or-cap working day.\n\nshift = ShiftLedger()\n...\nclosed = shift.advance_to(day)        # BOUNDARY FIRST\nif closed is not None:\n    rows.append(close_out(*closed))\nshift.note(cut=..., finish=..., standing=...)   # then this batch\n...\nlast = shift.final()                  # at run end: the day nothing closed",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/shift_ledger.py::ShiftLedger.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simdriver/shift_ledger.py::ShiftLedger.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Optimization/simdriver/shift_ledger.py::ShiftLedger.advance_to": {
      "decorators": [],
      "doc": "Move to `day`.  Returns the snapshot of the day that just ENDED, or None.\n\nNone on the first batch (no previous day exists) and within a day.  On a boundary\nthe returned tuple is `(day, standing, last_finish, cut)` for the day that ended --\n`_shift_close_out`'s exact parameter order -- and the new day is opened uncut with\nno finish.  Days need not be contiguous: an empty day still consumes a release slot,\nand nothing is invented for the days in between.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, day)"
    },
    "Optimization/simdriver/shift_ledger.py::ShiftLedger.final": {
      "decorators": [],
      "doc": "The still-open day, for the run-end close-out, or None if none ever opened.\n\nThe last day of a run has no next-day boundary, so without this every era run would\nreport one day fewer than it worked and the equilibrium check's \"every day drained\"\nwould be read over a window missing its last member.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/shift_ledger.py::ShiftLedger.note": {
      "decorators": [],
      "doc": "Fold one batch into the open day.  Call AFTER `advance_to` -- see the docstring.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, cut: bool, finish: float, standing: tuple) -> None"
    },
    "Optimization/simdriver/sim_assets.py": {
      "decorators": [],
      "doc": "sim_assets.py \u2014 per-pair shared-asset construction for a run.\n\nbuild_shared_assets loads the inventory+affinity pair once, sizes the warehouse\n(Inventory_Manager.plan_warehouse with the per-regime sizing from CONFIG), persists\nthe planned inventory + warehouse stats (rename-proof fingerprint), and returns the\npicklable shared dict every strategy worker receives.  Split out of run_simulation\nso the runner holds orchestration only; run_analysis imports this directly for its\nshape-only rebuild.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/sim_assets.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Optimization/simdriver/sim_assets.py::_target_fill": {
      "decorators": [],
      "doc": "The store's sizing target for the warehouse-stats row: the DERIVED section fill when\nthe coverage record carries one (`era_coverage.stamp_fill`), else the typed fill.",
      "is_async": false,
      "kind": "function",
      "signature": "(coverage: dict | None, typed: float) -> float"
    },
    "Optimization/simdriver/sim_assets.py::build_shared_assets": {
      "decorators": [],
      "doc": "Load inventory + affinity from DB and build warehouse A.\n\nWarehouse is sized so total bins \u2265 N_SKUS \u00d7 1.1 (minimum replicas of the\n60-type layout satisfying that constraint).\n\nfrozen_inventory_db (what-if harness): when set, load THIS already-planned inventory and\nDO NOT re-sample \u2014 every scenario/cell shares one frozen sampled inventory, so the batch\nfingerprint (which depends on the sampled SKUs' freq/qty) is identical across layout cells.\nThe warehouse SHAPE still comes from regime_sizing (the cell's aisle_split/zoning), only the\nsampled inventory is held fixed.  None \u21d2 current behavior (byte-identical).\n\ncoverage_record (the REBUILD path): one finished run's own\n`staffing.calibration[<pair>].coverage` block.  A rebuild loads that run's CATALOGUE, which\nsince ADR-0002 carries no stock level, and the warehouse is sized from levels on every path\n-- so a rebuild must re-declare before it plans, and must declare what the RUN declared.\nPassing the record re-declares from it exactly (`era_coverage.declare_from_record`); passing\nNone on a path that samples is normal (the run derives its own), and on a path that does not\nmeans \"the orders already carry a declaration\", which is true of a frozen inventory.",
      "is_async": false,
      "kind": "function",
      "signature": "(inventory_db: str, affinity_db: str, log: logging.Logger, max_skus: int | None=None, max_aisles: int | None=None, max_bins: int | None=None, min_bins: int | None=None, composition: dict | None=None, regime_sizing: dict | None=None, keyframe_interval: int=CONFIG['global']['keyframe_interval'], warehouse_db_path: str | None=None, frozen_inventory_db: str | None=None, coverage_record: dict | None=None) -> dict"
    },
    "Optimization/simdriver/sim_assets.py::build_shared_assets._agg_cap": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(key)"
    },
    "Optimization/simdriver/sim_assets.py::build_shared_assets._build": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cfg)"
    },
    "Optimization/simdriver/sim_assets.py::build_shared_assets._plan": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bucket_hold: dict | None=None)"
    },
    "Optimization/simdriver/sim_assets.py::load_run_inventory": {
      "decorators": [],
      "doc": "The ONE loader both the parent (`build_shared_assets`) and the workers\n(`strategy_runner`) use for a run's inventory.\n\nIt is a thin pass-through today, and the reason it survives as a named seam is the reason\nit stopped branching.  It used to CLEAR the `pipeline_qty` stamp on a flag-off load, so the\nmanager's `rp x lead / (lead + 1)` heuristic stood byte for byte whatever file was handed\nin.  That guard existed because the era was the only regime that declared its own levels;\nflag-off inherited the catalogue's authored ones and had to stay byte-identical with the\narchive.  Since ADR-0002 there is ONE planner contract: every run declares its levels at\nsetup, in days, and stamps the lead pipeline that goes with them (\"Field the floor\",\ndecision 6).  Honouring the stamp in one mode and discarding it in the other would field a\nlevel whose reorder point encodes a LINE while pricing its pipeline by a heuristic that\nassumes the reorder point encodes lead-time demand -- the exact defect the stamp was\nintroduced to fix.  The era flag now decides only whether the clock cuts and caps.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, limit: int | None=None)"
    },
    "Optimization/simdriver/strategy_runner.py": {
      "decorators": [],
      "doc": "strategy_runner.py \u2014 concurrent strategy worker for run_simulation.py.\n\nSeparates all parallel/CPU machinery from the configuration, analysis, and\nplotting logic in run_simulation.py.\n\nPublic API\n----------\n_run_strategy_worker(args) -> dict\n    Simulate one assignment strategy end-to-end (one process in the flat pool\n    owned by run_simulation._run_workers_flat).\n\nsave_worker_checkpoint(run_dir, strategy, next_batch_id)\nload_worker_checkpoint(run_dir, strategy) -> int\n    Per-strategy crash-recovery checkpoints written inside each worker.\n\nImplementation notes\n--------------------\n_run_strategy_worker must be a module-level function so ProcessPoolExecutor\ncan pickle it by reference in Windows spawn mode.  The child resolves it by\nqualified name (Optimization.simdriver.strategy_runner._run_strategy_worker): spawn's\nprepare() propagates the parent's sys.path \u2014 which the entry script seeded\nwith the repo root \u2014 before anything is unpickled, so this module needs no\nsys.path bootstrap of its own.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/strategy_runner.py::ArmAssembly": {
      "decorators": [],
      "doc": "Everything one ARM holds for the length of a run -- built once, then read.\n\n`_build_leaf` bound 139 distinct names at one indent level, and the three closures it\nreturns reached back into them: 76 of those names are DURABLE (a closure reads or\nwrites one) and the other 60 are construction temporaries that nothing outside the\nsetup ever looks at. Nothing sat between \"the whole arm\" and a 600-line closure body,\nso every behavioural test of a batch had to hand-assemble a run tree -- five e2e\nharnesses totalling ~2,000 lines exist for that reason, and one of them says so.\n\nThe split is computed rather than judged: DURABLE means a closure touches it. Which\nis also why this list is 76 names and not the ~40 the ticket estimated -- an arm\nreally does carry that much, and seeing it is the point.\n\n## What this is NOT\n\nNot a bag of parameters. The closures no longer declare `nonlocal` at all: a carry\nbetween batches (`arm_clock`, `put_clock`, `recv_clock`, `last_dur`, `skipped`,\n`_pending`) is an attribute write, so \"this value survives the batch\" is stated by\nwhere it lives instead of by a declaration in each closure that happens to mention it.\n\nSlotted: one per leaf, and a stray attribute would be a name that looks like arm state\nand is not.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/strategy_runner.py::ArmAssembly.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, buf, n_catalogue, n_skus, _cut_at_day_end, _drain_or_cap, _fs_w, _gc_detail, _gc_stats0, _gc_thresh, _pending, _pick_workers, _put_crews, _put_workers, _recv_day, _recv_workers, _release, _roll_over, _seed_terms, _shift_seconds, _space_tl, affinity, arm_clock, audit, batch_cfg, batches, bin_rec, checkpoint, ckpt_win, cov, ctx, db_path, denom, expected_pick, fi, freq_by_idx, freq_by_sku, inventory, k_pickers, keyframe_interval, kf_db, last_dur, lift_cache, log, map_lap_pct, mgr, n_batches, opt_x, opt_y, pb, pe, pick_cfg, pk, pm, pq, pqs, pt, put_clock, qty_by_sku, recv_clock, reloader, run_dir, run_id, scope, sd, seed_batches, shift, skipped, start_i, strat, strategy, t_loop, t_precompute, timers, warehouse, we, wp, yd, yt)"
    },
    "Optimization/simdriver/strategy_runner.py::_CKPT_REPLACE_ATTEMPTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "5"
    },
    "Optimization/simdriver/strategy_runner.py::_CKPT_REPLACE_DELAY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.1"
    },
    "Optimization/simdriver/strategy_runner.py::_GC_THRESHOLD_DEFAULT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "gc.get_threshold()"
    },
    "Optimization/simdriver/strategy_runner.py::_Leaf": {
      "decorators": [
        "_dataclass(frozen=True)"
      ],
      "doc": "One channel leaf of a work unit: what it is, and its two halves as callables.\n\n`step(i)` runs batch `i`; `finish()` does the run-end flush and returns the leaf's result\ndict. Both close over `_build_leaf`'s setup, so the leaf's manager, crews, clocks and\naccumulators are reachable only through them -- which is the point: a driver sequences\nleaves and does not reach into one. A site-scoped coordinator (site-dock 01/04) is built\nABOVE the leaves and injected through their payloads, never fished out of one here.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/strategy_runner.py::_POLICY_STATE_NAMES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset(('aisle_' + b for b in _AisleLedger.POLICY_BOOKS))"
    },
    "Optimization/simdriver/strategy_runner.py::_SiteDock": {
      "decorators": [],
      "doc": "The SITE's inbound, at UNIT scope: one dock, one yard, one coordinator, one DB.\n\nThe receiving twin of `_build_put_pool`'s `PutawayPool`, and built for the same three\nreasons: the crew is the pair's derived receiving crew (ONE site total, which the\nleaves no longer carry), the uid block chains off the put pool's so a receiver is the\nsame person in both DBs, and one yard is one yard -- two leaves each ticking their own\nsupplier-lead queue would land every order a batch early (`SITE_PHASES`).\n\nIt also owns the coupled unit's THIRD OUTPUT, the contract's `site_inbound_db`\n(ADR-0005).  A trailer's load is mixed by construction and a door is occupied by the\ntrailer rather than by either channel's share of it, so those rows belong to neither\nleaf -- writing them to `leaves[0]` would be `a-right-site-total-hides-two-wrong-shares`\nbuilt deliberately, which is exactly why site-dock 21 parked them and left the artifact\nto this ticket.",
      "kind": "class",
      "signature": ""
    },
    "Optimization/simdriver/strategy_runner.py::_SiteDock.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, coord, workers, release, db_path: str, log)"
    },
    "Optimization/simdriver/strategy_runner.py::_SiteDock.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Optimization/simdriver/strategy_runner.py::_SiteDock.collect": {
      "decorators": [],
      "doc": "Take this batch's SITE-scoped yard rows off the coordinator and the transit.\n\nEvery batch, unconditionally -- all three sources RESET, so a collection that rode a\ncondition would compound rows and hand whatever finally reads them several batches'\nworth stamped as one.  The leaves' own `drain_yard_*` accessors REFUSE under a site\nscope, so there is exactly one collector and it is this one.\n\nTHE SITE DOCK'S OWN TOTALS come with the coordinator's day index on them and are\nre-stamped with `i` only after the two are checked to agree.  Both are `day_of(i)`\nby construction (`drive` opens the day the release schedule names), and the pool\nrefuses any grid that is not one batch per site day -- so the check costs nothing\nand is the one place that structural promise is READ rather than restated.  A\nsilently re-indexed row would land the site's day-3 labour under batch 1 and put\nthe closure check's per-batch localisation onto the wrong batch for the whole run.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_SiteDock.drive": {
      "decorators": [],
      "doc": "Run site day `i`'s seven phases across every bound leaf; return what each\ntriggered, keyed by `id(leaf)`.\n\nTHE WHOLE COMPOSITION, ONCE -- never each leaf's own `check_reorders`.  Phase 5 is\nrouted from inside `SiteReceiving.drain`, which is the only placement that puts the\nsite's receive BEFORE every leaf's put drain; run through the leaves' own\ncompositions instead and the earlier leaf's put drain fires before the site has\nunloaded anything, so its receipts wait a whole batch for a bin (site-dock 21's\nhand-off list says this in as many words).\n\nBoth deadlines are SITE values and both accessors are idempotent per site day, so\nasking here and asking again inside a leaf returns the same answer rather than two.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int, pool) -> dict"
    },
    "Optimization/simdriver/strategy_runner.py::_SiteDock.finish": {
      "decorators": [],
      "doc": "The censored tail, then one write of the whole run's site rows.\n\nTHE TAIL IS READ HERE AND NOWHERE ELSE.  `standing_yard_trailers` does not drain,\nso two leaves reading it would bill every trailer still on site at run end twice --\nwhich is why that accessor refuses under a site scope too, unlike its two\ndrain-shaped neighbours.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_arm_expected_pick": {
      "decorators": [],
      "doc": "The arm's expected day under ITS OWN initial placement, or None flag-off.\n\nThe pair's demand was derived at setup from the class-uniform expectation\n(`workunits._derive_staffing_for_pair`); this is the SAME closed form\n(`simconfig/expected_travel`) over the bins the arm just filled, at the pair's derived\nline count and spread, stamped for the throughput audit's per-arm band\n(`equilibrium.arm_expectations`).  It sizes nothing.  Flag-off (no derived block in\nthe payload) it computes nothing and returns None, so the store-only path is untouched.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict, warehouse, orders, pick_cfg, log) -> dict | None"
    },
    "Optimization/simdriver/strategy_runner.py::_build_arm": {
      "decorators": [],
      "doc": "Construct one arm: everything a leaf holds for the length of a run.\n\nThis was the first 810 lines of `_build_leaf`, which bound 139 names at one indent and\nthen handed the two batch closures back into that scope. The split is where CONSTRUCTION\nSTOPS and STEPPING STARTS: 76 of those names are durable (a closure touches one) and\nbecome the returned `ArmAssembly`; the other 60 are construction temporaries and stay\nhere, where nothing outside can see them.\n\n**It is a callable seam, which is the point.** Every behavioural test of a batch used to\nneed an e2e harness that hand-assembled a run tree -- five of them exist, ~2,000 lines,\nand `test_channel_runner_smoke.py` says out loud that they duplicate the driver's own\nassembly because the assembly had no entry. This one costs a payload dict.\n\nThe four arguments are `_build_leaf`'s, unchanged and for the same reasons: `unit` is the\nunit-scope payload under coupling (the put and receiving crews are the SITE's), `pool` the\nsite put pool, `site_gain` the site gain bundle, `site` the site dock.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict, unit: dict | None=None, pool=None, site_gain=None, site=None) -> ArmAssembly"
    },
    "Optimization/simdriver/strategy_runner.py::_build_arm._arm_aisle_state": {
      "decorators": [],
      "doc": "Rebuild per-aisle affinity + demand/labor state from the placed bins.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_build_leaf": {
      "decorators": [],
      "doc": "One channel leaf, built but not yet run \u2014 the setup half of a work unit.\n\nEverything here is per channel and stays so under coupling: one inventory partition, one\nwarehouse view, one manager, one set of crews, one DB, one resume plan.  What it returns\nis the leaf's two halves as closures over that setup \u2014 `step(i)`, one batch, and\n`finish()`, the run-end flush and the result dict \u2014 so a caller can drive ONE leaf (every\nrun today) or step two leaves through one batch loop (the coupled unit, site-dock 02 \u00a72).\n\n`pool` is the SITE PUT POOL (`Inbound.putaway_pool.PutawayPool`), built above the leaves\nand injected: when it is present this leaf's put queues are bound to the pool's shared\nclock list rather than minting their own, its put rows are stamped with the pool's\nworkers and the pool's site-wide epoch, and phase 5 of every batch belongs to the pool.\nNone is every uncoupled run and every flag-off leaf, which then fields its own put crew\nexactly as it always did \u2014 double count and all, structurally, because the pool is not\nCONSTRUCTED rather than constructed and bypassed.\n\n`site_gain` is the SITE GAIN BUNDLE (`Inbound.gain.SiteGainBundle`), built above the\nleaves the same way and for the same reason: one mixed trailer is priced per unit by\nthe OWNING channel's arm, and only a scope that sees both leaves can hold both arms.\nThis leaf binds its own bundle into it and hands the composite to its transit. None is\nevery uncoupled run, which wraps its one bundle in a `OneOwnerBundle` exactly as it\nalways did \u2014 so a single-owner run reads the very object `_gain_bundle_for` returned.\n\n`site` is the SITE DOCK (`_SiteDock`), built above the leaves for the third time and\nfor the third version of the same reason: one dock, one yard and one receiving crew\nserve the whole site, so only a scope that sees both leaves can build them once. When\nit is present this leaf receives the site's dock, the site's transit and the site's\nreceiving roster instead of minting its own, binds itself onto the coordinator, and\nstops running phases 0-5 in its own `replenish` \u2014 the unit drives them once for every\nleaf (`SiteReceiving.drain`). None is every uncoupled run AND the coupled inbound-OFF\npole, which names no trailer pipeline and therefore has no site inbound to build.\n\nThe closures are why the setup is not forked: `_run_strategy_worker_impl` used to be this\nfunction with the loop inline, and the split moved the loop body and the tail VERBATIM.\nThey rebind the enclosing locals through `nonlocal`, which is what makes a batch's carries\n(`arm_clock`, `put_clock`, `recv_clock`, the checkpoint accumulators) survive between\ncalls exactly as they survived between iterations.\n\nUses DeferredPickSimulation for parallel Phase-1 picker execution within\neach batch.  Log records travel through a multiprocessing.Queue to the\nQueueListener in the main process so they appear in real time.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict, unit: dict | None=None, pool=None, site_gain=None, site=None) -> '_Leaf'"
    },
    "Optimization/simdriver/strategy_runner.py::_build_leaf._finish": {
      "decorators": [],
      "doc": "The run-end half: flush the unflushed window, close the last day, censor the\nstanding yard, release the graph, and return this leaf's result dict.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Optimization/simdriver/strategy_runner.py::_build_leaf._note_triggered": {
      "decorators": [],
      "doc": "Record what the SITE drain fired for THIS leaf.  Coupled units only.\n\n`SiteReceiving.drain` keys its result by `id(leaf)` because a manager is not\nhashable by value there; the leaf knows which manager is its own, so the lookup\nhappens here rather than the driver guessing at an order.",
      "is_async": false,
      "kind": "function",
      "signature": "(trig: dict) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_build_leaf._replenish": {
      "decorators": [],
      "doc": "Batch `i`'s REPLENISHMENT half, for this leaf: the release instant, the two\nwhistles, the standing-demand injection, the re-slot and `check_reorders`.\n\nSPLIT OUT SO EVERY LEAF REPLENISHES BEFORE ANY LEAF PICKS.  The site put pool\ndivides one day between the channels and then re-drains both against the whole\nof it, so an earlier leaf can spend what a later one did not -- and the later\nleaf's queue does not exist until its own phases 1-3 have run.  With the whole\nbatch in one call that residue pass would land after the earlier leaf had\nalready snapshotted its queues and stamped its rows: the placements would be\nreal, the rows would be stamped a day late, and `put_queue_state` would report\na queue depth that was never standing.  Nothing would raise.\n\nOne leaf calls this and `_step` back to back, which is the loop this was cut\nout of, line for line.",
      "is_async": false,
      "kind": "function",
      "signature": "(i: int) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_build_leaf._step": {
      "decorators": [],
      "doc": "Batch `i`, for this leaf. Was `for i in range(start_i, n_batches):`; the body\nbelow is that loop's, unchanged, so a carry rebound here is rebound in the\nenclosing setup scope exactly as it was between iterations.",
      "is_async": false,
      "kind": "function",
      "signature": "(i: int) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_build_put_pool": {
      "decorators": [],
      "doc": "The SITE's put crew, for a coupled unit: one clock list, one roster, one carry.\n\nBuilt at UNIT scope and above both leaves, because that is the only scope that can see\nboth: the crew comes from the pair's derived block (ONE site total, which the leaves no\nlonger carry at all), the uid block has to clear BOTH channels' dense picker uids, and\nthe day's division needs both channels' recorded expectations.  A leaf cannot compute\nany of the three, which is why the pool is injected into it rather than fished out of\nit (site-dock 04 section 5).\n\nTHE BLOCK STARTS AT `max(k_pickers)`.  Every existing analysis assumes `actor_uid ==\npicker_id` for pickers, and the two channels have different picker counts, so a put\nblock chained off either leaf's own pickers would give the same physical putter two\ndifferent uids in the two DBs -- and any site-level rollup joining on `actor_uid` would\nmerge two different people.  The smaller leaf gets a GAP in its uid space instead,\nwhich nothing reads (`put_rows` bounds-checks `widx` against the roster, `work_events`\nhas no uniqueness constraint).  A dense-but-lying uid is worse than a sparse-but-true\none.\n\nTHE WORKING DAY IS READ ONCE, from leaves that are checked to agree.  It reaches the\nworker per leaf (`work_day_spec()` rides `_shared`), so both copies come from one\naccessor -- but \"both copies come from one accessor\" is a fact about the parent, and a\ncoupled unit assembled by hand could carry two.  The pool divides ONE day.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict)"
    },
    "Optimization/simdriver/strategy_runner.py::_build_site_dock": {
      "decorators": [],
      "doc": "The SITE's dock, yard and receiving coordinator -- or None when the unit fields none.\n\nNone is the coupled INBOUND-OFF pole, which the funnel runs in every cell (site-dock\n06): no trailer type is named, so no transit exists, each leaf keeps its own batch lead\nqueue byte-identically and `check_reorders` stays each leaf's own composition.  That is\na real configuration, not a degenerate one, so it is a return rather than a refusal.\n\nEverything else about a coupled unit's inbound IS refused rather than degraded, because\nevery degradation here is the double count with a coupled label on it:\n\n  * a NON-STANDING trailer pipeline -- the v1 drain runs through each manager's own dock\n    deque, so two leaves would field two docks and two crews under one site's declared\n    one, and no table would say so;\n  * a standing yard with NO receiving crew -- a yard nobody unloads produces no receipts\n    at all, silently, for the whole run;\n  * two leaves carrying DIFFERENT inbound specs -- one site is one yard, and two specs\n    would be two arrival calendars over one set of doors.\n\nTHE PRICE LIST IS BUILT HERE (site-dock 27).  The unload price is a statement about the\nMERCHANDISE, so the dock holds one `UnloadCost` per regime, each derived from ITS OWN\nchannel's pick config through the same picking -> put-away -> receiving chain a leaf\nbuilds for itself.  The two constants that exist today ARE the two entries, which is\nwhy an uncoupled run is untouched: it constructs no list at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict, pool, log)"
    },
    "Optimization/simdriver/strategy_runner.py::_build_site_gain": {
      "decorators": [],
      "doc": "The SITE's gain bundle, for a coupled unit \u2014 or None when nothing would read one.\n\nOne `SiteGainBundle` above both leaves, into which each leaf binds the bundle\n`_gain_bundle_for` built for IT (site-dock 05 decision 1).  It has to be created here,\nat unit scope, for the same reason the put pool does: the leaves are built one at a\ntime and the first leaf's transit needs the object the second leaf will bind into.  The\nowners themselves cannot be built here \u2014 a bundle is made out of one leaf's manager,\nstrategy and workload params, which do not exist until `_build_leaf` runs.\n\nNONE UNLESS A GAIN POLICY IS NAMED, exactly as the per-leaf injection is gated: the\nseeded fifo/lifo keys never read a bundle, so a composite over arm machinery nothing\nconsumes would be unconsumed infra (memory `gpu-broker-dormant-not-for-placement`).\nA leaf that names no gain policy binds no owner, and a mixed load whose regime has no\nowner then REFUSES at `for_key` rather than being priced under the other channel's arm.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict)"
    },
    "Optimization/simdriver/strategy_runner.py::_check_declared_crew": {
      "decorators": [],
      "doc": "Refuse a payload whose sized crews are not its declared (or derived) crews.\n\n`k_pickers` is the count the worker sizes its pick crew from; `args['staffing']` is the\nstaffing record -- its INPUTS (`sim_config.staffing_spec`), keyed here by the worker's\nchannel, plus under the calibrated era the pair's `derived` block, whose put and\nreceiving crews the payload's `put_crew` / `recv_crew` records must match.  Every one of\nthese was read or derived in the parent from the same source, so they can only disagree\nwhen a caller assembled the payload by hand -- and that caller would otherwise run a\nwhole arm under a crew its run spec never declared.  An absent record (a bench harness,\na test predating it) is nothing to check against.  Module-level so a unit test can hand\nit a payload without running an arm.\n\n`site_crews=False` checks the pick crew ONLY.  A coupled unit's put and receiving crews\nare the SITE's and live at unit scope, not on either leaf (site-dock 02 section 6), so\nthe unit checks them once -- `_check_site_crews` -- and each leaf checks its own pickers,\nwhich really are per channel.  Checking them per leaf is what guarded the double count\ninto place.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict, k_pickers: int, *, site_crews: bool=True) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_check_site_crews": {
      "decorators": [],
      "doc": "The put and receiving half of the crew check, over whatever scope OWNS those crews.\n\nOne leaf today, so `args` is the leaf payload and this runs inside `_check_declared_crew`\nexactly as it always did.  On a coupled unit the site crews sit at UNIT scope and the\ndriver calls this once with the unit payload -- which is the whole point of site-dock 02\nsection 6: two leaves each verifying the site total against the record is what made the\ndouble count look correct.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_cleanup_checkpoints": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::_declared_pools": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{p.key for p in _POLICY_BY_KEY.values() if p.gain == 'pool'}"
    },
    "Optimization/simdriver/strategy_runner.py::_futuresight_window": {
      "decorators": [],
      "doc": "Batch i's futuresight window: the demand of script batches `i+1 .. i+w`\n('all' = to the run's end), each `.items` dict COPIED \u2014 `batches[j]` is the\nSHARED pickle every arm of the family reads, and a reference travelling into\nthe frozen view would let any downstream mutation corrupt every sibling arm's\ndemand silently.  The copies guard the shared objects; they are not a cache.\n\nClamped at `n_batches`, not `len(batches)`: past the run's end nothing\nreleases, so the tail \u2014 and w=0 \u2014 yield `()`, an empty window, never an\nerror.",
      "is_async": false,
      "kind": "function",
      "signature": "(batches, i: int, w, n_batches: int) -> tuple"
    },
    "Optimization/simdriver/strategy_runner.py::_futuresight_window_w": {
      "decorators": [],
      "doc": "The futuresight window width \u2014 `int` batches or `'all'` \u2014 or None when neither\nstanding policy names the arm.  This is the arm's REFUSAL-UNTIL-CLEAN gate\n(\"Define the inbound objective\" decision 6), checked once at worker startup:\n\n* the knob unset is a config error, never a silent default \u2014 an unlawful\n  reference arm must be run on purpose, with its window stated;\n* `batches` None means this worker fell back to INLINE batch sampling\n  (fingerprint miss / missing script), and a window sampled inline would be a\n  silently different future than the one the run releases.  The script is\n  REQUIRED; the run refuses before simulating a single batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec, batches)"
    },
    "Optimization/simdriver/strategy_runner.py::_gain_bundle_for": {
      "decorators": [],
      "doc": "The gain evaluator's faithful-to-arm bundle \u2014 driver-built, driver-injected\n(`mgr.transit.gain_bundle`), because `Inbound -> wh_inventory / wh_placement` are\nforbidden edges: the broker holds what it is handed (the `drain_sku` precedent).\n\nThe fidelity seam per arm FAMILY (prototype ticket 04): tmin/tmax ride the proven\nk-cheapest merge; rank_popularity gets its own pool rebuilt over aisle-state\ncopies (its selector must read the COPY of aisle_demand_sum, not the live dict);\nrank_random gets the pool with a deterministic stand-in selector and expectation\npricing over the aisle heads (an ordering entry may consume no RNG \u2014 the decided\ndeviation 04 recorded); fifo gets the uniform adapter, which needs nothing from\nhere at all.  Any other placement family FAILS LOUDLY: an evaluator that cannot\nrebuild the arm's pool over copies would price a fiction under that arm's name,\nand phase 2's top-k should extend this map consciously, not silently.\n\nWhich is what the last three branches are (ticket 20).  Phase 1 ranked, and\n`rank_minlabor` / `rank_labor` / `rank_cartlabor` are the families its top five\nnamed that this map did not already serve -- 08's cap of three, exactly consumed.\nThey arrive on the SAME pool adapter as rank_popularity and through their own\nbuilders, so nothing about fidelity is new; what IS new is that their pools commit\nto live aisle bookkeeping beyond the three dicts the older families touch, and\n`aisle_state` is where each declares its own.  A dict left off that list is not a\nrefusal, it is a virtual placement advancing the REAL warehouse -- so the list is\nread off the pool's `take`, not off the builder's signature (`_sku_pick_load_product`\nand `_sku_vol_product` are handed over LIVE below precisely because `take` only\nreads them).\n\nThe two arm-level values hoisted out of the per-evaluation path belong to the same\nreading.  `_make_pool` rebuilds the policy for every virtual placement, so anything\nthe builder computes ONCE per arm would be recomputed O(yard^2) times per drain: the\ntravel-balanced geometry memo, and rank_cartlabor's `total_freq`, whose own docstring\nforbids moving it into the pool.  Both ride optional builder keywords that every\nproduction call leaves at None.",
      "is_async": false,
      "kind": "function",
      "signature": "(strat, mgr, sctx, wp, put_speed, spec) -> '_GainBundle'"
    },
    "Optimization/simdriver/strategy_runner.py::_gc_cb": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(phase, info, _st=_GC_STATE)"
    },
    "Optimization/simdriver/strategy_runner.py::_map_lap_pct": {
      "decorators": [],
      "doc": "Share of assigned UNITS the optimal map solved exactly, or None for a non-map arm.\n\nUnits rather than classes: at catalogue scale the classes small enough for the exact\nsolver hold a tiny slice of the inventory, so a class-weighted figure would read far\nmore favourably than the placement it produced.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr) -> float | None"
    },
    "Optimization/simdriver/strategy_runner.py::_peak_rss_mib": {
      "decorators": [],
      "doc": "This process's peak working-set (high-water RSS) in MiB, or None if unreadable.\n\nStdlib-only by design \u2014 psutil is deliberately not a production dependency.  Peak (as\nopposed to current) RSS is only knowable from the OS: Windows tracks it in\nPROCESS_MEMORY_COUNTERS.PeakWorkingSetSize; POSIX reports it as ru_maxrss (KiB on\nLinux, bytes on macOS \u2014 normalised here).  One syscall, called once per arm.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> float | None"
    },
    "Optimization/simdriver/strategy_runner.py::_peak_rss_mib._PMC": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": "(ctypes.Structure)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_rank_minlabor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sctx, mgr)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_rank_minlabor._factory": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cands, state, wp_local)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_rank_popularity": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sctx, mgr)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_rank_popularity._factory": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cands, state, wp_local)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_rank_random": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sctx, mgr)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_rank_random._factory": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cands, state, wp_local)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_travel_balanced": {
      "decorators": [],
      "doc": "`rank_labor` and `rank_cartlabor`: one pool class, one seam, the cart term apart.\n\n`_aisle_pick_load_sum` is the LPT balance's running total and `_aisle_vol_sum` the\ncart's \u2014 both committed to by `take`, so both are in the record's `ledger_terms` and\ntherefore copied; `_sku_pick_load_product` / `_sku_vol_product` are per-SKU constants it\nonly reads, and stay live.",
      "is_async": false,
      "kind": "function",
      "signature": "(cart: bool)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_travel_balanced._make": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sctx, mgr)"
    },
    "Optimization/simdriver/strategy_runner.py::_pool_travel_balanced._make._factory": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(cands, state, wp_local)"
    },
    "Optimization/simdriver/strategy_runner.py::_run_strategy_worker": {
      "decorators": [],
      "doc": "Simulate one assignment strategy in its own process.\n\nThin wrapper whose ONLY job is the finally: the impl freezes the startup object graph\nfor the duration of the batch loop (see the gc.freeze block there), and the thaw +\nGC-callback removal must happen on EVERY exit path.  Production workers die with\ntheir process either way; the guarantee exists for in-process callers (the calltree\nfullfid tier, e2e tests) where a mid-arm raise would otherwise leave the HOST process\nwith a permanently frozen heap and a leaked gc callback.  Both cleanups are no-ops on\nthe happy path (the impl already thawed and removed).",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict) -> dict"
    },
    "Optimization/simdriver/strategy_runner.py::_run_strategy_worker_impl": {
      "decorators": [],
      "doc": "One work UNIT end-to-end \u2014 the body behind _run_strategy_worker.\n\nA unit is one or more channel leaves driven through ONE batch loop. Every run today\nhands it a single leaf and this is the loop that function always had. A coupled unit\n(site-dock 02) hands it two, and the leaves then step in lockstep: leaf B cannot begin\nbatch i before leaf A has finished it, which is what lets a site-scoped coordinator sit\nbetween them and what makes a torn pair impossible to produce.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict) -> dict"
    },
    "Optimization/simdriver/strategy_runner.py::_shift_close_out": {
      "decorators": [],
      "doc": "Close working day `day`: the ledger row `(day, cap_end, end_s, drained,\nstanding, standing_put, standing_dock, standing_carry, standing_carry_labour,\nstanding_carry_supply, last_finish)`, and the log line.  A day DRAINED if nothing\nwas cut in it, no standing LABOUR survives it -- put queues + held + the dock floor\n+ the cut's own carry (`unpicked_daycut`) -- and no task finished past its cap\n(START-gate overtime is labour that did not fit the day).  Never the lead queue (transit is\ncalendar, not labour; and releases are exhausted by construction at a day\nboundary), and never the SUPPLY carry (`unpicked_unavailable` /\n`unpicked_unstocked`: stock not delivered, which `missed_share` judges -- counted as\nstanding work it made every finite stock level cap every day).\n`equilibrium.is_drained` is the one definition; this closure only gathers its\narguments.  The end instant is `timeline.shift_end`'s arithmetic; days stay\norigin-aligned, so this is a REPORT of when the crews got off the clock, never a\nscheduler.\n\nThe state is PASSED IN, never read live: the close-out fires at the first batch of\nthe NEXT day, after that batch has already been processed, so the manager's live\ndepths and the folded clocks belong to the new day by then.  The caller hands it\nthe snapshot the previous day's last batch left behind (`_shift_standing`) and the\nclocks and cut flag accumulated before this batch was folded in.  The log-only\nledger read them live and mis-attributed every day's first batch to the day before\n-- day 0's `last_finish` read 2x its cap and the final day's read 0.0, which\npersisting the row was what made visible.  Called once per day boundary, and once\nmore after the loop for the final day, which has no next day to close it.",
      "is_async": false,
      "kind": "function",
      "signature": "(day: int, standing: tuple, last_finish: float, cut: bool, *, release, log) -> tuple"
    },
    "Optimization/simdriver/strategy_runner.py::_site_unload_cost": {
      "decorators": [],
      "doc": "ONE channel's entry in the site dock's price list (site-dock 27).\n\nThe SAME chain a leaf builds for its own dock, evaluated against THIS channel's pick\nconfig: pickers -> put-away (`PutawayCost.from_pick`) -> receiving\n(`UnloadCost.from_putaway`), then the inbound spec's own coefficients overlaid.  Built\nfrom the leaf payload rather than copied off the leaf, because the dock exists before\nany leaf does -- and because re-deriving it here from the same inputs is what makes the\ntwo entries provably the two constants an uncoupled run already charges.",
      "is_async": false,
      "kind": "function",
      "signature": "(leaf_args: dict)"
    },
    "Optimization/simdriver/strategy_runner.py::_timed_build": {
      "decorators": [],
      "doc": "Run the strategy's build hook and return its wall seconds.\n\nSETUP, not loop time.  The batch loop's clock (`t_loop` .. `elapsed`) starts long\nafter this, so every section column in runtime_metrics excludes it \u2014 which is why the\nMap family's offline address-map solve was, for a long time, an unmeasured cost that\nthe published pages could only describe qualitatively.  It is a real number now, and\n`runtime_metrics.OUTSIDE_TOTAL` is where it says it is not part of `total_s`.\n\nFor rules with no build step this is a few microseconds of function call; the number\nis still recorded, because \"measured, and it was nothing\" is a different statement\nfrom \"never measured\".",
      "is_async": false,
      "kind": "function",
      "signature": "(strat, mgr, ctx) -> float"
    },
    "Optimization/simdriver/strategy_runner.py::_work_day_of": {
      "decorators": [],
      "doc": "The working day a payload describes, as a `WorkDay`.\n\nONE expression, two callers: a leaf builds its release schedule from it and the site\nput pool divides it.  Two constructions of the same day would be two days the moment\none of them picked up a fallback the other did not, and the symptom would be a put\ncrew whose whistle was minutes away from its own pickers'.",
      "is_async": false,
      "kind": "function",
      "signature": "(args: dict)"
    },
    "Optimization/simdriver/strategy_runner.py::close_skipped_batch": {
      "decorators": [],
      "doc": "Everything a batch that produced no tasks still owes.\n\nReturns `(batch_stats_row, work_event_rows, put_clock)`.\n\nA batch with no tasks still HAPPENED.  `check_reorders` runs above the skip guard and may\nhave put hundreds of units away -- measured at 66-426 records per batch from batch 1 on.\nTwo things used to ride on the bare `skipped += 1; continue`:\n\n  * those put records were never drained, so they carried into the NEXT batch's drain and\n    were stamped against the next batch's epoch;\n  * no `batch_stats` row was written, which is why the absolute axis could not be\n    recovered downstream by a cumsum.  The runner's comment described that as the design;\n    it was the leak.\n\nThe row is zero-duration and zero-items, and `arm_clock` is deliberately NOT advanced:\nthere is no principled duration for an empty batch until a release schedule exists to say\nwhat a day-slot costs.  So the skip still precedes the advance.\n\nA MODULE-LEVEL FUNCTION, not an inline branch, because inline it was reachable only by a\nfull sweep -- and no arm in the coverage sweep ever skips a batch, so it had no coverage\nat all.  Source-scanning it instead does not work: `if False:` and a commented-out call\nboth still contain the strings a scan looks for, and two sabotages proved it.\n\nTHE TWO POOLED PARAMETERS.  `put_base` overrides the crew's epoch with the SITE's --\none shared clock list cannot carry two epochs -- and `reset_clocks=False` leaves the\ncrew standing for the pool to reset once both leaves have recorded.  The defaults are\nevery uncoupled run and reproduce this function's whole history; a skipped batch still\nput hundreds of units away, so it needs both exactly as the picked path does.\n\n`lead_depth` is the third, and it is a SITE-DOCK parameter rather than a pooled one:\n`mgr.lead_queue_depth` reads the transit's own census, and under coupling that transit\nis the site's ONE yard, so both leaves would report every in-flight entry on the site\nand a sum across the pair would double it (ADR-0005 rejected exactly that shape).  The\ndriver passes this leaf's own share; None is every uncoupled run.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, batch_id, mgr, arm_clock, put_clock, k_pickers, run_id, demanded, sigma_fd, reload_moves, reorder_placements, skus_reordered, units_ordered, put_workers, put_crews, shift_seconds, put_base=None, reset_clocks=True, lead_depth=None)"
    },
    "Optimization/simdriver/strategy_runner.py::load_worker_checkpoint": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, strategy: str) -> int"
    },
    "Optimization/simdriver/strategy_runner.py::placement_fingerprint": {
      "decorators": [],
      "doc": "A rename-proof hash of a placement: `bin_map[sku] = [(aisle_id, bayX, bayY, qty), ...]`.\n\nSorted by SKU and by location, so two managers holding the same units in the same bins\nagree whatever order they were filled in; sixteen hex digits, the size of the geometry\nfingerprint it sits beside.  An empty placement hashes to a fixed value rather than\nraising: an arm that placed nothing still stamps.",
      "is_async": false,
      "kind": "function",
      "signature": "(bin_map: dict) -> str"
    },
    "Optimization/simdriver/strategy_runner.py::reset_strategy_db": {
      "decorators": [],
      "doc": "Discard a partially-run strategy's outputs so it restarts bit-identically from batch 0\n(strategy-level resume granularity).  Removes its sim_<key>.db, that db's keyframe sibling,\nand its checkpoint.  Runs in the PARENT before any worker reopens the file (Windows-safe).",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, db_path: str, strategy: str) -> None"
    },
    "Optimization/simdriver/strategy_runner.py::save_worker_checkpoint": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, strategy: str, next_batch_id: int) -> None"
    },
    "Optimization/simdriver/supervisor.py": {
      "decorators": [],
      "doc": "simdriver.supervisor \u2014 the crash-recovery pool driver + finalize gate.\n\nOwns the flat ProcessPoolExecutor lifetime: submit every work unit, isolate ordinary\nper-unit failures, rebuild + resubmit on a BrokenProcessPool (hard worker death) up to\nmax_retries, and finalize a group ONLY when every arm succeeded (so a crashed group\nstays resumable).  _run_strategy_worker is the only pickled callable (imported from\nstrategy_runner, unchanged).",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/supervisor.py::_finalize_config_run": {
      "decorators": [],
      "doc": "Post-completion: write sim_meta.json, THEN remove resume file + checkpoints.\n\nTHE ORDER IS THE SAFETY.  A kill inside this function must leave a directory that is\nstill either resumable or complete \u2014 never neither.  The skip guard\n(`workunits._build_work_units`) reads \"complete\" as *sim_meta.json present AND\nresume.pkl absent*, so writing the marker FIRST makes the crash window hold a dir with\nBOTH files: not complete (so it is re-planned) and still resumable (so `_load_resume`\nanswers).  `_plan_strategy_start` then takes its documented \"resumed done arm\" branch\n\u2014 ckpt == n_batches, so reuse prev_id, start n_batches, empty loop, re-finalize.\n\nThe reverse order removed resume.pkl FIRST and left a dir that was NEITHER: the guard\nsaw no sim_meta.json and declined to skip, `_load_resume` returned None because the\nfile was already gone, and the fresh-run branch therefore ran `create_run` over a\nPOPULATED db with no `reset_strategy_db`.  That corruption has no symptom \u2014 `find_run`\nresolves `ORDER BY run_id LIMIT 1`, so every run_id-filtered query would answer from\nthe ABANDONED run and every unfiltered aggregate over the file would double.\nNo new state and no new code path \u2014 only the sequence.\n\nReturns the sim_result dict (subset of sim_skeleton without inv_db/aff_db).",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_skeleton: dict) -> dict"
    },
    "Optimization/simdriver/supervisor.py::_finalize_ready_groups": {
      "decorators": [],
      "doc": "Finalize every group whose members have ALL succeeded and isn't finalized yet.",
      "is_async": false,
      "kind": "function",
      "signature": "(meta, done_uids, finalized, log, cell='')"
    },
    "Optimization/simdriver/supervisor.py::_run_pool": {
      "decorators": [],
      "doc": "One ProcessPoolExecutor lifetime over `remaining` [(uid, args)].  Returns\n(failed_uids, broke).  A genuine success adds uid to done_uids and finalizes its group once\nALL members succeeded; a hard worker death (BrokenProcessPool) sets broke and abandons the\nrest so the driver can rebuild + resubmit; an ordinary worker Exception is isolated.  `cell`\nis prefixed on every per-arm tag so interleaved multi-cell output is attributable.",
      "is_async": false,
      "kind": "function",
      "signature": "(remaining, meta, max_workers, recycle, log, done_uids, finalized, cell='', run_root=None)"
    },
    "Optimization/simdriver/supervisor.py::_run_workers_flat": {
      "decorators": [],
      "doc": "Flat ProcessPoolExecutor pool with automatic crash-recovery.\n\nAll (pair, config, channel, strategy) units share one pool.  A worker that raises is\nisolated; a hard worker death that BREAKS the pool triggers a rebuild + resubmit of the\nunfinished units (each resumes from its on-disk checkpoint), up to max_retries.  A group's\nsim_meta.json is written only when ALL its strategies genuinely succeed, so a crashed arm\nkeeps its resume.pkl and stays resumable.  The Manager/QueueListener live out here so worker\ndeaths + pool rebuilds don't touch shared logging.\n\nGraph generation is decoupled: run run_analysis.py <base_dir> afterwards.",
      "is_async": false,
      "kind": "function",
      "signature": "(pairs: list, base_dir: str, shared_by_pair: dict, max_workers: int, log: logging.Logger, cell: str='', cell_index: int=1, cell_total: int=1, max_tasks_per_child: int | None=1, skip_completed: bool=False, max_retries: int=2, resume_granularity: str='strategy') -> None"
    },
    "Optimization/simdriver/supervisor.py::_supervise": {
      "decorators": [],
      "doc": "Bounded retry driver: on a hard worker death, rebuild the pool and resubmit the\nunfinished units (each resumes from its on-disk checkpoint), up to max_retries.  Ordinary\nper-unit exceptions are NOT auto-retried (near-always deterministic bad-config).  Quarantine\n+ continue: never abort the run, never infinite-loop; unrecovered units keep their resume\nstate and are reported with a resume command.\n\nWORKER RECYCLING IS PINNED AT 1 \u2014 one fresh process per job.  `max_tasks_per_child` was\naccepted but never forwarded (`_run_whatif_matrix` dropped it, and every run is a matrix),\nso every run in this repo's history has in fact recycled at 1.  The first run that actually\nhonoured a larger value DEADLOCKED at the cell boundary: cell 1 finished all 244 arms, then\nthe pool sat at zero CPU with one live worker of eighteen and never shut down\n(comparison_whatif_20260815_234253, recycle=6).  The identical profile at recycle=1 the day\nbefore completed both cells (comparison_whatif_20260814_083723).  A worker holding a queue\nof jobs is a bad trade in multi-hour code regardless: the measured saving is ~10 s of spawn\nper job against a job that runs for minutes, and workers reload their assets per job anyway.\nSo the value is fixed here rather than plumbed, and the CLI flag warns if asked for more.",
      "is_async": false,
      "kind": "function",
      "signature": "(pairs, base_dir, shared_by_pair, max_workers, log, *, log_queue, max_tasks_per_child, skip_completed, max_retries, resume_granularity, cell='', cell_index=1, cell_total=1)"
    },
    "Optimization/simdriver/supervisor.py::_tag_of": {
      "decorators": [],
      "doc": "Standardized per-arm/per-group log tag: [cell/pair/config[/channel][/strategy]].",
      "is_async": false,
      "kind": "function",
      "signature": "(cell, key)"
    },
    "Optimization/simdriver/workunits.py": {
      "decorators": [],
      "doc": "simdriver.workunits \u2014 plan the per-strategy work units for the flat pool.\n\nBuilds one work unit per (pair, config, channel, strategy) arm: shared assets +\nbatches + resume-aware start planning + the picklable strategy_args dict shipped to\neach worker.  Runs only in the parent process.",
      "kind": "module",
      "signature": null
    },
    "Optimization/simdriver/workunits.py::_SITE_CREW_KEYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('put_crew', 'recv_crew')"
    },
    "Optimization/simdriver/workunits.py::_arm_db_path": {
      "decorators": [],
      "doc": "`<channel run dir>/sim_<arm>.db` \u2014 ONE spelling of an arm's result DB.\n\n`_prepare_channel_run` builds the map the workers are handed and `_reconcile_coupled_unit`\nhas to name the same files to discard them, so the two read it from here.  A second\nspelling would not fail: it would remove nothing and leave the arm's rows to be appended\nto on the replay, which is the silent-doubling shape `_plan_strategy_start`'s duplicate-run\nrefusal exists to make impossible.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, arm: str) -> str"
    },
    "Optimization/simdriver/workunits.py::_arm_position": {
      "decorators": [],
      "doc": "The batch this arm would resume AT, from its on-disk state.\n\nThe advanced per-arm checkpoint when there is one, else the counter the last prepare\nrecorded in the leaf's resume file.  ONE definition, because `_plan_strategy_start` decides an\narm's branch from it and `_reconcile_coupled_unit` decides whether a coupled pair's two\nleaves AGREE from it \u2014 and two leaves compared by a different rule from the one that\nplans them is a comparison that can be true while the plan disagrees.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, arm: str, prev_start: int=0) -> int"
    },
    "Optimization/simdriver/workunits.py::_build_work_units": {
      "decorators": [],
      "doc": "(Re-)prepare all work units from on-disk state.\n\nReturns (work_units, meta):\n  work_units : list of (uid, args) where uid = (label, cfg_name, channel, strategy) and\n               args carries its own `group_keys` + `arm_key` (the parent reads those, not\n               uid slices \u2014 the uid's slots are not stable across unit kinds).\n  meta       : {group_key: {'sim_skeleton', 'members'}}; group_key = uid[:3].  A group is\n               finalized only when EVERY member uid succeeds (see _run_pool) \u2014 so a crashed\n               arm never finalizes its group, keeping resume.pkl and staying resumable.\nRe-derives each arm's start from its ADVANCED _ckpt_*.pkl, so resubmit/resume is idempotent.\n\n`mid_flight` says the POOL IS STILL UP and this is the supervisor's rebuild-and-resubmit\nafter a hard worker death, not a `--resume` of a dead run.  Only the coupled reconciler\nreads it, and only to decline a repair whose outputs nothing would rebuild \u2014 see\n`_reconcile_coupled_unit`.  Default False, so an uncoupled run never reaches it.",
      "is_async": false,
      "kind": "function",
      "signature": "(pairs, base_dir, shared_by_pair, log, log_queue, max_workers, skip_completed, resume_granularity, mid_flight=False)"
    },
    "Optimization/simdriver/workunits.py::_channel_batch_plan": {
      "decorators": [],
      "doc": "(batch_cfg, seed_batches, channel_regime) for one channel-run's batch stream.\n\nMixed catalogue: the channel's own BatchConfig over its regime's SKU count, its own seed\noffset, its regime as the precompute filter.  Store-only: the pair-level shared\nBatchConfig (= the store channel's shape), the base seed, no filter -- byte-identical\nto the pre-channel pipeline.  ONE helper for the same reason as\n`_worker_inventory_args`: two callers computing the plan is two fingerprints.",
      "is_async": false,
      "kind": "function",
      "signature": "(ch, inventory, mixed: bool, shared: dict) -> tuple"
    },
    "Optimization/simdriver/workunits.py::_channel_runs_for": {
      "decorators": [],
      "doc": "Plan the channel-runs for one catalog.\n\nStore and fulfillment sweep their OWN config sets independently (a UNION, not a cross\nproduct): every STORE_CONFIGS entry drives a store channel-run, and \u2014 only when the\ncatalog is mixed (contains fulfillment units) \u2014 every FULFILLMENT_CONFIGS entry drives a\nfulfillment channel-run.  A store-only catalog yields just the store runs (byte-identical\nto the pre-fulfillment pipeline).\n\nReturns (mixed, [(channel, cfg), ...]) where each channel carries its own pick cost + pool.",
      "is_async": false,
      "kind": "function",
      "signature": "(inventory) -> tuple[bool, list[tuple]]"
    },
    "Optimization/simdriver/workunits.py::_check_campaign_pin": {
      "decorators": [],
      "doc": "Refuse a pair whose fresh derivation is not the one the campaign pinned.\n\n`pin` is `run_spec['staffing']['pin']` -- `whatif_config.PHASE2_STAFFING_PIN`, stamped on\nthe run at launch, and None on every run that is not a phase-2 campaign, which is the\nno-op path.  The digest is `staffing.pin_digest` over a rounded projection of the derived\nblock (\"Re-size the funnel in site days\").\n\nTHIS IS THE HALF `validate_spec` CANNOT DO.  The spec check fires before a directory\nexists, so all it can see is that a pin was declared; the derivation is per pair and only\nexists once the catalogue is read, the warehouse built and the script precomputed -- here.\nTwo refusals, one question: is phase 2 running the warehouse phase 1 ranked?\n\nA pin that names no labels at all, or does not name THIS one, is a refusal too. Phase 1\nranked the pairs it ran; a pair phase 2 adds afterwards was never ranked, and letting it\nthrough unchecked is the silent case the pin exists to end.",
      "is_async": false,
      "kind": "function",
      "signature": "(pin: dict | None, label: str, derived: dict) -> None"
    },
    "Optimization/simdriver/workunits.py::_derive_staffing_for_pair": {
      "decorators": [],
      "doc": "Run the calibrated era's staffing derivation for ONE inventory pair.\n\nReturns `(channel_runs, derived, calibration)`: the channel-runs with their batch\nfractions REPLACED by the derived batch content (a store-only pair also gets\n`shared['batch_cfg']` replaced, since that is the plan its workers read), the derived\nblock (`staffing.derive`), and the `calibration` block -- which, since \"Derive the\nexpected-travel closed form\", records HOW the constants were computed (the method,\nthe placement distribution, the geometry fingerprint, any declared override) rather\nthan which record they were copied from.  There is no calibration record.\n\nTwo stages, because the script is derived from the pickers and the crews from the\nscript (`Optimization/simconfig/staffing.py`):\n\n  A. per channel, from the CATALOGUE and the BUILT GEOMETRY: the class-uniform\n     placement distribution over the section's packs (`expected_travel.PlacementDist\n     .uniform` -- arm-independent, and the FIFO restock's steady state), the\n     expected day at every line count, and the FIXED POINT `n` at which that day\n     fills the declared crew's capacity (`expected_travel.solve_n`); `s_pick` is the\n     expected seconds per unit AT that point, the daily demand its units, and the\n     batch content follows.  A declared `--s-pick-*` override replaces the fixed point\n     with the demand that number buys, and the expectation is still recorded beside it.\n  B. per channel, from the SCRIPT: precompute the batches under the derived content\n     (the same call `_prepare_channel_run` makes, so the cache is warm for it), read\n     them back, total the demand, the implied reorders' packs priced with the\n     expected put travel (`expected_travel.put_site_pricer`) and their exact unload\n     seconds, then size the two site crews.\n\nThe pricing config of a channel is its FIRST config (`store` / `ful_calibrated`).\nWarnings, never failures: a saturated batch (the crew asks for more lines than the\ncatalogue has SKUs), an unpriced section (empty), SKUs the packer placed nowhere.",
      "is_async": false,
      "kind": "function",
      "signature": "(shared: dict, channel_runs: list, mixed: bool, pair_dir: str, log: logging.Logger, workers: int=1) -> tuple"
    },
    "Optimization/simdriver/workunits.py::_forget_arms": {
      "decorators": [],
      "doc": "Drop `arms` from one leaf's resume record so the planner starts them FRESH.\n\n`reset_strategy_db` removes the arm's db and its checkpoint but cannot touch the resume\nrecord, and the record is the planner's fallback: `_arm_position` reads the checkpoint\n`or prev_start`, so an arm whose checkpoint was just deleted would be planned at the\ncounter the LAST prepare wrote -- typically `n_batches` -- and would then run an empty\nloop over a database that no longer exists.  Nothing raises; the arm simply produces no\nrows.  Dropping the entry makes `prev_id` None, which is the fresh branch.\n\nThe file is removed outright when no arm survives it, so the leaf reads as a new run\nrather than a resume of nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, arms) -> None"
    },
    "Optimization/simdriver/workunits.py::_leaf_is_complete": {
      "decorators": [],
      "doc": "Today's completeness test for ONE channel-run dir \u2014 no new marker.\n\nThe marker present AND the resume file absent, which is the pair of facts\n`supervisor._finalize_config_run` writes in that order (site-dock 16 closed the window\nwhere a kill could leave neither).  A coupled unit needs nothing recorded beyond this:\nfinalize runs only when every member uid succeeded, so TWO finalized leaves cannot exist\nwithout a successful unit, and the pair of markers already carries the fact a third one\nwould record.  A third marker could also tear in its own right -- a kill after both leaves\nfinalize but before it is written re-runs a FINISHED pair -- and adding a third thing to\nkeep in sync is not how two things being out of sync gets fixed.\n\nONE function, and the uncoupled skip guard reads it too: the coupled rule is this same\ntest applied to two directories instead of one, and two spellings of \"complete\" would be\ntwo things to keep in step over exactly the question this file is reconciling.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str) -> bool"
    },
    "Optimization/simdriver/workunits.py::_meta_path": {
      "decorators": [],
      "doc": "The completeness marker of ONE channel-run dir.  The declaration's name is spelled\nHERE and nowhere else in this module -- `runschema`'s ratchet counts every hand-written\ncopy of a contract path, comments included, and three readers of one marker is three\nplaces to edit when the tree moves.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str) -> str"
    },
    "Optimization/simdriver/workunits.py::_plan_strategy_start": {
      "decorators": [],
      "doc": "Decide (run_id, start_batch) for one strategy, honoring resume granularity.\n\n- Fresh run / a strategy new on resume \u2192 init DB + create_run, start 0; RAISES if\n  the arm's DB already holds a run (see the branch \u2014 a second run in one file is a\n  silent corruption, never a recoverable state).\n- Resumed done arm (checkpoint \u2265 n_batches) \u2192 reuse run_id, start n_batches (empty loop).\n- Resumed PARTIAL arm (0 < ckpt < n_batches):\n    strategy granularity \u2192 reset the arm's DB + fresh run_id, start 0 (bit-identical to an\n                           uncrashed run \u2014 no un-replayed physical state);\n    batch granularity    \u2192 reuse run_id, start = ckpt (fast, but NOT bit-identical \u2014 warn),\n                           and REFUSED outright when the carry is on, when a dock exists,\n                           or when the arm is a LEAF OF A COUPLED UNIT (see below).\n\nTHE COUPLED REFUSAL IS THE STRONGEST OF THE THREE, and independent of the other two\n(site-dock 10 section 4).  Two leaves resume from two independently written\n`_ckpt_<arm>.pkl` files in two directories, so a batch-level resume could lawfully start\nthem at DIFFERENT batches -- and one batch is one site day (`simconfig/staffing.py`, which\nrefuses channels with differing batch counts outright).  A site day half-run in one channel\nand not the other is not a degraded run; it is a run whose shared dock, put pool and recv\nclock never existed.  It is stated separately rather than left to the other two because\nNEITHER of them is a statement about coupling: `receiving` evaporates if a derived crew\never rounds below 1, and `roll_over` is a work-day knob any cell may clear, so today's\ncoverage is a coincidence nobody would notice losing.\n\nThere is no assertion anywhere downstream that the two leaves' batch-level starts agree,\nand there must not be: refusing here is what makes the case unreachable, and asserting\nabout it would imply it is not.\n\nTHE CARRY MAKES BATCH-LEVEL RESUME LOSE DEMAND, not just precision.  `_pending` -- the\nunits a day cut or a stock clamp rolled into the next batch -- lives in the worker's\nlocals and is in no checkpoint.  Resuming at batch N therefore discards everything the\npre-crash run had carried, and the resumed stream never asks for it again.  That is not\nthe \"un-replayed physical state\" the warning describes: a resumed run would report BETTER\nthroughput than it earned, because the work it failed to do stopped being counted, which\nis precisely the error `items_demanded` exists to make impossible.  So with rollover on\nit raises instead of warning.  Strategy granularity -- the default -- is unaffected: it\nreplays from batch 0, and a carry that never happened cannot be lost.",
      "is_async": false,
      "kind": "function",
      "signature": "(ch_run_dir, s, n_batches, db_path, run_params, identity, granularity, prev_id, prev_start, is_resume, log, roll_over: bool=False, receiving: bool=False, coupled: bool=False)"
    },
    "Optimization/simdriver/workunits.py::_prepare_channel_run": {
      "decorators": [],
      "doc": "Pre-initialise ONE channel-run (one config driving one channel): create DBs, get\nrun_ids, build strategy_args for the flat ProcessPoolExecutor.\n\nStore and fulfillment sweep their own config sets independently, so a run drives a single\nchannel.  ``mixed`` is True when the catalog holds more than one regime \u2014 the run then\nfilters inventory to ``channel.regime``, draws its own batch stream, and writes a\n``<config>/<channel>/`` subtree.  A store-only catalog (``mixed`` False) collapses to the\nlegacy ``<config>/`` layout + shared precomputed batch stream (byte-identical).\n\nReturns (strategy_args_list, [sim_result_skeleton]).  log_queue is NOT set \u2014 the caller\ninjects it before submission.\n\n``coupled`` says this run is one leaf of a COUPLED unit (`_prepare_site_run`).  It changes\nnothing here except the resume grain the planner will accept: a leaf may not resume at a\nbatch, because its sibling's checkpoint lives in another directory and the two would start\non different site days.  Default False, so every uncoupled caller is byte-identical.",
      "is_async": false,
      "kind": "function",
      "signature": "(channel, cfg: dict, mixed: bool, shared: dict, pair_dir: str, log: logging.Logger, workers: int=1, resume_granularity: str='strategy', coupled: bool=False) -> tuple[list, list]"
    },
    "Optimization/simdriver/workunits.py::_prepare_channel_run._decomp": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(lbl: str) -> dict"
    },
    "Optimization/simdriver/workunits.py::_prepare_channel_run._yardsticks": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(orders_subset, x_speed, y_speed, wp_)"
    },
    "Optimization/simdriver/workunits.py::_prepare_site_run": {
      "decorators": [],
      "doc": "Pre-initialise ONE pair's COUPLED runs: two channel leaves per work unit.\n\n`_prepare_channel_run` survives unforked and is called once per channel (site-dock 02\nsection 3) -- each leaf genuinely needs its own run dir, batch stream, pick config, wp,\nyardsticks and DB, and every one of those stays per channel under coupling.  What this\nadds is only the pairing: the two channels' ordered arm lists zipped into arm PAIRS, the\nsite crews lifted out of both leaf payloads to unit scope, and the two group keys carried\non the unit so no parent-side reader has to slice a uid for them.\n\nReturns (unit_args_list, sim_skeletons) in the same shape `_prepare_channel_run` returns,\nso `_build_work_units` composes them identically.",
      "is_async": false,
      "kind": "function",
      "signature": "(channel_runs, mixed: bool, shared: dict, pair_dir: str, log: logging.Logger, workers: int=1, resume_granularity: str='strategy') -> tuple[list, list]"
    },
    "Optimization/simdriver/workunits.py::_reconcile_coupled_unit": {
      "decorators": [],
      "doc": "Is this coupled pair complete \u2014 and if it is torn, REPAIR it.  Site-dock 10 sections 1-3.\n\n`leaves` is `[(channel_run_dir, [arm_key, ...]), ...]` in the declared channel order\n(store first), positionally aligned: arm rank r of one leaf pairs with rank r of the\nother, which is exactly how `_prepare_site_run` builds the units.  Returns True only when\nevery leaf is complete, i.e. when `_build_work_units` may skip the pair.\n\nTHE FOUR STATES.  Both complete -> skip.  Neither started, or both mid-flight and in step\n-> nothing to do; the ordinary per-arm resume below handles them.  The other two are the\nones this function exists for:\n\n  * TORN -- one leaf finalized and the other not.  Reachable only by a kill between the\n    two `_finalize_config_run` calls, and the repair is whole-dir: a finalized leaf has\n    had its checkpoints cleaned, so there is no per-arm position left to compare and every\n    arm of BOTH leaves replays.  The complete leaf's marker is removed, which is what\n    un-finalizes it.\n  * SKEWED -- the two leaves' copies of one arm disagree about where they are.  Each leaf\n    writes its own `_ckpt_<arm>.pkl` inside one batch loop (`strategy_runner`: leaf A\n    saves, then leaf B), so a kill between the two saves leaves the pair one checkpoint\n    apart.  That rank replays in both leaves.\n\nTHE REPLAY IS FORCED, NOT CHOSEN.  The leaves step in lockstep over a shared dock, a\nshared put clock and a shared receiving clock, so leaf B cannot be stepped without leaf A\nbeing stepped.  There is no version of this where the leaf that got further is spared; the\nonly question was whether its output is discarded CLEANLY or left to collide with the\nreplay, and that question has one answer.  The reset is exact -- the same reset\n`_plan_strategy_start` already applies to a partial arm, which replays bit-identically\nfrom batch 0 -- which is why refusing the resume outright would buy nothing.\n\nTHE SKEW IS WHY THIS IS NOT OPTIONAL.  `_run_strategy_worker_impl` refuses a unit whose\nleaves do not share one batch range, and that refusal cannot clear itself: a leaf already\nat `n_batches` is planned as a done arm and never reset, while its sibling resets to 0, so\nevery later `--resume` reproduces the same disagreement and the pair is wedged for good.\n\nTHE SITE DB IS THE UNIT'S THIRD OUTPUT and joins the reset -- for every rank that\nREPLAYS, not only for the ranks that disagree.  `reset_strategy_db` knows an arm's\n`sim_<arm>.db`, its keyframe sibling and its checkpoint; it does not know the site DB, so\na replayed unit would append a SECOND run's trailer and drain rows to it, and `find_run`\nresolves the OLDEST run -- so every site yard figure would render over the abandoned one.\nA pair in step but PARTIAL is not torn and not stale, and it replays all the same.  It is the only such artifact -- every pack-denominated\nreceiving quantity lives in its own channel's sim DB (ADR-0005), and the run layout's\n`coupled` marker is written once per run by the parent, not per unit -- so the reset surface\nis exactly three things per leaf plus one per rank.\n\n`mid_flight` is the supervisor's RETRY, not a resume of a dead run.  A tear seen there\nmeans a `_finalize_config_run` raised while the pool was still up, and every unit is\nalready in `done_uids` -- so a repair would delete the output of units that will never be\nresubmitted.  The tear is reported and left for the next `--resume`, where the units are\nplanned again and the repair is safe.\n\nIT IS NOT SILENT.  `run.log` is the only place a multi-hour run's damage is visible, and\n\"a finished leaf was discarded and replayed\" is a line a reader must be able to find.",
      "is_async": false,
      "kind": "function",
      "signature": "(pair_dir, leaves, n_batches, log, tag='', mid_flight=False) -> bool"
    },
    "Optimization/simdriver/workunits.py::_record_coverage": {
      "decorators": [],
      "doc": "Record this pair's COVERAGE block in the run spec, in every mode.\n\nA run declares its own stock levels whether or not the era flag is on (ADR-0002), and the\nonly thing that varies between one run's declaration and another's is the fixed point's\nline count -- so this block is what lets anything reproduce the levels the run FIELDED.\n`run_analysis` and `run_map_precompute` re-plan a finished run's warehouse from its\noriginal catalogue, which holds no level, and re-declare from exactly this record\n(`era_coverage.declare_from_record`).\n\nUnder the era the record rides `_record_derived`'s calibration block and this is a no-op\non it; flag-off there is no derivation to record, and without this a flag-off run would\ndeclare levels nobody could ever reproduce -- its own analysis stage would then refuse to\nrebuild the warehouse and silently emit no graphs, which is how this was found.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, label: str, coverage: dict | None, log: logging.Logger) -> None"
    },
    "Optimization/simdriver/workunits.py::_record_derived": {
      "decorators": [],
      "doc": "Write this pair's `derived` and `calibration` blocks into the run spec's `staffing`\nrecord -- or, when the run spec ALREADY holds a derived block for the pair, check that\nthe fresh derivation agrees with it and RAISE if it does not.\n\nThe recorded block is authoritative on resume (\"Design the staffing record\", decision 3):\nan arm fields the crew it started with, so a re-derivation that disagrees -- a changed\nderivation, a changed calibration record, a changed catalogue -- is refused rather than\nquietly resuming under different crews.  A multi-cell run hits the agreeing branch on\nevery cell after the first, which is the free drift check across cells.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, label: str, derived: dict, calibration: dict, log: logging.Logger) -> None"
    },
    "Optimization/simdriver/workunits.py::_run_root_spec": {
      "decorators": [],
      "doc": "(run_root, run_spec) for a cell dir or a run root; (None, None) when no run spec\nexists (a harness that never wrote one).  The run spec lives at the RUN ROOT while\n`_build_work_units` is handed a CELL dir -- the same parent-vs-self distinction\n`run_analysis._apply_run_shape` documents.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> tuple"
    },
    "Optimization/simdriver/workunits.py::_site_db_path": {
      "decorators": [],
      "doc": "`<pair>/_site/inbound_<arm_store>__<arm_ful>.db` \u2014 the coupled unit's THIRD output.\n\nSite-dock 03 section 1: the pair is the only directory that dominates both leaves (their\nconfigs are siblings, so they share no ancestor below it), and the arm pair rides in the\nfilename stem rather than a directory, on the `strategy` precedent.  The `_` prefix is the\nrun tree's RESERVED one, so every walker skips the subtree.\n\nBuilt from `pair_dir` here rather than resolved through `runschema.resolver_for`: the\nartifact is not DECLARED yet -- that is the contract bump site-dock 24 carries with the\nwriter -- so there is no accessor to ask, and the rule this repo actually enforces is\nabout CONSUMERS walking a finished tree positionally.  This is the parent building a path\nunder a directory it owns, which is what `reset_strategy_db` does with `_ckpt_<arm>.pkl`.\nStated in ONE place so the writer reads it from here instead of spelling it a second time.\nThe stem is TWO-armed by construction, because a site is two channels (`_prepare_site_run`\nrefuses anything else); a third leaf raises here rather than quietly naming a file after\ntwo of the three.",
      "is_async": false,
      "kind": "function",
      "signature": "(pair_dir: str, arm_store: str, arm_ful: str) -> str"
    },
    "Optimization/simdriver/workunits.py::_stamp_identity": {
      "decorators": [],
      "doc": "Carry the unit's IDENTITY on its payload; return the unit's uid.\n\nA uid is positional, and its four slots mean `(pair, config, channel, arm)` only because\nevery unit today is one channel leaf.  The coupled unit (site-dock 02) is\n`(label, 'coupled', arm_store, arm_ful)` \u2014 same arity, different meanings \u2014 so a parent-side\nreader asking \"which group(s) does this finalize\" or \"which arm is this\" must not answer by\nslicing.  It reads `group_keys` / `arm_key`, which the unit states about itself.\n\nA one-leaf unit states exactly `[uid[:3]]` and `uid[3]`, so nothing moves today.",
      "is_async": false,
      "kind": "function",
      "signature": "(sa: dict, label: str, cfg_name: str) -> tuple"
    },
    "Optimization/simdriver/workunits.py::_stamp_site_identity": {
      "decorators": [],
      "doc": "`_stamp_identity` for a COUPLED unit: two group keys, two arms, one uid.\n\nThe uid is `(label, 'coupled', arm_store, arm_ful)` \u2014 arity 4, so `_tag_of` and every\nexisting key path read it unchanged, but only the first slot still means what it used to.\nThe literal `'coupled'` in the config slot is deliberate and honest (site-dock 02 section\n1): the unit is a member of NEITHER config subtree \u2014 `config` sits ABOVE `channel` in the\ntree and the two channels draw from different config sets, so its two leaves share no\nancestor below `<pair>/` \u2014 and a synthetic config name that looked real would invite a\nwalker to go looking for its directory.\n\n`arm_key` is None, not one of the two: a unit with two arms has no single arm, and a\nreader that takes one of them gets the other leaf's number with nothing to say so.\n`arm_keys` carries both, positionally with `group_keys`.",
      "is_async": false,
      "kind": "function",
      "signature": "(ua: dict, label: str, cfg_names: dict) -> tuple"
    },
    "Optimization/simdriver/workunits.py::_worker_inventory_args": {
      "decorators": [],
      "doc": "(inv_db, sku_allowlist, max_skus) exactly as the workers load inventory.\n\nWorkers load the PLANNED inventory DB (grown equilibrium_qty + cross-tier stock plans)\nwhen available so they reproduce the placement the warehouse was sized for; the\nallowlist and the limit then already live in that DB.  ONE helper, because the batch\nprecompute's fingerprint is over exactly these inputs: the derivation's precompute and\n`_prepare_channel_run`'s must load the same candidates or they compute two scripts.",
      "is_async": false,
      "kind": "function",
      "signature": "(shared: dict) -> tuple"
    },
    "Optimization/simdriver/workunits.py::put_constant": {
      "decorators": [],
      "doc": "ONE channel's put-away price, as the recorded constant (\"Give put-away a per-channel\nexpected travel\", 2026-09-08).\n\nThis was a SITE ratio, `sum(put_s) / sum(put_units)` over both channels, and it averaged a\nreal 3.4x spread away: the era re-read measured 98.5 s/unit put away on the store against\n29.2 on fulfillment.  The site TOTAL was right either way -- the ratio's denominator makes\n`units x price` sum back to `sum(put_s)` -- so the crew was correctly SIZED while both\nleaves' expected utilization sat about 0.21 outside the band, in OPPOSITE directions.\n`ScriptTotals.put_s` was already this channel's own expectation (priced by\n`expected_travel.put_site_pricer` over its own section's class-uniform destination); only\nthis constant threw the per-channel structure away.\n\n`override` is `--s-put`, which stays ONE key: an operator declaring a price is asserting a\nsingle site-wide number, which is an honest declaration rather than the derived average\nthat was the defect.  It is stamped onto EVERY channel, and each channel's own expectation\nrides along as `expected`, so the record still shows what the declaration displaced -- and\nthe two channels therefore carry different `expected` values under one flag.\n\nA section the script implies no put-away for prices at 0.0 rather than dividing by zero;\nthe caller warns, because a silent 0 under a note claiming an expected travel is a lie.\n\nModule-level and pure so it is testable without a run, and spawn-safe by construction\n(the repo's `ProcessPoolExecutor` rule).",
      "is_async": false,
      "kind": "function",
      "signature": "(totals, override) -> dict"
    },
    "Optimization/simdriver/workunits.py::refuse_unpriceable_put": {
      "decorators": [],
      "doc": "REFUSE a put-away configuration the era's closed form cannot price.\n\n`staffing.implied_reorders` prices a put as travel from the mouth plus handling at the\ndestination -- no cart-swap term, one site speed -- because that is the whole cost of a\nput on the ONE uncarted queue the era runs (`put_queue.single_queue`, `swap_coef = 0`).\nA split queue set carries per-queue crews with their own speeds and a `swap_coef` the\nrunner does bind (`strategy_runner`: `store_and_fulfillment(... swap_coef=...)`), and\nthe derivation would silently under-price both.  `run_simulation._check_era_flags`\nrefuses the two flags at the parser; this is the same refusal at the SEAM, for a\ncaller that reached the derivation without the CLI (a test harness, a programmatic\nlaunch, a spec that wrote CONFIG directly).  Pure and module-level: tested with a dict.",
      "is_async": false,
      "kind": "function",
      "signature": "(global_cfg: dict) -> None"
    },
    "Schema/__init__.py": {
      "decorators": [],
      "doc": "Schema \u2014 the shape and identity of this project's SQLite databases.\n\n`Schema/` owns the shape of each **database**.\n`Optimization/runschema/` owns the shape of the run **directory tree**.\nThey are different questions and neither imports the other. See README.md.\n\nStdlib only, and importable from every layer \u2014 which is the reason it is a top-level package\nrather than living under `Optimization/`: `context/architecture.yml` forbids\n`warehouse_core -> optimization`, and `Warehouse/generation/` writes two of the DB families.\n\nNothing is imported eagerly; take what you need:\n\n    from Schema import connect          # read_only / writer / bulk_writer\n    from Schema import shape            # canonical_shape / shape_id / diff_shapes\n    from Schema import identity         # Family / register / resolve / check",
      "kind": "module",
      "signature": null
    },
    "Schema/capability.py": {
      "decorators": [],
      "doc": "capability.py \u2014 negotiating for data that only SOME vetted schemas carry.\n\n`compat.py` splits a family's tables into a guaranteed surface (present in every vetted shape, so\na consumer may read it unconditionally) and a conditional one. This module is how the conditional\nhalf is reached: **probe, then degrade with a recorded caveat.** Never assume, and never quietly\nreturn something plausible.\n\nTwo things make that more than a `try/except`:\n\n  * **A table that EXISTS can still be empty, and empty is common.** `aisle_metrics` and\n    `reorder_queue` are in every vetted shape but are only written by strategies that maintain\n    that state. Measured across two archived what-if cells: `aisle_metrics` carries rows in 52 of\n    60 arms, `reorder_queue` in 68 of 166 \u2014 so a third of arms have the table and none of the\n    data. A probe that checks for the TABLE rather than for ROWS reports a capability that yields\n    nothing, and the UI then renders 0.0 as though it were a measurement.\n  * **Sources are not interchangeable.** Three different tables can answer \"which bins were\n    occupied at batch N\" with different fidelity AND at different instants of a batch. Picking the\n    best available one is only safe if the answer carries which one it was, so a number cannot be\n    read without its caveat.\n\nThis module owns the TYPE and the PROBE. It does not own any particular family's capabilities:\nthose live next to the DDL that defines the tables (see `Picking_Data.SIM_CAPABILITIES`), because\n`Schema/` is the stdlib-only leaf and must not learn what a warehouse is.\n\nBefore this existed the same idea was hand-rolled twice, independently, and could not be shared:\n`Visualization/readers/protocol.py` (`CAP_*` + `SqliteSimReader.capabilities`) and\n`Diagnostics/replay_run.py` (`_SOURCES` + `_has_rows` + `_occupancy`). `context/architecture.yml`\nforbids `optimization -> visualization`, so the analysis layer could not reuse either and had no\nway to negotiate at all \u2014 it could only hard-fail.",
      "kind": "module",
      "signature": null
    },
    "Schema/capability.py::Capability": {
      "decorators": [
        "dataclass(frozen=True, eq=False)"
      ],
      "doc": "One optional data source, and what it is worth.\n\n`exact`, `phase` and `caveat` are not documentation \u2014 they are the payload. A consumer that\nselects a source is expected to carry them into whatever it emits, so the number cannot be\nread without them. `Diagnostics/replay_run.py` copies them verbatim into its exported JSON;\nthat is the pattern.\n\n`table=None` marks a capability whose availability cannot be answered by a row probe (a\nsibling file exists; a derived cache is fresh). The caller supplies those to `probe(extra=...)`\n\u2014 the negotiation stays uniform even when the evidence is not a table.",
      "kind": "class",
      "signature": ""
    },
    "Schema/capability.py::Capability.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Schema/capability.py::CapabilityError": {
      "decorators": [],
      "doc": "Base for capability-negotiation failures.",
      "kind": "class",
      "signature": "(Exception)"
    },
    "Schema/capability.py::NoSourceAvailable": {
      "decorators": [],
      "doc": "Nothing in the ladder is available, and the caller declared no acceptable fallback.",
      "kind": "class",
      "signature": "(CapabilityError)"
    },
    "Schema/capability.py::best": {
      "decorators": [],
      "doc": "The first capability in `ladder` whose name is available, else None.\n\n`ladder` is ordered BEST-FIRST by the consumer, because \"best\" is a question about what is\nbeing asked: an exact end-of-batch source beats an approximate one for occupancy, and the\nordering is not a property of the capabilities themselves.",
      "is_async": false,
      "kind": "function",
      "signature": "(available, ladder)"
    },
    "Schema/capability.py::has_rows": {
      "decorators": [],
      "doc": "Does `table` exist AND carry at least one row (for `run_id`, when given)?\n\nThe `run_id` filter is not decoration: an arm that was resumed or interrupted can leave a\ntable created-but-empty for the run being replayed, and an unfiltered probe would then select\na source that folds to nothing.\n\nA missing table and an empty one both answer False, deliberately \u2014 to a consumer deciding\nwhether it can draw a panel, \"the schema predates this\" and \"this arm never wrote any\" are\nthe same fact.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, table: str, run_id: int | None=None) -> bool"
    },
    "Schema/capability.py::probe": {
      "decorators": [],
      "doc": "Names of the capabilities this database actually HAS.\n\n`extra` names capabilities the caller has already established by other means (a keyframe\nsidecar exists; a derived cache is fresh) \u2014 anything whose `table` is None.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, capabilities, run_id: int | None=None, extra=()) -> frozenset"
    },
    "Schema/capability.py::provenance": {
      "decorators": [],
      "doc": "The caveat payload to attach to whatever a consumer emits from `cap`.\n\nReturned as plain JSON-serializable data so it can go straight into an API response, an\nexported JSON file, or a figure caption without a converter.",
      "is_async": false,
      "kind": "function",
      "signature": "(cap) -> dict"
    },
    "Schema/capability.py::require": {
      "decorators": [],
      "doc": "`best`, but raising `NoSourceAvailable` naming every source that was tried.\n\nFor a consumer that cannot degrade any further and must fail loudly rather than emit\nsomething shaped like an answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(available, ladder, *, what: str)"
    },
    "Schema/compat.py": {
      "decorators": [],
      "doc": "compat.py \u2014 what may a consumer READ, given every schema its family still vets?\n\n`identity.py` answers \"is this file one we know\". That is a whole-file question, and it is the\nwrong granularity for a consumer, which reads a handful of tables out of a dozen. The gap between\nthose two granularities is where this project's remaining schema babysitting lives:\n\n    SIM_DB_FAMILY vets four ids. Three of them exist on the results drive and they differ by\n    three whole tables (bin_eviction, bin_placement, bin_inventory) and one column\n    (simulation_runs.sim_schema_id). `check()` passes all three. Nothing adapts. Today that is\n    safe only because no evaluation happens to read those tables \u2014 an accident, not a guarantee.\n    An evaluation that read `bin_placement` would be certified against the 2026-07-29 run that\n    Experiment 6 is published from, and would then fail on it.\n\n    HOW it fails depends on the loader, and both ways are bad:\n      * `load_bin_placements` has NO OperationalError guard, so it RAISES `no such table:\n        bin_placement` \u2014 loud, but only after a long analysis has already run;\n      * a `SELECT *` loader guarded with `row.keys()` (which is most of them) yields the\n        `common/frames.py` default 0.0 and PUBLISHES it \u2014 silent, and indistinguishable from a\n        measurement.\n    Declaring the read up front collapses both into one CI failure, before anything runs.\n\nSo a consumer declares what it needs (`Requires`), and two checks enforce it:\n\n  STATIC   `validate(req)` \u2014 offline, no database. Is every table/column this consumer reads\n           present in EVERY id the family vets? Runs in CI with no archive, and is the check\n           that turns \"safe by accident\" into \"safe by construction\".\n  RUNTIME  `check_requirements(path, req)` \u2014 this file, right now, at the consumer's own\n           granularity. Names the missing COLUMN, not the file's hash.\n\nAnything outside the guaranteed surface is reachable only by NEGOTIATION \u2014 probe for it and\ndegrade with a recorded caveat (`Diagnostics/replay_run._SOURCES` and\n`Visualization/readers/protocol.CAP_*` are both this pattern, hand-rolled independently).\n\nWHY HISTORICAL SHAPES ARE COMMITTED, NOT COMPUTED\n-------------------------------------------------\nA family's declared shape is BUILT from its writer's own DDL, so it is always re-derivable. A\nHISTORICAL shape is not: `Picking_Data.PRE_STAMP_SIM_SCHEMA_ID` is frozen precisely because the\nsource that produced it is gone. The intersection therefore cannot be computed from declarations\nalone, and a store of committed documents is the only way to make it computable offline \u2014 the\nsame reasoning, and the same layout, as `Optimization/schemas/run_tree/<short>.json`.\n\nAn id in `known_ids` with no committed document is UNRECOVERABLE: its shape is unknown, so the\ntrue intersection is unknowable and any surface computed without it is over-optimistic. Every\nfunction here reports that rather than quietly computing the smaller intersection.\n\nThe CLI over this module lives in `scripts/schema_report.py`, NOT here: reporting on every family\nmeans importing every writer, and `context/architecture.yml` forbids the `schema` layer from\nimporting `opt_persistence`, `generation` or `visualization`. This module takes a family NAME and\nreads the committed store; it never learns who writes what.\n\n    python scripts/schema_report.py --report                     # the surface, per family\n    python scripts/schema_report.py --capture sim_db <file.db>   # commit a historical shape",
      "kind": "module",
      "signature": null
    },
    "Schema/compat.py::RequirementUnmet": {
      "decorators": [],
      "doc": "A consumer's declared tables/columns are not all present.",
      "kind": "class",
      "signature": "(SchemaError)"
    },
    "Schema/compat.py::Requires": {
      "decorators": [
        "dataclass(frozen=True, eq=False)"
      ],
      "doc": "The tables and columns ONE consumer reads out of ONE family.\n\nDeclared next to the code that does the reading, so the declaration and the query move\ntogether. `label` names the consumer and goes in every message \u2014 \"Performance_Evaluations\ncontext\" is what a reader needs to see, not a stack trace.",
      "kind": "class",
      "signature": ""
    },
    "Schema/compat.py::Requires.missing_from": {
      "decorators": [],
      "doc": "Which of this consumer's needs `surface` does not satisfy, as readable clauses.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, surface: dict) -> list"
    },
    "Schema/compat.py::SHAPES_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(os.path.dirname(os.path.abspath(__file__)), 'shapes')"
    },
    "Schema/compat.py::UncommittedShape": {
      "decorators": [],
      "doc": "A writer is producing files whose declared shape has no committed document.\n\nThe window this names: a DDL edit ships, new files stamp the new id and READ fine (they match\nthe declaration) \u2014 but the shape exists nowhere except in the live source, so the moment the\nDDL moves again it is unrecoverable, and meanwhile the OUTGOING id fell out of\n`supported_ids()`, quietly orphaning every file already on the archive.  `--sync` closes the\nfirst half, `--adopt` the second; this exception is how a WRITER finds out at write time\ninstead of a reader months later.",
      "kind": "class",
      "signature": "(SchemaError)"
    },
    "Schema/compat.py::UnrecoverableShape": {
      "decorators": [],
      "doc": "A vetted id has no committed shape document, so the guaranteed surface is unknowable.",
      "kind": "class",
      "signature": "(SchemaError)"
    },
    "Schema/compat.py::_surface": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(shape: dict) -> dict"
    },
    "Schema/compat.py::check_requirements": {
      "decorators": [],
      "doc": "RUNTIME check against one real file. Returns its schema id; raises `RequirementUnmet`.\n\nComplements `identity.check()` rather than replacing it: that one asks whether the FILE is\nvetted, this one asks whether it can answer THIS consumer's questions. A file can be vetted\nand still lack a table a particular caller needs \u2014 which is exactly the case `known_ids`\ncreates and the reason this function exists.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, req: Requires, *, verify: bool=True) -> str"
    },
    "Schema/compat.py::conditional_surface": {
      "decorators": [],
      "doc": "`{table: frozenset(columns)}` present in SOME vetted shape but not all.\n\nEverything here must be negotiated at runtime \u2014 probed for, and degraded from with a caveat\nthe consumer records. Nothing may read it unconditionally.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, *, strict: bool=True) -> dict"
    },
    "Schema/compat.py::guaranteed_surface": {
      "decorators": [],
      "doc": "`{table: frozenset(columns)}` present in EVERY vetted shape \u2014 the version-free surface.\n\nA consumer that stays inside this reads every vetted vintage identically and needs no\nper-version code at all. That is the whole point: bespoke compatibility should be required\nonly for what genuinely varies.\n\nWith `strict` (the default) an unrecoverable id raises, because an intersection taken over\nthe ids we happen to have is larger than the truth and would bless reads that are not\nactually safe.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, *, strict: bool=True) -> dict"
    },
    "Schema/compat.py::known_shapes": {
      "decorators": [],
      "doc": "`{schema_id: shape}` for every id this family vets, as far as it is recoverable.\n\nThe DECLARED shape is always present (it is built from the writer's DDL); historical ids come\nfrom the committed store. Missing ones are reported by `unrecoverable_ids`, not silently\ndropped \u2014 see the module docstring.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str) -> dict"
    },
    "Schema/compat.py::load_shape": {
      "decorators": [],
      "doc": "The committed shape for one historical id, or None if it was never captured.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, sid: str) -> dict | None"
    },
    "Schema/compat.py::shape_path": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, sid: str) -> str"
    },
    "Schema/compat.py::stamp_checked": {
      "decorators": [],
      "doc": "`identity.stamp` + `verify_family_store`, as one call every writer makes at DB creation.\n\nThe stamp is written FIRST and unconditionally: whatever the store's state, an unstamped file\nis strictly worse than a stamped one \u2014 the stamp is what lets a future reader identify the\nvintage without deriving.  Then the store is verified:\n\n  strict=False  warn once per (family, finding-kind) per process, via `emit` or\n                `warnings.warn`.  For writers inside a long run: a store gap discovered at\n                hour N must not kill an arm the run-start precheck already blessed.\n  strict=True   raise `UncommittedShape`.  For interactive data-gen CLIs, where the fix is\n                one command and stopping costs nothing.\n\nReturns whatever `identity.stamp` returned (None for a meta_table-less family \u2014 the VERIFY\nhalf still runs for those, which is exactly why this exists for sim_db and keyframes_db too).",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, family_obj, *, strict: bool=False, emit=None) -> str | None"
    },
    "Schema/compat.py::unrecoverable_ids": {
      "decorators": [],
      "doc": "Vetted ids whose shape was never captured. Non-empty \u21d2 the surface is a guess.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str) -> tuple"
    },
    "Schema/compat.py::validate": {
      "decorators": [],
      "doc": "STATIC check, no database: is `req` inside its family's guaranteed surface?\n\nEmpty list \u21d2 this consumer is version-free by construction. Anything returned is a read that\nonly some vetted vintages can serve, and must move behind a runtime capability probe.",
      "is_async": false,
      "kind": "function",
      "signature": "(req: Requires) -> list"
    },
    "Schema/compat.py::verify_family_store": {
      "decorators": [],
      "doc": "Findings for one family's store state, empty when clean.  Stdlib + committed store only.\n\nTwo checks, matching the two halves of the `UncommittedShape` window:\n\n  (a) the DECLARED shape has a committed document \u2014 else the shape this writer is producing\n      right now becomes unrecoverable at the next DDL move;\n  (b) no committed document is orphaned outside `supported_ids()` \u2014 an orphan IS the outgoing\n      shape of a DDL change that has not been adopted, and every archived file written with\n      it is currently unreadable.\n\n(b) is `adopt()`'s per-family logic from `scripts/schema_report.py`, re-homed where it is\nimport-legal \u2014 a writer cannot import the CLI (`scripts/` is not a package consumers may\ndepend on), but compat owns the store and may read its own directory.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str) -> list[str]"
    },
    "Schema/compat.py::write_shape": {
      "decorators": [],
      "doc": "Commit one historical shape. `captured_from` is a RUN NAME, never a path.\n\nA machine-local path here would be written into a tracked file and blocked by\n`context/guards/path_guard.py` \u2014 and would be useless to anyone else besides.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, sid: str, shape: dict, *, captured_from: str, note: str='') -> str"
    },
    "Schema/connect.py": {
      "decorators": [],
      "doc": "connect.py \u2014 the three ways this project should open a SQLite file.\n\nBefore this module there were **seven** distinct connect idioms across 35 production call sites\nand no shared helper, which had two concrete consequences:\n\n  * `Optimization/run_whatif_delta.py` and `run_whatif_labor.py` opened **archived** sim DBs\n    read-WRITE with no pragmas, dropping `-wal`/`-shm` sidecars beside a 1 GB file \u2014 exactly the\n    condition `scripts/archive_cells.py::_quick_check` exists to defend against.\n  * A shape fingerprint is only meaningful if reading a database cannot change it, and a\n    read-write open can (WAL mode is itself a write).\n\nThree intents, named \u2014 plus the one way to hand a writer back:\n\n    read_only(path)    a finished artifact.  Cannot mutate the file, so it is the only safe way\n                       to fingerprint one.\n    writer(path)       a database being written during a run: WAL + a busy timeout, so parallel\n                       workers do not trip over each other's checkpoints.\n    bulk_writer(path)  a DERIVED file being rebuilt from scratch.  Durability pragmas off,\n                       because a crash means \"rebuild it\", not \"lose data\".\n    close(con)         finish with a writer and leave no `-wal`/`-shm` beside it.\n\nStdlib only; no imports from anywhere else in the repo.",
      "kind": "module",
      "signature": null
    },
    "Schema/connect.py::_BUSY_TIMEOUT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "60.0"
    },
    "Schema/connect.py::_CACHE_PAGES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "-262144"
    },
    "Schema/connect.py::_uri": {
      "decorators": [],
      "doc": "A read-only URI that survives Windows paths.\n\n`sqlite3` wants forward slashes in a URI even on Windows; a raw backslash path silently\nfails to open. That bug is why this is one function and not five copies.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, *, immutable: bool=False) -> str"
    },
    "Schema/connect.py::bulk_writer": {
      "decorators": [],
      "doc": "Open a DERIVED file for a full rebuild.\n\nDurability is deliberately off. It is safe precisely because the file is derived: a crash\nleaves a cache with no completion marker, which its reader treats as absent and rebuilds.\nDo not use this for anything that is a matter of record.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str)"
    },
    "Schema/connect.py::close": {
      "decorators": [],
      "doc": "Close a connection, folding its WAL back into the database file first.\n\nWHY: one archived sweep carried **572** stray `-wal`/`-shm` files.  They are harmless to\ncorrectness, but they are counted as undeclared paths by `runschema.preflight.verify`, they make\nan archived copy differ from its original, and `scripts/archive_cells.py::_sweep_sidecars`\nexists solely to delete the ones that get that far.\n\nWHAT ACTUALLY PRODUCES THEM \u2014 measured, because the intuitive answer is wrong.  \"SQLite removes\nthem when the last connection closes cleanly\" is true, but an unclean close is NOT the main\nsource: a dropped handle, and even a killed worker, still gets finalized by CPython and leaves\nnothing behind.  The dominant producer is a **read-only** connection \u2014 opening a WAL database\n`mode=ro` CREATES the pair and cannot remove them, so an archived run accumulates sidecars just\nby being plotted, fingerprinted or opened in the viewer, long after every writer is gone.  No\nwrite-side change can eliminate those, and `_sweep_sidecars` stays the answer for them.\n\nWhat closing through here fixes is the narrower, real case: folding the WAL back in when this\nwriter is genuinely the file's last, so a finished artifact is a single file.  Measured on a\n`--profile tiny` sweep \u2014 241 finished DBs, zero sidecars, against 249-291 in each archived run.\n\nTWO PROPERTIES THIS DELIBERATELY HAS, because the obvious implementation has neither:\n\n  * **It cannot lose data on a crash.**  A checkpoint only ever moves already-committed frames\n    from the WAL into the main file and fsyncs them; it is a durability *increase*.  Crash\n    mid-checkpoint and the WAL is still there and recovery replays it, exactly as before.  Note\n    it does NOT commit for you \u2014 an open transaction is rolled back by the close below, which\n    is plain `sqlite3` behaviour and must stay visible rather than be papered over here.\n  * **It cannot stall the hot path.**  `wal_checkpoint(TRUNCATE)` normally waits for every\n    reader to finish, and this connection carries a 60 s busy timeout, so the naive version\n    could block a finishing worker for a minute while the viewer holds the file open.  The\n    busy handler is disabled for the checkpoint alone: it then either succeeds immediately or\n    reports busy and is skipped.  Skipping costs a sidecar, which is the outcome we already\n    have; blocking would cost a minute of every worker's wall-clock, which is worse.\n\n`checkpoint=False` closes without any of this \u2014 for a `bulk_writer` (journal_mode=OFF, so\nthere is no WAL) or a `read_only` handle, where the pragma is a no-op or an error either way.\nSafe to call on any connection: nothing here raises.",
      "is_async": false,
      "kind": "function",
      "signature": "(con, *, checkpoint: bool=True) -> None"
    },
    "Schema/connect.py::read_only": {
      "decorators": [],
      "doc": "Open a finished artifact strictly for reading.\n\nNever use a writer here: `PRAGMA journal_mode=WAL` is a WRITE, so it fails on a read-only\nmount and otherwise leaves sidecar files next to an archived database.\n\n`immutable=True` additionally promises the file will not change while open, which lets SQLite\nskip locking entirely \u2014 correct for an archived run, wrong for one still being written.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, *, row_factory: bool=True, immutable: bool=False)"
    },
    "Schema/connect.py::writer": {
      "decorators": [],
      "doc": "Open a database that a run is actively writing.\n\nWAL allows concurrent readers alongside the single writer, which is what lets the viewer\nopen a run that is still going.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, *, timeout: float=_BUSY_TIMEOUT, tuned: bool=False)"
    },
    "Schema/dataset.py": {
      "decorators": [],
      "doc": "dataset.py \u2014 bind a database file to its OWN schema version, and read it by NAME.\n\nThe read-side half of runtime versioning.  ``compat.py`` answers \"may this consumer read that\nfamily\"; this module answers the next question \u2014 \"read THIS file, whatever vintage it is\" \u2014\nwithout the consumer writing SQL, probing PRAGMAs, or branching on a version anywhere.\n\n    ds = dataset.bind(path, 'sim_db', requires=REQUIRES)\n    for row in ds.query('batch_frame', run_id=7):\n        row['thr_batch']            # a LOGICAL column name \u2014 stable across every vintage\n\nThree layers, and where each lives:\n\n  MACHINERY (this module, stdlib + Schema only)\n      `bind()` resolves the file's own id (stamped -> pinned -> derived), loads THAT shape from\n      the committed store, and returns a `Dataset` whose every read is validated against the\n      bound shape.  `Query`/`register_query`/`override` are the registry types.\n  VOCABULARY (beside each family's DDL \u2014 the PUBLISHER side)\n      Named queries with a stable LOGICAL output column set, e.g. sim_db's in\n      `Optimization/persistence/Picking_Data.py`.  The publisher dictates shapes, so the\n      publisher's module is where physical->logical mapping lives.\n  OVERRIDES (small per-vintage modules beside the family)\n      A vintage whose physical schema differs (renamed column, absent table) registers a variant\n      for its schema id \u2014 the same dispatch `Visualization/readers/` proves with `SCHEMA_IDS`.\n      Consumers never see any of it.\n\nWHY LOGICAL COLUMNS ARE THE CONTRACT\n------------------------------------\nA column rename is the quietest schema change: `SELECT *` + `row.keys()` guards turn it into a\ndefault 0.0 on a published figure.  Naming the OUTPUT vocabulary decouples every consumer from\nthe physical schema: canonical SQL aliases physical->logical (`SELECT completion_rate AS\nthr_batch`), an override for an old vintage re-aliases its OWN physical names to the SAME logical\nnames, and the registry enforces that every variant of a query yields the same logical set.  The\nrepo already hand-rolled this twice before it was a system (the loaders' legacy-alias branches,\n`_bdf`'s completion_rate->thr_batch mirror); those become registrations instead of special cases.\n\nSQL POLICY\n----------\nIdentifier positions (tables, columns) are GENERATED from validated names; predicate/aggregate\ntext is HUMAN-WRITTEN and parameterized \u2014 generating the drift-prone part, never the expressive\npart.  `Dataset.read()` builds simple selections; `Query.sql` carries arbitrary hand-written SQL\n(joins, aggregates) whose output columns are the declared logical set; `.con` remains the\ndocumented escape hatch, with the rule that escape-hatch reads still appear in a `Requires`.",
      "kind": "module",
      "signature": null
    },
    "Schema/dataset.py::Dataset": {
      "decorators": [],
      "doc": "One bound file: reads validated against the file's OWN shape, queries served by vintage.\n\nNot a context manager by accident \u2014 it IS one (`with dataset.bind(...) as ds:`), and `.con`\nstays public as the documented escape hatch for hand-written aggregate SQL (whose reads must\nstill appear in the consumer's `Requires`, where CI can see them).",
      "kind": "class",
      "signature": ""
    },
    "Schema/dataset.py::Dataset.__enter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> 'Dataset'"
    },
    "Schema/dataset.py::Dataset.__exit__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *exc) -> None"
    },
    "Schema/dataset.py::Dataset.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, path: str, family, con: sqlite3.Connection, schema_id: str, source: str, shape: dict)"
    },
    "Schema/dataset.py::Dataset.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Schema/dataset.py::Dataset.close": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Schema/dataset.py::Dataset.has": {
      "decorators": [],
      "doc": "Does THIS file's shape carry `table` (and `cols`, when given)?\n\nThis replaces ad-hoc `PRAGMA table_info` probes: the bound shape already knows, and it\nknows for a committed historical vintage even before the file is touched.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, table: str, cols=()) -> bool"
    },
    "Schema/dataset.py::Dataset.negotiate": {
      "decorators": [],
      "doc": "Probe-and-pick over a capability ladder \u2014 see `Schema.capability`.\n\nReturns the chosen `Capability` (carry `capability.provenance(cap)` into whatever you\nemit) or raises `NoSourceAvailable` naming everything tried.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, ladder, run_id: int | None=None, extra=())"
    },
    "Schema/dataset.py::Dataset.query": {
      "decorators": [],
      "doc": "Run a NAMED query, served by whichever SQL matches this file's vintage.\n\nResolution: an `override(...)` registered for this schema id wins; else the canonical\nSQL runs if the bound shape carries the tables/columns it declares; else\n`UnsupportedQuery` names the (id, query) pair.  Missing OPTIONAL logical columns are\nfilled with their declared defaults, so the output contract is identical on every\nvintage that can be served at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, name: str, **params)"
    },
    "Schema/dataset.py::Dataset.read": {
      "decorators": [],
      "doc": "Rows from one table, identifiers validated against the bound shape.\n\n`table`/`cols`/`order_by` columns must exist in THIS file's shape \u2014 a typo or a\nwrong-vintage read fails by name at the call site instead of returning silent defaults.\n`where` stays a caller-written parameterized fragment: the expressive part of SQL is\nnever generated here (see the module docstring's SQL POLICY).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, table: str, cols, where: str='', params=(), order_by: str='')"
    },
    "Schema/dataset.py::DatasetError": {
      "decorators": [],
      "doc": "Base for dataset-binding and named-query failures.",
      "kind": "class",
      "signature": "(_identity.SchemaError)"
    },
    "Schema/dataset.py::Query": {
      "decorators": [
        "dataclass(frozen=True, eq=False)"
      ],
      "doc": "One named read: hand-written SQL whose OUTPUT columns are a stable logical vocabulary.\n\n`columns` is the contract consumers bind to \u2014 every override must yield exactly the same set.\n`optional` maps logical names that may be absent on some vintages to their default value;\nthe canonical/override SQL simply omits them and `Dataset.query` fills the default, which is\nthe registry form of the loaders' `row.keys()` guards.  `tables` names what the CANONICAL\nsql touches, so servability can be answered from the bound shape without parsing SQL.",
      "kind": "class",
      "signature": ""
    },
    "Schema/dataset.py::UnsupportedQuery": {
      "decorators": [],
      "doc": "No canonical or override SQL can serve this (schema id, query) pair.\n\nRaised instead of guessing: a query silently served by SQL written for a different shape is\nthe plausible-wrong-number failure this whole layer exists to prevent.",
      "kind": "class",
      "signature": "(DatasetError)"
    },
    "Schema/dataset.py::UnsupportedQuery.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, query: str, schema: str, detail: str='')"
    },
    "Schema/dataset.py::_q": {
      "decorators": [],
      "doc": "Double-quote a VALIDATED identifier.  Validation happened against the bound shape, so the\nonly job left is quoting; embedded quotes cannot occur in a name the shape store accepted.",
      "is_async": false,
      "kind": "function",
      "signature": "(identifier: str) -> str"
    },
    "Schema/dataset.py::bind": {
      "decorators": [],
      "doc": "Open `path` read-only and bind it to ITS OWN schema version.\n\nResolution is stamped -> pinned -> derived (`identity.resolve`); an id the family does not\nvet raises `UnsupportedSchema` with the structural diff, exactly like `identity.check`.  The\nbound shape is the file's own \u2014 the declared shape when the id is current, else the committed\ndocument (`UnrecoverableShape` if the store lacks it, which the write-time gates now prevent).\n\n`requires` (a `compat.Requires`) is validated against the BOUND shape at bind time, so a\nconsumer fails at open with the missing column named, not mid-analysis with a default 0.0.\n`immutable=True` is for finished archives only \u2014 it skips WAL sidecar creation but the caller\nmust promise nothing is writing (see `Schema.connect.read_only`).",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, family: str, *, requires=None, pinned: str | None=None, verify: bool=True, immutable: bool=False) -> 'Dataset'"
    },
    "Schema/dataset.py::override": {
      "decorators": [],
      "doc": "Register a per-vintage variant.  `schema_ids` may be one id or an iterable.\n\nThe override's SQL must yield the query's exact logical column set minus any `optional`\nnames it cannot supply \u2014 `Dataset.query` fills those defaults.  Enforced at call time (the\nrow keys are checked against the contract) and by the registry tests.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, query_name: str, schema_ids, sql: str) -> None"
    },
    "Schema/dataset.py::queries_for": {
      "decorators": [],
      "doc": "Registered query names for one family, sorted \u2014 the discoverable read surface.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str) -> tuple"
    },
    "Schema/dataset.py::register_query": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(q: Query) -> Query"
    },
    "Schema/dataset.py::sql_for": {
      "decorators": [],
      "doc": "The composed SQL that would serve named query `name` on vintage `schema_id` \u2014 no file.\n\nSame resolution as `Dataset.query` (an `override(...)` for the id wins, else the canonical\nSQL if the COMMITTED shape for that id carries the tables/columns it declares), answered\nfrom the store instead of a bound connection.  For consumers that want the statement itself\nrather than rows \u2014 the run_whatif_* trio pattern, where one composed query fans out over\nmany files.  Raises `UnsupportedQuery` rather than guessing, exactly like the bound path.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, name: str, schema_id: str) -> str"
    },
    "Schema/fingerprint.py": {
      "decorators": [],
      "doc": "fingerprint \u2014 the source-hashing half that three content-addressed stores shared by COPY.\n\n`runschema/contract.py`, `Schema/profile_tree.py` and `Schema/store_index.py` each carry a\n`source_fingerprint`, and two of the three say in their own docstring that they are a copy of the\nfirst. `Schema/` may not import `Optimization/` (the declared boundary \"shape identity must not\ndepend on the run harness\"), so copying was the only way to share it -- and a copy nothing\ncompares is a copy that drifts.\n\n**It drifted.** `runschema.contract` grew a SECOND input class -- `SHAPE_SOURCE_DIRS`, hashed by\nname list and content, so an ADDED file registers in an auto-discovered directory -- and the two\ncopies did not. From that moment \"same algorithm as `runschema.contract.source_fingerprint`\" was\nfalse, and `test_source_fingerprint_matches_the_runtree_implementation`, which asserts the copies\nhold byte-for-byte, has been failing ever since\n(`.scratch/architecture-drift/issues/05`).\n\n## What that ticket calls this, and what it actually is\n\nThe ticket calls it \"the dangerous one of the seven\": two implementations disagreeing on\nidentical input, so \"one caller population thinks a tree is current while another thinks it is\nstale, with no error either way\". That is not what the disagreement was.\n\nMeasured before fixing: with `SHAPE_SOURCE_DIRS` emptied, the two produce the SAME digest on the\nsame input -- the files half was always byte-identical, and 100% of the difference was the dirs\nhalf that only one of them has. Neither computed a wrong answer for its own inputs; neither reads\nthe other's index; and on the real tree they hash different source LISTS anyway (nine files\nagainst roughly twenty-five plus a directory), so they were never going to agree. The live hazard\nthe ticket describes -- a tree one caller thinks is current and another thinks is stale -- needs\ntwo readers of ONE index, and there is no such pair.\n\nSo this is a false-equivalence bug, not a live fingerprint hazard: a docstring claiming an\nidentity that stopped being true, and a test asserting it. Recorded here because the severity is\nwhat decides whether the next person drops everything for it, and the ticket's own answer to that\nis wrong.\n\n## The shape of the fix\n\n`update_files` takes the hash object and folds the files into it IN PLACE, rather than returning\na digest the caller then re-hashes. That is deliberate and it is the whole reason this refactor\ncosts nothing: `contract` folds files and then directories into ONE hash, so a helper that\nreturned a digest would change its output, invalidate every recorded `source_fingerprint`, and\nforce a canary re-prove for a pure code move. In place, every one of the three callers emits the\nbyte-identical value it emitted before.",
      "kind": "module",
      "signature": null
    },
    "Schema/fingerprint.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['update_files', 'update_dirs', 'of_files']"
    },
    "Schema/fingerprint.py::of_files": {
      "decorators": [],
      "doc": "`update_files` on a fresh hash, as the two file-only stores want it.\n\n`contract` does NOT use this: it continues the same hash into its directory half, which is\nwhat keeps its emitted value unchanged across this refactor.",
      "is_async": false,
      "kind": "function",
      "signature": "(rels, repo_root: str) -> str"
    },
    "Schema/fingerprint.py::update_dirs": {
      "decorators": [],
      "doc": "Fold whole DIRECTORIES of `.py` files into the hash `h`, by NAME LIST and then content.\n\nThe input class a path tuple cannot describe: a tuple can only name files someone already\nthought of, and `Optimization/simconfig/configs/` is auto-discovered, so an ADDED pick config\nwould register nowhere. Only `runschema.contract` has such an input -- which is the entire\nreason its fingerprint differs from the two file-only stores on shared input\n(`architecture-drift/05`), and why that difference is a DECLARED asymmetry, not drift.\n\nThe NAME LIST is hashed BEFORE any content, so a RENAME registers even when no byte moves --\nrenaming a pick config renames a run-tree directory.\n\nContent is normalised exactly as `update_files` normalises it, and it lives here so that ONE\nplace decides what a line ending means. It was a fourth copy of that rule until this module.",
      "is_async": false,
      "kind": "function",
      "signature": "(h, rel_dirs, repo_root: str) -> None"
    },
    "Schema/fingerprint.py::update_files": {
      "decorators": [],
      "doc": "Fold `rels` (repo-relative paths, in declared order) into the hash `h`.\n\nThree properties, each of which a caller has been bitten by:\n\n  * **Line endings are NORMALISED to `\\n`.** On Windows a checkout, a stash/pop or an\n    `autocrlf` change rewrites CRLF<->LF without touching a statement; hashing raw bytes made\n    that look like a structural change and cost a needless canary pair.\n  * **A MISSING file contributes its path plus a marker** rather than being skipped, so\n    DELETING a source is detected instead of silently matching.\n  * **The path is hashed before the content**, so moving a file registers even when its bytes\n    do not change.",
      "is_async": false,
      "kind": "function",
      "signature": "(h, rels, repo_root: str) -> None"
    },
    "Schema/hook_check.py": {
      "decorators": [],
      "doc": "hook_check.py \u2014 Stop-hook backstop that nudges a DB-shape store sync on drift.\n\nWired via .claude/settings.json (Stop hook), beside Optimization/runschema/hook_check.py \u2014 the\nsame bargain for the OTHER contract.  It compares the DDL source fingerprint recorded in\n``Schema/shapes/INDEX.json`` against the working tree and stats the per-family committed\ndocuments.  It NEVER imports the writers (that pulls ~1350 modules, matplotlib and pandas\nincluded) and ALWAYS exits 0: advisory, never blocking, never slow.\n\nThe nag it prints is the whole point.  A DDL edit that ships without ``--sync`` opens the\nwindow `Schema.compat.UncommittedShape` documents: the new shape is unrecoverable at the next\nchange, and the outgoing one silently orphans the archive.  Catching it at the END OF THE TURN\nthat edited the DDL costs one line; catching it at the next run's precheck costs a blocked run;\ncatching it at analysis time costs an unreadable archive.\n\nRun standalone:  python Schema/hook_check.py",
      "kind": "module",
      "signature": null
    },
    "Schema/hook_check.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Schema/hook_check.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> int"
    },
    "Schema/identity.py": {
      "decorators": [],
      "doc": "identity.py \u2014 which schema is this database, and is it the one we expect?\n\nThe registry of DB families and the stamped -> pinned -> derived resolution that turns a file into\na known shape.\n\nWhy a database needs an identity at all\n---------------------------------------\nNothing in this project raises when a column disappears. Every loader in\n`Optimization/persistence/Picking_Data.py` does `SELECT *` and guards with `row.keys()`, and\n`Optimization/Performance_Evaluations/` reaches all of them through those loaders \u2014 so a renamed\nor dropped column does not fail, it silently becomes the dataclass default `0.0`/NaN in\n`common/frames.py`, and a published number quietly changes. That is the quietest failure mode in\nthe codebase. An identity check converts it into a loud one.\n\nIdentity is DERIVED FROM SHAPE, never chosen \u2014 the same trick\n`Optimization/runschema/contract.py` uses for the run tree. Nobody picks a version number, two\nbranches cannot both call themselves \"v3\", and any consumer can re-derive it to check.\n\nResolution order is **stamped -> pinned -> derived**, because deriving costs ~10 PRAGMA round\ntrips per file and an archive holds hundreds:\n\n    stamped   the writer recorded its own id in the file (families that have a meta table)\n    pinned    a previous derivation was cached alongside (e.g. a derived sidecar)\n    derived   read out of `sqlite_master` now",
      "kind": "module",
      "signature": null
    },
    "Schema/identity.py::Family": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One database family: what it is called, how it declares its shape, where it stamps it.\n\n`declared_shape` is a callable rather than a literal so a family's expectation is always\nproduced by BUILDING the schema its writer builds \u2014 a hand-listed column set is exactly the\nkind of declaration that drifts.",
      "kind": "class",
      "signature": ""
    },
    "Schema/identity.py::Family.declared_id": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Schema/identity.py::Family.supported_ids": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Schema/identity.py::META_TABLE_DDL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'CREATE TABLE IF NOT EXISTS %s (key TEXT PRIMARY KEY, value TEXT)'"
    },
    "Schema/identity.py::SchemaDrift": {
      "decorators": [],
      "doc": "The stamped or pinned id disagrees with the file's actual shape.",
      "kind": "class",
      "signature": "(SchemaError)"
    },
    "Schema/identity.py::SchemaError": {
      "decorators": [],
      "doc": "Base for every schema-identity failure.",
      "kind": "class",
      "signature": "(Exception)"
    },
    "Schema/identity.py::UnsupportedSchema": {
      "decorators": [],
      "doc": "This database's shape is not one this build knows how to read.",
      "kind": "class",
      "signature": "(SchemaError)"
    },
    "Schema/identity.py::UnsupportedSchema.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, family: str, observed: str, known: tuple, detail: str='')"
    },
    "Schema/identity.py::check": {
      "decorators": [],
      "doc": "Open `path` read-only, resolve its id, and refuse anything unvetted.\n\nThe one call a consumer needs before trusting a file's columns.\n\n**Pass `verify=True` unless you have measured that you cannot afford it.** Without it a\nSTAMPED file is taken at its word, and a stamp is a claim the file makes about itself: a\ndatabase altered after the run that wrote it still carries the old id, so `check` returns a\nvetted answer for a shape that is no longer vetted. Every wired consumer in the repo passes\nit. The stamped fast path is for cheaply IDENTIFYING files in bulk (which reader do these\n272 arms need?), not for deciding whether to trust one \u2014 and the archive is unstamped\nanyway, so for it the two cost exactly the same.\n\nMeasured on the results drive: ~27 ms per archived sim DB, of which the re-derivation\n`verify=True` adds is ~1 ms. The cost is OPENING the file, not reading `sqlite_master`.\n\n**Side effect worth knowing**: opening a WAL database `mode=ro` CREATES its `-shm` (and\n`-wal`), and a read-only connection cannot remove them again \u2014 so fingerprinting an\narchived run leaves sidecars beside it. That is not this function's doing; it is true of\nevery read here, and it is the mechanism behind the sidecars the archive accumulates. Only\n`connect.read_only(..., immutable=True)` avoids it, and only a caller can promise the file\nis not being written. Every current call site opens the same file again immediately\nafterwards, so none of them creates a sidecar that was not coming anyway.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, family_name: str, *, verify: bool=False) -> str"
    },
    "Schema/identity.py::check_or_warn": {
      "decorators": [],
      "doc": "`check()` for consumers that must DEGRADE rather than die.  Returns None if unvetted.\n\nThe split is deliberate and the choice is per-site, not per-family:\n\n  hard `check()`   a simulation about to WRITE from this file, or an analysis about to\n                   PUBLISH a number from it.  A wrong column there becomes a plausible\n                   `0.0` and a figure nobody can tell is wrong.\n  `check_or_warn`  an interactive, read-only, exploratory consumer, and derived caches that\n                   can rebuild themselves.  Refusing to open a 2026-06 archive outright is a\n                   worse failure than drawing it with a named caveat in the log.\n\nEither way the `UnsupportedSchema` TEXT \u2014 which names the differing tables and columns \u2014 is\nwhat reaches the caller; that message is the whole point of the layer, and a bare `except`\nthat drops it leaves the consumer no better off than before this package existed.\n\n`emit` takes the message (e.g. `log.warning`); the default routes to `warnings.warn`, which\na viewer's logging config already captures and a test can assert on with `pytest.warns`.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, family_name: str, *, verify: bool=False, emit=None) -> str | None"
    },
    "Schema/identity.py::check_tables": {
      "decorators": [],
      "doc": "`check()` for a consumer that can only reach PART of its family's declaration.\n\nA whole-file id needs the family, and a family lives with its WRITER \u2014 which is sometimes a\nmodule the reader must not import (`Warehouse/catalog/Affinity_Store.py` reads a file whose\nfamily is registered in `Warehouse/generation/generate_affinity.py`, a data-gen CLI that\npulls matplotlib and pandas into every simulation worker). Comparing only the tables the\nconsumer actually queries, against the DDL it already mirrors, gives that reader the same\nguarantee without the import \u2014 and turns \"both DDLs must stay in step\" from a comment into\nsomething the file enforces on itself every time it opens a database.\n\nRaises `UnsupportedSchema` naming the differing columns. `label` goes in the message in\nplace of a family name, so it should say which tables were compared.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str, expected: dict, tables, *, label: str) -> None"
    },
    "Schema/identity.py::families": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple"
    },
    "Schema/identity.py::get": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> Family"
    },
    "Schema/identity.py::meta_ddl": {
      "decorators": [],
      "doc": "The stamp table's DDL, for a family to include in its own schema.",
      "is_async": false,
      "kind": "function",
      "signature": "(table: str='schema_meta') -> str"
    },
    "Schema/identity.py::read_stamp": {
      "decorators": [],
      "doc": "The id a writer recorded, or None.\n\nNone is the expected answer for every file written before its family was stamped \u2014 that is\nthe normal path into derivation, not an error.\n\nA family with no meta table but a `stamp_reader` (sim_db: the stamp is a\n`simulation_runs.sim_schema_id` column value) answers through that callable, guarded the same\nway \u2014 any sqlite error means \"pre-stamp file\", never a failure.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, family: Family) -> str | None"
    },
    "Schema/identity.py::register": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(family: Family) -> Family"
    },
    "Schema/identity.py::resolve": {
      "decorators": [],
      "doc": "Resolve the schema id of `con`. Returns `(schema_id, source)`.\n\nWith `verify=True` the id is always re-derived and checked against whatever was stamped or\npinned; a disagreement means the file was migrated after it was written, and raises. The\nstamp is provenance, but the OBSERVED shape is what queries must be planned against.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, family: Family, *, pinned: str | None=None, verify: bool=False) -> tuple"
    },
    "Schema/identity.py::stamp": {
      "decorators": [],
      "doc": "Record this build's declared id into the file. No-op for a family with no meta table.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection, family: Family) -> str | None"
    },
    "Schema/pathtpl.py": {
      "decorators": [],
      "doc": "pathtpl.py \u2014 the contract path-template vocabulary: render, and invert-with-capture.\n\nMOVED here from ``Optimization/runschema/resolver.py`` (which re-exports): the profiles-tree\nresolver lives in ``Schema/`` \u2014 the only layer both the generation writers and the run-harness\nconsumers may import \u2014 and must interpret the SAME template vocabulary the run tree uses.  Two\ncopies of a template engine is how the two contracts drift apart; one copy in the stdlib-only\nleaf is how they cannot.\n\nThe vocabulary (shared by both contracts' FEATURES declarations):\n  `{name}`   required segment/part\n  `{name?}`  OPTIONAL segment \u2014 dropped with its separator when the part is None\n  `*` / `**` globs; `**` as a whole segment means ZERO or more directories (glob semantics \u2014\n             compiling it as one-or-more once silently disowned zero-depth files)",
      "kind": "module",
      "signature": null
    },
    "Schema/pathtpl.py::_capture_regex": {
      "decorators": [],
      "doc": "Compile a template into a matcher that CAPTURES one placeholder from a concrete path.\n\nEvery other `{part}` becomes a wildcard; `{part?}` becomes an optional segment.  This is how\na resolver inverts `\u2026/sim_{strategy}.db` back to the arm name without hardcoding `len('sim_')`.\nPass a placeholder that appears in no template (any non-identifier string) to get a pure\nmatcher with nothing captured.",
      "is_async": false,
      "kind": "function",
      "signature": "(template: str, placeholder: str) -> re.Pattern"
    },
    "Schema/pathtpl.py::render": {
      "decorators": [],
      "doc": "Render a contract path template to a relative path.\n\n`{name}` substitutes parts[name]; `{name?}` is an OPTIONAL segment that is dropped along with\nits separator when parts.get(name) is None.  Raises KeyError for a missing required part, so a\ntypo fails loudly instead of producing a path with a literal brace in it.",
      "is_async": false,
      "kind": "function",
      "signature": "(template: str, **parts) -> str"
    },
    "Schema/profile_resolver.py": {
      "decorators": [],
      "doc": "profile_resolver.py \u2014 the generic resolver over the profiles-tree contract.\n\nThe catalogue analogue of ``runschema.resolver.RunTree``, deliberately NOT a subclass (Schema\nmust not import Optimization): one class interpreting the ``Schema.profile_tree`` declaration\nvia the shared ``Schema.pathtpl`` vocabulary.\n\nDescriptor-first, walk-fallback: a profile run carrying ``profile_layout.json`` is served from\nthe descriptor (which also carries the binding a simulation records); a pre-contract run \u2014 every\ncatalogue generated before the descriptor existed, forever, per the no-backfill decision \u2014 is\nserved by a walk that mirrors ``runschema.runlayout._profile_pairs`` byte-for-byte.  The label\nformat ``f'{run}__{profile}'`` is the run tree's ``<pair>`` level and must never drift.",
      "kind": "module",
      "signature": null
    },
    "Schema/profile_resolver.py::ProfileTree": {
      "decorators": [],
      "doc": "Resolver for one profiles ROOT (the directory holding profile-run dirs).\n\nConstruct via ``ProfileTree(profiles_root)``; each accessor takes the profile-run name where\nscope demands it.  Existence is not implied \u2014 optional artifacts legitimately do not exist.",
      "kind": "class",
      "signature": ""
    },
    "Schema/profile_resolver.py::ProfileTree.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, profiles_root: str, contract: dict | None=None)"
    },
    "Schema/profile_resolver.py::ProfileTree.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Schema/profile_resolver.py::ProfileTree.binding_of": {
      "decorators": [],
      "doc": "The catalogue-version binding a RUN should record for one pair label, or None.\n\nNone whenever the label's profile run has no descriptor \u2014 with descriptors forward-only,\nthat is every pre-contract catalogue, and recording `null` is honest (absence of\nevidence, never fabricated evidence).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, label: str) -> dict | None"
    },
    "Schema/profile_resolver.py::ProfileTree.latest": {
      "decorators": [],
      "doc": "The newest profile run with at least one complete pair.\n\nDescriptor `created` timestamps order the descriptor-bearing runs; runs WITHOUT a\ndescriptor order by name (the historical lexicographic behavior, which their timestamped\nnames make correct).  When both kinds exist, the maximum across the merged order wins;\nif that disagrees with the pure lexicographic answer, a warning names both \u2014 the\nsituation is legal (a backdated --name) but worth a human glance.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str | None"
    },
    "Schema/profile_resolver.py::ProfileTree.latest._key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run: str)"
    },
    "Schema/profile_resolver.py::ProfileTree.layout_of": {
      "decorators": [],
      "doc": "The run's own descriptor, or None for a pre-contract catalogue (normal, not an error).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, profile_run: str) -> dict | None"
    },
    "Schema/profile_resolver.py::ProfileTree.pairs": {
      "decorators": [],
      "doc": "(label, inventory_db, affinity_db) for every complete profile in one run.\n\nDescriptor-first: profiles listed in `profile_layout.json`, resolved through the\ncontract templates, existence-checked (a descriptor is a claim; the DB is the fact).\nFallback: the byte-for-byte mirror of `runlayout._profile_pairs` \u2014 sorted listdir,\nliteral `inventory/inventory.db` + `affinity/affinity.db`, both-exist filter.\nThe label is ALWAYS `f'{profile_run}__{profile}'` \u2014 the run tree's pair level.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, profile_run: str) -> list"
    },
    "Schema/profile_resolver.py::ProfileTree.path": {
      "decorators": [],
      "doc": "Absolute path of any contract artifact within one profile run.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, artifact: str, profile_run: str, **parts) -> str"
    },
    "Schema/profile_resolver.py::ProfileTree.runs": {
      "decorators": [],
      "doc": "Profile-run names under the root, ascending.  Disk is the authority for EXISTENCE\n(a descriptor names only what its generator wrote; a run dir is real either way).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Schema/profile_resolver.py::ProfileTree.schema_id": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Schema/profile_resolver.py::log": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "logging.getLogger(__name__)"
    },
    "Schema/profile_tree.py": {
      "decorators": [],
      "doc": "profile_tree.py \u2014 THE DECLARATION of the generated-catalogue (profiles) tree, and its store.\n\nThe run tree got its contract first (``Optimization/runschema/schema.py``); this is the same\ntreatment for the tree the INVENTORY GENERATORS write \u2014 the catalogue every simulation consumes.\nBefore this module existed the profiles tree had no descriptor, no declaration, and no version\nof any kind on disk: four writers hand-composed the layout, \"latest catalogue\" was a\nlexicographic directory-name sort, and a run recorded only WHERE its catalogue was, never WHICH \u2014\na catalogue regenerated in place silently re-pointed every prior run, undetectably.\n\nThree things live here and nowhere else:\n\n  * ``FEATURES`` / ``LEVELS`` / ``ARTIFACTS`` \u2014 the declaration (hashed \u2192 the schema id);\n  * the committed store at ``Schema/schemas/profile_tree/<short>.json`` + ``INDEX.json``\n    (immutable content-addressed documents + a mutable head, the run-tree layout exactly);\n  * ``write_profile_layout(...)`` \u2014 the descriptor every generator stamps at the profile-run\n    root, carrying the schema id, repo provenance, and per-profile params digests.\n\nWHY THIS LIVES IN ``Schema/`` AND IS A PATTERN COPY, NOT A SHARED ABSTRACTION\n-----------------------------------------------------------------------------\nThe writers are in ``Warehouse/generation`` and the consumers in ``Optimization/runschema`` \u2014\nthe only layer both may import is ``Schema/``.  The store machinery deliberately COPIES\n``runschema/contract.py`` (~its hashing, store, and adopt shapes) rather than refactoring the two\nonto a shared base: ``schema \u2192 optimization`` is forbidden, and an abstraction serving two\ncontracts is exactly the coupling both were built to avoid.  An honesty test pins this module's\nhashing against ``runschema.contract`` on shared input so the copy cannot drift\n(the ``store_index.source_fingerprint`` precedent).\n\nDESCRIPTORS ARE FORWARD-ONLY (user decision).  Existing catalogues have no descriptor and never\nget one fabricated; every consumer keeps a byte-for-byte legacy walk for them.  A descriptor is\nevidence a GENERATOR wrote it, or it is nothing.\n\nCLI:\n    python -m Schema.profile_tree --write    # mint/refresh the document for this declaration\n    python -m Schema.profile_tree --check    # exit 1 if the committed store is stale",
      "kind": "module",
      "signature": null
    },
    "Schema/profile_tree.py::ARTIFACTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'profile_layout': {'path': DESCRIPTOR, 'format': 'json', 'scope': 'run', 'writer': 'write_profile_layout@Schema/profile_tree.py', 'note': 'the descriptor; carries schema_id, provenance, and per-pr..."
    },
    "Schema/profile_tree.py::AXES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('profile_run', 'profile')"
    },
    "Schema/profile_tree.py::DESCRIPTOR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'profile_layout.json'"
    },
    "Schema/profile_tree.py::FEATURES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('optional-segments', 'globs')"
    },
    "Schema/profile_tree.py::LEVELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[{'name': 'profile_run', 'optional': False, 'note': 'one generation invocation: mixed_<YYYYMMDD_HHMMSS>, or the --name given to it. The legacy suite generator used profile_<ts>.'}, {'name': 'profil..."
    },
    "Schema/profile_tree.py::SHAPE_SOURCES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('Schema/profile_tree.py', 'Schema/profile_resolver.py', 'Schema/pathtpl.py', 'Warehouse/generation/generate_mixed_profile.py', 'Warehouse/generation/generate_inventory.py', 'Warehouse/generation/g..."
    },
    "Schema/profile_tree.py::SHORT_LEN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "12"
    },
    "Schema/profile_tree.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Schema/profile_tree.py::_INDEX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_TREE_DIR, 'INDEX.json')"
    },
    "Schema/profile_tree.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(_HERE, '..'))"
    },
    "Schema/profile_tree.py::_TREE_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_HERE, 'schemas', 'profile_tree')"
    },
    "Schema/profile_tree.py::_sha": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(blob: bytes) -> str"
    },
    "Schema/profile_tree.py::_shape_only": {
      "decorators": [],
      "doc": "The projection that DEFINES the tree \u2014 exactly what `schema_id` hashes.\n\nSame exclusion rules as `runschema.contract._shape_only`: no schema_id (an identity cannot\nfeed its own hash), no note/condition/description/writer (prose and attribution never mint a\nschema), no provenance.  `family` is attribution too \u2014 unhashed, per the run-tree precedent.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> dict"
    },
    "Schema/profile_tree.py::_write_index": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(index: dict) -> str"
    },
    "Schema/profile_tree.py::adopt": {
      "decorators": [],
      "doc": "Store `doc`, set it as head, record provenance + the current source fingerprint.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict, *, label: str='') -> str"
    },
    "Schema/profile_tree.py::build": {
      "decorators": [],
      "doc": "Generate the contract document for the CURRENT declaration above.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str=_REPO_ROOT) -> dict"
    },
    "Schema/profile_tree.py::contract_path": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sid: str) -> str"
    },
    "Schema/profile_tree.py::entries_from_disk": {
      "decorators": [],
      "doc": "Descriptor `profiles` entries derived from a run dir the CALLER JUST WROTE.\n\nFor generators whose write path does not thread per-side metadata through one loop (the\nlegacy suite; the preflight canary): the generator claims what it wrote by walking its own\noutput.  This is NOT backfill \u2014 a generator deriving its own just-written tree is the writer\nspeaking; deriving someone else's old tree would be fabricated provenance, and stays banned.\nSides without a params.json record `params_digest: None` (the canary writes none) \u2014 absence\nstays honest.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str) -> dict"
    },
    "Schema/profile_tree.py::head": {
      "decorators": [],
      "doc": "The schema id a NEW catalogue stamps.  A named pointer \u2014 hashes have no order.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> str | None"
    },
    "Schema/profile_tree.py::load": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sid: str) -> dict | None"
    },
    "Schema/profile_tree.py::load_all": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Schema/profile_tree.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Schema/profile_tree.py::params_digest": {
      "decorators": [],
      "doc": "sha256 over the params.json FILE BYTES as written.\n\nNo canonicalisation, deliberately: the params timestamp participating in the digest is a\nFEATURE \u2014 a regeneration mints a new digest even under identical parameters, which is\nexactly what makes silent in-place re-pointing detectable from a run's recorded binding.",
      "is_async": false,
      "kind": "function",
      "signature": "(params_json_path: str) -> str"
    },
    "Schema/profile_tree.py::read_index": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Schema/profile_tree.py::read_profile_layout": {
      "decorators": [],
      "doc": "The descriptor at a profile-run root, or None (a pre-contract catalogue \u2014 the normal\nanswer for everything generated before this module existed; never an error).",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str) -> dict | None"
    },
    "Schema/profile_tree.py::schema_id": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> str"
    },
    "Schema/profile_tree.py::short_id": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(sid: str) -> str"
    },
    "Schema/profile_tree.py::source_fingerprint": {
      "decorators": [],
      "doc": "Hash of every shape-defining source (path + content, CRLF-normalised, MISSING-marked).\n\nTHE ALGORITHM IS SHARED NOW, not copied: `Schema.fingerprint.of_files` is the one\nimplementation, and `runschema.contract` folds the same helper into the first half of its\nown fingerprint.  It was a copy until `architecture-drift/05`, and it had already drifted --\n`contract` grew a SECOND input class (`SHAPE_SOURCE_DIRS`, for auto-discovered files) and\nthis copy did not, which is what made the honesty test fail.\n\nTHIS STORE HASHES FILES ONLY, and that is correct rather than an omission: the profiles tree\nhas no auto-discovered directory in its shape.  A directory half here would hash nothing and\nread as parity.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str=_REPO_ROOT) -> str"
    },
    "Schema/profile_tree.py::stale_reasons": {
      "decorators": [],
      "doc": "Cheap staleness findings for the Stop hook \u2014 file hashes and stats only.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Schema/profile_tree.py::verify_store": {
      "decorators": [],
      "doc": "Findings for the committed store, empty when clean (id-vs-filename, head present).",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list"
    },
    "Schema/profile_tree.py::write_profile_layout": {
      "decorators": [],
      "doc": "Stamp `profile_layout.json` at a profile-run root.  Atomic (tmp + os.replace).\n\n`profiles` maps profile name -> its per-side entries, e.g.\n    {'mixed_realistic_bell_lt0': {\n        'inventory': {'db': 'inventory/inventory.db', 'params_digest': 'sha256:...',\n                      'db_schema_id': '...', 'db_bytes': 12345678},\n        'affinity':  {..., 'source_inventory': '../inventory/inventory.db'}}}\nCallers REWRITE after every completed profile, so a crashed multi-profile generation still\nleaves a valid partial descriptor covering what finished.",
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, profiles: dict, *, generator: str, argv=None, created: str | None=None) -> str"
    },
    "Schema/provenance.py": {
      "decorators": [],
      "doc": "provenance.py \u2014 which CODE produced an artifact, derived without raising, git binary optional.\n\nMOVED here from ``Optimization/runschema/sim_manifest.py`` (which re-exports for its existing\ncallers): the profiles-tree descriptor is written by ``Warehouse/generation`` \u2014 a layer that must\nnot import the run harness \u2014 and provenance stamping is not a run-tree concern to begin with.\n``Schema/`` is the stdlib-only leaf every layer may import, and \"what code wrote this file\" is a\nsibling question to \"what shape is this file\".\n\nWHY THIS MUST NOT RAISE: it runs at the front of every simulation and every catalogue generation,\nand a producer that dies because provenance could not be derived is strictly worse than one that\nrecords ``unknown``.  A shallow clone, a source export with no ``.git``, a machine with no git on\nPATH and a wedged index lock all resolve to a recorded value rather than an exception.",
      "kind": "module",
      "signature": null
    },
    "Schema/provenance.py::_GIT_TIMEOUT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "10.0"
    },
    "Schema/provenance.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))"
    },
    "Schema/provenance.py::_git_dir": {
      "decorators": [],
      "doc": "The `.git` directory for `repo_root`, or None when there is no git metadata at all.\n\n`.git` is a DIRECTORY in a normal clone and a FILE holding `gitdir: <path>` in a linked\nworktree or a submodule, which is why this is not a bare isdir() check.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str) -> str | None"
    },
    "Schema/provenance.py::_head_commit": {
      "decorators": [],
      "doc": "HEAD's full sha by reading files only \u2014 no subprocess, and no git binary required.\n\nA source export (a zip, a docker COPY) has no `.git` and returns None from _git_dir before we\nget here; a SHALLOW clone does have one and resolves normally, which is the case the naive\n`git describe` approach gets wrong.  Three shapes are handled: a detached HEAD (the sha\ninline), a loose ref, and a ref that only exists in `packed-refs` (a fresh clone's usual state).",
      "is_async": false,
      "kind": "function",
      "signature": "(git_dir: str) -> str | None"
    },
    "Schema/provenance.py::repo_provenance": {
      "decorators": [],
      "doc": "{'repo_commit': <short sha> | 'unknown', 'repo_dirty': True | False | None}.\n\n`repo_dirty` is deliberately THREE-valued: True/False are answers, `None` means \"not\nestablished\" \u2014 which is what an export or a missing git binary honestly is, and reading it as\n\"clean\" would be the one wrong inference.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str=_REPO_ROOT) -> dict"
    },
    "Schema/semantics.py": {
      "decorators": [],
      "doc": "semantics \u2014 what a column MEANS, declared once, machine-checkable.\n\nThe repo has hit at least twelve distinct classes of column-semantic misread \u2014 a stamp read\nas a span, a level summed as a flow, packs added to pieces, wall-clock seconds ratioed\nagainst sim seconds \u2014 and every one failed silently, because the semantics lived in DDL\ncomment prose that no gate executes (see `.scratch/inbound-groundwork/assets/column-audit.md`\nwhile the effort is in flight; the incident ledger will move to a design doc when it closes).\nThis module is the structured home those facts never had.\n\n# \u2500\u2500 the vocabulary \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nNine KINDS, defined in root `CONTEXT.md`'s Measurement section and repeated here only as\nconstants: STAMP (a point on a clock), SPAN (elapsed), LEVEL (standing, re-measured; never\nsummed across snapshots), FLOW (additive at the row's grain), COUNT (a discrete flow), RATE\n(flow / span; the denominator is part of the meaning), SCORE (policy-relative), SHARE\n(proportion), LABEL (identity / ordinal / enum).\n\nEach column declares kind + unit + grain always; clock, account, per, null-meaning, a\nlogical name and a scar note when they apply.  A column whose kind depends on a sibling's\nVALUE (`carryover.qty` by `reason`) declares a `ByDiscriminator` \u2014 the case no comment\nconvention can check, and the sharpest single argument for this module existing.\n\n# \u2500\u2500 where declarations live \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nBeside the DDL, keyed exactly as `Family.declared_shape()` keys its tables \u2014 so the\ncompleteness gate is a dict diff with nothing to drift.  A family registers its table with\n`register(family_name, semantics, covered)`; `covered` names the tables whose tagging is\nCOMPLETE and therefore gated (`Tests/architecture/test_column_semantics.py`).  The end state\nis every table of every family covered \u2014 \"leave no remainder\" \u2014 reached family by family.\n\n# \u2500\u2500 what this module deliberately does NOT do \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nUnit CONVERSION.  `SECONDS_PER_HOUR` lives in `Warehouse/kernel/timeline` with one blessed\nimport chain through the analysis suite's `common/units.py`, and this module is a\nstdlib-only leaf that imports neither.  The guards here CHECK (may you sum this? are these\nthe same clock? the same unit of account?); turning a checked value into hours stays where\nthe one divisor already lives.",
      "kind": "module",
      "signature": null
    },
    "Schema/semantics.py::ByDiscriminator": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "A column whose kind depends on a sibling column's VALUE in the same row.\n\n`carryover.qty` is the canonical case: three `reason` values are re-emitted LEVELS and\nfour are FLOWS, sharing one physical column under one PK.  Row-free aggregation over\nsuch a column is not defined, and every guard here says so rather than guessing.",
      "kind": "class",
      "signature": ""
    },
    "Schema/semantics.py::ByDiscriminator.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Schema/semantics.py::Col": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One column's declared semantics.  kind + unit + grain are mandatory; the rest are\nconditionally mandatory (the gate demands them when they apply) \u2014 nothing is\noptional-and-silent.",
      "kind": "class",
      "signature": ""
    },
    "Schema/semantics.py::Col.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Schema/semantics.py::SemanticsError": {
      "decorators": [],
      "doc": "A read refused because the declared semantics forbid it. The message carries the\nincident history when the column has one \u2014 that is what makes the refusal auditable.",
      "kind": "class",
      "signature": "(Exception)"
    },
    "Schema/semantics.py::check_add": {
      "decorators": [],
      "doc": "May these two columns be added element-wise? Same unit AND same unit of account.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, table: str, a: str, b: str) -> None"
    },
    "Schema/semantics.py::check_completeness": {
      "decorators": [],
      "doc": "Problems between a family's declared shape and its declared semantics.\n\n`shape_tables` is `Family.declared_shape()['tables']` verbatim \u2014 {table: {'columns':\n[{'name', 'type', 'notnull', 'pk'}, ...], ...}} \u2014 consumed directly so there is no\nparallel column list to drift.  For every COVERED table: every column tagged, no tag\nnaming a vanished column, null-meaning present wherever the DDL allows NULL, and a\nclock wherever the unit is time.  Empty list = the gate passes.",
      "is_async": false,
      "kind": "function",
      "signature": "(shape_tables: dict, family: str) -> list"
    },
    "Schema/semantics.py::check_ratio": {
      "decorators": [],
      "doc": "May a/b form a ratio? Same clock when both are time \u2014 the same word \"seconds\" on\ndifferent instruments cost a day and a wrong committed claim (incident class 11).",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, table_a: str, a: str, table_b: str, b: str) -> None"
    },
    "Schema/semantics.py::register": {
      "decorators": [],
      "doc": "Declare a family's column semantics and which tables are COMPLETELY tagged.\n\n`semantics` is {table: {column: Col | ByDiscriminator}}, keyed exactly as\n`Family.declared_shape()['tables']` keys them.  `covered` is the gate's scope: every\ncolumn of a covered table must be tagged, and every tag must name a real column.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, semantics: dict, covered: tuple) -> None"
    },
    "Schema/semantics.py::resolve": {
      "decorators": [],
      "doc": "The tag for one column, accepting physical OR logical names.\n\nA `ByDiscriminator` needs `row` (a mapping) to pick its case; without one it raises,\nbecause row-free semantics for such a column do not exist.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, table: str, column: str, row=None)"
    },
    "Schema/semantics.py::semantics_for": {
      "decorators": [],
      "doc": "(semantics, covered) for a registered family; raises on an unknown one.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str) -> tuple"
    },
    "Schema/semantics.py::sum_of": {
      "decorators": [],
      "doc": "Total `column` across `rows` \u2014 refused unless its kind is additive.\n\nThe additive statistic for a LEVEL is the count of rows non-zero, never the sum: a\nlevel re-counts the whole standing quantity every snapshot.",
      "is_async": false,
      "kind": "function",
      "signature": "(rows, family: str, table: str, column: str) -> float"
    },
    "Schema/semantics.py::validate_uses": {
      "decorators": [],
      "doc": "Refusal clauses for a consumer's declared reads, `Requires`-style.\n\n`uses` is {'table.column': 'sum' | 'ratio' | 'read'}.  Returned clauses are readable\nsentences naming the consumer, so a failure reads like a review comment rather than a\nstack trace.  Empty list = every declared use is legal.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, label: str, uses: dict) -> list"
    },
    "Schema/shape.py": {
      "decorators": [],
      "doc": "shape.py \u2014 the canonical SQL shape of a SQLite database, and the diff between two.\n\nStdlib only, and deliberately family-agnostic: every function here takes a bare\n`sqlite3.Connection` and knows nothing about which of the project's databases it is looking at.\nThat is what lets one implementation serve all eight families.\n\nWhat the shape covers, per table (excluding `sqlite_%`):\n  * `PRAGMA table_info`  -> (name, normalized type, notnull, pk) in cid order\n  * `PRAGMA index_list` + `index_info` -> each index's name, uniqueness, and ORDERED columns\n  * AUTOINCREMENT / WITHOUT ROWID, regexed out of `sqlite_master.sql`\n\nDeliberately EXCLUDED \u2014 do not \"fix\" these without reading why:\n  `sqlite_sequence`     a side effect of AUTOINCREMENT carrying no shape of its own, and not\n                        reliably present on a database rebuilt by another path.  The bit that\n                        matters is recovered by the AUTOINCREMENT flag instead.\n  `sqlite_stat1..4`     created by ANALYZE.  Hashing them would mint a new id for a statistics\n                        refresh and orphan every vetted reader \u2014 and ANALYZE is a plausible\n                        thing to run here, since one viewer query is an unindexed 24 s scan.\n  foreign keys          declared but never enforced (nothing sets `PRAGMA foreign_keys=ON`),\n                        so a read-only consumer cannot observe them.\n  views, triggers       none exist; excluding them makes adding one a conscious act.\n\nEverything is sorted, so the ORDER of CREATE statements is not a schema change. Column types are\nupper-cased and whitespace-collapsed but NOT resolved to SQLite affinities: renaming `INTEGER` to\n`INT` is harmless but real, and should be visible rather than silently absorbed.",
      "kind": "module",
      "signature": null
    },
    "Schema/shape.py::SHORT_LEN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "12"
    },
    "Schema/shape.py::_AUTOINCREMENT_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('\\\\bAUTOINCREMENT\\\\b', re.IGNORECASE)"
    },
    "Schema/shape.py::_WHITESPACE_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('\\\\s+')"
    },
    "Schema/shape.py::_WITHOUT_ROWID_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('\\\\bWITHOUT\\\\s+ROWID\\\\b', re.IGNORECASE)"
    },
    "Schema/shape.py::canonical_shape": {
      "decorators": [],
      "doc": "The hashable SQL shape of every user table on this connection.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection) -> dict"
    },
    "Schema/shape.py::describe_diff": {
      "decorators": [],
      "doc": "One finding per clause, for an exception message.",
      "is_async": false,
      "kind": "function",
      "signature": "(d: dict, limit: int=6) -> str"
    },
    "Schema/shape.py::diff_shapes": {
      "decorators": [],
      "doc": "Structural difference `expected` -> `actual`, as plain data.\n\nExists because every family's DDL is `CREATE TABLE IF NOT EXISTS`: a database written by old\ncode and reopened by new code gains the missing TABLES but never the missing COLUMNS, so it\nmatches neither declaration. \"hash 4f2a\u2026 is unknown\" is useless for that; \"picks is missing\ncolumn quantity\" is actionable.",
      "is_async": false,
      "kind": "function",
      "signature": "(expected: dict, actual: dict) -> dict"
    },
    "Schema/shape.py::is_same_shape": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(a: dict, b: dict) -> bool"
    },
    "Schema/shape.py::observed_id": {
      "decorators": [],
      "doc": "The id of the schema the database behind *con* actually has.",
      "is_async": false,
      "kind": "function",
      "signature": "(con: sqlite3.Connection) -> str"
    },
    "Schema/shape.py::shape_id": {
      "decorators": [],
      "doc": "12-hex id of a canonical shape.",
      "is_async": false,
      "kind": "function",
      "signature": "(shape: dict) -> str"
    },
    "Schema/shape.py::shape_of_ddl": {
      "decorators": [],
      "doc": "Canonical shape of a schema built in memory from DDL statements.\n\nThis is how a family declares its expected shape: by BUILDING it, never by hand-listing\ncolumns. A declaration derived from the same DDL the writer executes cannot drift from it.",
      "is_async": false,
      "kind": "function",
      "signature": "(statements) -> dict"
    },
    "Schema/store_index.py": {
      "decorators": [],
      "doc": "store_index.py \u2014 the mutable head of the committed shape store, and its change trigger.\n\n`Schema/shapes/<family>/<short>.json` documents are IMMUTABLE (filename = content hash).  Mutable\nstate \u2014 which declared id each family currently has, and a cheap trigger for \"did a DDL source\nchange since the store was last synced\" \u2014 needs somewhere else to live.  This is that somewhere:\n``Schema/shapes/INDEX.json``, the exact division of labour ``Optimization/schemas/run_tree/``\nuses (immutable documents + a mutable INDEX carrying `head` and `source_fingerprint`).\n\nTwo consumers, with opposite budgets:\n\n  * ``scripts/schema_report.py --sync`` \u2014 the ONLY writer.  It has every family imported, so it\n    can compute declared ids; it records them plus the fingerprint after committing shapes.\n  * ``Schema/hook_check.py`` \u2014 a Stop hook that must NOT import the writers (importing them pulls\n    ~1350 modules, matplotlib and pandas included).  It compares the fingerprint and stats the\n    per-family documents, nothing more.  The fingerprint is a cheap TRIGGER, not proof: a\n    docstring edit trips it, and the nag says \"run --sync\", which is idempotent and settles it.\n\nWHY THE FINGERPRINT IS COPIED, NOT IMPORTED\n-------------------------------------------\n``Optimization/runschema/contract.source_fingerprint`` is the same ~15 lines, but importing it\nhere is `schema -> optimization` \u2014 forbidden by `context/architecture.yml`, and enforced by\n`Tests/architecture/test_schema_compatibility.py::test_the_schema_package_imports_nothing_above_its_own_layer`.\nPaths-as-strings are data, not imports (the precedent is `contract.SHAPE_SOURCES` itself, which\nlists files across four layers).  A test keeps the copy honest by comparing the two algorithms on\nidentical input.",
      "kind": "module",
      "signature": null
    },
    "Schema/store_index.py::DDL_SOURCES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('Optimization/persistence/Picking_Data.py', 'Optimization/persistence/Warehouse_Data.py', 'Optimization/persistence/runtime_metrics.py', 'Visualization/cache_schema.py', 'Warehouse/generation/gene..."
    },
    "Schema/store_index.py::INDEX_PATH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_HERE, 'shapes', 'INDEX.json')"
    },
    "Schema/store_index.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Schema/store_index.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(_HERE, '..'))"
    },
    "Schema/store_index.py::read_index": {
      "decorators": [],
      "doc": "The committed INDEX, or None when the store has never been synced.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict | None"
    },
    "Schema/store_index.py::source_fingerprint": {
      "decorators": [],
      "doc": "Hash of every DDL-defining source file (path + content), in declared order.\n\nTHE ALGORITHM IS SHARED NOW (`Schema.fingerprint.of_files`) rather than copied from\n`runschema.contract`.  The copy this replaces is the precedent `profile_tree`'s own test\ncites, and it drifted for the same reason any uncompared copy does.  Files only, like\n`profile_tree` and unlike `contract`: no DDL source is auto-discovered.",
      "is_async": false,
      "kind": "function",
      "signature": "(repo_root: str=_REPO_ROOT) -> str"
    },
    "Schema/store_index.py::stale_reasons": {
      "decorators": [],
      "doc": "Cheap staleness findings for the hook \u2014 NO writer imports, file hashes and stats only.\n\nReturns human-readable reasons (empty = quiet).  Each names the remedy, which is always the\nsame idempotent command; deciding whether the trip was a real shape change or a docstring\nedit is --sync's job, not the hook's.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list[str]"
    },
    "Schema/store_index.py::write_index": {
      "decorators": [],
      "doc": "Record `{family: declared_id}` plus the current fingerprint.  Atomic (tmp + os.replace).\n\nOnly ``scripts/schema_report.py --sync`` calls this \u2014 it is the one place that has every\nfamily imported and every declared shape freshly committed.  Everything else treats INDEX as\nread-only evidence.",
      "is_async": false,
      "kind": "function",
      "signature": "(families: dict) -> str"
    },
    "Visualization/__init__.py": {
      "decorators": [],
      "doc": "Visualization \u2014 the spatial run viewer.\n\nA package so `python -m Visualization.precompute` works and the reader layer can be imported\npackage-absolutely.  See README.md for what belongs here; RECONSTRUCTION.md for what the\npersisted data can and cannot answer.\n\nNothing is imported eagerly: `server.py` needs flask, `precompute.py` must NOT, and the readers\nneed neither.  Importing this package pulls in nothing but this docstring.",
      "kind": "module",
      "signature": null
    },
    "Visualization/cache_schema.py": {
      "decorators": [],
      "doc": "cache_schema.py \u2014 DDL for the derived viewer sidecar (`_viz/**/<arm>.viz.db`).\n\nEverything here is DERIVED. Deleting a sidecar costs time, never data; the viewer runs without\none, just slowly. It exists because four things are too slow to compute per request on a\nproduction arm (384 aisles, 396,500 bins, 130,885 SKUs, 100 batches, ~1 GB sim DB):\n\n| Table                | Replaces                                                           |\n|----------------------|--------------------------------------------------------------------|\n| `aisle_batch_rollup` | a ~0.7 s whole-warehouse state rebuild per pane per batch step, run |\n|                      | only to count occupied bins per aisle                               |\n| `sku_rank`           | a measured 24.4 s `GROUP BY sku` full scan of `picks`               |\n| `sku_series`         | the same scan again, per batch                                      |\n| `bin_span`           | a measured ~5-9 s scan for one bin's history \u2014 `bin_keyframe`'s PK  |\n|                      | starts `(run_id, batch_id)`, so no per-aisle index is usable        |\n| `final_home`         | the per-SKU destination map the convergence colouring is built on   |\n\n**`bin_span` is built from the LOG \u2014 `bin_placement` + `bin_eviction` + `picks`.** Those three\ntables are the complete bin-mutation record (`Optimization/metrics/bin_recorder.py`, proved\nagainst a live simulation by `Tests/integration/test_bin_log_replay.py`), so a span is exact at\n**every** batch, not merely at the keyframes. `t_from`/`t_to` are therefore arbitrary batch ids.\n\nA span still keys on SKU CHANGE rather than on qty: qty churns every batch and a per-bin-per-batch\ntable would be 396,500 x 100 = 39.65M rows, larger than the sim DB it caches. Exact qty at an\narbitrary `(batch, t)` is `qty_at_from` minus the picks since `t_from`, which the reader already\ndoes.\n\nRuns written BEFORE the log existed (every arm of the current archive) fall back to spans built\nfrom the keyframes \u2014 exact only on the keyframe grid, which is the old contract\n(`RECONSTRUCTION.md` \u00a71). Which of the two produced a given sidecar is recorded in\n`cache_meta.span_source`, because the reader's `exact` flag depends on it and guessing would be\nthe same class of silent error the log exists to remove.\n\nThis module is DDL and nothing else, so `context/verify_context.py` can point a `schema_file` at\nit and check every declared table actually exists.",
      "kind": "module",
      "signature": null
    },
    "Visualization/cache_schema.py::AISLE_ROLLUP_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('run_id', 'batch_id', 'aisle_id', 'occupied', 'capacity', 'qty', 'n_skus', 'picks', 'units_picked', 'visits', 'task_secs', 'home_match')"
    },
    "Visualization/cache_schema.py::CACHE_VERSION": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "2"
    },
    "Visualization/cache_schema.py::SPAN_SOURCE_KEYFRAME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'keyframe'"
    },
    "Visualization/cache_schema.py::SPAN_SOURCE_LOG": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'log'"
    },
    "Visualization/cache_schema.py::TABLES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('cache_meta', 'bin_span', 'sku_rank', 'sku_series', 'final_home', 'aisle_batch_rollup')"
    },
    "Visualization/cache_schema.py::V1_KEYFRAME_SPAN_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'28124bfb4f93'"
    },
    "Visualization/cache_schema.py::VIZ_CACHE_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='viz_cache_db', declared_shape=declared_cache_shape, meta_table='cache_meta'))"
    },
    "Visualization/cache_schema.py::_ALL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(_CREATE_CACHE_META, _CREATE_BIN_SPAN, _CREATE_BIN_SPAN_IDX, _CREATE_SKU_RANK, _CREATE_SKU_RANK_IDX, _CREATE_SKU_SERIES, _CREATE_FINAL_HOME, _CREATE_AISLE_ROLLUP, _CREATE_AISLE_ROLLUP_IDX)"
    },
    "Visualization/cache_schema.py::_CREATE_AISLE_ROLLUP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS aisle_batch_rollup (\\n run_id INTEGER NOT NULL,\\n batch_id INTEGER NOT NULL,\\n aisle_id INTEGER NOT NULL,\\n occupied INTEGER NOT NULL,\\n capacity INTEGER NOT NULL,\\n ..."
    },
    "Visualization/cache_schema.py::_CREATE_AISLE_ROLLUP_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_rollup_aisle\\n ON aisle_batch_rollup (run_id, aisle_id, batch_id)\\n'"
    },
    "Visualization/cache_schema.py::_CREATE_BIN_SPAN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS bin_span (\\n run_id INTEGER NOT NULL,\\n aisle_id INTEGER NOT NULL,\\n bayX INTEGER NOT NULL,\\n bayY INTEGER NOT NULL,\\n t_from INTEGER NOT NULL, -- first batch holding..."
    },
    "Visualization/cache_schema.py::_CREATE_BIN_SPAN_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_span_batch\\n ON bin_span (run_id, t_from, t_to, aisle_id, bayX, bayY, sku, qty_at_from)\\n'"
    },
    "Visualization/cache_schema.py::_CREATE_CACHE_META": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS cache_meta (\\n key TEXT PRIMARY KEY,\\n value TEXT\\n )\\n'"
    },
    "Visualization/cache_schema.py::_CREATE_FINAL_HOME": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS final_home (\\n run_id INTEGER NOT NULL,\\n sku INTEGER NOT NULL,\\n aisle_id INTEGER NOT NULL, -- primary home\\n bayX INTEGER NOT NULL,\\n bayY INTEGER NOT NULL,\\n qty I..."
    },
    "Visualization/cache_schema.py::_CREATE_SKU_RANK": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS sku_rank (\\n run_id INTEGER NOT NULL,\\n sku INTEGER NOT NULL,\\n rank INTEGER NOT NULL, -- 1 = most units picked\\n picks INTEGER NOT NULL,\\n units INTEGER NOT NULL,\\n ..."
    },
    "Visualization/cache_schema.py::_CREATE_SKU_RANK_IDX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE INDEX IF NOT EXISTS ix_sku_rank ON sku_rank (run_id, rank)\\n'"
    },
    "Visualization/cache_schema.py::_CREATE_SKU_SERIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS sku_series (\\n run_id INTEGER NOT NULL,\\n sku INTEGER NOT NULL,\\n batch_id INTEGER NOT NULL,\\n picks INTEGER NOT NULL,\\n units INTEGER NOT NULL,\\n PRIMARY KEY (run_id..."
    },
    "Visualization/cache_schema.py::cache_freshness": {
      "decorators": [],
      "doc": "'fresh' | 'stale' | 'absent' | 'partial'.\n\nLives here rather than in `precompute` so the READ path can call it without importing the\nbuilder (and without `readers -> precompute -> db_reader` becoming an import cycle).",
      "is_async": false,
      "kind": "function",
      "signature": "(viz_cache: str, sim_db: str, keyframe_db: str, warehouse_db: str) -> str"
    },
    "Visualization/cache_schema.py::declared_cache_shape": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Visualization/cache_schema.py::init_cache_db": {
      "decorators": [],
      "doc": "Create the sidecar and return an OPEN connection tuned for bulk writing.\n\nThe pragmas are safe precisely because this file is derived: a crash mid-build leaves a\ncache with no `built_utc`, which `precompute` treats as absent and rebuilds.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> sqlite3.Connection"
    },
    "Visualization/cache_schema.py::source_stamps": {
      "decorators": [],
      "doc": "Size + mtime of every source, so a rebuilt sim DB invalidates the cache.\n\n`st_mtime_ns` rather than `st_mtime`: the float form cannot be compared with `==` (repo rule)\nand the integer form is exact. Size AND mtime, because some volumes carry coarse timestamps\nand a rewrite inside the granularity window would otherwise be invisible.\n\nAn absent source records `<absent>` rather than being omitted \u2014 otherwise DELETING the\nkeyframe DB after a build would leave the cache reporting `fresh`.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_db: str, keyframe_db: str, warehouse_db: str) -> dict"
    },
    "Visualization/db_reader.py": {
      "decorators": [],
      "doc": "db_reader.py \u2014 run DISCOVERY, and the binding from a discovered run to a versioned reader.\n\nThis file owns two things and delegates everything else:\n\n  * :class:`RunRef` \u2014 one arm's coordinate on the five navigation axes, plus its file paths.\n  * :func:`discover_runs` \u2014 every arm across a whole run tree, resolved through the run's OWN\n    run-tree contract so an old run stays navigable after the tree shape moves on.\n\nAll *reading* lives in `Visualization/readers/`, one vetted implementation per sim-DB schema.\nThat split is the point: `discover_runs` answers \"what is here\", the readers answer \"what does it\nsay\", and a schema change touches only the second.\n\nBoth symbols must stay DEFINED IN THIS FILE.  `context/verify_context.py` matches\n``^\\s*(def|class)\\s+discover_runs\\b`` against this exact path, driven by the reader anchors in\n`context/artifacts.yml` for warehouse_db, sim_db and keyframes_db.  A re-export from elsewhere\ndoes not satisfy that regex.",
      "kind": "module",
      "signature": null
    },
    "Visualization/db_reader.py::AXIS_LABELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'pair': 'Warehouse', 'channel': 'Warehouse type', 'config': 'Pick config', 'cell': 'Layout / scheduler cell', 'strategy': 'Assignment function'}"
    },
    "Visualization/db_reader.py::NAV_AXES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('pair', 'channel', 'config', 'cell', 'strategy')"
    },
    "Visualization/db_reader.py::REQUIRES_DISCOVERY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='Visualization discovery (_read_run_meta)', tables={'simulation_runs': _compat.ANY_COLUMNS})"
    },
    "Visualization/db_reader.py::RunRef": {
      "decorators": [
        "dataclass"
      ],
      "doc": "One arm: where it is on the navigation axes, and which files hold it.",
      "kind": "class",
      "signature": ""
    },
    "Visualization/db_reader.py::RunRef.axis_values": {
      "decorators": [
        "property"
      ],
      "doc": "This run's coordinate on every navigation axis \u2014 what the UI filters on.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Visualization/db_reader.py::RunRef.reader": {
      "decorators": [],
      "doc": "The vetted reader for this arm, bound once and reused.\n\nReaders hold no live connection, so one instance is safe to share across request\nthreads; every query opens and closes its own read-only connection.\n\nThe sim DB's identity is `reader_for`'s job \u2014 that binding IS the sim-schema check, and\nan unknown shape there raises `UnsupportedSimSchema`.  Its two companion files have no\nsuch gate, so they get one here: geometry read from an unexpected `warehouse.db` draws a\nplausible warehouse that is quietly wrong, which is the same failure in a different\nartifact.\n\nWARNS rather than raising.  The viewer is an interactive, strictly read-only explorer of\nan archive that spans two months of schema evolution; refusing to open a 2026-06 run\noutright is a worse outcome than drawing it with the differing columns named in the log,\nand nothing here is published.  The paths that WRITE from a file, or publish a number\nfrom one, use the hard `identity.check` instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, verify: bool=False)"
    },
    "Visualization/db_reader.py::_discover_uncached": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> list[RunRef]"
    },
    "Visualization/db_reader.py::_nearest_warehouse_db": {
      "decorators": [],
      "doc": "The warehouse.db an arm was simulated against.\n\nWith a resolver (`rt` + the arm's cell/pair), the contract answers directly:\n``rt.warehouse_db(cell, pair)``.  The walk-up below is the documented NO-CONTRACT fallback \u2014\nlegacy runs have no descriptor, so the nearest `warehouse.db` on the ancestor chain is the\nbest available guess.  It is kept even when the resolver path misses, because a hand-arranged\ntree (an arm copied out for inspection) answers only to the walk.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_db: str, rt=None, cell: str | None=None, pair: str | None=None) -> str | None"
    },
    "Visualization/db_reader.py::_pinned_schema_id": {
      "decorators": [],
      "doc": "The schema id a previous precompute derived and cached, if any.\n\nEvery DB in the archive predates the `sim_schema_id` column, so without this the id is\nre-derived (~10 PRAGMA round trips) on every binding, times hundreds of arms.",
      "is_async": false,
      "kind": "function",
      "signature": "(viz_cache: str) -> str | None"
    },
    "Visualization/db_reader.py::_read_run_meta": {
      "decorators": [],
      "doc": "First run's identity from a sim DB, tolerant of the pre-identity schema.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_db: str) -> dict | None"
    },
    "Visualization/db_reader.py::_tree_stamp": {
      "decorators": [],
      "doc": "A cheap key that changes when the tree gains an arm or is replaced.\n\nOne `os.walk` (~0.15 s even on the results drive) over the run descriptor's mtime plus the\nnumber of `sim_*.db` files.  A finished run is static, so this is stable; a run still being\nwritten gains arms, and the count catches that without re-opening 272 databases.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> tuple"
    },
    "Visualization/db_reader.py::_warehouse_fingerprint": {
      "decorators": [],
      "doc": "The fingerprint via the full pipeline: bind the file to ITS OWN vintage and ask by\nname \u2014 the pre-fingerprint vintage is served by the repo's first production override\n(`Warehouse_Data`), which answers NULL instead of raising.  Discovery is tree-stamp-cached\nand touches a handful of warehouse files, so full binds are free here.  The blanket\nguard stays: unvetted/truncated -> None -> the walk-up fallback, unchanged.",
      "is_async": false,
      "kind": "function",
      "signature": "(warehouse_db: str) -> str | None"
    },
    "Visualization/db_reader.py::_warehouse_fp_index": {
      "decorators": [],
      "doc": "Map warehouse_fingerprint -> warehouse.db across the tree, so a run resolves its warehouse\neven if folders were renamed or moved after the run finished.\n\nCached on the tree stamp.  This walks the WHOLE run tree \u2014 up to ~500 GB \u2014 and it used to run\non every /api/runs request.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> dict[str, str]"
    },
    "Visualization/db_reader.py::discover_runs": {
      "decorators": [],
      "doc": "One RunRef per strategy run across the WHOLE run tree, via the versioned resolver.\n\n`base_dir` is the RUN ROOT (the directory holding `run_layout.json`), not a cell directory:\n`runschema.resolver_for` reads the run's own `run_layout.json` and spans all its cells.\n\nRename-proof: strategy/pair/config labels come from the DB's stored identity (falling back to\nthe file and directory names), and each run's warehouse.db is matched by\nwarehouse_fingerprint (falling back to the nearest warehouse.db by path).  The CELL, however,\nexists only in the path \u2014 sim_*.db has no cell column \u2014 so it always comes from the resolver.\n\nCached on the tree stamp: identifying 272 arms costs ~5.7 s of sim-DB opens on the results\ndrive, and `/api/runs` is hit on every page load.  Cached RunRefs keep their bound readers,\nwhich is the point \u2014 readers hold no live connection, so sharing one across request threads\nis safe.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> list[RunRef]"
    },
    "Visualization/db_reader.py::run_index": {
      "decorators": [],
      "doc": "{schema_id, schema_short, axes, axis_labels, runs} \u2014 the navigation contract for the UI.\n\n`axes` holds the distinct values actually present, so the front end never hardcodes a level.\nA store-only run yields `channel: []`; the UI HIDES that selector rather than inventing a\nvalue, which is the same optionality the run-tree contract declares.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str) -> dict"
    },
    "Visualization/db_reader.py::viz_cache_path": {
      "decorators": [],
      "doc": "Where the derived sidecar for one arm lives.\n\n``<run_root>/_viz/<cell>/<pair>/<config>[/<channel>]/<arm>.viz.db`` \u2014 under the driver's\nreserved ``_`` prefix (``runschema.schema.RESERVED_PREFIX``), which every tree walker skips.\nThe sidecar is DECLARED in the run-tree contract (`viz_cache_db`), so with a resolver the\ntemplate comes from the run's own schema document; the hand-join below is the documented\nNO-CONTRACT fallback for legacy trees, kept byte-identical to what the template renders.\n\nNOT ``sim_<arm>.viz.db`` beside the sim DB: that name satisfies all three predicates of\n``runlayout._sim_dbs_in`` (starts with ``sim_``, ends ``.db``, does not end\n``.keyframes.db``), so it would surface as an extra ARM through ``resolver.sim_dbs()`` and\ncrash ``Diagnostics/replay_run.py`` on a table it does not have.",
      "is_async": false,
      "kind": "function",
      "signature": "(base_dir: str, cell: str, pair: str, config: str, channel: str | None, strategy: str, rt=None) -> str"
    },
    "Visualization/precompute.py": {
      "decorators": [],
      "doc": "precompute.py \u2014 build the derived viewer sidecar for one or more arms.\n\n    python -m Visualization.precompute \"<run_root>\" --cell k1_off_rr --config store \\\n           --channel store --arms uni_fifo_norsl,opt_rank_labor_norsl\n\nEverything it writes is derived from the sim + keyframe DBs; deleting `_viz/` costs time, never\ndata. Filters are not a convenience \u2014 a full sweep is up to 272 arms, so the default is \"the arms\nyou name\", never \"everything\".\n\nCost per arm is dominated by two sequential passes:\n\n  * `picks` (~3.1 M rows) -> `sku_rank`, `sku_series`, per-aisle pick counts, per-bin depletion\n  * the bin-mutation LOG (~2.2 M rows) -> `bin_span`, occupancy, `final_home`\n\nBoth are single ordered scans over an existing index. The log pass reads `bin_placement` and\n`bin_eviction` in `(run_id, batch_id, seq)` order \u2014 their PK \u2014 and the picks pass takes no\n`ORDER BY` at all, because neither has an index on `aisle_id` and asking for one would add a\nmulti-million-row temp sort for nothing.\n\nTwo span sources, and the sidecar records which one it used\n-----------------------------------------------------------\n`bin_placement + bin_eviction + picks` is the complete bin-mutation record, so folding it gives\nthe simulation's own bin state at EVERY batch (`Tests/integration/test_bin_log_replay.py` proves\nthis against a live sim). Arms written before the log existed \u2014 every arm of the current archive \u2014\nfall back to `_keyframe_pass`, whose spans are exact only on the keyframe grid, which is the old\ncontract (`RECONSTRUCTION.md` \u00a71). `cache_meta.span_source` says which, and the reader will only\nreport `exact` off the keyframe grid for the first.\n\nMust not import flask: this runs under the bare repo interpreter.",
      "kind": "module",
      "signature": null
    },
    "Visualization/precompute.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='Visualization precompute (raw sidecar builder)', tables={'picks': ('run_id', 'batch_id', 'aisle_id', 'bayX', 'bayY', 'sku', 'quantity'), 'batch_stats': ('ru..."
    },
    "Visualization/precompute.py::_BATCH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "50000"
    },
    "Visualization/precompute.py::_bin_spans": {
      "decorators": [],
      "doc": "Fold ONE bin's PLACE/EVICT/PICK events into its exact occupancy spans.\n\n`events` are `(batch, kind, seq, sku, qty, topup)` sorted ascending, `kind` 0 = EVICT,\n1 = PLACE \u2014\nso the sort itself imposes the runner's within-batch order, EVICT -> PLACE -> PICK: the\nreloader runs first, then `check_reorders()`, and only then the pick simulation.\n`picks` is `{batch: units}` for this bin, or None.\n\nA span therefore OPENS at the batch of its PLACE (the bin already holds the unit at that\nbatch's START, which is the frame every keyframe and every `state_at` is defined on) and\nCLOSES at whichever comes first:\n\n  * the batch before the next PLACE/EVICT \u2014 a span that opens and closes inside one batch is\n    never visible at start-of-batch resolution and is dropped (`t_to < t_from`);\n  * the batch whose picks drain it to zero.\n\nReturns `[(t_from, t_to, sku, qty_at_from, applied)]`, where `applied` is the\n`[(batch, units)]` actually consumed inside the span \u2014 the caller needs it to keep the\nper-aisle qty rollup exact without re-reading `picks`.\n\nA TOP-UP (ADR-0003's own-bin rung, `bin_placement.bin_state = 'occupied'`) carries\n`topup = 1` and shares `kind = 1` with a plain PLACE deliberately, so the two sort by\n`seq` and keep the runner's true within-batch order.  It does not open an occupancy \u2014\nthe bin was already occupied \u2014 so it CLOSES the running span at `ev_batch - 1` and\nreopens one at `ev_batch` carrying `remaining + qty`.  A span holds a single `qty0` by\nconstruction (quantity inside one only ever falls, via picks), so a rise has to be a new\nspan; closing and reopening keeps the model exact at the start-of-batch resolution every\nframe is defined on.",
      "is_async": false,
      "kind": "function",
      "signature": "(events, picks, last_batch)"
    },
    "Visualization/precompute.py::_executemany": {
      "decorators": [],
      "doc": "Chunked executemany so a 3M-row insert never materialises as one list.",
      "is_async": false,
      "kind": "function",
      "signature": "(con, sql, rows_iter)"
    },
    "Visualization/precompute.py::_fmt": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(res: dict) -> str"
    },
    "Visualization/precompute.py::_keyframe_pass": {
      "decorators": [],
      "doc": "One ordered scan of `bin_keyframe` -> (spans, per-(batch,aisle) occupancy, final homes).\n\nThe FALLBACK for an arm written before `bin_placement` existed. Reads in `(run_id, batch_id)`\norder so the existing PK/index serves it. Spans close when a bin's SKU changes between\nconsecutive keyframes; a bin that disappears from a keyframe has become empty and its span\ncloses at the previous one.\n\nThese spans live on the KEYFRAME grid: they say nothing about what happened between two\nsnapshots, which is exactly the 59% gap `RECONSTRUCTION.md` \u00a71 measures. `build_one` records\n`span_source=keyframe` so the reader keeps reporting `exact: false` off that grid.",
      "is_async": false,
      "kind": "function",
      "signature": "(kf_con, run_id)"
    },
    "Visualization/precompute.py::_keyframe_pass.close_missing": {
      "decorators": [],
      "doc": "Any bin not seen in the keyframe just finished is now empty; close its span.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/precompute.py::_log_pass": {
      "decorators": [],
      "doc": "One ordered scan each of `bin_placement` + `bin_eviction` -> spans exact at EVERY batch.\n\nReturns `(spans, homes, final_batch, deltas)`.  `deltas` holds per-`(aisle, batch)` STEPS for\nthe four rollup metrics rather than their values: a span contributes `+1` at `t_from` and\n`-1` at `t_to + 1`, so the rollup prefix-sums in O(spans + picks) instead of expanding\nbins x batches \u2014 2.2M steps against 39.65M cells on a production arm.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_con, run_id, batches, bin_picks)"
    },
    "Visualization/precompute.py::_picks_pass": {
      "decorators": [],
      "doc": "One scan of `picks` -> per-SKU totals, per-(batch,aisle) pick counts, top-N series.\n\nReplaces the measured 24.4 s `GROUP BY sku` and a second scan for the series. Aggregated in\nPython rather than SQL so one read feeds all of them.\n\n`want_bin_picks` adds per-`(bin, batch)` depletion, which is the third input the span fold\nneeds (PLACE + EVICT tell it what a bin holds; picks tell it when the bin runs dry). It is\nopt-in because it is the one aggregate proportional to the pick count rather than to the SKU\nor aisle count, and the keyframe fallback has no use for it.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_con, run_id, top_n, want_bin_picks=False)"
    },
    "Visualization/precompute.py::_rollup_rows": {
      "decorators": [],
      "doc": "Per (batch, aisle) rollups on the KEYFRAME grid \u2014 the pre-log fallback.\n\nOccupancy is only known at keyframes, so a non-keyframe batch inherits the nearest keyframe\nat or below it \u2014 the same rule `state_at` uses without a log, and it is why `home_match` and\n`occupied` step rather than glide between keyframes.",
      "is_async": false,
      "kind": "function",
      "signature": "(run, batches, capacity, occupancy, aisle_picks, task_secs, home_sets, spans)"
    },
    "Visualization/precompute.py::_rollup_rows_from_log": {
      "decorators": [],
      "doc": "Per (batch, aisle) rollups, prefix-summed from `_log_pass`'s per-batch steps.\n\nExact at EVERY batch \u2014 nothing inherits a keyframe any more. The running totals are carried\nacross the full batch range rather than across `batches`, because a batch that produced no\ntasks writes no `batch_stats` row (`RECONSTRUCTION.md` \u00a77) and skipping its steps would\ndesynchronise every batch after it.",
      "is_async": false,
      "kind": "function",
      "signature": "(run, batches, capacity, deltas, aisle_picks, task_secs)"
    },
    "Visualization/precompute.py::_select": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(runs, args)"
    },
    "Visualization/precompute.py::_select.keep": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(r)"
    },
    "Visualization/precompute.py::_source_stamps": {
      "decorators": [],
      "doc": "This arm's source stamps, via the single implementation in cache_schema.",
      "is_async": false,
      "kind": "function",
      "signature": "(run) -> dict"
    },
    "Visualization/precompute.py::_worker": {
      "decorators": [],
      "doc": "Module-level entry point: ProcessPoolExecutor spawns, so this must be picklable.\n\nThe RunRef is passed IN rather than re-discovered.  Calling discover_runs here would make\nevery worker walk the whole run root and open every warehouse.db and sim DB \u2014 ~272 walks and\n~74k redundant opens on the results drive across a full sweep, since each payload is one arm\nand the per-process cache never gets a second hit.  RunRef is a plain dataclass whose\n`_reader` is None at submit time, so it pickles.",
      "is_async": false,
      "kind": "function",
      "signature": "(args)"
    },
    "Visualization/precompute.py::build_one": {
      "decorators": [],
      "doc": "Build (or refresh) one arm's sidecar. Returns a small report dict.\n\n`verify` is SUBSUMED: dataset.bind runs with verify=True unconditionally now (precompute\nwrites a persistent record, so a migrated file must fail the arm, not be pinned).  The\nflag is kept so existing invocations don't break; it changes nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(run, top_n: int=500, force: bool=False, verify: bool=False) -> dict"
    },
    "Visualization/precompute.py::cache_state": {
      "decorators": [],
      "doc": "'fresh' | 'stale' | 'absent' | 'partial' for this arm's sidecar.\n\nSame function the READ path uses (`SqliteSimReader.cache_status`), so the builder and the\nviewer can never disagree about whether a cache is trustworthy.",
      "is_async": false,
      "kind": "function",
      "signature": "(run) -> str"
    },
    "Visualization/precompute.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "Visualization/readers/__init__.py": {
      "decorators": [],
      "doc": "readers \u2014 the versioned data layer.\n\n`reader_for(...)` is the ONE entry point.  It resolves a sim DB's schema id, looks up the vetted\nreader registered for it, and binds it to the arm's files.  Everything above this line \u2014 routes,\nviews, the front end \u2014 sees only the `SimReader` protocol and never learns which schema it read.\n\nAdding support for a new schema is one module: subclass `SqliteSimReader` (or implement the\nprotocol outright), set `SCHEMA_IDS`, and register it here.  No route and no view changes.",
      "kind": "module",
      "signature": null
    },
    "Visualization/readers/__init__.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['reader_for', 'register', 'registered_schema_ids', 'SimReader', 'SimSchemaError', 'UnsupportedSimSchema', 'SimSchemaDrift', 'SqliteSimReader']"
    },
    "Visualization/readers/__init__.py::_assert_json1": {
      "decorators": [],
      "doc": "The viewer's scoped named queries pass id lists as ONE JSON parameter via json_each.\n\nJSON1 has been compiled into the bundled SQLite by default since 3.38 (Python 3.11 ships\nnewer), so this probe should never fire \u2014 but a viewer that silently lacked it would fail\non the first scoped request with an opaque OperationalError mid-session.  Probe ONCE at\nimport and fail with the cause named.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Visualization/readers/__init__.py::reader_for": {
      "decorators": [],
      "doc": "Bind the vetted reader for `sim_db`.\n\n`pinned_schema_id` short-circuits derivation \u2014 pass the value the sidecar cached, so a DB\nwith no stamp is inspected once at precompute time rather than on every request.\n\nRaises `UnsupportedSimSchema` (no vetted reader) or `SimSchemaDrift` (`verify=True` and the\nfile disagrees with what was stamped or pinned).  Neither is recoverable by guessing, which\nis the point: a viewer that reads an unknown schema on a best-effort basis produces a\nwarehouse picture that looks right and is not.",
      "is_async": false,
      "kind": "function",
      "signature": "(sim_db: str, warehouse_db: str, run_id: int, *, keyframe_db: str='', viz_cache: str='', pinned_schema_id: str | None=None, verify: bool=False, warehouse_schema_id: str | None=None, keyframe_schema_id: str | None=None) -> SimReader"
    },
    "Visualization/readers/__init__.py::register": {
      "decorators": [],
      "doc": "Register a reader class under every id in its `SCHEMA_IDS`.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls: type) -> type"
    },
    "Visualization/readers/__init__.py::registered_schema_ids": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple[str, ...]"
    },
    "Visualization/readers/base.py": {
      "decorators": [],
      "doc": "base.py \u2014 the SQLite implementation of `SimReader`, shared by every vetted schema.\n\nOne instance per arm.  Holds paths and small memos, never a live connection: `server.py` runs\nthreaded and `sqlite3.Connection` objects are `check_same_thread=True`, so a cached connection\ncrossing a request boundary raises.  Read-only URI opens are sub-millisecond against a 1 GB file\n\u2014 trivial next to the ~0.5 s keyframe read they precede \u2014 so every query opens and closes its own.\n\nThe reconstruction contract is `RECONSTRUCTION.md` \u00a71, and `state_at` has three implementations of\nit because a run can carry three different records:\n\n  1. **span index** \u2014 a fresh sidecar whose `bin_span` was built from the bin-mutation log.  One\n     indexed lookup per frame, exact at every batch.\n  2. **live log fold** \u2014 the run has `bin_placement`/`bin_eviction` but no (fresh) sidecar: take\n     the nearest keyframe and apply the log forward.  Exact, and bounded by the keyframe interval.\n  3. **keyframe + picks** \u2014 the pre-log archive.  Its `bin_inventory` (a table no run writes any\n     more) records picks and *never* restocks, so rolling deltas forward loses 59% of a real\n     warehouse within five batches; this path is exact only AT a keyframe and says\n     `exact: false` everywhere else.\n\nWhich one served a frame is visible in the payload: `exact` is the honest answer, never the\nconvenient one.\n\nThat `exact` is NOT `capability.provenance(cap)['exact']`, and the two must not be conflated.  A\ncapability's `exact` grades the SOURCE at its own phase \u2014 `CAP_KEYFRAMES.exact` is True because a\nkeyframe is an exact snapshot OF ITS OWN BATCH.  The payload's `exact` grades THIS FRAME, which for\nthe same source is `batch == kf` and false everywhere in between.  So the frame payload keeps its\nown `exact` / `restocks_pending` / `note` contract (`protocol.py`, and `static/views/aisle.js` and\n`diff.js` read those three by name); provenance is for a consumer that reports which source it\nchose, which the reader does through `capabilities()` instead.",
      "kind": "module",
      "signature": null
    },
    "Visualization/readers/base.py::REQUIRES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='sim_db', label='Visualization reader (SqliteSimReader)', tables={'simulation_runs': _compat.ANY_COLUMNS, 'batch_stats': ('run_id', 'batch_id', 'duration', 'num_tasks', 'tot..."
    },
    "Visualization/readers/base.py::REQUIRES_KEYFRAMES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='keyframes_db', label='Visualization reader (keyframe sidecar)', tables={'bin_keyframe': ('run_id', 'batch_id', 'aisle_id', 'bayX', 'bayY', 'sku', 'qty')})"
    },
    "Visualization/readers/base.py::REQUIRES_VIZ_CACHE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='viz_cache_db', label='Visualization reader (viz sidecar)', tables={'cache_meta': ('key', 'value'), 'bin_span': ('run_id', 'aisle_id', 'bayX', 'bayY', 't_from', 't_to', 'sku..."
    },
    "Visualization/readers/base.py::REQUIRES_WAREHOUSE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_compat.Requires(family='warehouse_db', label='Visualization reader (warehouse geometry)', tables={'aisle_layout': ('aisle_id', 'handling_type', 'category', 'unit_type', 'storage_size', 'bay_x', 'b..."
    },
    "Visualization/readers/base.py::SEMANTIC_USES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': {'simulation_runs.run_id': 'read', 'sku_scores.sku': 'read', 'sku_scores.map_target': 'read', 'sku_scores.labor_cost': 'read', 'sku_scores.handle_var': 'read', 'sku_scores.expected_popul..."
    },
    "Visualization/readers/base.py::SqliteSimReader": {
      "decorators": [],
      "doc": "Read one arm's `sim_<arm>.db` (+ keyframes, warehouse, and optional viz cache).",
      "kind": "class",
      "signature": ""
    },
    "Visualization/readers/base.py::SqliteSimReader.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, sim_db: str, warehouse_db: str, run_id: int, keyframe_db: str='', viz_cache: str='', schema_id: str='', schema_source: str='', warehouse_schema_id: str | None=None, keyframe_schema_id: str | None=None)"
    },
    "Visualization/readers/base.py::SqliteSimReader._apply_picks_upto_t": {
      "decorators": [],
      "doc": "Subtract batch `batch`'s own picks up to sim_time `t`.  No-op when `t` is None.\n\nThis is the intra-batch clock: `picks.sim_time` is the finest resolution the record has,\nand it is unaffected by the log (restocks carry no sub-batch time \u2014 `check_reorders()`\nruns entirely between two batches' simulations).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, con, bins: dict, batch: int, t, scope: str) -> None"
    },
    "Visualization/readers/base.py::SqliteSimReader._cache_conn": {
      "decorators": [],
      "doc": "An open sidecar connection, or None when there is nothing trustworthy to read.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> sqlite3.Connection | None"
    },
    "Visualization/readers/base.py::SqliteSimReader._has": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, family: str, table: str) -> bool"
    },
    "Visualization/readers/base.py::SqliteSimReader._has_col": {
      "decorators": [],
      "doc": "Does this vintage's `table` carry `column`?  The column-level twin of `_has`, and\nthe same no-PRAGMA answer off the committed shape.  Needed once `bin_placement`\ngained `bin_state` (ADR-0003): the table is old, the column is not.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, family: str, table: str, column: str) -> bool"
    },
    "Visualization/readers/base.py::SqliteSimReader._keyframe_at_or_before": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int) -> int | None"
    },
    "Visualization/readers/base.py::SqliteSimReader._keyframe_state": {
      "decorators": [],
      "doc": "`{bin: {sku, qty}}` at one keyframe \u2014 the post-restock, pre-pick start of that batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, keyframe: int | None, aisles) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader._log_start": {
      "decorators": [],
      "doc": "The first batch this run recorded a PLACE for, or None if it has no log.\n\n`MIN(batch_id)` on the `(run_id, batch_id, seq)` primary key, so it is an index seek,\nnot a scan.  None is the normal answer for every arm in the archive \u2014 those runs predate\n`bin_placement` \u2014 which is why the missing table is caught rather than raised.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int | None"
    },
    "Visualization/readers/base.py::SqliteSimReader._pending_restocks": {
      "decorators": [],
      "doc": "Restock placements this frame cannot show: batches (keyframe, batch].\n\nThe keyframe already includes its OWN batch's restocks \u2014 `check_reorders()` runs before\nthe snapshot is taken.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, keyframe: int, batch: int) -> int"
    },
    "Visualization/readers/base.py::SqliteSimReader._sid": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, family: str) -> str"
    },
    "Visualization/readers/base.py::SqliteSimReader._span_index": {
      "decorators": [],
      "doc": "An open sidecar, but ONLY when its `bin_span` came from the bin-mutation log.\n\nA v1 sidecar, or one rebuilt from the keyframes because the arm predates the log, holds\nspans on the keyframe grid.  Reading those as if they were exact between keyframes is\nprecisely the error this whole change removes, so the source is checked, not assumed.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> sqlite3.Connection | None"
    },
    "Visualization/readers/base.py::SqliteSimReader._sql": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, family: str, name: str) -> str"
    },
    "Visualization/readers/base.py::SqliteSimReader._state_from_keyframes": {
      "decorators": [],
      "doc": "The pre-log frame: nearest keyframe below, minus the picks since.\n\nExact when `batch` IS a keyframe batch.  Otherwise depletion-exact but missing the\nrestocks of the batches in between, which is reported rather than hidden.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisles: list[int] | None=None, t: float | None=None) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader._state_from_log": {
      "decorators": [],
      "doc": "The frame folded live from `bin_placement` + `bin_eviction` + `picks`.\n\nUsed when the run HAS a log but no fresh sidecar. The nearest keyframe is the base and\nthe log carries it forward, so the work is bounded by the keyframe interval. With no\nkeyframe below, an empty warehouse is the base \u2014 valid only when the log starts at batch\n0, i.e. it recorded the initial fill; a resumed run whose log starts later cannot be\nanchored and falls through.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisles, t) -> dict | None"
    },
    "Visualization/readers/base.py::SqliteSimReader._state_from_spans": {
      "decorators": [],
      "doc": "The frame from the sidecar's log-built span index. None = no such index.\n\nThe span gives the bin's SKU and the qty it was filled with at `t_from`; the qty NOW is\nthat minus the picks since.  A keyframe at or below `batch` is used as the qty anchor\nwhenever one exists \u2014 not for correctness, but to bound the pick scan to one keyframe\ninterval instead of the whole run.  Without keyframes the answer is the same, read from\na longer range.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisles, t) -> dict | None"
    },
    "Visualization/readers/base.py::SqliteSimReader._state_without_keyframes": {
      "decorators": [],
      "doc": "Last-resort path: an ARCHIVED run written with keyframe_interval=0.\n\nStill reachable, and not dead code: it is the only record such an arm has.  A run from\nthis build never lands here twice over \u2014 it has a log, so `_state_from_log` folds it from\nan empty warehouse exactly, and `bin_inventory` is not even in its schema.\n\n`bin_inventory` (archive-only; no longer written) holds one full snapshot at the run's\nFIRST batch \u2014 not necessarily 0 on a resumed run \u2014 and depletion-only deltas after it.\nOrdering is by `batch_id, id`: the loop is last-write-wins and a bin can receive rows\nfrom two branches in one batch, so `batch_id` alone is not a total order.\n\nThe table's ABSENCE is a normal outcome, not an error: it means the file is new enough\nto have no such record and old enough (keyframe_interval=0) to have no keyframe either.\nSay so, rather than raising `no such table` at a caller three layers up.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisles, t) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.aisle_geometry": {
      "decorators": [],
      "doc": "One row per aisle \u2014 never a per-bin list.\n\nMaterializing every bin for all 384 aisles is ~396,500 dicts and a ~40 MB JSON response\nper pane; the grid is regenerated from bay_x x bay_y wherever it is actually drawn.\n\nAlso assigns each aisle its palette coordinates: `family_index` (which of the 13 hue\nanchors) and `family_ord` (its rank inside that family's hue band, ordered by category\nthen aisle_id so same-category aisles sit adjacent).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[dict]"
    },
    "Visualization/readers/base.py::SqliteSimReader.aisle_rollup": {
      "decorators": [],
      "doc": "Per-aisle aggregates for one batch: occupancy, picks, visits.\n\nThe live fallback rebuilds whole-warehouse state (~0.7 s) purely to count occupied bins\nper aisle \u2014 which is what the sidecar's `aisle_batch_rollup` precomputes away.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int) -> list[dict]"
    },
    "Visualization/readers/base.py::SqliteSimReader.aisle_state": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisle: int, t: float | None=None) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.batch_index": {
      "decorators": [],
      "doc": "Per-batch timing, from `batch_stats` \u2014 the list legitimately has holes.\n\nA batch that produced no tasks `continue`s before its stats are appended, so\n`range(n_batches)` walks batches with no timing, no events and no deltas.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[dict]"
    },
    "Visualization/readers/base.py::SqliteSimReader.bin_history": {
      "decorators": [],
      "doc": "Everything this bin held, one row per occupancy.\n\nServed from the sidecar's `bin_span` when present \u2014 where, on a log-built cache, the\n`t_from`/`t_to` bounds are the real batches the bin held that SKU rather than the\nkeyframes it was observed at.  The live fallback is a scan of `bin_keyframe`, whose PK\nstarts `(run_id, batch_id)`, so there is no usable per-aisle index and it costs ~5 s on a\nproduction DB.  That is exactly why the sidecar exists.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisle: int, bayX: int, bayY: int) -> list[dict]"
    },
    "Visualization/readers/base.py::SqliteSimReader.bin_scores": {
      "decorators": [],
      "doc": "Static per-bin layout cost.  ALWAYS scope on a real warehouse.\n\nUnscoped this is 396,500 rows and a ~20 MB response; the viewer only ever draws the\naisles on screen.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisles: list[int] | None=None) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.cache_status": {
      "decorators": [],
      "doc": "'fresh' | 'stale' | 'absent' | 'partial' \u2014 re-checked, never pinned.\n\nFreshness matters on the READ path, not just at build time: an arm rebuilt by a\nstrategy-granularity resume gets a brand-new sim DB and its own `run_id = 1`, so a stale\nsidecar's rows collide by key and the viewer would keep serving the PREVIOUS run's\n`final_home` (the colour authority) and occupancy with nothing to signal it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Visualization/readers/base.py::SqliteSimReader.capabilities": {
      "decorators": [],
      "doc": "Which optional data this arm actually HAS \u2014 probed for ROWS, then memoised.\n\nA present-but-empty table is the common case, not an edge case: `aisle_metrics` and\n`reorder_queue` are only written by strategies that maintain that state, and are empty\nfor every arm of the current production run.  Reporting them as absent is what lets the\nUI hide a panel instead of rendering 0.0 as though it were a measurement.  That is why\nthe sweep is `Schema.capability.probe` \u2014 whose `has_rows` answers False for a missing\ntable and for an empty one alike \u2014 and not an introspection of `sqlite_master`.\n\nThree capabilities are not table questions and reach the probe through `extra`:\n\n  * **keyframes** \u2014 a sibling `.keyframes.db` with rows for this run;\n  * **bin_log** \u2014 `_log_start()`, i.e. `MIN(batch_id) FROM bin_placement`.  Semantically\n    this is the row probe (`batch_id` is in the primary key, so it is never NULL and a\n    non-None MIN means at least one row) \u2014 but it is also MEMOISED and shared with\n    `_state_from_log`, which needs the value itself and not merely its existence.  Letting\n    `probe` re-derive it would issue a second query, and would leave `_memo['log_start']`\n    cold for the request that needs it.  Behaviour first: the existing call stays, and its\n    answer is handed in;\n  * **viz_cache** \u2014 sidecar FRESHNESS, which is not a property of the sim DB at all.\n\nOnly the table probes are memoised.  `viz_cache` is deliberately re-checked on every call:\na cache built while the viewer is running must appear without a restart.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> frozenset[str]"
    },
    "Visualization/readers/base.py::SqliteSimReader.events": {
      "decorators": [],
      "doc": "Timed picker events for one batch.  Times are BATCH-RELATIVE (each batch ~0-based).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisle: int | None=None) -> list[dict]"
    },
    "Visualization/readers/base.py::SqliteSimReader.final_home": {
      "decorators": [],
      "doc": "Per-SKU destination at the LAST keyframe \u2014 the colour authority.\n\n`{sku: {aisle_id, bayX, bayY, qty, n_homes, home_aisles: [...]}}`.\n\nA SKU almost never lands in exactly one bin: the warehouse is sized\n``bins >= n_skus * 1.1``, and measured on the production run **64.3% of SKUs hold\nmultiple bins at the last keyframe \u2014 and only 9.7% of those keep every replica in one\naisle** (median span 2 aisles, max 8).  So `home_aisles` is the set, and it is what\n\"is this item home?\" must be tested against; scoring against a single bin would paint\nthe majority of perfectly-placed replicas as misplaced.\n\n`aisle_id`/`bayX`/`bayY` are the PRIMARY home \u2014 largest qty, ties broken by lowest\n(aisle, bayX, bayY) so the choice is deterministic.  That one is what the hue and\nlightness are derived from; the set is what the chroma is derived from.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.keyframe_batches": {
      "decorators": [],
      "doc": "The batches with an exact spatial snapshot \u2014 the animation grid.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[int]"
    },
    "Visualization/readers/base.py::SqliteSimReader.run_meta": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.schema_id": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Visualization/readers/base.py::SqliteSimReader.schema_source": {
      "decorators": [],
      "doc": "'stamped' | 'pinned' | 'derived' \u2014 how the id was obtained.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Visualization/readers/base.py::SqliteSimReader.sku_scores": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, skus: list[int] | None=None) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.sku_series": {
      "decorators": [],
      "doc": "Per-batch pick counts for the given SKUs.  Indexed by `(run_id, sku)`, so cheap live.\n\nThe sidecar's `sku_series` holds only the top-N window, so a mixed request is a PARTIAL\nhit: taking it as complete would report every SKU outside the window as never picked \u2014\nthe exact failure `sku_rank` exists to prevent.  So the cache is used for what it has and\nthe remainder falls through to the live query, which is index-served and cheap.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, skus: list[int]) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.state_at": {
      "decorators": [],
      "doc": "Occupied bins at (batch, t).\n\nTries the three records in cost order \u2014 cached span index, live log fold, keyframe \u2014\nand returns the first that can answer.  The first two are exact at EVERY batch; the\nthird is exact only AT a keyframe and says so.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisles: list[int] | None=None, t: float | None=None) -> dict"
    },
    "Visualization/readers/base.py::SqliteSimReader.tasks": {
      "decorators": [],
      "doc": "Per-task rows \u2014 one picker's single-aisle ordered pick sequence.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int | None=None, aisle: int | None=None) -> list[dict]"
    },
    "Visualization/readers/base.py::SqliteSimReader.top_skus": {
      "decorators": [],
      "doc": "The n most-picked SKUs.  The live fallback is a ~24 s full scan of `picks`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, n: int=50) -> list[dict]"
    },
    "Visualization/readers/base.py::_FAMILY_KEYS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('handling_type', 'unit_type', 'storage_size')"
    },
    "Visualization/readers/base.py::_FAMILY_SOURCES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'sim_db': 'Optimization.persistence.Picking_Data', 'keyframes_db': 'Optimization.persistence.Picking_Data', 'viz_cache_db': 'Visualization.cache_schema', 'warehouse_db': 'Optimization.persistence...."
    },
    "Visualization/readers/base.py::_PROBED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple((SIM_CAPABILITIES[name] for name in (CAP_AISLE_METRICS, CAP_BIN_SCORES, CAP_SKU_SCORES)))"
    },
    "Visualization/readers/base.py::_better_home": {
      "decorators": [],
      "doc": "Largest qty wins; ties broken by lowest (aisle, bayX, bayY) so the choice is stable.",
      "is_async": false,
      "kind": "function",
      "signature": "(cand: dict, cur: dict) -> bool"
    },
    "Visualization/readers/base.py::_composed_sql": {
      "decorators": [],
      "doc": "The per-vintage SQL for a named query \u2014 `dataset.sql_for`, memoised.\n\nTHE broker seam: a publisher-side `dataset.override(...)` registered for an old vintage\nchanges what this returns, and no reader method moves.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, name: str, schema_id: str) -> str"
    },
    "Visualization/readers/base.py::_deplete": {
      "decorators": [],
      "doc": "Subtract `n` picked units from a bin, dropping it when it empties.",
      "is_async": false,
      "kind": "function",
      "signature": "(bins: dict, aisle, bayX, bayY, n) -> None"
    },
    "Visualization/readers/base.py::_family_surface": {
      "decorators": [],
      "doc": "{table: columns} of the committed (or declared) shape for `schema_id` \u2014 the no-PRAGMA\nanswer to has-table questions (closes the bin_eviction probe gap).",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, schema_id: str) -> dict"
    },
    "Visualization/readers/base.py::_group_by_sku": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rows: list[dict]) -> dict"
    },
    "Visualization/readers/base.py::_int_list": {
      "decorators": [],
      "doc": "`1,2,3` for an IN clause, every element forced through int().\n\nThe only place a request parameter reaches SQL text; sqlite3 cannot parameterize IN, so the\ncast is the guard.  Callers must not pass anything else.",
      "is_async": false,
      "kind": "function",
      "signature": "(values) -> str"
    },
    "Visualization/readers/base.py::_ro": {
      "decorators": [],
      "doc": "Open `path` strictly read-only \u2014 `Schema.connect.read_only`, the sanctioned opener.\n\nNever `Picking_Data._open_db` (its `PRAGMA journal_mode=WAL` is a WRITE), and never\nimmutable: the viewer may be watching a run that is still being written.  This used to be\na hand-rolled URI connect; the broker-boundary ratchet now forbids `sqlite3.connect`\noutside `Schema/connect.py`.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> sqlite3.Connection"
    },
    "Visualization/readers/protocol.py": {
      "decorators": [],
      "doc": "protocol.py \u2014 the versioned data interface the views are written against.\n\n`SimReader` is the ONLY surface the HTTP layer touches. When a sim DB's schema changes, a new\nmodule in this package implements the same protocol and no view, route or front-end module\nchanges. That is the whole point of the split:\n\n    view (JS)  ->  /api/<verb>  ->  route  ->  ReaderBinding  ->  SimReader  ->  sqlite\n\nA view knows verb names and JSON. It does not know a table name, a column name, or which schema\nit is reading \u2014 so redesigning a view cannot break data access, and versioning the data access\ncannot break a view.\n\nTwo conventions every implementation must honour:\n\n* **Return plain JSON-serializable data.** Dicts, lists, str/int/float/bool/None. `server.py`\n  calls `jsonify` directly; there is no converter layer and there should not be one.\n* **Be honest about exactness.** Any payload describing bin state carries `exact` and, when\n  False, `restocks_pending` \u2014 the number of restock batches the frame is missing. See\n  `RECONSTRUCTION.md` \u00a71: a delta roll silently loses 59% of a real warehouse, and the cure is\n  for the reader to SAY so rather than to draw something plausible.",
      "kind": "module",
      "signature": null
    },
    "Visualization/readers/protocol.py::ALL_CAPABILITIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(CAP_KEYFRAMES, CAP_BIN_LOG, CAP_AISLE_METRICS, CAP_BIN_SCORES, CAP_SKU_SCORES, CAP_VIZ_CACHE)"
    },
    "Visualization/readers/protocol.py::SimReader": {
      "decorators": [
        "runtime_checkable"
      ],
      "doc": "Read one arm's persisted output. One implementation per vetted schema id.",
      "kind": "class",
      "signature": "(Protocol)"
    },
    "Visualization/readers/protocol.py::SimReader.aisle_geometry": {
      "decorators": [],
      "doc": "One row per aisle: id, family, bay_x, bay_y, capacity. NO per-bin list.\n\nThe bin grid is generated from bay_x x bay_y on demand \u2014 materializing it for all\naisles is ~396,500 dicts and a ~40 MB response on a production warehouse.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimReader.aisle_rollup": {
      "decorators": [],
      "doc": "Per-aisle occupancy/pick/visit aggregates for one batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimReader.aisle_state": {
      "decorators": [],
      "doc": "`state_at` for one aisle, plus that aisle's geometry and scores.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisle: int, t: float | None=None) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.batch_index": {
      "decorators": [],
      "doc": "Per batch: id, duration, items, whether it is a keyframe batch.\n\nBuilt from `batch_stats`, never `range(n_batches)` \u2014 a batch that produced no tasks\nwrites no row and the list legitimately has holes.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimReader.bin_history": {
      "decorators": [],
      "doc": "Every (sku, qty) this bin held, across the whole run.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisle: int, bayX: int, bayY: int) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimReader.bin_scores": {
      "decorators": [],
      "doc": "Static per-bin layout cost, optionally scoped to some aisles.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisles: list[int] | None=None) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.cache_status": {
      "decorators": [],
      "doc": "The viz sidecar's freshness: 'fresh' | 'stale' | 'partial' | 'absent' | 'skipped'.\nRe-checked per call, never resolved at construction \u2014 a cache built AFTER binding\nmust be picked up without a restart.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Visualization/readers/protocol.py::SimReader.capabilities": {
      "decorators": [],
      "doc": "Which optional data this arm actually HAS. Probed for rows, cached.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> frozenset[str]"
    },
    "Visualization/readers/protocol.py::SimReader.events": {
      "decorators": [],
      "doc": "Timed picker events for one batch. Times are BATCH-RELATIVE.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisle: int | None=None) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimReader.final_home": {
      "decorators": [],
      "doc": "Per-SKU destination at the last keyframe \u2014 the colour authority.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.keyframe_batches": {
      "decorators": [],
      "doc": "The batches at which an exact keyframe exists, ascending; [] without a sidecar.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[int]"
    },
    "Visualization/readers/protocol.py::SimReader.run_meta": {
      "decorators": [],
      "doc": "Run params + identity + the batch/keyframe grid the UI scrubs over.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.schema_id": {
      "decorators": [],
      "doc": "The resolved schema id of the DB behind this reader.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Visualization/readers/protocol.py::SimReader.schema_source": {
      "decorators": [],
      "doc": "HOW the id was resolved: 'stamped' | 'pinned' | 'derived' \u2014 published on\n/api/capabilities so a payload's provenance is inspectable.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Visualization/readers/protocol.py::SimReader.sku_scores": {
      "decorators": [],
      "doc": "Static per-SKU placement scores, optionally scoped.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, skus: list[int] | None=None) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.sku_series": {
      "decorators": [],
      "doc": "Per-batch pick counts and home bin for the given SKUs.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, skus: list[int]) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.state_at": {
      "decorators": [],
      "doc": "Occupied bins at (batch, t). Carries `exact` and `restocks_pending`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int, aisles: list[int] | None=None, t: float | None=None) -> dict"
    },
    "Visualization/readers/protocol.py::SimReader.tasks": {
      "decorators": [],
      "doc": "Per-task timing/effort rows \u2014 one picker's single-aisle pick sequence.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch: int | None=None, aisle: int | None=None) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimReader.top_skus": {
      "decorators": [],
      "doc": "The n most-picked SKUs, ranked.  n=50 is THE default \u2014 protocol, implementation\nand the /api/top_skus route agree (they used to say nothing/100/50 respectively).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, n: int=50) -> list[dict]"
    },
    "Visualization/readers/protocol.py::SimSchemaDrift": {
      "decorators": [],
      "doc": "The stamped or pinned id disagrees with the file's actual shape.",
      "kind": "class",
      "signature": "(SimSchemaError)"
    },
    "Visualization/readers/protocol.py::SimSchemaError": {
      "decorators": [],
      "doc": "Base for every schema-identity failure in the viewer.\n\nA SUBCLASS of `Schema.identity.SchemaError` (the `DatasetError` precedent), so one\n`except SchemaError` covers the viewer's own errors AND everything the shared pipeline\nraises (`SchemaDrift`, `UnsupportedSchema`, `UnsupportedQuery`, \u2026) \u2014 the server's 501\nboundary catches the whole hierarchy and nothing schema-shaped can become a 500.",
      "kind": "class",
      "signature": "(_identity.SchemaError)"
    },
    "Visualization/readers/protocol.py::UnsupportedSimSchema": {
      "decorators": [],
      "doc": "This DB's shape has no vetted reader.\n\nCarries a structural diff rather than a bare hash, because the realistic cause is a hybrid:\n`init_run_db` is all CREATE TABLE IF NOT EXISTS, so a DB written by old code and reopened by\nnew code gains missing tables but never missing columns, matching neither declaration.",
      "kind": "class",
      "signature": "(SimSchemaError)"
    },
    "Visualization/readers/protocol.py::UnsupportedSimSchema.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, observed: str, known: tuple[str, ...], detail: str='')"
    },
    "Visualization/readers/sim_2026_07.py": {
      "decorators": [],
      "doc": "sim_2026_07.py \u2014 the vetted reader for the current sim-DB schema.\n\nCovers the shape carrying `picks`, `picker_events`, `bin_placement`, `bin_eviction`,\n`bin_scores`, `sku_scores`, `aisle_metrics`, `reorder_queue`, `batch_stats`, `task_stats` and\n`simulation_runs`, alongside the one-table `bin_keyframe` sidecar file \u2014 and every earlier shape\nstill sitting in the archive, including the ones carrying `bin_inventory`.\n\nFour ids, one reader\n--------------------\n`SCHEMA_IDS` is `SIM_DB_FAMILY.supported_ids()` rather than a literal tuple, so the reader\nregistry and the family registry cannot disagree about which files are readable \u2014 a divergence\nwould surface as `UnsupportedSimSchema` on a file the writer considers perfectly current.\n\nThe four shapes differ only in tables and columns THIS READER TREATS AS OPTIONAL: the\n`simulation_runs.sim_schema_id` stamp column (nothing here reads it), the `bin_placement` /\n`bin_eviction` log (the reader probes for it and falls back to keyframes), and `bin_inventory`\n(the reverse \u2014 archive-only, probed for on the last-resort path). One implementation is correct\nfor all four; splitting them would duplicate ~400 lines to express \"columns the reader ignores\".\n\nThe PRE-stamp id is frozen as a literal on purpose.  Every run in the ~500 GB archive has that\nshape, those files will never be rewritten, and the id cannot be recomputed from today's source\nonce the column exists.  It is not a magic number: it was derived with\n`Picking_Data.observed_sim_schema_id` against the production run and matched the declared id of\nthe source at that commit.\n\nShapes older still are UNSUPPORTED, deliberately.  A viewer that guesses at columns draws a\nplausible-looking warehouse that is quietly wrong; `UnsupportedSimSchema` names what differs.",
      "kind": "module",
      "signature": null
    },
    "Visualization/readers/sim_2026_07.py::Sim2026_07Reader": {
      "decorators": [],
      "doc": "The current vetted reader.  All behaviour is inherited; this fixes the identity.",
      "kind": "class",
      "signature": "(SqliteSimReader)"
    },
    "Visualization/server.py": {
      "decorators": [],
      "doc": "server.py \u2014 the HTTP surface over the versioned readers.\n\nRouting and argument parsing only. Every route resolves a run id to a `RunRef`, asks its reader,\nand jsonifies the plain dict that comes back \u2014 there is no data logic here and there should not\nbe. That is what lets a sim-DB schema change land in `readers/` alone.\n\n    python Visualization/server.py \"<run_root>\"      # the dir holding run_layout.json\n    python Visualization/server.py                   # falls back to $COMPARISON_OUTPUT_DIR\n\nTwo properties worth knowing before adding a route:\n\n**Scope every bin-level payload.** On a production warehouse the unscoped forms are 396,500 rows\n\u2014 `/api/geometry` returned ~40 MB and `/api/scores` ~20 MB per pane before they took `?aisles=`.\nAisle lists are `int()`-cast on the way in; that cast is the only guard between a query parameter\nand SQL text, since sqlite3 cannot parameterize `IN`.\n\n**Nothing is fetched per time-step.** `t` is resolved client-side by replaying the batch's event\nlist, which is already loaded. Routes are run-scoped or batch-scoped, never t-scoped.",
      "kind": "module",
      "signature": null
    },
    "Visualization/server.py::_GZIP_MIN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "64 * 1024"
    },
    "Visualization/server.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Visualization/server.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Visualization/server.py::_aisles_arg": {
      "decorators": [],
      "doc": "`?aisles=1,2,3` -> [1,2,3].  Every element is int()-cast; see the module docstring.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> list[int] | None"
    },
    "Visualization/server.py::_float_arg": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name: str) -> float | None"
    },
    "Visualization/server.py::_gzip": {
      "decorators": [
        "app.after_request"
      ],
      "doc": "Compress large JSON in flight.  Stdlib only \u2014 no middleware dependency.\n\nThese payloads are highly repetitive JSON and compress ~10x: the colour authority\n(`/api/final_home`, ~70k SKUs) drops from ~6 MB to a few hundred KB, and a drill-in aisle\nfrom ~230 KB to ~25 KB.  Run-scoped payloads are fetched once per run, but the viewer opens\ntwo of them side by side and the difference is a visible pause.",
      "is_async": false,
      "kind": "function",
      "signature": "(response)"
    },
    "Visualization/server.py::_int_arg": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, default: int | None=None) -> int | None"
    },
    "Visualization/server.py::_json_error": {
      "decorators": [
        "app.errorhandler(400)",
        "app.errorhandler(404)",
        "app.errorhandler(501)"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(exc)"
    },
    "Visualization/server.py::_reader": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rid: str)"
    },
    "Visualization/server.py::_resolve_base": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> tuple[str, int]"
    },
    "Visualization/server.py::_run_or_404": {
      "decorators": [],
      "doc": "Resolve a run id.  `discover_runs` is cached on a tree stamp, so this is a dict build.",
      "is_async": false,
      "kind": "function",
      "signature": "(rid: str)"
    },
    "Visualization/server.py::_schema_error": {
      "decorators": [
        "app.errorhandler(SchemaError)"
      ],
      "doc": "Any schema-shaped failure escaping a ROUTE BODY is still a 501, never a Flask 500.\n\n`_reader` catches the hierarchy at binding; this is the second net, for a vetted-but-\nunservable read inside a route (the CI servability matrix makes that unreachable in\npractice \u2014 this handler exists so 'unreachable' never has to be load-bearing).",
      "is_async": false,
      "kind": "function",
      "signature": "(exc)"
    },
    "Visualization/server.py::_skus_arg": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> list[int]"
    },
    "Visualization/server.py::_startup_banner": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Visualization/server.py::api_aisle": {
      "decorators": [
        "app.route('/api/aisle')"
      ],
      "doc": "Drill-in: one aisle's bins, geometry and timed events.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_aisle_rollup": {
      "decorators": [
        "app.route('/api/aisle_rollup')"
      ],
      "doc": "Per-aisle aggregates for one batch \u2014 occupancy, picks, visits, home-match.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_bin_history": {
      "decorators": [
        "app.route('/api/bin_history')"
      ],
      "doc": "Everything one bin held across the run.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_capabilities": {
      "decorators": [
        "app.route('/api/capabilities')"
      ],
      "doc": "What this arm actually HAS \u2014 probed for rows, not just columns.\n\n`aisle_metrics` exists in every sim DB but is empty for most arms.  The UI hides that\npanel rather than rendering 0.0 as though it were a measurement.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_final_home": {
      "decorators": [
        "app.route('/api/final_home')"
      ],
      "doc": "The colour authority: each SKU's destination at the last keyframe.\n\n`home_aisles` is a SET \u2014 most SKUs hold replicas in several aisles, so \"is this item home?\"\nis tested against the set while hue and lightness come from the primary home.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_geometry": {
      "decorators": [
        "app.route('/api/geometry')"
      ],
      "doc": "One row per aisle, with its palette coordinates.  Never a per-bin list.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_meta": {
      "decorators": [
        "app.route('/api/meta')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_runs": {
      "decorators": [
        "app.route('/api/runs')"
      ],
      "doc": "The navigation contract: schema id, the axis values present, and every arm.\n\nThe UI builds its cascading selectors from `axes`/`axis_order`, so adopting a new run-tree\nschema changes what you can navigate by without a single JS edit.  An axis with no values\n(e.g. `channel` on a store-only run) is HIDDEN rather than given an invented value.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_schema": {
      "decorators": [
        "app.route('/api/schema')"
      ],
      "doc": "The run-tree contract THIS run was written with \u2014 not whatever head happens to be.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_scores": {
      "decorators": [
        "app.route('/api/scores')"
      ],
      "doc": "Static per-bin layout cost.  Scope with `?aisles=` \u2014 unscoped this is 396,500 rows.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_sku_scores": {
      "decorators": [
        "app.route('/api/sku_scores')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_sku_series": {
      "decorators": [
        "app.route('/api/sku_series')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_state": {
      "decorators": [
        "app.route('/api/state')"
      ],
      "doc": "Occupied bins at (batch, t).  Carries `exact` and `restocks_pending`.\n\nExact at EVERY batch for a run carrying the bin-mutation log \u2014 `state_at` folds\n`bin_placement` + `bin_eviction` + `picks`, which is the complete record of bin state.\n\nFor an ARCHIVED arm (no log) it is exact only at a keyframe batch: that vintage's\n`bin_inventory` never records restocks, so an in-between frame is depletion-exact and\nmissing that stretch's restocks.  The payload says which it got; the UI must show it rather\nthan drawing a plausible warehouse.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_tasks": {
      "decorators": [
        "app.route('/api/tasks')"
      ],
      "doc": "Per-task rows, optionally scoped to one batch and/or aisle.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::api_top_skus": {
      "decorators": [
        "app.route('/api/top_skus')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::app": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Flask(__name__, static_folder='static')"
    },
    "Visualization/server.py::index": {
      "decorators": [
        "app.route('/')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "Visualization/server.py::static_files": {
      "decorators": [
        "app.route('/<path:filename>')"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(filename)"
    },
    "Warehouse/__init__.py": {
      "decorators": [],
      "doc": "Warehouse \u2014 the domain engine as a package.\n\nPhysical model (aisles/bins/carts), inventory + demand, placement/assignment\nfunctions, and the pick simulations.  Import style is package-absolute\n(``from Warehouse.catalog.Order import Order``); the repo root must be on sys.path \u2014\nentry scripts self-bootstrap, pytest gets it from Tests/conftest.py.\n\nDeliberately side-effect-free: no imports here, so ``import Warehouse`` never\ndrags the heavy modules in and spawn workers pay no extra import cost.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/catalog/Affinity_Store.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore": {
      "decorators": [],
      "doc": "SQLite-backed affinity matrix with in-memory CSR acceleration.\n\nAt construction the full affinity table is loaded once into a scipy CSR\nsparse matrix so that delta_lift and sum_lift execute as pure numpy/scipy\noperations with no SQL round-trips.  The SQLite connection stays open only\nfor writes (load_for_skus, index_inventory).\n\nTypical workflow\n----------------\nstore = AffinityStore('affinity.db')          # loads matrix once (~1-2 s)\ndelta = store.delta_lift(sku, aisle_members)  # CSR row slice, O(partners)\ntotal = store.sum_lift(task_skus)             # scipy submatrix sum, O(k\u00b2)",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.__enter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> AffinityStore"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.__exit__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *_: object) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, db_path: str=':memory:', seed: int | None=None) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._db_stamp": {
      "decorators": [],
      "doc": "(main size, header change counter, mtime_ns, -wal size) \u2014 identity of the DB.\n\nThe WAL size is load-bearing: this store opens in WAL mode, so a committed write\nlives in `affinity.db-wal` and the MAIN file's header does not move until a\ncheckpoint \u2014 size+counter alone would serve a stale sidecar over fresh commits.\nA reader-created `-wal` is zero bytes and a clean writer close removes it, so the\nproduction read-only file stamps identically across opens.  mtime_ns catches a\nREGENERATED file: a deterministic generator can reproduce both the size and the\ncommit count, and a copied/restored file changing mtime only costs one spurious\nSQL reload \u2014 the safe direction.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[int, int, int, int]"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._init_schema": {
      "decorators": [],
      "doc": "Create the tables ONLY when something is actually missing.\n\nThis used to run unconditionally, which meant opening a complete, generator-written\n`affinity.db` \u2014 even purely to read it \u2014 silently ADDED `sku_group` and changed the\nfile's shape.  A database whose structure depends on whether it has been opened yet\ncannot be fingerprinted, and the mutation also drifted it away from what\n`context/artifacts.yml` declares.  The generator now declares `sku_group` too, so the\ncommon path finds everything present and touches nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._load_matrix": {
      "decorators": [],
      "doc": "Read the full affinity table into a CSR sparse matrix.\n\nUses searchsorted for vectorised SKU\u2192index mapping instead of a Python\nloop, so even a 10M-row table loads in a few seconds.  The matrix uses\nfloat32 to halve memory vs the float64 stored in SQLite (~60 MB for\n7.5M pairs).\n\nNote: load_for_skus writes new rows to SQLite but does NOT update this\nmatrix.  That path is only used for online/legacy generation and is not\ncalled by the comparison scripts.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._sidecar_path": {
      "decorators": [],
      "doc": "<db filename>.arrays.npz beside the DB \u2014 the NAME carries the db filename so\ntwo same-shaped stores in one directory can never serve each other's arrays\n(two seed-variant test DBs did exactly that under a fixed sidecar name).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str | None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._try_sidecar": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._verify_shape": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Refuse a generator-written affinity.db whose tables are not the ones we query.\n\nHARD FAIL: every caller is a simulation about to place ~400k bins from these lifts, and\n`delta_lift`/`sum_lift` read the CSR matrix built here \u2014 a `lift` column that is not the\n`lift` column produces placements that look entirely reasonable and are not.\n\nChecked with `identity.check_tables`, not `identity.check`, and that is a deliberate\nscope limit: the `affinity_db` FAMILY is registered by\n`Warehouse/generation/generate_affinity.py`, which is a data-gen CLI importing matplotlib\nand pandas.  Importing it here to reach one dataclass would drag both into every\nProcessPool worker.  Comparing the two tables this class actually reads against the DDL\nit already mirrors gives the same protection for the columns that matter, and makes the\n\"both DDLs must stay in step\" claim above self-enforcing.  That the mirror really does\nmatch the generator's declaration is asserted in\n`Tests/architecture/test_schema_identity.py`, where importing both is free.\n\nSkipped for `:memory:` and for a path this store is about to CREATE: there is no shape to\ndisagree with yet, and `_init_schema` authors it below.  Skipped equally when a table is\ngenuinely absent, which is the one case `_init_schema` still writes \u2014 verifying a shape\nwe are in the middle of completing would reject the legacy/in-memory path it exists for.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, db_path: str) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore._write_sidecar": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.close": {
      "decorators": [],
      "doc": "Close, folding the WAL back into affinity.db so no `-wal`/`-shm` is left beside it.\n\n`load_for_skus` and `index_inventory` both commit, so there is never an open\ntransaction here for `connect.close` to lose \u2014 it checkpoints, it does not commit.\n\nUnder the worker pool this file is SHARED: one affinity.db per inventory pair, opened\nby every arm running that pair at once.  The checkpoint then finds another connection\non the file and skips instantly (the busy handler is disabled for it), so this costs\nnothing in the common case and truncates for whichever worker happens to close last.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.delta_lift": {
      "decorators": [],
      "doc": "Association ABOVE independence between sku and aisle_members: \u03a3 (lift \u2212 1).\n\nLift is a multiplier with 1 = independence, so the co-location *value* of a\npartner is (lift \u2212 1); unstored pairs (lift = 1) contribute 0.  Walks the CSR\nrow for sku directly \u2014 bounded by sku's partner count, regardless of\naisle_members length.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, aisle_members: list[int]) -> float"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.delta_lift_idxs": {
      "decorators": [],
      "doc": "Association ABOVE independence: \u03a3 (lift \u2212 1) between sku and a pre-translated\nset of matrix indices.\n\nIndex-set form of delta_lift() (no per-call {_sku_to_idx[s] ...} comprehension).\nLift = 1 is independence \u21d2 each stored partner contributes (lift \u2212 1); unstored\npairs contribute 0.  The caller keeps member_idx_set current with the aisle's SKU\ncomposition (add on placement, discard on removal).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, member_idx_set: set[int]) -> float"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.delta_lift_sorted": {
      "decorators": [],
      "doc": "Sum of lift between sku and members described by a sorted numpy array.\n\nFaster than delta_lift_idxs for large member sets: uses np.searchsorted\non two pre-sorted arrays (CSR indices are sorted within each row by the\nscipy CSR format; sorted_member_arr is maintained by the caller).\nZero per-call allocation when sorted_member_arr is cached.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, sorted_member_arr: 'np.ndarray') -> float"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.index_inventory": {
      "decorators": [],
      "doc": "Store sku \u2192 lift_group for every order. Safe to call multiple times.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, inventory: Inventory) -> None"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.load_for_skus": {
      "decorators": [],
      "doc": "Return a local AffMatrix dict for all within-group pairs in *skus*.\n\nAlready-stored pairs are read from the DB.  Any missing within-group pair\nis generated (random.uniform), persisted, then included.  Cross-group pairs\nare absent; callers treat missing keys as 0.0.\n\nNote: new rows written here are NOT reflected in the in-memory CSR matrix.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, skus: Collection[int], min_lift: float=1.5, max_lift: float=5.0) -> AffMatrix"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.partners": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int) -> dict[int, float]"
    },
    "Warehouse/catalog/Affinity_Store.py::AffinityStore.sum_lift": {
      "decorators": [],
      "doc": "Total association ABOVE independence within skus: \u03a3 (lift \u2212 1) over all stored\nordered pairs.\n\nExtracts a (k \u00d7 k) submatrix and sums it in C, then subtracts the stored-entry\ncount (each stored pair contributes lift \u2212 1; independence pairs contribute 0).\nBoth (i,j) and (j,i) are stored, so each undirected pair is counted twice \u2014\nconsistent with the ordered-pair convention used throughout.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, skus: list[int]) -> float"
    },
    "Warehouse/catalog/Affinity_Store.py::_log": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "logging.getLogger(__name__)"
    },
    "Warehouse/catalog/Demand.py": {
      "decorators": [],
      "doc": "Demand.py \u2014 a SKU's demand rates, and the LAW one line's quantity is drawn from.\n\nTwo scalars and one object per SKU:\n\n  * `relative_frequency` \u2014 the SKU's pick weight as a [0, 1] RELATIVE share (batch\n    selection is weighted by it; it is not an absolute rate);\n  * `quantity_rate`      \u2014 the rate parameter of the line law (\u03bb for the Poisson family);\n    the ranking heuristics (velocity = freq \u00d7 rate) read this scalar as a weight;\n  * `line`               \u2014 the `LineDistribution`: the law one LINE's quantity is drawn\n    from, stamped on the SKU (.scratch/department-calibration, \"Choose the coverage\n    floor\", decision 10).  Every distribution-dependent number \u2014 the sampler's draw, the\n    mean units a line demands, the tail past a bin's stock, a fill rate \u2014 is READ off this\n    object, never re-derived by a consumer.  Before the stamp, `staffing.py` took a line as\n    `max(1, \u03bb)` and `coverage.py` as `\u03bb + e^-\u03bb`: two answers for one quantity, the drift\n    the stamp ends.\n\nThe one family today is `poisson_max1`, `max(1, Poisson(\u03bb))`: its sampler is Knuth's\n(`poisson_sample`, unchanged), so a stamped catalogue draws exactly what the unstamped one\ndid.  Two of the staffing record's five provenance values apply here (`Optimization/\nsimconfig/constants.py:PROVENANCE`, which this leaf may not import): `declared` \u2014 the law\nwas authored by the generator or read from the inventory file's stamp; `assumed` \u2014 the law\nwas RECONSTRUCTED as Poisson(`demand_qty_rate`) from a pre-stamp catalogue (or by a caller\nthat passed none), and the staffing record names that reconstruction.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/catalog/Demand.py::Demand": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Demand.py::Demand.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, min_frequency: float=0.0, max_frequency: float=1.0, min_quantity: float=0.5, max_quantity: float=20.0) -> None"
    },
    "Warehouse/catalog/Demand.py::Demand.from_rates": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Create a Demand with specific, pre-determined rates (used for reorders and every\nDB/generator construction).  `line` is the stamped law; absent, it is reconstructed\nas Poisson(`quantity_rate`) with provenance `assumed`.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, relative_frequency: float, quantity_rate: float, line: LineDistribution | None=None) -> 'Demand'"
    },
    "Warehouse/catalog/Demand.py::Demand.rate": {
      "decorators": [
        "property"
      ],
      "doc": "Alias for quantity_rate.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/catalog/Demand.py::Demand.sample": {
      "decorators": [],
      "doc": "One LINE's quantity, drawn from the stamped law \u2014 never below one.  (Before the\nstamp this returned the bare Poisson draw and the batch sampler floored it; the\nfloor is the law's now, and the draws are identical.)",
      "is_async": false,
      "kind": "function",
      "signature": "(self, rng: random.Random | None=None) -> int"
    },
    "Warehouse/catalog/Demand.py::LineDistribution": {
      "decorators": [],
      "doc": "The law one LINE's quantity `q` is drawn from \u2014 family + parameters, and every\ndistribution-dependent reading a consumer needs, so no consumer names a family:\n\n    mean()            E[q]\n    cdf(k)            P(q <= k)\n    quantile(p)       the smallest k with P(q <= k) >= p\n    survival(upto)    P(q > j) for j = 0..upto-1, as an ndarray (the tail a bin's stock\n                      is reached past; `expected_travel.accumulate` reads it per bin)\n    expected_min(S)   E[min(q, S)] = \u03a3_{j<S} P(q > j)  (the fill a stock level S buys)\n    sample(rng)       one draw, from `rng` (a `random.Random`) or the global module\n\nFamilies:\n    poisson_max1  {'lam': \u03bb}   q = max(1, X), X ~ Poisson(\u03bb).  mean = \u03bb + e^-\u03bb;\n                               P(q > 0) = 1 and P(q > j) = P(X > j) for j >= 1;\n                               sampled by Knuth's `poisson_sample`, floored at one.\n\n`to_row()` / `from_row()` are the inventory file's two columns (`line_family`,\n`line_params` JSON \u2014 the `creation_plan` table's precedent).  `provenance` is metadata\n(see the module docstring), not part of the law.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Demand.py::LineDistribution.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, family: str, params: dict, provenance: str='declared') -> None"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/catalog/Demand.py::LineDistribution._pmf_x": {
      "decorators": [],
      "doc": "P(X = i) for i = 0..upto-1 of the underlying Poisson, by the running product.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, upto: int)"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.cdf": {
      "decorators": [],
      "doc": "P(q <= k); zero below one, since a line always demands at least one unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, k: int) -> float"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.expected_min": {
      "decorators": [],
      "doc": "E[min(q, S)] \u2014 the units a stock level `S` is expected to deliver against one\nline (a fill rate's numerator).  \u03a3_{j<S} P(q > j) by the running product.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, S: int) -> float"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.from_row": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Rebuild from the inventory file's two columns.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, family: str, params_json: str | None, provenance: str='declared') -> 'LineDistribution'"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.mean": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.pmf": {
      "decorators": [],
      "doc": "P(q = i) for i = 0..upto-1: zero at i = 0, `P(X <= 1)` at i = 1 (the floor folds\nthe Poisson's zero into one unit), the Poisson mass above.  The lead-aware fill\n(`simconfig/coverage._served_under_lead`) convolves it; the founding family has a\nvectorised fast path there and this is the row-by-row form any family answers.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, upto: int) -> np.ndarray"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.poisson": {
      "decorators": [
        "classmethod"
      ],
      "doc": "`max(1, Poisson(lam))` \u2014 the founding family.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, lam: float, provenance: str='declared') -> 'LineDistribution'"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.quantile": {
      "decorators": [],
      "doc": "The smallest k with P(q <= k) >= p, for 0 <= p < 1 (p <= 0 is one).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, p: float) -> int"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.sample": {
      "decorators": [],
      "doc": "One line's quantity: Knuth's Poisson draw, floored at one.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, rng: random.Random | None=None) -> int"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.survival": {
      "decorators": [],
      "doc": "P(q > j) for j = 0..upto-1: `1.0` at j = 0, the Poisson upper tail after.\n\nThe regularised lower gamma `P(j+1, \u03bb)` IS `P(X > j)`; vectorised because the\nexpected-travel closed form reads it for every bin of every SKU.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, upto: int) -> np.ndarray"
    },
    "Warehouse/catalog/Demand.py::LineDistribution.to_row": {
      "decorators": [],
      "doc": "`(line_family, line_params)` \u2014 the JSON is NULL when the family takes none.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[str, str | None]"
    },
    "Warehouse/catalog/Demand.py::line_law_census": {
      "decorators": [],
      "doc": "What laws a catalogue carries and where they came from \u2014 the staffing record's\n`line_law` block: `{'n_skus', 'families': {family: n}, 'provenance': {p: n},\n'reconstructed_skus'}` (the last is the `assumed` count: SKUs whose law was rebuilt\nas Poisson(demand_qty_rate) from a pre-stamp file).",
      "is_async": false,
      "kind": "function",
      "signature": "(orders) -> dict"
    },
    "Warehouse/catalog/Demand.py::poisson_sample": {
      "decorators": [],
      "doc": "Knuth's algorithm: return a Poisson-distributed integer with mean `lam`.\n\nPass `rng` (a `random.Random`) to draw from a dedicated stream; default `None`\nuses the global `random` module (back-compatible).",
      "is_async": false,
      "kind": "function",
      "signature": "(lam: float, rng: random.Random | None=None) -> int"
    },
    "Warehouse/catalog/Inventory_Builder.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/catalog/Inventory_Builder.py::AffMatrix": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "dict[tuple[int, int], float]"
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order]) -> None"
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory.affinity_matrix": {
      "decorators": [],
      "doc": "Return per-pair lift values for within-(handling, category) SKU pairs.\n\nSKUs are grouped by storage_type \u2014 only items that share the same handling\nand category can be co-located, so cross-group lift is always zero.\nEach group is capped at max_per_group SKUs to keep the matrix sparse\nenough for batch sampling at warehouse scale.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, min_lift: float=1.5, max_lift: float=5.0, max_per_group: int=500) -> AffMatrix"
    },
    "Warehouse/catalog/Inventory_Builder.py::InventoryConfig": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory_Builder": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory_Builder.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory_Builder.build": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> Inventory"
    },
    "Warehouse/catalog/Inventory_Builder.py::Inventory_Builder.from_config": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, config: InventoryConfig) -> 'Inventory_Builder'"
    },
    "Warehouse/catalog/Order.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/catalog/Order.py::Order": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/catalog/Order.py::Order.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, storage_type: tuple[str, str], max_dim: int=_MAX_DIM) -> None"
    },
    "Warehouse/catalog/Order.py::Order._clamp_int": {
      "decorators": [
        "staticmethod"
      ],
      "doc": "Round to int and clamp to [lo, hi] \u2014 the single guardrail for physical fields.",
      "is_async": false,
      "kind": "function",
      "signature": "(x: float, lo: int, hi: int) -> int"
    },
    "Warehouse/catalog/Order.py::Order.build": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Construct a order from supplied physical + demand values (not random sampling),\napplying all physical guardrails.  The single construction path for generated and\nDB-loaded orders \u2014 accepts demand/quantity as params so the default random Demand()\nis never invoked.  `line` is the SKU's stamped line law (`Demand.line`); absent, it\nis reconstructed as Poisson(qty_rate) with provenance `assumed`.  The scalar is the\nlaw's rate parameter and every reader must see ONE value, so a Poisson law authored at\nthe unclamped rate follows the clamp (what every pre-stamp load sampled), and a law\nthat agrees with neither RAISES.\n\nTakes NO stock level: the catalogue carries demand and geometry only, and a level is\nthe run's declaration (`declare_stock`, ADR-0002).  Does NOT touch Order.next_sku\n(sku is explicit).",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, sku: int, handling: str, category: str, length: float, width: float, height: float, weight: float, relative_frequency: float, qty_rate: float, *, lead_time_mean: float=0.0, supply_cv: float=0.0, line: LineDistribution | None=None) -> 'Order'"
    },
    "Warehouse/catalog/Order.py::Order.compute_labor_cost": {
      "decorators": [],
      "doc": "Set (and return) labor_cost = per-unit pick regression cost for this order\nunder the given PickConfig coefficients.  Mirrors Pick._pick_time at qty=1,\nno cart swap, ground level.  Call once per worker after inventory load.\n\nAlso stores handle_var = the per-unit weight/volume term ALONE (without the\nintercept or the per-item charge).  labor_cost (= intercept + per_item + handle_var)\nis the qty=1 ground per-pick cost used to RANK items; the height multiplier scales\nthe whole at-location pick at placement time:\nper-pick at height = mult*(pick_intercept + qty*pick_per_item + qty*handle_var).\n\n`pick_per_item` is keyword-only and REQUIRED: a caller that forgot it would price\nthe pre-charge model and rank against a labor the sim no longer bills \u2014 silently,\nsince nothing compares the two until a lockstep test does.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, pick_intercept: float, pick_weight_coef: float, pick_volume_coef: float, pick_weight_fn: str='log', pick_volume_fn: str='log', *, pick_per_item: float) -> float"
    },
    "Warehouse/catalog/Order.py::Order.declare_stock": {
      "decorators": [],
      "doc": "Write the run's stock declaration onto this SKU -- THE one mutation site for the\nfour level slots (ADR-0002: a level is a run's fact, never the SKU's).\n\nThe order-up-to is at least one unit; the reorder point at least one and at most one\nbelow it, so the trigger always fires before the target (a `Q = 1` SKU takes\n`rp = 1`).  `stock_plan` is the packing the warehouse planner wrote for exactly this\nquantity (None = the default pallet/singleton rule); `pipeline_qty` the stamped lead\npipeline (None = not stamped, see `pipeline_allowance`).  Returns self.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, equilibrium_qty: int, reorder_point: int, *, stock_plan=None, pipeline_qty: int | None=None) -> 'Order'"
    },
    "Warehouse/catalog/Order.py::Order.expected_labor": {
      "decorators": [
        "property"
      ],
      "doc": "Expected picking labor per period = expected_popularity x labor_cost\n(= freq*qty*cost1).  Drives the Rank_labor enqueue order + aisle balance.\nZero until compute_labor_cost() has set labor_cost for this worker.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/catalog/Order.py::Order.expected_popularity": {
      "decorators": [
        "property"
      ],
      "doc": "Expected demand mass per period = frequency x quantity_rate (freq*qty).\n\nStatic (depends only on demand), so derived on access rather than stored.\nUsed as the per-aisle 'popularity' balance metric (Rank_popularity).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/catalog/Order.py::Order.pipeline_allowance": {
      "decorators": [],
      "doc": "The units expected in transit over the lead -- what the order-up-to is raised by\nso on-hand returns to the equilibrium when the lot lands (`inventory_reorder\n._fire_reorders`), and what the staffing derivation's expected lot reads.\n\nThe ONE definition.  Stamped (`pipeline_qty`, the era's `round(d_s x lead)` from\n`simconfig/coverage.py`), the stamp is the answer.  Unstamped -- every flag-off run,\nevery pre-stamp file -- the manager's heuristic stands, byte for byte:\n`round(rp x lead / (lead + 1))`, which assumed the reorder point encodes ~(lead + 1)\nbatches of demand.  Under the line floor `rp` encodes a LINE, which is why the era\nstamps the pipeline instead of inferring it (\"Choose the coverage floor\", decision 6).\nZero at lead 0 either way.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/catalog/Order.py::Order.popularity": {
      "decorators": [
        "property"
      ],
      "doc": "Pick weight for weighted random selection; backed by demand frequency.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/catalog/Order.py::Order.reorder": {
      "decorators": [],
      "doc": "Return a new shipment of this order: same SKU, dimensions, weight, type, and demand rates.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> 'Order'"
    },
    "Warehouse/catalog/Order.py::Order.sku": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/catalog/Order.py::Order.stock_declared": {
      "decorators": [],
      "doc": "Whether a run has declared this SKU's stock level (`declare_stock` ran).  False on\nevery freshly generated or catalogue-loaded order: the catalogue carries none.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/catalog/Order.py::Order.volume": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/catalog/Order.py::StorageHandleConfig": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "namedtuple('StorageHandleConfig', ['handling', 'category'])"
    },
    "Warehouse/catalog/Order.py::_sample_dim": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(max_dim: int=_MAX_DIM) -> int"
    },
    "Warehouse/catalog/Order.py::_sample_weight": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(length: int, width: int, height: int) -> int"
    },
    "Warehouse/catalog/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.catalog \u2014 The SKU catalogue and its demand model: Order (a SKU), Inventory_Builder, the affinity/lift matrix, and the per-SKU demand distribution.\n\nSee README.md for what does and does not belong here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.generation -- data-generation CLIs (build the inventory/affinity/profile DBs).\n\nSeparate from the run-a-sim modules in Warehouse/.  Imports are package-absolute\n(``from Warehouse.generation.generate_inventory import ...``); run as scripts they\nself-bootstrap the repo root onto sys.path.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/affinity_semantics.py": {
      "decorators": [],
      "doc": "affinity_semantics \u2014 the affinity family's column semantics.\n\nThree tiny tables; `lift` is a dimensionless co-demand ratio, comparable only within one\ncatalogue's generation \u2014 a SCORE, not a share and not a rate.\n\nTags are metadata: importing this module changes no DDL, no shape id, no result.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/affinity_semantics.py::_KEY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'id', 'row')"
    },
    "Warehouse/generation/generate_affinity.py": {
      "decorators": [],
      "doc": "Generate a demand-weighted sparse affinity matrix from an inventory DB.\n\nLift is a genuine co-purchase association **independent of demand**: within each\n(handling \u00d7 category) storage area, SKUs are partitioned into random latent\naffinity clusters (assigned independently of relative_frequency / demand_qty_rate),\nand same-cluster pairs get a positive association strength.  This makes lift a real\nsignal \u2014 `lift = 1` is independence, `lift > 1` is positive association \u2014 rather\nthan a restatement of popularity.  Only the top-K partners per SKU are stored,\nproducing a sparse matrix orders of magnitude smaller than all-pairs.\n\nLift formula\n------------\n1. Partition each storage area's SKUs into \u2308n / cluster_size\u2309 clusters, assigned\n   uniformly at random (independent of demand).\n2. For a pair (i, j) in the SAME cluster:\n\n       lift(i, j) = clip( min_lift + (max_lift - min_lift) * U(0,1], min_lift, max_lift )\n\n   (with `min_lift = 1` \u21d2 stored lift \u2208 (1, max_lift], i.e. positive association).\n3. Different-cluster pairs are independent (lift = 1) and are NOT stored.\n\nFor each SKU, the top-K cluster-mates by lift are stored.  Both (i\u2192j) and (j\u2192i)\nshare the same lift (first-writer-wins via INSERT OR IGNORE) so delta_lift and\nsum_lift remain symmetric.\n\nStorage\n-------\nExpected rows \u2248 N_skus \u00d7 top_k \u00d7 2 (some overlap reduced by INSERT OR IGNORE).\nFor 76 500 SKUs with top_k=10: ~1.5 M rows \u2248 43 MB.\nPrevious all-pairs approach: up to 488 M rows \u2248 13 GB.\n\nOutput layout\n-------------\n<out_dir>/<name>/\n    affinity.db              \u2014 SQLite: affinity + run_metadata\n    params.json              \u2014 full parameter record\n    stats.json               \u2014 descriptive statistics\n    plots/\n        lift_histogram.png   \u2014 distribution of stored lift values\n        lift_by_group.png    \u2014 mean/median lift per storage group\n        activity_vs_lift.png \u2014 demand throughput vs lift (confirms signal)\n        degree_distribution.png \u2014 how many SKUs reference each partner\n        cumulative_lift.png  \u2014 CDF of lift values\n\nUsage\n-----\npython generate_affinity.py --inventory-db path/to/inventory.db [options]\npython generate_affinity.py --inventory-db ... --estimate\n\nCallable API\n------------\nfrom Warehouse.generation.generate_affinity import generate_run\ngenerate_run(inventory_db='...', name='run1', top_k=10)",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/generate_affinity.py::AFFINITY_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='affinity_db', declared_shape=declared_affinity_shape, meta_table='run_metadata'))"
    },
    "Warehouse/generation/generate_affinity.py::_ALL_DDL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(_SCHEMA,)"
    },
    "Warehouse/generation/generate_affinity.py::_BYTES_PER_ROW": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "28"
    },
    "Warehouse/generation/generate_affinity.py::_CANDIDATE_K_DEFAULT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "60"
    },
    "Warehouse/generation/generate_affinity.py::_CLUSTER_SIZE_DEFAULT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "80"
    },
    "Warehouse/generation/generate_affinity.py::_DEFAULT_OUT_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_WH, 'generated', 'affinities')"
    },
    "Warehouse/generation/generate_affinity.py::_GB": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1073741824"
    },
    "Warehouse/generation/generate_affinity.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Warehouse/generation/generate_affinity.py::_MB": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1048576"
    },
    "Warehouse/generation/generate_affinity.py::_NOISE_STD_DEFAULT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "0.15"
    },
    "Warehouse/generation/generate_affinity.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_WH)"
    },
    "Warehouse/generation/generate_affinity.py::_SCHEMA": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "\"\\n CREATE TABLE IF NOT EXISTS affinity (\\n sku_i INTEGER NOT NULL,\\n sku_j INTEGER NOT NULL,\\n lift REAL NOT NULL,\\n PRIMARY KEY (sku_i, sku_j)\\n );\\n CREATE INDEX IF NOT EXISTS idx_affinity_sku_i..."
    },
    "Warehouse/generation/generate_affinity.py::_TOP_K_DEFAULT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "10"
    },
    "Warehouse/generation/generate_affinity.py::_WH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Warehouse/generation/generate_affinity.py::_init_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str) -> sqlite3.Connection"
    },
    "Warehouse/generation/generate_affinity.py::_rank_scores": {
      "decorators": [],
      "doc": "Return (sorted_skus, rank_score_map), sorted highest-to-lowest activity.\n\nrank_score = 1 / sqrt(rank + 1):\n    rank 0 (highest demand) \u2192 1.000\n    rank 1                  \u2192 0.707\n    rank 3                  \u2192 0.500\n    rank 9                  \u2192 0.316\n    rank 99                 \u2192 0.100\n    rank 399                \u2192 0.050\nThis gives a realistic Pareto-like spread so high-velocity SKUs are\nclearly dominant partners without saturating low-velocity pairs at zero.",
      "is_async": false,
      "kind": "function",
      "signature": "(skus: list[int], activity: dict[int, float]) -> tuple[list[int], dict[int, float]]"
    },
    "Warehouse/generation/generate_affinity.py::_save_close": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fig, path: str) -> None"
    },
    "Warehouse/generation/generate_affinity.py::compute_stats": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(conn: sqlite3.Connection, group_stats: dict, params: dict) -> dict"
    },
    "Warehouse/generation/generate_affinity.py::compute_stats._s": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(v)"
    },
    "Warehouse/generation/generate_affinity.py::declared_affinity_shape": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Warehouse/generation/generate_affinity.py::estimate": {
      "decorators": [],
      "doc": "Return expected pair counts and DB sizes for a top-K run.",
      "is_async": false,
      "kind": "function",
      "signature": "(group_counts: dict[str, int], top_k: int) -> dict"
    },
    "Warehouse/generation/generate_affinity.py::generate_affinity": {
      "decorators": [],
      "doc": "Generate latent-cluster top-K affinity pairs and insert to the DB.\n\nWithin each group, SKUs are partitioned into random clusters (independent of\ndemand); each SKU's top-k same-cluster partners are stored with a random\nassociation strength in (min_lift, max_lift].  Both (i\u2192j) and (j\u2192i) are inserted\nwith the same lift; INSERT OR IGNORE skips pairs already stored.\n\nReturns per-group statistics for stats.json and plots.",
      "is_async": false,
      "kind": "function",
      "signature": "(group_skus: dict[str, list[int]], sku_demand: dict[int, tuple[float, float]], min_lift: float, max_lift: float, noise_std: float, top_k: int, candidate_k: int, rng: random.Random, conn: sqlite3.Connection, cluster_size: int=_CLUSTER_SIZE_DEFAULT, batch_size: int=500000) -> dict"
    },
    "Warehouse/generation/generate_affinity.py::generate_run": {
      "decorators": [],
      "doc": "Generate one demand-weighted top-K affinity run.\n\nParameters\n----------\ninventory_db : path to inventory.db produced by generate_inventory.py\nname         : folder name under out_dir\nout_dir      : parent directory; run is created at out_dir/name/\ntop_k        : partners stored per SKU (default 10)\ncandidate_k  : candidate pool per SKU before noise reranking (default 60)\nmin_lift     : lower bound of the lift scale (default 1.0)\nmax_lift     : upper bound of the lift scale (default 5.0)\nnoise_std    : Gaussian noise std added to each lift score (default 0.15)\nseed         : RNG seed for reproducibility\nbatch_size   : DB insert batch size\nverbose      : print progress lines\n\nReturns the path to the created run directory.",
      "is_async": false,
      "kind": "function",
      "signature": "(inventory_db: str, name: str, out_dir: str=_DEFAULT_OUT_DIR, top_k: int=_TOP_K_DEFAULT, candidate_k: int=_CANDIDATE_K_DEFAULT, min_lift: float=1.0, max_lift: float=5.0, noise_std: float=_NOISE_STD_DEFAULT, cluster_size: int=_CLUSTER_SIZE_DEFAULT, seed: int=0, batch_size: int=500000, verbose: bool=True) -> str"
    },
    "Warehouse/generation/generate_affinity.py::generate_run._log": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(msg)"
    },
    "Warehouse/generation/generate_affinity.py::iter_affinity_from_db": {
      "decorators": [],
      "doc": "Yield (sku_i, sku_j, lift) rows in chunks without loading all into RAM.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str, chunk_size: int=100000)"
    },
    "Warehouse/generation/generate_affinity.py::load_affinity_from_db": {
      "decorators": [],
      "doc": "Load entire affinity matrix into a plain dict[(sku_i, sku_j) \u2192 float].\nOnly suitable for matrices that fit comfortably in RAM.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str) -> dict"
    },
    "Warehouse/generation/generate_affinity.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Warehouse/generation/generate_affinity.py::plot_activity_vs_lift": {
      "decorators": [],
      "doc": "Scatter plot of geometric-mean demand throughput vs stored lift.",
      "is_async": false,
      "kind": "function",
      "signature": "(conn: sqlite3.Connection, group_skus: dict[str, list[int]], sku_demand: dict[int, tuple[float, float]], out_dir: str, sample_n: int=50000) -> None"
    },
    "Warehouse/generation/generate_affinity.py::plot_cumulative_lift": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(conn: sqlite3.Connection, out_dir: str) -> None"
    },
    "Warehouse/generation/generate_affinity.py::plot_degree_distribution": {
      "decorators": [],
      "doc": "Histogram of how many SKUs list each SKU as a partner (in-degree).\n\nHigh-demand SKUs appear as sku_j for many others \u2192 long right tail.",
      "is_async": false,
      "kind": "function",
      "signature": "(conn: sqlite3.Connection, out_dir: str) -> None"
    },
    "Warehouse/generation/generate_affinity.py::plot_lift_by_group": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(group_stats: dict, out_dir: str) -> None"
    },
    "Warehouse/generation/generate_affinity.py::plot_lift_histogram": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(conn: sqlite3.Connection, out_dir: str, min_lift: float, max_lift: float) -> None"
    },
    "Warehouse/generation/generate_affinity.py::print_estimate": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(est: dict, top_k: int) -> None"
    },
    "Warehouse/generation/generate_inventory.py": {
      "decorators": [],
      "doc": "Generate an Inventory with configurable dimension and weight distributions and\npersist it to SQLite.\n\nEach order's length, width, and height are sampled from a dim_spec; weight is\nsampled from a weight_spec.  Both specs support plain distributions and mixture\nmodels, making it easy to produce bimodal, trimodal, or heavy-tailed inventories.\n\nSupported dim distributions\n---------------------------\n  triangular  low, high, mode (default mode=high \u2192 left-skewed toward large)\n  uniform     low, high\n  normal      mean, std  (clipped to [3, 48])\n  beta        alpha, beta, low, high  (beta variate scaled to [low, high])\n  mixture     components: [{prob, spec}, ...]  (recursive, picks one component per draw)\n\nSupported weight distributions\n-------------------------------\n  volume_poisson         Poisson(lam = volume^(1/3))  \u2014 default, size-correlated\n  volume_scaled_poisson  Poisson(lam = scale * volume^(1/3))  \u2014 adjustable scale\n  poisson_fixed          Poisson(lam)  \u2014 fixed mean, size-independent\n  uniform                low, high\n  normal                 mean, std  (clipped to [1, \u221e))\n  mixture                components: [{prob, spec}, ...]\n\nOutput layout\n-------------\n<out_dir>/<name>/\n    inventory.db    \u2014 SQLite: orders + run_metadata\n    params.json     \u2014 full parameter record (dim_spec, weight_spec, seed, \u2026)\n    stats.json      \u2014 per-group counts, dimension / weight / demand summaries\n    plots/\n        group_sizes.png\n        dimensions.png\n        weight.png\n        demand.png\n        volume_vs_weight.png\n        singleton_split.png\n\nUsage (CLI)\n-----------\npython generate_inventory.py [--name NAME] [--num-skus 76500] [--seed 42]\n    [--dim-spec '{\"dist\":\"triangular\",\"low\":3,\"high\":48,\"mode\":48}']\n    [--weight-spec '{\"dist\":\"volume_poisson\"}']\npython generate_inventory.py --help\n\nCallable API (used by generate_profile_suite.py)\n-----------------------------------------\nfrom Warehouse.generation.generate_inventory import generate_run, load_inventory_from_db\nrun_dir  = generate_run(name='default', num_skus=76500, seed=42, out_dir='...')\ninventory = load_inventory_from_db(run_dir + '/inventory.db')",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/generate_inventory.py::DEFAULT_DIM_SPEC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'dist': 'triangular', 'low': 3, 'high': 48, 'mode': 48}"
    },
    "Warehouse/generation/generate_inventory.py::DEFAULT_FF_WEIGHT_SPEC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'dist': 'triangular', 'low': 1.0, 'mode': 2.5, 'high': 10.0}"
    },
    "Warehouse/generation/generate_inventory.py::DEFAULT_FREQ_SPEC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'dist': 'uniform', 'low': 0.0, 'high': 1.0}"
    },
    "Warehouse/generation/generate_inventory.py::DEFAULT_QTY_SPEC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'dist': 'uniform', 'low': 1.0, 'high': 20.0}"
    },
    "Warehouse/generation/generate_inventory.py::DEFAULT_WEIGHT_SPEC": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'dist': 'volume_poisson'}"
    },
    "Warehouse/generation/generate_inventory.py::Family": {
      "decorators": [
        "dataclass"
      ],
      "doc": "One category's SKU archetype.  Every *_spec accepts any dist incl. 'mixture'\n(multimodal).  length/width/height are sampled INDEPENDENTLY from their own specs;\nweight from weight_spec; demand from freq_spec (rate in (0,1]) and qty_spec\n(integer units/pick).  handling_split is the per-family (conveyable, non-conveyable)\npropensity; share is the family's relative SKU fraction (normalized across the plan).",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/generation/generate_inventory.py::INVENTORY_DB_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_identity.register(_identity.Family(name='inventory_db', declared_shape=declared_inventory_shape, meta_table='run_metadata', known_ids=(PRE_LINE_LAW_INVENTORY_SCHEMA_ID, PRE_PIPELINE_INVENTORY_SCHE..."
    },
    "Warehouse/generation/generate_inventory.py::PRE_LINE_LAW_INVENTORY_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'0e234fbfc739'"
    },
    "Warehouse/generation/generate_inventory.py::PRE_PIPELINE_INVENTORY_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'4ff06991df47'"
    },
    "Warehouse/generation/generate_inventory.py::PRE_STOCK_SPLIT_INVENTORY_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'025f4b1548a9'"
    },
    "Warehouse/generation/generate_inventory.py::UNVETTED_ARCHIVE_INVENTORY_SCHEMA_ID": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'c59cc6aa5fd3'"
    },
    "Warehouse/generation/generate_inventory.py::_ALL_DDL": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(_SCHEMA,)"
    },
    "Warehouse/generation/generate_inventory.py::_CARTON_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('sku', 'handling', 'category', 'length', 'width', 'height', 'weight', 'relative_frequency', 'demand_qty_rate', 'lead_time_mean', 'supply_cv', 'line_family', 'line_params')"
    },
    "Warehouse/generation/generate_inventory.py::_CARTON_MAX_DIM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "48"
    },
    "Warehouse/generation/generate_inventory.py::_CATEGORIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['food', 'clothing', 'electronic', 'furniture', 'seasonal', 'chemical']"
    },
    "Warehouse/generation/generate_inventory.py::_CP_PARAMS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[('length', 'length', (1, 48)), ('width', 'width', (1, 48)), ('height', 'height', (1, 48)), ('weight', 'weight', (1, 200)), ('relative_frequency', 'relative_frequency', (0, 1)), ('quantity', 'deman..."
    },
    "Warehouse/generation/generate_inventory.py::_DEFAULT_OUT_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_WH, 'generated', 'inventories')"
    },
    "Warehouse/generation/generate_inventory.py::_FF_SUBTYPE_COLORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'rect': '#70ad47', 'cube_4': '#4472c4', 'cube_6': '#7030a0', 'cube_8': '#c00000'}"
    },
    "Warehouse/generation/generate_inventory.py::_HANDLINGS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['conveyable', 'non-conveyable']"
    },
    "Warehouse/generation/generate_inventory.py::_HANDLINGS_ORDER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['conveyable', 'non-conveyable']"
    },
    "Warehouse/generation/generate_inventory.py::_HANDLING_COLORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'conveyable': _CONV_COLOR, 'non-conveyable': _NCONV_COLOR, 'fulfillment': _FF_COLOR}"
    },
    "Warehouse/generation/generate_inventory.py::_HANDLING_LABELS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['conveyable', 'non-conveyable']"
    },
    "Warehouse/generation/generate_inventory.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Warehouse/generation/generate_inventory.py::_LEVEL_COLS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('sku', 'equilibrium_qty', 'reorder_point', 'stock_plan', 'pipeline_qty')"
    },
    "Warehouse/generation/generate_inventory.py::_PLAN_PARAM_SPECS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[('length', 'length_spec'), ('width', 'width_spec'), ('height', 'height_spec'), ('weight', 'weight_spec'), ('relative_frequency', 'freq_spec'), ('quantity', 'qty_spec')]"
    },
    "Warehouse/generation/generate_inventory.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_WH)"
    },
    "Warehouse/generation/generate_inventory.py::_SCHEMA": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'\\n CREATE TABLE IF NOT EXISTS cartons ( -- persisted table name (data, not code): kept stable across the Order rename\\n sku INTEGER PRIMARY KEY,\\n handling TEXT NOT NULL,\\n category TEXT NOT NULL,..."
    },
    "Warehouse/generation/generate_inventory.py::_SINGLETON_MAX_DIM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "16"
    },
    "Warehouse/generation/generate_inventory.py::_WH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Warehouse/generation/generate_inventory.py::_cartons_override_sql": {
      "decorators": [],
      "doc": "The `cartons` query for a vintage holding the first `n_present` of `_CARTON_COLS`:\nthe columns it has by name, every later one as NULL, in the canonical order.",
      "is_async": false,
      "kind": "function",
      "signature": "(n_present: int) -> str"
    },
    "Warehouse/generation/generate_inventory.py::_ff_subtypes": {
      "decorators": [],
      "doc": "Subtypes present in the fulfillment frame, in canonical order (rect, cubes ascending).",
      "is_async": false,
      "kind": "function",
      "signature": "(ff_df: pd.DataFrame) -> list[str]"
    },
    "Warehouse/generation/generate_inventory.py::_init_db": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str) -> sqlite3.Connection"
    },
    "Warehouse/generation/generate_inventory.py::_levels_from_cartons_sql": {
      "decorators": [],
      "doc": "The `stock_levels` query for a pre-split vintage, which holds the declaration in\n`cartons`.  `pipeline=False` is a vintage older than the stamp: the column is served NULL\nand `Order.pipeline_allowance` falls back to the manager's heuristic, as those runs fired.",
      "is_async": false,
      "kind": "function",
      "signature": "(*, pipeline: bool) -> str"
    },
    "Warehouse/generation/generate_inventory.py::_poisson_rng": {
      "decorators": [],
      "doc": "Knuth's algorithm with normal approximation for large \u03bb.",
      "is_async": false,
      "kind": "function",
      "signature": "(lam: float, rng: random.Random) -> int"
    },
    "Warehouse/generation/generate_inventory.py::_save_close": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fig, path: str) -> None"
    },
    "Warehouse/generation/generate_inventory.py::_save_creation_plan": {
      "decorators": [],
      "doc": "Write one creation_plan row per (handling, category, parameter).  `plan` is the\nlist of family dicts (dataclasses.asdict(Family)); each *_spec is split into its\ndistribution name + a JSON params string (NULL when the distribution takes none).",
      "is_async": false,
      "kind": "function",
      "signature": "(conn: sqlite3.Connection, plan: list) -> None"
    },
    "Warehouse/generation/generate_inventory.py::build_inventory_from_plan": {
      "decorators": [],
      "doc": "Build one mixed inventory from a per-category creation plan.\n\nPer SKU: pick a family by share; handling by the family's propensity; sample each\ndimension independently + weight + demand + the supply side (lead, supply_cv).\n`lead` is the fixed `lead_time` unless `lead_time_range=(lo,hi)` is given, in which case each\nSKU draws lead ~ randint(lo, hi) from a SEPARATE rng \u2014 so two profiles built with the same seed\n(e.g. lead 0 vs random) have IDENTICAL physical inventory, differing only in lead.\nAll physical guardrails (integer, min 1, caps) are applied inside Order.build.\nNO stock level is authored here: a run derives every SKU's order-up-to and reorder point\nat setup from its declared coverage in days (ADR-0002).",
      "is_async": false,
      "kind": "function",
      "signature": "(num_skus: int, plan: list, seed: int, *, lead_time: float=0.0, lead_time_range: tuple | None=None, supply_cv_max: float=0.15, demand_override: tuple | None=None) -> Inventory"
    },
    "Warehouse/generation/generate_inventory.py::build_inventory_with_profile": {
      "decorators": [],
      "doc": "Build an Inventory from one dimension spec + one weight spec (the profile suite path).\n\nBypasses Order.__init__ so any distribution can be used.\nOrder.next_sku is reset to 1 at the start of this function.\n\nPer-SKU attributes (all stored in DB):\n  expected_batch_demand = freq \u00d7 qty_rate\n  lead_time_mean    = mean batches before a placed order arrives.\n                      0 = immediate.  Sampled per-SKU from N(mean, mean*cv)\n                      when lead_time_mean_batches > 0, else fixed at 0.\n  supply_cv         = coefficient of variation for received quantity.\n                      0.0 = perfect; 0.1 \u2192 \"ordered 50, got 45\".\n                      Per-SKU value drawn from HalfNormal(supply_cv_mean).\n\nNO stock level is authored (ADR-0002): the order-up-to and the reorder point are a run's\ndeclaration, derived at setup from its coverage in days; the reorder rule at runtime is\n`ideal = equilibrium_qty + pipeline - position; received = max(1, round(N(ideal, ideal*supply_cv)))`\nover the levels the run declared.",
      "is_async": false,
      "kind": "function",
      "signature": "(num_skus: int, handling_splits: list[float], category_splits: list[float], singleton_fraction: float, dim_spec: dict, weight_spec: dict, seed: int, lead_time_mean_batches: float=LEAD_TIME_MEAN_BATCHES, lead_time_cv: float=LEAD_TIME_CV, supply_cv_mean: float=SUPPLY_CV_MEAN) -> Inventory"
    },
    "Warehouse/generation/generate_inventory.py::compute_stats": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame) -> dict"
    },
    "Warehouse/generation/generate_inventory.py::compute_stats._summary": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(s: pd.Series) -> dict"
    },
    "Warehouse/generation/generate_inventory.py::declared_inventory_shape": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> dict"
    },
    "Warehouse/generation/generate_inventory.py::fulfillment_families": {
      "decorators": [],
      "doc": "A fulfillment sub-catalog summing to *total_share*: a RECTANGULAR family (independent L/W/H,\neach triangular over [dim_low, dim_high]) plus a small-CUBE family.  Both are tagged\nhandling_override='fulfillment' so the planner routes them to fulfillment bins, and both draw\nweight from *weight_spec* (default: right-skewed, mode ~2.5, max 10).\n\ncube_fraction is the cube share of the fulfillment SKUs (default 0.3 \u21d2 rectangles preferred).\ndim_high is capped at 16 so every SKU fits the 16\u00d716\u00d718 FulfillmentBin envelope (footprint 16,\ntallest tier 18).  Returns [rect_family, cube_family]; omit either when its share rounds to 0.",
      "is_async": false,
      "kind": "function",
      "signature": "(total_share: float, *, cube_fraction: float=0.3, weight_spec: dict | None=None, dim_low: int=3, dim_high: int=16, cube_sizes: tuple=(4, 6, 8), freq_spec: dict | None=None, qty_spec: dict | None=None) -> list['Family']"
    },
    "Warehouse/generation/generate_inventory.py::fulfillment_family": {
      "decorators": [],
      "doc": "A ready-made fulfillment family for a mixed catalog: small cubes (4/6/8), tagged with\nthe ('fulfillment','fulfillment') BinKey so the planner routes them to fulfillment bins.\ndimension specs are placeholders (cube_sizes overrides them); weight is volume-scaled so\nthe small cubes stay light.",
      "is_async": false,
      "kind": "function",
      "signature": "(share: float=0.3, cube_sizes: tuple=(4, 6, 8), freq_spec: dict | None=None, qty_spec: dict | None=None) -> 'Family'"
    },
    "Warehouse/generation/generate_inventory.py::generate_run": {
      "decorators": [],
      "doc": "Generate one inventory run and return the path to the created run_dir.\n\nParameters\n----------\nname               : folder name under out_dir\nnum_skus           : total SKU count\nhandling_splits    : [conv_weight, nconv_weight]  (normalised internally)\ncategory_splits    : 6 category weights (normalised internally)\nsingleton_fraction : fraction of SKUs capped at singleton dimensions (\u226416)\nseed               : reproducibility seed\nout_dir            : parent directory; run is created at out_dir/name/\ndim_spec           : dict describing the dimension distribution (see module docstring)\nweight_spec        : dict describing the weight distribution\nverbose            : print progress lines",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, num_skus: int=76500, handling_splits: list[float] | None=None, category_splits: list[float] | None=None, singleton_fraction: float=0.5, seed: int=42, out_dir: str=_DEFAULT_OUT_DIR, dim_spec: dict | None=None, weight_spec: dict | None=None, lead_time_mean_batches: float=LEAD_TIME_MEAN_BATCHES, lead_time_cv: float=LEAD_TIME_CV, supply_cv_mean: float=SUPPLY_CV_MEAN, creation_plan: list | None=None, lead_time: float=0.0, lead_time_range: tuple | None=None, supply_cv_max: float=0.15, demand_override: tuple | None=None, verbose: bool=True) -> str"
    },
    "Warehouse/generation/generate_inventory.py::generate_run._log": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(msg)"
    },
    "Warehouse/generation/generate_inventory.py::load_inventory_from_db": {
      "decorators": [],
      "doc": "Reconstruct an Inventory from a DB written by save_inventory_to_db.\n\nBound to the file's OWN vintage (`Schema.dataset.bind`) and read by NAME (the `cartons`\nquery above): the current shape serves the stamped line law; the pre-stamp vintage's\noverride serves NULLs and the law is reconstructed as Poisson(demand_qty_rate) with\nprovenance `assumed` -- built by `Order.build` from the CLAMPED rate, which is exactly\nwhat every pre-stamp load sampled.  Every order is rebuilt through Order.build, so the\nsame physical guardrails (integer dims/weight, min-1, caps) are enforced on load as on\ngeneration.\n\nThe STOCK DECLARATION rides a second named query (ADR-0002).  A file that declares one --\na run's own planned inventory, or any pre-split vintage, whose override reads the four\ncolumns out of `cartons` -- has `declare_stock` called for each SKU it names; a generated\ncatalogue declares nothing and its orders come back with the level slots UNSET, which\n`Order.stock_declared()` reports.  The two reads share `ORDER BY sku LIMIT :limit`, so a\nlimited load sees the same SKUs on both sides.\n\nHARD FAIL on an unvetted shape, and of the six wired checks this is the least negotiable:\nevery caller is a simulation about to consume ~500 GB of compute and WRITE the result.\n`bind` raises `UnsupportedSchema` naming the id and the whole structural diff before a\nsingle row is read, and `UnsupportedQuery` if a vetted vintage has no SQL to serve it --\nnever a bare `OperationalError: no such column` that says nothing about WHICH vintage.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str, limit: int | None=None) -> Inventory"
    },
    "Warehouse/generation/generate_inventory.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_creation_plan": {
      "decorators": [],
      "doc": "Render a graph for EVERY creation_plan row plus an aggregate-by-handling view.\n\ndf         : order dataframe (handling, category, length/width/height/weight,\n             relative_frequency, demand_qty_rate).\nplan_lookup: {(handling, storage_type, parameter): (distribution, params_str)} \u2014\n             the creation_plan rows, used to annotate each subplot.\n\nOutput (out_dir):\n  param_<parameter>.png   \u2014 grid (rows=category \u00d7 cols=handling): the realized\n                            histogram for each (handling, category, parameter) row,\n                            titled with that row's distribution family + params.\n  aggregate_by_handling.png \u2014 conveyable vs non-conveyable overlaid per parameter,\n                            + overall handling counts + per-category handling share.",
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, plan_lookup: dict, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_demand": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str) -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_dim_kde_overlay": {
      "decorators": [],
      "doc": "Overlay KDE curves for each dimension on a single plot.",
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_dimensions": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_fulfillment_distributions": {
      "decorators": [],
      "doc": "Fulfillment-only distribution plots, faceted by the `subtype` sub-family.\n\nThe store-centric (handling \u00d7 category) faceting collapses fulfillment into a single\ncell, so this renders its OWN views broken down by subtype (rect | cube_4 | cube_6 |\ncube_8).  ``plan_lookup`` keys are (handling, storage_type, parameter) where storage_type\nis the fulfillment FAMILY ('rect' | 'cube'); a subtype maps to its family for annotation.",
      "is_async": false,
      "kind": "function",
      "signature": "(ff_df: pd.DataFrame, plan_lookup: dict, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_fulfillment_distributions._family": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(subtype: str) -> str"
    },
    "Warehouse/generation/generate_inventory.py::plot_group_sizes": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_singleton_split": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str) -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_volume_vs_weight": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::plot_weight": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(df: pd.DataFrame, out_dir: str, title_suffix: str='') -> None"
    },
    "Warehouse/generation/generate_inventory.py::sample_dim": {
      "decorators": [],
      "doc": "Sample one order dimension from *spec*, clipped to [3, max_cap].\n\nmax_cap is passed down through mixture components so singleton orders\n(max_cap=16) are respected even inside nested mixture specs.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec: dict, rng: random.Random, max_cap: int=_CARTON_MAX_DIM) -> int"
    },
    "Warehouse/generation/generate_inventory.py::sample_rate": {
      "decorators": [],
      "doc": "Sample a demand rate (pick frequency or quantity-per-pick) from *spec*.\n\nReturns a float; the caller (Order.build) clamps frequency to (0,1] and\nqty_rate to the integer range [MIN_QTY, MAX_QTY].  Supported:\n  uniform   low, high\n  fixed     value\n  normal    mean, std\n  mixture   components: [{prob, spec}, ...]",
      "is_async": false,
      "kind": "function",
      "signature": "(spec: dict, rng: random.Random) -> float"
    },
    "Warehouse/generation/generate_inventory.py::sample_weight": {
      "decorators": [],
      "doc": "Sample order weight from *spec*.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec: dict, length: int, width: int, height: int, rng: random.Random) -> int"
    },
    "Warehouse/generation/generate_inventory.py::save_inventory_to_db": {
      "decorators": [],
      "doc": "Write an inventory to `db_path`: the SKUs' own facts into `cartons`, and \u2014 only for the\norders a run has DECLARED a level on \u2014 the declaration into `stock_levels`.\n\nA generated catalogue declares nothing, so its `stock_levels` is empty; a run's\n`planned_inventory.db` carries one row per fielded SKU (ADR-0002).  Both are one shape.",
      "is_async": false,
      "kind": "function",
      "signature": "(inventory: Inventory, db_path: str, params: dict) -> None"
    },
    "Warehouse/generation/generate_mixed_profile.py": {
      "decorators": [],
      "doc": "Generate ONE realistic mixed inventory (per-category SKU families with multimodal,\nper-dimension distributions) + its affinity matrix, written in the leaf layout that\nrun_simulation's discover_db_pairs consumes.\n\nFamilies = the 6 categories, each with its own multimodal length/width/height/weight\nspecs and a per-family conveyable/non-conveyable propensity (aggregate ~0.75/0.25).\nDimensions are sampled INDEPENDENTLY per axis; every stored number is a grounded\ninteger >= 1, capped (enforced in Order.build).\n\nWhat a leaf carries is the SKU's own facts: geometry, demand (relative frequency, the\nquantity rate, the stamped line law) and the supply side -- lead_time_mean and supply_cv.\nIt carries NO STOCK LEVEL (ADR-0002): an order-up-to quantity, a reorder point and a\npacking plan are a RUN's declaration, derived at setup from a declared coverage in DAYS\n(Optimization/simconfig/coverage.py, in every mode) and written to that run's own\nplanned_inventory.db.  The catalogue's `stock_levels` table is created empty and stays\nempty -- which is why this generator has no coverage knob to offer.\n\nlead_time is therefore the one supply-side dial a leaf varies: a deterministic per-dataset\nvalue (e.g. 0 vs 1 -> two sibling datasets) for how many batches pass before a placed order\narrives.  Same seed, same cartons; only the lead differs.\n\nOutput layout\n-------------\n<PROFILE_INPUT_DIR>/<run_name>/<profile_name>/\n    inventory/  inventory.db, params.json, stats.json, plots/\n    affinity/   affinity.db,  params.json, stats.json, plots/\n\nUsage\n-----\npython generate_mixed_profile.py --estimate\npython generate_mixed_profile.py --num-skus 3000 --lead-times 1 --name smoke\npython generate_mixed_profile.py --num-skus 76500 --lead-times 0   # immediate arrival\npython generate_mixed_profile.py --num-skus 76500 --lead-times 1   # 1-batch transit",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/generate_mixed_profile.py::BELL_CREATION_PLAN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[replace(fam, freq_spec=dict(BELL_STORE_FREQ[fam.category])) for fam in CREATION_PLAN]"
    },
    "Warehouse/generation/generate_mixed_profile.py::BELL_FF_FREQ": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_mix((1 / 3, _norm(0.15, 0.05)), (1 / 3, _norm(0.25, 0.05)), (1 / 3, _norm(0.55, 0.1)))"
    },
    "Warehouse/generation/generate_mixed_profile.py::BELL_STORE_FREQ": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'food': _norm(0.8, 0.1), 'chemical': _norm(0.5, 0.1), 'clothing': _norm(0.35, 0.05), 'electronic': _norm(0.35, 0.05), 'seasonal': _norm(0.15, 0.05), 'furniture': _norm(0.05, 0.015)}"
    },
    "Warehouse/generation/generate_mixed_profile.py::CREATION_PLAN": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[Family('food', share=0.25, handling_split=(0.91, 0.09), length_spec=_tri(4, 24, 12), width_spec=_tri(4, 24, 12), height_spec=_tri(4, 20, 10), weight_spec={'dist': 'volume_poisson'}, freq_spec={'di..."
    },
    "Warehouse/generation/generate_mixed_profile.py::_DEFAULT_PROFILES_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_clean_path(os.getenv('PROFILE_INPUT_DIR', os.path.join(_WH, 'generated', 'profiles')))"
    },
    "Warehouse/generation/generate_mixed_profile.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Warehouse/generation/generate_mixed_profile.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_WH)"
    },
    "Warehouse/generation/generate_mixed_profile.py::_WH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Warehouse/generation/generate_mixed_profile.py::_build_plan": {
      "decorators": [],
      "doc": "The 6 store families, plus a fulfillment sub-catalog when --fulfillment-fraction > 0.\nStore shares scale to (1-F) and the fulfillment families sum to F, so shares stay relative.\n--freq-profile bell swaps the per-category freq specs for bell (normal) ones and gives\nfulfillment SKUs an equal-weight 3-normal mixture; 'uniform' keeps the baseline plan.",
      "is_async": false,
      "kind": "function",
      "signature": "(args) -> list"
    },
    "Warehouse/generation/generate_mixed_profile.py::_expected_conveyable_fraction": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(plan) -> float"
    },
    "Warehouse/generation/generate_mixed_profile.py::_ff_weight_spec": {
      "decorators": [],
      "doc": "Fulfillment weight spec from --ff-weight-spec JSON, else triangular(min, mode, max).",
      "is_async": false,
      "kind": "function",
      "signature": "(args) -> dict"
    },
    "Warehouse/generation/generate_mixed_profile.py::_mix": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(*pairs)"
    },
    "Warehouse/generation/generate_mixed_profile.py::_norm": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(mean, std)"
    },
    "Warehouse/generation/generate_mixed_profile.py::_parse_lead_spec": {
      "decorators": [],
      "doc": "A --lead-times token \u2192 (profile_name, lead_time, lead_time_range).\n'random' \u2192 per-SKU lead ~ randint(rand_range); a number \u2192 fixed per-SKU lead.\nA non-uniform freq_profile (e.g. 'bell') is tagged into the name so its run leaf stays\ndistinct from the uniform baseline (mixed_realistic_bell_lt1 vs mixed_realistic_lt1);\n'uniform' keeps the historical names unchanged.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec, rand_range, freq_profile='uniform') -> tuple"
    },
    "Warehouse/generation/generate_mixed_profile.py::_tri": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(low, high, mode)"
    },
    "Warehouse/generation/generate_mixed_profile.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Warehouse/generation/generate_mixed_profile.py::main._side_entry": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(side_dir: str, db_name: str, extra: dict | None=None) -> dict"
    },
    "Warehouse/generation/generate_profile_suite.py": {
      "decorators": [],
      "doc": "Run generate_inventory + generate_affinity for a suite of order profiles.\n\nEach profile varies the dimension and / or weight distribution to produce\ninventories with meaningfully different physical characteristics \u2014 from\ntight unimodal clusters to bimodal, trimodal, and heavy-tailed mixtures.\nThe goal is to build a library of inventory \u00d7 affinity pairs that capture\ndiverse warehouse scenarios without regenerating them at simulation runtime.\n\nOutput layout\n-------------\n<PROFILE_INPUT_DIR>/<profile_name>/\n    profile_manifest.json   written at start with all profile specs\n    profile_summary.json    written at end with timing and stats\n    <carton_profile_name>/\n        inventory/          \u2192 inventory.db, params.json, stats.json, plots/\n        affinity/           \u2192 affinity.db,  params.json, stats.json, plots/\n    cross_profile/\n        dim_length_overlay.png      length KDE across all profiles\n        dim_volume_overlay.png      volume KDE across all profiles\n        weight_overlay.png          weight KDE across all profiles\n        profile_stats_table.png     summary table image\n\nUsage\n-----\n# dry run \u2014 print profile list and estimated affinity sizes, then exit\npython generate_profile_suite.py --estimate\n\n# full overnight run\npython generate_profile_suite.py\n\n# run only selected profiles\npython generate_profile_suite.py --profiles default bimodal_size heavy_tail_weight\n\nOptions\n-------\n--name NAME             profile run folder name (default: profile_<timestamp>)\n--num-skus N            SKU count for all inventories (default: 76500)\n--seed INT              base seed; profile i uses seed + i (default: 42)\n--max-per-group INT     affinity SKU cap per group (default: no cap)\n--skip-affinity         generate inventories only\n--profiles NAME [...]   subset of profiles to run (default: all)\n--out-dir PATH          root output directory (overrides PROFILE_INPUT_DIR env var)\n--affinity-seed INT     base seed for affinity generation (default: 0)\n--affinity-min-lift F   (default: 1.5)\n--affinity-max-lift F   (default: 5.0)\n--estimate              print profile specs and size estimates, then exit",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/generate_profile_suite.py::CARTON_PROFILES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "[{'name': 'default', 'description': 'Triangular dims (mode=max), Poisson weight ~ volume^(1/3). Reproduces the original Inventory_Builder behaviour.', 'dim_spec': {'dist': 'triangular', 'low': 3, '..."
    },
    "Warehouse/generation/generate_profile_suite.py::PROFILE_MAP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{p['name']: p for p in CARTON_PROFILES}"
    },
    "Warehouse/generation/generate_profile_suite.py::_DEFAULT_PROFILES_DIR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_clean_path(os.getenv('PROFILE_INPUT_DIR', os.path.join(_WH, 'generated', 'profiles')))"
    },
    "Warehouse/generation/generate_profile_suite.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "Warehouse/generation/generate_profile_suite.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_WH)"
    },
    "Warehouse/generation/generate_profile_suite.py::_WH": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "Warehouse/generation/generate_profile_suite.py::_save_close": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fig, path: str) -> None"
    },
    "Warehouse/generation/generate_profile_suite.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "Warehouse/generation/generate_profile_suite.py::plot_cross_profile_overlay": {
      "decorators": [],
      "doc": "One figure per dimension + weight showing KDE for every profile overlaid.",
      "is_async": false,
      "kind": "function",
      "signature": "(profile_dfs: dict[str, pd.DataFrame], out_dir: str) -> None"
    },
    "Warehouse/generation/generate_profile_suite.py::plot_profile_stats_table": {
      "decorators": [],
      "doc": "Render a summary table as an image for quick comparison.",
      "is_async": false,
      "kind": "function",
      "signature": "(profile_stats: dict[str, dict], out_dir: str) -> None"
    },
    "Warehouse/generation/inventory_semantics.py": {
      "decorators": [],
      "doc": "inventory_semantics \u2014 the profiles-tree (inventory) family's column semantics.\n\nTwo tables, two different kinds of statement.  `cartons` holds the SKU's own facts: physical\ndimensions in inches (the inch/second boundary is `cost_model.sec_per_inch`), demand rates\nper BATCH, the stamped line law (`line_family` + `line_params`, department-calibration \"Stamp\nthe line distribution on the SKU\"), and the batch-denominated lead the trailer feature\nreplaces flag-on.  It declares NO stock level at all (ADR-0002, department-calibration \"Field\nthe floor\", decision 5): an order-up-to quantity is a decision a run makes about a warehouse,\nnot a fact about a carton, and while it lived here it was authored as\n`coverage_batches x expected_batch_demand` \u2014 a generation batch is not a unit of time, so the\nauthored level was worth 1,771 store days on the reference catalogue.\n\n`stock_levels` is where that decision lands instead.  A run derives its levels at setup from\na declared coverage in DAYS (`Optimization/simconfig/coverage.py`) and records them in its\nown `planned_inventory.db`; a GENERATED catalogue leaves the table empty.  That emptiness is\nthe contract \u2014 the absence of a ROW, not a NULL column, is what \"no declaration\" means, so\n`Order.stock_declared()` can tell \"nobody has decided yet\" from \"decided, and the answer is\nmissing\".  `stock_plan` still earns the loudest note in the family, now from its new home: a\nplanned SKU bypasses the pallet/singleton packing rule, which is why splitting a delivery can\nproduce FEWER units \u2014 \"splitting is worse\" is not a safe assumption on any run that planned\nits packing.\n\nTags are metadata: importing this module changes no DDL, no shape id, no result.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/generation/inventory_semantics.py::_DIM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LEVEL, 'inches', 'sku', note='catalogue constant; seconds enter only through sec_per_inch')"
    },
    "Warehouse/generation/inventory_semantics.py::_ENUM": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'enum', 'row')"
    },
    "Warehouse/generation/inventory_semantics.py::_KEY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Col(LABEL, 'id', 'row')"
    },
    "Warehouse/inventory/Inventory_Management.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": "(PlanningMixin, OptimalLayoutMixin, ReorderMixin, ZoningMixin)"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, warehouse: Warehouse, assignment_fn: AssignmentFn=_uniform_assignment, affinity: AffinityStore | None=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._admit": {
      "decorators": [],
      "doc": "Put one unit on the put-away queue, stamped with its arrival age.\n\nThe single admission point.  Every producer -- intake, reorder arrivals, reloader\nevictions, and inbound when it exists -- goes through here, so a new producer cannot\nforget to stamp and quietly enter the queue as age -1.\n\nA queue at its staging limit REFUSES, and the item goes to `_held` rather than\nback to the caller.  Handling it here rather than at each producer is deliberate:\nthere are four producers and the failure mode of forgetting one is a unit that\nsilently disappears -- and NOT one the conservation ledger would catch, because that\nledger is a stock ledger over BINS and this unit never reached one. Nothing would\nraise and nothing would count it. Exactly the bug that takes a day to find.  Callers therefore never see a refusal and none of them changed.\n\nThe stamp is taken on ARRIVAL, not on admission.  A pallet that waited three batches\non the dock is three batches old when it finally gets floor space, and stamping it\nat admission would make it the youngest thing in the warehouse -- turning\nbackpressure into a priority inversion.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, source: str) -> 'PutawayItem'"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._admit_held": {
      "decorators": [],
      "doc": "Retry the held items, oldest first within each queue.  Returns how many got in.\n\nStops at the FIRST refusal for a given queue rather than scanning past it for\nsomething that happens to fit.  Letting a younger item slip into the gap an older one\ncould not use is exactly the inversion the age stamp exists to prevent, and it would\nalso make the backpressure unfair in a way no real dock is.\n\nCOST IS O(queues + admitted), and getting there took three attempts:\n\n  1. The original walked every held item on every call, doing a `route()` each time.\n     `_stock`'s refill loop calls this once per pass and needs roughly\n     `work / staging` passes, so passes and the held list grew together: quadratic.\n     Measured at 40 batches, staging=4 -- 27,248,644 `route()` calls at 2,400 SKUs.\n  2. An early exit on \"every queue has refused\" cut the routing 96x at that size, and\n     was reported as the fix.  It was not.  It still did `still.extend(rest)` into a\n     fresh deque, so every call copied the whole list -- and worse, THE EXIT WAS\n     UNREACHABLE.  It fired on `len(blocked) >= len(self.put_queues)`, every queue in\n     the set, while a store-only catalogue routes to two of the split's three and\n     `fulfillment` stays empty forever.  Touches kept growing at k=1.81 against 1.82\n     before the exit existed.\n  3. `_held` is now PARTITIONED by queue (`HeldItems`), so a full queue is skipped in\n     O(1) and there is no exit condition to get wrong.  Routing already happened when\n     the item was held, so this does none.\n\nA per-queue COUNT beside the single deque was tried between 2 and 3 and rejected:\nderived state that can drift, and a stale one makes this stop instantly and livelock,\nwhich is worse than the slowness it cures.  The partition needs nothing kept in sync.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._bind_put_crews": {
      "decorators": [],
      "doc": "Give every queue a crew: its own `spec.crew` if it has one, else the manager's\ndefault from `enable_putaway_timing`.\n\nA crew per STREAM, because that is what a crew is: a forklift crew's clock has\nnothing to do with a cart crew's, and two streams working in parallel must not\nserialise onto one clock -- which is exactly what a single manager-level\n`_put_clocks` did.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._candidates": {
      "decorators": [],
      "doc": "Viable bins for *unit* (smallest fitting tier), optionally restricted to the unit's\nvelocity band when zoning is on.  OFF \u21d2 returns the raw list object unchanged.  Used by the\nranked wave (once per wave); the per-unit path uses _band_pick directly for O(#bands).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit) -> list[Aisle.Bin]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._candidates_raw": {
      "decorators": [],
      "doc": "Return ``(tier_key, bins)``: the SMALLEST fitting tier's BinKey and its free-bin list\n(the live ``self._index`` object), or ``(None, [])`` when no fitting tier has a free bin.\nThe key lets the per-unit path index the per-band sub-index without re-deriving the tier.\n\nA pallet of size S fits in a bin of size S or larger.  We return the\nsmallest non-empty tier \u2265 S (the unit's own tier first), spilling UP to\nlarger tiers only when the exact tier is full.  This is both physically\nsensible (don't waste an extra_large bin on a small pallet) and keeps\nper-tier demand mapped to per-tier capacity, which is how the warehouse\nis sized \u2014 preventing small units from starving large-tier bins.\n\nReturning a single tier keeps the candidate list small (one index\nbucket) regardless of warehouse size.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit) -> tuple"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._charge_repack": {
      "decorators": [],
      "doc": "Price one rescue as RECEIVING work: an unload-priced act per RESULTING pack.\n\nA rescue breaks a unit no bin could hold into several that fit.  Somebody does that,\nand it is dock work: the receiving crew already owns \"take merchandise apart and\nmake it storable\", it is priced per pack by `unload_cost`, and there is no travel\nterm because the merchandise does not go anywhere to be repacked.  So the rescue\ncosts the dock's own per-pack price and NO new coefficient enters the model.\n\nCounted even when no dock is bound.  A dockless run can still repack, and the\nstaffing record expects ZERO of them (provenance `assumed`); a rescue nobody counted\nis exactly the finding the equilibrium audit exists to make loud.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order, new_units: list) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._cost_putaway": {
      "decorators": [],
      "doc": "Charge one placement to ITS QUEUE's crew and record it.\n\nCalled from `_execute_placement` and `_execute_topup` only -- the TWO put-away commit\npoints the bin-mutation allowlist names (ADR-0003 added the second) -- so this adds no\nnew bin writer. A top-up passes a unit built for the `n` items that actually moved,\nnot the whole unit they were cut from, so the price is of the trip that happened.\n\n`queue` is the stream the unit came off.  None means the caller does not know,\nwhich happens on a direct `_execute_placement` from a test or a diagnostic; the\nunit is then routed the same way admission routed it, so the charge still lands on\nthe crew that would really have done the work.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, bin_: Aisle.Bin, source, queue=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._execute_placement": {
      "decorators": [],
      "doc": "Commit one unit\u2192bin placement and update all manager state dicts.\n\n`score` / `score_rank` / `policy` are what the assignment policy chose on, when\nthere was a policy that computed anything.  Like `source`, the manager does not act\non them; they pass through to `BinRecorder`, because this is the one call every\nput-away funnels through and there is nowhere later to recover them from -- the\npools return the number as they decide, and nothing recomputes it.\n\n`source` is the `PutawayItem` origin the drain popped this unit from\n(`intake` / `reorder` / `reslot`), passed through untouched: the manager does not\nact on it, but this is the one call every put-away funnels through, so it is the\nonly place an observer can learn where a placement came from.  Optional and\nignored here, so a direct caller that does not know about the queue \u2014 a test, a\nfuture inbound writer \u2014 is unaffected.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, bin_: Aisle.Bin, *, source: str | None=None, score: float | None=None, score_rank: int | None=None, policy: str | None=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._execute_topup": {
      "decorators": [],
      "doc": "Commit ONE top-up of `n` items into `bin_`, which already holds `order`'s SKU.\n\nThe own-bin twin of `_execute_placement`, and a separate method for the same reason\nthat one exists: it is the single commit point an observer can wrap.  `BinRecorder`\nrebinds it to emit the `bin_placement` row carrying `bin_state='occupied'`, exactly\nas it rebinds `_execute_placement` for the `'empty'` rows -- so the spatial log stays\ncomplete without `_top_up_own_bins` knowing anything about recording.\n\nWhich bins and how much is `_top_up_own_bins`' decision; this method only commits.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order, bin_: Aisle.Bin, n: int, *, source: str | None=None, queue=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._index_add": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_: Aisle.Bin) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._index_remove": {
      "decorators": [],
      "doc": "O(1) removal via swap-remove: move last element into the vacated slot.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_: Aisle.Bin) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_: Aisle.Bin) -> BinKey"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._lead_queue": {
      "decorators": [
        "property"
      ],
      "doc": "The transit's entries, by their historical name.\n\nA PROPERTY so the many readers (and the tests that build a bare manager and assign\ndirectly) survive the transit split unchanged: the entries live on `self.transit`,\ntiming methods live there too, and this name is a window, not a second copy.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._queue": {
      "decorators": [],
      "doc": "Route one STAMPED item to its put queue, holding it if the queue is full.\n\nSplit out of `_admit` so the receiving crew has somewhere to hand an item it has\nalready taken off a trailer.  `_admit` stamps and decides WHERE work enters; this\ndecides which put queue takes it.  A dock unload calls this directly -- the item was\nstamped on arrival and must not be re-stamped.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, item: 'PutawayItem') -> 'PutawayItem'"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._refuse_site_scope": {
      "decorators": [],
      "doc": "Raise when a LEAF is asked a question only the SITE can answer.\n\nOne place, TEN callers, one message shape: `name` is what was asked and `instead`\nis what a caller at the right scope asks instead.  A method rather than ten copies\nof the raise, because ten spellings of one rule is ten things to keep in step \u2014\nand because the refusal is what the site-scope flag exists for, so it should be\none readable thing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, name: str, instead: str) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._rung_empty_bin": {
      "decorators": [],
      "doc": "Rung 0: an EMPTY bin, chosen by the arm's own placement policy.\n\nTHE ONLY RUNG THAT RANKS ANYTHING, and ADR-0003 says why it stays first: \"the\nnew-bin decision is where placement optimisation happens: a restock that returned to\nits own bin would hand the arms nothing to rank until a shelf was taken to zero.\"\nDemoting this rung would leave every arm running and every comparison between them\nmeasuring less, with nothing raising.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, item, queue) -> RungResult"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._rung_own_bins": {
      "decorators": [],
      "doc": "Rung 1: the SKU's OWN bins, fullest first, filled to capacity (ADR-0003).\n\nAHEAD OF THE TWO RESCUES IS THE WHOLE POINT: the rescues exist for a pallet meeting\na small-bin warehouse, and they must not fire for a carton whose own shelf has room.\nA rescue is rework; a top-up is a put.\n\nThe bin count is read off the `_put_topups` FLOW rather than returned, so\n`_top_up_own_bins` keeps its one-number interface.  It matters: a unit absorbed into\nthree own bins is three trips, three `bin_placement` rows and three\n`_reorder_placements`, so it must cost three against the budget too, or a crew cap is\nsilently generous exactly when the warehouse is most fragmented.\n\nA PARTIAL absorb sends the remainder back as ONE unit to take the chain from the\ntop -- a smaller unit may now find an empty bin, which is still empty-first.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, item, queue) -> RungResult"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._rung_repack": {
      "decorators": [],
      "doc": "Rung 2: repack into a smaller size tier \u2014 pallet OR fulfillment.\n\nBoth families are size-tiered; `UNIT_CLASSES` + the unit class select which.  The\nrework is RECEIVING work, priced per RESULTING pack (`_charge_repack`).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, item, queue) -> RungResult"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._rung_singleton": {
      "decorators": [],
      "doc": "Rung 3: singleton bins of the same order type \u2014 STORE ONLY.\n\nA fulfillment unit has no singleton fallback; it stays ff.  Same rework and same\nprice as the repack above: a singleton rescue is a repack that happens to land in\nthe forward-pick family.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, item, queue) -> RungResult"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._serve_order": {
      "decorators": [],
      "doc": "WHO is served first, out of one BinKey group.  The drain's decision.\n\n`putaway_window` is the tolerance, in units:\n\n  None  the policy's whole request is granted -- pre-Phase-2 behaviour, and still\n        the default, so nothing moves until a caller asks for a window.\n  1     strict FIFO.  The policy chooses the BIN and has no say in the order.\n  K     the policy may pick its favourite from the K OLDEST units still waiting.\n        Serve it, slide the window forward by one, repeat.\n\nA window of K >= len(units) is exactly `pool.order(units)`, including the\ntie-breaking: `order` is a stable sort so equal keys keep queue order, and the heap\nbelow breaks ties on arrival index for the same reason.  That equivalence is\nasserted in Tests/unit/test_putaway_window.py rather than assumed, because it is\nwhat makes \"K = infinity reproduces today\" a fact instead of a hope.\n\nThis method is the whole point of Phase 1.  Until the pool inversion, an assignment\nfunction returned `[(unit, bin)] in priority order` and the drain iterated it, so\nevery ranked policy was choosing the ORDER as well as the bin -- 14 of the 17\nrestock rules, none of them asked to.  Put-away is supposed to be FIFO with a little\ntolerance; instead the queue was freely re-sorted by whatever score maximised the\nassignment function.\n\nThe pool split that in two.  `pool.order(units)` is now a REQUEST: the policy states\nthe precedence it would like and this method decides what to grant.  Today it grants\nall of it, which is why everything is still byte-identical -- the seam is real but\nnot yet load-bearing.  A finite K-oldest window lands here, and only here.\n\n`put_key` is the QUEUE's own precedence, from `put_policy`.  When present it\nreplaces the pool's `sort_key` entirely -- the pool still chooses every bin, it just\nstops choosing who goes first.  `k` then bounds the result exactly as it bounds the\npool's own order, so the two knobs compose rather than override each other.\n\n`units` arrives in queue order (the group's insertion order into `_stock_queue`), so\nthe FIFO answer is already in hand and needs no extra bookkeeping to recover.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, pool, units: list, k: int | None, put_key=None) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._sigma_delta": {
      "decorators": [],
      "doc": "f_s \u00b7 D(bin) increment for the incremental \u03a3 f\u00b7D tracker.\n\nThe ONE shared expression behind every += / -= on _sigma_fd (placement adds,\neviction/depletion subtracts) \u2014 kept as a method so the formula can't drift\nbetween the three sites.  Callers guard on ``self._sigma_freq is not None``.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, bin_: Aisle.Bin) -> float"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._sku_demand_product": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict[int, float]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._sku_pick_load_product": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict[int, float]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._sku_vol_product": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict[int, float]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._stamp": {
      "decorators": [],
      "doc": "Wrap one unit as a stamped `PutawayItem` -- the ONE place the age stamp is\ntaken.  Split out of `_admit` for the standing yard, whose units are stamped at\nYARD ARRIVAL (their age is the trailer's, not the unload's) but enter no queue\nuntil the crew pulls them -- `_admit` routes as well as stamps, and routing a\ndeferred unit would put it away for free.  Every stamp still comes from here, so\nno producer can invent its own and quietly enter as age -1.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, source: str) -> 'PutawayItem'"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._stock": {
      "decorators": [],
      "doc": "Dispatch the queued wave to the placement policy: a ranked wave if the\npolicy carries a ``place_wave``, otherwise the per-unit path.  Single entry\nused by enqueue/enqueue_all (initial stock) and check_reorders (reorders).\n\n``budget`` caps how many units may be PLACED in this call; whatever is left stays\nqueued for the next one, which is the deferral the queue already does for a unit\nno bin can hold.  ``None`` (the default, and every caller today) means place\neverything, exactly as before.\n\nA budget is what a finite crew and a finite number of dock doors impose: the\nparameter exists so that constraint has somewhere to go without the drain being\nrewritten around it.\n\n``deadline`` is the WHISTLE, in seconds on the crews' batch-local clocks: a putter\nalready past it starts nothing new, and what is left rolls into the next call.  The\ntwo constraints are independent and both are checked -- a budget is people, a\ndeadline is the clock, and a warehouse can run out of either first.  ``None`` is\nevery run that does not ask for a day cut.\n\n``charge_cut=False`` drains against the deadline but leaves the cut UNCHARGED, for\na caller that drains twice in one day and charges once at the end -- see\n`count_put_cut`.  True is the default and every caller today.\n\nRuns the coupling guard first \u2014 even on an empty queue \u2014 so an armed/fn\nmismatch fails loudly before any placement: when travel costs are armed,\ncandidates is passed as None and place_one MUST read mgr._aisle_index instead;\na mismatch (index armed but fn scans, or vice-versa) silently returns None for\nevery placement.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, budget: int | None=None, deadline: float | None=None, charge_cut: bool=True) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._stock_per_unit": {
      "decorators": [],
      "doc": "Place queued StorageUnit objects one at a time via placement.place_one.\n\nUsed for initial enqueue, FIFO/cohesion reorders, and the stragglers a ranked\nwave leaves behind.  The coupling guard runs in _stock() (the single entry).\n\n**The fallback chain is `self._put_chain`, not a branch tree in here.** ADR-0003\nfixes what happens when no empty bin fits -- the SKU's own bins, then a repack into\na smaller tier, then a singleton rescue, then pending -- and each of those is a rung\nanswering `RungResult` (`Warehouse/inventory/put_rungs.py`). This loop walks the\nchain, takes the first rung that did something, and books it. Adding a rung is a\nname in a tuple; it used to be an edit inside these 180 lines.\n\n``budget`` caps PLACEMENTS, not pops: a repack splits one unit into several and\npushes them back, and charging a budget for that would make the cap depend on how\nbadly the warehouse is packed rather than on how much the crew can move. A rung\nreports its own `bins`, so a unit absorbed into three of its own bins costs three.\n\n``deadline`` is the day's whistle on the crew's batch-local clock; see `_stock`.\nChecked per unit rather than once, because each put advances the clock that decides\nit -- a crew with ten minutes left takes as many units as fit in ten minutes.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, budget: int | None=None, queue=None, deadline: float | None=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._stock_queue": {
      "decorators": [
        "property"
      ],
      "doc": "The put-away queue.\n\nA live deque while there is ONE queue -- which is the default, and why every\nexisting consumer, mutation site and test keeps working unchanged.  With the streams\nsplit there is no single deque to hand back, so this returns a read-only view and\nany code that tried to mutate it would fail loudly rather than mutate a copy.\nInternal drain code takes a `PutQueue` explicitly and never reaches for this.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._stock_ranked": {
      "decorators": [],
      "doc": "Ranked placement: sort units by pick-effort priority, then drain.\n\nGroups the queue by BinKey (handling, category, storage_size, unit_type)\n\u2014 the same key used by _candidates() \u2014 so units only compete with others\nin the same bin pool.  Within each group, placement.place_wave returns\n(unit, bin|None) pairs sorted by pick-effort priority so high-effort\nitems claim the best (lowest-D) bins before lower-priority items.\n\nUnits the wave cannot place are handed to the per-unit path\n(_stock_per_unit), which re-fetches candidates per unit so they spill into\nother tiers / remaining bins exactly as FIFO does (see below).\n\n``budget`` is spent per GROUP here, not per unit, and that is not a simplification.\n`place_wave` scores a whole group at once and mutates the manager's aisle running\nbalances as it decides, so each unit's bin depends on where the earlier units in\nthe same wave went.  Truncating a wave mid-way would place units under a balance\nthat assumed the rest landed too.  So the check happens BEFORE the wave is called:\na group that cannot be afforded is requeued untouched, having never influenced an\naisle balance.  A wave is the atom.\n\n``deadline`` is spent the same way and for the same reason: a wave the crew cannot\nstart before the whistle is requeued untouched rather than half-placed.  The clock\nmoves DURING a wave (every `_execute_placement` charges its queue), so a wave that\nbegins before the whistle can end after it -- that is the bounded overtime the gate\nis defined to allow, and it is why the check is per group rather than per unit.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, budget: int | None=None, queue=None, deadline: float | None=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._top_up_own_bins": {
      "decorators": [],
      "doc": "Add `unit`'s items to bins ALREADY HOLDING ITS SKU, fullest first.  Returns how\nmany items landed (0 when the SKU holds no bin with room).\n\nTHE SECOND RUNG of the put-away chain (ADR-0003): tried only after `place_one` has\nfailed to find an empty bin, and ahead of the repack/singleton rescues and\n`pending`.  Empty-first is what keeps the new-bin decision \u2014 the moment an\nassignment arm actually optimises anything \u2014 happening on every top-up it can; this\nrung is what stops the run falling off a cliff when the free index is dry.\n\nTHE NO-OP CONDITION IS PER-BUCKET, NOT GLOBAL, and the ADR states it loosely.  This\nrung fires exactly when `place_one` returns None, which is a free index empty FOR THE\nUNIT'S OWN BinKey \u2014 a run with plenty of free bins elsewhere can still reach it.  So\n\"a no-op in any run whose free index never exhausts\" is true only read per bucket;\nthe checkable claim is \"a no-op in any run where every unit finds an empty bin\".  The\nsame correction applies to the rescues, which this rung now pre-empts whenever the\nSKU has room \u2014 that was a reachable path before, so an arm that fires a rescue is NOT\nbyte-identical to HEAD even with a deep index.\n\nFULLEST FIRST, ties by `location`.  Remnants merge into the bin that is already\nnearest full, which leaves the emptiest bin emptiest and lets `drain_sku`'s\nsmallest-first pick take it to zero and hand it back to the free index.  The two\nrules are one mechanism and neither works alone.\n\nIT CROSSES THE FORWARD-PICK SPLIT.  Both `_sku_singleton_bins` and `_sku_pallet_bins`\nare drawn from, so a pallet unit's items can land in a forward-pick singleton bin and\nthe reverse.  That is deliberate and safe -- `own_bin_room` measures against the BIN's\nown family, so nothing is asked to hold more than it can -- but it does mean the rung\nconsolidates across a split that `_candidates_raw` never crosses.\n\nTHE FIFTH BIN-MUTATION SITE.  Every other write to bin state replaces a whole\n`storage`; this one adds to an existing unit's `quantity`, so it updates the same\nmanager dicts as `_execute_placement` MINUS everything that is about a bin becoming\noccupied:\n\n  * `_bin_sku`, `_sku_*_bins`, `_unavailable`, `_index` \u2014 unchanged: the bin was\n    already occupied by this SKU and already out of the free index.\n  * `_aisle_sku_counts` \u2014 unchanged: the SKU gained no aisle it was not already in.\n  * `_sigma_fd` \u2014 unchanged: \u03a3 f\u00b7D counts a SKU's bin OCCUPANCY, and no bin changed\n    hands.  Adding a delta here would charge the same bin twice.\n  * `space_timeline.fill` \u2014 NOT called: it means \"a free bin became occupied\", and\n    a bin that was already occupied is not news to the yard's space forecast.\n\nWhat DOES move is the merchandise: on-hand up, on-order down, and a put charged per\nBIN TOUCHED, because each is a separate trip for the putter.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, source: str | None=None, queue=None) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager._window_for": {
      "decorators": [],
      "doc": "The ordering tolerance for one queue: its own `k_cap`, or the manager's default.\n\nA spec's None means INHERIT rather than \"unbounded\", so the manager-wide\n`putaway_window` still governs every queue that has no reason to differ, and the\ndefault single queue behaves exactly as it did before queues existed.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, queue) -> int | None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.assigned_bins": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[Aisle.Bin]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.available": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[Aisle.Bin]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.carryover_rows": {
      "decorators": [],
      "doc": "`(batch_id, reason, sku, qty)` for every unit still standing unbinned.\n\nTHREE reasons, spanning TWO classes, and the split is deliberate:\n\n  placement failure   'unplaced' could not reach a bin; 'held' was refused floor\n                      space.  Keeping these apart is the whole value of the column:\n                      different problems, different fixes, and one carried-over\n                      count cannot tell them apart.\n  pre-placement       'dock' has not been offered a bin at all -- it is still on a\n                      trailer, waiting to be unloaded.\n\nSo sum ALL THREE for \"what is standing unbinned\", and filter to the first two for\n\"what did placement fail to do\".  Both sums are over THIS method's output, which is\nentirely levels.  The `carryover` TABLE also carries the pick side's `unpicked_*`\nFLOWS from a different producer, and a level and a flow must never be added: see\nthe reason column's own comment.  The dock is here because leaving it out made this\nsurface and `queue_contents` disagree about the same question: `queue_contents`\nemits a 'dock' kind, and on a 200-batch run that was 52,479 rows this table denied.\nA consumer summing carryover to size the backlog silently missed all of them.\n\nThe three are disjoint by construction -- a unit is on the dock, or admitted to a\nqueue, or held, never two of those -- so no sku can collide across reasons, and the\nprimary key stays one row per producer.  See `_insert_carryover`, which now RAISES\non a duplicate rather than replacing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch_id: int) -> list[tuple]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.count_put_cut": {
      "decorators": [],
      "doc": "Charge every queue the whistle stopped: `cut += len(queue.items)`.\n\nCOUNTED ONCE, AND OUTSIDE THE DRAIN LOOP.  The three gates in `_stock` each defer\nwork without counting it, deliberately: any of them can fire on several refill\npasses, and a counter incremented inside the loop would report how many passes the\ndrain happened to need rather than how much work the day boundary left standing --\nthe exact defect `blocked` was fixed for.\n\nSafe to compute afterwards because a clock only ever advances: a queue that could\nnot start during the drain still cannot start now, so what remains on it is\nprecisely what the whistle stopped.\n\n`_held` is NOT counted.  A held item was refused FLOOR SPACE and never reached a\nqueue; that is `blocked`, a different problem with a different fix, and the two\ncounters are worth having only while they stay disjoint.\n\nPUBLIC, AND ONCE PER DAY RATHER THAN ONCE PER DRAIN.  A caller that drains twice\nin one day -- a site put pool giving each channel a sub-deadline and then\nre-draining both against the whole day -- passes `charge_cut=False` to both drains\nand calls this itself, once, against the FULL-DAY deadline.  `cut` is a LEVEL:\nit re-counts the standing queue every time it is charged, so a second charge\ninside one batch inflates it where no downstream \"count the non-zero batches\" rule\ncan undo it.  `_stock` calls this inline, so the single-drain path is unchanged.\n\n`deadline is None` charges nothing: no whistle blew, so nothing was stopped.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, deadline: float | None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.dock_depth": {
      "decorators": [
        "property"
      ],
      "doc": "Storage units standing on the dock. 0 when there is no receiving crew.\n\nDISJOINT from `queue_depth`, which counts the put queues and `_held`: an item is in\none place or the other, never both. A reader wanting the whole unbinned backlog sums\nthem, which is why both are reported rather than one merged number.\n\nREFUSES on a coupled leaf: the dock is the site's, and \"how deep is MY dock\" has no\nanswer when one dock holds both channels' merchandise.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.drain_putaway_records": {
      "decorators": [],
      "doc": "Hand over this batch's put-away records, and start the crew's clock over.\n\nA DRAIN IS A BATCH BOUNDARY.  The records carry `t_start` on the crew's own clock\nmeasured from the start of the batch, because the consumer\n(`Optimization.metrics.work_events.put_rows`) offsets them onto the arm's absolute\naxis by adding the batch epoch -- and the batch epoch is not known until after the\nbatch's picks have been simulated, so the manager cannot stamp absolute times\nitself.\n\nResetting here is therefore load-bearing, not tidiness.  Without it `_put_clock`\naccumulates across the whole arm while `put_rows` still adds the epoch, so every\nput row after batch 0 is stamped too late by the total put-away seconds of every\npreceding batch, and the error grows without bound: measured on an eight-batch\nstore arm, batch 7's puts landed at `t_local` 53,769-64,152 s against a 17,906 s\nbatch, and batch 6's puts overran batch 7's picks so the merged view interleaved\nbatches.\n\nDrained rather than read so the caller takes ownership once per batch and the\nmanager never holds a run's worth of rows.\n\n`reset_clocks=False` hands the records over and LEAVES THE CREW WHERE IT IS, for a\ncaller that owns the reset because it owns the clocks -- a site put pool whose one\nlist is bound to both channels' queues, where a leaf resetting at its own drain\nwould zero the other leaf's half-spent day with nothing raising and every\nsubsequent row plausible.  Such a caller must still reset once per batch, or the\nparagraph above describes exactly what happens to it.  The default is every caller\ntoday.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, reset_clocks: bool=True) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.drain_receiving_records": {
      "decorators": [],
      "doc": "This batch's unload records, and restart the crew's clock. `[]` when no crew.\n\nA drain is a batch boundary -- see `Dock.drain_records` and, for what happens when\nthe reset is missed, `drain_putaway_records`.\n\nREFUSES on a coupled leaf, and this one is worse than reporting a wrong level: the\nfirst leaf to call it would take the OTHER channel's unload records into its own DB\nand restart the shared crew's clocks half-way through the site's batch, leaving the\nsecond leaf with an empty drain and every later row rebased.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.drain_repack_records": {
      "decorators": [],
      "doc": "This batch's repack records, and start the list over. `[]` when no crew.\n\nA SEPARATE STREAM from `drain_receiving_records`, not a widened one: `put_rows`\nrefuses to mix event types in one call (the `role`/`event_type` split it exists to\nkeep honest), so `repack` rows have to arrive as their own list or the writer would\nhave to re-derive the type per row from a discriminator nothing declares.\n\nREFUSES on a coupled leaf, for `drain_receiving_records`' reason: the list is the\nsite dock's and the first drain empties it for both channels.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.drain_yard_drains": {
      "decorators": [],
      "doc": "This batch's per-drain yard levels, and start the list over.\n\n`[]` when the standing yard is not bound, which is not the same as a row of zeros \u2014\nsee the field's own note.  Drained per batch for `drain_putaway_records`' reason.\n\nREFUSES on a coupled leaf.  The row is `(yard_start, free_doors_start, yard_end,\nremainder)` \u2014 trailer- and door-denominated, so the SITE's and not a channel's\n(ADR-0005), which is why the coordinator parks it on `site_rows` rather than handing\nit down.  A leaf that drained this anyway would report an empty yard while the\nsite's was full, because nothing was ever appended to its own list.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.drain_yard_trailers": {
      "decorators": [],
      "doc": "This batch's FINISHED trailer stamps, and start the transit's list over.\n\n`[]` on any transit without the standing surfaces (`BatchTransit`, `TrailerTransit`)\n\u2014 probed the way `_receive` probes for `STANDING`, so neither is edited to satisfy\na reader neither has anything to say to.\n\nREFUSES on a coupled leaf: both leaves hold ONE transit, so the first to drain takes\nevery trailer on the site into its own table and the second finds none \u2014 one\nchannel's yard scorecard rendered over the whole site's trailers, and the other's\nrendered empty. The mixed trailer is exactly what makes this undecomposable.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.empty_bins": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[Aisle.Bin]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.enable_putaway_timing": {
      "decorators": [],
      "doc": "Bind a put crew's travel speed + cost model, so every placement costs seconds.\n\nFollows `enable_sigma_fd`'s precedent: a binder the harness calls, so the domain\nnever reaches into CONFIG for it.  `speed` is a `cost_model.SpeedProfile` (a put\ncrew's own, which is why a Mode exists); `cost` defaults to `PutawayCost()`.\n\nADDITIVE BY CONSTRUCTION.  Put-away time does not move the pick clock, does not\ncontend for an aisle, and does not change which unit lands in which bin -- the same\nitems are picked, in the same order, at the same instants.  It records a duration\nand a row.  Contention is the inbound feature, not this seam.\n\n`clocks` gives the DEFAULT crew a pre-built worker list rather than minting one --\nthe seam a site-wide put pool binds to, so one list of putters serves two managers\nover segregated volume.  Held on the MANAGER because the queue set can be replaced\nafter this call (`put_queues` setter -> `_bind_put_crews`), and a rebind that minted\nfresh clocks would drop the pool on the floor with nothing raising.  A caller that\npasses it owns the reset; see `drain_putaway_records`.  None is every caller today.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, speed, cost=None, size: int=1, clocks: list | None=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.enable_receiving": {
      "decorators": [],
      "doc": "Give the warehouse a receiving crew, and a dock for it to work.\n\nFollows `enable_putaway_timing`'s precedent: a binder the harness calls, so the domain\nnever reaches into CONFIG for its own configuration.  There is deliberately no\n`disable_receiving` -- the off state is \"this was never called\", which is what makes\nthe feature's no-op structural rather than a flag nobody can see.\n\nNOT ADDITIVE, unlike put-away timing, and that is the point.  A receiving crew changes\nWHEN merchandise reaches a put queue, so it changes which units are binned in which\nbatch.  It does not change the pick simulation, the packing, or which bin a given unit\nlands in once it is offered.\n\n`dock` arrives CONSTRUCTED (`Inbound.dock.Dock`, already priced): the broker holds\nwhat it is handed and imports nothing -- `Warehouse/ -> Inbound/` is a forbidden\nedge, so construction is the driver's job, like every other injected policy.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dock) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.enqueue": {
      "decorators": [],
      "doc": "Queue one order for bin placement.\n\nquantity=None (default) reads equilibrium_qty from the order \u2014 the normal\npath for inventory intake.  Pass an explicit integer only when you need\nto override the order's own stock level (e.g. overstock sampling).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order, quantity: int | None=None) -> 'Inventory_Manager'"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.enqueue_all": {
      "decorators": [],
      "doc": "Queue a list of orders for bin placement.\n\nquantity=None (default) reads equilibrium_qty from each order \u2014 the normal\npath for inventory intake.  Pass an explicit integer only when you need\nto override every order's stock level (e.g. overstock sampling).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], quantity: int | None=None) -> 'Inventory_Manager'"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.free_bin_depth": {
      "decorators": [],
      "doc": "How many bins are in the free index right now -- a LEVEL, read at batch end.\n\nADR-0003's other half of the record: the own-bin rung fires because this ran to\nzero, so the share is only readable next to the depth that produced it.  Not reset,\nbecause a level is not a flow.\n\nTHE WHOLE GEOMETRY, not the leaf's section: `_index` holds every bucket the warehouse\nwas built with, and a channel leaf simulates one section of it, so this total counts\nthe other channel's untouched bins as free (memory `free-bins-counts-the-whole-\ngeometry`: 57% read where the section's real headroom was 15-18%).  It keeps its\nmeaning under its name -- `batch_stats.free_bins` is published -- and the reading a\nclause can act on is `free_bin_depth_by_bucket`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.free_bin_depth_by_bucket": {
      "decorators": [],
      "doc": "The free index PER BUCKET: `[(BinKey, free), ...]` in key order, a LEVEL per bucket.\n\nOne row per key `_index` has ever held, INCLUDING buckets whose list is empty -- a\nbucket that ran dry is the finding, and dropping its row would report it as \"no such\nbucket\" rather than as zero.  The keys are the four coordinates the record's\n`coverage.final.<ch>.fielded.buckets` stamps its setup `free` under, so a reader joins\nthe two on `(handling, category, size, unit)` and needs no other map.  A leaf's own\nsection is the rows whose `unit` is its regime's; the other section's rows are the\nartefact `free_bin_depth` cannot separate.  Sorted, so the row order is a pure function\nof warehouse state (the same determinism contract every other reader keeps).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.held_depth": {
      "decorators": [
        "property"
      ],
      "doc": "Units refused floor space and waiting upstream. 0 unless a queue sets `staging`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.in_transit_qty": {
      "decorators": [
        "property"
      ],
      "doc": "Total pieces on order and not yet released \u2014 the transit census's level.\nFor `BatchTransit` this is the historical sum of entry quantities, unchanged.\n\nREFUSES on a coupled leaf: the transit is the SITE's one yard, so this level is\nboth channels' merchandise and reporting it per leaf would double the site's\nin-flight pieces across the pair's two DBs.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.init_demand_state": {
      "decorators": [],
      "doc": "Populate demand-product lookup and per-aisle demand sums.\n\nMust be called after init_placement_state() so _aisle_sku_sets already\nreflects the actual placement.  Call once per strategy worker before\nswapping to a trip-cost assignment function.\n\nWhen *wp* is given, also build the cost-weighted labor twin\n(_sku_pick_load_product = f*q*cost1 = order.expected_labor, and the\nper-aisle _aisle_pick_load_sum) used by the Rank_labor balance selector.\n\n**`terms` prices only the levels the ARM maintains** -- the ledger book names its\n`PlacementPolicy.ledger_terms` declares.  `None` prices everything, which is what\nevery caller without an arm (tests, the benches, the diagnostics) wants.\n\nPricing more than the arm maintains is not free, and it is ticket 20's finding.\n`enqueue_all` runs the initial stock through the arm's OWN policy, which commits\nonly the levels it scores on; from the first placed unit the others are stale, and\n`drop_sku` then decrements them unconditionally, so they DECAY.  Measured at 65 of\n65 aisles on a cluster_map fixture.  Nothing read them -- `aisle_metrics.lift_sum`\nand `.pick_load_sum` were both deleted on that evidence -- but that is exactly what\nwas true of `vol_sum` until `rank_cartlabor` started reading it.\n\nThe per-SKU PRODUCT goes with its level, deliberately.  Seeding the product without\nthe level would leave `AisleLedger.reconcile()` comparing an empty level against\nproducts nobody summed -- a finding about the seed rather than about the warehouse.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, inventory: Any, wp: Any=None, terms: 'tuple[str, ...] | None'=None) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.init_placement_state": {
      "decorators": [],
      "doc": "Rebuild every index derived from WHERE things are currently placed.\n\nTwo jobs, and the split matters.  The aisle ledger's membership half --\nwhich SKUs an aisle holds, their matrix indices, their bin counts and\ntheir column positions -- plus the manager's own per-bin indexes:\n`_bin_sku`, `_current_quantities` and the per-SKU singleton/pallet sets.\n\nCall after uniform stocking, before swapping to an affinity-aware\nassignment_fn, so reorder decisions see the actual aisle composition\nrather than starting from zero, and so the incremental `_current_quantities`\ncounter agrees with the bins after any bulk stocking operation.\n\nNamed `init_lift_state` until 2026-09-16, when the aisle lift sum it was\nnamed for was deleted as write-only.  The name outlived the quantity by\nabout an hour and the method by none of it: everything else it seeds is\nstill load-bearing.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, affinity: AffinityStore) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.init_travel_costs": {
      "decorators": [],
      "doc": "Precompute _D on every bin and build the per-aisle sorted secondary index.\n\nMust be called after init_placement_state() and before swapping to a\nload-aware assignment_fn built with build_load_*_assignment_fn(...,\naisle_index=self._aisle_index).  After this call, _index_add and\n_index_remove maintain _aisle_index incrementally.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, wp: Any) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.lead_queue_depth": {
      "decorators": [
        "property"
      ],
      "doc": "In-flight transit ENTRIES: lead-queue records flag-off, trailers flag-on.\n\nThrough the transit's own census, NOT the `_lead_queue` shim below: the shim\nexposes `BatchTransit`'s entry list and a trailer transit has no such list \u2014 the\nfirst e2e run with the trailer flag on found exactly that AttributeError here.\nFor `BatchTransit`, `depth` IS `len(entries)`, so flag-off reads are unchanged.\n\nREFUSES on a coupled leaf, for `in_transit_qty`'s reason and with a sharper edge:\nunder a trailer pipeline an in-flight ENTRY is a TRAILER, and a trailer's load is\nmixed by construction \u2014 so this level is the whole site's and reporting it per leaf\nwould double the site's in-flight count across the pair's two DBs (the shape\nADR-0005 rejected in as many words). The site-scoped replacement counts this leaf's\nown in-flight LOTS instead, which is what `BatchTransit.depth` counted before the\ntrailer pipeline existed and the only reading that both decomposes and sums.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.put_chain": {
      "decorators": [
        "property"
      ],
      "doc": "ADR-0003's fallback rung names, in order \u2014 what `_stock_per_unit` walks.\n\nThe ADR parks two rejected alternatives (home bin, relocate on top-up) and says the\nrule \"may become a knob later, which is why the rework is recorded rather than\nhidden\".  This is that knob: a chain is a tuple of names, resolved through\n`put_rungs.chain_for`, which refuses an unknown one rather than leaving a rung that\nsilently never fires.  Before ticket 17 the same change was an edit inside a\n180-line loop.\n\nSetting it does NOT re-litigate the ADR.  The default IS the ADR's ruling, and\n`put_rungs.DEFAULT_PUT_CHAIN` records what a reordering forfeits.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[str, ...]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.put_queues": {
      "decorators": [
        "property"
      ],
      "doc": "The put-away streams.  Assigning a new set re-binds the crews, so a manager that\nalready had timing enabled keeps it -- swapping in `store_and_fulfillment()` after\n`enable_putaway_timing` is the normal order and must not silently turn timing off.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> PutQueueSet"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.putaway_seconds": {
      "decorators": [
        "property"
      ],
      "doc": "Total put-away labor recorded so far, in seconds.  0.0 when timing is off.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.queue_contents": {
      "decorators": [],
      "doc": "What is waiting for a bin, per stream, aggregated for the replay viewer.\n\nReturns `(kind, sku, unit_type, storage_size, queue, qty)`.  `kind` is 'stock' for\nitems on a queue and 'held' for items a full queue refused -- the second is invisible\nin `_stock_queue` by construction, so a reader that only walked the queues would\nunder-report the backlog exactly when backpressure is doing something.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[tuple]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.queue_depth": {
      "decorators": [
        "property"
      ],
      "doc": "Units waiting for a bin, INCLUDING those a full queue is holding upstream.\n\nHeld items are waiting just as much as queued ones -- the only difference is that\nthe floor has no room for them yet.  Reporting only the queued half would make a\nstaging limit look like the backlog had vanished, which is the opposite of what a\nbackpressure model is for.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.queue_state_rows": {
      "decorators": [],
      "doc": "One row per queue: depth and oldest age (LEVELS) plus the flow counters.\n\nDRAINS the counters, so it must run exactly once per batch or the next batch\ndouble-counts this one.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, batch_id: int) -> list[dict]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.receiving_seconds": {
      "decorators": [
        "property"
      ],
      "doc": "Total receiving labor recorded so far, in seconds. 0.0 when there is no crew.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.receiving_snapshot": {
      "decorators": [],
      "doc": "`(depth, unloaded, cut, seconds)` for this batch, resetting the three flows.\n\n`(0, 0, 0, 0.0)` when there is no receiving crew, so the caller writes the same row\nshape either way and a no-dock run records four honest zeros rather than a NULL that\nevery consumer then has to special-case.\n\nREFUSES on a coupled leaf: all four counters are the site dock's, and the flows\nRESET, so the first caller would take the site's whole batch and leave the second\nchannel reporting an idle dock.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.snapshot_putaway_rework": {
      "decorators": [],
      "doc": "`(put_topups, put_spills, recv_repacks, recv_repacked_packs)`, resetting all four.\n\nPer-batch FLOWS, so they reset -- the `cut`-is-a-level trap in reverse: these really\nare flows and summing them over batches is the right thing to do, which is only true\nbecause the drain happens exactly once per batch.  The spill rides second so the\nthree ADR-0003 events read in the order the chain fires them: a spill (the tier chain\nmoved up), a top-up (the whole chain was dry), a repack (the own bins were full too).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.standing_yard_trailers": {
      "decorators": [],
      "doc": "Stamps for trailers STILL ON SITE \u2014 read at run end, and never drained.\n\nThe censored tail.  A run that stops with trailers standing has held them for at\nleast as long as it ran, and dropping those rows would report the adversarial arm's\nfee as clipped rather than concentrated.\n\nREFUSES on a coupled leaf, and this one is worse than its two neighbours: it drains\nNOTHING, so both leaves would report the SAME censored trailers and every detention\nday still standing at run end would be billed twice.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.summary": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.transit_snapshot": {
      "decorators": [],
      "doc": "(sku, qty, remaining_lead) tuples \u2014 the public read the replay viewer uses\ninstead of reaching for a private attribute.\n\nREFUSES on a coupled leaf, for `in_transit_qty`'s reason: the rows are the site\nyard's and every one of them would be written into BOTH leaves' replay tables.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.unavailable": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[Aisle.Bin]"
    },
    "Warehouse/inventory/Inventory_Management.py::Inventory_Manager.units_ordered": {
      "decorators": [
        "property"
      ],
      "doc": "Units ORDERED in the most recent check_reorders() batch (\u03a3 reorder qty).  Distinct from\nreorder_placements (units PLACED) and in_transit_qty (units still on order).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/Inventory_Management.py::_MAX_REFILL_PASSES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "10000"
    },
    "Warehouse/inventory/Inventory_Management.py::_MultiQueueView": {
      "decorators": [],
      "doc": "A read-only, age-ordered view across split put-away streams.\n\n`_stock_queue` was a single deque that three external consumers iterate and measure\n(`Diagnostics/bucket_fill.py`, the runner's queue-depth ledger, and the conservation\ncheck).  Once the streams split there is no one deque to return, and returning a merged\nCOPY would let a caller mutate it and lose the write in silence.  This is iterable and\nsizeable and nothing else, so the mutation fails at the attribute rather than at the\nconsequence.\n\nMerged by arrival age, so a consumer asking \"what is waiting, oldest first\" gets the\nsame answer it always did rather than one queue's contents followed by another's.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/Inventory_Management.py::_MultiQueueView.__bool__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/Inventory_Management.py::_MultiQueueView.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, queues)"
    },
    "Warehouse/inventory/Inventory_Management.py::_MultiQueueView.__iter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/Inventory_Management.py::_MultiQueueView.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/Inventory_Management.py::_MultiQueueView.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/Inventory_Management.py::_ranked_by_score": {
      "decorators": [],
      "doc": "Attach each placement's 0-based rank within its group, best first.\n\n`prefers_low` comes from the pool, so rank 0 always means \"the best choice available\"\nrather than \"the smallest number\" -- three arms (tmax, rank_maxlabor, expn) optimise\nupward on purpose, and a consumer should not have to know which.\n\nUnscored placements (a policy with nothing to report for that unit, or a `None` bin)\nrank NULL rather than last: they were not worse, they were not measured.  Equal scores\nshare the LOWEST rank, competition-style, so a tie never implies an ordering the policy\ndid not make -- the tie was broken by candidate order, not by the objective.",
      "is_async": false,
      "kind": "function",
      "signature": "(taken: list, prefers_low: bool) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::_windowed": {
      "decorators": [],
      "doc": "Serve `units` best-first, but only ever choosing from the K oldest still waiting.\n\nRanked ONCE from a stable descending sort, then windowed on the rank.  The rank detour\nis not indirection for its own sake: the first version negated each key for a min-heap,\nwhich silently required every key to be a number, and `sku_batched` needs a two-level\nkey.  Ranking also puts the tie rule in one place -- `sorted` is stable, so units the\nkey cannot separate keep their arrival order, which is what a FIFO queue should do\nanyway.\n\nKeys are computed once by the caller and reused across every window an item appears in,\nwhich is what keeps this O(n log n) rather than O(n*k) key evaluations.",
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, keys: list, k: int) -> list"
    },
    "Warehouse/inventory/Inventory_Management.py::log": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "logging.getLogger(__name__)"
    },
    "Warehouse/inventory/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.inventory \u2014 The Inventory_Manager placement engine and its four mixins (planning, optimal, reorder, zoning) plus their shared leaf helpers.  Only inventory_reorder and inventory_zoning exist SOLELY for Inventory_Management; inventory_planning and inventory_optimal are also read from Optimization/ (see README.md for the two call sites), so a change to either is wider than the package boundary suggests.\n\nSee README.md for what does and does not belong here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/aisle_ledger.py": {
      "decorators": [],
      "doc": "aisle_ledger.py \u2014 what each aisle currently holds, priced.\n\nThe **aisle ledger** (CONTEXT.md) is one concept kept in ten parallel dicts: which SKUs an\naisle holds, which matrix indices they translate to, how many bins each occupies, where those\nbins sit, and the three priced LEVELS that membership implies \u2014 demand, pick load and cart\nvolume \u2014 plus the three per-SKU products those levels are summed from.\n\n    \u250c\u2500 membership \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500 priced levels \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500 per-SKU products \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n    \u2502 sku_sets              \u2502 demand_sum             \u2502 sku_demand_product          \u2502\n    \u2502 idx_sets              \u2502 pick_load_sum          \u2502 sku_pick_load_product       \u2502\n    \u2502 sku_counts            \u2502 vol_sum                \u2502 sku_vol_product             \u2502\n    \u2502 member_pos            \u2502                        \u2502                             \u2502\n    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\nWhy this module exists\n----------------------\nThese dicts had TWO writers who did not know about each other.  The *add* half lived\ninside the placement policies \u2014 ELEVEN hand-copied commit blocks in `Assignment_Functions.py`,\neach maintaining one of three different subsets \u2014 while `_execute_placement` committed only\n`sku_counts`.  (The ticket that opened this counted ten; reading the module found an eleventh,\n`_cluster_map_commit`, hiding as a helper next to `_commit_aisle`.)\nThe *drop* half lived in `inventory_reorder.py` in two copies, the second a hoisted-locals\ninline twin whose docstring asked a human to KEEP THE TWO IN SYNC.\n\nThey were not in sync, and nothing could have said so:\n\n- `vol_sum` was incremented by `rank_cartlabor` and decremented by neither drop path, so the\n  cart-swap penalty that arm competes on grew for the length of a run.  It is READ in the\n  scoring expression, so it moved placements, not just a recorded number.\n- `lift_sum` was decremented by both drop paths and incremented only by a production-dead\n  family, so it decayed monotonically in every shipped arm.  It was write-only end to end \u2014\n  no Quantity, figure, view or experiment read its column \u2014 so it was DELETED rather than\n  repaired, along with the `load_min`/`load_max` family that was its only in-memory reader.\n\nBoth are the same failure: a quantity whose add and drop live in different files, maintained by\ndifferent people at different times, with no place that owns the pair.\n\nWhat is and is NOT in here\n--------------------------\n`init_placement_state` on the manager does two jobs and only one of them is the ledger's \u2014 it also\nrebuilds `_bin_sku`, `_current_quantities` and the per-SKU bin indexes, none of which are aisle\nstate.  Those stay on the manager.  This module owns the aisle's own books and nothing else.\n\nPer-BIN and per-SKU are separate operations, deliberately\n---------------------------------------------------------\n`member_pos` records a COLUMN POSITION PER LIVE BIN, while the priced levels are SKU-once.  Six\nof the eleven historical commit blocks correctly kept the index add and the `member_pos` append\nOUTSIDE the `if sku not in sku_sets[aid]` guard for exactly this reason; the other five kept no\npositions at all.  So the interface\nsplits at each end \u2014 `add_bin`/`add_sku` and `drop_bin`/`drop_sku` \u2014 and the guard stays at\nthe call site, where each caller already has it.  Collapsing either into one method would\nquietly change which SKUs' positions are tracked.\n\nThe add end has a third method, `count_bin`, because the per-bin COUNT is the one book the\nmanager maintained rather than the policy.  Its drop-side mirror has no separate method: the\ndecrement is what decides \"last bin\", so it is fused into `drop_sku`.\n\nFloat discipline\n----------------\nEvery level is a running float sum, so its value depends on accumulation ORDER.  `add_sku` takes\nthe already-multiplied scalar from the caller rather than recomputing it from `f` and `q`: some\nhistorical blocks accumulate `+= f_s * q_s` and others `+= fq` computed earlier, and re-deriving\neither would move the last bits.  `drop_sku` clamps at zero exactly as the hand-written\ndecrements did.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger": {
      "decorators": [],
      "doc": "The aisle ledger: ten dicts, one owner, one add/drop pair at each grain.\n\nThe dicts are exposed as plain attributes, not properties.  They are read on the placement\nhot path by closures that hoist them once outside their loop, and an indirection there is a\nper-candidate cost \u2014 the same argument `inventory_zoning` records for staying a mixin.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.add_bin": {
      "decorators": [],
      "doc": "Record ONE bin's column position and its SKU's matrix index.  Mirrors `drop_bin`.\n\nPer-BIN, so it sits OUTSIDE the `holds()` guard at every call site: `member_pos`\ntracks live bin columns and must see every placement, while the priced levels are\nSKU-once.  Six of the eleven historical blocks already had it on this side of the\nguard and five kept no positions at all; none had it on the wrong side.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, idx: int | None, x_phys: float) -> None"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.add_sku": {
      "decorators": [],
      "doc": "FIRST bin of this SKU in this aisle: membership, and the levels it is priced into.\n\nPer-SKU, so it sits INSIDE the `holds()` guard, which stays at the call site\nbecause six of the eleven callers do per-bin work on the other side of it.\n\n**`None` is not 0.0.**  A level is `None` when the calling family does not\nmaintain it at all, and is then skipped \u2014 not added as zero, which would\nmaterialise a `defaultdict` key that family never had and change the rows\n`aisle_metrics` later writes.  A family that DOES maintain a level passes its\nvalue even when that value is 0.0, exactly as the hand-written `+= splp.get(sku,\n0.0)` did.  This is ticket 03's `ledger_terms` in its usable form: the terms a\nfamily maintains are the keywords it passes, derived at the call site rather\nthan hand-listed a second time somewhere else.\n\nThe caller passes the ALREADY-MULTIPLIED scalar (`f_s * q_s`, or an `fq` computed\nat a run boundary) because these are running float sums: re-deriving the product\nhere from `f_s` and `q_s` would move the last bits on the families that had\nalready hoisted it, and the placement oracles compare exact floats.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, sku: int, idx: int | None=None, *, demand: float | None=None, pick_load: float | None=None, vol: float | None=None) -> None"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.assert_sound": {
      "decorators": [],
      "doc": "`reconcile()` as an assertion, for tests and end-of-drain checks.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.bin_count": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, sku: int) -> int"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.bound": {
      "decorators": [
        "property"
      ],
      "doc": "Which books this ledger was HANDED.  Derived, so it cannot disagree with them.\n\nAn owning ledger holds them all; a policy's ledger holds only the ones its family\nmaintains, which is how `reconcile` knows which levels it may ask about.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> frozenset"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.count_bin": {
      "decorators": [],
      "doc": "One more bin of this SKU in this aisle.  The manager's half of a placement.\n\n`_execute_placement` has committed this and nothing else since before the other\nnine dicts existed \u2014 which is exactly how a policy that forgot a sum left the\nCOUNT right and the PRICE wrong, and read as a working warehouse.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, sku: int) -> None"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.drop_bin": {
      "decorators": [],
      "doc": "Retire ONE bin's column position.  Called for every reclaimed bin, live-bin only.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, idx: int | None, x_phys: float) -> None"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.drop_sku": {
      "decorators": [],
      "doc": "Decrement the SKU's bin count; on its LAST bin, retire it and subtract its levels.\n\nReturns True when the SKU left the aisle.  Every subtraction is clamped at zero, as\nthe two hand-written copies were.\n\n`last_when_zero` reproduces the difference between the two original copies: the cold\npath used `elif n == 1`, so a defensive n == 0 fell through and did nothing; the hot\ntwin used a bare `else`, treating n == 0 like n == 1.  The twin's callers pass True.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, sku: int, idx: int | None, *, last_when_zero: bool=False) -> bool"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.holds": {
      "decorators": [],
      "doc": "Is this SKU resident in this aisle?  The guard every commit block already uses.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int, sku: int) -> bool"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.maintained_levels": {
      "decorators": [],
      "doc": "The priced levels this ledger is entitled to be reconciled on.\n\nFor a policy's ledger these are the levels its family was handed and therefore\nmaintains -- `demand_sum` for every family, plus `pick_load_sum` on the two labour\nbalancers and `vol_sum` on `rank_cartlabor`.  Derived from the wiring, so it cannot\ndisagree with it; hand-listing the same fact a second time is what ticket 03 calls\n`ledger_terms` and what this makes unnecessary.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[str, ...]"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.members": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid: int) -> set[int]"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.over": {
      "decorators": [
        "classmethod"
      ],
      "doc": "A ledger bound to dicts SOMEBODY ELSE owns, rather than ones it created.\n\nTwo callers, and they are the reason this exists rather than `__init__`:\n\n- a placement policy, which is handed the aisle dicts down a long parameter\n  chain and must write through to whichever set it was given;\n- the inbound gain evaluator, which hands a policy copy-on-write wrappers so a\n  VIRTUAL placement cannot advance the real warehouse (`Inbound/gain.py`).\n\nA book left out reads as empty and REFUSES a write (`_UnboundBook`), so a family\nthat never maintains `pick_load_sum` has somewhere harmless to look without a\n`None` check on the hot path, and a family that writes a book nobody gave it\nfails loudly instead of writing where no reader can follow.\n\n**Spelled out rather than looped, deliberately.**  `**books` plus ten `setattr`s\ncost 2.00 us; this costs 0.29 us, and the difference is 23.4% of a whole pool open\nagainst 4.2% -- paid once per virtual placement in the gain evaluator, which\nrebuilds the arm's policy every time.  `**unknown` keeps the helpful refusal for a\nmisspelled book and is free (0.014 us).",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, *, sku_sets=None, idx_sets=None, sku_counts=None, member_pos=None, demand_sum=None, pick_load_sum=None, vol_sum=None, sku_demand_product=None, sku_pick_load_product=None, sku_vol_product=None, **unknown) -> 'AisleLedger'"
    },
    "Warehouse/inventory/aisle_ledger.py::AisleLedger.reconcile": {
      "decorators": [],
      "doc": "Every priced level must equal the sum of its members' products.  [] when sound.\n\nThis is the assertion the eleven loose dicts made impossible to write, and the reason\nboth drifts survived for the life of their features.  Returns findings rather than\nraising so a caller can report them all at once.\n\n**Unconditional, and it took two tickets to get there.**  It briefly took a `levels`\nargument, because `init_demand_state` priced all three levels off the current\nplacement while a family maintains only the ones it scores on -- so on fifteen of the\nseventeen arms two levels drifted from the first placed unit, and asking the whole\nquestion would have reported the SEED rather than the warehouse.  Ticket 23 made the\nseed take the arm's terms, so there is no level left that is priced and then\nabandoned, and the argument was deleted with the condition that needed it.  A level\nwhose products were never seeded is skipped below, which is a different thing: that\nis a level this run does not have, not one it has and mismaintains.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, *, tol: float=1e-06) -> list[str]"
    },
    "Warehouse/inventory/aisle_ledger.py::_UNBOUND": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_UnboundBook()"
    },
    "Warehouse/inventory/aisle_ledger.py::_UnboundBook": {
      "decorators": [],
      "doc": "A book its ledger was never handed.  Reads see empty; a WRITE is refused.\n\n`over` binds this ONE shared instance for every book a caller leaves out, instead of\nallocating a private dict per missing book.  That is ten allocations saved per call on\na path the inbound gain evaluator walks once per VIRTUAL PLACEMENT -- it rebuilds the\narm's policy every time, at 12.59 pool opens per placement.  Measured in place against\na real pool open: binding a ledger went from 2.00 us to 0.29 us, from 23.4% of a whole\npool open down to 4.2%.\n\nRefusing is also the better failure.  A private empty dict ABSORBS a write that nobody\ncan ever read -- a family committing to a book its builder was never given, silently.\nRefusing is what makes ONE shared instance safe to hand every ledger: there is no state\nhere for one family's stray write to leave behind for the next.",
      "kind": "class",
      "signature": "(dict)"
    },
    "Warehouse/inventory/aisle_ledger.py::_UnboundBook._refuse": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *_a, **_k)"
    },
    "Warehouse/inventory/aisle_ledger.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['AisleLedger']"
    },
    "Warehouse/inventory/inventory_common.py": {
      "decorators": [],
      "doc": "inventory_common.py \u2014 leaf types/constants/helpers shared by the Inventory_Manager\nmixins and by Assignment_Functions.\n\nExtracted so the manager can be split into cohesive mixin modules\n(inventory_planning / inventory_optimal / inventory_reorder) without import cycles:\nthis module depends only on the low-level warehouse primitives, never on the mixins or\non Inventory_Management itself.  Inventory_Management re-exports these names for\nbackward compatibility (`from Inventory_Management import Placement, BinKey, ...`).",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/inventory_common.py::AssignmentFn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Callable[[StorageUnit, list[Aisle.Bin]], Aisle.Bin | None]"
    },
    "Warehouse/inventory/inventory_common.py::BinKey": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "tuple[str, str, str, str]"
    },
    "Warehouse/inventory/inventory_common.py::FORWARD_PICK_FAMILY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'singleton'"
    },
    "Warehouse/inventory/inventory_common.py::PUTAWAY_SOURCES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('intake', 'reorder', 'reslot', 'trailer')"
    },
    "Warehouse/inventory/inventory_common.py::Placement": {
      "decorators": [],
      "doc": "One named placement policy \u2014 the single object a strategy hands the manager.\n\n``place_one`` (per-unit: ``(unit, candidates) -> bin|None``) is ALWAYS present; it\ndrives the per-unit drain (initial stock, FIFO/cohesion reorders) and places the\nstragglers a ranked wave leaves behind.  ``place_wave``\n(``(units, candidates_fn) -> [(unit, bin|None)]``), when present, makes this a\nRANKED policy: a whole BinKey group is placed at once in pick-effort order.\n\nBecause every strategy sets exactly one ``mgr.placement`` (never None), the ranked\ndrain is just a policy that also carries a ``place_wave`` \u2014 no special-casing, and\na future ranked-cohesion policy is expressible the same way.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_common.py::Placement.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, name: str, place_one: AssignmentFn, place_wave: 'RankedAssignmentFn | None'=None, order_score: 'Callable[[Any], float] | None'=None, open_pool: 'PoolFn | None'=None) -> None"
    },
    "Warehouse/inventory/inventory_common.py::Placement.is_pooled": {
      "decorators": [
        "property"
      ],
      "doc": "The drain may choose the order: this policy answers one unit at a time.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/inventory_common.py::Placement.is_ranked": {
      "decorators": [
        "property"
      ],
      "doc": "Placed a whole BinKey group at once -- by pool or by wave.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/inventory_common.py::PoolFn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Callable[[list], 'Pool']"
    },
    "Warehouse/inventory/inventory_common.py::PutawayItem": {
      "decorators": [],
      "doc": "One unit waiting for a bin, and where it came from.\n\nThe queue used to hold bare `StorageUnit`s, fed identically by initial intake\n(`enqueue`/`enqueue_all`), reorder arrivals (`_release_to_stock`) and reloader\nevictions (`requeue_bin`).  Once a unit was in, its origin was unrecoverable \u2014 so\n`BinRecorder` reconstructed 'reslot' by holding a set of `id(unit)` and testing\nmembership at placement time.  That works only because the evicted unit is the\nIDENTICAL object and is still alive; it is not a property anything declares.\n\nCarrying the source with the work item makes `bin_placement.cause` a fact the queue\nknows rather than one the recorder infers, and it is the field a trailer id will\noccupy when inbound loads become a source of their own.\n\n`age` is a monotonic arrival stamp, and it exists because \"FIFO\" stopped being a\nproperty of the deque the moment the drain got a K-oldest window.  Queue POSITION used\nto be the only record of how long something had waited, which is fine while nothing ever\nre-enters \u2014 and three things do: the repack rescue, the singleton rescue, and a group\nrequeued whole when the put-away budget runs out.  A stamp survives all three and can be\nasserted; a position cannot even be inspected after the fact.\n\nIt is also what the queue-state snapshots report as `oldest_age`, and the field a\ntrailer's arrival time will map onto when inbound becomes a producer.\n\nFrozen because a queued item's origin must not be editable in flight; `respawn` is the\none legal derivation, used by the repack and singleton rescues, which split one unit\ninto several without changing where any of them came from \u2014 or how old any of them is.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_common.py::PutawayItem.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, source: str='intake', age: int=-1) -> None"
    },
    "Warehouse/inventory/inventory_common.py::PutawayItem.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/inventory/inventory_common.py::PutawayItem.__setattr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, *_a)"
    },
    "Warehouse/inventory/inventory_common.py::PutawayItem.respawn": {
      "decorators": [],
      "doc": "A unit split out of this one during a rescue, keeping the origin AND the age.\n\nA repack does not make a unit newer.  The pallet that arrived first and had to be\nbroken into three should still be put away before a pallet that arrived after it;\ninheriting the stamp is what says so, and it is what keeps the rescue paths from\nbecoming a way to jump the queue.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit) -> 'PutawayItem'"
    },
    "Warehouse/inventory/inventory_common.py::RankedAssignmentFn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "Callable[[list[StorageUnit], Callable[[StorageUnit], list[Aisle.Bin]]], list[tuple[StorageUnit, 'Aisle.Bin | None']]]"
    },
    "Warehouse/inventory/inventory_common.py::UndeclaredStock": {
      "decorators": [],
      "doc": "A SKU's Order-Up-To target was read before any run declared one.",
      "kind": "class",
      "signature": "(RuntimeError)"
    },
    "Warehouse/inventory/inventory_common.py::UnfieldableRequirement": {
      "decorators": [],
      "doc": "A bucket's emitted capacity cannot hold the bins the declared levels need.\n\nThe line floor is a PROMISE (department-calibration, \"Field the floor\", decision 3): the\nwarehouse is sized to hold the run's declaration, so the only way here is a declaration\nthe sizing was not allowed to follow -- a `max_bins` / `max_aisles` cap, a composition\nbasis vector, or one of the two aisle-shape splits (`aisle_split`, `depth_classes`) losing\nmore bins to rounding than the sizing left slack for.  A cap and a floor are two\ndeclarations by the same person that contradict; the run says which bucket and how many\nbins short rather than silently fielding less.\n\n`short` carries the shortfall STRUCTURALLY -- `[(BinKey, requirement, capacity, budget),\n...]`, every short bucket, never the message's truncated dozen.  The prose is for the\noperator; a caller (a test, a driver deciding what to retry with) reads this instead of\nparsing it.  The message's own tail names all three possible causes, so a substring\nsearch over it can neither identify the bucket nor the cause.",
      "kind": "class",
      "signature": "(RuntimeError)"
    },
    "Warehouse/inventory/inventory_common.py::UnfieldableRequirement.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, message: str, short: list | None=None) -> None"
    },
    "Warehouse/inventory/inventory_common.py::WarehousePlan": {
      "decorators": [
        "dataclass"
      ],
      "doc": "Result of Inventory_Manager.plan_warehouse: a warehouse sized to hold the run's\ndeclared levels, plus the orders it FIELDS at exactly those levels.\n\n`sampled` is a historical name for \"the orders this plan fields\".  It was a genuine\nsubset while the planner sampled SKUs to fill a capacity it had sized independently; it\nis now the whole list, because every bucket is sized from `requirement` and every SKU is\nfielded at its declaration (department-calibration, \"Field the requirement\").",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins": {
      "decorators": [],
      "doc": "A set-of-bins whose iteration order is PERMANENTLY `location` order.\n\nReplaces the `set[Aisle.Bin]` values of `_sku_singleton_bins` / `_sku_pallet_bins` so\n`Task.from_batch` can drain a SKU's bins without paying `sorted(...)` twice per batch\nSKU (the deep-ladder t_task k\u22482.3 offender: batch-SKU count AND per-SKU bin multiplicity\nboth grow with the catalogue).  The maintained order is exactly what those sorts\nproduced \u2014 `location = (aisle_id, bayX, bayY)` is immutable for a bin's lifetime and\nglobally unique (monotone aisle ids, unique bay tuples), so iteration here is\nbyte-identical to `sorted(old_set, key=location)`.\n\nSemantics preserved from `set`: identity membership (Aisle.Bin has no __eq__/__hash__),\n`discard` of a non-member is a silent no-op, truthiness/len.  Duplicate `add` is an\nASSERT rather than a silent dedupe: `_execute_placement` cannot re-place an indexed bin\n(`_index_remove` pops `_bin_index_pos` first), so a duplicate here is a real bug.\n\nThe bisect-with-key idiom mirrors `_index_add`/`_index_remove`\n(Inventory_Management.py) \u2014 the in-repo precedent this container copies.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.__bool__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.__contains__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_) -> bool"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.__iter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.add": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_) -> None"
    },
    "Warehouse/inventory/inventory_common.py::_SortedBins.discard": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_) -> None"
    },
    "Warehouse/inventory/inventory_common.py::_by_location": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "operator.attrgetter('location')"
    },
    "Warehouse/inventory/inventory_common.py::_equilibrium_qty": {
      "decorators": [],
      "doc": "Return the Order-Up-To target for *order*.\n\nReads `equilibrium_qty` if a run has DECLARED one (`Order.declare_stock`); falls back to\nthe legacy duck-typed `stock_qty` attribute so old in-memory inventories still work.\n\nRAISES on an undeclared order rather than defaulting.  It used to answer 1, and a default\nhere is the worst possible silence: the catalogue no longer authors a level (ADR-0002), so\nevery SKU of a freshly loaded catalogue would answer 1, `bucket_requirements` would size\nevery bucket for one unit per SKU, and the run would build a warehouse an order of\nmagnitude too small -- with no error, at the one moment nothing downstream can detect it.\nA level is a run's declaration; asking for one before it is made is a bug in the caller.",
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order) -> int"
    },
    "Warehouse/inventory/inventory_common.py::_max_qty_fitting_size": {
      "decorators": [],
      "doc": "Max number of *order* items that stack into ONE unit of *unit_category* whose\nstorage_size is at most *target_size* (the pallet/fulfillment twins, unified via\nthe UNIT_CLASSES registry).\n\nStacking height increases monotonically with quantity, so the required\nstorage_size also increases: scan from 1 upward until the unit outgrows the\ntarget tier and return the last fitting quantity.  Used by _stock to repack a\nstranded unit into smaller bins and by the planner's reachability pass.",
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order, target_size: str, unit_category: str='pallet') -> int"
    },
    "Warehouse/inventory/inventory_common.py::_uniform_assignment": {
      "decorators": [],
      "doc": "Pick uniformly at random from the candidate bin list.\n\ncandidates is pre-filtered by _candidates() to the correct handling type,\nstorage category, unit type, and largest available size tier.  Picking\nrandomly within that filtered set uniformly distributes placements across\nthe matching bin locations.",
      "is_async": false,
      "kind": "function",
      "signature": "(unit: StorageUnit, candidates: list[Aisle.Bin]) -> Aisle.Bin | None"
    },
    "Warehouse/inventory/inventory_common.py::_wp_for": {
      "decorators": [],
      "doc": "Resolve the WorkloadParams for *obj*'s storage regime from a mixed-warehouse\n``wp.by_regime`` map, falling back to *wp* itself.  Placement/assignment sites process\none regime at a time (a BinKey group / a single unit), so resolving from that entity is\nexact; single-regime runs carry no map and return *wp* unchanged (store byte-identical).",
      "is_async": false,
      "kind": "function",
      "signature": "(wp, obj)"
    },
    "Warehouse/inventory/inventory_common.py::binkey_of": {
      "decorators": [],
      "doc": "The 4-tuple BinKey of a StorageUnit or an Aisle.Bin (duck-typed like regime_of).\n\nUnits read (handling, category) from their order's storage_handle_config plus their\nown (storage_size, unit_category) \u2014 Singleton's fixed 'singleton' label comes from\nSingleton.storage_size itself, so no special-casing.  Bins read their four mirror\nattributes.  One constructor for the key that used to be hand-built at ~8 sites.",
      "is_async": false,
      "kind": "function",
      "signature": "(obj) -> BinKey"
    },
    "Warehouse/inventory/inventory_common.py::is_forward_pick": {
      "decorators": [],
      "doc": "Whether a unit or a bin belongs to the FORWARD-PICK index rather than reserve.\n\n`Task.from_batch` drains `_sku_singleton_bins` before `_sku_pallet_bins` \"so that\nforward-pick locations are always preferred over reserve locations\" -- this is the\npredicate that decides which of those two a bin is filed under.\n\nDuck-typed through `binkey_of`, which is what lets ONE expression serve both sides.\nIt replaced three: `isinstance(unit, Singleton)` on the unit side and\n`bin_.unit_type == 'singleton'` twice on the bin side.  Those agreed, but only because\n`_candidates_raw` matches `unit_type` EXACTLY in every branch, so a unit never reaches\na bin of another family -- an invariant nothing stated and nothing checked.  Three\nspellings of one concept, two of them on the opposite object from the third, is a\ndisagreement with a delay on it.\n\nFULFILLMENT IS NOT FORWARD-PICK, and that is deliberate rather than overlooked: a\nfulfillment SKU has exactly one bin family, so the two-phase drain is a no-op for it\nand everything lands in the reserve index.  If fulfillment ever grows a forward/reserve\nsplit, this is the one place that decides it.",
      "is_async": false,
      "kind": "function",
      "signature": "(obj) -> bool"
    },
    "Warehouse/inventory/inventory_common.py::own_bin_room": {
      "decorators": [],
      "doc": "How many MORE of *order*'s items the unit already standing in *bin_* can take.\n\nThe own-bin rung of the put-away chain (ADR-0003) needs one number: a bin holding this\nSKU has room for `capacity(bin's own tier) - on_hand` more, and zero is the answer for\nan empty bin, a full one, or a bin holding something else.  Callers must already know\nthe bin holds *order* \u2014 this measures room, it does not check ownership.\n\nTHE BIN'S OWN TIER IS THE CEILING, not the family's largest.  A 'small' pallet bin\nholds what a small pallet holds, whatever a large one could, because the bin was built\nto a size and the unit standing in it has to keep fitting.\n\nSingleton bins are measured against `Singleton` directly rather than through\n`_max_qty_fitting_size`: they carry no tier (`storage_size is None`) and that helper\nfalls back to the PALLET tables for an unknown category, which would overstate a\nsingleton's room by the whole difference between the two families.",
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order, bin_: Aisle.Bin) -> int"
    },
    "Warehouse/inventory/inventory_common.py::tier_ranks_for": {
      "decorators": [],
      "doc": "(size_ranks, sizes_descending) for a unit_category \u2014 registry lookup; unknown\ncategories (e.g. 'singleton', which has no tiers) fall back to the pallet tables,\npreserving the original behaviour.",
      "is_async": false,
      "kind": "function",
      "signature": "(unit_category: str) -> tuple[dict, tuple]"
    },
    "Warehouse/inventory/inventory_optimal.py": {
      "decorators": [],
      "doc": "inventory_optimal.py \u2014 optimal layout, \u03a3f\u00b7D objective, full-labor floor + optimal map.\n\n`OptimalLayoutMixin` holds the benchmark/optimal-placement methods.  Mixed into\nInventory_Manager so the public API (`place_optimal`, `optimal_sigma_fd`, `optimal_work`,\n`build_optimal_map`, `current_sigma_fd`, `enable_sigma_fd`, `tracked_sigma_fd`) is\nunchanged.  All instance state it reads (`self._key`, `self._execute_placement`,\n`self._originals`, `self._unavailable`, `self._bin_pref`, `self._map_target`,\n`self._sigma_*`) is initialised by Inventory_Manager.__init__.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/inventory_optimal.py::BRANCHES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('lap', 'greedy_gate', 'greedy_error', 'greedy_empty')"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin._handle_var": {
      "decorators": [
        "staticmethod"
      ],
      "doc": "Per-unit weight/volume handling term v_s (no intercept, no quantity) \u2014 the\nheight-scalable part of pick effort.  Mirrors Order.compute_labor_cost.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, wp) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin._optimal_assign": {
      "decorators": [],
      "doc": "Pure-global-D optimal layout: per BinKey class, assign the highest\npick-frequency units to the lowest-D bins (rearrangement-inequality optimum\nfor within-aisle travel).  Returns the minimal Sigma f*D.  When place=True,\ncommits each unit to its bin and registers originals so reorders work.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], freq_of: dict, x_speed: float, y_speed: float, place: bool) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin._optimal_work_assign": {
      "decorators": [],
      "doc": "Exact minimal expected WORK (travel + height-weighted handling) for this\nwarehouse + inventory, and each SKU's optimal preferred score.\n\nPer BinKey class solves the assignment  min \u03a3 f_s\u00b7D_b + (f_s\u00b7q_s\u00b7v_s)\u00b7M(y_b)\nexactly (scipy LAP) \u2014 the rearrangement/transportation optimum that drives the\nhighest height-sensitivity (f\u00b7q\u00b7v) units to the lowest-M bins and the highest\nfrequency to the lowest-D bins.  Returns:\n  W*          = \u03a3 (assigned bins) f\u00b7(intercept + D_b) + (f\u00b7q\u00b7v)\u00b7M_b\n                (per occupied-bin convention, matching current_sigma_fd)\n  sku_target  = {sku \u2192 pref(b*)} where b* is the SKU's assigned bin and\n                pref(b) = D_b + M(y_b)\u00b7V_REF is the quantity-free bin basis.\nPure computation \u2014 does not mutate manager state.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], freq_of: dict, qty_of: dict, wp) -> tuple[float, dict]"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin._optimal_work_assign._Dk": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bn, _x=xs_k, _y=ys_k)"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin._optimal_work_assign._Mk": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bn, _b=brackets_k)"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin._take_optimal_bin": {
      "decorators": [],
      "doc": "Pop the lowest-D available bin for *unit*, smallest fitting tier first\n(same tier logic as _candidates, spilling UP only when a tier is empty).\nbins_by_key maps BinKey -> deque of bins pre-sorted by D ascending.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bins_by_key: dict, handling: str, category: str, unit_type: str, unit: StorageUnit) -> 'Aisle.Bin | None'"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.build_optimal_map": {
      "decorators": [],
      "doc": "Build the optimal map (the score-match basis) on this manager and return W*.\nSets `_bin_pref` (quantity-free location score for EVERY bin) and `_map_target`\n(each SKU's optimal preferred score).  Call once at warehouse build, after the\ninventory is assigned.\n\nAlso leaves `_map_lap_stats` on the manager \u2014 how much of the assignment was\nactually solved exactly rather than greedily.  Read it rather than assuming: at\nproduction catalogue size the large BinKey classes are far past the exact-solve\ngate, so \"the optimal map\" is exact on a tail of small classes and near-optimal\non the bulk.  The return value stays W* alone; a second return would have to be\nunpacked at four call sites for a number only the analysis layer wants.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], freq_of: dict, qty_of: dict, wp) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.current_sigma_fd": {
      "decorators": [],
      "doc": "Realised demand-weighted within-aisle travel = sum over occupied bins of\nfreq[sku] * D(bin).  The primary convergence metric.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, freq_of: dict, x_speed: float, y_speed: float) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.enable_sigma_fd": {
      "decorators": [],
      "doc": "Bind the freq map + speeds and seed the incremental Sigma f*D from a\nsingle full scan.  Afterwards tracked_sigma_fd() is O(1): the running sum is\nmaintained on every placement / pick-empty / eviction.  x_speed/y_speed are ft/s;\n_sigma_x/_sigma_y store the per-inch PACE so the incremental updates stay multiplies.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, freq_of: dict, x_speed: float, y_speed: float) -> None"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.optimal_sigma_fd": {
      "decorators": [],
      "doc": "The minimal achievable Sigma f*D for this warehouse + inventory (the\nyardstick).  Pure computation \u2014 does not mutate manager state.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], freq_of: dict, x_speed: float, y_speed: float) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.optimal_work": {
      "decorators": [],
      "doc": "Minimal achievable expected work W* (travel + height handling) \u2014 the floor\nyardstick.  Pure computation; does not mutate manager state.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], freq_of: dict, qty_of: dict, wp) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.place_optimal": {
      "decorators": [],
      "doc": "Stock the warehouse at the pure-global-D optimal layout.  Returns the\noptimal Sigma f*D.  Bumps _reorder_placements per unit (the worker discards\nthe initial-stock churn with a pop_churn() before the batch loop).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: list[Order], freq_of: dict, x_speed: float, y_speed: float) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::OptimalLayoutMixin.tracked_sigma_fd": {
      "decorators": [],
      "doc": "The incrementally-maintained Sigma f*D (see enable_sigma_fd).  O(1).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/inventory/inventory_optimal.py::log": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "logging.getLogger(__name__)"
    },
    "Warehouse/inventory/inventory_planning.py": {
      "decorators": [],
      "doc": "inventory_planning.py \u2014 warehouse sizing/fielding (pre-instantiation).\n\n`PlanningMixin` holds the static/class methods that size a warehouse FROM an inventory\nbefore any manager instance or warehouse exists.  Mixed into Inventory_Manager so the\npublic API is `Inventory_Manager.plan_warehouse(...)`, `.bucket_requirements(...)`,\n`.field_requirement(...)`.\n\nTHE PLANNER HAS ONE CONTRACT (department-calibration, \"Field the floor\" decisions 1-4 and\n8-11, built by \"Field the requirement\").  `bucket_requirements` states, per bin bucket,\nexactly what the run's declared stock levels need; every bucket is sized from that statement;\nand every SKU is then FIELDED at exactly its declaration, packed the way that statement\ncounted it.  There is no re-choice, no growth into leftover capacity, and no shrink to fit \u2014\nso `planned_sum_q == sum_q`, and a fill rate priced before the plan equals one priced after.\n\nThat makes the line floor a PROMISE rather than a request.  The planner used to size a\nwarehouse and then sample SKUs into whatever it had built, which is how a third of the\nreference pair's fulfillment section came to be fielded below its own line floor: a shelf\nbelow the line is a treadmill under base stock, and the equilibrium check's clauses cannot\npass under one.  Where the promise cannot be kept \u2014 a cap, a composition vector or an aisle\nsplit that leaves a bucket short \u2014 the run REFUSES (`UnfieldableRequirement`) and names the\nbucket and the bins short, rather than fielding less in silence.\n\n`store_fill` / `ff_fill` therefore mean \"the share of each bucket the declared levels occupy\nat setup\"; the rest is free bins, which is the headroom a base-stock top-up lands in.\n\nTHE PROMISE, RE-STATED for a run that DERIVES its headroom (\"Derive the fill headroom from\nthe fragmentation\").  A typed fill is an assumption about how much of that headroom the\nchurn will need; under the calibrated era the caller knows (`simconfig/fragmentation.py`\nstamps the expected stationary extra bins per bucket) and hands the planner `bucket_hold`:\nthe bins each bucket must HOLD -- its declaration plus its stationary fragmentation, never\nless than the declared minimum headroom leaves (`era_coverage.derive_fill`).  A bucket\nnamed there is sized to hold that many bins and the promise is checked against it, so\n\"the warehouse holds its declaration\" becomes \"the warehouse holds its declaration AND the\nfragmentation base stock creates\", and a refusal names the extra bins short, not only the\nrequirement.  A bucket the map does not name keeps the fill rule; `bucket_hold=None` is the\nfill rule for every bucket, byte for byte.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin._packing": {
      "decorators": [
        "staticmethod"
      ],
      "doc": "One pass over the catalogue answering BOTH halves of the planner's contract:\n`({bucket: bins needed}, {id(order): [(is_singleton, qty_per_unit, count), ...]})`.\n\nSizing and fielding are the same statement \u2014 what the declared levels need, and how\nthey are packed to need it \u2014 so they are computed together rather than by two passes\nthat could drift apart.  (They also each cost about a second per 20k SKUs, and the\ncoverage fixed point re-plans a 400k-SKU catalogue every round.)\n\nThe run-length flag is tested POSITIVELY (`isinstance(u, (Singleton, FulfillmentBin))`)\nand never as `not isinstance(u, Pallet)`: both of those classes SUBCLASS Pallet, so the\nnegative test calls every singleton a pallet \u2014 which repacks a remainder as a partial\npallet and charges a bucket the warehouse was not sized for.",
      "is_async": false,
      "kind": "function",
      "signature": "(orders: list[Order]) -> tuple[dict, dict]"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.bucket_requirements": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Exact bin count per (handling, category, storage_size, unit_type)\nbucket, computed by running each order through viable_storage_units at\nits equilibrium_qty.  This is the authoritative per-tier demand \u2014 and,\nsince \"Field the requirement\", also the packing the run fields.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, orders: list[Order]) -> dict[BinKey, int]"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.declared_packing": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Both halves of `_packing` in one pass, for a caller that needs the requirement AND\nthe per-order slots -- `({bucket: bins}, {id(order): slots})` -- and will hand the\nslots to `field_requirement` rather than pay for a second packing.  The era's\ncoverage loop stamps every SKU's plan BEFORE it plans the warehouse, because the\nfragmentation chain the derived headroom reads packs a lot by that plan\n(`era_coverage.derived_holds`).",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, orders: list[Order]) -> tuple[dict, dict]"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.field_requirement": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Field every order at exactly its declared level, packed exactly as\n`bucket_requirements` counted it.  Returns (fielded_orders, their sku ids).\n\n`slots` is the packing half of `_packing`, keyed by `id(order)`.  `plan_warehouse`\npasses the one it already computed to size the buckets, which is what makes the two\nhalves the same statement rather than two passes that agree by luck; omit it and this\ncomputes its own.\n\nTHE PACKING IS THE REQUIREMENT.  `bucket_requirements` states what the declared\nlevels need by running each order through `viable_storage_units` at its\norder-up-to; this runs the same call and RECORDS the answer as the order's\n`stock_plan`, so the bins the warehouse was sized for are the bins the run fields,\nand every later reorder rebuilds the same tier mix.  The two agree by construction\nrather than by margin.\n\nWHAT THIS REPLACED, and why (department-calibration, \"Field the floor\", decision 2).\nThis used to be `sample_to_capacity`: it sized bin capacity independently, then held\na contest for it \u2014 phase 1 round-robined every SKU into its EMPTIEST reachable\nbucket until it reached its order-up-to, phase 2 shared out whatever was left, and\nthe order was re-declared at whatever it won.  Both halves broke the floor.  The\nemptiest-bucket rule charged multi-tier SKUs at low density to buckets budgeted for\nothers, so single-tier SKUs stopped two to six units short of their own line floor \u2014\n25,241 of them (15.8%) on the reference pair even with every tier's budget above its\nrequirement.  Phase 2 then grew 64,989 other SKUs above their declaration, so a\nlevel the run recorded as declared was not the level it fielded.  A shelf below the\nline is a treadmill under base stock, and a level grown past the declaration is a\ncoverage nobody chose.\n\nSo there is no contest left to hold: no re-choice of tier, no growth into leftover\ncapacity, no shrink to fit.  Every SKU fits because `plan_warehouse` sized its\nbuckets from this same statement and REFUSED the run if it could not.  Nothing here\nis random \u2014 the old `rng` seeded the contest, and the contest is gone.\n\n`Order.declare_stock` is the one mutation site for the four level slots (ADR-0002);\nthe order-up-to, the reorder point and the pipeline stamp pass through unchanged,\nbecause the planner no longer has an opinion about them.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, orders: list[Order], slots: dict | None=None) -> tuple[list[Order], set]"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Size a warehouse to hold *orders* at their DECLARED levels, and field them there.\n\n1. Enumerate the full bucket set (every handling\u00d7category gets 4 pallet\n   size tiers + 1 singleton) \u2014 the structural floor that guarantees\n   every SKU has a place.\n2. Replica per bucket either from the requirement (default,\n   max(1, ceil(requirement/(eff\u00b7fill)))) or from an explicit\n   *composition* basis vector (bins \u221d weight).\n3. Scale up to >= min_bins, then down to <= max_bins/max_aisles (never\n   below 1 replica/bucket; min_bins wins if it conflicts with max).\n4. Check the promise on the EMITTED capacity \u2014 `capacity \u00b7 fill >= requirement`\n   per bucket \u2014 and REFUSE (`UnfieldableRequirement`) when a bucket is short.\n5. Field every SKU at exactly its declaration, packed as `bucket_requirements`\n   counted it (`field_requirement`).  Skipped when sample=False, which wants only\n   the warehouse SHAPE; the sizing and the promise check still run, because both\n   read the declaration rather than the fielding.\n\nPlanning is DETERMINISTIC: there is no sampling left to seed.  A caller that used to\npass `rng` was seeding a SKU contest for capacity that no longer happens.\n\nbucket_hold: optional `{bucket: bins}` -- the bins a bucket must HOLD, which under\nthe calibrated era is its requirement plus the stationary fragmentation base stock\ncreates, never less than the declared minimum headroom leaves\n(`era_coverage.derive_fill`; \"Derive the fill headroom from the fragmentation\").\nA bucket named here is sized at `max(1, ceil(hold / eff))` replicas (the aisle-split\ninflation still applies) INSTEAD of the fill rule, and step 4 checks\n`capacity >= hold` for it, so the refusal names the extra bins short and not only\nthe requirement.  Its recorded `fill` is `requirement / hold` -- the share the\ndeclaration occupies of what the bucket was sized for.  A bucket not named keeps\nthe fill rule; None (the default, and every flag-off run) is the fill rule for all\nof them, byte for byte.\n\ncomposition: optional factored basis vector of *bin* ratios \u2014 a dict with\nany of the keys 'handling', 'category', 'size', 'unit', each mapping a\nvalue to a relative weight (missing values default to 1.0).  The per-bucket\nweight is the product of the matching dimension weights; bins are allocated\nproportionally.  Total scale comes from min_bins (or demand if min_bins is\nunset).  Example:\n    {'unit': {'pallet': 0.7, 'singleton': 0.3},\n     'size': {'small': 0.1, 'medium': 0.2, 'large': 0.3, 'extra_large': 0.4}}\n\nregime_sizing: optional PER-REGIME sizing that overrides the single\nmin_bins/max_bins/max_aisles/target_fill/composition scalars \u2014 a dict\n{'store': {...}, 'fulfillment': {...}} where each sub-dict may carry\n'min_bins', 'max_bins', 'max_aisles', 'fill', 'composition', and the two\naisle-shape knobs ('depth_classes', 'aisle_split').  When None (the\ndefault), the warehouse is sized globally.  When given, store and fulfillment are\nsized and capped INDEPENDENTLY, each with its own fill headroom.\n\nBoth regimes are sized from the requirement.  Fulfillment's fixed tier distribution\n('mode', 'distribution', 'target_bins') is RETIRED: it spread a demand-derived bin\ntotal 0.5/0.3/0.2 across the three tiers while the declared levels needed 11/63/26,\nwhich is how ff_medium came to be short by 293k bins on a section that was, in total,\nthe right size (\"Field the floor\", decision 8).  Depth classes and the aisle split\nsurvive \u2014 they reshape aisles, not the bin count.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, orders: list[Order], *, categories: list[str], handlings: list[str], aisle_width: int, aisle_height: int, ff_aisle_width: int | None=None, ff_aisle_height: int | None=None, target_fill: float=DEFAULT_TARGET_FILL, min_bins: int | None=None, max_bins: int | None=None, max_aisles: int | None=None, composition: dict | None=None, regime_sizing: dict | None=None, sample: bool=True, log: Any=None, bucket_hold: dict | None=None) -> 'WarehousePlan'"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse._eff": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bucket: tuple) -> int"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse._emit": {
      "decorators": [],
      "doc": "Emit `n_aisles` AisleConfigs of width a_w \u2014 OR, when `split` is set, cut each into k\nshorter segments (throughway construction) via _aisle_split.  Returns bins emitted.\nsplit=None/{'k':1} \u21d2 emits exactly `n_aisles` at a_w \u21d2 byte-identical.",
      "is_async": false,
      "kind": "function",
      "signature": "(h, cat, unit_type, a_w, a_h, sizes_arg, bin_w, bin_hs, n_aisles, eff_at_aw, eff_fn, bin_width, split) -> int"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse._fill_eff": {
      "decorators": [],
      "doc": "The bucket's declared fill, divided down by the bins its aisle split sacrifices\n\u2014 so sizing asks for the replicas that leave the requirement covered AFTER the cut.",
      "is_async": false,
      "kind": "function",
      "signature": "(bucket: tuple) -> float"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse._line": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(b, need, cap, bud)"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse._size_fill": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bucket: tuple) -> float"
    },
    "Warehouse/inventory/inventory_planning.py::PlanningMixin.plan_warehouse._split_for": {
      "decorators": [],
      "doc": "(aisle_split cfg, aisle width, bin width) for a bucket \u2014 what `_split_inflation`\nneeds to know whether this bucket's aisles get cut, and by how much.",
      "is_async": false,
      "kind": "function",
      "signature": "(bucket: tuple)"
    },
    "Warehouse/inventory/inventory_planning.py::_aisle_split": {
      "decorators": [],
      "doc": "Cut an aisle of width `a_w` (holding `target_bins` bins for its bucket) into `k` shorter\nsegments of width ~a_w/k, modeling cross-aisle throughways that consume `capacity_loss` of the\nbins.  Under one-way lanes, shorter aisles \u21d2 less x-traversal.\n\nReturns (seg_w, n_segments, seg_eff, cap_total) or None (no split \u21d2 byte-identical caller path).\n`eff_fn(width) -> bins-per-segment` (uniform_aisle_bins or catalog_aisle_bins bound to the\ntier).  k is bounded to \u2264 a_w // bin_width (\u22651 column/segment) and \u2264 MAX_AISLE_SPLIT_K so the\nemitted aisle count cannot blow up.  Total usable bins \u2248 target_bins\u00b7(1-capacity_loss): the\nbins in the throughways are genuinely sacrificed HERE.\n\nWHAT THE SPLIT NOW MEANS, which is not what it used to.  The loss no longer reaches the\ninventory: `_split_inflation` adds the compensatory aisles at SIZING time, so a split\nbucket is asked for `1/(1-loss)` times the replicas and the run still holds its declared\nlevels (\"Field the floor\", decision 9 \u2014 the line floor is a promise, and a throughway is\nnot a reason to break it).  A split arm therefore trades AISLES for travel, not capacity\nfor travel; it used to raise fill by shrinking the shelf under a fixed stock, and an\narchived `ks` sweep measured that older trade, so its results are not comparable to a\nnew one.",
      "is_async": false,
      "kind": "function",
      "signature": "(a_w: int, target_bins: int, eff_fn, bin_width: int, k, capacity_loss)"
    },
    "Warehouse/inventory/inventory_planning.py::_apply_caps": {
      "decorators": [],
      "doc": "Scale `replicas` for `buckets` UP to >= min_bins, then DOWN to <= max_bins/max_aisles\n(never below 1 replica/bucket; min_bins wins on conflict), mutating `replicas` in place.\nThis is the original global steps 3a/3b, scoped to one bucket set so it can run per regime.",
      "is_async": false,
      "kind": "function",
      "signature": "(buckets, replicas, eff_fn, min_bins, max_bins, max_aisles, log=None)"
    },
    "Warehouse/inventory/inventory_planning.py::_comp_weight": {
      "decorators": [],
      "doc": "Factored basis-vector weight for a bucket: product of the matching dimension weights\n(each dimension defaults to 1.0 when unspecified).",
      "is_async": false,
      "kind": "function",
      "signature": "(bucket: tuple, comp: dict) -> float"
    },
    "Warehouse/inventory/inventory_planning.py::_demand_replicas": {
      "decorators": [],
      "doc": "Demand-driven replicas: max(1, ceil(bucket_requirement / (bins_per_aisle \u00b7 fill))).\n\n`fill_fn(bucket)` is the EFFECTIVE fill \u2014 the declared `store_fill`/`ff_fill` already\ndivided down by an aisle split's capacity loss (`_split_inflation`), so a split's\nsacrificed bins are paid for in replicas here rather than discovered as a shortfall at\nthe promise check.",
      "is_async": false,
      "kind": "function",
      "signature": "(buckets, req, eff_fn, fill_fn) -> dict"
    },
    "Warehouse/inventory/inventory_planning.py::_demand_total": {
      "decorators": [],
      "doc": "Total bins the demand-driven replicas would occupy (the default scale for ratio modes).",
      "is_async": false,
      "kind": "function",
      "signature": "(buckets, req, eff_fn, fill_fn) -> int"
    },
    "Warehouse/inventory/inventory_planning.py::_ff_depth_split": {
      "decorators": [],
      "doc": "Split one fulfillment size tier's target bins across DEPTH classes (shallow\u2026deep aisles\nthat share the same BinKey), so a placement wave can route hot SKUs to shallow aisles.\n\nEach class is {'columns': n, 'share': w}; its aisle_width = n\u00b7FULFILLMENT_BIN_WIDTH and it\nholds ~ (share) of `target_bins` worth of bins.  Returns (classes, total_bins) where\nclasses = [(aisle_width, n_aisles, eff_per_aisle), \u2026].  Preserves total bins (rounding UP)\nwhile growing the aisle count; guarantees \u22651 aisle per positive-share class so no tier is\ndropped.  Classes with columns < 1 bin width or non-positive share are skipped.\n\nThe aisle count rounds UP for the same reason `_aisle_split`'s does: the bins a bucket was\nsized for are the bins its declared levels need, and `_demand_replicas` leaves only the\n~3% slack of one `ceil`.  Rounding each class down spends that slack on nothing \u2014 measured\nat 85 to 295 bins short on a single depth class over a 4,000-SKU catalogue, which is a\nREFUSED run.  Over-emitting is free bins; under-emitting is a run that will not start.",
      "is_async": false,
      "kind": "function",
      "signature": "(size: str, target_bins: int, depth_classes: list, ff_h: int)"
    },
    "Warehouse/inventory/inventory_planning.py::_ratio_replicas": {
      "decorators": [],
      "doc": "Replicas allocated proportionally to weight_fn(bucket), scaled to ~target_total bins.\nUsed by the store's composition basis vector -- the only caller left since fulfillment's\nfixed tier distribution was retired.",
      "is_async": false,
      "kind": "function",
      "signature": "(buckets, weight_fn, eff_fn, target_total: float) -> dict"
    },
    "Warehouse/inventory/inventory_planning.py::_split_inflation": {
      "decorators": [],
      "doc": "`1 / (1 - capacity_loss)` when an aisle split would actually cut this bucket's aisles,\nelse 1.0 \u2014 the factor its demand replicas inflate by so the bins the split SACRIFICES do\nnot come out of the declared levels (\"Field the floor\", decision 9).\n\nMirrors `_aisle_split`'s own admission test (k > 1 after bounding by columns-per-segment\nand MAX_AISLE_SPLIT_K, loss < 1); a split that would not apply inflates nothing, so a\nconfiguration the emitter ignores never over-sizes the warehouse either.",
      "is_async": false,
      "kind": "function",
      "signature": "(split, a_w: int, bin_width: int) -> float"
    },
    "Warehouse/inventory/inventory_planning.py::structural_bin_floor": {
      "decorators": [],
      "doc": "(aisles, bins) the STORE cannot go below, for any catalogue: one aisle per bucket.\n\nBuckets are the handling x category x tier cross-product, so this floor is a property of\nthe CONFIGURATION, not of the SKUs \u2014 capping SKUs does not lower it.  `_apply_caps` never\ntrims a bucket below one replica (every SKU must be placeable), so a `--s-max-bins` under\nthis number is unachievable by construction.  Exposed so a CLI can say so in milliseconds\ninstead of mid-build, per pair, after the inventory load.",
      "is_async": false,
      "kind": "function",
      "signature": "(handlings, categories, aisle_width, aisle_height) -> tuple[int, int]"
    },
    "Warehouse/inventory/inventory_reorder.py": {
      "decorators": [],
      "doc": "inventory_reorder.py \u2014 churn counters, reload primitive, pick notifications, and\nOrder-Up-To reorder logic.\n\n`ReorderMixin` holds the per-batch lifecycle methods.  Mixed into Inventory_Manager so\nthe public API (`pop_churn`, `requeue_bin`, `check_reorders`, and the O(1) pick\nnotifications called by PickSimulation) is unchanged.  All instance state and the\ncollaborators it calls (`self._index_add`, `self._stock`) are provided by\nInventory_Manager.__init__ / its placement methods.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit": {
      "decorators": [],
      "doc": "The order port's flag-off transit: the legacy lead queue as an object.\n\nEntries are the same mutable ``[sku, qty, remaining_lead]`` records they always were,\nticking one BATCH per ``advance()`` \u2014 the unit note further down this module is why that is\na decision.  This is the DEFAULT the manager constructs for itself; the trailer\npipeline binds its own transit (absolute-clock leads, yard, doors) on the same\nseam, flag-on \u2014 injection, never import, like ``packer`` above.\n\nThe manager keeps the SCALAR ledger (``_deferred_qty`` credits and debits) either way:\ntransit owns TIMING, the ledger owns POSITION, and that split is what let the dock land\nwith zero reorder-ledger edits.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.advance": {
      "decorators": [],
      "doc": "One batch elapsed: every in-transit order moves one tick closer.\n\nThe tick is one BATCH, not one second \u2014 the module's unit note is the decision.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.depth": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.dispatch": {
      "decorators": [],
      "doc": "Accept one fired reorder from the order port.  `unit_volume` and `now_s`\nare the trailer transit's business, carried on the seam so the two transits\nare indistinguishable to the phase bodies; the batch countdown ignores both.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, lead: int, unit_volume: int | None=None, now_s: float | None=None) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.merchandise": {
      "decorators": [],
      "doc": "Total pieces in transit \u2014 the in_transit_qty level.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.release": {
      "decorators": [],
      "doc": "Pop and return every arrived entry (remaining_lead <= 0), in queue order.\n\nUn-arrived entries keep their order; a negative remainder is an order that arrived\nwhile nothing drained it \u2014 never clamped, because clamping would hide the backlog.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, now_s: float | None=None) -> list[list]"
    },
    "Warehouse/inventory/inventory_reorder.py::BatchTransit.snapshot": {
      "decorators": [],
      "doc": "(sku, qty, remaining_lead) tuples for the replay/reorder_queue rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._advance_lead_queue": {
      "decorators": [],
      "doc": "One batch elapsed: decrement pre-existing in-transit orders only.\n\nRuns BEFORE `_fire_reorders`, which is what stops an order fired this batch from\nbeing decremented in the same batch it was placed.\n\nThe tick is one BATCH, not one second -- see `LEAD_TIME_UNIT` above for why that\nis a decision and what would change it.  The tick itself lives on the TRANSIT\nobject (`BatchTransit.advance`): this phase is the wrapper the phase ratchet pins,\nand the trailer transit replaces the body's target, never this call site.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._apply_picks_batch": {
      "decorators": [],
      "doc": "Apply all pick notifications accumulated during one simulation run.\n\nAggregates quantity by SKU before calling _notify_pick so the body\nexecutes once per unique SKU rather than once per pick event,\ncutting ~430k individual function calls down to ~5k.\n\n`empties` is `(bin, when)` from `PickSimulation`.  Bare bins are still accepted\nbecause this is a public-ish notification hook and a caller with no clock is a\nlegitimate one \u2014 the stamp is metadata, not a precondition.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, picks: list[tuple[int, int]], empties: 'list[tuple[Aisle.Bin, float]] | list[Aisle.Bin]') -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._drop_sku_from_aisle": {
      "decorators": [],
      "doc": "Remove one bin's contribution to the per-SKU aisle state (affinity on).\n\nBoth halves now live in `AisleLedger` (Warehouse/inventory/aisle_ledger.py):\n`drop_bin` retires the bin's column position, `drop_sku` decrements the count\nand \u2014 on the SKU's LAST bin \u2014 retires it from the sku/idx sets and subtracts\nits lift/demand/pick-load/cart-volume levels.\n\nThere used to be an INLINE TWIN of this body in `_reclaim_empty_bins`, with a\ndocstring on each asking a human to KEEP THE TWO IN SYNC.  They were not in\nsync: `vol_sum` was subtracted by neither and `lift_sum` by both while only a\nproduction-dead family added to it.  One body now serves both callers, and the\ndifference that actually mattered between them \u2014 the hot twin's bare `else`\ntreats a defensive n == 0 like n == 1, this cold path's `elif n == 1` does not \u2014\nis the explicit `last_when_zero` flag rather than a remark in prose.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, bin_: 'Aisle.Bin') -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._fire_reorders": {
      "decorators": [],
      "doc": "Fire Order-Up-To reorders for depleted SKUs into the lead queue.\n\nReturns the SKUs triggered and sets `_units_ordered` for the batch.  Inventory\nPOSITION = on_hand + queued (stock queue) + deferred (lead queue), so a SKU with an\norder already in flight is not reordered again.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[int]"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._notify_bin_emptied": {
      "decorators": [],
      "doc": "Queue an emptied bin for reclaim at the next check_reorders call.\n\nCalled by PickSimulation immediately after bin_.storage is set to\nNone \u2014 must be O(1).  The bin stays in _unavailable until\n_reclaim_empty_bins processes _pending_reclaim.\n\n``at`` is the ABSOLUTE second the bin ran dry -- the pick sim's carried clock, on\nthe same axis as every other event stamp.  Recorded in `_emptied_at` and read at\nexactly one place: the space timeline's reclaim-harvest in `_reclaim_empty_bins`,\nthe one moment the stamps and the bins meet before both are wiped (pinned by\n`Tests/unit/test_bin_empty_timing.py`).  The reclaim itself still happens once,\nat the top of the next batch, so a slot freed mid-batch is invisible to PLACEMENT\nuntil then -- what the stamp feeds is the standing dock's space view, not the\nreclaim cadence.\n``None`` from a caller that has no clock (a test, the legacy notification path).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_: Aisle.Bin, at: float | None=None) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._notify_pick": {
      "decorators": [],
      "doc": "Decrement the incremental quantity counter and flag the SKU if it\ncrosses its reorder_point.\n\nCalled by PickSimulation after each pick event \u2014 must be O(1).\nAdds the SKU to _depleted_skus so check_reorders only iterates SKUs\nthat actually need attention rather than all N_skus.\n\nThe depletion flag compares reorder_point against the SKU's INVENTORY\nPOSITION (on-hand in bins + on-order queued + on-order deferred), not\non-hand alone.  A SKU that has already been reordered but whose units are\nstill waiting for a bin is therefore not flagged again \u2014 preventing\nduplicate reorders every batch for unbinned items.\n\nTWO ABSENCES, TWO ANSWERS, and they used to be one test.  A SKU with no TEMPLATE is\ntolerated; a template with no DECLARED level raises.  See the guards below \u2014 the\ndifference is the whole reason this method is not three lines.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._receive": {
      "decorators": [],
      "doc": "Unload what is standing on the dock, until the crew runs out of day.\n\nReturns immediately when there is no receiving crew, which is every run that does not\nask for one -- the dock is not constructed at all, so this is one `is None` test and\nnot a flag.\n\nWHERE THIS SITS IS THE DESIGN. Above it, steps 0-3 are the CALENDAR: a lead time\nelapses whether or not anyone is at work, and a trailer that arrives at four o'clock\nhas still arrived. Below it, `drain_putaway` is the put crew's labour. Receiving is\nlabour too, and it must run BEFORE the put drain, or a unit unloaded at nine in the\nmorning would wait a whole batch for a bin -- a latency that would be an artifact of\nwhere the hook sits rather than anything about a warehouse.\n\nTHE WHISTLE IS A START GATE, exactly as put-away's is: a receiver already past\n`deadline` begins nothing new, and the unload in progress when it blows finishes. So\novertime is bounded by one unload per worker, which is what someone carrying a pallet\noff a tail lift actually does. `deadline` is a REMAINDER on the crew's batch-local\nclock, not an instant -- see `crew_clock.can_start`.\n\nWhat the whistle stops stays on the dock and is the first thing tomorrow's crew\ntouches: the deque is in arrival order, so the rollover is FIFO and no unit can be\novertaken by merchandise that arrived after it.\n\nTHE STANDING YARD REROUTES THIS PHASE WHOLE.  When the bound transit carries the\nstanding surfaces (`STANDING` \u2014 `YardTransit`; the other transits satisfy them\ntrivially by not having them), every door and crew decision happens here instead:\nplans-at-arrival, the whistle-independent door fill, the budget-gated unload off\nstaged trailers, mid-drain refills, and the canonical handoff.  The dock-floor\ndeque below stays EMPTY in that mode \u2014 the standing buffer is the trailer itself.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, arrivals=(), deadline: float | None=None) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._reclaim_empty_bins": {
      "decorators": [],
      "doc": "Return bins in _pending_reclaim to the available index.\n\nWith _unavailable as a dict and _pending_reclaim as a targeted list,\nthis is O(pending_bins) \u2014 typically a handful per batch \u2014 instead of\nthe previous O(total_bins) full scan.  Attribute refs are hoisted\noutside the loop to avoid repeated self. lookups across ~7k iterations.\n\nThe per-SKU aisle teardown inside the loop is the hoisted INLINE TWIN of\n_drop_sku_from_aisle (the canonical cold-path version used by requeue_bin)\n\u2014 KEEP THE TWO IN SYNC when editing either.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._release_arrivals": {
      "decorators": [],
      "doc": "Release arrived orders (remaining_lead \u2264 0) into the stock queue.\n\nCatches both decremented-to-0 olds AND fresh lead-0 newcomers in the same batch,\nwhich is why it runs after `_fire_reorders` rather than before.\n\nReturns this batch's `LoadPlan`s -- what came off the trucks, in arrival order.  The\nphase is a producer with no consumer today; the return exists so a receiving crew is\na caller of an existing phase rather than a rewrite of one.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._release_to_stock": {
      "decorators": [],
      "doc": "Convert an arrived (sku, qty) order into storage units and append them to the\nstock queue, updating the queued-unit / queued-qty trackers.  Shared by every\nlead-queue arrival (including lead-0 orders released the same batch).\n\nRETURNS the `LoadPlan`s it built, rather than stashing them.  An earlier version kept\nthem on the manager behind a `drain_inbound()` -- which was write-only state with an\nextra method, since nothing ever drained it, and it pinned a tuple of live\n`StorageUnit`s plus a cloned `Order` for every arrival of the whole run.  There is\nexactly one caller, so the return value is the honest shape: nothing to leak, and a\nreceiving crew takes the plans from here rather than from a buffer it must remember\nto empty.\n\nTHE INBOUND SEAM.  Packing has always been a function of how much arrives AT ONCE --\n`viable_storage_units` takes a quantity -- but nothing named that, so nothing could\nexpress an order that would be palletized arriving whole and lands as two singleton\npacks when a trailer splits it.  `deliveries` is that: a list of quantities summing\nto `qty`, each packed on its own.  None means one delivery of the whole amount,\nwhich is what every caller does today and is byte-identical.\n\nThe split is the CALLER's to decide.  Trailers, docks and load planning belong\nupstream of here; this only says that when a shipment arrives in pieces, each piece\npacks as the piece it is.  When the driver binds `mgr.packer`\n(`Inbound.pack.packer`), the plans are `LoadPlan`s carrying the counterfactual, so\nthe cost of the split is answerable (`Inbound.pack.shipment_penalty`); unbound, the\ndefault packs the identical unit stream and records nothing.\n\nA caller that does not want to thread `deliveries` through every arrival sets\n`inbound_split` instead -- consulted here, so the split is reachable from the\nordinary lead-queue release without `_release_arrivals` knowing about it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, deliveries=None) -> list"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin._tick_batch": {
      "decorators": [],
      "doc": "Advance the replenishment calendar by one batch.\n\nThis is the simulation's only calendar: lead time is denominated in BATCHES, not\nseconds, and `_batch_num` is also the per-reorder RNG key, so calling this twice\nwould silently redraw every reorder quantity.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.accept": {
      "decorators": [],
      "doc": "The canonical handoff for ONE unloaded unit: ledger flip, labour, queue.\n\n`position = on_hand + queued + deferred` never wobbles, because the three ledger\nmoves and the queue entry happen together here.\n\nTakes `dur` and not the whole `(t0, dur, w)` record: `t0` and `w` are fields of\nthe DOCK's row, and the dock is the coordinator's \u2014 handing them to a leaf would\nbe two dead parameters plus an invitation to write the row twice.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, item, dur: float) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.check_reorders": {
      "decorators": [],
      "doc": "Order-Up-To replenishment through an explicit, deterministic lead queue.\n\nEvery reorder enters `_lead_queue` as a [sku, qty, remaining_lead] record \u2014 even\nlead 0.  Per call (one completed batch), in this order and no other:\n  0. Advance the batch calendar and reclaim bins the picks emptied.\n  1. Advance every pre-existing in-transit order: remaining_lead -= 1.\n  2. Fire OUP reorders for depleted SKUs \u2192 append new records (remaining_lead =\n     round(lead_time_mean) \u2265 0); reorder qty ~ Normal(ideal, ideal\u00b7supply_cv)\n     centred on the equilibrium fill (supply_cv set at generation).\n  3. Release every arrived order (remaining_lead \u2264 0) into the stock queue \u2014 this\n     catches both decremented-to-0 olds AND fresh lead-0 newcomers (same batch).\n  4. Unload the dock, if there is a receiving crew, until its day runs out.\n  5. Place the stock queue into bins via _stock().\n\nInventory POSITION = on_hand + queued (stock queue) + deferred (lead queue), so a\nSKU with an order already in flight is not reordered again.\n\n`put_deadline` and `recv_deadline` are the two crews' whistles, each in seconds on\nits OWN batch-local clock, and each reaching exactly one phase.  They are separate\nparameters rather than one shared day because the crews are separate: handing\nreceiving the put crew's remaining day would be arithmetically well-formed and wrong\nby an unrelated crew's overrun, and a cut count of zero reads as \"the boundary cost\nnothing\".\n\nNeither reaches anything above step 4 on purpose: steps 0-3 are the CALENDAR\nadvancing \u2014 a lead time elapses whether or not anyone is at work, and a trailer that\narrives at four o'clock has still arrived.  What a day bounds is the LABOUR, and the\nlabour is steps 4 and 5.\n\nTHE ORDER IS THE BEHAVIOUR.  Each step above is a method so a future caller can\ndrive them separately, but reordering them changes results: firing before the lead\ntick would decrement an order in the batch it was placed, and releasing before\nfiring would delay every lead-0 arrival by a batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, put_deadline: float | None=None, recv_deadline: float | None=None, now_s: float | None=None) -> list[int]"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.drain_putaway": {
      "decorators": [],
      "doc": "Place the stock queue into bins (retries prior-batch stragglers too).\n\n`deadline` is the day's whistle on the put crews' batch-local clocks; what the\nwhistle stops stays queued and is drained by the next batch, which is the put side\nof \"work that does not complete rolls over to the next shift\".\n\nPUBLIC, like `reclaim_emptied_bins` and for the same reason: a second work stream\ndrives this phase without replaying the five above it.  The site put pool\n(`Inbound/putaway_pool.py`) calls it TWICE in one site day \u2014 once at this leaf's\nshare of the day, once at the whole of it \u2014 which is what `charge_cut=False` is\nfor: `cut` is a LEVEL, so each drain charging it would inflate it inside one batch\nwhere no downstream rule can undo it.  The pool charges once itself, through\n`count_put_cut`.  True is every other caller.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, deadline: float | None=None, charge_cut: bool=True) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.owned_skus": {
      "decorators": [],
      "doc": "The catalogue partition THIS leaf is responsible for, as a view of its keys.\n\nThe third port, and the only one the coordinator reads outside a drain: it builds\nits `{sku: leaf}` owner dict from every bound leaf's set ONCE, at bind time, and\nrefuses an overlap there (`Inbound/receiving.py`).  Public rather than reaching\n`_originals` across the package boundary \u2014 a cross-package caller that touched a\nprivate would be a boundary violation dressed as an underscore.\n\n`_originals` is the templates the manager was loaded with, which IS the partition:\na channel leaf loads its own regime-filtered inventory, so this set is that filter's\nresult and the two leaves' sets are disjoint by construction.\n\nIT IS FILLED BY INTAKE (`enqueue` / `enqueue_all` / `place_optimal`), which the\ndriver runs to completion BEFORE the batch loop \u2014 not by anything a drain does. So\nthe set is stable for the life of a coupled run, and the bind-time read is a read\nof the whole partition rather than of however much of it had arrived. A caller that\nbroke that would break loudly rather than quietly: a sku the dict never learned\nabout refuses at `_owner_of` instead of being delivered somewhere plausible.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.plan_lot": {
      "decorators": [],
      "doc": "Plans-at-arrival for ONE contiguous lot: `(plans, items)`.\n\nReturns both halves because both are consumed and neither derives the other: the\n`LoadPlan`s become `trailer.plans` and the dock's arrival notice, while the\nstamped items become `trailer.pending`.  A plan holds several units, so the\ngrouping is not recoverable from the items alone.\n\n`source` rides in from the transit (`transit.SOURCE`) rather than being read off\n`self.transit`, because the coordinator's transit is the site's and a leaf's may\nbe unbound entirely once there are two.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, sku: int, qty: int, source: str) -> tuple"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.pop_churn": {
      "decorators": [],
      "doc": "Return (reload_moves, reorder_placements) since the last call and reset.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[int, int]"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.reclaim_emptied_bins": {
      "decorators": [],
      "doc": "Return bins the pick loop emptied to the free index.\n\nPublic because the pick side FILLS `_pending_reclaim` (`_notify_bin_emptied`) while\nthe reorder side DRAINS it, and the drain currently happens once, at the top of the\nnext batch \u2014 so a slot freed mid-batch is invisible until then.  Naming it is the\nfirst step to letting that change.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::ReorderMixin.requeue_bin": {
      "decorators": [],
      "doc": "Evict a placed unit back into the reorder queue and reclaim its bin \u2014\nthe inverse of _execute_placement.\n\nThe unit's quantity moves from on-hand to on-order (queued), so inventory\nPOSITION is unchanged (no spurious reorder fires).  The freed bin returns to\nthe available index and the ranked drain re-places the queued unit in its\nproper priority slot.  Bumps the reload churn counter.  No-op on an empty bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_: 'Aisle.Bin') -> None"
    },
    "Warehouse/inventory/inventory_reorder.py::_PlainDelivery": {
      "decorators": [],
      "doc": "The default packer's per-delivery record: the unit stream and nothing else.\n\nWhat `_release_to_stock` observes from a plan when no packer is bound is exactly\n`.units` (a dock that would count the plans only exists when the driver also binds the\nrich packer) \u2014 so this is the whole flag-off contract, and the unit stream is\nbyte-identical to `Inbound.pack.receive`'s by construction: both call\n`viable_storage_units` once per delivery quantity.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_reorder.py::_PlainDelivery.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, units)"
    },
    "Warehouse/inventory/inventory_reorder.py::_pack_plain": {
      "decorators": [],
      "doc": "Pack an arrival with no Inbound package in sight: same units, no LoadPlan records.",
      "is_async": false,
      "kind": "function",
      "signature": "(order, quantity: int, deliveries=None) -> list"
    },
    "Warehouse/inventory/inventory_reorder.py::_undeclared_stock": {
      "decorators": [],
      "doc": "Raise `UndeclaredStock` for an order no run has declared a stock level on.\n\nThe `inventory_common._equilibrium_qty` precedent, at the other end of the same\ndeclaration and for the same reason.  A RAISER rather than a `_reorder_point(order)`\naccessor on purpose: `_notify_pick` is the pick loop's O(1) hook -- once per pick\nmutation, hundreds of thousands of times a batch -- and both call sites already had the\n`is None` test they need.  Written this way the guard costs the fast path nothing at all,\nso no future profile can argue it back out of existence.",
      "is_async": false,
      "kind": "function",
      "signature": "(order) -> NoReturn"
    },
    "Warehouse/inventory/inventory_zoning.py": {
      "decorators": [],
      "doc": "inventory_zoning.py \u2014 velocity zoning: the per-regime \"like-with-like\" candidate layer.\n\n`ZoningMixin` holds the whole cluster: the band MAPS (`_sku_band`, `_aisle_band`), the\nper-band free-bin sub-index (`_band_index`, `_band_pos`), the spill order, the two filters\nand the ranked-wave group key.  Mixed into `Inventory_Manager` beside `PlanningMixin`,\n`OptimalLayoutMixin` and `ReorderMixin`, so the public API (`configure_zoning`) and every\ninternal caller are unchanged.\n\nOFF is the identity: `configure_zoning(False)` clears every map, `_group_key` falls back to\nthe bare `BinKey`, and `_candidates` never calls `_band_pick` -- so a run with zoning off is\nbyte-identical to one from before zoning existed.\n\n# \u2500\u2500 why a mixin and not a collaborator object \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`Inventory_Manager._index_add` / `._index_remove` mirror EVERY bin mutation into the\nper-band sub-index, and they are the hottest path in the system.  A mixin method resolves\nexactly as a method defined on the class does; a `self._zoning.` hop would add an attribute\nlookup per bin moved, for no reader's benefit.  So the two mirror sites stay inline on the\nmanager, and `Tests/unit/test_velocity_zoning.py` asserts they stay there.\n\nAll instance state is provided by `Inventory_Manager.__init__` -- this module constructs no\nmanager and imports none, which is what keeps the split a split rather than a cycle.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._band_of_unit": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit) -> int"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._band_pick": {
      "decorators": [],
      "doc": "O(#bands) equivalent of _zone_filter via the maintained per-band sub-index: the first\nnon-empty band bucket in spill order.  `key` is the tier BinKey from _candidates_raw; the\ncaller guarantees the tier is non-empty, so some bucket is non-empty (the fallback to the\nraw tier list is unreachable defensive cover).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, key) -> list"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._build_abc_bands": {
      "decorators": [],
      "doc": "Manual A/B/C bands: SKUs cut at cumulative demand-mass thresholds; aisles allocated per\nBinKey by each band's SKU footprint (few hot SKUs \u21d2 few hot aisles).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, orders: Any, n: int, abc: dict | None) -> None"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._build_band_index": {
      "decorators": [],
      "doc": "Partition each free-bin tier list into per-band buckets, once, after the band maps are\nset.  Anchors the O(#bands) `_band_pick` fast path; maintained incrementally thereafter by\n`_index_add`/`_index_remove`.  Re-derives `_band_pos` from scratch, so it is correct\nregardless of whether bins were registered before or after zoning was enabled.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._group_key": {
      "decorators": [],
      "doc": "Ranked-wave grouping key.  With zoning, sub-group by (BinKey, band) so each sub-wave\nis a single band (the once-per-wave candidate fetch is then band-correct); OFF \u21d2 BinKey\nonly (byte-identical).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit)"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._spill_bands": {
      "decorators": [],
      "doc": "Band visit order for zoning spill: the unit's band, then COLDER (downgrade to the next\nhotness), then HOTTER (upgrade) \u2014 the single source shared by _zone_filter and _band_pick.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, target: int)"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin._zone_filter": {
      "decorators": [],
      "doc": "Restrict *bins* to the unit's velocity band, spilling COLDER-first then HOTTER-fallback:\na hot item whose band is full drops to the next available (colder) aisle; a cold item whose\nband is full UPGRADES toward hotter aisles until it is placed.  Never returns empty.\n\nThe O(len(bins)) partition path used by the ranked wave (once per wave) and the direct unit\ntests; the per-unit path uses the O(#bands) _band_pick over the maintained sub-index.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit: StorageUnit, bins: list) -> list"
    },
    "Warehouse/inventory/inventory_zoning.py::ZoningMixin.configure_zoning": {
      "decorators": [],
      "doc": "Enable/disable velocity zoning and precompute the band maps (once, before stocking).\n\nmode='equal' (default): SKUs and aisles sliced into n_bands EQUAL-COUNT groups (hottest /\nshallowest = band 0).  mode='abc': SKUs banded by cumulative demand-MASS thresholds\n(abc['mass_thresholds'], manual A/B/C), and aisles allocated per BinKey PROPORTIONAL to each\nband's SKU footprint \u2014 so the hot A-band is a SMALL aisle fraction and the cold C-band is\nmany rarely-visited aisles (batches skip them).  Disabled \u21d2 maps cleared (byte-identical).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, enabled: bool, n_bands: int=DEFAULT_ZONING_BANDS, orders: Any=None, *, mode: str='equal', abc: dict | None=None) -> None"
    },
    "Warehouse/inventory/inventory_zoning.py::_apportion": {
      "decorators": [],
      "doc": "Allocate `m` aisles across `n` bands proportional to `weights`, with a floor of 1 per band\nwhen `m >= n` (so every band is reachable for the zone-filter spill).  When `m < n`, the first\n`m` (hottest) bands get 1 each and the rest 0 (spill covers the empties).  Largest-remainder.",
      "is_async": false,
      "kind": "function",
      "signature": "(m: int, weights: list, n: int) -> list"
    },
    "Warehouse/inventory/put_policy.py": {
      "decorators": [],
      "doc": "put_policy.py \u2014 how a put-away queue decides which waiting item to work next.\n\n# \u2500\u2500 two different questions \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nA placement pool answers \"which BIN for this unit\". Until the pool inversion it also answered\n\"which unit first\", by returning its assignments pre-sorted \u2014 and that conflation is what\nthis branch exists to undo, because put-away order is an operational decision and a scoring\nfunction is not the thing that should be making it.\n\n`PutQueueSpec.k_cap` narrows how far the pool may reach past the head of the queue. A put\nPOLICY is the other half: it replaces the pool's opinion with the floor's own. \"Put the\nheaviest away first\" and \"finish one SKU before starting the next\" are real rules that no\nplacement objective expresses, and they belong to the queue, not to the assignment function.\n\nThe two compose. A policy proposes an order; `k_cap` then bounds how far that order may\ndepart from arrival order. `fifo` with any `k_cap` is still FIFO; `largest_first` with\n`k_cap=3` is \"of the three oldest, take the biggest\".\n\n# \u2500\u2500 the contract \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nA policy is `(PutawayItem) -> comparable`, HIGHER served first, or None for `inherit` \u2014\nwhich means \"no opinion of my own, use the placement pool's precedence\", the pre-registry\nbehaviour and still the default. A tuple is fine and `sku_batched` uses one; the drain ranks\nby a stable sort rather than by arithmetic on the key, so nothing requires a number.\n\nKeys must be a pure function of the item and of state frozen for the batch. The drain\ncomputes each key ONCE and reuses it across every window the item appears in, so a key that\nchanged as placements happened would be read stale \u2014 and it would also make the window's\ncost quadratic, which is the reason the pool states a `sort_key` rather than sorting.\n\n# \u2500\u2500 what is NOT here yet \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nAnything that reads a clock. `Warehouse.kernel.timeline` now has the `WorkDay` such a rule\nwould need, but nothing schedules against it yet \u2014 a \"finish what fits before the whistle\"\npolicy lands here as one more registry entry once the runner does, rather than as a change\nto any of these.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/put_policy.py::INHERIT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'inherit'"
    },
    "Warehouse/inventory/put_policy.py::_SET_SCOPED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "frozenset({'sku_batched'})"
    },
    "Warehouse/inventory/put_policy.py::_fifo": {
      "decorators": [],
      "doc": "Strict arrival order, whatever the placement policy would prefer.\n\nDistinct from `k_cap=1`, which also produces arrival order: that constrains the POOL,\nthis replaces it. With `fifo` the pool has no say even at a wide `k_cap`, which is what\nyou want when the constraint is physical (no floor to re-sort on) rather than a\ntolerance you chose.",
      "is_async": false,
      "kind": "function",
      "signature": "(item) -> float"
    },
    "Warehouse/inventory/put_policy.py::_largest_first": {
      "decorators": [],
      "doc": "Biggest quantity first: clear the bulk while the crew is fresh and the aisles are\nempty, on the theory that a big unit placed late has to thread a fuller warehouse.",
      "is_async": false,
      "kind": "function",
      "signature": "(item) -> float"
    },
    "Warehouse/inventory/put_policy.py::_lifo": {
      "decorators": [],
      "doc": "Newest first. Not an aspiration \u2014 it is what an unmanaged stack of pallets against a\nwall actually produces, and having it named makes the cost of that measurable.",
      "is_async": false,
      "kind": "function",
      "signature": "(item) -> float"
    },
    "Warehouse/inventory/put_policy.py::_sku_batched": {
      "decorators": [],
      "doc": "Finish one SKU before starting the next, oldest SKU first.\n\nA real floor practice \u2014 one trip, one item, many bins \u2014 and it happens to restore the\nSKU-run caches that a FIFO window otherwise destroys, because those caches key on\n`sku != last_sku` and only ever hit on adjacent same-SKU units. That is a side effect\nworth knowing about and NOT the reason to choose this policy: the caches stay correct\neither way, they just stop paying.\n\nThe key is two-level: the negated age of the SKU's OLDEST waiting item, then the\nnegated age of this item.  The second level is load-bearing -- with only the first, every\nunit of a SKU ties and their order falls back to whatever order the caller listed them\nin, which is arrival order in the drain and arbitrary anywhere else.  Requires the\nbatch-scoped table below; the drain builds it once per queue per batch.",
      "is_async": false,
      "kind": "function",
      "signature": "(item) -> float"
    },
    "Warehouse/inventory/put_policy.py::key_for": {
      "decorators": [],
      "doc": "Resolve a policy name to a `(item) -> float` key over THIS batch's waiting items.\n\nReturns None for `inherit` (or None), meaning the caller should use the placement pool's\nown precedence. Raises on an unknown name rather than silently inheriting: a typo in a\nswept configuration would otherwise produce a run that looks like a legitimate arm.",
      "is_async": false,
      "kind": "function",
      "signature": "(policy: str | None, items)"
    },
    "Warehouse/inventory/put_queue.py": {
      "decorators": [],
      "doc": "put_queue.py \u2014 one configurable put-away queue, instantiated N times.\n\n# \u2500\u2500 why this is one object and not three \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nA store warehouse has at least two put-away streams that share nothing operationally: loose\nsingletons that a person walks into a cart, and pallets that need a forklift. Fulfillment has\na third. They differ in who does the work, how fast, how much can be staged, and how much\nfreedom the assignment policy gets \u2014 pallets go away in arrival order almost regardless of\nwhat a scoring function would prefer, because there is no floor to lay a hundred of them out\non and re-sort them.\n\nWriting three classes would encode today's three streams as the shape of the code. The axes\nabove are the real content, so they are a SPEC and the queue is one object configured by it.\nA fourth put-away stream is a row.\n\nInbound turned out to be half of that. It IS a producer into an existing queue -- receiving\nmerchandise enters through `_admit` exactly like every other source, unchanged. But the\nreceiving CREW is not a member of this set, and could not be: `Inventory_Manager._stock`\ndrains every queue here into the placement pool, so a dock among them would have its trailers\nbinned on batch 1; `_cost_putaway` folds `max(x.finish for x in self._put_queues)` into\n`_put_clock`, which offsets every put row on the absolute axis; and a fourth member flips\n`len(self.put_queues) == 1`, swapping `_stock_queue` for `_MultiQueueView`, whose setter\nraises. So `Inbound/dock.py` stands alone and shares the one part that genuinely\nis the same -- the crew's clock, via `Warehouse.kernel.crew_clock`.\n\n# \u2500\u2500 the axes \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n`accepts`   which unit categories this queue takes. Routing is on `unit_category`, which is\n            already a BinKey component, so a unit's queue is decided by what it IS and never\n            by where it might go.\n`k_cap`     the assignment policy's ordering tolerance, in units. 1 = strict arrival order.\n            None means INHERIT the manager's `putaway_window`, not \"unbounded\" -- so a queue\n            states a tolerance only when it has a reason to differ, and the manager-wide\n            knob keeps working. See Inventory_Manager._serve_order.\n`staging`   how many items may wait at once. None = unbounded. A full queue REFUSES\n            admission and the producer holds; it never drops. This is the modelling of\n            floor space, and it is what makes pallet FIFO physical rather than stipulated.\n`policy`    which waiting item to work next, from `put_policy.PUT_POLICIES`. Where `k_cap`\n            narrows the placement pool's freedom, this REPLACES its opinion with the floor's\n            own \u2014 \"finish one SKU before starting the next\" is a real rule that no placement\n            objective expresses. The two compose.\n`crew`      who does the work \u2014 role, mode and speeds. Carried here so a queue's throughput\n            is a property of the queue and not a global.\n`cart`      the vehicle, and its VOLUME limit. Same capacity as the channel's picking cart:\n            a store putter and a store picker push the same cart. A load is bounded by\n            volume, not by item count \u2014 `staging` bounds the floor, this bounds the trip.\n\n# \u2500\u2500 what is deliberately NOT here \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nReach height. A store singleton bin can sit at y_phys 1416 (SINGLETON_BIN_HEIGHT 48, aisles\nup to 1440 inches), so \"singleton \u21d2 cart \u21d2 walking speed\" charges a walk for something 118\nfeet up. The honest discriminator is a BIN property known only AFTER assignment, while the\nqueue is chosen BEFORE it. Splitting the store singleton family by aisle height at build\ntime \u2014 which is what fulfillment already does with FULFILLMENT_AISLE_HEIGHT \u2014 would fix it\nproperly. Until then `accepts` on `unit_category` is a PROXY, and this paragraph is the\nrecord that it is one.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/put_queue.py::ANY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('*',)"
    },
    "Warehouse/inventory/put_queue.py::FULFILLMENT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'fulfillment'"
    },
    "Warehouse/inventory/put_queue.py::HeldItems": {
      "decorators": [],
      "doc": "Put-away items a queue refused, partitioned BY THAT QUEUE.\n\nOne deque per queue rather than one global deque, and the reason is asymptotic. The\nrefill loop retries the held items once per pass and needs roughly `work / staging`\npasses, so the passes and the held list grow together. With a single deque the retry had\nto WALK to discover that the items it could admit were not there -- O(H) per call, O(H)\ncalls, quadratic. Keyed by queue, a full queue is skipped in O(1) and the retry costs\nO(queues + admitted).\n\nThe earlier attempt at this kept the single deque and added an early exit on \"every queue\nhas refused\". That exit is unreachable whenever a queue in the set never receives\nanything -- which is every store-only run of the three-queue split, where `fulfillment`\nstays empty and `blocked` tops out at 2 of 3. Measured: ~347 items examined per call,\ntouches growing at k=1.81. The exit was not too weak; it was asking a question the\npartition answers structurally.\n\nA per-queue COUNT was also tried and rejected: derived state that can drift from the\ndeque, and a stale one makes the retry stop instantly and livelock. Here the partition IS\nthe state -- there is nothing to keep in sync.\n\nORDER. Oldest-first is preserved within a queue, which is the only place it means\nanything: an item competes for floor space only with other items bound for its own\nqueue, so a global age order across queues never affected an admission. `__iter__` yields\nqueue by queue and is used for aggregation (`carryover_rows`, `queue_contents`), where\norder is irrelevant.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/put_queue.py::HeldItems.__bool__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/put_queue.py::HeldItems.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::HeldItems.__iter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::HeldItems.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/put_queue.py::HeldItems.append": {
      "decorators": [],
      "doc": "Hold one item against the queue that refused it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, queue_name: str, item: PutawayItem) -> None"
    },
    "Warehouse/inventory/put_queue.py::HeldItems.clear": {
      "decorators": [],
      "doc": "Drop everything held.  Tests use it to isolate a queue's own behaviour; nothing in\nthe product does, because merchandise does not evaporate.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/put_queue.py::HeldItems.loaded": {
      "decorators": [],
      "doc": "`(queue_name, deque)` for the queues actually holding something.\n\nA list rather than a generator: `_admit_held` drains these in place, and mutating a\ndict's values while iterating its items is the kind of thing that works until a\nqueue empties.",
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::PALLET": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'pallet'"
    },
    "Warehouse/inventory/put_queue.py::PutQueue": {
      "decorators": [],
      "doc": "One spec plus the items waiting under it.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/put_queue.py::PutQueue.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, spec: PutQueueSpec)"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.admit": {
      "decorators": [],
      "doc": "Take one item if there is room. False means the producer must hold it.\n\nRefusing rather than dropping is the whole point: the backpressure has to reach\nwhatever is upstream, because \"inbound packs faster than put-away absorbs\" is the\ncondition being modelled, and a silent drop would make it look like it never\nhappened.\n\n`arrival=False` for a RETRY of something already held.  Only genuine arrivals count\ntoward `blocked`, because a retry is the same work still waiting and was counted\nwhen it first arrived.  Counting both made one number mean two things, and the\nsecond was about the drain's refill loop rather than about the warehouse -- measured\nat 392 / 195 / 47 spurious refusals for staging 4 / 8 / 32, which moves with how\nmany passes the loop happened to need.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, item: PutawayItem, arrival: bool=True) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.bind_crew": {
      "decorators": [],
      "doc": "Give this queue its own crew. Idempotent per (speed, cost, size).\n\n`clocks` INJECTS a pre-built worker list instead of minting one, so two queues --\nin one manager or in two -- can be worked by the same people.  `crew_clock` is\nfunctions over a bare `list[float]` and `reset` mutates IN PLACE, which is what\nmakes sharing free: every `charge` on either queue books against the same list, so\na worker busy on one stream is busy on the other.  Whoever passes it OWNS THE\nRESET -- see `Inventory_Manager.drain_putaway_records`.\n\nNone -- every caller today -- mints this queue's own crew, which is the whole\nhistory of this method.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, speed, cost, size: int, clocks: list | None=None) -> None"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.can_start": {
      "decorators": [],
      "doc": "Is anyone on this crew free to BEGIN a put before `deadline`?  A START gate --\nsee `crew_clock.can_start` for why, and for what `deadline` is measured against.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, deadline: float | None) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.carted": {
      "decorators": [
        "property"
      ],
      "doc": "Does this queue model a cart at all?  False = unbounded loads, i.e. today.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.charge": {
      "decorators": [],
      "doc": "Book `dur` to whoever is free earliest; return (start, worker index).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, dur: float)"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.crew_size": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.drain_counters": {
      "decorators": [],
      "doc": "Hand over this batch's counters and reset. Depth and oldest age are read at the\nmoment of the snapshot and are NOT reset \u2014 they are levels, not flows.\n\n`cut` is reset here with the flows and is NOT one of them; see its declaration.\nBeing reset per batch is what makes a counter per-batch, not what makes it\nsummable.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> dict"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.finish": {
      "decorators": [
        "property"
      ],
      "doc": "When the last worker on this queue becomes free, on the batch-local clock.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.has_room": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.load": {
      "decorators": [],
      "doc": "Put `volume` on the cart; True when that needed a SWAP first.\n\n`cost_model.cart_step`, which is the same next-fit the two picker loops and the LPT\nmakespan predictor share.  Reusing it is the point: a second cart model would be a\nsecond set of numbers to reconcile, and a putter and a picker pushing the same\nphysical cart must agree about when it is full.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, volume: float) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.name": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.oldest_age": {
      "decorators": [
        "property"
      ],
      "doc": "The arrival stamp of the longest-waiting item, or None when empty.\n\nThe head is the oldest because every re-entry path preserves order: the rescues\n`appendleft` children back to the head their parent vacated, and the drain requeues\nwhat it popped. Pinned by Tests/integration/test_putaway_age.py.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int | None"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.reset_clocks": {
      "decorators": [],
      "doc": "Restart every worker at 0 -- the drain does this per batch, and the runner adds\nthe batch epoch back on when it writes the rows.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/inventory/put_queue.py::PutQueue.timed": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet": {
      "decorators": [],
      "doc": "The queues a manager owns, and the routing between them.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.__getitem__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, name: str) -> PutQueue"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, specs)"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.__iter__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.depth": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.is_single": {
      "decorators": [
        "property"
      ],
      "doc": "One queue that takes everything \u2014 the default, and byte-identically the manager\nas it behaved before queues existed.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.route": {
      "decorators": [],
      "doc": "The queue this unit belongs in, by `unit_category`.\n\nFIRST match wins, so spec order is precedence and a catch-all belongs last. A unit\nno queue accepts is an error and not a silent drop -- and RAISING is the whole\ndefence, because nothing downstream would notice. The conservation ledger is a STOCK\nledger over BINS: a unit lost before it reaches one is invisible to it, which is how\ntwo separate defects hid during 2026-08. There is no ledger to vanish from here.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit) -> PutQueue"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSet.snapshot": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSpec": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "The configuration of one put-away queue. Frozen: a queue's identity must not drift\nmid-run, and a sweep that varies these needs to be able to hash them.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/put_queue.py::PutQueueSpec.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/inventory/put_queue.py::PutQueueSpec.takes": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, category: str | None) -> bool"
    },
    "Warehouse/inventory/put_queue.py::SINGLETON": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'singleton'"
    },
    "Warehouse/inventory/put_queue.py::single_queue": {
      "decorators": [],
      "doc": "The default: one unbounded queue that takes everything, with the policy's full\nordering freedom. Reproduces the pre-queue-set manager exactly.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> PutQueueSet"
    },
    "Warehouse/inventory/put_queue.py::store_and_fulfillment": {
      "decorators": [],
      "doc": "The three-stream default: singletons into carts, pallets onto a forklift,\nfulfillment on its own.\n\n`k_cap=1` on the pallet queue is the modelling claim, not a tuning knob: with almost no\nstaging floor there is nowhere to lay pallets out and re-sort them, so they are put away\nin arrival order whatever the assignment policy would prefer. The other two keep the\npolicy's freedom until someone measures a reason to narrow it.\n\nOrdered singleton, pallet, fulfillment \u2014 specific before general, though none of these\nthree is a catch-all, so a category outside the list raises rather than falling through.",
      "is_async": false,
      "kind": "function",
      "signature": "(cart_crew=None, pallet_crew=None, ff_crew=None, pallet_staging: int | None=None, cart_staging: int | None=None, ff_staging: int | None=None, store_cart=None, ff_cart=None, swap_coef: float=0.0) -> PutQueueSet"
    },
    "Warehouse/inventory/put_rungs.py": {
      "decorators": [],
      "doc": "put_rungs \u2014 the put-away fallback chain, as a sequence instead of a diff.\n\nADR-0003 fixes what happens when a queued unit finds no empty bin, and in what order:\n\n    0. an EMPTY bin                    (`placement.place_one` \u2014 where placement is chosen)\n    1. the SKU's OWN bins, fullest first, filled to capacity\n    2. repack into a smaller size tier (pallet or fulfillment)\n    3. singleton bins of the same order type (store only)\n    4. pending \u2014 back on the queue, retried next batch, no expiry\n\nAll five were written inline in one 180-line `while waiting:` loop, each hand-maintaining\n`_queued_sku_counts`, the budget charge and `waiting.appendleft(...)` with subtly different\nbookkeeping.  There was no seam: **a new rung was an edit INSIDE the hottest and most\ninvariant-dense loop in the manager.**  The ADR itself says the rule \"may become a knob\nlater, which is why the rework is recorded rather than hidden\" \u2014 and that knob would have\ncost exactly that edit.\n\nThe contrast was one file away.  `put_policy.py` answers the ADJACENT question \u2014 which\nwaiting item to work next \u2014 with a one-line interface, five adapters, a `key_for` resolver\nthat raises on an unknown name and a documented \"what is NOT here yet\".  Two sibling policy\nquestions; one had a real seam and the other had none.\n\n## What is here and what is not\n\nHere: the result type, the registry of rung names, and the resolver.  **Not** here: the rung\nBODIES, which stay on `Inventory_Manager`.  They reach into `_index`, `_execute_placement`,\n`_top_up_own_bins`, `_charge_repack` and `_queued_sku_counts` \u2014 that depth is why\n`put_policy`'s adapters could leave the manager and these cannot, and pretending otherwise\nwould buy a thinner file at the cost of a wider interface.\n\n## The bookkeeping the driver now expresses once\n\nEvery rung is handed the popped item and answers with what it did.  The driver reads the\nanswer and does the same three things for all of them:\n\n    placed += result.bins                         # the budget counts PLACEMENTS\n    waiting.extendleft(reversed(result.units))     # push back at the head, in order\n    _queued_sku_counts[sku] += len(result.units) - 1\n\nThat last line is the one worth staring at.  The item was POPPED, so it stops counting as\none queued unit; `len(units)` go back in its place.  Every rung's hand-written delta was a\nspecial case of it:\n\n| rung | bins | units back | delta | was written as |\n|---|---|---|---|---|\n| empty bin | 1 | 0 | \u22121 | inside `_execute_placement` |\n| own bins, absorbed | n | 0 | \u22121 | a hand-rolled pop-or-decrement |\n| own bins, partial | n | 1 | 0 | \"needs no delta (unlike the rescues, which split)\" |\n| repack | 0 | k | k\u22121 | `+ delta` where `delta = len(new_units) - 1` |\n| singleton | 0 | k | k\u22121 | the same expression again |\n| pending | 0 | 0 | \u2014 | the item is held, not consumed |\n\n`counts_booked` is the one asymmetry, and it is declared rather than buried: the empty-bin\nrung goes through `_execute_placement`, which drops the count itself because it is also\nreached from places that never touched a queue.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/inventory/put_rungs.py::DECLINED": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "RungResult()"
    },
    "Warehouse/inventory/put_rungs.py::RungResult": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "What one rung did with the unit it was handed.  Falsy means it declined.\n\n`bins` are PLACEMENTS \u2014 trips a putter made, `bin_placement` rows written, and what the\nbudget caps.  A unit absorbed into three of its own bins is three of them, which is why\nthe budget cannot simply count popped items: a cap read that way is silently generous\nexactly when the warehouse is most fragmented.\n\n`units` go back on the head of the queue IN ORDER and take the chain from the top.  A\nrepack's splits and a top-up's remainder are the same shape to the driver even though\none is rework and the other is a part-filled trip.\n\n`counts_booked` says this rung already applied the `_queued_sku_counts` change itself.\nOnly the empty-bin rung sets it, because its placement call is shared with callers that\nnever had a queue item to drop.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/inventory/put_rungs.py::RungResult.__bool__": {
      "decorators": [],
      "doc": "A rung that neither placed nor respawned anything DECLINED, and the chain walks on.\n\nDeliberately derived rather than a fourth field: a rung that reports work and also\nreports declining is not a state the chain has, and a flag would let one exist.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/inventory/put_rungs.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['RungResult', 'DECLINED', 'PUT_RUNGS', 'DEFAULT_PUT_CHAIN', 'chain_for']"
    },
    "Warehouse/inventory/put_rungs.py::chain_for": {
      "decorators": [],
      "doc": "Resolve rung names to a chain, refusing an unknown one.\n\nMirrors `put_policy.key_for`: a typo must be a refusal at build time, not a rung that\nsilently never fires.  `None` is the default chain.",
      "is_async": false,
      "kind": "function",
      "signature": "(names) -> tuple[str, ...]"
    },
    "Warehouse/kernel/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.kernel \u2014 Zero-dependency value objects shared across the domain: the pick-cost primitives and the store/fulfillment regime identity.  Nothing here may import from elsewhere in Warehouse.\n\nSee README.md for what does and does not belong here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/kernel/allocation.py": {
      "decorators": [],
      "doc": "allocation \u2014 split a fixed set of work across N workers.\n\nExtracted from `Pick.assign_tasks`, which was and remains the ONE task\u2192picker partition\nboth pick simulations call, so they cannot drift.  What is here is the part that has\nnothing to do with picking: given some items, a cost for each, and a tie-break order, hand\nthem out to N workers under a named policy.\n\nA second work stream \u2014 an inbound crew unloading trailers \u2014 needs exactly this and nothing\nelse from the scheduler.  Keeping the policies in one place means round-robin and LPT get\nimplemented once rather than reimplemented next to the dock, and it means the two streams\ncannot quietly disagree about what \"balanced\" means.\n\n`Warehouse/kernel/` may import NOTHING (see `architecture.yml`'s `wh_kernel -> *` forbid),\nwhich is exactly why this module is a good home: both the domain engine and any future\ninbound package can reach it, and it can reach neither.\n\n## The two policies, and why LPT balances a proxy\n\n`round_robin` is `i % n` \u2014 the legacy assignment, byte-identical, and the default.\n\n`lpt` visits items HEAVIEST-FIRST and appends each to the LEAST-LOADED worker.  That is a\nstandard makespan heuristic; it is NOT a guaranteed bound on a realized step-function\nmakespan and on adverse inputs can do no better than round-robin.  It never yields an\ninvalid partition and total work is unchanged, so results stay correct either way.\n\nThe caller supplies `cost_of`, and in the pick case it is deliberately a *continuous* proxy\nrather than the exact cost: a cart-swap step function can be fooled by a light item that\nhappens to look free on a cart-favourable worker.  Balancing on a smooth monotone\napproximation and then MEASURING the real thing is the split that keeps the scheduler\nhonest \u2014 see `Pick.count_cart_swaps`, the exact-makespan predictor the proxy is checked\nagainst.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/kernel/allocation.py::POLICIES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('round_robin', 'lpt')"
    },
    "Warehouse/kernel/allocation.py::partition": {
      "decorators": [],
      "doc": "Split `items` across `n` workers and return one list per worker.\n\n`items`      must already be in the order the consumer will process them; the\n             round-robin policy preserves exactly that order within each worker.\n`cost_of`    per-item balancing cost.  Required by `lpt`, unused by `round_robin`.\n`order_key`  the sort key each worker's list is restored to after LPT reorders it,\n             AND the tie-break among equal costs.  Required by `lpt`.\n\nThe tie-break is subtle and load-bearing: LPT sorts on `(cost, order_key)` with\n`reverse=True`, so items of equal cost are visited in DESCENDING key order.  That is\nwhat the retired implementation did, and changing it silently repartitions every run.",
      "is_async": false,
      "kind": "function",
      "signature": "(items: Iterable[Any], n: int, *, policy: str='round_robin', cost_of: Callable[[Any], float] | None=None, order_key: Callable[[Any], Any] | None=None) -> list[list]"
    },
    "Warehouse/kernel/cost_model.py": {
      "decorators": [],
      "doc": "cost_model.py \u2014 single source of truth for the pick-cost primitives.\n\nThese three helpers (height multiplier, per-unit handling, travel) were previously\ncopy-pasted across Pick, Workload, Assignment_Functions, Inventory_Management, Order\nand Capacity_Reloader.  Centralising them keeps the simulated pick time, the analytical\nworkload W, the placement scorers, and the optimal-layout/map computations in lockstep.\n\nDepends only on `math` + `functools`, so every layer (Warehouse + Optimization) can import it\nwithout introducing a cycle or a layer violation.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/kernel/cost_model.py::SpeedProfile": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One actor's travel speeds: **ft/s in, s/inch out**, converted once.\n\nA speed is configured in ft/s and spent in s/inch, because bin positions are inches.\nNothing named that boundary, so every consumer crossed it by hand \u2014 47 separate\n`sec_per_inch` calls across the two pick simulations, the analytical workload, the\nassignment functions, the optimal-placement search and the strategy runner.\n\nThe discipline at those sites is already right: every one hoists the conversion to the\ntop of its function and passes `x_pace`/`y_pace` down, so the arithmetic is never\nper-bin.  What was missing is a NAME for the hoisted pair.  Two bare floats travelling\ntogether through a dozen signatures is the shape that lets an x pace reach a y slot, and\nit is also the shape that cannot be extended: a putter and an unloader need their own\nspeeds, and `(x_pace, y_pace)` has nowhere to record whose they are.\n\nLives HERE, in `cost_model`, rather than in a module of its own or in the operations\npackage that will own roles and modes.  `architecture.yml` forbids `wh_kernel -> *` and\nthe wildcard matches the kernel itself, so a separate `kernel/speed.py` importing\n`sec_per_inch` is a boundary violation \u2014 the kernel's modules stay independently\nimportable.  Beside the two functions it uses is also simply where it belongs.\n\nTHE AXES ARE NOT INTERCHANGEABLE.  `x` is horizontal travel ALONG an aisle\n(`aisle_width_for(50)` = 200 ft of run); `y` is VERTICAL lift (`aisle_height_for(10)` =\n40 ft of rack).  Separate DB columns record them (`pick_travel_x/y`,\n`non_pick_travel_x/y`) and `height_multiplier` brackets on the same `y_phys` this paces\n\u2014 so a machine that drives fast and lifts slowly is a DIFFERENT profile from a walker,\nnot a scaled one.\n\nFrozen and picklable, so it crosses the spawn boundary in a worker payload and can be\nshared rather than copied.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/kernel/cost_model.py::SpeedProfile.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/kernel/cost_model.py::SpeedProfile.paces": {
      "decorators": [
        "property"
      ],
      "doc": "`(x_pace, y_pace)` \u2014 for the call sites that still want the bare pair.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[float, float]"
    },
    "Warehouse/kernel/cost_model.py::TRANSFORMS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'log': lambda v: math.log(max(v, 1.0)), 'linear': lambda v: float(v), 'sqrt': lambda v: math.sqrt(max(v, 0.0))}"
    },
    "Warehouse/kernel/cost_model.py::aisle_exit_cost": {
      "decorators": [],
      "doc": "One-way lane EXIT (s): traverse from the last pick to the aisle far end, then\ndescend to the ground.  Returns `(exit_x, exit_y)`.\n\nTHE definition both pick simulations use, and the reason it exists separately from\n`aisle_traverse_cost`: the sims never compute an aisle ENTRY as such \u2014 their first\nbin-to-bin segment already covers it, and they do not track a \"first pick\" position to\npass.  What they genuinely share is this half, so this half is what is shared.\n\nCharged to NON-PICK travel: the picker is walking to leave, not to pick.  A two-way\nlane has no exit segment at all, which is the caller's `if cfg.one_way` \u2014 kept there\nbecause the caller also has to decide whether to accumulate the result.",
      "is_async": false,
      "kind": "function",
      "signature": "(last_x: float, last_y: float, aisle_length: float, x_pace: float, y_pace: float) -> tuple[float, float]"
    },
    "Warehouse/kernel/cost_model.py::aisle_traverse_cost": {
      "decorators": [],
      "doc": "Non-pick aisle-traversal time (s) for ONE aisle visit, split into ENTRY and EXIT\nper axis.\n\nNOT called by the pick simulations, and the docstring here claimed otherwise for as\nlong as it existed: it said it was \"the single definition shared by the sim (Pick /\nfast_pick), the analytical workload (Workload), and the travel decomposition so they\ncan never drift\" while having zero callers, and both sims inlined the arithmetic.  A\nseam that lies about being one is worse than an absent seam, because it stops anyone\nlooking for the real duplication.\n\nThe half the sims DO share is `aisle_exit_cost`, which this now delegates to.  The\nentry half is folded into their generic bin-to-bin travel loop, so there is nothing\nhere for them to call.\n\nENTRY = aisle entrance \u2192 first pick:\n    entry_x = |first_x - entrance_x| * x_pace ;  entry_y = first_y * y_pace  (raise from ground)\nEXIT  = last pick \u2192 aisle far end (one-way lane only), descend to ground:\n    exit_x  = |aisle_length - last_x| * x_pace ;  exit_y = last_y * y_pace\nTWO-WAY (default): no exit \u2014 the picker leaves the way it came, reproducing today's model.\n\n`x_pace`/`y_pace` are per-inch paces (sec_per_inch of the regime's speed).  Returns\n(entry_x, entry_y, exit_x, exit_y) in seconds; sum them for the total non-pick travel.",
      "is_async": false,
      "kind": "function",
      "signature": "(first_x: float, first_y: float, last_x: float, last_y: float, aisle_length: float, x_pace: float, y_pace: float, one_way: bool, entrance_x: float=0.0) -> tuple[float, float, float, float]"
    },
    "Warehouse/kernel/cost_model.py::cart_step": {
      "decorators": [],
      "doc": "One next-fit cart step for a single pick of volume ``needed_vol`` against a cart with\n``cart_remaining`` free volume (capacity ``cart_cap``).  Returns ``(swapped, new_remaining)``.\n\nA **swap** fires when the pick doesn't fit the current cart; the cart is then refilled to full\nbefore the pick is loaded.  This is the SINGLE source of the cart next-fit, shared by the sim's\nper-pick loop (``Pick`` / ``fast_pick``) and the scheduler's makespan predictor\n(``count_cart_swaps``), so the two can never drift \u2014 which is what makes the LPT scheduler's\npredicted makespan equal the sim's realized makespan.",
      "is_async": false,
      "kind": "function",
      "signature": "(needed_vol: float, cart_remaining: float, cart_cap: float) -> tuple[bool, float]"
    },
    "Warehouse/kernel/cost_model.py::handle_var": {
      "decorators": [],
      "doc": "Per-unit weight/volume handling term v_s = pw\u00b7fn_w(w) + pv\u00b7fn_v(v) (no intercept, no\nquantity).  fn defaults to natural log (the original behaviour); other base functions\n(linear / sqrt / pow:p / log:b) are honored so a config's pick_weight_fn/pick_volume_fn\nactually changes labor.  The transforms clamp w/v internally (log\u2192\u22651, sqrt/pow\u2192\u22650).",
      "is_async": false,
      "kind": "function",
      "signature": "(weight: float, volume: float, weight_coef: float, volume_coef: float, weight_fn: str='log', volume_fn: str='log') -> float"
    },
    "Warehouse/kernel/cost_model.py::height_multiplier": {
      "decorators": [],
      "doc": "Handling multiplier for a pick at physical height y_phys (step over brackets).",
      "is_async": false,
      "kind": "function",
      "signature": "(brackets: tuple, y_phys: float) -> float"
    },
    "Warehouse/kernel/cost_model.py::per_pick": {
      "decorators": [],
      "doc": "The composite at-location expression  mult \u00b7 (intercept + qty \u00b7 per_item + qty \u00b7 var).\n\nTHE one formula behind every per-pick cost in the codebase \u2014 the sim's pick time\n(mult = height bracket M(y)), the put-away and unload costs, the analytical workload\nP-term, the per-SKU labor_cost (mult=1, qty=1), the optimal-map coefficients (mult =\nfrequency), the gain evaluator's at-bin price and the marginal placement scores (\u2026 + D).\nPreviously inlined at 7 sites with \"mirrors _pick_time\" comments; one helper makes the\ninvariant structural.\n\n`per_item` is the per-UNIT charge (see DEFAULT_PICK_PER_ITEM above).  It is a parameter\nHERE, defaulting to 0.0, so the primitive does not fork: every caller passes its own\ncrew's value (`PickConfig.pick_per_item`, `PutawayCost.per_item`, \u2026), and the model-level\ndefaults are non-zero where the decision says so.  With `per_item == 0.0` the expression\nis bit-identical to the pre-charge `mult \u00b7 (intercept + qty \u00b7 var)`: `intercept + 0.0`\nis exactly `intercept`, and the remaining associativity is unchanged.",
      "is_async": false,
      "kind": "function",
      "signature": "(mult: float, intercept: float, var: float, qty: float=1, per_item: float=0.0) -> float"
    },
    "Warehouse/kernel/cost_model.py::resolve_transform": {
      "decorators": [
        "lru_cache(maxsize=None)"
      ],
      "doc": "Resolve a transform spec \u2192 callable.  Cached per process (the returned lambda is never\npickled \u2014 only the spec string crosses process boundaries), so per-pick use is cheap.\n  'log' | 'log:e' -> ln ;  'log:b' -> log base b ;  'linear' ;  'sqrt' ;  'pow:p' -> v**p.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec: str)"
    },
    "Warehouse/kernel/cost_model.py::sec_per_inch": {
      "decorators": [],
      "doc": "Per-inch pace (s/inch) for a travel SPEED in ft/s.  Positions are in inches, so this\nis the factor the hot loops multiply by:  travel = x_phys\u00b7sec_per_inch(x_speed) + \u2026\nGuards speed \u2264 0 \u2192 inf (a zero/negative speed never moves).",
      "is_async": false,
      "kind": "function",
      "signature": "(speed_ft_per_sec: float) -> float"
    },
    "Warehouse/kernel/cost_model.py::travel_cost": {
      "decorators": [],
      "doc": "Demand-blind travel time (s) to a bin.  x_speed/y_speed are SPEEDS in ft/s; positions\nare in inches.  Hot inner loops inline this with a precomputed sec_per_inch() pace to\navoid the per-bin division; everywhere else, call this.",
      "is_async": false,
      "kind": "function",
      "signature": "(x_phys: float, y_phys: float, x_speed: float, y_speed: float) -> float"
    },
    "Warehouse/kernel/cost_model.py::validate_speeds": {
      "decorators": [],
      "doc": "Reject a travel speed that would poison every score with NaN.  Raises ValueError.\n\nTHE TRAP this exists to close.  `sec_per_inch` maps a non-positive speed to `inf`, which\nis the honest answer on its own (never moves \u2192 infinite time).  But the hot loops spend it\nas `pace \u00b7 phys`, and a bin ON the axis origin has `phys == 0` \u2014 so `inf \u00b7 0` yields\n**NaN**, not 0.  NaN then propagates through every travel term, and because every\ncomparison against NaN is False, `min()`/`max()`/`sorted()` silently degrade to \"keep\nwhichever candidate came first\".  Placement quietly becomes insertion order.  No warning,\nno exception, no visibly wrong number \u2014 just a run whose assignment function did nothing.\n\nNothing else catches it: `x_speed`/`y_speed` are read straight out of the config JSON\n(`Optimization/config/sim_config.py`), so one `\"y_speed\": 0` in a config file would do this\nto a whole sweep.  Validating at the two dataclass boundaries (PickConfig, WorkloadParams)\ncosts one call per config rather than per bin, and cannot perturb a valid run \u2014 it only\nraises on input that was already producing NaN.",
      "is_async": false,
      "kind": "function",
      "signature": "(x_speed: float, y_speed: float, *, source: str) -> None"
    },
    "Warehouse/kernel/crew_clock.py": {
      "decorators": [],
      "doc": "crew_clock \u2014 one crew's workers as a list of instants, and the five rules over it.\n\n# \u2500\u2500 why this is functions over a list, and not a class \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nEvery timed work stream in this simulator needs the same five things: a clock per worker,\n\"who is free earliest\", \"is anyone free before the whistle\", \"when does the last one finish\",\nand \"start the batch over\". Put-away had them as five `PutQueue` methods. Receiving needs the\nidentical five, and two copies of a rule like `min(clocks) < deadline` will drift \u2014 one will\nsay `<=` some day, and the number it produces will be plausible forever.\n\nThey are **functions over a bare `list[float]`** rather than a `CrewClock` class for two\nreasons, in order. First, `Warehouse/inventory/put_queue.py` states in its own docstring that\nthree streams must not become three classes; a base class is exactly the hierarchy it refuses,\nand it would force `PutQueue` to swap a real slot for a property. Second, `list[float]` is\nalready the representation everything holds, so nothing has to be migrated and no test that\nassigns to `q.clocks` has to change.\n\n`architecture.yml` forbids `wh_kernel -> *`, so this module imports nothing at all. That is\nwhat lets both `Warehouse/inventory/` and `Warehouse/operations/` reach it without inverting\nanything.\n\n# \u2500\u2500 the two contracts a caller must know \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n**The clock is BATCH-LOCAL.** Every value here starts at 0 at the top of a batch and is reset\nby whoever drains that batch's records. The absolute instant is the caller's business: it\nholds a per-arm carry and adds the batch epoch when it writes rows. This is not tidiness --\n`Inventory_Manager.drain_putaway_records` records what happened when it was skipped, with the\nmeasurement: batch 7's puts landed at t_local 53,769-64,152 s against a 17,906 s batch, and\nthe error grows without bound.\n\n**`can_start` is a START gate, not a completion gate.** A worker already past the whistle\nbegins nothing new; the job in progress when it blows runs to the end. Overtime is therefore\nbounded by one job per worker, which is what the physical thing does -- a putter does not set\na pallet down halfway up an aisle, and a receiver does not leave half a pallet on the tail\nlift. A completion gate would need the job's duration, which is known only after its\ndestination is chosen, so it would mean choosing and then un-choosing a placement.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/kernel/crew_clock.py::can_start": {
      "decorators": [],
      "doc": "Is anyone free to BEGIN work before `deadline`?\n\n`deadline` is on the same batch-local clock these values run on -- a REMAINDER, not an\ninstant. That is what lets a caller state a day boundary without knowing the batch epoch,\nand it is also the easiest thing to get wrong: hand this an absolute instant and the\nwhistle never fires, so the day reads as unbounded and the cut counter reads as zero,\nwhich looks exactly like \"the boundary cost nothing\".\n\nTrue when unbound: a stream with no crew has no clock to be past, and every run that does\nnot ask for a cut passes None anyway.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks, deadline: float | None) -> bool"
    },
    "Warehouse/kernel/crew_clock.py::can_start_subset": {
      "decorators": [],
      "doc": "Is anyone on the TEAM `idxs` free to BEGIN before `deadline`?\n\nSame START-gate contract as `can_start`, restricted to a team: a worker already past\nthe whistle begins nothing new, the job in progress finishes.  False on an empty team\n(nobody can start), True when `deadline` is None (no whistle today) \u2014 note the order:\nan empty team is not working even on an unbounded day.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks, idxs, deadline: float | None) -> bool"
    },
    "Warehouse/kernel/crew_clock.py::charge": {
      "decorators": [],
      "doc": "Book `dur` to whoever is free earliest; return `(start, worker_index)`.\n\nGreedy list scheduling. Ties break to the lowest index, so a crew of one is exactly a\nserial clock -- which is what makes a size-1 stream byte-identical to the single-clock\nmodel that preceded this.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks, dur: float)"
    },
    "Warehouse/kernel/crew_clock.py::charge_subset": {
      "decorators": [],
      "doc": "Book `dur` to the team member free earliest; return `(start, worker_index)`.\n\nThe returned index is into `clocks` \u2014 the CREW's index space, not the team's \u2014 so a\nrecord written from it names the same worker whichever team they were dealt to.  Ties\nbreak to the lowest crew index, exactly as `charge` does, so a team of one is a serial\nclock and a team of everybody IS `charge`.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks, idxs, dur: float)"
    },
    "Warehouse/kernel/crew_clock.py::earliest_subset": {
      "decorators": [],
      "doc": "When the first worker of the TEAM `idxs` becomes free, on the batch-local clock.\n\nThe subset trio (`earliest_subset` / `can_start_subset` / `charge_subset`) exists for\ndoor-team crew allocation: one crew's clock list, dealt into teams that each work one\ntrailer, every rule still stated once over the same bare `list[float]`.  `idxs` are\nindices into `clocks`; an empty team returns +inf \u2014 a team of nobody is never free,\nwhich is what lets a caller take a global min over teams without special-casing.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks, idxs) -> float"
    },
    "Warehouse/kernel/crew_clock.py::finish": {
      "decorators": [],
      "doc": "When the LAST worker becomes free, on the batch-local clock. 0.0 when unbound.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks) -> float"
    },
    "Warehouse/kernel/crew_clock.py::new_clocks": {
      "decorators": [],
      "doc": "`size` idle workers. Raises below 1, because a crew of nobody is a configuration\nerror rather than a crew that does nothing slowly.\n\n`what` names the owner in the message -- a queue name, a dock name -- so a bad size in a\nmulti-stream configuration says WHICH stream.",
      "is_async": false,
      "kind": "function",
      "signature": "(size: int, what: str='crew') -> list"
    },
    "Warehouse/kernel/crew_clock.py::reset": {
      "decorators": [],
      "doc": "Restart every worker at 0, IN PLACE.\n\nIn place rather than rebinding, so a caller holding the list keeps the one the owner\nuses. A drain does this per batch and the runner adds the batch epoch back on when it\nwrites the rows -- see the module docstring for what happens when it does not.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks) -> None"
    },
    "Warehouse/kernel/crew_clock.py::size_of": {
      "decorators": [],
      "doc": "How many workers. 0 when unbound, which is every stream that never got a crew.",
      "is_async": false,
      "kind": "function",
      "signature": "(clocks) -> int"
    },
    "Warehouse/kernel/regime.py": {
      "decorators": [],
      "doc": "regime.py \u2014 storage-regime identity (store vs fulfillment).\n\nA *regime* is a self-contained ``(handling, storage_type)`` family with its own bin\ncatalog, batch stream, and picker cost.  Fulfillment items/bins/aisles are tagged with\nthe ``'fulfillment'`` handling + storage_type (+ ``unit_category``); everything else is\n``'store'``.  ``regime_of`` duck-types over Orders, StorageUnits, Bins, and Aisles so every\nlayer (Warehouse primitives, planner, placement, cost routing, runner, analysis) can key\non the SAME single-valued regime without importing warehouse internals \u2014 this module has\nno dependencies, so it never introduces an import cycle.\n\nTwo spellings, one rule: ``regime_of`` reads an ENTITY and ``regime_of_key`` reads a\n**BinKey**.  The second exists because a key is a plain tuple that the first cannot read\nand does not refuse \u2014 see its docstring.\n\nThe single-valued-per-entity property is what keeps the per-regime cost routing cheap:\neach bin's ``_D`` and each order's ``labor_cost`` is one value, computed with that entity's\nregime parameters, not a per-regime dict.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/kernel/regime.py::regime_of": {
      "decorators": [],
      "doc": "Storage regime ('store' | 'fulfillment') of an Order, StorageUnit, Bin, or Aisle.\n\nDuck-typed and single-valued.  Unknown / legacy objects fall back to 'store', so any\ninventory or warehouse built before this feature behaves exactly as before.\n\nA TUPLE IS REFUSED, LOUDLY.  The fallback above is what makes a BinKey dangerous here:\na key is a plain tuple, every `getattr` below falls through it, and the answer comes\nback 'store' for a fulfillment key with nothing raising.  `regime_of_key` is the\nspelling for that input and the two names are three characters apart, so the mistake is\na typo away.  An identity check on `type` rather than `isinstance`: this is duck-typed\nand hot-ish, no entity is a tuple subclass, and the check must cost nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(x) -> str"
    },
    "Warehouse/kernel/regime.py::regime_of_key": {
      "decorators": [],
      "doc": "Storage regime of a **BinKey** \u2014 the 4-tuple ``(handling, category, storage_size,\nunit_category)`` a bin or a unit is classified under, never an object.\n\nIts own function rather than a branch inside ``regime_of``, because a key is a PLAIN\nTUPLE: every ``getattr`` above falls through it, so ``regime_of(key)`` answers 'store'\nfor a fulfillment key \u2014 silently, and always in the same direction.  Nothing raises,\nwhich is why the trap needs a correct spelling to point at rather than a warning\n(``Inbound/site_space.py`` records the same trap on the bin side, where the fix was to\nask the bin instead).\n\nReads the same three fields ``regime_of`` reads \u2014 handling, category, unit_category \u2014\nwhich is what makes a key sufficient to name an OWNER: regime is itself a BinKey\ncomponent (see ``inventory_common``), so a group of units sharing a key shares a\nregime.  Slot 2 (``storage_size``) is deliberately not read: tier names are per\nunit_category and carry no regime of their own.\n\nA malformed key raises on the unpack, which is the loud end of the same argument.",
      "is_async": false,
      "kind": "function",
      "signature": "(key) -> str"
    },
    "Warehouse/kernel/timeline.py": {
      "decorators": [],
      "doc": "timeline \u2014 what the simulation's clock is made of, and how batches sit on one axis.\n\nSimulated time is a per-picker `float`, advanced by accumulation at three sites \u2014 travel,\ncart swap, handling \u2014 and merged across pickers by sorting events on `.time`.  It STARTS AT\nTHE BATCH EPOCH, not at zero: the runner hands every picker `start_times=[arm_clock] * k`\nand both loops begin at that `t0`.  (This module said \"reborn at 0.0 at the start of every\nbatch\" for a while after the carry landed; it was describing the older model.)\n\nThere is no clock OBJECT driving the simulation, and this module still does not add one.\nWhat it adds below is a `WorkDay` and a `ReleaseSchedule`: value objects that answer \"when\ndoes batch i start\" and \"when does its day end\", so a caller can schedule against a working\nday instead of against the previous batch's makespan.  They compute; they do not run\nanything.\n\n## 1. The unit\n\n`cost_model` produces SECONDS and says so: `sec_per_inch` is `1/(12\u00b7ft_per_s)` and is\ndocumented as s/inch.  The constants added to the same accumulator read the same way \u2014\n`pick_intercept: 15`, `cart_swap_coef: 300` are 15 seconds a pick and five minutes a cart\nswap, which are warehouse numbers; at 15 ms and 0.3 s they are not physical.\n\nThe ANALYSIS layer used to declare the opposite: `Performance_Evaluations/common/units.py`\nsaid \"durations are milliseconds\" and divided by `3.6e6` for hours.  3.6e6 SECONDS is 1000\nhours, so every ABSOLUTE figure the suite published was 1000x out \u2014 and every RATIO was\nright, which is exactly why nothing caught it for the life of the project.\n\nThat is fixed.  `units.py` now IMPORTS `SECONDS_PER_HOUR` from here instead of restating a\nliteral, so the sim's unit and the analysis layer's divisor cannot disagree again.\n\nThis module also declares BOTH DAYS, side by side: `DEFAULT_SHIFT_SECONDS` (the SITE day,\n28,800 s -- a batch) and `SECONDS_PER_DAY` (the CALENDAR day, 86,400 s -- a carrier's\ndetention).  They used to live three modules apart with neither naming the other, which is\nhow a threshold measured in one got consumed in the other.  Both are legitimate; the\nambiguity is in the word.\n\n## 2. The epoch\n\nA picker's `t` is absolute only because the runner gives it the epoch to start from.  The\nepoch is the running sum of the batch durations before it; the RUNNER computes it directly\nnow (`arm_clock = bs.batch_start_time + bs.duration`), and the analysis layer's bare\n`np.cumsum` is no longer the only place it exists.  `batch_epoch` is that same arithmetic,\nnamed, in a module both the domain and the harness can import.\n\nOne case the sum cannot cover on its own: a batch that produces no tasks has a zero\nduration, so a schedule -- not a makespan -- is what makes the next one start later.  That\nis `ReleaseSchedule`.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/kernel/timeline.py::DEFAULT_SHIFT_SECONDS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "8 * SECONDS_PER_HOUR"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule": {
      "decorators": [],
      "doc": "When batch `i` is allowed to start.\n\nTwo modes, and the default is the one that reproduces today:\n\n  CONTINUOUS (`per_day=None`)  batch i starts when batch i-1 finished.  The schedule has\n                               no opinion, so `release_at` returns the ready instant it\n                               was handed.  This is the current runner, exactly.\n  PACED (`per_day=N`)          the day is cut into N equal slots and batch i is released\n                               at its slot, or when the previous batch finished if that\n                               is later.  A batch that produced no tasks still consumes\n                               its slot, which is the whole point: an empty batch must\n                               advance the axis, and a makespan of zero cannot.\n\n`release_at` takes `ready_at` rather than reading a clock, so the schedule stays a pure\nfunction and the runner keeps owning the arm's state.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, day: 'WorkDay | None'=None, per_day: int | None=None)"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.cadence": {
      "decorators": [
        "property"
      ],
      "doc": "Seconds between releases.  The whole day when the schedule is continuous, which\nis not a release interval so much as the absence of one.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.day_of": {
      "decorators": [],
      "doc": "Which working day batch `index` is scheduled into.  Always 0 when continuous,\nbecause a continuous schedule has no day structure to place it in.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int) -> int"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.is_continuous": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> bool"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.missed_by": {
      "decorators": [],
      "doc": "How late batch `index` is against its slot.  0.0 when on time or continuous.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int, ready_at: float) -> float"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.release_at": {
      "decorators": [],
      "doc": "When batch `index` starts, given the arm is free at `ready_at`.\n\nNEVER EARLIER THAN `ready_at`.  A schedule says when work may begin, not when a\npicker teleports: if the previous batch overran its slot, this batch starts late and\nthe schedule has been missed.  `missed_by` reports that rather than hiding it.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int, ready_at: float) -> float"
    },
    "Warehouse/kernel/timeline.py::ReleaseSchedule.scheduled_at": {
      "decorators": [],
      "doc": "Where the schedule WANTED batch `index`, ignoring whether the arm was free.\n\n`release_at` clamps to the ready instant; the difference between the two is the only\nrecord that a slot was missed, so it needs its own accessor.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int) -> float"
    },
    "Warehouse/kernel/timeline.py::SECONDS_PER_DAY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "24.0 * SECONDS_PER_HOUR"
    },
    "Warehouse/kernel/timeline.py::SECONDS_PER_HOUR": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "3600.0"
    },
    "Warehouse/kernel/timeline.py::TIME_UNIT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'seconds'"
    },
    "Warehouse/kernel/timeline.py::WorkDay": {
      "decorators": [],
      "doc": "A fixed-length working day on the absolute axis.\n\n`length` seconds long, starting at `origin`.  Days are back to back: there is no gap\nbetween the end of one and the start of the next, because a gap would need a calendar\nand this model has none.  Overnight is expressed by making the day shorter, not by\ninserting emptiness between days.\n\nPure arithmetic.  Nothing here reads or advances a clock -- a caller passes an instant\nin and gets an instant back, which is what makes it testable without a simulation.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/kernel/timeline.py::WorkDay.__eq__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, other)"
    },
    "Warehouse/kernel/timeline.py::WorkDay.__hash__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/kernel/timeline.py::WorkDay.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, length: float=DEFAULT_SHIFT_SECONDS, origin: float=0.0)"
    },
    "Warehouse/kernel/timeline.py::WorkDay.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/kernel/timeline.py::WorkDay.end_of": {
      "decorators": [],
      "doc": "The instant the day CLOSES -- which is the next day's start.\n\nHalf-open by construction: an instant exactly at `end_of(i)` belongs to day i+1, and\n`index_of(end_of(i)) == i + 1`.  Anything else double-counts the boundary instant.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int) -> float"
    },
    "Warehouse/kernel/timeline.py::WorkDay.fits": {
      "decorators": [],
      "doc": "Does work of `duration` starting at `t_abs` finish before its day closes?",
      "is_async": false,
      "kind": "function",
      "signature": "(self, t_abs: float, duration: float) -> bool"
    },
    "Warehouse/kernel/timeline.py::WorkDay.index_of": {
      "decorators": [],
      "doc": "Which day the absolute instant falls in, 0-based from `origin`.\n\nAn instant BEFORE the origin belongs to a negative day rather than to day 0.  Day 0\nis a real interval, and folding everything earlier into it would make the first day\nlook longer than the rest.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, t_abs: float) -> int"
    },
    "Warehouse/kernel/timeline.py::WorkDay.remaining": {
      "decorators": [],
      "doc": "Seconds left in the day containing `t_abs`.  Never negative, never zero.\n\nExactly at a boundary the answer is a WHOLE day, not zero: the instant belongs to\nthe day that is opening. Returning zero there would stop a picker that has just\nstarted rather than one that has run out of time.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, t_abs: float) -> float"
    },
    "Warehouse/kernel/timeline.py::WorkDay.start_of": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, index: int) -> float"
    },
    "Warehouse/kernel/timeline.py::batch_epoch": {
      "decorators": [],
      "doc": "Absolute start time of batch `index`: the sum of every batch before it.\n\n`durations[i]` is batch i's makespan, in the sim's own unit.  Adding this to a\npicker-local `t` puts an event from any batch on one axis \u2014 which is what a trailer\narriving on a schedule, or a slot freeing four batches from now, has to be measured\nagainst.\n\nRaises on a negative index rather than silently wrapping, because `durations[-1]` is a\nreal batch and would produce a plausible wrong answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(durations: Sequence[float], index: int) -> float"
    },
    "Warehouse/kernel/timeline.py::epochs": {
      "decorators": [],
      "doc": "The absolute start time of every batch \u2014 `[0, d0, d0+d1, \u2026]`, one per batch.\n\nThe whole-series form, for a caller converting a run to one timeline in a pass.  This\nis the arithmetic `run_whatif_volume` performs with `np.cumsum`, offset by one: cumsum\ngives each batch's END, this gives each batch's START.",
      "is_async": false,
      "kind": "function",
      "signature": "(durations: Iterable[float]) -> list[float]"
    },
    "Warehouse/kernel/timeline.py::shift_end": {
      "decorators": [],
      "doc": "When the drain-or-cap shift ENDED: the drain instant or the cap, whichever first.\n\n`drained` is the CALLER's judgment \u2014 no standing work remains (released-but-unpicked\ndemand, queued puts and held items, the dock floor; never merchandise in transit) and\nnothing was cut at the whistle.  This function owns only the arithmetic: a drain that\nhappened before the cap ends the shift there (\"off the clock\"); anything else \u2014 work\nstanding, or a finish past the whistle (START-gate overtime) \u2014 ends it at the cap,\nbecause the cap came first.  Days stay origin-aligned either way: an early end stops\nthe labour, never the calendar.",
      "is_async": false,
      "kind": "function",
      "signature": "(cap_end_s: float, last_finish_s: float, drained: bool) -> float"
    },
    "Warehouse/kernel/timeline.py::shift_index": {
      "decorators": [],
      "doc": "Which fixed-length shift the absolute instant `t_abs` falls in.  0-based.\n\nA REPORTING FRAME OVER A CONTINUOUS CLOCK, and that is the whole contract: fixed-length\nshifts label the timeline, they do not schedule against it.  Work never pauses at the\nwhistle and no task is split at a boundary \u2014 a task that spans one is recorded once,\nunder the shift it STARTED in, because that is where the actor picked it up.\n\nThis is what \"work carries over\" means in a model whose scheduling atom is a whole\ntask: the second shift picks up mid-aisle exactly where the first left off.\n\nA DISPATCH-AFFECTING boundary is a different model, and it is `WorkDay` below, not this.\nDo not reach for `_stock(budget=)` as its seam even though an earlier version of this\ndocstring pointed there: it defers a whole WAVE and never truncates one, which is the\nwrong granularity for a cut that has to stop a picker mid-task \u2014 and it has no\nproduction caller, so it is an untested path as well.\n\nRaises on a non-positive shift, which would otherwise divide by zero or label every\ninstant shift 0 with no warning.",
      "is_async": false,
      "kind": "function",
      "signature": "(t_abs: float, shift_seconds: float=DEFAULT_SHIFT_SECONDS) -> int"
    },
    "Warehouse/kernel/timeline.py::shift_offset": {
      "decorators": [],
      "doc": "How far into its own shift the absolute instant `t_abs` is, in seconds.",
      "is_async": false,
      "kind": "function",
      "signature": "(t_abs: float, shift_seconds: float=DEFAULT_SHIFT_SECONDS) -> float"
    },
    "Warehouse/layout/Aisle_Dimensions.py": {
      "decorators": [],
      "doc": "Aisle_Dimensions.py \u2014 physical dimension helpers for warehouse aisle layout.\n\nAll sizes in \"storage units\" where:\n  - PALLET_WIDTH      = 48  (Pallet.max_width)\n  - PALLET_HEIGHT_MAX = 48  (Storage_Size extra_large height)\n  - SINGLETON_WIDTH   = 16  (Singleton.max_width)\n\nBin counts per aisle are derived from physical dimensions and unit type:\n  bins_per_row  = aisle_width  // unit_width      (x direction)\n  bins_per_col  = aisle_height // bin_height       (y direction, per size tier)",
      "kind": "module",
      "signature": null
    },
    "Warehouse/layout/Aisle_Dimensions.py::aisle_height_for": {
      "decorators": [],
      "doc": "Physical aisle height for *n_levels* of *reference_size* pallets.\n\nExample: aisle_height_for(30) \u2192 30 \u00d7 48 = 1440 physical units.",
      "is_async": false,
      "kind": "function",
      "signature": "(n_levels: int, reference_size: str='extra_large') -> int"
    },
    "Warehouse/layout/Aisle_Dimensions.py::aisle_width_for": {
      "decorators": [],
      "doc": "Physical aisle width for *n_pallet_columns* pallet columns.\n\nExample: aisle_width_for(25) \u2192 25 \u00d7 48 = 1200 physical units.",
      "is_async": false,
      "kind": "function",
      "signature": "(n_pallet_columns: int) -> int"
    },
    "Warehouse/layout/Aisle_Dimensions.py::bins_along_x": {
      "decorators": [],
      "doc": "Number of bins per row given *aisle_width* and *unit_type*.",
      "is_async": false,
      "kind": "function",
      "signature": "(aisle_width: int, unit_type: str) -> int"
    },
    "Warehouse/layout/Aisle_Dimensions.py::bins_along_y": {
      "decorators": [],
      "doc": "Number of bin rows given *aisle_height* and pallet *size* tier.",
      "is_async": false,
      "kind": "function",
      "signature": "(aisle_height: int, size: str) -> int"
    },
    "Warehouse/layout/Aisle_Dimensions.py::catalog_aisle_bins": {
      "decorators": [],
      "doc": "Bin count for a single-size aisle from an EXPLICIT bin footprint width + height.\n\nGeneralizes uniform_aisle_bins to bin families whose geometry isn't derived from\nunit_type (e.g. fulfillment tiers, which pass FULFILLMENT_BIN_WIDTH + FF_TIER_HEIGHTS).",
      "is_async": false,
      "kind": "function",
      "signature": "(bin_width: int, bin_height: int, aisle_width: int, aisle_height: int) -> int"
    },
    "Warehouse/layout/Aisle_Dimensions.py::uniform_aisle_bins": {
      "decorators": [],
      "doc": "Bin count for a single-size-tier aisle (every bin one storage_size).\n\nPallet aisles: n_cols (48-wide) \u00d7 n_rows (aisle_height // tier height).\nSingleton aisles: n_cols (16-wide) \u00d7 n_rows (aisle_height // 48).",
      "is_async": false,
      "kind": "function",
      "signature": "(unit_type: str, storage_size: str, aisle_width: int, aisle_height: int) -> int"
    },
    "Warehouse/layout/Aisle_Dimensions.py::unit_bin_width": {
      "decorators": [],
      "doc": "Physical width of one bin for *unit_type* ('pallet' or 'singleton').",
      "is_async": false,
      "kind": "function",
      "signature": "(unit_type: str) -> int"
    },
    "Warehouse/layout/Aisle_Storage.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.Bin": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.Bin.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisle: Aisle, bayX: int, bayY: int, storage_size: str, x_step: int, y_step: int, y_phys: int) -> None"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.Bin.location": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[int, int, int]"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.Bin.storage_handling_type": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[str, str]"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.Bin.x_phys": {
      "decorators": [
        "property"
      ],
      "doc": "Physical centre X position of this bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.Bin.y_phys": {
      "decorators": [
        "property"
      ],
      "doc": "Physical centre Y position of this bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.__init__": {
      "decorators": [],
      "doc": "Create a uniform-size aisle from physical dimensions.\n\nParameters\n----------\nstorage_size  : single size tier for all bins ('small', 'medium', etc.)\naisle_width   : physical width in storage units (e.g. 25 \u00d7 48 = 1200)\naisle_height  : physical height in storage units (e.g. 30 \u00d7 48 = 1440)\nbin_width     : explicit bin footprint width (fulfillment); None \u21d2 from unit_type\nbin_height    : explicit bin height (fulfillment); None \u21d2 from unit_type/SIZE_HEIGHTS",
      "is_async": false,
      "kind": "function",
      "signature": "(self, storage_size: str, handling_type: str, storage_type: str, unit_type: str, aisle_width: int, aisle_height: int, bin_width: int | None=None, bin_height: int | None=None) -> None"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.from_size_distribution": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Create a multi-size aisle from physical dimensions.\n\nEach size tier is allocated a proportional share of *aisle_height*.\nWithin each tier slice, bins stack at the tier's bin height, giving\nmore Y levels for small tiers and fewer for large tiers.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, storage_sizes: list[str], probabilities: list[float], handling_type: str, storage_type: str, unit_type: str, aisle_width: int, aisle_height: int) -> Aisle"
    },
    "Warehouse/layout/Aisle_Storage.py::Aisle.storage_handling_type": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[str, str]"
    },
    "Warehouse/layout/Storage_Primitive.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/layout/Storage_Primitive.py::FulfillmentBin": {
      "decorators": [],
      "doc": "Forward-pick bin for the FULFILLMENT regime \u2014 a small footprint with a few short\nsize tiers, sitting on ~6 ft shelves.\n\nDistinct ``unit_category`` so BinKey routes fulfillment items only into fulfillment\nbins (and store items never land here).  Reuses the pallet size-tier fit, but over\n``FulfillmentBin.TIERS`` instead of the pallet tiers.  Because every tier is short\n(<< 96\"), a fulfillment bin's ``y_phys`` always falls in the ground height bracket, so\nthe pick-time height multiplier is 1 automatically \u2014 no per-regime height config.\n\nGeometry here is a PLACEHOLDER until the fulfillment inventory profile is calibrated.",
      "kind": "class",
      "signature": "(Pallet)"
    },
    "Warehouse/layout/Storage_Primitive.py::FulfillmentBin._fit": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::FulfillmentCart": {
      "decorators": [],
      "doc": "Smaller tote-cart for the fulfillment channel \u2014 1/5 the store cart (50*50*10 = 25,000).\nThe reduced volume trips the cart-swap threshold more often, shifting fulfillment labor\ntoward the cart/route terms (see the pick-time model).",
      "kind": "class",
      "signature": "(StorageCart)"
    },
    "Warehouse/layout/Storage_Primitive.py::Pallet": {
      "decorators": [],
      "doc": "Standard pallet storage \u2014 full 48\u00d748 footprint.\n\nSubclasses (e.g. Singleton) may override max_width / max_length to model\nnarrower footprints while inheriting the same size-tier logic and gaining\na valid storage_size attribute from _fit().",
      "kind": "class",
      "signature": "(StorageUnit)"
    },
    "Warehouse/layout/Storage_Primitive.py::Pallet.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order, quantity: int) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::Pallet._fit": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::Singleton": {
      "decorators": [],
      "doc": "Forward-pick / small-footprint storage.\n\nSingleton bins are a single fixed size (no small/medium/large/extra_large\ntiers).  Any item whose two smallest dimensions both fit within 16\u00d716 can\nbe placed; items are rotated to minimise the stacking height.  storage_size\nis always 'singleton' \u2014 one bucket per (handling, category), distinct from\nall pallet size tiers and never None so DB NOT NULL constraints are satisfied.",
      "kind": "class",
      "signature": "(Pallet)"
    },
    "Warehouse/layout/Storage_Primitive.py::Singleton._fit": {
      "decorators": [],
      "doc": "Validate fit in 16\u00d716 footprint; set dimensions without size-tier logic.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart": {
      "decorators": [],
      "doc": "Base cart type: the picking container whose volume sets the cart-swap threshold.\n\nSubclasses model different cart sizes (see StoreCart / FulfillmentCart) by overriding\nthe dimensions; capacity() reads the volume off the *type* so config/sim code never has\nto instantiate a cart just to know how much it holds.  Add a new cart = new subclass.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart.add_from_bin": {
      "decorators": [],
      "doc": "Take up to `quantity` units of the bin's order into the cart.\n\nAssumes perfect packing \u2014 placement succeeds if order volume fits in\nremaining cart volume, regardless of orientation.\nReturns the number of units actually taken.\nClears bin_.storage when its quantity reaches zero.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, bin_: Aisle.Bin, quantity: int) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart.capacity": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Total cart volume (length * width * height), read off the type.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart.contents": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[tuple[Order, int]]"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart.remaining_volume": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageCart.total_volume": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": "(ABC)"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order, quantity: int) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit._fit": {
      "decorators": [
        "abstractmethod"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, order: Order) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit.height": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit.length": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit.stack_axis": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit.total_labor_cost": {
      "decorators": [
        "property"
      ],
      "doc": "Labor to pick this whole unit = quantity x the order's per-unit labor_cost.\nDerived from the order's precomputed labor_cost (set once per worker).",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/layout/Storage_Primitive.py::StorageUnit.width": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::Storage_Size": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Storage_Primitive.py::Storage_Size.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::Storage_Type": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Storage_Primitive.py::Storage_Type.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/layout/Storage_Primitive.py::StoreCart": {
      "decorators": [],
      "doc": "Standard picking cart for the store restock channel (50*50*50 = 125,000).",
      "kind": "class",
      "signature": "(StorageCart)"
    },
    "Warehouse/layout/Storage_Primitive.py::T": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "TypeVar('T', bound='StorageUnit')"
    },
    "Warehouse/layout/Storage_Primitive.py::_FIT_CACHE_MAX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "100000"
    },
    "Warehouse/layout/Storage_Primitive.py::_build_units": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order, unit_class: type[T], quantity: int) -> list[T]"
    },
    "Warehouse/layout/Storage_Primitive.py::_can_fit": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order, unit_class: type[T], qty: int) -> bool"
    },
    "Warehouse/layout/Storage_Primitive.py::_max_qty_fits": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order, unit_class: type[T]) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::_pallet_fit_dims": {
      "decorators": [],
      "doc": "Smallest-tier pallet fit for a geometry, or None if no orientation fits.\nReturns (storage_size, height, width, length, stack_axis).  Thin wrapper over\n_tiered_fit_dims with the store pallet tiers.",
      "is_async": false,
      "kind": "function",
      "signature": "(h: int, w: int, l: int, quantity: int, max_width: int, max_length: int)"
    },
    "Warehouse/layout/Storage_Primitive.py::_singleton_fit_dims": {
      "decorators": [
        "lru_cache(maxsize=_FIT_CACHE_MAX)"
      ],
      "doc": "Min-stacked-height singleton fit for a geometry, or None if no orientation fits.\nReturns (height, width, length, stack_axis).",
      "is_async": false,
      "kind": "function",
      "signature": "(h: int, w: int, l: int, quantity: int, max_width: int, max_length: int, bin_h: int)"
    },
    "Warehouse/layout/Storage_Primitive.py::_tiered_fit_dims": {
      "decorators": [
        "lru_cache(maxsize=_FIT_CACHE_MAX)"
      ],
      "doc": "Smallest-tier fit for a geometry over *tiers*, or None if no orientation fits.\n\n*tiers* is an ASCENDING ``((name, height), ...)`` tuple (hashable, so the lru_cache\nkeys on geometry + tier set).  Generalizes the pallet/fulfillment size-tier search so\neach bin family passes its own tiers instead of reading a single global tier map.\nReturns (storage_size, height, width, length, stack_axis).",
      "is_async": false,
      "kind": "function",
      "signature": "(h: int, w: int, l: int, quantity: int, max_width: int, max_length: int, tiers: tuple)"
    },
    "Warehouse/layout/Storage_Primitive.py::_total_volume": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list[StorageUnit]) -> int"
    },
    "Warehouse/layout/Storage_Primitive.py::viable_storage_units": {
      "decorators": [],
      "doc": "Pack *quantity* items using the minimum number of full pallets, with any\nremainder routed to a singleton unit.\n\nPrefers Singleton over Pallet when the total item volume fits within a\n'small' singleton slot (12 \u00d7 16 \u00d7 16 physical units), since singletons use\na narrower footprint that leaves full-size pallet locations free.\n\nOtherwise, packs complete pallets at maximum capacity then routes the\nremainder to a singleton.  Falls back to a partial pallet for the remainder\nif the order cannot be singletonised.\n\nExamples  (max_per_pallet = 3, small_slot_vol = 3072)\n-------------------------------------------------------\n  volume*qty \u2264 3072  \u2192  1 singleton \u00d7 qty items  (forward-pick preferred)\n  quantity = 9       \u2192  3 pallets \u00d7 3 items,  no singleton\n  quantity = 10      \u2192  3 pallets \u00d7 3 items,  1 singleton \u00d7 1 item\n  quantity = 2       \u2192  0 pallets,             1 singleton \u00d7 2 items",
      "is_async": false,
      "kind": "function",
      "signature": "(order: Order, quantity: int) -> list[StorageUnit]"
    },
    "Warehouse/layout/Warehouse_Builder.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/layout/Warehouse_Builder.py::AisleConfig": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisles: list[Aisle], name: str='') -> None"
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse.bins": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[Aisle.Bin]"
    },
    "Warehouse/layout/Warehouse_Builder.py::WarehouseConfig": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse_Builder": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse_Builder.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse_Builder.add_aisle": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, storage_size: str, handling_type: str, storage_type: str, unit_type: str, aisle_width: int, aisle_height: int, bin_width: int | None=None, bin_height: int | None=None) -> 'Warehouse_Builder'"
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse_Builder.add_aisle_from_distribution": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, storage_sizes: list[str], probabilities: list[float], handling_type: str, storage_type: str, unit_type: str, aisle_width: int, aisle_height: int) -> 'Warehouse_Builder'"
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse_Builder.build": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> Warehouse"
    },
    "Warehouse/layout/Warehouse_Builder.py::Warehouse_Builder.from_config": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, config: WarehouseConfig) -> 'Warehouse_Builder'"
    },
    "Warehouse/layout/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.layout \u2014 Physical geometry: storage units and carts, aisles and their bins, aisle sizing, and the warehouse builder.  Storage_Primitive and Aisle_Storage import each other and must stay co-located.\n\nSee README.md for what does and does not belong here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/operations/__init__.py": {
      "decorators": [],
      "doc": "operations \u2014 WHO does the work: roles, modes, workers, crews.\n\nThe domain's actor model plus the operations themselves, split out from the pick simulation\nso a second work stream can reuse it.  Imports the kernel and the VALUE layers (`wh_layout`,\n`wh_catalog`) but none of the machinery that consumes them -- not the pick simulation, the\nplacement engine, the assignment functions or the run harness -- which is what let the\ntop-level `Inbound/` package build its crew of unloaders on these same layers with no\ndependency inversion.  (It landed beside `Warehouse/`, not inside it as this line once\npredicted, and took `unload.py` and `inbound.py` -- now `pack.py` -- with it.)\n\n    Role     pick | put\n    Mode     foot | machine          -- the axis that decides speed\n    Worker   uid + local_id + role + mode + SpeedProfile\n    Crew     a pool of identical workers, and the uid allocator across pools\n\nThe speed VALUE object (`SpeedProfile`) deliberately lives in `Warehouse/kernel/cost_model`,\nnot here: `cost_model` and the placement scorers consume speeds, and `architecture.yml`\nforbids `wh_kernel -> *`, so the kernel cannot import this package.  The value lives in the\nkernel; whose value it is lives here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/operations/__init__.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['Mode', 'Role', 'Crew', 'Worker']"
    },
    "Warehouse/operations/putaway.py": {
      "decorators": [],
      "doc": "putaway \u2014 what a put-away COSTS, in the same seconds the pick clock counts.\n\nPut-away has been a zero-duration phase for the life of the project: `check_reorders` \u2192\n`_stock` \u2192 `_execute_placement` move inventory and produce no time value anywhere. So a\nstrategy that defers placement looked free, and a second crew had nothing to be measured\nin.\n\nThis gives it a cost, deliberately mirroring the PICK model rather than inventing a second\none \u2014 travel to the location plus the same height-bracketed handling expression, through\nthe same `cost_model` primitives. That is the honest starting point: put-away and picking\nare the same physical act with the inventory moving the other way, and a model that shares\nits shape is one that can be re-tuned against the pick model rather than drifting from it.\n\n## What this deliberately does NOT model\n\nThe user's own framing for this phase: *\"do what you can to align puts stream but I didn't\nreally model out that intended behavior yet\u2026 it will be changing.\"* So:\n\n- **No contention.** A putter and a picker never wait for each other, an aisle holds any\n  number of actors, and put-away time does not move the pick clock by one second. The two\n  streams are simulated independently and merged, which is the same assumption\n  `fast_pick` already makes across its own pickers.\n- **No travel between placements.** Each put is costed from the aisle mouth, exactly as a\n  pick's aisle-entry travel is. A real putter carries a pallet down a lane and drops\n  several; that is a routing problem, and routing put-away is the feature, not the seam.\n- **No dock, no trailer, no unload.** `_stock(budget=)` bounds how much a crew places in a\n  tick and is where those will attach.\n\nBecause it adds a measurement without perturbing anything, turning it on changes no pick\nresult: the same items are picked, in the same order, at the same instants. It adds rows\nand a number.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/operations/putaway.py::PutawayCost": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "The put-away time model's coefficients \u2014 the put-side twin of `PickConfig`'s.\n\nPICKING'S SHAPE, PICKING'S COEFFICIENTS, TWO DECLARED SCALARS.  Every value here is the\npick model's BY REFERENCE \u2014 the kernel's one literal set (`cost_model.DEFAULT_PICK_*`),\nthe same declaration `PickConfig` reads \u2014 and the only way a put crew's numbers may\ndiffer from a picker's is through the two scales the calibrated era declares:\n\n    intercept = picking's \u00d7 PUT_INTERCEPT_SCALE   (\"putting is less work\", default 0.5)\n    per_item  = picking's \u00d7 PUT_ITEM_RATIO        (default 0.2, i.e. 0.1 s per unit)\n\nThe fields hold the EFFECTIVE (already-scaled) values, so `put_cost` stays one call to\nthe shared primitive and the object is self-describing.  `from_pick` is how a run\nbuilds one from its own `PickConfig` (or `WorkloadParams`) and the run's scales; the\ndataclass defaults are what that yields for the kernel defaults.  There is deliberately\nno second literal set: this project has already paid for two default sets that drifted\n55x apart, and the by-reference link is what makes the drift structurally impossible.\n\nThis module may not import the pick simulation (architecture.yml forbids\nwh_operations -> wh_picking), which is why the shared declaration lives in the kernel.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/operations/putaway.py::PutawayCost.from_pick": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Build the put crew's cost from a pick config's coefficients (duck-typed: a\n`PickConfig` or a `WorkloadParams`, anything with the `pick_*` attributes).\n\nTHE constructor the run harness uses, so a registered pick config's intercept\n(15 s on the store arms, 10 s on fulfillment) actually reaches the put crew \u2014 the\nclass defaults alone would price every put at the kernel's 1.0 while the pickers\nbeside it were charged 15.  The scales are the run's declared knobs\n(`settings.PUT_INTERCEPT_SCALE` / `PUT_ITEM_RATIO`), carried in the worker payload.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, cfg, *, intercept_scale: float=DEFAULT_PUT_INTERCEPT_SCALE, item_ratio: float=DEFAULT_PUT_ITEM_RATIO) -> 'PutawayCost'"
    },
    "Warehouse/operations/putaway.py::put_cost": {
      "decorators": [],
      "doc": "What the simulation BILLS for one put \u2014 `put_seconds_at` with the handling term in.\n\nKept as its own name because it is the billing call and reads as one at every call site\n(`Inventory_Manager._cost_putaway`), and because `cost` is not optional here: a put that\nis charged always pays for the handling.",
      "is_async": false,
      "kind": "function",
      "signature": "(x_phys: float, y_phys: float, weight: float, volume: float, quantity: int, speed: SpeedProfile, cost: PutawayCost) -> float"
    },
    "Warehouse/operations/putaway.py::put_seconds_at": {
      "decorators": [],
      "doc": "Seconds to put `quantity` units away at `(x_phys, y_phys)` \u2014 the ONE expression.\n\nTravel from the aisle mouth plus height-bracketed handling \u2014 the same two terms, in the\nsame order, through the same primitives as `Pick._pick_time` and `Workload`'s P-term:\n\n    travel + M(y) \u00b7 (intercept + quantity \u00b7 per_item + quantity \u00b7 var)\n\nReturns SECONDS, the unit the whole simulator counts in\n(`Warehouse.kernel.timeline.TIME_UNIT`).\n\n## Two callers price the same physical act, and one of them drops a term\n\n`put_cost` below BILLS a put; `Inbound.gain._Evaluator._cost_at` OPTIMISES where to put\nit.  Until 2026-09-17 the objective restated this formula and kept only `travel` \u2014 no\nintercept, no per-item charge, and no HEIGHT MULTIPLIER, which is bin-dependent and\ntherefore does not cancel out of a difference between two candidate bins.  The\nsimplification was recorded in prose (`gain.py`'s header: \"put travel is paid once at\nthe put crew's speeds\") and nowhere in the code, so **whether it was a decision or drift\nwas undecidable from the tests** \u2014 and the put formula moved three times in two months\n(ADR-0001's per-item charge, the one-clock refactor, ADR-0003).\n\n`cost=None` is that drop, made VISIBLE: the handling term is not merely unwritten, it is\nrefused at the call site, by name, where a reader can see it and a test can pin it.\n`Tests/unit/test_put_seconds_at.py` asserts the exact relationship between the two\nreadings, so a change to either half now has somewhere to fail.\n\n`Inbound/unload.py`'s `UnloadCost.from_putaway` makes the same argument for the\nreceiving twin: a reference makes the drift \"structurally impossible rather than merely\na diff someone might notice\".",
      "is_async": false,
      "kind": "function",
      "signature": "(x_phys: float, y_phys: float, *, speed: SpeedProfile, cost: 'PutawayCost | None'=None, weight: float=0.0, volume: float=0.0, quantity: int=0) -> float"
    },
    "Warehouse/operations/roles.py": {
      "decorators": [],
      "doc": "roles \u2014 what an actor DOES and what it does it ON.\n\nTwo axes, deliberately separate, because they vary independently: a `pick` can be done on\n`foot` or on a `machine`, and so can a `put`.  Four combinations, each with its own travel\nspeed \u2014 which is the whole reason this file exists.\n\nThe mode axis is not new; it has just never been readable.  `Optimization/config/channels.py`\nhas named its two pools `'store_machine'` and `'fulfillment_walker'` for as long as they have\nexisted, and `simconfig/constants.py` comments them as \"machine order-picker pool\" and \"human\n-walker pool\".  Nothing anywhere reads either name: no forklift, machine, vehicle or equipment\nconcept existed in the repo.  The distinction was real, documented in prose, and inert.\n\n## Why `str` enums\n\nA role and a mode are written into a DB column and read back, and they appear in run\nmetadata.  `str`-valued members compare equal to their own text, so a stored `'machine'`\nround-trips without a conversion layer, and an f-string renders the value rather than\n`Mode.MACHINE`.  The cost is that any string comparison silently succeeds \u2014 hence\n`Role.of` / `Mode.of` below, which are the only sanctioned way to turn text back into a\nmember and which raise on anything unrecognised.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/operations/roles.py::Mode": {
      "decorators": [],
      "doc": "How the actor moves: on foot, or on a machine.\n\nThe axis that decides speed.  A machine drives faster along an aisle than a person\nwalks and lifts differently up a rack \u2014 and because x (horizontal run) and y (vertical\nlift) are separate quantities, a machine is a DIFFERENT `SpeedProfile` from a walker,\nnot a scaled one.",
      "kind": "class",
      "signature": "(_Named)"
    },
    "Warehouse/operations/roles.py::Role": {
      "decorators": [],
      "doc": "What the actor is doing with inventory: taking it out, putting it away, or\nbringing it in.\n\nTHE VALUE IS THE DB COLUMN.  `work_events.role` is free TEXT with no CHECK, written as\n`str(w.role)`, and nothing calls `Role.of` on the way in -- so a typo persists silently\nand every `WHERE role='receive'` returns nothing while the row count still reconciles.\nThat is why these are an Enum at all.\n\nOne consequence of the SPELLING, recorded because it is invisible and easy to\n\"improve\": the merged event stream's declared order breaks ties on `role` as a STRING,\nand the batch epoch is a real tie in every run.  `'pick' < 'put' < 'receive'`, so a\nreceive sorts LAST at a shared instant.  Renaming it to `'dock'` or `'inbound'` would\nsilently move it FIRST, ahead of every pick -- and no test would notice, because Python's\n`sorted` and SQLite's BINARY collation agree under any name.  The order is arbitrary and\nmust stay uninteresting; do not rename one of these to get a sort you like.",
      "kind": "class",
      "signature": "(_Named)"
    },
    "Warehouse/operations/roles.py::_Named": {
      "decorators": [],
      "doc": "Shared behaviour: render as the bare value, and parse strictly.",
      "kind": "class",
      "signature": "(str, Enum)"
    },
    "Warehouse/operations/roles.py::_Named.__str__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/operations/roles.py::_Named.of": {
      "decorators": [
        "classmethod"
      ],
      "doc": "Parse `value` into a member, raising on anything unrecognised.\n\nThe strict counterpart to being a `str` enum.  Reading a role out of a DB gives you\ntext, and `'machien' == Role.PICK` is simply False rather than an error \u2014 so a typo\nin stored data would quietly route an actor nowhere.  Every text\u2192member conversion\ngoes through here.",
      "is_async": false,
      "kind": "function",
      "signature": "(cls, value)"
    },
    "Warehouse/operations/worker.py": {
      "decorators": [],
      "doc": "worker \u2014 one actor, and a pool of them, with the two id spaces kept apart.\n\n## The two id spaces, and why there are two\n\nEvery existing reader assumes picker ids are **dense and per-crew**: `assign_tasks`\npartitions work into `num_pickers` buckets and the bucket index IS the id;\n`Simulation_Analytics._group_events_by_picker` allocates exactly `k_pickers` lists and now\nRAISES on anything outside `[0, k)`; `_ProgressAPIMixin.progress_at` enumerates\n`range(num_pickers)`; and the `picker_events` table and the viewer are built on the same\nassumption.\n\nA second crew breaks that the moment it shares an event stream \u2014 its actors carry ids from\na different space, and the old `0 <= pid < k_pickers` filter used to swallow them silently.\n\nSo a `Worker` carries both:\n\n  `local_id`  dense within its own crew, `0..size-1`.  This is what goes to `picker_events`\n              and to everything that predates a second crew.  It is NOT unique across crews.\n  `uid`       unique across every crew in the run.  This is what goes to the merged event\n              stream, beside a role and a mode, so a row can say WHO without ambiguity.\n\nKeeping them as two named fields rather than one clever encoding is the point: a `uid` that\nleaked into a `local_id` slot would land inside `[0, k)` and be accepted.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/operations/worker.py::Crew": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "A pool of identical workers: one role, one mode, one speed, a size.\n\nThis is what `Optimization/config/channels.PickerProfile` has always been -- a name, a\ncost profile carrying travel speeds, and a count -- with the two things it was missing\nmade explicit, and with no dependency on the run harness so a second stream can reuse it.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/operations/worker.py::Crew.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/operations/worker.py::Crew.next_uid": {
      "decorators": [],
      "doc": "The first uid a crew allocated AFTER this one may use.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, first_uid: int=0) -> int"
    },
    "Warehouse/operations/worker.py::Crew.workers": {
      "decorators": [],
      "doc": "This crew's workers, `local_id` 0..size-1 and `uid` running from `first_uid`.\n\nThe caller owns uid allocation across crews \u2014 pass the previous crew's\n`first_uid + size` \u2014 because only the caller knows how many crews there are.  See\n`next_uid`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, first_uid: int=0) -> tuple[Worker, ...]"
    },
    "Warehouse/operations/worker.py::Worker": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One actor: who it is, what it does, how it moves, and how fast.\n\nFrozen and picklable \u2014 a crew is built in the parent and crosses the spawn boundary in\nthe worker payload, like every other value object here.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/operations/worker.py::Worker.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/operations/worker.py::Worker.kind": {
      "decorators": [
        "property"
      ],
      "doc": "`'pick/machine'` \u2014 the (role, mode) pair as one label, for an event row.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/physical.py": {
      "decorators": [],
      "doc": "physical.py \u2014 the two physical footprint constants (zero-dependency leaf).\n\nSingle source for the numbers that used to be repeated as literals across\nOrder (dim clamps), Storage_Primitive (unit footprints + tallest pallet tier),\nand the docs:\n\n  * PALLET_FOOTPRINT  \u2014 pallet max width/length AND the tallest pallet-tier\n    height (extra_large), in storage units (inches).\n  * COMPACT_FOOTPRINT \u2014 the small forward-pick footprint shared by the store\n    Singleton and the fulfillment bin.\n\nKept in a leaf module (imports nothing) so both Order and Storage_Primitive can\nread it without a cycle (Storage_Primitive imports Order).",
      "kind": "module",
      "signature": null
    },
    "Warehouse/picking/Pick.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/picking/Pick.py::PickConfig": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Pick.py::PickConfig.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/picking/Pick.py::PickConfig.speed": {
      "decorators": [
        "property"
      ],
      "doc": "This picker's travel speeds as one value, ft/s converted to s/inch once.\n\nA PROPERTY, not a field, and deliberately: `x_speed`/`y_speed` are a public\nserialization contract.  `run_map_precompute` rebuilds a `PickConfig` from an\nARCHIVED `config.json` by filtering `dataclasses.fields`, and `docs/macros.py` reads\n`c[\"x_speed\"]` to render the published pick-time formula.  Both would fall back to\ndefaults SILENTLY if the fields were replaced \u2014 and no canary would catch it,\nbecause none of them reads an archived config.\n\nSo the pair stays exactly where it is and this is the derived view of it, for the\ncallers that want to hoist one object instead of two floats.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> SpeedProfile"
    },
    "Warehouse/picking/Pick.py::PickEvent": {
      "decorators": [
        "dataclass(slots=True)"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Pick.py::PickEvent.__lt__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, other: PickEvent) -> bool"
    },
    "Warehouse/picking/Pick.py::PickSimulation": {
      "decorators": [],
      "doc": "Simulate multiple pickers processing a set of Tasks in aisle order.\n\nTasks are sorted by aisle_id and distributed to pickers round-robin so that\neach picker works through their assigned aisles in order.",
      "kind": "class",
      "signature": "(_ProgressAPIMixin)"
    },
    "Warehouse/picking/Pick.py::PickSimulation.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, tasks: list[Task], config: PickConfig, manager: Inventory_Manager | None=None, start_times: list[float] | None=None, day_end: float | None=None) -> None"
    },
    "Warehouse/picking/Pick.py::PickSimulation._simulate_picker": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, picker_id: int, tasks: list[Task], picks: list[tuple[int, int]], empties: list['Aisle.Bin'], t0: float=0.0, day_end: float | None=None, residue: dict | None=None, unmet: dict | None=None) -> list[PickEvent]"
    },
    "Warehouse/picking/Pick.py::PickSimulation.run": {
      "decorators": [],
      "doc": "Simulate all pickers and return all events sorted by time.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[PickEvent]"
    },
    "Warehouse/picking/Pick.py::PickerProgress": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Pick.py::PickerProgress.__repr__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> str"
    },
    "Warehouse/picking/Pick.py::_ProgressAPIMixin": {
      "decorators": [],
      "doc": "Post-run progress inspection shared by BOTH pick simulations.\n\nprogress_at/step_table/_state_at were verbatim duplicates in PickSimulation\nand DeferredPickSimulation (cosmetic drift only).  They read nothing but\nself._events (set by run()) and self._config.num_pickers, so one mixin\nserves both.  NOT part of the numeric sim path - the picker loops stay\ndeliberately separate (deferred-mutation contract).",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Pick.py::_ProgressAPIMixin._start_at": {
      "decorators": [],
      "doc": "Picker `picker_id`'s clock at the start of this batch.\n\n`0.0` unless the caller supplied `start_times`, so every construction that does not\ntrack clocks -- tests, Diagnostics, the calltree harness -- keeps the old model where\nevery picker is reborn at zero each batch.  The runner threads the previous batch's\nfinish times through instead, which is what puts a whole run's events on one axis.\n\nA short list is tolerated and reads 0.0 past its end: a crew can grow between\nbatches, and a new picker starting at the origin is the honest answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, picker_id: int) -> float"
    },
    "Warehouse/picking/Pick.py::_ProgressAPIMixin._state_at": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, picker_id: int, t: float) -> PickerProgress"
    },
    "Warehouse/picking/Pick.py::_ProgressAPIMixin.progress_at": {
      "decorators": [],
      "doc": "State of every picker at time t. run() must be called first.\n\nEnumerates `range(num_pickers)`, so it reports the PICK crew and only the pick\ncrew.  Any other actor sharing the event stream \u2014 a putter, an unloader \u2014 is\ninvisible here no matter what id it carries, which is the same per-crew, dense\nid space `Simulation_Analytics._group_events_by_picker` enforces, and the reason\na second stream records to its own table rather than widening `picker_events`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, t: float) -> list[PickerProgress]"
    },
    "Warehouse/picking/Pick.py::_ProgressAPIMixin.step_table": {
      "decorators": [],
      "doc": "Progress snapshots at regular time steps until all pickers are done.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, step: float=1.0) -> list[list[PickerProgress]]"
    },
    "Warehouse/picking/Pick.py::_pick_time": {
      "decorators": [],
      "doc": "The at-location HANDLING time to pick `quantity` units of one SKU from one bin:\n\n    M(y) \u00b7 (pick_intercept + quantity \u00b7 pick_per_item + quantity \u00b7 var)\n\nONE pick LINE \u2014 one bin visit for one SKU.  The intercept is charged once for the line,\nthe per-item charge once per unit, and `var` (the log-linear weight/volume term) once\nper unit.  `cost_model.per_pick` is the formula; this passes the picker's coefficients.\n\nweight and volume must be \u2265 1; values of 0 would cause math.log(0) which\nraises ValueError.  Clamp both to 1 as a safety floor \u2014 a zero-weight or\nzero-volume order is physically impossible and indicates bad data.\n\ny_phys is the bin's physical height; the height bracket factor scales the ENTIRE\nat-location pick operation \u2014 the intercept, the per-item charge AND the per-unit\nweight/volume handling (equipment is slower at everything up high).  y_phys defaults\nto 0 (ground bracket \u2192 factor 1.0) so callers without a bin are unaffected.\n\nThe cart-swap penalty (cfg.cart_swap_coef) is NOT part of this handling term \u2014 the sim loops\ncharge it as its own timed step at the swap and the decomposition attributes it to travel.",
      "is_async": false,
      "kind": "function",
      "signature": "(cfg: PickConfig, weight: int, volume: int, quantity: int, y_phys: float=0.0) -> float"
    },
    "Warehouse/picking/Pick.py::_task_static": {
      "decorators": [],
      "doc": "A task's assignment-INDEPENDENT time (travel + handling, NO cart) plus its ordered pick-volume\nsequence \u2014 the inputs the LPT scheduler balances.  Replicates PickSimulation._simulate_picker's\nper-task arithmetic exactly (position resets to (0,0) each aisle; one-way exit to the far end);\nthe cart term is session-persistent so it is added separately via count_cart_swaps.  Kept in\nlockstep with the sim by the scheduler\u2194sim self-consistency test.\n\nNOTE: it reads the UNCAPPED ``task.items[sku]``, matching PickSimulation; fast_pick's deferred\nloop can cap a pick at the snapshot stock under cross-picker contention, so predicted==realized is\nexact for PickSimulation and for the normal disjoint well-stocked aisles, but can differ for a bin\nunder-stocked relative to demand under DeferredPickSimulation.",
      "is_async": false,
      "kind": "function",
      "signature": "(task: Task, cfg: PickConfig, x_pace: float, y_pace: float) -> tuple[float, list]"
    },
    "Warehouse/picking/Pick.py::assign_tasks": {
      "decorators": [],
      "doc": "Partition aisle-Tasks across ``cfg.num_pickers`` pickers.  ``sorted_tasks`` MUST be aisle_id\nsorted (the sim processes each picker's tasks in that order).  The SINGLE task\u2192picker assignment\nused by both PickSimulation and fast_pick, so they cannot drift.\n\n- ``'round_robin'`` (default): ``i % num_pickers`` \u2014 byte-identical to the legacy scheduler.\n- ``'lpt'``: LPT \u2014 visit tasks **heaviest-first** and append each to the **least-loaded** picker.\n  Both use a per-task cost ``est(t) = (travel+handling) + swap_coef \u00b7 task_volume / cart_cap`` \u2014\n  a *continuous* cart proxy that is monotone in volume, so it can't be fooled by the cart\n  step-function the way an exact-but-myopic greedy is (a light task looking free on a\n  cart-favourable picker).  Heaviest-first + least-loaded is a standard makespan heuristic \u2014 but\n  note it is NOT a guaranteed bound on the *realized* step-function makespan and can, on adverse\n  inputs, do no better than round-robin; it never yields an invalid partition and total work is\n  unchanged, so results stay correct regardless.  Each picker's tasks are then kept in aisle_id\n  order \u2014 exactly the order the sim picks them \u2014 so the **realized** makespan (and the cart swaps\n  that dominate it) is computed exactly by the shared next-fit; only the *balancing decisions* use\n  the smooth proxy.  Runs once per batch in ``__init__`` at O(total_picks + tasks\u00b7pickers).",
      "is_async": false,
      "kind": "function",
      "signature": "(sorted_tasks: list, cfg: PickConfig) -> list"
    },
    "Warehouse/picking/Pick.py::assign_tasks._est": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(t)"
    },
    "Warehouse/picking/Pick.py::carry_residue": {
      "decorators": [],
      "doc": "Fold a task's UNDONE planned work into `into`, as `{sku: qty}`.\n\nTHE ONE DEFINITION OF THE CARRY, imported by both picker loops so they cannot drift.\nA day-cut carry is the PLANNED quantity at every bin the picker did not reach --\n`task.planned[k]` for k >= start -- and deliberately not a residual computed later as\n\"demand minus picked\".  Two definitions of one number is how a carry becomes either dead\ncode or a double count, and the consumer must read this rather than re-deriving it.\n\nThe plan, not what the bins now hold: the next batch re-derives its own bins, and\nclamping here would forgive demand a restock is about to satisfy.\n\nA bin whose storage is gone contributes nothing -- that quantity was never pickable from\nthere, and it is the live-stock clamp's business (`unpicked_unavailable`) rather than the\ncut's (`unpicked_daycut`).  Keeping the two causes apart is the whole point of the\n`carryover.reason` column.",
      "is_async": false,
      "kind": "function",
      "signature": "(task, start: int, into: dict) -> dict"
    },
    "Warehouse/picking/Pick.py::count_cart_swaps": {
      "decorators": [],
      "doc": "Next-fit cart swaps for an ORDERED sequence of pick volumes, starting from ``cart_remaining``.\nReturns ``(swaps, new_remaining)``.  Uses the same ``cart_step`` primitive the sim runs per pick,\nso this reproduces exactly what the sim charges for those picks.\n\nThis is the **exact-makespan predictor** \u2014 a partition's realized cart cost, computable up front.\nThe LPT scheduler does NOT balance on this; it balances a cheaper continuous proxy (see\n``assign_tasks``).  This is the oracle the scheduler\u2194sim self-consistency test checks the sim\nagainst, plus a reusable helper for scoring a partition's exact makespan.",
      "is_async": false,
      "kind": "function",
      "signature": "(pick_volumes, cart_remaining: float, cart_cap: float) -> tuple[int, float]"
    },
    "Warehouse/picking/Workload_Builder.py": {
      "decorators": [],
      "doc": null,
      "kind": "module",
      "signature": null
    },
    "Warehouse/picking/Workload_Builder.py::Batch": {
      "decorators": [],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Workload_Builder.py::Batch.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, config: BatchConfig, inventory: Inventory, affinity: AffMatrix | AffinityStore | None=None, rng: random.Random | None=None) -> None"
    },
    "Warehouse/picking/Workload_Builder.py::BatchConfig": {
      "decorators": [
        "dataclass"
      ],
      "doc": null,
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Workload_Builder.py::Task": {
      "decorators": [],
      "doc": "Single-aisle ordered pick sequence derived from a Batch.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Workload_Builder.py::Task.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, aisle_id: int, path: list[Aisle.Bin], items: dict[int, int], cart: type[StorageCart]=StoreCart, bin_qty: dict[int, int] | None=None) -> None"
    },
    "Warehouse/picking/Workload_Builder.py::Task.from_batch": {
      "decorators": [
        "staticmethod"
      ],
      "doc": "Decompose a Batch into one Task per aisle.\n\n`_shortfall`, when given, accumulates `{sku: units_short}` for demand no bin could\nsatisfy.  Underscored because callers should reach for `from_batch_with_shortfall`\nrather than pass their own dict; it exists so one drain serves both entry points.\n\nFor each SKU in the batch, bins are drained SMALLEST-ON-HAND FIRST, ties by\n`location`, across both tiers as one ordering \u2014 see `drain_sku`, which both\nbranches below call.  Singletons were drained before pallets until ADR-0003; the\nforward-pick preference no longer decides pick order.\n\nIf manager is provided its pre-built _sku_singleton_bins/_sku_pallet_bins\ndicts are used directly, skipping the O(N_all_bins) warehouse scan that\nwould otherwise rebuild the index on every batch.\n\nDETERMINISM \u2014 the manager index is `dict[int, set[Aisle.Bin]]`, and `Aisle.Bin`\ndefines no `__hash__`/`__eq__`, so it hashes by identity and a set iterates in\nMEMORY-ADDRESS order.  Whenever a SKU has more on-hand bins than the batch\nquantity drains \u2014 the normal case \u2014 that order decides WHICH bins are taken,\nhence which aisles, tasks, travel and depletion follow.  Under spawn + ASLR the\naddresses differ per process, so two identical-seed runs of the same arm used to\nproduce different `batch_stats`.  Iterating in `location` order \u2014 the canonical\n(aisle_id, bayX, bayY) spatial key persisted everywhere \u2014 makes the selection a\npure function of warehouse state.  Cost is O(k log k) on k bins of ONE SKU; see\n`Tests/unit/test_task_bin_selection_determinism.py`.",
      "is_async": false,
      "kind": "function",
      "signature": "(batch: Batch, warehouse: Warehouse, manager=None, cart: type[StorageCart]=StoreCart, _shortfall: dict[int, int] | None=None) -> list[Task]"
    },
    "Warehouse/picking/Workload_Builder.py::Task.from_batch_with_shortfall": {
      "decorators": [
        "staticmethod"
      ],
      "doc": "`from_batch`, plus the demand it could NOT satisfy: `{sku: units_short}`.\n\nA second entry point rather than a wider return type: `from_batch` has ~30 call\nsites and none of them want the extra value, so churning all of them for no\nbehaviour change is the more expensive mistake.  One drain serves both, so the\ntasks are identical either way.\n\nTHE SHORTFALL IS PRE-SIMULATION.  It is demand this batch could not reach on the\nshelf -- not demand a picker ran out of time for.  Those are different failures\nwith different fixes, and only this one is knowable before the sim runs.\n\nUntil now it was a loop-local named `remaining`, overwritten on the next SKU.  So\nan arm that placed nothing for a SKU and an arm that placed 500 of it produced\nindistinguishable records, and a rule that fails to pick looked cheap.",
      "is_async": false,
      "kind": "function",
      "signature": "(batch: Batch, warehouse: Warehouse, manager=None, cart: type[StorageCart]=StoreCart) -> tuple[list[Task], dict[int, int]]"
    },
    "Warehouse/picking/Workload_Builder.py::_Fenwick": {
      "decorators": [],
      "doc": "Prefix-sum tree over non-negative weights: O(log n) point-set and prefix-search.\n\nBacks the v2 sampler only.  Pure Python by choice: at 160k SKUs the whole v2 draw\nis ~0.5s (vs v1's 21.6s), so numpy adds nothing but a second float-grouping story.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Workload_Builder.py::_Fenwick.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, weights: list) -> None"
    },
    "Warehouse/picking/Workload_Builder.py::_Fenwick.find": {
      "decorators": [],
      "doc": "Smallest 0-based index whose prefix sum reaches `u` (the weighted draw).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, u: float) -> int"
    },
    "Warehouse/picking/Workload_Builder.py::_Fenwick.set": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int, value: float) -> None"
    },
    "Warehouse/picking/Workload_Builder.py::_Fenwick.total": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/picking/Workload_Builder.py::_SAMPLERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'v1': _lift_weighted_sample, 'v2': _lift_weighted_sample_v2, 'v3': _lift_weighted_sample_v3}"
    },
    "Warehouse/picking/Workload_Builder.py::_SegTree": {
      "decorators": [],
      "doc": "Sum tree over non-negative weights in which every internal node is RECOMPUTED from\nits two children \u2014 never adjusted by a delta.  O(log n) point-set and prefix-search.\n\nBacks the v3 sampler.  The difference from `_Fenwick` is the entire reason v3 exists.\nA Fenwick node is maintained as `t[j] += (new - old)`, so removing a weight that is\norders of magnitude larger than the other weights aggregated into that node ANNIHILATES\nthem (``1e19 + 1e4 == 1e19`` in float64) and the node keeps the difference as phantom\nmass.  Measured on the reference pair's fulfillment section, where the conditional-demand\nmodel's multiplicative lift (values 2.8\u20135.0 over a median 35 partners) drives `lift_mult`\nto ~1e20 against base frequencies of ~1e-6: the Fenwick's total ran **14.6% above** the\ntrue sum of its own leaves, and 8.64% of draws landed on SKUs already selected.\n\nRecomputing a node from its children instead makes each node exactly `fl(left + right)`\nof the weights that are live NOW, so no residue can survive a removal \u2014 the same property\nv1 gets by rebuilding its cumsum every draw, at O(log n) per update instead of O(n).\n\n`find` descends only into subtrees carrying strictly positive mass, so the leaf it\nreturns ALWAYS has a positive weight.  That is a structural guarantee, not a tolerance:\nan already-drawn SKU (set to 0.0) is unreachable however large the float error grows.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/Workload_Builder.py::_SegTree.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, weights: list) -> None"
    },
    "Warehouse/picking/Workload_Builder.py::_SegTree.find": {
      "decorators": [],
      "doc": "0-based index of the leaf the weighted draw `u` lands on.\n\nBranches right only when the right subtree actually holds mass, so every step stays\ninside a positively-weighted subtree and the returned leaf is guaranteed live.  This\nalso makes the call TOTAL: a `u` at or above the root total (`random.uniform` can\nreturn its upper bound) walks to the last live leaf instead of off the end.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, u: float) -> int"
    },
    "Warehouse/picking/Workload_Builder.py::_SegTree.set": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int, value: float) -> None"
    },
    "Warehouse/picking/Workload_Builder.py::_SegTree.total": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> float"
    },
    "Warehouse/picking/Workload_Builder.py::_bin_qty_location": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(bin_)"
    },
    "Warehouse/picking/Workload_Builder.py::_get_partner_map": {
      "decorators": [],
      "doc": "Build sku -> [(partner_sku, lift), ...] from either a dict AffMatrix\n{(i, j): lift} or an AffinityStore (CSR matrix).  Cached per affinity-object id\nso the O(|affinity|) build is paid once per run.  `None` \u21d2 {} (pure demand weighting).",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity) -> dict[int, list[tuple[int, float]]]"
    },
    "Warehouse/picking/Workload_Builder.py::_lift_weighted_sample": {
      "decorators": [],
      "doc": "Sample k distinct items from candidates without replacement.\n\nWeight of a candidate B is the conditional-demand model\n    weight(B) = demand.relative_frequency(B) \u00b7 \u03a0 lift(A, B)\nover the already-selected partners A of B.  Lift enters MULTIPLICATIVELY (its\nnatural sense: lift = 1 = independence \u21d2 no change), so demand and affinity stay\ncommensurable.  affinity=None \u21d2 pure demand weighting (empty partner map).\n\nUses a numpy cumsum draw per step + the module-level partner-map cache.  Pass `rng`\n(a `random.Random`) to draw from a dedicated stream; default `None` uses the global\nmodule.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates: list, k: int, affinity: 'AffMatrix | AffinityStore | None', rng: random.Random | None=None) -> list"
    },
    "Warehouse/picking/Workload_Builder.py::_lift_weighted_sample_v2": {
      "decorators": [],
      "doc": "Fenwick-tree form of `_lift_weighted_sample`: same conditional-demand weight model\n(weight(B) = freq(B) \u00b7 \u03a0 lift(A,B) over selected partners A), O((k\u00b7(1+P))\u00b7log n)\ninstead of v1's O(k\u00b7n) full-array passes per draw.\n\nNOT byte-compatible with v1 \u2014 the tree groups float additions differently than v1's\nsequential cumsum, so draws eventually land on different SKUs and the batch sequence\nis a new version (BatchConfig.sampler='v2'; batch_precompute fingerprints it apart).\nDeterministic for a given rng seed, one rng.uniform consumed per draw like v1.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates: list, k: int, affinity: 'AffMatrix | AffinityStore | None', rng: random.Random | None=None) -> list"
    },
    "Warehouse/picking/Workload_Builder.py::_lift_weighted_sample_v3": {
      "decorators": [],
      "doc": "Segment-tree form of `_lift_weighted_sample`: the same conditional-demand weight model\n(weight(B) = freq(B) \u00b7 \u03a0 lift(A,B) over selected partners A) and the same O((k\u00b7(1+P))\u00b7log n)\ncost as v2, but drawing k DISTINCT candidates \u2014 which v2 does not.\n\nv2 returns `k` entries of which a measured 8.64% (fulfillment) / 1.02% (store) are repeats\nof a SKU it has already drawn; `Batch.items` is keyed by sku, so the repeats collapse and\nthe batch silently delivers fewer lines than the era declared.  The cause is `_Fenwick`'s\nsubtractive update under this model's enormous weight dynamic range; `_SegTree` removes it.\n\nNOT byte-compatible with v1 or v2 \u2014 a different draw sequence is the whole point, so this\nis a new results era (BatchConfig.sampler='v3'; batch_precompute fingerprints it apart).\nDeterministic for a given rng seed, one rng.uniform consumed per draw like v1 and v2.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates: list, k: int, affinity: 'AffMatrix | AffinityStore | None', rng: random.Random | None=None) -> list"
    },
    "Warehouse/picking/Workload_Builder.py::_plan_aisle_path": {
      "decorators": [],
      "doc": "Order bins by bayX; within each x-column traverse bayY in whichever\ndirection (ascending or descending) minimises entry distance from the\ncurrent position.",
      "is_async": false,
      "kind": "function",
      "signature": "(bins: list[Aisle.Bin]) -> list[Aisle.Bin]"
    },
    "Warehouse/picking/Workload_Builder.py::_rederive_plan": {
      "decorators": [],
      "doc": "Per-bin quantities for a `Task` built without an explicit plan.\n\nThe same greedy `from_batch` uses -- walk the path, take what the bin has up to the\nSKU's remaining demand -- so a hand-built Task behaves like a real one.  It can pick a\nDIFFERENT distribution than `from_batch` would when the path order differs from the\ndrain order, which is exactly why the real caller passes its plan instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: list, items: dict[int, int]) -> list[int]"
    },
    "Warehouse/picking/Workload_Builder.py::drain_sku": {
      "decorators": [],
      "doc": "THE SIM'S OWN DRAIN RULE for one SKU, extracted so a forecast shares it by\nconstruction: the SMALLEST-QUANTITY bin first, ties by `location`, across both tiers\nas one ordering; take = min(remaining, bin quantity).\n\nAccumulates per-bin takes into `out` (a `defaultdict(int)` keyed by the bin object)\nand returns the demand no bin could satisfy.  Two callers, deliberately:\n`Task.from_batch` builds the pick tasks from it, and the standing yard's space\ntimeline (`Inbound/space.py`) projects predicted-clear bins with it \u2014 handed this\nfunction by the driver, because the import edge `Inbound -> wh_picking` is forbidden.\nOne body means the projection is exact rather than a re-implementation that can\ndrift; `_rederive_plan` below is explicitly NOT this rule (its own docstring warns\nits distribution can differ).\n\nSMALLEST-FIRST IS THE CONSOLIDATION ENGINE (ADR-0003, decision 7 of\n`.scratch/department-calibration/issues/20-...`).  Under the empty-first top-up a SKU\nacquires a second bin whenever the free index is dry; draining the emptiest bin first\nclears remnants and hands bins back to the index, which is what makes the pair\nconverge instead of fragmenting further.\n\nTWO CONSEQUENCES, both deliberate and neither reversible by a flag:\n\n1. **The forward-pick preference is retired from the drain.**  Singletons were drained\n   before pallets \"so that forward-pick locations are always preferred over reserve\n   locations\"; one ordering across both tiers ends that.  The split still exists\n   everywhere else (`is_forward_pick`, the two indexes, `_candidates_raw`) \u2014 it no\n   longer decides pick order.\n2. **This is NOT byte-identical to the location-order rule**, and not only when the\n   free index is dry: a SKU-tier holds 2+ bins straight out of initial stocking\n   (measured: 800\u20131145 of ~1200 SKU-tiers, 224\u2013327 of them order-sensitive, across\n   coverage 1\u201310), so travel, tasks and depletion move on essentially every run.\n   Absolute pick/travel/throughput numbers published before this commit are not\n   comparable with ones after it.\n\nPure: reads bin state, writes only `out`, consumes no RNG.  The sort is unconditional\nnow \u2014 a `_SortedBins` container maintains `location` order, but on-hand changes with\nevery pick and cannot be maintained, so the container's skip no longer applies.  The\ndeterminism contract is still the ORDER, not the container\n(see test_task_bin_selection_determinism).\n\nWHAT THAT COSTS, measured rather than asserted: 4,000 SKUs holding 137,552 bins (34.4\nper SKU), one drain each \u2014 22.9 ms for the old location-order walk with the container\nfast path, 42.8 ms here, so ~1.9x on the drain itself.  A batch touches ~15% of the\ncatalogue, so that is roughly +3 ms per batch at this size against a makespan in minutes.\nIt scales with per-SKU bin multiplicity, which the deep ladder already flags as growing\n(`t_task`, k\u22482.3), so re-measure before assuming it stays negligible on a much larger\ncatalogue.",
      "is_async": false,
      "kind": "function",
      "signature": "(singleton_bins, pallet_bins, qty: int, out) -> int"
    },
    "Warehouse/picking/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.picking \u2014 The pick simulations and the batch/task workload they consume.  fast_pick is the production two-phase simulation; Pick is the reference implementation.\n\nSee README.md for what does and does not belong here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/picking/fast_pick.py": {
      "decorators": [],
      "doc": "Two-phase deferred-mutation pick simulation.\n\nPhase 1 (concurrent):  All pickers run inside a ThreadPoolExecutor.\n  Each thread reads a bin-quantity snapshot taken before Phase 1 starts,\n  then simulates its full task list without touching any shared state.\n  Output: (list[PickEvent], list[_PickMutation]) per picker.\n\nPhase 2 (sequential):  Main thread applies every _PickMutation to the\n  actual bin objects, capping each pick at remaining quantity to handle\n  any cross-picker contention (rare -- pickers get disjoint aisles).\n  After updating bin_.storage.quantity it calls manager._notify_pick /\n  _notify_bin_emptied exactly as the original PickSimulation did.\n\nThe return value of DeferredPickSimulation.run() is identical to\nPickSimulation.run() so extract_batch_stats / extract_task_stats need\nno changes.\n\nGIL note: CPython threads share the GIL.  For pure-Python compute the\ngain from Phase 1 threading is ~1.5-3x via GIL-switch interleaving.\nCython 'nogil' on _simulate_picker_deferred gives the full N-picker\nparallelism without a language rewrite of the data model.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/picking/fast_pick.py::DeferredPickSimulation": {
      "decorators": [],
      "doc": "Two-phase pick simulation with the same interface as PickSimulation.\n\nAfter run() completes, .phase1_time and .phase2_time hold wall-clock\nseconds for Phase 1 (concurrent picker compute) and Phase 2 (sequential\nmutation application) respectively -- useful for benchmarking.",
      "kind": "class",
      "signature": "(_ProgressAPIMixin)"
    },
    "Warehouse/picking/fast_pick.py::DeferredPickSimulation.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, tasks: list[Task], config: PickConfig, manager: Inventory_Manager | None=None, start_times: list[float] | None=None, day_end: float | None=None) -> None"
    },
    "Warehouse/picking/fast_pick.py::DeferredPickSimulation.run": {
      "decorators": [],
      "doc": "Two-phase simulation; returns all events sorted by time.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> list[PickEvent]"
    },
    "Warehouse/picking/fast_pick.py::_PickMutation": {
      "decorators": [
        "dataclass"
      ],
      "doc": "Deferred write recorded by Phase 1; applied to bin state in Phase 2.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/picking/fast_pick.py::_simulate_picker_deferred": {
      "decorators": [],
      "doc": "Phase 1 worker -- read-only picker simulation.\n\nUses bin_snap so no bin_.storage.quantity reads/writes happen on the\nshared bin objects.  bin_.storage.order is read (sku, weight, volume)\nbut never mutated, so it is safe across all concurrent threads.",
      "is_async": false,
      "kind": "function",
      "signature": "(picker_id: int, tasks: list[Task], cfg: PickConfig, bin_snap: dict[int, int], t0: float=0.0, day_end: float | None=None) -> tuple[list[PickEvent], list[_PickMutation], dict, dict]"
    },
    "Warehouse/placement/Assignment_Functions.py": {
      "decorators": [],
      "doc": "Assignment_Functions.py -- placement (assignment) functions.\n\nSeparated from Inventory_Management so the placement POLICIES the simulation\ncompares live in one module.  A strategy bundles these builders into one\n``Placement`` (per-unit ``place_one`` + optional ranked ``place_wave``) and hands it\nto the manager.  The shared constants/types they need (BinKey, Placement, _SIZE_RANKS,\n...) stay in Inventory_Management and are imported here (one-way -- no import cycle).",
      "kind": "module",
      "signature": null
    },
    "Warehouse/placement/Assignment_Functions.py::_CLUSTER_MAP_W_CENT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "1.0"
    },
    "Warehouse/placement/Assignment_Functions.py::_CoDemandPool": {
      "decorators": [],
      "doc": "`_co_demand_ranked_impl` as a pool -- compaction (`comp`) and expansion (`expn`).\n\nThis is the first ported policy that scores a unit against what has ALREADY been placed,\nwhich is the case a pool has to get right to be worth anything.  It turns out to be the\nnatural shape: every affinity read in the per-placement body is against LIVE manager\nstate (`aisle_idx_sets`, `aisle_member_pos`) that `take` itself mutates.  That is pool\nstate by definition.  Exactly one thing was a whole-set snapshot -- `all_idx` -- and it\nis read only by the sort key.\n\n`all_idx` is therefore computed here, in `__init__`, at the instant the old code took it,\nand is deliberately frozen for the group even though placements mutate `aisle_idx_sets`\nunderneath it.  It is derived from the AISLES, not from the units, so a drain that\nnarrows or reorders the unit set does not invalidate it; when the sort finally goes, it\ngoes with it, because nothing else reads it.\n\nThe SKU-run cache (`key_cache`) carries the same guarantee and the same caveat as\n`_TravelBalancedPool`'s: the guard is a value comparison on the SKU, so losing\nadjacency costs hit rate and not correctness.  The winner refresh after every placement\nis load-bearing and is NOT a redundant recompute -- see the ulp note on\n`_ClusterMapPool`, which is where that lesson was paid for.",
      "kind": "class",
      "signature": "(_Pool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_CoDemandPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cands, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta, compact: bool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_CoDemandPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_CoDemandPool.prefers_low": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_CoDemandPool.sort_key": {
      "decorators": [],
      "doc": "Pick-effort priority with the co-occurrence term. `all_idx` is frozen for the\nlife of the pool, so a SKU's co term is one value and the memo is bit-safe.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_CoDemandPool.take": {
      "decorators": [],
      "doc": "(bin, score) for one unit; (None, None) when no aisle has a bin left.\n\n`score` is the compaction objective in seconds -- `x_pace * |x(bin) - cx|`, the\ndistance from the partners' demand-weighted column centroid that the bin choice\nminimises (compact) or maximises (expand).  None before this SKU has any partner\nplaced, because there is then no centroid and the bin was taken by the cold-start\nrule instead.  The AISLE was chosen on lift, which is a different quantity and not\na property of the bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_D_map": {
      "decorators": [],
      "doc": "id(bin) \u2192 travel-time D map.  The identical dict-comprehension sat at every\nranked-impl site; ONE helper so the formula can't drift.  Paces are s/inch\n(sec_per_inch of the ft/s speeds); expression shape preserved exactly.",
      "is_async": false,
      "kind": "function",
      "signature": "(cands, x_pace, y_pace) -> dict[int, float]"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool": {
      "decorators": [],
      "doc": "`_ranked_minlabor_impl` as a pool -- `rank_minlabor`, and `rank_maxlabor` with\n`maximize=True` (one function, both arms, every extremum flipped).\n\nUnlike `_TravelBalancedPool` there is NO per-aisle running load: this policy MINIMISES\ntotal labor rather than BALANCING it.  Its path dependence flows entirely through the\ndeques (pool state) and through two manager dicts that `take` commits to --\n`aisle_idx_sets`, which the affinity reward reads, and `aisle_member_pos`, whose appended\ncolumns the partner centroid sums in placement order.  That second one is the quieter of\nthe two and worth naming: `cx` is not a tie-break, it is the term that picks the bin.\n\nTHE DELTA SUM IS INLINED ON PURPOSE (`for ci, w in row_items: if ci in ais`).  It looks\nlike a candidate for `_delta_lift_from_row`, and unifying them would be a bug:\n`_delta_lift_from_row` switches which side it iterates on `len(row) <= len(member_set)`,\nso its summation ORDER flips as an aisle fills.  That flip is the one-ulp drift b91cf38\nwas written to fix, pinned by Tests/calltree/test_rank_cache_equivalence.py.  This loop\niterates CSR column order unconditionally and must keep doing so.\n\nThe SKU-run cache (`bc_by_aid`, `row_items`, `max_reward`) refreshes the last winner\nLAZILY -- on the next unit rather than eagerly after the pop -- because a unit that finds\nno bin leaves nothing stale to repair.  `_last_winner` is cleared every call and re-set\nonly after a successful drop.",
      "kind": "class",
      "signature": "(_Pool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cands, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, lam, maximize=False)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool._aisle_best_cost": {
      "decorators": [],
      "doc": "Extremal (min, or max if maximize) over the aisle's bracket ends of the\nper-pick labor + travel:  M*(intercept + per_item + var) + D  (height scales the whole pick).",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid, var)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool._better": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, a, b)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool.prefers_low": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool.sort_key": {
      "decorators": [],
      "doc": "Costliest SKUs claim the best (or, for maxlabor, the worst) slots first.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_MinLaborPool.take": {
      "decorators": [],
      "doc": "(bin, score) for one unit; (None, None) when nothing is placeable.\n\n`score` is the AISLE decision score, `fq*bc - lam*delta` -- the marginal labor of\nthe aisle's best bracket end minus the affinity reward, which is the objective the\nargmin actually compared.  It is deliberately not `cbest`: the bin is chosen in a\nsecond pass that adds a centroid term, and that term is a tie-shaping device rather\nthan labor, so persisting it would put a different quantity in the same column.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_NO_RUN_SKU": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "object()"
    },
    "Warehouse/placement/Assignment_Functions.py::_OptMapPool": {
      "decorators": [],
      "doc": "The optmap objective as a POOL: one `_PrefPool` over the group's candidates, one\n`take` per unit, and the order left entirely to the caller.\n\nBody-for-body the loop `build_optmap_wave_fn` already ran -- that function was written\nserving units in QUEUE ORDER with no priority re-sort, so it is the one ranked policy\nwhose behaviour a pool reproduces exactly rather than approximately.  It is the proof\nthat the seam is real before the four impls that DO re-sort are moved onto it.",
      "kind": "class",
      "signature": "(_Pool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_OptMapPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bins, target, bin_pref, capped: bool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_OptMapPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_OptMapPool.take": {
      "decorators": [],
      "doc": "(bin, score) for one unit; (None, None) once the pool is exhausted.\n\n`score` is the objective this policy actually minimises -- the gap between the bin's\npref and the SKU's map target -- read at the moment of choice, so persisting it\ncosts a dict lookup and not a second scoring pass.  None target means the SKU has no\nmap entry, and there is no gap to report.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_Pool": {
      "decorators": [],
      "doc": "Base for the placement pools: `take` is required, `order` defaults to queue order.\n\nA policy overrides `order` only if it has a genuine opinion about precedence.  The\ndrain is free to ignore it -- see the PoolFn note in inventory_common.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/placement/Assignment_Functions.py::_Pool.order": {
      "decorators": [],
      "doc": "The full precedence, best first.  A REQUEST -- the drain decides what to grant.\n\nStable, so units the key cannot separate keep their queue order: a tie was not an\nopinion, and the older unit should not lose to a younger one for no reason.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, units)"
    },
    "Warehouse/placement/Assignment_Functions.py::_Pool.sort_key": {
      "decorators": [],
      "doc": "The precedence this policy would like, HIGHER FIRST -- or None for no opinion.\n\nStated as a key rather than as a sorted list because the drain's K-oldest window\nasks \"which of these K next\" once per placement, and re-deriving a key inside every\nwindow would mean N*K affinity slices.  Every key here is a pure function of the\nunit and of state frozen at pool-open, never of what the pool has already placed, so\ncomputing it once per unit is safe as well as fast.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_Pool.take": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool": {
      "decorators": [],
      "doc": "A wave-local pool of candidate bins sorted by `pref`, with O(log) closest-to-target\nqueries and O(\u03b1) consumption (a bin taken by one unit is invisible to later queries).\n\nSort key is geometry-derived and process-reproducible: (pref, aisle_id, x_phys, y_phys)\n\u2014 never id(bin), whose value is not reproducible across processes.  Consumption uses a\nunion-find \"next/prev alive\" so no tombstones are ever re-scanned.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, bins, pref)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool._find_nxt": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int) -> int"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool._find_prv": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int) -> int"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool._take": {
      "decorators": [],
      "doc": "Mark index i dead, splice it out of both alive-chains, return its bin.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, i: int)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool.take_closest": {
      "decorators": [],
      "doc": "Take & return the alive bin with min |pref - target| (tie -> lower pref); None if empty.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, target: float)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool.take_ge": {
      "decorators": [],
      "doc": "Take the alive bin with the smallest pref >= target (map_rank cap); if none qualify,\nfall back to the alive bin with the largest pref (least-prime).  None if empty.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, target: float)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool.take_max": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_PrefPool.take_min": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool": {
      "decorators": [],
      "doc": "`_ranked_assign_impl`, split along its real seam.\n\nThe old function did three separable things in one pass: snapshot the group's candidates\n(candidate-derived), decide precedence (unit-derived), and hand out bins (per unit).\nOnly the middle one ever needed the whole unit set, and only the middle one is what the\ndrain is taking back.  So:\n\n  * `__init__` -- the candidate snapshot: `_D_map`, the per-aisle deques sorted\n    extremal-D-first, and the two head dicts.  Plus `all_idx`, the union of every placed\n    SKU index, snapshotted at exactly the instant the old code took it: before the sort,\n    before any placement in this group.  It is deliberately frozen for the whole group\n    even though placements below mutate `aisle_idx_sets`.\n  * `order`  -- the descending pick-effort priority.  This is now a request.\n  * `take`   -- one aisle choice, one head pop, one commit.\n\n`head_bin`/`head_D` key order is load-bearing in three places and is preserved exactly:\n`min`/`max` keep the FIRST extremal, so a D tie resolves to whichever aisle appeared\nfirst in the candidate list; `rank_popularity`'s selector has the same tie behaviour; and\n`rank_random` indexes into `list(head_bin.keys())`.  Insertion order is first-appearance\nin `cands`, refreshed in place on a pop and deleted on exhaustion, so a key never moves\nand never comes back.\n\nTwo dead parameters did not survive the port: `pick_intercept`/`pick_weight_coef`/\n`pick_volume_coef` were unpacked and never read (the priority uses the precomputed\n`c.labor_cost` instead), and `aisle_extra_sum`/`sku_extra_product` had no caller anywhere\nin the repo.",
      "kind": "class",
      "signature": "(_Pool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cands, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, freq_by_idx, freq_by_sku, qty_by_sku, beta, minimize, aisle_selector=None, order_key=None, aisle_key=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool._heap_key": {
      "decorators": [],
      "doc": "The ordering the scan computed, as a value.\n\nDefault: the head's D, NEGATED when maximising, so one min-heap serves `tmin` and\n`tmax` alike.  `-0.0 == 0.0`, so a zero-D tie still falls through to `_rank`.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool._pick_effort_priority": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit) -> float"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool.prefers_low": {
      "decorators": [
        "property"
      ],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool.sort_key": {
      "decorators": [],
      "doc": "Pick-effort priority: the highest-effort unit claims the extremal-D bin first.\nA policy may supply its own per-unit score instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_RankedAssignPool.take": {
      "decorators": [],
      "doc": "(bin, score) for one unit; (None, None) when every aisle is drained.\n\n`score` is the chosen bin's travel cost D -- the float the aisle argmin compared,\nso reporting it is one dict read and no arithmetic.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool": {
      "decorators": [],
      "doc": "`_travel_balanced_impl` as a pool -- Rank_labor, and Rank_cartlabor with `cart`.\n\nEverything this policy remembers between placements is either candidate-derived (the\nper-(aisle, bracket) min-D deques, `D_of`, `M_of`) or a RUNNING TOTAL seeded once from\nmanager state (`load`, `vol_load`).  Nothing is an aggregate over the unit set.  So the\nsplit is clean: the whole prologue is `__init__`, the LPT sort is `order`, and one\nplacement is `take`.\n\nTHE SKU-RUN CACHE comes across unchanged, and its validity argument is worth restating\nbecause it is the thing a reorder threatens.  `ab_cache`/`score_cache` are rebuilt when\n`sku != run_sku` and refreshed for the WINNER after every placement; a non-winning\naisle's inputs are provably frozen inside a run (`fq`/`var`/`m_s` are per-SKU constants,\nand `load`/`vol_load`/`aisle_sku_sets`/the deque heads move for the winner only).  The\nguard is a value comparison on the SKU, so a drain that stops clustering same-SKU units\ndoes not make the cache WRONG -- it makes it never hit.  That is the correct failure\nmode, and it is why no `assume_clustered` flag is needed here.\n\nWhat would break it: turning the cache into a persistent `{sku: ...}` dict to win the\nhit rate back under FIFO.  Between two units of one SKU, OTHER SKUs commit and grow\nother aisles' state, so a keyed cache would serve stale scores.  Don't.",
      "kind": "class",
      "signature": "(_Pool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, cands, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_pick_load_sum, sku_pick_load_product, freq_by_sku, qty_by_sku, cart=None, geo_memo=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool._aisle_best": {
      "decorators": [],
      "doc": "(cost, mult, bin) of the cheapest available bin in the aisle for this var.\nHeight scales the whole at-location pick: cost = m*(intercept + per_item + var) + D.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid, var)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool._cart_cost": {
      "decorators": [],
      "doc": "Expected cart-swap cost for an aisle holding raw volume mass v_raw.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, v_raw)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool._score_of": {
      "decorators": [],
      "doc": "The per-(unit, aisle) score -- the ORIGINAL expressions verbatim.\n\nbalance TOTAL expected aisle labor = handling+travel + expected cart swaps,\nso an aisle nearing a full cart is penalised and further volume disperses.\nThe SKU's volume mass counts ONCE per aisle (a second bin of a SKU already\nhere adds no new expected picked volume), mirroring aisle_pick_load_sum.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, aid, ab, sku, fq, m_s)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool.sort_key": {
      "decorators": [],
      "doc": "Longest-processing-time first: the highest expected-labor unit is placed while\nthe most aisles are still cheap.  A FIFO window degrades LPT to arbitrary-order\ngreedy; the balance mechanics below are untouched by that.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_TravelBalancedPool.take": {
      "decorators": [],
      "doc": "(bin, score) for one unit; (None, None) when no aisle has a bin left.\n\n`score` is the MARGINAL expected labor this placement adds, `fq * cost` -- the\nquantity the running balance is updated with, so reporting it is free.  The aisle\ntotal `best_score` would be the wrong number to persist: it carries every prior\nplacement in that aisle and is not comparable across aisles, waves or arms.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::_affinity_row": {
      "decorators": [],
      "doc": "{partner_idx: lift} for `sku`, or None when the SKU has no stored partners.\nOne numpy CSR slice per call (hoist to once per unit, then reuse across aisles).",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, sku)"
    },
    "Warehouse/placement/Assignment_Functions.py::_aisle_anchor_gap": {
      "decorators": [],
      "doc": "Best achievable map-anchor gap in one aisle's candidate bins (lower = more favored):\nmin |pref \u2212 target|, or min pref when the SKU has no target (prefer prime).",
      "is_async": false,
      "kind": "function",
      "signature": "(lst, pref, target)"
    },
    "Warehouse/placement/Assignment_Functions.py::_aisle_extremal_bins": {
      "decorators": [],
      "doc": "Reduce candidates to one bin per aisle \u2014 the extremal-D representative.\n\nProof of correctness\n--------------------\nFor a fixed aisle (fixed ls, dl), the score tuple (delta_l2, old_L) is\nstrictly monotone increasing in D = x_pace*x_phys + y_pace*y_phys\n(pace = sec_per_inch(speed); speeds are ft/s):\n  old_L  = D + \u03bb(D/k)^\u03b3 ls           \u2014 increasing in D\n  new_L  = D + \u03bb(D/k)^\u03b3 (ls+dl)      \u2014 increasing in D\n  delta_l2 = new_L\u00b2 \u2212 old_L\u00b2          \u2014 product of two positive increasing\n                                         functions, so also increasing in D\n\nConsequence: within a fixed aisle, the minimum-D bin always yields the\nminimum score (best for minimising) and the maximum-D bin always yields the\nmaximum score (best for maximising).  Reducing O(N_bins) candidates to one\nrepresentative per aisle is exact \u2014 no approximation.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates: list[Any], x_speed: float, y_speed: float, minimize: bool) -> tuple[dict[int, float], dict[int, Any]]"
    },
    "Warehouse/placement/Assignment_Functions.py::_aisle_index_for_unit": {
      "decorators": [],
      "doc": "(best_D, best_bin_map) \u2014 one extremal-D representative bin per aisle for *unit*,\nread from the manager's pre-sorted secondary index (the fast-path twin of\nInventory_Manager._candidates; this identical block used to sit inline at 3 sites).\n\nResolves the unit's BinKey tier exactly as _candidates does: singleton -> its one\nbucket; tiered families (pallet / fulfillment) -> the smallest non-empty tier >= the\nunit's own, via the unit's OWN size table (fulfillment sizes are ff_*, absent from the\npallet _SIZE_RANKS \u2014 a pallet-only lookup would silently drop every fulfillment unit).\nminimize picks each aisle deque's min-D head, else its max-D tail (sorted ascending).\nReturns ({}, {}) when no tier has bins.",
      "is_async": false,
      "kind": "function",
      "signature": "(aisle_index, unit, minimize: bool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_aisle_score_fn": {
      "decorators": [],
      "doc": "Compose a per-unit AssignmentFn from a named aisle SCORER + direction.\n\nShared by the travel (f_s*D - beta*co_occur) and cohesion (demand-weighted lift)\npolicies; they differ only in the score tuple and which D-rank bin represents\neach aisle.  The returned closure carries a programmatic ``.name`` (e.g.\n'travel_min') so downstream processing can build/identify functions by name.\n\nWhen ``aisle_index`` (mgr._aisle_index) is supplied, Step 1 reads the\npre-sorted per-aisle index directly (O(N_aisles)) instead of scanning a flat\ncandidates list \u2014 the same fast path as build_load_*.  The candidates argument\nis then ignored (the manager passes None).  bin_minimize picks the head (min-D)\nor tail (max-D) of each aisle's ascending-by-D list, identical to\n_aisle_extremal_bins, so placements are unchanged.",
      "is_async": false,
      "kind": "function",
      "signature": "(name, *, score_kind, maximize, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, freq_by_idx, freq_by_sku, qty_by_sku, beta, aisle_index=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_aisle_score_fn.assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit, candidates)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_aisle_score_fn.assign.score_of": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(aid)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_co_demand_place_one": {
      "decorators": [],
      "doc": "Per-unit co-demand fn (place_one) \u2014 same scoring as the wave, one unit at a time.\nUsed for the ranked policy's stragglers; accumulates positions like the wave.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, compact, name)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_co_demand_place_one.assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit, candidates)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_co_demand_place_one.assign.aisle_key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(aid)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_co_demand_pool_fn": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta, compact)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_co_demand_pool_fn.open_pool": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, rep=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_minlabor_pool_fn": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, lam, maximize=False)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_minlabor_pool_fn.open_pool": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, rep=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_ranked_assign_pool_fn": {
      "decorators": [],
      "doc": "`open_pool` shared by the four ranked-assign arms (tmin / tmax / rank_random /\nrank_popularity).  Mirrors `_ranked_assign_impl`'s parameter list exactly.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, freq_by_idx, freq_by_sku, qty_by_sku, beta, minimize, aisle_selector=None, order_key=None, aisle_key=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_ranked_assign_pool_fn.open_pool": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, rep=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_travel_balanced_pool_fn": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_pick_load_sum, sku_pick_load_product, freq_by_sku, qty_by_sku, cart=None, geo_memos=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_build_travel_balanced_pool_fn.open_pool": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, rep=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_closest_abs": {
      "decorators": [],
      "doc": "min |p - target| over the ascending `prefs` via bisect neighbours (O(log n)).\nReturns the identical value as `min(abs(p - target) for p in prefs)`; +inf if empty.",
      "is_async": false,
      "kind": "function",
      "signature": "(prefs: list[float], target: float) -> float"
    },
    "Warehouse/placement/Assignment_Functions.py::_cluster_map_choose_aisle": {
      "decorators": [],
      "doc": "Cohesion-first aisle: max \u03a3(lift\u22121)\u00b7f to members, tie-break / cold-start by anchor gap.\n\nSame argmax as ``max(live, key=(lift, -anchor_gap))`` but LAZY: the O(B) anchor-gap scan is\nreplaced by an O(log) ``_closest_abs`` bisect on the aisle's pre-sorted ``prefs_by_aisle`` and\nis evaluated ONLY for the aisles tied at the max lift (in the warm case, one aisle wins on lift\n\u2192 no gap work at all).  ``row`` is the SKU's pre-sliced affinity row (hoisted once per unit).\n\n``lifts`` may carry the delta values precomputed by a caller's same-SKU run cache (a\nsuperset keyed by aisle); values for the current live aisles are read from it instead\nof recomputed \u2014 identical numbers, because within a same-SKU run the member idx-sets\ngain only the SKU's own index, which (self-pairs are not stored) is never in ``row``.",
      "is_async": false,
      "kind": "function",
      "signature": "(by_aisle, prefs_by_aisle, row, aisle_idx_sets, freq_by_idx, target, lifts=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_cluster_map_pick_bin": {
      "decorators": [],
      "doc": "Choose the cluster's bin within one aisle: anchor at the favored map location and\ncompact toward the partner centroid; honour the prime-spot cap when capped.\n\nReturns `(bin, cost)`.  The cost is RETURNED rather than recomputed by the caller, and\nthat is deliberate: `_CLUSTER_MAP_W_CENT * x_pace * abs(...)` re-associated as\n`(W * x_pace) * abs(...)` is a different float, so a caller reconstructing the number\nwould eventually persist something that is not what decided.  `None` on the capped\nleast-prime fallback, which is chosen by max-pref and not by this cost at all --\nreporting the cost of a bin that was picked on a different rule would be a lie.",
      "is_async": false,
      "kind": "function",
      "signature": "(lst, pref, target, cx, x_pace, capped)"
    },
    "Warehouse/placement/Assignment_Functions.py::_cluster_map_pick_bin.cost": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(b)"
    },
    "Warehouse/placement/Assignment_Functions.py::_co_demand_ranked_impl": {
      "decorators": [],
      "doc": "Ranked co-demand placement.  SUPERSEDED by `_CoDemandPool`, which is what `comp`\nand `expn` actually run; kept as the frozen oracle the port is tested against.\n\nUnits are placed in the same pick-effort order as\n_ranked_assign_impl and membership is committed incrementally, but the BIN choice is\nposition-aware: the aisle is scored by demand-weighted lift to its members (MAX for\ncompact / MIN for expand), and within it the bin NEAREST (compact) / FARTHEST (expand)\nthe partners' column centroid is taken \u2014 vs the extremal-D head.  Each placement also\nappends (x_phys, idx) to aisle_member_pos so later units in the wave see it.\n\nTHIN DRIVER over `_CoDemandPool` since ticket 04. It was a second implementation of the same\nscoring rule, and the wave form has had no production caller since the pool inversion --\n14 of 17 shipped rules are pooled and the other three have no group path at all. What it\nis FOR is the equivalence suites, and they gain from this rather than lose: the reference\nthey compare against is the FROZEN oracle in `Tests/calltree/test_rank_cache_equivalence.py`, and this leg is now the drain\ncontract (pool driven in the wave's own order == wave) instead of an echo of it.\n\n`pool.order(units)` is the sort the body used to do inline, and driving through it is the\nwhole claim: given the same order, the pool makes bit-identical decisions. A drain that\ndeclines that order gets a different -- and intentionally different -- answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(units, candidates_fn, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta, compact: bool)"
    },
    "Warehouse/placement/Assignment_Functions.py::_delta_lift_from_row": {
      "decorators": [],
      "doc": "\u03a3 (lift - 1)\u00b7f_i over partners i in `member_idx_set`, from a pre-built `row`\n({idx: lift}).  Identical value to _demand_weighted_delta_lift, no CSR slice.",
      "is_async": false,
      "kind": "function",
      "signature": "(row, member_idx_set, freq_by_idx) -> float"
    },
    "Warehouse/placement/Assignment_Functions.py::_demand_weighted_delta_lift": {
      "decorators": [],
      "doc": "Sum of (lift(s,i) \u2212 1) * f_i for all affinity partners i in the aisle.\n\nSame CSR row-slice as delta_lift_idxs but weights each partner's association\nABOVE independence (lift \u2212 1; lift = 1 \u21d2 0) by its demand frequency, so common\nstrongly-associated partners dominate over rare or near-independent ones.\n\nDelegates to the row helpers so the cold path (place_one, sort keys, tests) and the\nhoisted hot path (_delta_lift_from_row with a pre-built row) share ONE arithmetic\npath \u2014 bit-identical, both promoting the CSR float to Python float before the sum.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity: AffinityStore, sku: int, member_idx_set: set[int], freq_by_idx: dict[int, float]) -> float"
    },
    "Warehouse/placement/Assignment_Functions.py::_demand_weighted_partner_centroid": {
      "decorators": [],
      "doc": "Lift-weighted COLUMN centroid of an aisle's already-placed affinity partners of\n`sku`.  ``member_pos`` is the aisle's ``{sku_idx -> [x_phys, ...]}`` (one x per LIVE\nbin; pruned on reclaim so no stale members).  Returns (mass, centroid_x);\n(0.0, None) when `sku` has no placed partners there.\n\nMirrors _demand_weighted_delta_lift's CSR row-slice but accumulates a position\ncentroid weighted by lift(s, partner) * f_partner.  Iterating per distinct partner\nSKU (not per placement) keeps this O(distinct SKUs in aisle).",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, sku, member_pos, freq_by_idx)"
    },
    "Warehouse/placement/Assignment_Functions.py::_pick_extremal_aisle": {
      "decorators": [],
      "doc": "Return the aisle id whose score_of(aid) is extremal (max if maximize else min).",
      "is_async": false,
      "kind": "function",
      "signature": "(best_D, score_of, maximize)"
    },
    "Warehouse/placement/Assignment_Functions.py::_ranked_assign_impl": {
      "decorators": [],
      "doc": "Shared core for ranked-minimizing and ranked-maximizing assignment.\n\nSUPERSEDED by `_RankedAssignPool`, which is what the four arms actually run.  Kept as\nthe frozen oracle the port is tested against, and as the straggler-path reference.\n\nPriority formula (pick-effort x frequency + co-occurrence):\n  priority = f_i x (pick_intercept + pick_weight_coef x log(weight)\n                                    + pick_volume_coef x log(volume))\n             + beta x co_occur\n\nSorted descending by priority; highest-priority unit claims the extremal-D\nbin first within each same-BinKey group.  minimize=True -> lowest-D bin\n(easiest access); minimize=False -> highest-D bin (hardest access).\n\nW (task workload) remains a measurement metric only; this formula\ndrives bin placement at reorder time.\n\nTHIN DRIVER over `_RankedAssignPool` since ticket 04. It was a second implementation of\nthe same scoring rule and has had no production caller since the pool inversion. The\nreference it used to BE now lives in `Tests/unit/test_ranked_assign_pool_equivalence.py`\nas `_oracle_ranked_assign_impl`, frozen verbatim -- this family was the last one whose\ntest read a live production impl as its oracle.\n\n`pool.order(units)` is the sort this body did inline. Note the pool takes the SCAN branch\nwhenever `aisle_selector` is given and the heap branch otherwise, which is exactly the\nsplit the wave had; a caller that wants the heap passes `aisle_key` to the pool directly,\nand the wave never knew how to do anything but scan.",
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn, affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float, minimize: bool, aisle_selector=None, order_key=None) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::_ranked_minlabor_impl": {
      "decorators": [],
      "doc": "Greedy MINIMISER (or, with maximize=True, MAXIMISER) of expected total task labor.\n\nSUPERSEDED by `_MinLaborPool`, which is what `rank_minlabor` and `rank_maxlabor` run;\nkept as the frozen oracle the port is tested against.\n\nModels the objective E[task labor] = \u03a3_s f_s\u00b7[ M(y_s)\u00b7(intercept + q_s\u00b7v_s) + D ] and\nplaces each unit in the (aisle, bin) that minimises its MARGINAL contribution:\n\n    cost(s, b) = f_s\u00b7( M(y_b)\u00b7(intercept + v_s) + D_b )  \u2212  \u03bb\u00b7\u03a3_{partners p in aisle} lift(s,p)\u00b7f_p\n\nwhere v_s = handle_var (per-unit weight/volume term), M(y) = height bracket multiplier,\nD_b = x_speed\u00b7x_phys + y_speed\u00b7y_phys.  This fuses three slotting levers into one score:\n  \u2022 golden-zone height  \u2014 the M(y_b)\u00b7v_s term pushes heavy/frequent SKUs to low brackets;\n  \u2022 effort-to-front     \u2014 the D_b term pulls them to near, low bins;\n  \u2022 affinity compaction \u2014 the \u2212\u03bb\u00b7delta_lift aisle reward co-locates demand partners in the\n    SAME aisle (the big travel win: fewer aisle-tasks), and within the chosen aisle the bin\n    is pulled toward the partners' column centroid (+x_speed\u00b7|x\u2212cx|) to shorten the sweep.\n\nmaximize=True flips every extremum (argmax score, farthest/highest bins, scatter partners)\nto deliberately MAXIMISE the objective \u2014 a worst-case upper-bound sanity control (Rank_maxlabor)\nthat brackets how much the minimiser is worth.  Unlike _travel_balanced_impl (Rank_labor) there\nis NO LPT load term \u2014 this MINIMISES (or maximises) total labor, it does not BALANCE it.  Units\nare placed highest expected_labor first so the costliest SKUs claim the best (or worst) slots.\n\nBoth the aisle pre-screen AND the final bin choice scan only per-(aisle,bracket) extremal-D\ndeque ends \u2014 O(units\u00b7aisles\u00b7brackets), independent of bins-per-aisle.  The chosen bin is popped\nfrom its deque end, so the ends are always live (no stale-bin bookkeeping).\n\nTHIN DRIVER over `_MinLaborPool` since ticket 04. It was a second implementation of the same\nscoring rule, and the wave form has had no production caller since the pool inversion --\n14 of 17 shipped rules are pooled and the other three have no group path at all. What it\nis FOR is the equivalence suites, and they gain from this rather than lose: the reference\nthey compare against is the FROZEN oracle in `Tests/calltree/test_rank_cache_equivalence.py`, and this leg is now the drain\ncontract (pool driven in the wave's own order == wave) instead of an echo of it.\n\n`pool.order(units)` is the sort the body used to do inline, and driving through it is the\nwhole claim: given the same order, the pool makes bit-identical decisions. A drain that\ndeclines that order gets a different -- and intentionally different -- answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(units, candidates_fn, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, lam, maximize=False)"
    },
    "Warehouse/placement/Assignment_Functions.py::_require_affinity": {
      "decorators": [],
      "doc": "Fail loudly if an affinity-DRIVEN policy (cohesion / co-demand) is built without a\nusable affinity matrix, rather than silently scoring 0 lift and degrading to uniform.\nMirrors the batch sampler's refusal to silently fall back (Workload_Builder.Batch).\nTravel/ranked policies are exempt: they only use lift as a minor tie-break and rank\nfine on frequency alone.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, policy: str) -> None"
    },
    "Warehouse/placement/Assignment_Functions.py::_require_demand": {
      "decorators": [],
      "doc": "Fail loudly if a demand-WEIGHTED policy is built with an empty frequency map,\nrather than silently weighting every SKU by 0 and degrading to uniform.  Ranked\ndrains are exempt: their priority reads order.demand.relative_frequency directly.",
      "is_async": false,
      "kind": "function",
      "signature": "(freq_map, policy: str, what: str) -> None"
    },
    "Warehouse/placement/Assignment_Functions.py::_score_expected_labor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit) -> float"
    },
    "Warehouse/placement/Assignment_Functions.py::_score_expected_popularity": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit) -> float"
    },
    "Warehouse/placement/Assignment_Functions.py::_travel_balanced_impl": {
      "decorators": [],
      "doc": "Travel- AND height-aware LPT load balance (Rank_labor).\n\nThe expected labor of placing a unit in a bin is freq\u00b7qty times the per-pick cost\nTHERE:  height_mult(y_bin)\u00b7(pick_intercept + handle_var) + D_bin, where handle_var is\nthe per-unit weight/volume handling, height_mult(y) scales the WHOLE at-location pick\n(intercept + handling), and D_bin = x_speed\u00b7x_phys + y_speed\u00b7y_phys is travel.  Each\nunit is placed in the specific empty bin that minimises the resulting BUSIEST aisle's\ntotal labor (greedy LPT).  So a bin is only chosen high/far when the aisle's load is\nlow enough to absorb the extra handling/travel \u2014 heavy, frequently-picked SKUs gravitate\nto low, near bins.\n\nWithin an aisle the best bin is SKU-dependent (high handle_var prefers low brackets),\nso bins are grouped per aisle by height bracket, each a min-D deque; for each unit we\nscan one min-D representative per (aisle, bracket) \u2014 O(units \u00b7 aisles \u00b7 n_brackets).\n\ncart (optional) makes this Rank_cartlabor: a tuple (aisle_vol_sum, sku_vol_product,\nexpected_batch_skus, total_freq) that adds each aisle's EXPECTED CART-SWAP cost to its\nbalanced load, so the balancer disperses volume that would overflow a cart.  Default\nNone \u21d2 byte-identical Rank_labor.\n\nTHIN DRIVER over `_TravelBalancedPool` since ticket 04. It was a second implementation of the same\nscoring rule, and the wave form has had no production caller since the pool inversion --\n14 of 17 shipped rules are pooled and the other three have no group path at all. What it\nis FOR is the equivalence suites, and they gain from this rather than lose: the reference\nthey compare against is the FROZEN oracle in `Tests/unit/test_travel_balanced_equivalence.py`, and this leg is now the drain\ncontract (pool driven in the wave's own order == wave) instead of an echo of it.\n\n`pool.order(units)` is the sort the body used to do inline, and driving through it is the\nwhole claim: given the same order, the pool makes bit-identical decisions. A drain that\ndeclines that order gets a different -- and intentionally different -- answer.",
      "is_async": false,
      "kind": "function",
      "signature": "(units, candidates_fn, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_pick_load_sum, sku_pick_load_product, freq_by_sku, qty_by_sku, cart=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::_travel_or_cohesion": {
      "decorators": [],
      "doc": "Make a builder with the legacy (affinity, wp, ...state..., beta) signature that\nroutes through the shared core.",
      "is_async": false,
      "kind": "function",
      "signature": "(name, score_kind, maximize)"
    },
    "Warehouse/placement/Assignment_Functions.py::_travel_or_cohesion.builder": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, freq_by_idx, freq_by_sku, qty_by_sku, beta=1.0, aisle_index=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement": {
      "decorators": [],
      "doc": "One Placement (ranked place_wave + per-unit place_one) for cluster_map (capped=False)\nor cluster_map_rank (capped=True).  Reads mgr._bin_pref / mgr._map_target at call time, so\nbuild_optimal_map must have run (wired in strategies._build_cluster_map[_rank]).",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta=1.0, *, capped) -> Placement"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._ClusterMapPool": {
      "decorators": [],
      "doc": "`place_wave`'s body, split into open / order / take.\n\nThe thinnest port of the four, because `_place` was already the per-unit body and is\nshared with the straggler path -- there is nothing to restructure, only to relocate.\n`_group` and `all_idx` move into `__init__`, the priority sort becomes `order`, and\n`run_cache` becomes an attribute instead of a wave-local dict.\n\nTHE RUN CACHE IS THE ONE THING TO BE CAREFUL WITH, and this policy is where the\nlesson was paid for. Its validity argument is written out in `_place`'s docstring:\nwithin a same-SKU run the only idx-set that mutates is the winner's, and the winner's\ncached delta is recomputed immediately after each commit. The first cut instead\ncached ACROSS the winner's set growth, and drifted by one ulp -- not because the\nvalue was wrong, but because `_delta_lift_from_row` iterates the smaller side, so\nadding one member flipped the summation ORDER. That moved a real placement at 8k-SKU\nmeso scale. The argument has to be about summation order, not about values.\n\nThe cache keys on `run_cache['sku'] == sku`, so losing same-SKU adjacency costs the\nhit rate and not correctness. Making it a persistent `{sku: lifts}` dict to win that\nback would reintroduce exactly the bug above.",
      "kind": "class",
      "signature": "(_Pool)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._ClusterMapPool.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, candidates)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._ClusterMapPool.__len__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._ClusterMapPool.sort_key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._ClusterMapPool.take": {
      "decorators": [],
      "doc": "(bin, score) for one unit.\n\n`score` is `_cluster_map_pick_bin`'s own cost -- |pref - target| plus the\nweighted centroid pull -- handed back from the comparison that chose the bin\nrather than reconstructed here. None on the capped least-prime fallback, which\nis decided by max-pref instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, unit)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._group": {
      "decorators": [],
      "doc": "Group candidate bins by aisle + build each aisle's ascending pref list (for the\nO(log) anchor bisect).  The pref list is kept in multiset-sync with by_aisle on removal.",
      "is_async": false,
      "kind": "function",
      "signature": "(candidates)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement._place": {
      "decorators": [],
      "doc": "Shared aisle+bin choice; mutates the chosen aisle's bin list + pref list + aisle state.\n\n`run_cache` (place_wave only) reuses the SKU's affinity row AND the per-aisle\ndelta-lift values across a same-SKU run (sorted_units clusters same-SKU units:\nidentical priority, stable sort).  Byte-identity argument: within a run the ONLY\nidx-set that mutates is the winner's (commit adds this SKU's own index), and the\nwinner's cached delta is recomputed fresh right after each commit below.  Every\nother aisle's set is untouched, so its cached value is bit-for-bit what a fresh\nrecompute would return \u2014 same operands AND same summation order (an unmutated\nset iterates identically; _delta_lift_from_row's iterate-the-smaller-side branch\nsees the same lengths).  A value-only argument is NOT enough here: the first cut\ncached across the winner's set growth and drifted by one ulp when the summation\norder flipped, moving one placement at 8k-SKU meso scale.",
      "is_async": false,
      "kind": "function",
      "signature": "(sku, by_aisle, prefs_by_aisle, f_s, q_s, run_cache=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement.open_pool": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, rep=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_map_placement.place_one": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit, candidates)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_maximizing_assignment_fn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_travel_or_cohesion('cohesion_max', 'cohesion', True)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_cluster_minimizing_assignment_fn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_travel_or_cohesion('cohesion_min', 'cohesion', False)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_co_demand_placement": {
      "decorators": [],
      "doc": "One Placement (place_one + ranked place_wave) for co-demand compaction (compact=True)\nor expansion (compact=False).  Wired by strategies._build_compaction/_build_expansion.",
      "is_async": false,
      "kind": "function",
      "signature": "(compact, affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta=1.0) -> Placement"
    },
    "Warehouse/placement/Assignment_Functions.py::build_optmap_fn": {
      "decorators": [],
      "doc": "Optimal-map (soft, score-matched) placement.  Places each unit in the free candidate\nbin whose quantity-free preferred score `mgr._bin_pref[bin]` matches the SKU's optimal\ntarget `mgr._map_target[sku]` (the pref of its labor-optimal bin from the full LAP).\n\ncapped=False (default, the `map` arm): symmetric match \u2014 argmin |pref(b) \u2212 target|.\nNon-greedy in spirit (a low-value SKU's target is far from prime bins), but with no hard\ncap it can still UPGRADE into a prime bin when its own tier is full and the nearest free\nbin happens to be prime.\n\ncapped=True (the `map_rank` arm): rank-relative, upgrade-CAPPED \u2014 the SKU never settles in\na bin more prime (lower pref) than its own optimal rank.  Among free candidates it takes\nthe closest one AT OR BELOW its tier (pref \u2265 target); only if none are free does it fall\nback to the least-prime remaining bin.  This keeps prime spots open for the higher-ranked\nSKUs that future orders will bring.  (`_candidates` returns a single BinKey tier and pref\nis monotone with goodness within it, so the pref-floor is exactly the relative-rank cap.)\n\nReads mgr state at call time, so build_optimal_map may run before or after this is wired.\nPer-unit (not a ranked wave); the drain places one unit at a time over live free bins.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, capped=False)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_optmap_fn.place_one": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit, candidates)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_optmap_pool_fn": {
      "decorators": [],
      "doc": "`open_pool` for the optimal-map policies -- the pool twin of build_optmap_wave_fn.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, capped=False)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_optmap_pool_fn.open_pool": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(candidates, rep=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_optmap_wave_fn": {
      "decorators": [],
      "doc": "Ranked-wave twin of build_optmap_fn: the SAME per-unit objective (argmin |pref\u2212target|,\nor capped nearest-with-pref\u2265target), but the O(B) scan is amortized \u2014 one ``_PrefPool`` sort\nover the group's candidates per wave, then an O(log B) bisect per unit.  Units are served in\nQUEUE ORDER (no priority re-sort) so placement matches the per-unit ``place_one`` path except\non exact pref ties.  A None (pool exhausted) falls to the per-unit straggler path, which\nre-fetches candidates and spills up tiers exactly as today.",
      "is_async": false,
      "kind": "function",
      "signature": "(mgr, capped=False)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_optmap_wave_fn.place_wave": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units, candidates_fn)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_cartlabor_fn": {
      "decorators": [],
      "doc": "Cart-swap-aware LPT labor balancer (Rank_cartlabor).  Same as build_ranked_labor_fn\nbut the balanced aisle load also includes each aisle's EXPECTED cart-swap cost\n(cart_swap_coef\u00b7max(0, expected_aisle_volume/cart_capacity \u2212 1)), so high-volume demand\nthat would overflow a cart is dispersed across aisles.  With the big store cart the term\nis ~0 (aisles rarely fill a cart) so store plans barely move; with the small fulfillment\ncart it bites.  Returns (unit, bin) pairs.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, aisle_pick_load_sum: dict, sku_pick_load_product: dict, aisle_vol_sum: dict, sku_vol_product: dict, expected_batch_skus: float, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_cartlabor_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_cartlabor_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_cartlabor_fn \u2014 same signature.\n\n`total_freq` is summed HERE, once when the policy is built, exactly as the wave builder\ndoes it: a per-pool sum over the same dict would be the same value today but would make\na dict-order change silently repricing every cart penalty.\n\nWhich is the whole reason it can be passed IN.  A caller that rebuilds this policy per\nevaluation rather than per arm -- there is one, the inbound gain evaluator -- would turn\n\"once when the policy is built\" into once per pool open, an O(catalogue) sum in an\nO(yard^2)-per-drain loop, and would re-expose exactly the dict-order hazard the\nparagraph above closes.  Such a caller hoists the sum to its own arm scope and hands it\ndown; it must be `sum(freq_by_sku.values())` over this same dict and nothing else.\nNone -- every production call -- sums here as before.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_pick_load_sum, sku_pick_load_product, aisle_vol_sum, sku_vol_product, expected_batch_skus, freq_by_idx, freq_by_sku, qty_by_sku, beta: float=1.0, total_freq=None, geo_memos=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_labor_fn": {
      "decorators": [],
      "doc": "Travel-aware LPT labor balancer (see _travel_balanced_impl): places each unit in\nthe specific empty bin that minimises the busiest aisle's total expected labor,\nwhere labor = freq\u00b7qty\u00b7(pick_time + travel_time).  Returns (unit, bin) pairs.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, aisle_pick_load_sum: dict, sku_pick_load_product: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_labor_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_labor_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_labor_fn \u2014 same signature, plus the optional caller-owned\ngeometry memo (`_build_travel_balanced_pool_fn`, which carries the whole argument).\nOmitted, as every production call omits it, this is the builder it always was.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_pick_load_sum, sku_pick_load_product, freq_by_idx, freq_by_sku, qty_by_sku, beta: float=1.0, geo_memos=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_maximizing_assignment_fn": {
      "decorators": [],
      "doc": "Ranked assignment: high pick-effort items get highest-D (hardest) bins.\n\nMirror of build_ranked_minimizing_assignment_fn \u2014 strategy-C upper bound.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_maximizing_assignment_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_maximizing_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_maximizing_assignment_fn \u2014 same signature.",
      "is_async": false,
      "kind": "function",
      "signature": "(*a, **kw)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_maxlabor_fn": {
      "decorators": [],
      "doc": "Worst-case sanity control: greedy MAXIMISER of expected task labor (Rank_maxlabor) \u2014\nthe exact mirror of build_ranked_minlabor_fn (high/far bins, scatter co-demanded SKUs).\nIt should land WORST on the objective_task_labor metric, bracketing the minimiser so the\nlever's direction and magnitude are verifiable.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, aisle_member_pos: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_maxlabor_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_maxlabor_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_maxlabor_fn \u2014 same signature.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_minimizing_assignment_fn": {
      "decorators": [],
      "doc": "Ranked assignment: high pick-effort items get lowest-D (easiest) bins.\n\nSame parameter signature as build_trip_minimizing_assignment_fn.  Used as the\nplace_wave of a ranked Placement (place_one = build_trip_minimizing for stragglers).",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_minimizing_assignment_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_minimizing_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_minimizing_assignment_fn \u2014 same signature.",
      "is_async": false,
      "kind": "function",
      "signature": "(*a, **kw)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_minlabor_fn": {
      "decorators": [],
      "doc": "Greedy minimiser of expected task labor (see _ranked_minlabor_impl): golden-zone\nheight + effort-to-front + affinity compaction fused into one marginal-cost score.\nThe opposite of build_ranked_labor_fn (which BALANCES aisle load).  `beta` is \u03bb, the\naffinity-reward weight that converts lift\u00b7freq into the labor (time) units of the score.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, aisle_member_pos: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_minlabor_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_minlabor_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_minlabor_fn \u2014 same signature.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, aisle_member_pos, freq_by_idx, freq_by_sku, qty_by_sku, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_popularity_fn": {
      "decorators": [],
      "doc": "Ablation: order units by expected_popularity (freq*qty) and place each into the\naisle with the LEAST \u03a3 popularity (aisle_demand_sum); nearest-D bin within it,\nnearest-aisle as the tiebreak.  Disperses demand mass evenly across aisles.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_popularity_fn._selector": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(head_D, head_bin)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_popularity_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_popularity_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_popularity_fn \u2014 same signature.\n\nIts selector reads the LIVE `aisle_demand_sum`, which `take` itself increments, so the\naisle choice depends on what this pool has already placed.  That is pool state and ports\nas-is; what it means is that this arm's result moves when the service order moves.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets, aisle_idx_sets, aisle_demand_sum, freq_by_idx, freq_by_sku, qty_by_sku, beta=1.0)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_popularity_pool_fn._key": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(aid, head_D)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_uniform_assignment_fn": {
      "decorators": [],
      "doc": "Ranked assignment: rank units by pick-effort priority (same as\nranked-minimizing), but place each into a UNIFORM-RANDOM aisle's\nminimum-travel-cost bin instead of the globally min-D aisle.\n\nAblation control: keeps the ranking (incl. demand-weighted lift) so the only\ndifference from ranked-minimizing is random vs D-optimal aisle selection \u2014\nisolating whether the trip-min aisle choice is necessary.",
      "is_async": false,
      "kind": "function",
      "signature": "(affinity, wp, aisle_sku_sets: dict, aisle_idx_sets: dict, aisle_demand_sum: dict, freq_by_idx: dict, freq_by_sku: dict, qty_by_sku: dict, beta: float=1.0, rng: random.Random | None=None)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_uniform_assignment_fn.ranked_assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(units: list, candidates_fn) -> list"
    },
    "Warehouse/placement/Assignment_Functions.py::build_ranked_uniform_pool_fn": {
      "decorators": [],
      "doc": "Pool twin of build_ranked_uniform_assignment_fn \u2014 same signature.\n\nOne RNG draw per unit that finds a live aisle, in the order the drain serves them, so\nthe stream re-pairs with different units the moment the order changes.  That is a real\nconsequence of the reorder, not a defect of the port.",
      "is_async": false,
      "kind": "function",
      "signature": "(*a, rng=None, **kw)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_trip_maximizing_assignment_fn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_travel_or_cohesion('travel_max', 'travel', True)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_trip_minimizing_assignment_fn": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_travel_or_cohesion('travel_min', 'travel', False)"
    },
    "Warehouse/placement/Assignment_Functions.py::build_uniform_aisle_trip_min_assignment_fn": {
      "decorators": [],
      "doc": "Pick an aisle UNIFORMLY at random among the candidate aisles, then place in\nthat aisle's minimum-travel-cost bin.\n\nAblation control with no affinity, no demand, no priority \u2014 the candidate set\nfrom _candidates is already scoped to the unit's (handling, category, size,\nunit_type), so the random aisle is always a legal one.  Per-unit; the FIFO\nPlacement uses this as its place_one (no place_wave).",
      "is_async": false,
      "kind": "function",
      "signature": "(wp, rng: random.Random | None=None) -> AssignmentFn"
    },
    "Warehouse/placement/Assignment_Functions.py::build_uniform_aisle_trip_min_assignment_fn.assign": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(unit: Any, candidates: list[Any]) -> Any | None"
    },
    "Warehouse/placement/Capacity_Reloader.py": {
      "decorators": [],
      "doc": "Capacity_Reloader.py \u2014 bounded re-slot via evict-and-requeue.\n\nRather than swapping bin contents (and breaking ties), a Capacity_Reloader EVICTS\ntargeted pallets back into the reorder queue (`manager.requeue_bin`) and reclaims\ntheir bins.  The manager's subsequent ranked drain (in `check_reorders`) then\nre-places everything in priority order, so popular items claim the freed prime bins\nand the evicted ones settle into their proper rank \u2014 no swapper, no tie-breaks.\n\nThe number of evictions per pallet aisle is capped at a percent of a reference\naisle's bin capacity (the operational pallet-move labor ceiling).\n\nThree named variants (target selectors):\n  - ``promote_popular``  : evict high-frequency pallets sitting in high-D (bad) bins,\n                           so the ranked re-drain lifts them into better bins.\n  - ``demote_unpopular`` : evict low-frequency pallets sitting in low-D (prime) bins,\n                           freeing prime bins for higher-ranked items.\n  - ``rebalance``        : both (split the per-aisle budget) \u2014 the swap result\n                           without a swapper.\n\nRe-placement is not the reloader's concern: it only evicts/requeues, and the manager's\nown ``placement`` policy (via check_reorders) re-drains the freed bins afterwards.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/placement/Capacity_Reloader.py::Capacity_Reloader": {
      "decorators": [],
      "doc": "Evict-and-requeue re-slot policy with a per-aisle move budget.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/placement/Capacity_Reloader.py::Capacity_Reloader.__init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self, name: str, target_selector, *, move_limit_pct: float=0.005, ref_unit_type: str='pallet', ref_size: str='extra_large')"
    },
    "Warehouse/placement/Capacity_Reloader.py::Capacity_Reloader.per_aisle_cap": {
      "decorators": [],
      "doc": "K = floor(move_limit_pct \u00d7 bins in a reference (XL pallet) aisle).  One\nconstant across the run; cached.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, warehouse) -> int"
    },
    "Warehouse/placement/Capacity_Reloader.py::Capacity_Reloader.reload": {
      "decorators": [],
      "doc": "Evict up to `per_aisle_cap` targeted pallets per pallet aisle back into\nthe queue (+ reclaim their bins).  The caller's ranked drain re-places them.\nReturns the number evicted.",
      "is_async": false,
      "kind": "function",
      "signature": "(self, manager, freq_of: dict, x_speed: float, y_speed: float) -> int"
    },
    "Warehouse/placement/Capacity_Reloader.py::RELOADERS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'promote_popular': promote_popular_reloader, 'demote_unpopular': demote_unpopular_reloader, 'rebalance': rebalance_reloader}"
    },
    "Warehouse/placement/Capacity_Reloader.py::_SELECTORS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'promote_popular': promote_popular, 'demote_unpopular': demote_unpopular, 'rebalance': rebalance}"
    },
    "Warehouse/placement/Capacity_Reloader.py::demote_unpopular": {
      "decorators": [],
      "doc": "Lowest-frequency pallets in the lowest-D (prime) bins \u2014 evicting frees prime\nbins for higher-ranked items.",
      "is_async": false,
      "kind": "function",
      "signature": "(occupied, freq_of, bin_sku, D, k)"
    },
    "Warehouse/placement/Capacity_Reloader.py::demote_unpopular_reloader": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(**kw) -> Capacity_Reloader"
    },
    "Warehouse/placement/Capacity_Reloader.py::promote_popular": {
      "decorators": [],
      "doc": "Highest-frequency pallets in the highest-D (worst) bins \u2014 evicting lets the\nranked re-drain lift them into better available bins.",
      "is_async": false,
      "kind": "function",
      "signature": "(occupied, freq_of, bin_sku, D, k)"
    },
    "Warehouse/placement/Capacity_Reloader.py::promote_popular_reloader": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(**kw) -> Capacity_Reloader"
    },
    "Warehouse/placement/Capacity_Reloader.py::rebalance": {
      "decorators": [],
      "doc": "Both: spend half the budget demoting unpopular-in-prime, half promoting\npopular-in-bad (de-duplicated).",
      "is_async": false,
      "kind": "function",
      "signature": "(occupied, freq_of, bin_sku, D, k)"
    },
    "Warehouse/placement/Capacity_Reloader.py::rebalance_reloader": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(**kw) -> Capacity_Reloader"
    },
    "Warehouse/placement/__init__.py": {
      "decorators": [],
      "doc": "Warehouse.placement \u2014 The assignment functions themselves \u2014 the research subject.  Assignment_Functions builds the placement closures the manager dispatches through (see context/arch/resolver_hints.yml: those edges are DYNAMIC and invisible to static import analysis), and Capacity_Reloader is the bounded re-slot.\n\nSee README.md for what does and does not belong here.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/placement/policy.py": {
      "decorators": [],
      "doc": "policy \u2014 one placement family, declared once.\n\nCompeting placement policies are what this repo exists to measure, and adding one cost\n5-8 declarations across four packages: a pool class and builder here, an import plus a\n`_build_*` helper plus a `_RESTOCKS` row in `Optimization/config/strategies.py`, a branch in\n`strategy_runner._gain_bundle_for` naming the exact aisle books its `take` commits to, a\nname in `Inbound.gain.FAITHFUL_GAIN_FAMILIES`, rows in three dead registries, an equivalence\ntest and an arch hint.\n\nThree of those are already gone (the dead registries, deleted 2026-09-16). This record takes\nfour more: the positional `_RESTOCKS` tuple becomes named fields, `FAITHFUL_GAIN_FAMILIES`\nbecomes derived, the gain evaluator's per-family `aisle_state=` dicts become derived, and its\n`if restock == ...` chain becomes a lookup on `gain`.\n\n## Where the records live, and why not here\n\nThe TYPE is here because a placement family is a placement concept. The seventeen INSTANCES\nlive in `Optimization/config/strategies.py`, beside the `build` functions they carry: a build\ntakes `(mgr, StrategyContext)`, and `StrategyContext` is the run harness's own assembly of\nwhat a worker happens to have. Moving the records here would drag that up with them, and\n`wh_placement -> optimization` is a forbidden edge for exactly the reason it should be \u2014\n\"an assignment function must not import the run harness; it is a closure over its own\ninputs\".\n\n## `ledger_terms` is DECLARED, and then checked against the wiring\n\nIt names the `AisleLedger` books a family's `take` commits to. It has to be declared rather\nthan read off a pool, because the gain evaluator needs it BEFORE it builds one: it copies\nexactly those books so a virtual placement cannot advance the real warehouse. The\ndeclaration's own hazard is the one `_gain_bundle_for` states \u2014 \"a dict left off that list is\nnot a refusal, it is a virtual placement advancing the REAL warehouse\" \u2014 so it is not left\nas a claim: `Tests/unit/test_placement_policy.py` BUILDS each family's pool and compares the\ndeclaration against `AisleLedger.bound`, the books that pool was actually handed. A symbol\ntable that resolves names cannot catch a wrong relationship; exercising it can.",
      "kind": "module",
      "signature": null
    },
    "Warehouse/placement/policy.py::PlacementPolicy": {
      "decorators": [
        "dataclass(frozen=True)"
      ],
      "doc": "One restock (reorder placement) rule: what it needs, what it builds, what it commits.\n\nFrozen, because the grid in `strategies.py` reads these to build every `Strategy` and a\nmutable record would let one arm's setup change another's.",
      "kind": "class",
      "signature": ""
    },
    "Warehouse/placement/policy.py::PlacementPolicy.__post_init__": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> None"
    },
    "Warehouse/placement/policy.py::PlacementPolicy.state_names": {
      "decorators": [
        "property"
      ],
      "doc": "`ledger_terms` as the MANAGER attribute names the gain evaluator keys on.\n\nTwo vocabularies for the same books: the ledger calls them `sku_sets`, the manager\n`_aisle_sku_sets`, and `Inbound.gain`'s copier tables key on `aisle_sku_sets` because\n`Inbound/` may not import the placement engine.  The mapping is mechanical and lives\nhere so no third hand-written list exists to disagree.",
      "is_async": false,
      "kind": "function",
      "signature": "(self) -> tuple[str, ...]"
    },
    "Warehouse/placement/policy.py::__all__": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['PlacementPolicy', 'GAIN_ADAPTERS']"
    },
    "docs/experiments/ingest.py": {
      "decorators": [],
      "doc": "Pull a simulation run's curated files into a docs experiment folder.\n\nCopies, from a completed run's output directory (on the external drive) into\n``docs/experiments/<exp>/``, exactly the small files the site needs:\n\n  * per inventory \u00d7 config:  ``config.json`` + the curated comparison PNGs\n  * per inventory:           ``params.json`` + the inventory distribution PNGs\n\n``--source`` is the RUN ROOT.  Every run is a cell matrix, so the run root holds one\n``<cell>/`` subtree per what-if cell and ONE invocation now stages them all; previously this\nscript assumed the pre-cell flat tree (``<source>/<inv>/<cfg>``) and only worked if you pointed\nit at a single cell directory by hand, once per cell.\n\nThe docs-side layout is unchanged \u2014 ``images/<cell>/<inv>/<config>/`` \u2014 because that is what\n``docs/macros.py`` indexes and what every committed snapshot already uses.  The ``<channel>``\nlevel is flattened away by the recursive figure lookup (a store config only ever runs on the\nstore channel, and likewise for fulfillment), so no site path moves.\n\nInventories \u00d7 configs come from each cell's ``run_manifest.json``; for older runs without one it\nfalls back to the experiment's ``experiment.yml``.  With ``--gen-manifest`` it also writes a\nstarter ``experiment.yml`` you then edit (titles, short keys, winners).\n\nCross-cell what-if outputs at the run root (``whatif_delta.json`` + the ``whatif_*.png`` set) are\nstaged too \u2014 those used to be hand-copied and hand-renamed.\n\nUsage (run locally \u2014 CI has no drive access):\n    python docs/experiments/ingest.py --exp experiment-2         --source \"$COMPARISON_OUTPUT_DIR/comparison_20260801_120000\" --gen-manifest --dry-run\n    python docs/experiments/ingest.py --exp experiment-5 --source \"$COMPARISON_OUTPUT_DIR/comparison_whatif_...\"\n    python docs/experiments/ingest.py --exp experiment-5 --source \"$COMPARISON_OUTPUT_DIR/<run>\" --cell k1_off_lpt\n\nNothing here runs at site-build time; it just stages committed snapshots.",
      "kind": "module",
      "signature": null
    },
    "docs/experiments/ingest.py::DEFAULT_WHATIF_DATA": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "['whatif_delta.json']"
    },
    "docs/experiments/ingest.py::DEFAULT_WHATIF_PNG_GLOB": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'whatif_*.png'"
    },
    "docs/experiments/ingest.py::WHATIF_PNG_GLOB": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'whatif_*.png'"
    },
    "docs/experiments/ingest.py::_DEFAULTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_registry_lists('default')"
    },
    "docs/experiments/ingest.py::_DOCS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.dirname(os.path.abspath(__file__)))"
    },
    "docs/experiments/ingest.py::_FIGURE_DIR_PREFERENCE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('figures/diagnostics', 'figures/headline', 'figures/labor', 'figures/layout', 'figures/significance', 'figures/task_time', 'figures/throughput', 'figures/trajectories', 'figures/yard', 'tables')"
    },
    "docs/experiments/ingest.py::_GROUP_PROBE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'whatif': ('whatif_delta_json',), 'dossier': ('dossier_dir',)}"
    },
    "docs/experiments/ingest.py::_LEGACY": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "_registry_lists('legacy')"
    },
    "docs/experiments/ingest.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', '..'))"
    },
    "docs/experiments/ingest.py::_RUN_DATA_EXT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('.json', '.csv')"
    },
    "docs/experiments/ingest.py::_RUN_GROUPS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('whatif', 'dossier')"
    },
    "docs/experiments/ingest.py::_cell_inventory_configs": {
      "decorators": [],
      "doc": "(run_manifest|None, inventories, configs) for one cell \u2014 manifest first, experiment.yml next.\n\nThe manifest's location comes from the contract (`run_manifest`) when the run has one; the\nliteral join stays for pre-descriptor sources and single-cell invocations.",
      "is_async": false,
      "kind": "function",
      "signature": "(cell_dir, exp_dir, ymldoc, log, rt=None, cell=None)"
    },
    "docs/experiments/ingest.py::_cells_of": {
      "decorators": [],
      "doc": "(resolver|None, [(cell_name, cell_dir), \u2026], schema_id|None) for a run root.\n\nUses the versioned run-tree resolver, and hands it back so every later lookup (run_spec,\nrun_manifest, figures, what-if outputs) resolves through the run's OWN contract.  When the\ndescriptor is missing (a pre-v1 run, or a single CELL directory passed the old way), fall\nback to treating `source` itself as one cell so an existing workflow still stages \u2014 but say\nso, because the cell name then comes from the directory name rather than the run.",
      "is_async": false,
      "kind": "function",
      "signature": "(source, log)"
    },
    "docs/experiments/ingest.py::_copy": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(src, dst, dry, log)"
    },
    "docs/experiments/ingest.py::_declared_png_names": {
      "decorators": [],
      "doc": "Every PNG basename figures.yml names, RETIRED ENTRIES INCLUDED.\n\nDeliberately the same set `context/guards/experiment_guard.py._registry_png_names`\nbuilds, because the prune pass below and that guard must not be able to disagree: one\ndeletes and the other reports, and a file one considers an orphan while the other does\nnot is a build that fails after the cleanup already ran.\n\nA `retired:` entry keeps protecting its staged copies on purpose \u2014 retired means the\nwriter is gone, not that the evidence a published page cites should vanish.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> set"
    },
    "docs/experiments/ingest.py::_find": {
      "decorators": [],
      "doc": "First path matching `name` anywhere under `root` (run_analysis nests PNGs in\ncompare/top, compare/breakdown, compare/overlay, \u2026).\n\nThe NO-CONTRACT lookup: pre-descriptor runs and the profiles tree (which has no run-tree\ncontract at all) resolve through this walk.  Runs with a descriptor resolve through\n`_leaf_file` instead.",
      "is_async": false,
      "kind": "function",
      "signature": "(root, name)"
    },
    "docs/experiments/ingest.py::_inv_root_from_bindings": {
      "decorators": [],
      "doc": "Resolve the catalogue's inventory dir BY NAME from the run's recorded pair binding.\n\nThe binding (the run-layout descriptor's `pair_bindings`, v2) names the profile_run +\nprofile, so the catalogue resolves under ANY profiles root \u2014 including after the tree moved\ndrives, which is exactly when the sim_meta absolute path (the fallback below) goes dead.\nNone when the run predates v2, the binding is null (pre-contract catalogue), or the resolved\ndir does not exist under this root.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, inv, profiles_root, log)"
    },
    "docs/experiments/ingest.py::_leaf_file": {
      "decorators": [],
      "doc": "One curated file at a (cell, inventory, config) leaf, or None if absent.\n\nWith a resolver, `config.json` is the contract's `config_json` artifact, and a figure is\nlooked up inside the CONTRACT-RESOLVED channel-run leaf (`rt.channel_runs`) \u2014 the resolver\nconsumes the `<channel>` level, so this function never has to guess whether one exists.\nWithin a leaf the pick is by the EXPLICIT dir preference above (which analysis subtree owns\na duplicated basename is now data, not walk order); `_find` remains for figures outside\nthat vocabulary, and \u2014 without a resolver (pre-descriptor run, `--source` at a single cell\ndir) or for a leaf not yet finalized \u2014 over the config dir, the unchanged legacy route.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell, inv, cfg, name, cfg_src)"
    },
    "docs/experiments/ingest.py::_load_env": {
      "decorators": [],
      "doc": "Inject KEY=VALUE pairs from *path* into os.environ (shell vars take priority).\n\nByte-for-byte the entry-script idiom in `Warehouse/generation/generate_mixed_profile.py`\nand `generate_profile_suite.py`; `Optimization/config/sim_config.py` carries its own.\n\nWITHOUT this, `--profiles-root`'s documented `$PROFILE_INPUT_DIR` default could never\npopulate: the key lives in `.env`, nothing here read it, so `os.getenv` returned None,\n`_inv_root_from_bindings` returned at its first guard, and BY-NAME catalogue resolution\nsilently fell through to the per-leaf meta file's recorded ABSOLUTE `inv_db` \u2014 the one\nroute pair_bindings exists to replace, and the one that does not survive a moved drive.\nNo warning was emitted, because the fallback is a legitimate path for pre-v2 runs.",
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> None"
    },
    "docs/experiments/ingest.py::_prune_orphan_pngs": {
      "decorators": [],
      "doc": "Delete staged PNGs whose basename no live registry entry declares.\n\nStaging COPIES; it has never removed anything.  So when a figure is renamed \u2014 and\n`delta_travel_vs_baseline.png` became `percent_travel_per_arm.png` the day figure views\nstopped being editorial \u2014 the new file lands beside the old one, and the old one stays\ncommitted, staged, and reachable, describing a chart the suite no longer draws.\n\nNothing caught that except `context/guards/experiment_guard.py --scan`, which runs at\nthe END of the publish loop and reports it for a human to chase.  The guard's rule is\n\"a PNG with no declared producer\", so applying exactly that rule HERE means ingest and\nthe guard cannot disagree: the orphan is gone before the guard looks.\n\nConservative by construction \u2014 only under `images/`, only `.png`, and only a basename\nthe registry does not name at all.  What-if PNGs are exempt: their producer is a\nrunschema writer, which figures.yml never lists.",
      "is_async": false,
      "kind": "function",
      "signature": "(exp_dir, dry, log)"
    },
    "docs/experiments/ingest.py::_reader_tree": {
      "decorators": [],
      "doc": "A HEAD-contract resolver for an ANALYSIS artifact, falling back to `rt`.\n\nThin alias for `runschema.reader_for`, which now owns the rule \u2014 contract SELECTION\nbelongs in the resolver layer, and the analysis suite's run-scope evaluations hit the\nidentical problem from the other side.  The argument (and the silent failure it\nprevents) lives in that docstring.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, artifact)"
    },
    "docs/experiments/ingest.py::_registry_lists": {
      "decorators": [],
      "doc": "(top3, full_suite, inventory_plots) for entries carrying `flag`, or None without\npyyaml.",
      "is_async": false,
      "kind": "function",
      "signature": "(flag: str)"
    },
    "docs/experiments/ingest.py::_repo_provenance_of": {
      "decorators": [],
      "doc": "(commit, dirty) for a run root \u2014 the simulator CODE that produced it.\n\nRead from the run root's ``run_spec.json`` (``_write_run_spec`` stamps it there), which is the\nonly file on a run that distinguishes two runs of the same tree and the same tables from\ndifferent code.  Runs made before the stamp existed have no such keys and yield\n``('unknown', None)``; that is a fact about the run, so it is recorded and rendered rather than\nquietly dropped.  With a resolver the location comes from the contract (`run_spec`); the\nliteral join stays for pre-descriptor sources.",
      "is_async": false,
      "kind": "function",
      "signature": "(source, log, rt=None)"
    },
    "docs/experiments/ingest.py::_short_key": {
      "decorators": [],
      "doc": "Derive a short inventory key from a pair label, e.g.\n'mixed_20260624_083549__mixed_realistic_lt0' -> 'lt0'.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_id)"
    },
    "docs/experiments/ingest.py::_stage_inventory_assets": {
      "decorators": [],
      "doc": "params.json + the inventory distribution plots.\n\nLocated, in preference order: (1) BY NAME via the run's recorded `pair_bindings` under\n--profiles-root (v2 run_layout + a descriptor-bearing catalogue \u2014 survives drive moves);\n(2) via a sim_meta.json's recorded inv_db ABSOLUTE path \u2014 the frozen legacy route, load-\nbearing for every pre-v2 run.  The `_find` walks on the resolved root remain: the profile\nplots live in generator-owned subtrees the docs deliberately flatten.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_src, inv, exp_dir, args, inv_plots, log, rt=None, cell=None)"
    },
    "docs/experiments/ingest.py::_stage_leaf_tables": {
      "decorators": [],
      "doc": "The per-leaf arm-vs-baseline table into data/<cell>/<pair>/<config>/.\n\nThe headline figure prints an effect size, an interval and a corrected p for each\narm; before this stage those numbers existed only as pixels, and a reader who wanted\nto check one \u2014 or to ask why an arm that is NOT on the podium missed it \u2014 had nothing\nto open.  Resolver-only and reached by NAME, like the rollup stage beside it.",
      "is_async": false,
      "kind": "function",
      "signature": "(exp_dir, dry, log, rt=None, cells=())"
    },
    "docs/experiments/ingest.py::_stage_rollups": {
      "decorators": [],
      "doc": "Per-cell channel-rollup CSVs into data/<cell>/ \u2014 the per-arm labor rows and the\nper-channel best/saving summary the labor page's headline quotes.\n\nAdded for Experiment 8 after a reviewer traced the labor headline to the rollup\nsummary and found the site had never staged it: every quoted number must have a\ncommitted, diffable source.  Resolver-only (the artifacts are\ncell-scope contract members reached by NAME via `rt.rollup_csv` / `rt.path` \u2014 no\nfilename literals here, per the run-tree consumption ratchet); a pre-descriptor\ntree logs a NOTE and stages nothing, matching the other resolver-gated stages.",
      "is_async": false,
      "kind": "function",
      "signature": "(exp_dir, dry, log, rt=None, cells=())"
    },
    "docs/experiments/ingest.py::_stage_whatif": {
      "decorators": [],
      "doc": "Run-root outputs \u2014 the cross-cell what-if summaries AND the run dossier \u2014 into\ndata/ + images/.\n\nwhatif_delta.json is the file docs/macros.py:whatif_matrix() reads; before this it had no\nproducer and no ingest path, so it was hand-copied from whatif_labor.json under a new name.\nAbsent on a single-cell run, which is not an error.\n\nWith a resolver the candidates come from the contract's GROUP TAGS (`_RUN_GROUPS`), so a\nnew document its writers emit stages itself with no edit here; what is STAGED is\nshape-based, never name-based: every group data file into data/ (the committed sources\nthe prose quotes) and every group PNG into images/.  That is why this function can serve\ntwo producers where `_stage_rollups` and `_stage_leaf_tables` name their artifacts one at\na time and cost an edit each.  The literal joins below remain the pre-descriptor route.",
      "is_async": false,
      "kind": "function",
      "signature": "(source, exp_dir, dry, log, rt=None)"
    },
    "docs/experiments/ingest.py::_write_starter_manifest": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(exp_dir, rm, catalogue, top3, full_suite, inv_plots, dry, log, cells=None, schema_id=None, commit=None, dirty=None)"
    },
    "docs/experiments/ingest.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None)"
    },
    "docs/experiments/ingest.py::yaml": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "None"
    },
    "docs/experiments/site_tree.py": {
      "decorators": [],
      "doc": "site_tree.py \u2014 the staged docs tree, declared as data.\n\nThe website's file layout under ``docs/experiments/<exp>/`` is a six-template contract with\none writer (ingest.py, which copies files IN) and one reader (docs/macros.py, which renders\npaths OUT at mkdocs build time).  Both sides historically retyped the joins; this module is\nthe single declaration, rendered through the same ``Schema.pathtpl`` vocabulary as the run\ntree and the profiles tree, so a site path can only move by editing a template here \u2014 where\na golden test and the committed snapshots will notice.\n\nmacros.py deliberately does NOT import this module: it runs inside mkdocs (mkdocs-macros\nloads it standalone, and the site must build from a bare checkout with no guarantee about\nsys.path).  Its f-string joins stay, PINNED to these templates by\n``Tests/architecture/test_site_tree.py`` \u2014 the same literal-equals-render tie the ingest\ndestinations get by construction.\n\nAll paths are RELATIVE to the experiment dir (``docs/experiments/<exp>/``); ingest joins them\nonto its ``exp_dir``, macros onto the site root.  ``{figure}``/``{plot}``/``{fname}`` are\nbasenames from the curated lists in ``experiment.yml``.",
      "kind": "module",
      "signature": null
    },
    "docs/experiments/site_tree.py::TEMPLATES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'config_json': 'images/{run}/{inv}/{cfg}/config.json', 'figure_png': 'images/{run}/{inv}/{cfg}/{figure}', 'pair_params': 'data/{inv}/params.json', 'run_data': 'data/{fname}', 'run_png': 'images/{f..."
    },
    "docs/experiments/site_tree.py::_REPO_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', '..'))"
    },
    "docs/experiments/site_tree.py::path": {
      "decorators": [],
      "doc": "Render template `name` with `parts`, joined under `exp_dir` (native separators).\nA missing part raises KeyError by name \u2014 same contract as every other tree resolver.",
      "is_async": false,
      "kind": "function",
      "signature": "(name: str, exp_dir: str='', **parts) -> str"
    },
    "docs/macros.py": {
      "decorators": [],
      "doc": "mkdocs-macros helpers: inject run parameters and formulas into the docs.\n\nValues are pulled from JSON snapshots committed per experiment under\n``docs/experiments/<exp>/`` (raw run outputs live on an external drive, gitignored;\nCI only has the repo). Each ``config.json`` sits next to its run's images under\n``images/<run>/<inv>/<cfg>/``; inventory ``params.json`` under ``data/<inv>/``.\n\nTwo modes, decided per page by its experiment folder:\n  * **manifest mode** \u2014 the experiment has an ``experiment.yml`` (single source of\n    truth for its run id, inventory keys\u2192ids, configs, figures). Pages call macros with\n    a short inventory key, e.g. ``setup_table('lt0')``; run/config/figures resolve from\n    the manifest. This is how new experiments (2+) are authored. See the ingest script\n    ``docs/experiments/ingest.py`` and ``scripts/new_experiment.py``.\n  * **legacy mode** \u2014 no ``experiment.yml`` (Experiment 1). Pages pass explicit\n    ``(run, inv, cfg)`` and the module defaults (``_CONFIGS`` etc.) apply. Unchanged.\n\nFormula *shapes* are fixed here from the code that defines them \u2014 the pick-time calibrations\nfrom ``Optimization/run_simulation.py``, the stock levels from\n``Optimization/simconfig/coverage.py``, which is where a RUN declares them (the generator\nauthors none since ADR-0002). Only the *numbers* come from JSON, so pages never hard-code a\nvalue twice.\n\nThe one long-standing exception is closed. ``assignment_formulas`` used to transcribe the\ntop-3 assignment-function *equations* by hand, because no snapshot emitted them; this\ndocstring asked for \"a small registry\" so the macro could read them like the others, and\n``Optimization/config/objectives.py`` is it. Every run now emits its rule catalogue, and\n``rule_catalog()`` renders it \u2014 with a test tying each objective to the symbol its builder\nactually calls, which is the part hand-maintenance could never offer.\n\n``assignment_formulas`` survives FROZEN, for the pre-dossier experiments whose pages call\nit and whose runs have no catalogue staged. Nothing new may use it: a new experiment reads\nthe artifact.",
      "kind": "module",
      "signature": null
    },
    "docs/macros.py::_CACHE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{}"
    },
    "docs/macros.py::_verify_manifest_schema": {
      "decorators": [],
      "doc": "Check the experiment's recorded run-tree contract still matches the macros' path scheme.\n\n``ingest.py`` stamps the source run's ``schema_id`` into ``experiment.yml`` \u2014 the content\naddress of the run-tree contract the staged snapshot was pulled from\n(``Optimization/schemas/run_tree/<short>.json``).  Every image path a macro constructs is\n``images/<run>/<inv>/<cfg>/\u2026``, i.e. the contract's three REQUIRED levels\n(cell/pair/config) with the optional ``<channel>`` flattened away by ingest.  If a future\ncontract changes those levels, snapshots staged from it no longer mean what these paths say \u2014\nso fail the build LOUDLY here, naming the schema, instead of rendering pages whose figures\nsilently come from somewhere else.  Manifests without a ``schema_id`` (Experiments 1\u20135\npredate the stamp) are exempt: nothing was recorded, so there is nothing to verify.",
      "is_async": false,
      "kind": "function",
      "signature": "(manifest, docs_dir, exp)"
    },
    "docs/macros.py::define_env": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(env)"
    },
    "docs/macros.py::define_env._census_row": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(metric, grouping, group=None)"
    },
    "docs/macros.py::define_env._cfg_list": {
      "decorators": [],
      "doc": "[(cfg_name, label)] from the manifest, else the module default.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env._declared_three": {
      "decorators": [],
      "doc": "Whether a declaration carries all three inputs a page can name.",
      "is_async": false,
      "kind": "function",
      "signature": "(d)"
    },
    "docs/macros.py::define_env._exp_dir": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env._figs": {
      "decorators": [],
      "doc": "[(filename, caption, width)] for kind in {'top3','full_suite','run_suite'}.\n\nA manifest names its own figures.  WITHOUT one the experiment is a legacy\nsnapshot, and it renders the registry's `legacy` set \u2014 frozen to what that sweep\nactually produced \u2014 NOT the `default` starter set, which names the figures the\nCURRENT suite writes and which a 2026-06 run never had.  Conflating the two\npointed Experiment 1 at chart names that postdate it by two months.",
      "is_async": false,
      "kind": "function",
      "signature": "(kind)"
    },
    "docs/macros.py::define_env._floor_lines": {
      "decorators": [],
      "doc": "`floor_lines` in words: \"1 line\", \"1.5 lines\".\n\nThe floor is declared in LINES of the SKU's own mean line, so the singular earns its\nkeep \u2014 the default declares exactly one pick's worth of the SKU, and \"1 line(s)\" on a\npublished page reads like a macro that could not be bothered.",
      "is_async": false,
      "kind": "function",
      "signature": "(v)"
    },
    "docs/macros.py::define_env._floor_words": {
      "decorators": [],
      "doc": "The declared line floor as words, or None when the snapshot does not carry it.\n\n`floor_text` is rendered by the inventory document itself (ADR-0004: the floor is\nSOLVED per section under the era, so `floor_lines` is the INPUT and None when\nsolved); an older snapshot carries only the scalar, which is rendered here.",
      "is_async": false,
      "kind": "function",
      "signature": "(d)"
    },
    "docs/macros.py::define_env._fmt_brackets": {
      "decorators": [],
      "doc": "Human-legible M(y) brackets for a calibration, e.g. '\u00d71.0 (y<96\u2033) \u00b7 \u2026'.\nConfigs with no height brackets (e.g. the fulfillment channel, whose short bins\nnever reach a higher tier) render as flat \u00d71.",
      "is_async": false,
      "kind": "function",
      "signature": "(cfg)"
    },
    "docs/macros.py::define_env._fmt_fn": {
      "decorators": [],
      "doc": "Render a pick-time term like 0.58\u00b7w^1.5 or 0.7\u00b7log2(v).",
      "is_async": false,
      "kind": "function",
      "signature": "(coef, fn, var)"
    },
    "docs/macros.py::define_env._fmt_fn_tex": {
      "decorators": [],
      "doc": "LaTeX form of a pick-time term: pow:1.5 -> ``0.58\\,w^{1.5}``,\nlog:2 -> ``0.7\\,\\log_{2} V``.",
      "is_async": false,
      "kind": "function",
      "signature": "(coef, fn, var)"
    },
    "docs/macros.py::define_env._fmt_spec": {
      "decorators": [],
      "doc": "One-line summary of a dimension/size distribution spec.",
      "is_async": false,
      "kind": "function",
      "signature": "(spec)"
    },
    "docs/macros.py::define_env._fmt_weight": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(spec)"
    },
    "docs/macros.py::define_env._hbrackets": {
      "decorators": [],
      "doc": "Height-bracket rows for a config: manifest first, else the code-sourced map.",
      "is_async": false,
      "kind": "function",
      "signature": "(cfg)"
    },
    "docs/macros.py::define_env._inv1": {
      "decorators": [],
      "doc": "-> inv id.  legacy: (inv_id,);  manifest: (inv_key,).",
      "is_async": false,
      "kind": "function",
      "signature": "(args)"
    },
    "docs/macros.py::define_env._load_cfg": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run, inv, cfg)"
    },
    "docs/macros.py::define_env._load_json": {
      "decorators": [],
      "doc": "Load a JSON file relative to the docs dir, cached. Fails loudly so a\ntypo'd path breaks `mkdocs build --strict` instead of rendering blank.",
      "is_async": false,
      "kind": "function",
      "signature": "(rel_path)"
    },
    "docs/macros.py::define_env._load_params": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(inv)"
    },
    "docs/macros.py::define_env._load_yaml": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(rel_path)"
    },
    "docs/macros.py::define_env._manifest": {
      "decorators": [],
      "doc": "The parsed experiment.yml for the current page's experiment, or None (legacy).\n\nIn manifest mode the manifest's recorded `schema_id` (if any) is verified against the\ncommitted run-tree contract ONCE per experiment per build \u2014 a mismatch raises, which\n`mkdocs build --strict` turns into a build failure.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env._num": {
      "decorators": [],
      "doc": "Trim trailing zeros: 15.0 -> '15', 0.58 -> '0.58', 1.5 -> '1.5'.",
      "is_async": false,
      "kind": "function",
      "signature": "(x)"
    },
    "docs/macros.py::define_env._pct": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(v, places=1)"
    },
    "docs/macros.py::define_env._registry": {
      "decorators": [],
      "doc": "The figure registry (experiments/figures.yml) \u2014 THE caption/section/default\nsource.  This module once carried three hardcoded caption tables that drifted\nindependently of ingest's staging lists and every experiment.yml; now all three\nconsumers read one declaration, and Tests/architecture/test_figure_registry.py\nties it to the code that writes each figure.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env._ri": {
      "decorators": [],
      "doc": "-> (run, inv).  legacy: (run, inv);  manifest: (inv_key,).",
      "is_async": false,
      "kind": "function",
      "signature": "(args)"
    },
    "docs/macros.py::define_env._ric": {
      "decorators": [],
      "doc": "-> (run, inv, cfg).  legacy: (run, inv, cfg);  manifest: (inv_key[, cfg]).",
      "is_async": false,
      "kind": "function",
      "signature": "(args)"
    },
    "docs/macros.py::define_env._run_doc": {
      "decorators": [],
      "doc": "A staged dossier document by short name; raises when it is not staged.\n\nLoud on purpose, matching `_verify_manifest_schema`: under `--strict` a missing\ndocument must fail the build.  A macro that degraded to a blank sentence would put\nan EMPTY claim on a published page, which is worse than no page at all.",
      "is_async": false,
      "kind": "function",
      "signature": "(kind)"
    },
    "docs/macros.py::define_env._stock_declaration": {
      "decorators": [],
      "doc": "This experiment's stock-level DECLARATION for one pair, or None on a pre-ADR-0002\nsnapshot.  ``{}`` means \"declared, but this snapshot does not record the inputs\".\n\nThe soft counterpart to `_run_doc`, and the exception is earned: here the absence is\nDATA, not a staging mistake.  Two absences, and they mean the same thing \u2014\n\n  * no inventory document staged at all (Experiments 1\u20137 predate the run-scope\n    documents entirely), and\n  * a document whose pairs still carry `rop_formulas`, the closed-form scores that\n    only ever existed while a CATALOGUE authored the levels (Experiment 8).\n\nBoth identify a run whose levels came with its inventory rather than from its own\ndeclaration, and its page must keep describing what it ran \u2014 the same rule\n`pick_time_formula` follows for a config that predates the per-item charge.  The\nvintage is read across the whole document, never per pair, so a pair this page cannot\nname never flips an archive into the current model by accident.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv)"
    },
    "docs/macros.py::define_env._stock_footnote": {
      "decorators": [],
      "doc": "The sentence under the distribution table \u2014 one per document vintage.\n\nA snapshot staged BEFORE ADR-0002 carries `rop_formulas`: the closed forms the\ncatalogue evaluation scored against the levels its CATALOGUE authored, published\nbecause \"neither form holds\" deserved to be a measurement rather than an\nassertion.  Those experiments keep the sentence they published, word for word \u2014\nit describes what they ran, exactly as `pick_time_formula` keeps rendering a\nconfig that predates the per-item charge without the term.\n\nA snapshot WITHOUT the block is post-ADR-0002, and there the score has no subject\nleft: no closed form authors a level, so reproducing one would measure the\nagreement of two things the run never used.  What this says instead is whose\nnumbers these are and which declaration produced them.",
      "is_async": false,
      "kind": "function",
      "signature": "(m)"
    },
    "docs/macros.py::define_env._unused_stock_declaration_tail": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(inv, pairs)"
    },
    "docs/macros.py::define_env.assignment_formulas": {
      "decorators": [
        "env.macro"
      ],
      "doc": "FROZEN. Hand-transcribed equations for the top-3 assignment functions.\n\nSuperseded by `rule_catalog()`, which renders the catalogue the run itself emits\nfrom `Optimization/config/objectives.py` \u2014 all 17 rules, each tied by test to the\nsymbol its builder actually calls. This survives only for the experiments whose\nruns predate the dossier and whose pages still call it; those pages must keep\nbuilding, and their runs have no catalogue to read.\n\nNothing new may use it. A new experiment calls `rule_catalog()`.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env.census_claim": {
      "decorators": [
        "env.macro"
      ],
      "doc": "One count/span claim, composed from the census artifact.\n\nRenders the sentence the pages used to type by hand beside a figure: how many\ncomparisons went the claimed way, the exact sign test and its interval, the median\nand the span.",
      "is_async": false,
      "kind": "function",
      "signature": "(metric, grouping='channel', group=None, places=1)"
    },
    "docs/macros.py::define_env.census_table": {
      "decorators": [
        "env.macro"
      ],
      "doc": "The full census grid for one metric \u2014 every group, plus the overall row.",
      "is_async": false,
      "kind": "function",
      "signature": "(metric, grouping='channel', places=1)"
    },
    "docs/macros.py::define_env.experiment": {
      "decorators": [
        "env.macro"
      ],
      "doc": "The current page's experiment manifest (dict), or {} in legacy mode. Lets\nmanifest-driven template pages loop, e.g.\n``{% for k, inv in experiment().inventories.items() %}``.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env.full_suite_section": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Collapsible per-config full-suite figure blocks (all strategies) for one\nrun/inventory variant \u2014 used by the compiled Full-results report.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.held_fixed_table": {
      "decorators": [
        "env.macro"
      ],
      "doc": "What the sweep varied, what it held fixed, and what it has no knob for.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env.inv_distribution_table": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Per-category distribution table for an inventory variant's params.json.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.inv_lead_time": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Short human description of a variant's replenishment lead time.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.inv_params": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Parsed inventory params.json \u2014 legacy (inv_id) or manifest (inv_key).",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.inventory_model": {
      "decorators": [
        "env.macro"
      ],
      "doc": "The realised inventory model \u2014 distributions, not a formula.\n\nPublishing a distribution is not a retreat from a formula that would not hold: a\nstock level is a RUN's declaration, never a SKU's fact (ADR-0002).  The catalogue\ncarries none, every run derives its own at setup from a coverage in DAYS, and\nwhat that produced across the SKUs it fielded IS the model \u2014 there is nothing\nclosed-form behind it to publish instead.\n\nThe table renders whichever quantities the staged document carries, so each\nvintage shows its own: a pre-ADR-0002 snapshot measured the per-wave demand its\nauthored level was coverage of, a current one measures the lead pipeline the run\nstamps.  `_stock_footnote` carries the sentence underneath.",
      "is_async": false,
      "kind": "function",
      "signature": "(inv_key=None)"
    },
    "docs/macros.py::define_env.pick_calibration_table": {
      "decorators": [
        "env.macro"
      ],
      "doc": "One row per calibration: the weight/volume handling terms (from each config's\nJSON) and the height M(y) brackets (manifest, else code-sourced map).",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.pick_time_formula": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Pick-time cost model (LaTeX) with this config's calibrated coefficients.\nMatches Warehouse/picking/Pick.py:\n    t_pick = M(y)\u00b7(t0 + q\u00b7t1 + q\u00b7h) + c_cart\u00b71[cart swap]\nwhere t1 is the per-item charge (ADR-0001).  An archived config WITHOUT a\n`pick_per_item` key predates the charge: it ran the model without the term, so\nthe term is rendered absent rather than at this checkout's default \u2014 the page\ndescribes the run, not the current code.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.reorder_formula": {
      "decorators": [
        "env.macro"
      ],
      "doc": "How this run's stock levels were SET, and the averages it fielded.\n\nTwo vintages, told apart by `_stock_declaration` off the staged inventory\ndocument, and the page describes ITS OWN run either way:\n\n  * a run that DECLARED its levels (ADR-0002 onward) gets the derivation it\n    actually used \u2014 a coverage in DAYS against each SKU's expected daily demand,\n    floored at a line of that SKU's own demand \u2014 with the declared inputs quoted\n    when the snapshot records them;\n  * an archived run gets the authored model it ran, NAMED as retired: its\n    catalogue carried the levels and the planner rescaled them to fit the\n    warehouse afterwards, which is why no closed form reproduced the stored\n    values.\n\nWhat this will not do is print an equation beside numbers it does not produce.\nThe archived branch says so outright; the declared branch's formulas ARE the ones\nthat produced its averages, which is the whole of the difference ADR-0002 made.\n`inventory_model()` publishes the distribution behind the averages in both.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.rule_catalog": {
      "decorators": [
        "env.macro"
      ],
      "doc": "The placement-rule catalogue, generated from the registry the run emitted.\n\nReplaces per-rule prose that existed in three places at once.  Anchors reproduce\nthe ones the docs already link to (`#rank-labor`, `#map`, \u2026) \u2014 `docref_guard`\nchecks a different reference form, so a renamed anchor here would be a silently\nbroken published link.",
      "is_async": false,
      "kind": "function",
      "signature": "(only=None, family=None)"
    },
    "docs/macros.py::define_env.rule_cost_table": {
      "decorators": [
        "env.macro"
      ],
      "doc": "What each rule costs to RUN \u2014 real CPU seconds, not modeled warehouse labor.",
      "is_async": false,
      "kind": "function",
      "signature": "(channel='store', top=None)"
    },
    "docs/macros.py::define_env.run_commit": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Which simulator CODE produced this experiment's run, rendered for a caption.\n\nThe one ``schema_id`` a page can already cite names the run's DB **table** contract, and a\nchange that moves no table is byte-identical in it: ``753d01e`` shifted absolute throughput\n~1.4 % and left every id on the run unchanged. This reads the commit that\n``Optimization/runschema/sim_manifest.py`` stamps into ``run_spec.json`` and\n``docs/experiments/ingest.py`` carries into ``experiment.yml``.\n\nTwo lookup sites, because a run has two names. A **what-if** experiment's ``run:`` is a\nCELL inside the sweep, so the run root \u2014 and therefore its commit \u2014 lives beside\n``whatif.source_run``; an ordinary experiment's ``run:`` IS the run and its commit sits at\nthe top level next to ``schema_id``, which is where ingest writes it.\n\nRuns that predate the stamp render as *not recorded* rather than silently as nothing: the\nabsence is the point, and a page that quietly omitted it would be the same invisible gap\nthis macro exists to close.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env.run_config": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Parsed config.json \u2014 legacy (run,inv,cfg) or manifest (inv_key[,cfg]).",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.run_section": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Collapsible per-config figure blocks for one run/inventory variant.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.run_suite_section": {
      "decorators": [
        "env.macro"
      ],
      "doc": "The RUN-scope figure blocks \u2014 one set per run, not one per config leaf.\n\n`full_suite_section` composes `images/{run}/{inv}/{cfg}/{fname}`, which is right for\nevery figure a channel-run leaf produces and wrong for one the run root produces.\nThe `cost` family renders at run scope and ingest stages it flat, so it gets its own\nsection and its own path here.  A registry test ties the section to the\nevaluation's declared scope in both directions, so neither can move alone.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env.setup_table": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Markdown table of the headline simulation setup for a run/inv/config.",
      "is_async": false,
      "kind": "function",
      "signature": "(*args)"
    },
    "docs/macros.py::define_env.whatif_matrix": {
      "decorators": [
        "env.macro"
      ],
      "doc": "Cross-cell delta table for a what-if experiment, from data/whatif_delta.json.\n\nOne row per matrix cell, per-channel **median** \u0394(task makespan = labor) and\n\u0394(throughput / batch makespan) vs the reference cell (generated from whatif_delta.csv by the\nrun's post-processing). Only a WHAT-IF experiment has this JSON; ordinary single-run\nexperiments never call this macro. Numbers live in the committed JSON, so the page holds only\nprose.  Key-tolerant: prefers the four-metric keys (dthr_batch/dtask_ms/dbatch_ms/dthr_task)\nand falls back to the legacy dthr/dlabor so older experiment JSON still renders.",
      "is_async": false,
      "kind": "function",
      "signature": "()"
    },
    "docs/macros.py::define_env.whatif_matrix._med": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ch, *keys)"
    },
    "docs/macros.py::define_env.whatif_matrix.lab": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ch)"
    },
    "docs/macros.py::define_env.whatif_matrix.pct": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(v)"
    },
    "docs/macros.py::define_env.whatif_matrix.thr": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(ch)"
    },
    "scripts/archive_cells.py": {
      "decorators": [],
      "doc": "archive_cells.py \u2014 move completed what-if cells off the hot drive while the run continues.\n\nA finished cell is ~99.8 % SQLite: the sim DBs and their keyframe siblings. The hot drive cannot\nhold a whole sweep, so each cell has to move to slower bulk storage as soon as it is done \u2014 without\ndisturbing the simulation, and without breaking anything that reads the cell later.\n\nTwo facts shape the whole design:\n\n  * A cell is quiescent the moment its scenario returns. Cells run strictly sequentially and no\n    handle outlives an arm (every SQLite connection is opened and closed inside a save call; the\n    only long-lived files, run.log and runtime_metrics.db, live at the RUN ROOT, not in a cell).\n  * But analysis runs AFTER every cell and opens each sim DB by the ABSOLUTE path recorded in\n    sim_meta.json, and the cross-cell what-if scans need every cell at once. So a plain move\n    breaks the run.\n\nThe resolution is a Windows directory junction. Python does not classify a junction as a link --\nos.path.islink is False, is_dir() is True -- and this repo contains no follow_symlinks=False,\nislink, or scandir call anywhere. So every walker and every absolute db_path keeps working through\nit, unchanged. The cell's bytes are on the cold drive; its path is not.\n\nBecause the cold drive is slow, this analyses each cell BEFORE moving it: the heavy per-arm reads\nhappen while the data is still local, and only the light cross-cell scan (one aggregate query per\nDB) ever crosses to bulk storage. Run the simulation with --no-analyze and let this drive analysis.\n\nConfiguration: COLD_DRIVE in .env, the same file that holds COMPARISON_OUTPUT_DIR. Importing the\nsim config injects it into the environment; nothing here hardcodes a location.\n\n    python scripts/archive_cells.py --run RUN --dry-run\n    python scripts/archive_cells.py --run RUN --watch          # loop while the sim runs\n    python scripts/archive_cells.py --run RUN --cell k1_off_rr",
      "kind": "module",
      "signature": null
    },
    "scripts/archive_cells.py::RETRY_ERRNOS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "(5, 32)"
    },
    "scripts/archive_cells.py::STAGING_SUFFIX": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "'.__archiving'"
    },
    "scripts/archive_cells.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "scripts/archive_cells.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.normpath(os.path.join(_HERE, '..'))"
    },
    "scripts/archive_cells.py::_SIDECAR_SUFFIXES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('-wal', '-shm')"
    },
    "scripts/archive_cells.py::_existing_ancestor": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> str"
    },
    "scripts/archive_cells.py::_in_flight": {
      "decorators": [],
      "doc": "resume.pkl / _ckpt_*.pkl are removed at finalize; their presence means an arm is live.\n\nThe names come from the CONTRACT (`resume_pkl` / `checkpoint_pkl`), not from hardcoded\nliterals: preflight declared both transients, so renaming either in schema.py moves this\nprobe with it instead of silently blinding it.  Returned as sorted cell-relative paths, the\nsame shape the old cell walk produced.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str) -> list"
    },
    "scripts/archive_cells.py::_quick_check": {
      "decorators": [],
      "doc": "Integrity-check a copied SQLite file WITHOUT touching the directory it lives in.\n\n`immutable=1` is load-bearing, not a micro-optimisation. These DBs are written in WAL mode, so\na plain read-only open makes SQLite create `-wal` and `-shm` sidecars next to the file. Doing\nthat while verifying an archive adds two files per DB to the destination \u2014 548 extra files for\na 274-DB cell \u2014 which silently inflated the copy and broke the count check that follows.\n`immutable=1` promises the file cannot change, so SQLite skips the sidecars entirely.",
      "is_async": false,
      "kind": "function",
      "signature": "(db_path: str) -> str | None"
    },
    "scripts/archive_cells.py::_retry": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(fn, attempts: int=5, delay: float=2.0)"
    },
    "scripts/archive_cells.py::_sweep_sidecars": {
      "decorators": [],
      "doc": "Remove any -wal/-shm left beside a copied DB. Belt and braces behind immutable=1.\n\nA stray sidecar is not just clutter: it makes the archived copy differ from the original, and\non a later read SQLite would try to recover from a WAL that describes nothing.",
      "is_async": false,
      "kind": "function",
      "signature": "(root: str) -> int"
    },
    "scripts/archive_cells.py::_tree_stats": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(root: str, *, skip_sidecars: bool=False) -> tuple[int, int]"
    },
    "scripts/archive_cells.py::_verify_copy": {
      "decorators": [],
      "doc": "Every file present at the same size, and every SQLite file structurally intact.\n\nThe integrity pass is the reason this is safe to delete after: a truncated copy from a\ndisconnected external drive has the right name and the wrong contents.",
      "is_async": false,
      "kind": "function",
      "signature": "(src: str, dst: str, echo) -> list"
    },
    "scripts/archive_cells.py::analyse_cell": {
      "decorators": [],
      "doc": "Run this cell's analysis while it is still on fast storage.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str, workers: int, echo=print) -> bool"
    },
    "scripts/archive_cells.py::archivable": {
      "decorators": [],
      "doc": "Complete cells that are also safe to touch.\n\nCells run sequentially, so a cell is provably not the live one once a LATER cell has started.\nThe final cell only becomes safe when the run itself is over, which the caller asserts with\ninclude_last.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, layout: dict, mixed: bool, include_last: bool) -> list"
    },
    "scripts/archive_cells.py::archive_cell": {
      "decorators": [],
      "doc": "Copy one cell to bulk storage, verify it, then replace it with a junction.\n\nOrdering is the safety property: the source is only ever renamed aside AFTER a verified copy\nexists, and it is only deleted AFTER the junction is in place and proven to read. There is no\nwindow in which the cell path does not resolve -- which matters because the driver's resume\nprobe treats a missing directory as \"never ran\" and would re-simulate the whole cell.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str, cold: str, *, dry_run: bool=False, echo=print) -> dict"
    },
    "scripts/archive_cells.py::archive_keyframes": {
      "decorators": [],
      "doc": "Move only the keyframe DBs \u2014 the one family with no downstream reader.\n\nThis is what remains possible when the hot volume cannot hold a junction. Keyframes are ~19% of\na cell and are read by NOTHING in the analysis path: not run_analysis, not the rollup, not\neither what-if scan. The replay viewer is the only consumer and it already degrades to an empty\nstring when the file is absent. So they can simply leave, with no link and no path rewriting.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str, cold: str, *, dry_run: bool=False, echo=print) -> dict"
    },
    "scripts/archive_cells.py::cell_state": {
      "decorators": [],
      "doc": "(is_complete, why). Stricter than the driver's own resume probe.\n\nThe driver's `_cell_complete` accepts one sim_meta.json per pair, so a cell with two of eight\nleaves finalized reports complete. Archiving on that would move a half-written cell.",
      "is_async": false,
      "kind": "function",
      "signature": "(rt, cell: str, layout: dict, mixed: bool) -> tuple[bool, str]"
    },
    "scripts/archive_cells.py::cold_root": {
      "decorators": [],
      "doc": "The configured bulk-storage root, or None.\n\nNone rather than a fallback path on purpose: a silent fallback is how a run ends up writing\nhundreds of gigabytes somewhere nobody intended.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> str | None"
    },
    "scripts/archive_cells.py::copy_verified": {
      "decorators": [],
      "doc": "Copy a tree file-by-file, verifying each file as it lands. Resumable, and never destructive.\n\nshutil.copytree is all-or-nothing: an interrupted copy leaves a partial tree that the next\nattempt refuses to touch, and it verifies nothing. That is the wrong shape for a multi-hundred-\ngigabyte move onto an external disk, where a disconnect mid-copy is a routine failure rather\nthan an exceptional one.\n\nEach file is copied, size-checked, and (if SQLite) integrity-checked before the next one\nstarts, so an interruption leaves a tree where every file present is known-good. Re-running\nskips those and continues.",
      "is_async": false,
      "kind": "function",
      "signature": "(src_root: str, dst_root: str, *, resume: bool=True, echo=print) -> dict"
    },
    "scripts/archive_cells.py::evacuate": {
      "decorators": [],
      "doc": "Move a whole tree to bulk storage \u2014 for emptying a drive, not for mid-run archival.\n\nNo junction is left behind: this exists for the case where the source volume is about to be\nreformatted, so a link back to it would be pointless. That also means the caller is responsible\nfor repointing anything that referenced the old location.",
      "is_async": false,
      "kind": "function",
      "signature": "(src: str, dst: str, *, delete_source: bool=False, dry_run: bool=False, echo=print) -> dict"
    },
    "scripts/archive_cells.py::is_archived": {
      "decorators": [],
      "doc": "True if this path is already a junction (Python reports a junction as a plain dir).",
      "is_async": false,
      "kind": "function",
      "signature": "(cell_dir: str) -> bool"
    },
    "scripts/archive_cells.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "scripts/archive_cells.py::supports_reparse": {
      "decorators": [],
      "doc": "Can this volume hold a directory junction?\n\nexFAT cannot \u2014 it has no reparse points \u2014 and an external SSD formatted for portability is very\noften exFAT. Creating the junction is the only reliable test: the failure is a bare\nOSError(22, 'Incorrect function') from the FSCTL, and it leaves an empty directory behind that\nmust be cleaned up. Probing here costs milliseconds; discovering it after copying a hundred\ngigabytes does not.",
      "is_async": false,
      "kind": "function",
      "signature": "(directory: str) -> bool"
    },
    "scripts/archive_cells.py::sweep": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(run_dir: str, *, cold: str, workers: int, analyse: bool, dry_run: bool, include_last: bool, only: list | None, mode: str='auto', echo=print) -> list"
    },
    "scripts/new_experiment.py": {
      "decorators": [],
      "doc": "Scaffold a new docs experiment from the template.\n\nCopies docs/experiments/_template/ -> docs/experiments/<name>/, turning\nexperiment.yml.example into a starter experiment.yml. With --source it then runs the\ningest to auto-fill experiment.yml (from the run's run_manifest.json) and pull the files.\n\n    python scripts/new_experiment.py --name experiment-2\n    python scripts/new_experiment.py --name experiment-2 --source \"$COMPARISON_OUTPUT_DIR/comparison_20260801_120000\"\n\nThen: edit experiment.yml (title, short keys, winners), add the nav block it prints to\nmkdocs.yml, and `mkdocs serve`.",
      "kind": "module",
      "signature": null
    },
    "scripts/new_experiment.py::_EXPERIMENTS": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_REPO, 'docs', 'experiments')"
    },
    "scripts/new_experiment.py::_REPO": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.dirname(os.path.abspath(__file__)))"
    },
    "scripts/new_experiment.py::_TEMPLATE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.join(_EXPERIMENTS, '_template')"
    },
    "scripts/new_experiment.py::_nav_block": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(name, title)"
    },
    "scripts/new_experiment.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None)"
    },
    "scripts/prune_stale_analysis.py": {
      "decorators": [],
      "doc": "prune_stale_analysis.py \u2014 delete analysis output the CURRENT contract cannot have written.\n\nRe-analysing an existing run with newer code writes the new layout beside the old one: nothing\nin the pipeline removes a folder the current evaluations no longer produce, because the parent\npre-pass only wipes the tops it is told about.  A tree re-analysed across a layout change\ntherefore carries both \u2014 the live outputs and an orphaned copy of the previous suite, which\ncosts disk, confuses a reader browsing the tree, and shows up as a wall of `undeclared`\nfindings the next time anyone validates the run.\n\nThe rule this applies, in two steps so a tree LEVEL is never mistaken for an orphan:\n\n  PROTECT   every directory whose name a HEAD template spells literally (figures, tables, and\n            each family folder), plus all of its ancestors \u2014 an ancestor leads to live output\n            even though its own name is run-chosen and unknowable from the contract.\n  PRUNE     a directory that is not protected, whose parent is, AND which has at least one\n            protected SIBLING \u2014 so an orphan is removed only where its replacement is\n            demonstrably sitting beside it.\n\nThat sibling clause is the load-bearing one.  Without it, a stage that has not run yet (an\ninterrupted analysis, or a cell caught between the config and aggregate stages) presents its\nprevious output with no replacement in sight, and the tool would happily delete the only copy.\n\nHEAD, deliberately, not the run's own stamped contract \u2014 the question here is \"could the\nanalysis code that just ran have written this?\", and the run's stamped contract predates that\ncode (it is the contract the SIMULATION was written with, which is why validating a re-analysed\ntree against it reports the NEW files as undeclared).\n\nFiles are never touched, only whole directories.  The tool refuses to act on a tree that holds\nno current-layout output at all, because there every directory would look stale \u2014 that state\nmeans \"the analysis has not been re-run yet\", not \"delete everything\".  Dry-run is the default;\n`--apply` is required to delete.\n\n    python scripts/prune_stale_analysis.py <run_or_cell_dir>            # report only\n    python scripts/prune_stale_analysis.py <run_or_cell_dir> --apply    # delete",
      "kind": "module",
      "signature": null
    },
    "scripts/prune_stale_analysis.py::_HERE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(os.path.abspath(__file__))"
    },
    "scripts/prune_stale_analysis.py::_PLACEHOLDER": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('\\\\{\\\\w+\\\\??\\\\}')"
    },
    "scripts/prune_stale_analysis.py::_ROOT": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "os.path.dirname(_HERE)"
    },
    "scripts/prune_stale_analysis.py::_SKIP": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "{'_viz', '_frozen', '_runtime', '__pycache__'}"
    },
    "scripts/prune_stale_analysis.py::_protected": {
      "decorators": [],
      "doc": "Every directory that IS, or leads to, output the current contract declares.",
      "is_async": false,
      "kind": "function",
      "signature": "(base: str, known: set[str]) -> set[str]"
    },
    "scripts/prune_stale_analysis.py::_size_mb": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(path: str) -> float"
    },
    "scripts/prune_stale_analysis.py::declared_dir_names": {
      "decorators": [],
      "doc": "Every literal directory-segment name any HEAD template can render.\n\nA segment that is entirely a placeholder renders to a run-chosen name (a cell, a pair) and\ntells us nothing; a segment carrying literal text is a name the contract owns.",
      "is_async": false,
      "kind": "function",
      "signature": "(doc: dict) -> set[str]"
    },
    "scripts/prune_stale_analysis.py::find_stale": {
      "decorators": [],
      "doc": "Stale subtree ROOTS under `base`, or [] \u2014 raising when the tree has no live output.",
      "is_async": false,
      "kind": "function",
      "signature": "(base: str) -> list[str]"
    },
    "scripts/prune_stale_analysis.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv: list[str]) -> int"
    },
    "scripts/schema_report.py": {
      "decorators": [],
      "doc": "schema_report \u2014 the CLI over `Schema/compat.py`, and the one place that knows every family.\n\n    python scripts/schema_report.py --report                     # the surface, per family\n    python scripts/schema_report.py --capture sim_db <file.db>   # commit a historical shape\n\nWHY THIS IS NOT IN `Schema/`\n----------------------------\n`Schema/` is the stdlib-only leaf every layer may import, and `context/architecture.yml` forbids\nit from importing `opt_persistence`, `optimization`, `generation`, `evaluations`, `visualization`\nand `warehouse_core`. But a family exists only once its WRITER's module has been imported, so\nanything that reports on *all* families must import all of them \u2014 which is exactly what the\n`schema` layer may not do.\n\nThat import list lived in `Schema/compat.py` briefly, behind `importlib.import_module` on string\nliterals. It worked, and it was wrong twice over:\n\n  * `context/arch/extract.py` only visits `ast.Import` / `ast.ImportFrom`, so a string-literal\n    dynamic import records NO edge. `verify_architecture.py` reported OK on a real violation \u2014\n    the gate was green because it could not see, not because the import was legal. (A plain\n    `from Optimization.persistence import Picking_Data` in the same function WOULD have produced\n    the edge and failed the boundary.)\n  * it made the leaf heavy in fact if not in form: the call pulls ~1350 modules, matplotlib,\n    pandas, numpy and scipy included, into the package whose own README advertises \"stdlib-only,\n    imports nothing else in the repo, so every layer may use it\". `identity.check_tables` exists\n    precisely to stop `Warehouse/catalog/Affinity_Store.py` dragging a data-gen CLI into every\n    simulation worker; this would have re-opened that door one level down.\n\nThere is also a genuine cycle: `Optimization/persistence/Picking_Data.py` imports `Schema.compat`\nat module scope, and the family list imports `Picking_Data` back. Deferring it inside a function\nsurvives, but only by accident of ordering. Here, in the `scripts` layer, importing downward is\nsimply legal and the cycle does not exist.\n\n`Schema/compat.py` stays a pure library: it takes a family NAME and reads the committed shape\nstore, and never learns who writes what.",
      "kind": "module",
      "signature": null
    },
    "scripts/schema_report.py::FAMILY_MODULES": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "('Optimization.persistence.Picking_Data', 'Optimization.persistence.Warehouse_Data', 'Optimization.persistence.runtime_metrics', 'Visualization.cache_schema', 'Warehouse.generation.generate_invento..."
    },
    "scripts/schema_report.py::_KNOWN_IDS_RE": {
      "decorators": [],
      "doc": null,
      "kind": "const",
      "signature": null,
      "value_preview": "re.compile('known_ids\\\\s*=\\\\s*\\\\((?P<body>[^)]*)\\\\)', re.S)"
    },
    "scripts/schema_report.py::_apply_known_ids": {
      "decorators": [],
      "doc": "Write the `known_ids` adoption edit into the family's own source file.\n\nThe same bargain as `preflight --apply` (which inserts observed ARTIFACTS entries): the\nMECHANICAL half is automated, the JUDGEMENT half is left loud \u2014 every inserted id carries a\n`TODO(schema-adopt)` comment the human replaces with the commit window it covers.\n\nSafety: locate the file via `inspect.getsourcefile(fam.declared_shape)` (the declaration and\nthe registration live together, by convention and by test); refuse on zero or multiple\n`known_ids=` matches inside the family's registration block; `ast.parse` the edited source\nbefore writing a byte.",
      "is_async": false,
      "kind": "function",
      "signature": "(family_name: str, orphans: list, out=print) -> bool"
    },
    "scripts/schema_report.py::accept": {
      "decorators": [],
      "doc": "`adopt --apply` then `sync`: the one-command close of a DDL change.\n\nOrder is load-bearing: `--apply` EDITS a DDL source file, so the fingerprint must be recorded\nAFTER it (a sync-first ordering would record a fingerprint the apply immediately staled).\nExit is `adopt`'s exit - 1 means something was adopted and the TODO comments now need filling.",
      "is_async": false,
      "kind": "function",
      "signature": "(out=print) -> int"
    },
    "scripts/schema_report.py::adopt": {
      "decorators": [],
      "doc": "Report every committed shape no family vets any more - the outgoing shapes to adopt.\n\nExit 1 when something needed adopting, so a hook or CI can catch a DDL change that shipped\nwithout its `known_ids` entry.  With `apply`, the tuple edit is WRITTEN into the family's\nsource (TODO-marked); the human keeps the commit-window comment and the commit itself.",
      "is_async": false,
      "kind": "function",
      "signature": "(out=print, apply: bool=False) -> int"
    },
    "scripts/schema_report.py::capture": {
      "decorators": [],
      "doc": "Commit the shape of a real file of a vetted historical vintage.",
      "is_async": false,
      "kind": "function",
      "signature": "(family: str, path: str, note: str='', out=print) -> int"
    },
    "scripts/schema_report.py::import_families": {
      "decorators": [],
      "doc": "Populate `Schema.identity`'s registry by importing every writer.",
      "is_async": false,
      "kind": "function",
      "signature": "() -> None"
    },
    "scripts/schema_report.py::main": {
      "decorators": [],
      "doc": null,
      "is_async": false,
      "kind": "function",
      "signature": "(argv=None) -> int"
    },
    "scripts/schema_report.py::report": {
      "decorators": [],
      "doc": "Print each family's guaranteed/conditional surface. Exit 1 if any id is unrecoverable.",
      "is_async": false,
      "kind": "function",
      "signature": "(out=print) -> int"
    },
    "scripts/schema_report.py::sync": {
      "decorators": [],
      "doc": "Commit the DECLARED shape of every family that has not been committed yet. Idempotent.\n\nRun this BEFORE changing any DDL \u2014 and it is safe to run always, which is the point. A\ndeclared shape is re-derivable today and gone tomorrow; committing it is what makes the next\nchange adoptable without an archive.",
      "is_async": false,
      "kind": "function",
      "signature": "(out=print) -> int"
    }
  },
  "version": 1
}
